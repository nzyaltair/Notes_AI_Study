# 监督微调

## 1. 概述

监督微调（Supervised Fine-Tuning, SFT）是大语言模型训练流水线中连接预训练与偏好对齐的关键阶段。预训练模型只能"续写文本"，SFT通过在高质量指令-回答对数据上训练，使模型从"续写"转变为"理解并执行人类指令"。

InstructGPT首先确立了SFT在对齐流程中的地位，Alpaca证明了Self-Instruct生成的数据可以有效微调开源模型，LLaMA 2/3、Qwen等模型均将SFT作为指令跟随能力的主要来源。

SFT的核心原则是**数据质量远比数量重要**——少量高质量数据的训练效果优于大量低质量数据。

## 2. 发展历史

| 时间 | 里程碑 | 核心贡献 |
|------|--------|---------|
| 2021 | FLAN | 多任务指令微调的开创性尝试（拼合现有 NLP 数据集） |
| 2022 | InstructGPT | 确立SFT在RLHF三阶段流程中的地位 |
| 2023 | Alpaca（52K） | Self-Instruct数据可有效微调开源模型 |
| 2023 | Vicuna / ShareGPT | 真实多轮对话数据微调 |
| 2023 | OpenAssistant | 大规模众包人工数据（类维基百科模式） |
| 2023 | LLaMA 2 | 百万级SFT数据，多轮对话 |
| 2023 | UltraChat（1.5M） | 大规模合成多轮对话 |
| 2024 | Qwen 2 / LLaMA 3 | 多语言SFT + 严格数据质量控制 |
| 2024 | 拒绝采样微调（RFT） | 从模型自身采样中筛选高质量数据 |
| 2025 | Agentic SFT（Nemotron 等） | 工具调用轨迹成为 SFT 数据主力 |

### 2.1 SFT 数据演化脉络

开源社区对"SFT 数据该长什么样"的理解经历了六代演进，每代都修正上一代的一个错误假设：

| 代际 | 代表 | 思路 | 教训 |
|------|------|------|------|
| ① 多任务 NLP 数据集 | FLAN (2021) | 把现有 NLP 基准（Enron 邮件主题预测、CNN/DM 摘要）转成指令数据 | 结构生硬（输入→程序化输出，像在跟基准测试对话）；继承原数据集质量问题与幻觉摘要；以为要靠规模取胜，实际小而精即可 |
| ② 模型自举 | Self-Instruct (2023) | 让模型自己生成指令-回复对 | 模型即数据生成器，数据质量随模型能力水涨船高 |
| ③ 对闭源模型蒸馏 | Alpaca / Vicuna (2023) | 蒸馏 ChatGPT 交互记录 | 对话式数据能稳定激发类 ChatGPT 行为——"做出聊天模型没那么难"的关键节点（细节做对仍难） |
| ④ 众包人工数据 | OpenAssistant (2023) | 类维基百科式大规模志愿者写 prompt+回复 | 方向值得称赞但最终停滞；专家回复同时教知识与格式，可能引入幻觉（见 3.5） |
| ⑤ 复杂合成指令 | WizardLM / Evol-Instruct (2023) | 用 LM 迭代进化出越来越复杂的指令 | 指令复杂度可系统化提升 |
| ⑥ Agentic / 工具使用 | Nemotron 等 (2024-2025) | SFT 数据转向工具调用、待办清单、多步执行轨迹 | 重点从"聊天"转向"agent 与工具使用" |

**三次高层次转变**：① 从程序化 NLP 输出到"聊天感"（详细、拟人的回复）；② 从廉价众包到高质量/专家标注员；③ 从纯文本回复到工具调用与 agent 接口。

## 3. 核心概念

### 3.1 指令跟随

SFT的核心目标是让模型学会"指令跟随"——理解用户的自然语言指令，并生成符合格式和内容要求的回答。这不同于预训练的"续写"行为。

### 3.2 数据质量原则

- **质量 > 数量**：LIMA研究表明，1000条精心标注的数据即可训练出有效的指令跟随模型
- **多样性**：覆盖多种任务类型、难度级别和领域
- **真实性**：反映实际用户需求，包含边缘情况
- **"提取"视角**：SFT 的理想情况是提取预训练中已隐含的行为——把正确模式从模型里激发出来，少量高质量数据即可（安全微调约 500 条样本即可大幅降低恶意指令遵循率）。反例是往里堆模型不具备的尾部长尾知识，反而制造幻觉。判断"什么已在预训练里"并不精确：可证伪的是反面（如对罕见编程语言的泛化用 SFT 做不到），"安全行为已隐含在预训练中"只是合理假设而非已证结论

### 3.3 损失函数

SFT使用标准交叉熵损失，但**仅计算模型对目标响应部分的预测损失**，输入部分（指令）不参与损失计算：

$$\mathcal{L} = -\frac{1}{|y|} \sum_{t=1}^{|y|} \log P(y_t \mid x, y_{<t}; \theta)$$

其中 $x$ 为输入指令，$y$ 为目标响应。输入部分的标签设为-100（PyTorch中被忽略）。

是否对 prompt 计算损失是实现选择：SFT 通常只对响应计算损失，但也有方案对 prompt 一并预测；当指令数据被混入预训练退火阶段（见 [[16_后训练范式与推理训练]]）时则是纯预训练形式，prompt 也参与预测。

### 3.4 风格与能力解耦

后训练数据对模型的影响可分解为两个近乎独立的轴：

- **风格轴**：回复长度、项目符号列表、语气（"Claude 的语调"vs"ChatGPT 话太多"）——由数据收集团队刻意选择，占 post-training 工作量很大比重
- **能力轴**：标准基准测试（知识、推理）上的真实水平

**实证现象**：用不同后训练数据集训练同一基座，偏好评测（人类判断/Arena 风格）差异巨大，但标准基准分数几乎不动——模型没有变得更聪明，只是风格更讨喜。

推论：

- **参与度指标会骗人**：盯着用户参与度优化，很容易自以为拿到了更好的数据，实际能力毫无变化——人类天然偏好带列表、更长、更详细的回答
- **偏好评测必须控制风格混杂**：长度、列表、格式不控制，测到的是风格而非能力（LLM-as-a-Judge 的长度偏差分析见 [[../../K_AI工程化/07_AI评测体系/00_AI评测体系_综述|AI评测体系]]）
- **仅对回复长度做偏好优化即可在许多基准上刷高分**——长度偏差是偏好学习与模型标注最顽固的混杂变量

### 3.5 知识与格式的双重教学：SFT 幻觉机制

SFT 的每条数据都在同时教模型两件相对独立的事：

```text
一条带参考文献的回复
├── 教知识：下一 token 预测记住引用内容本身
└── 教格式："回复时应附上参考文献"的输出模式

问题：格式是现成的，知识模型未必知道
  → 强迫模型输出它不知道的知识 = 教它幻觉
  → 模型学会的不是"引用正确的文献"而是"总要引用点什么"
```

- **尾部知识尤其有害**：冷门事实 + 引用标记 → 模型凭空捏造参考文献
- **SFT 无法教会"知道自己不知道"**：SFT 是密集的教师监督——只要输出偏离参考序列就被惩罚，但它同时教模板与知识两个混杂信号，模板泛化错了无从阻止。教会模型区分"知道/不知道"必须依赖模型自身策略（policy-dependent）的信号，这正是 RL 存在的理由之一（John Schulman 的论点）
- **RL 也只在模型"内部知道"时有效**：若模型内部存在"我知道"的激活信号，RL 能把它提取到输出策略（输出参考文献时奖励高、输出"我不知道"时奖励低，模型学会泛化识别该信号）；若模型在哪个层面都不知道，RL 也无济于事——它解决不了校准问题

## 4. 技术原理

### 4.1 数据格式

**Alpaca格式**：

```json
{"instruction": "翻译以下句子", "input": "Hello world", "output": "你好世界"}
```

**ChatML格式**（Qwen等）：

```
<|im_start|>system
你是一个有帮助的助手。<|im_end|>
<|im_start|>user
你好<|im_end|>
<|im_start|>assistant
你好！有什么可以帮助你的？<|im_end|>
```

**多轮对话格式**：包含多轮交互的对话历史，每轮交替user/assistant角色。

**Agent / 工具调用格式**（Nemotron 等）：assistant 字段中回复文本与结构化工具调用并行/交错出现，可包含待办清单（todo list）——类似 Claude Code / Codex 一边执行一边打勾的行为模式，通过监督方式显式训练进模型。

### 4.2 数据集构建原则

1. **多样性**：覆盖多种任务类型（生成、分类、摘要、翻译、推理、编码）、难度级别和领域
2. **高质量**：指令清晰、响应准确完整、语法正确
3. **难度梯度**：从简单到复杂、从具体到抽象
4. **真实场景**：反映实际用户需求，包含边缘情况和对抗性输入
5. **格式统一**：确保所有数据遵循统一的对话模板

### 4.3 主流数据集

| 数据集 | 规模 | 特点 | 来源 |
|-------|------|------|------|
| Alpaca | 52K | Self-Instruct生成 | text-davinci-003 |
| ShareGPT | 90K+ | 真实多轮对话 | 用户分享 |
| UltraChat | 1.5M | 大规模多轮对话 | 模型生成 |
| Dolly | 15K | 人工生成 | Databricks员工 |
| LIMA | 1K | 精选高质量 | 人工标注 |
| OpenOrca | 4M+ | GPT-4生成 | 指令扩展 |

### 4.4 训练策略

**全参数微调**：微调所有参数，性能潜力高，适合小模型或高质量数据集。计算资源需求大，存在灾难性遗忘风险。

**参数高效微调**：仅训练少量参数（[[11_LoRA|LoRA]]/[[12_QLoRA|QLoRA]]/[[13_Adapter|Adapter]]），内存效率高，训练速度快，适合大模型。

### 4.5 关键超参数

| 参数 | 常见范围 | 说明 |
|------|---------|------|
| 学习率 | 1e-5 ~ 5e-5 | 低于预训练学习率 |
| Warmup比例 | 0.03 ~ 0.1 | 防止训练初期不稳定 |
| Batch Size | 56-1024 | 含梯度累积的总批次 |
| 序列长度 | 1024-32768 | 根据任务和硬件调整 |
| 训练轮次 | 3-10 | 通常2-5轮即可 |
| Weight Decay | 0.01 | 防止过拟合 |

### 4.6 拒绝采样微调（RFT）

拒绝采样微调（Rejection Sampling Fine-Tuning）是一种迭代数据增强方法：

1. 用SFT模型对同一指令生成多个候选回答
2. 用奖励模型或规则筛选高质量回答
3. 将筛选后的数据加入训练集
4. 重新训练模型

这种方法可以让模型从自身的高质量输出中学习，逐步提升性能。

### 4.7 灾难性遗忘的应对

- 使用较小学习率
- 限制训练步数和轮次
- 采用参数高效微调（冻结大部分参数）
- 在训练数据中混入少量预训练数据

## 5. 关键方法/模型

### 5.1 数据生成方法

| 方法 | 原理 | 代表 |
|------|------|------|
| Self-Instruct | 让模型生成自己的训练数据 | Alpaca |
| Evol-Instruct | 迭代优化指令复杂度 | WizardLM |
| 拒绝采样（RFT） | 从模型输出中筛选高质量数据 | LLaMA 2 |

### 5.2 微调方法

| 方法 | 特点 | 适用场景 |
|------|------|---------|
| 全参数微调 | 性能最优，资源需求大 | 小模型/高质量数据 |
| [[11_LoRA|LoRA]] | 参数量~1%，可合并 | 大模型通用微调 |
| [[12_QLoRA|QLoRA]] | 4-bit量化+LoRA | 单卡微调70B |

### 5.3 重要论文

| 论文 | 作者 | 年份 | 核心贡献 |
|------|------|------|---------|
| InstructGPT | Ouyang et al. | 2022 | SFT在RLHF中的地位 |
| Self-Instruct | Wang et al. | 2023 | 模型自举生成指令数据 |
| LIMA | Zhou et al. | 2023 | 1000条数据即可对齐 |
| Alpaca | Taori et al. | 2023 | 开源SFT民主化 |

## 6. 优势与局限

### 优势

- **流程简洁**：标准的监督学习，易于实现和调试
- **效果显著**：少量高质量数据即可显著提升指令跟随能力
- **灵活性强**：可通过PEFT方法适配不同规模模型
- **可控性好**：数据质量直接决定效果，可针对性优化

### 局限

- **数据质量依赖**：低质量数据会导致模型学习错误模式
- **教出幻觉**：SFT 同时教知识与格式，强迫模型输出它不知道的知识会诱发捏造（见 3.5）
- **灾难性遗忘**：微调可能损害预训练获得的能力
- **多样性不足**：数据覆盖面不够会导致模型在某些任务上表现差
- **无法学习偏好**：SFT只学习"正确答案"，无法区分"好"与"更好"

## 7. 应用场景

- **指令跟随**：让模型理解并执行各类指令
- **领域适配**：医疗、法律、金融等垂直领域微调
- **多语言能力**：通过多语言SFT数据增强跨语言能力
- **多模态扩展**：结合视觉/音频指令的多模态SFT
- **代码能力**：用代码数据SFT增强编程能力

## 8. 与其他技术关系

- SFT是[[01_预训练流程|预训练流程]]之后的必要阶段
- SFT模型作为[[05_RLHF|RLHF]]/[[06_DPO|DPO]]的初始策略（参考模型）
- 参数高效微调方法（[[11_LoRA|LoRA]]、[[12_QLoRA|QLoRA]]）是SFT的主流实现
- SFT数据质量依赖[[02_数据收集与清洗|数据收集与清洗]]的方法论
- SFT是[[04_偏好对齐方法|偏好对齐方法]]的前置步骤

## 9. 前沿发展

### 9.1 数据质量优化

- **LIMA范式**：极少量精选数据（~1000条）即可实现有效对齐
- **数据去重与多样性分析**：确保SFT数据集的覆盖面和平衡性
- **自动质量评估**：用模型自动评估SFT数据质量

### 9.2 迭代微调

- **拒绝采样微调（RFT）**：从模型自身输出中学习
- **迭代SFT**：SFT → 生成数据 → 筛选 → 再SFT的循环
- **在线SFT**：实时从用户反馈中学习

### 9.3 合成数据

- **GPT-4级别数据蒸馏**：用强模型生成高质量SFT数据
- **指令进化**：Evol-Instruct等方法逐步增加指令复杂度
- **多模态合成**：生成图文对话数据用于多模态SFT

### 9.4 Agentic SFT

用户需求从纯聊天界面转向完整 agent 系统，新一代 SFT 流水线专门生成工具使用与 agentic 数据（工具调用、待办清单、多步执行轨迹）。闭源厂商则在互相蒸馏的同时继续收集人类数据，且数据收集方式本身是竞争关键——相关信息是商业机密，公开可参考的细节集中在 ChatGPT 竞争白热化之前（如 InstructGPT 附录的标注指南、Anthropic HH 论文的详细标注流程）。

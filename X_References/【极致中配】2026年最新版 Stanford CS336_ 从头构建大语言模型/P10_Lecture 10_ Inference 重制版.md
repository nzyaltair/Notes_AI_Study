---
title: "【极致中配】2026年最新版 Stanford CS336: 从头构建大语言模型"
url: "https://www.bilibili.com/video/BV11LEA6eEuj/?p=10"
bvid: "BV11LEA6eEuj"
cid: "39163134572"
author: "常青藤名校课"
upload_date: "2026-06-05"
created: 2026-08-17
tags: ["bilibili", "clippings"]
---

## 简介

如果这个中文配音版对您理解 CS336 有帮助，欢迎点赞、投币、收藏支持一下，也可以点个关注。
我会持续更新更多 Stanford / AI / 大模型相关课程的中文配音课程，尽量帮大家用更低的理解成本看懂这些高质量课程。你的支持会让我更有动力把后续内容继续做完、做好。

## 字幕

`00:00:00` 好咱们开始
`00:00:01` 上次他就讲了skin los
`00:00:03` 咱们先放一放
`00:00:04` 聊聊inference
`00:00:05` 所以inference问题其实很简单
`00:00:08` 你已经训练好了一个模型
`00:00:10` 你输入一个prompt
`00:00:11` 然后通常希望模型尽可能准确快速的给出回复
`00:00:16` 所以inference虽然只有一节课
`00:00:18` 但他的地位其实越来越重要
`00:00:20` and啊它的应用场景很多
`00:00:22` 显然模型训练完你总不能干坐着啊
`00:00:25` 啊如果你是做研究的
`00:00:27` 你会在论文里放张图啊
`00:00:29` 说明你的模型是怎么跑的啊
`00:00:32` 对对大多数人来说
`00:00:33` 你是真想把这个模型用起来
`00:00:35` 那具体怎么用呢
`00:00:37` 嗯比如跟AI助手或插包聊天
`00:00:40` 或者用它做代码补全啊
`00:00:43` 现在agent很火
`00:00:45` 这就需要用到inference
`00:00:47` 还有批量数据处理啊
`00:00:49` 这对评估也很有用
`00:00:50` 尤其是那种需要靠生成来做的评估
`00:00:54` 训练里面也会用到
`00:00:56` 要是你再做reinforcement learning
`00:00:58` 就得有个模型先生成roll outs
`00:01:00` 然后给他们打分
`00:01:01` 再相应的更新权重
`00:01:03` 所以inference到处都用得到
`00:01:05` 而且效率这事儿嗯
`00:01:08` 作为咱们这门课的主题
`00:01:09` 它真的比以往任何时候都更重要啊
`00:01:13` 哪怕咱们只从实际应用的角度来看
`00:01:17` 训练是一次性的
`00:01:18` 成本可能非常高
`00:01:20` 确实非常高
`00:01:21` 但做完也就完了
`00:01:22` 但inference是反复产生的成本
`00:01:25` 你基本每天都得为他买单
`00:01:27` 据估计
`00:01:28` OpenAI每天产生IPHONE6000000000000个token啊
`00:01:32` 作为参考
`00:01:33` 今年早些时候发布的deep seek v four
`00:01:35` 其训练数据量为32万亿个token
`00:01:38` 因此在不到四天的时间里
`00:01:40` OpenAI需要生成的token数量及其对应的计算量
`00:01:44` 至少相当于训练一个deep sv four
`00:01:47` 当然即使是前沿模型
`00:01:50` 其训练所用的token数量啊可能更多
`00:01:55` 但即便如此
`00:01:56` inference阶段生成的token数量仍然会更高
`00:01:59` 这只是个背景参考
`00:02:01` 而且我觉得过去这一年inference的重要性上升了
`00:02:05` 因为以前我们觉得语言模型主要就是个chat
`00:02:08` bot或者助手呃
`00:02:10` 你输入一个prompt
`00:02:12` 它返回一个回复
`00:02:13` 而人类的目标大概就是啊读这个回复
`00:02:17` 但现在随着我们越来越进入ERGENTIC的世界
`00:02:22` 情况变了
`00:02:23` 你发起一个查询
`00:02:25` agent就会去做一堆事
`00:02:27` 他会思考
`00:02:28` 会推理
`00:02:29` 会调用工具
`00:02:30` 还会自我检查
`00:02:31` 最后才生成结果
`00:02:33` 让人类来看
`00:02:34` 所以你看agent生成的那些token
`00:02:37` 大部分其实都不是给人读的
`00:02:40` 所以你得真正把生成的token数量
`00:02:43` 看作是花掉的算力
`00:02:45` 这其实没什么上限
`00:02:47` 如果你要解决的问题足够宏大
`00:02:50` 那就需要投入很多算力和大量token
`00:02:54` 所以如果你做的是chat out inference
`00:02:58` 到了某个点之后可能就足够快了
`00:03:01` 因为人的阅读速度就那么快
`00:03:03` 但对agent来说
`00:03:04` 你从inference里多压榨出一点性能
`00:03:07` 能带来的价值是没有上限的啊
`00:03:10` 谢谢嗯
`00:03:11` 现在在商业这边有很多人在做inference
`00:03:15` 当然所有闭源API提供商
`00:03:17` 都得为自己的模型提供服务
`00:03:19` 所以inference对他们来说是件大事啊
`00:03:22` 另外还有一批提供商啊
`00:03:24` 专门服务开源权重模型也提供inference服务
`00:03:29` 所以via and em算是个很火的选择
`00:03:31` 也算是首选
`00:03:32` 还有SGL啊
`00:03:34` 另一个在IJTIC工作负载上
`00:03:36` 表现特别好的是SG蓝
`00:03:38` 不过可能还没那么火
`00:03:40` 然后是NVIDIA的tensor RT
`00:03:42` 速度非常快
`00:03:43` 不过适用范围相对窄一些
`00:03:46` 还有lama cpp
`00:03:47` 如果你想在CPU上做inference
`00:03:49` 这是个挺流行的包
`00:03:50` 好希望我刚才已经说明了inference有多重要啊
`00:03:55` 如果你能让influence快一倍
`00:03:57` 哪怕只快十倍甚
`00:03:59` 那都是件大事
`00:04:01` 那现在问题来了
`00:04:02` 到底什么是快
`00:04:03` 有几个指标能衡量快
`00:04:06` 它们适用于不同场景
`00:04:07` 各有取舍
`00:04:09` 所以衡量快的一个指标是手token时间TTF
`00:04:14` 这基本上就是用户在开始生成任何内容之前
`00:04:17` 要等的时长
`00:04:19` 比如你把查询发给拆GBT过几毫秒
`00:04:22` 从第一个token出来的那一刻算起
`00:04:25` 这就是时间被对交互式应用通常很有用
`00:04:30` 因为那种你干等着什么也做不了的
`00:04:33` 延迟越长
`00:04:33` 用户体验就越差
`00:04:35` 等token1出来
`00:04:36` 速度倒未必需要那么快
`00:04:38` 反正你都要读
`00:04:39` 而你也读不了那么快
`00:04:41` 谢谢所以第二个指标是延迟
`00:04:43` 咱们从单个用户的角度看一次查询里
`00:04:46` token出来的速度有多快
`00:04:48` 这对交互式应用也很重要
`00:04:50` 基本上这就是看token流式输出的速度有多快
`00:04:54` 然后是第二个相关概念
`00:04:56` 吞吐量
`00:04:57` 也就是每秒能处理多少个token
`00:05:01` 这是指面对多个查询时
`00:05:03` token出来的速度
`00:05:05` I延迟
`00:05:07` 或者说延迟的倒数和吞吐量显然关系密切啊
`00:05:12` 他们其实挺一致的
`00:05:13` 总体来说
`00:05:14` 很多干预手段都能同时改善延迟和吞吐量
`00:05:18` 不过咱们后面会看到这里其实有个权衡取舍
`00:05:21` 而且如果你再做大量的批量处理
`00:05:24` 吞吐量就很有用嗯
`00:05:26` may交的有KB级的数据啊
`00:05:29` 想用语言模型来处理它嗯
`00:05:32` 你只想把这个任务干完
`00:05:34` 单个查询是快是慢
`00:05:36` 先来后到都无所谓好
`00:05:39` 那什么决定了效率呢
`00:05:41` 也就是用推理的这些指标里的任何一个
`00:05:45` 来衡量啊
`00:05:46` 那么高层面的要点是这样的
`00:05:49` 这大概是这节课要记住的一个高层面要点
`00:05:52` 训练时你能一次性看到所有token
`00:05:55` 因为在监督微调中
`00:05:57` 你可以看到全部token
`00:05:58` 并在序列维度上做并行计算
`00:06:01` 大家记住
`00:06:01` 想一想
`00:06:02` transformer里
`00:06:02` 注意力机制和MLP的计算序列
`00:06:05` 只是其中一个维度
`00:06:06` 所以它是一个参与相乘的大张量
`00:06:08` 因此你本质上是一次性处理所有token
`00:06:11` 但在inference阶段
`00:06:13` 这种做法行不通
`00:06:14` 由于inference的自回归特性
`00:06:16` 我们必须按顺序一次一个D生成token
`00:06:20` 这就是为什么
`00:06:21` influence的工作负载和训练有本质区别
`00:06:24` 你没法在序列维度上做并行
`00:06:27` 所以正如我们稍后会看到的
`00:06:29` 这会让高算术强度更难实现
`00:06:32` 也很难充分利用计算资源
`00:06:34` 嗯好的
`00:06:35` 咱们接着往下讲
`00:06:36` 首先我先给大家简单预告一下这次课要讲什么
`00:06:41` 第一步我会做一些数学推导来理解算术强度
`00:06:45` 吞吐量和延迟
`00:06:47` 以及怎么针对transformer来思考这些问题啊
`00:06:51` 接下来我会介绍几种降低成本的技术
`00:06:54` 比如缩小kv cash模型
`00:06:57` 量化和减脂
`00:06:58` 最后我会讲一讲推测解码
`00:07:00` Speculative decoding
`00:07:01` 以及一些实际应用中需要注意的问题啊
`00:07:04` 谢谢谢谢
`00:07:05` 好
`00:07:05` 这部分算是复习一下
`00:07:07` 主要是把符号统一起来
`00:07:08` 确保咱们对记号的理解是一致的
`00:07:12` 这次课的内容很多都参考了google那本
`00:07:15` 关于transformer和inference的scaling book
`00:07:18` 所以我很推荐大家去看看
`00:07:21` 那本书写的非常好
`00:07:22` 里面有些图我也直接借用过来了啊
`00:07:26` 为了看懂这张图
`00:07:27` 咱们先定一些符号
`00:07:29` 这里会用一些符号按irn UPS来理解
`00:07:32` 用来表示维度啊
`00:07:33` 同时也表示啊它们的长度
`00:07:36` 所以B是batch维度
`00:07:38` 也就是序列的数量
`00:07:40` D是序列维度
`00:07:41` 也就是token的数量
`00:07:42` B是模型维度啊
`00:07:44` H啊是head维度好
`00:07:48` 如果我这么写
`00:07:49` 这就是把一个维度为BTD的tensor
`00:07:52` 跟另一个维度为DH的matrix做乘积
`00:07:56` 得到的结果是B按TH这里的情况是
`00:08:00` 有些维度被标成了红色
`00:08:03` 这些维度会被收缩掉
`00:08:05` 它们同时出现在两个操作数里
`00:08:07` 却从结果中消失了
`00:08:09` 另外还有些常规的黑色维度
`00:08:11` 它们只在一个操作数里出现
`00:08:14` 然后保留在结果中
`00:08:16` 所这是一般情况
`00:08:17` 还有一种情况
`00:08:18` 如果你有蓝色维度
`00:08:19` 那说明他们是P处理维度
`00:08:22` 既出现在两个操作数
`00:08:24` 理也保留在结果中
`00:08:26` 它们不会被收缩或缩减好
`00:08:29` 所以这里的含义是
`00:08:30` 就像我们后面会看到的
`00:08:31` 对每个序列和token位置
`00:08:33` 我们都有这样一个tensor
`00:08:35` 里面是一个向量
`00:08:36` 你想把它乘以一个matrix
`00:08:38` 然后啊
`00:08:39` 这里你基本上就是在两个tensor之间
`00:08:42` 做一系列点击运算好
`00:08:44` 有了这个概念
`00:08:45` 这就是transformer模块完整形态的描述啊
`00:08:50` 所以它看起来有点像电路图
`00:08:53` 这里细节很多
`00:08:54` 但我觉得这可能是对transformer是什么
`00:08:57` 最清晰的定义了啊
`00:08:59` 你会看到很多关于transformer的描述
`00:09:02` 坦白说
`00:09:03` 其中很多都让人很难真正理解到底发生了什么
`00:09:07` 这个版本基本上会告诉你张亮的确切形状
`00:09:10` 这样你就能理清它们之间的依赖关系了
`00:09:13` 所以啊对于一个transformer模块
`00:09:16` 你会先取出X也就是某一层的激活值
`00:09:19` 把它输入注意力机制
`00:09:21` 然后再输入MLP
`00:09:23` 简单复习一下
`00:09:24` X是用查询矩阵键
`00:09:26` 矩阵和值矩阵投影出来的
`00:09:29` 你看这里我们有啊啊N代表头的数量
`00:09:33` H是每个头的维度
`00:09:36` 所以查询矩阵的形状就是batch
`00:09:39` chinese sequence乘以头的数量乘以每个头的维度
`00:09:42` 而对于K和V我们不用N而是用K
`00:09:47` 这个我们待会儿会看到它对应的key value
`00:09:50` HEA数量可能更少
`00:09:52` 接下来是注意力操作
`00:09:54` 注意这里涉及批处理维度
`00:09:56` 所以B在两处都出现
`00:09:58` 我们在head维度上做收缩
`00:10:00` 咱们稍后再回过头看
`00:10:02` 为什么B同时出现在两处
`00:10:05` 会让inference变得困难
`00:10:07` 以及为什么attention会成为一个瓶颈
`00:10:11` 我想先把这个想法记住
`00:10:12` mp其实特别简单
`00:10:14` 直接嗯啊你只需要做点矩阵乘法
`00:10:18` 就是门控部分
`00:10:19` 这是个上投影矩阵
`00:10:21` 还有个下投影矩阵
`00:10:22` 我觉得你们之前都实现过这个
`00:10:24` 所以我不打算细讲
`00:10:26` 只是换一下符号表示
`00:10:27` 按惯例
`00:10:28` 咱们假设mp会把呃地位的模型维度扩展到四倍
`00:10:33` 所以以后看到F分
`00:10:35` 你就把它当成4D来看
`00:10:37` 然后模型维度会被拆分到N个head上
`00:10:41` 所以啊模型维度永远等于head的数量
`00:10:44` 乘以每个head维度
`00:10:46` 以分组查询注意力
`00:10:48` group query attention为例
`00:10:49` head的总数会拆分成组数和每组的head数
`00:10:53` 最后我们有S和T两个变量
`00:10:56` 它们都表示序列维度
`00:10:57` Reduce
`00:10:58` 两者的区别在于
`00:11:00` S指的是需要生成的输入token数量的
`00:11:04` T则代表输出的token数量
`00:11:06` 所以在训练阶段
`00:11:07` 这两个值是一样的
`00:11:08` 因为模型是把所有输入直接当做输出来
`00:11:11` 预测到了inference阶段
`00:11:13` T固定为one rs din输入部分
`00:11:16` 谢谢好
`00:11:17` 咱们再来回顾一下算术强度
`00:11:19` Ariarithmetic intensity
`00:11:21` 这我在第二章里讲过
`00:11:23` 先做个热身练习
`00:11:25` 假设我们要做两个矩阵的乘法
`00:11:27` 一个是B乘D的矩阵
`00:11:29` 另一个是D乘F的矩阵
`00:11:32` 直观来看
`00:11:33` B代表批次维度
`00:11:34` D是隐藏维度及模型维度
`00:11:36` 而F则是MLP中的上投影维度
`00:11:39` 回想一下算术强度到底是什么
`00:11:42` 我们要算两样东西
`00:11:44` 浮点运算量和内存搬运量啊
`00:11:47` 咱们这么算
`00:11:48` 你要做矩阵乘法
`00:11:50` 具体得干嘛呢
`00:11:51` 记得咱们系统课讲过的吗
`00:11:53` 你得先从hp m把X读出来
`00:11:56` 假设你全用BF sixteen存数据推理时
`00:11:59` 通常就是这样
`00:12:00` 那这部分数据量就是二乘B乘D
`00:12:02` 接着你要读权重W
`00:12:04` 这又得加上二乘以D乘F
`00:12:07` 然后做矩阵乘法
`00:12:08` 所以浮点运算量是二乘B乘D乘F啊
`00:12:13` 最后还得写回去
`00:12:14` 所以你看呃
`00:12:15` 每次读写基本上就是数据条目的总数
`00:12:19` 这差不多是个二次方向
`00:12:20` 而矩阵乘法是三次方
`00:12:22` 记住这点很关键
`00:12:24` 因为只有这样
`00:12:25` 你才能实现高算术密集度
`00:12:27` 所以呃总的浮点运算次数就是那个三次方向
`00:12:32` 而传输的字节数则是读写之和
`00:12:36` 好记住
`00:12:36` 算术密集度就是每传输一个字节所做的计算量
`00:12:40` 我们希望这个值越高越好
`00:12:42` 所以密集度等于即浮点运算次数除以
`00:12:46` 传输字节数
`00:12:47` 这里我用符号来表示这些量
`00:12:50` 因为这样比用具体数字更直观更好处理
`00:12:53` 那我们可以做一个简化
`00:12:55` 假设这里的batch维度远小于D和F
`00:12:58` 那就可以简化这个式子哦
`00:13:00` 啊具体来说
`00:13:02` 这里假设D等于C乘以BF等于C乘以B
`00:13:06` 并令C趋向于无穷大
`00:13:09` 这样D和F都很大
`00:13:11` 而B相对较小
`00:13:12` 该式就简化为B的密集度
`00:13:15` 所以总结一下
`00:13:16` 大家还记得咱们之前算矩阵乘法的计算强度吗
`00:13:19` 结果大概是三分之N嗯
`00:13:23` 这跟之前的情况类似
`00:13:25` 只不过之前只有一个变量来表示含义
`00:13:29` 咱们现在用的是非方阵
`00:13:31` 而不是方阵好
`00:13:32` 为了做个对比
`00:13:33` 硬件的计算强度是这样看的
`00:13:35` 先看规格书里的每秒浮点运算数
`00:13:38` 再看内存带宽
`00:13:40` 也就是内存在高带宽
`00:13:42` 内存HBM之间的传输速度
`00:13:44` 把这两个数一除就算出了计算强度
`00:13:48` 接下来啊
`00:13:49` 把你算出来的计算强度
`00:13:51` 跟硬件的计算强度比一比
`00:13:53` 如果比它大
`00:13:55` 那就说明你受限于计算能力
`00:13:58` 也就是计算瓶颈
`00:13:59` 这是好事
`00:14:00` 所以呃以h one hundred为例
`00:14:03` 针对这个特定的矩阵乘法
`00:14:05` 如果批次大小超过2995
`00:14:07` 你就处于计算瓶颈状态
`00:14:10` 咱们来看个极端情况
`00:14:11` 假设你只有一个样本
`00:14:13` 明白吗啊如果你只有一个样本
`00:14:16` 那算术强度arithmetic intensity就是一比啊
`00:14:21` 这时候就会受限于内存带宽
`00:14:23` 你大概能看出这里发生了什么
`00:14:25` 你在读取这个D乘次F矩阵
`00:14:27` 但B只有一
`00:14:28` 所以你实际上只做了二乘D乘F次浮点运算啊
`00:14:33` 这基本上就是你在inference阶段会遇到的工作负载
`00:14:38` 你拿到的不是完整的矩阵
`00:14:40` 而是这种非常薄的矩阵或张量
`00:14:42` 好我先讲到这里
`00:14:44` 看看大家有没有什么问题
`00:14:46` 我意思上来确认一下
`00:14:47` 抱歉这个我没法引用
`00:14:49` 咱们在这些动作里搜一圈
`00:14:52` 对我能跨着搜吗
`00:14:53` 就是跨域跨域跨域哦
`00:14:57` 还得检查这个交叉点
`00:14:59` 对
`00:14:59` 这意思是所有动作都要跨域吗
`00:15:01` 对到底哪些在动
`00:15:03` 我有点搞不清这个关联嗯
`00:15:06` 咱们看看就是这里的组数SI等等
`00:15:09` 我就是确认一下嗯
`00:15:11` K是key value heads的数量啊
`00:15:14` 还有所以你有Q抱歉是G抱歉
`00:15:19` G用于kv heads
`00:15:22` 我管你一件事
`00:15:24` 就像你每个键位会有几个模糊的
`00:15:27` 再见是的对应关系
`00:15:32` 你和计数器以及中性衰老疯狂是韩国人
`00:15:37` 我重复一遍
`00:15:38` 这意味着我们有三个地图
`00:15:42` 这是我的问题
`00:15:43` 好的嗯
`00:15:44` 我觉得你说的对
`00:15:45` 所以K代表组的数量
`00:15:46` G代表每个组里的投数呀
`00:15:49` Thank you
`00:15:50` 好的
`00:15:50` 谢谢我回头会修正这一点
`00:15:52` 谢谢好的
`00:15:54` 那我们再多聊聊推理过程中的算术强度
`00:15:56` S好的嗯
`00:15:59` 我们可以这样理解推理过程中发生的情况
`00:16:04` 呃
`00:16:04` 假设你用最朴素的方式来做
`00:16:07` 这里有个prompt
`00:16:08` 内容是never gonna give you
`00:16:11` 输入到transformer
`00:16:12` transformer会生成keys values和ACTIVATIONS呃
`00:16:17` 最终在输出词表上生成large
`00:16:20` 接着采样出一个token
`00:16:23` 把它拼接到prompt后面
`00:16:26` 然后你不断重复生成never
`00:16:28` 把它接在prompt后面
`00:16:30` 以此类推
`00:16:31` 好吧
`00:16:32` 这就是最基础的做法
`00:16:33` 如果你有个黑盒输入序列
`00:16:35` 就能输出token分布
`00:16:36` transformer正是如此
`00:16:37` 那你直接反复套用就行行啊
`00:16:40` 是行得通
`00:16:41` 但效果真的很差
`00:16:43` 嗯因为每生成一个token for2嗯
`00:16:47` 你实际花费的时间是ota po rod catch
`00:16:52` 就是你目前为止生成的token总数啊
`00:16:55` OK所以生成T和token的复杂度
`00:16:57` 其实是T的三次方
`00:16:59` 明白了吧
`00:17:00` 因为注意力机制本身的计算量就是T的平方
`00:17:03` 这部分是绕不开的
`00:17:05` Where are you
`00:17:06` 也就是说每个token都要算一遍
`00:17:10` 谢谢所以这效率挺低的
`00:17:13` 不过我们发现其实没必要这么算啊
`00:17:16` 其实很多计算是可以跨前缀复用的
`00:17:19` 打个比方
`00:17:20` 比如你生成never或者up
`00:17:21` 你会发现很多键值key values是重复计算的哎
`00:17:25` 哎比如never gonna
`00:17:27` Give you up
`00:17:28` 这句前面的token其实是不变的对吧
`00:17:32` 这是因为我们用的是英国transformer
`00:17:34` 如果是双向模型
`00:17:35` 那你加个新token
`00:17:37` 前面的所有状态都得变
`00:17:39` 但在因果模型里
`00:17:40` 你往后追加token
`00:17:42` 前面这些激活值ACTIVATIONS是不会变的
`00:17:45` 基于这个观察
`00:17:46` 最显而易见的做法就是存一个KV缓存
`00:17:50` 存在hp m里
`00:17:52` 这样在连续生成token之间
`00:17:54` 你就能直接复用之前的KV缓存
`00:17:57` 也就是说当你要生成GANA的时候啊
`00:18:00` 其实不用再去重新计算这些
`00:18:03` 之前token的所有键和激活值了啊
`00:18:06` 之前的那些token
`00:18:08` 所以这就是用上KV缓存之后的样子好
`00:18:12` 这里分两个阶段
`00:18:13` 先是预填充
`00:18:14` 你先拿到prompt
`00:18:15` 把KV缓存填满
`00:18:17` 也就是transformer算出来的那些键值对
`00:18:21` 然后你就开始生成
`00:18:22` 用的还是跟之前一样的计算
`00:18:24` 现在你就有了这个KB缓存
`00:18:27` 然后从这个分布里你采样出一个token
`00:18:30` 现在你把它喂给transformer
`00:18:31` 它会用到这个缓存
`00:18:33` 接着他输出下一个token的概率分布
`00:18:36` 并生成一个新的KV缓存
`00:18:39` 这基本上是对应到该token的激活值呃
`00:18:42` 谢谢
`00:18:43` 然后这个结果被输入回去
`00:18:44` 现在你有了增强了KV1缓存
`00:18:47` 有了这些LODGETS进行采样
`00:18:50` 然后把它喂给transformer
`00:18:52` 输出下一个token的分布
`00:18:54` 以及下一个嗯那个新的token
`00:18:59` 你把它加到KV缓存里
`00:19:01` 如此反复循环好
`00:19:04` KV缓存在形式上是呃对每个序列而言的
`00:19:08` 所以有B个序列对每个token有S个对每一层哦
`00:19:15` 在每个head中
`00:19:17` 你存储每个维度的向量好再强调一下
`00:19:20` 在inference阶段
`00:19:21` 首先是PREFILE
`00:19:22` 你拿到一个prompt
`00:19:23` 把它编码成这些向量
`00:19:25` 这和训练师一样是可以并行的
`00:19:28` 因为你能看到完整的prompt
`00:19:29` 就可以计算出kv cash
`00:19:31` 然后在生成阶段
`00:19:34` 你就按顺序逐个生成新的响应token
`00:19:38` 但至少对于已经看过的token
`00:19:41` 你不用再花开销去计算他们的kv cash
`00:19:44` 好
`00:19:45` 大家对kv cash都清楚了吗
`00:19:46` 关于这部分还有什么问题吗
`00:19:48` 好那咱们来算算AFLOS和内存
`00:19:51` 也就是MLP层和注意力层的IO
`00:19:55` 记住S是我们作为条件的token数量
`00:19:58` 而T是我们要生成的token数量
`00:20:01` 咱们先做一个抽象的推导
`00:20:02` 后面再具体到PREFIL阶段
`00:20:05` 此时T等于S和生成阶段
`00:20:07` 此时T等于一
`00:20:08` 咱们先看MOP层
`00:20:10` 整个推导过程我都只看矩阵乘法
`00:20:13` 因为真正费计算量的就是它剩下的部分
`00:20:17` 浮点运算量不大
`00:20:18` 而且可以合并到矩阵乘法里乘
`00:20:20` 好咱们来算一下
`00:20:21` 这跟矩阵计算一样
`00:20:23` 这部分我大概就不细讲了啊
`00:20:25` 也不会逐一去抠每个细节啊
`00:20:27` 不过大致的形式就是你去读取
`00:20:29` 把X从内存里读进来啊
`00:20:33` 你你读出所有参数
`00:20:36` 然后计算上投影
`00:20:38` 接着把它写入HPM
`00:20:40` 然后计算呃门控写入HPM
`00:20:43` 然后你计算简单来说就是做一下这个向下投影
`00:20:49` 然后把它写到HPM里好
`00:20:52` 那IPHOPS的数值就取决于BTD和F
`00:20:56` 这几个变量
`00:20:57` 也就是批量大小
`00:20:59` 序列长度
`00:20:59` 模型维度
`00:21:00` 还有前馈维度MLP维度
`00:21:03` 而传输的字节数就是上面这个表达式
`00:21:07` 好现在咱们就能算出算术强度了
`00:21:10` 也就是FLOS除以传输的字节数嗯
`00:21:14` 这就是个表达式
`00:21:15` 咱们还是像之前那样
`00:21:17` 假设B乘T远小于DNF
`00:21:20` 有了这个假设
`00:21:22` 咱们就能看出强度其实就是B乘T等于好
`00:21:26` 这跟矩阵乘法的情况很像
`00:21:28` 虽然矩阵和维度多了不少
`00:21:30` 但本质上是一回事
`00:21:31` 这很合理
`00:21:33` MP本质上就是一次大的矩阵乘法
`00:21:36` 而且这也说得通
`00:21:37` 因为批量维度和序列长度维度基本上互不影响
`00:21:42` 各自独立嗯
`00:21:43` 接下来我们看MLP啊
`00:21:46` 它内部没有交互啊
`00:21:48` TENTION的情况就不一样了
`00:21:50` 所以就是B成一体
`00:21:52` 只要你有啊
`00:21:53` 在预填充阶段
`00:21:54` 只要把B乘以T设的足够大
`00:21:57` 用大批量长序列
`00:21:58` 那就没问题
`00:22:00` 接下来看生成阶段
`00:22:02` 大家还记得生成阶段吧
`00:22:03` 啊这里有两个问题
`00:22:05` 第一生成阶段T等于音
`00:22:08` 也就是说你每次只生成一个dot token
`00:22:12` 这就意味着你的算术强度会是B
`00:22:16` 不过在生成阶段
`00:22:18` B指的是并发请求数
`00:22:20` 或者啊如果你是在做批处理
`00:22:23` 那还能稍微控制一下
`00:22:24` 但如果你是在服务一个chat board
`00:22:26` 那并发请求数基本上就是并发用户数可能很高
`00:22:29` 也可能很低
`00:22:31` 它可能会随时间变化
`00:22:33` 所以接下来讲连续批处理continuous spectrum时
`00:22:37` 我们会解决这个问题
`00:22:38` 不过总的来说这还行吧
`00:22:41` 因为只要batch够大
`00:22:43` 序列长度其实帮不上什么忙
`00:22:45` 只要batch够大
`00:22:46` 效果通常就没问题啊
`00:22:48` 咱们来看看注意力层
`00:22:50` S是已经生成的那些token
`00:22:52` T则是你要生成largest的token数量嗯
`00:22:57` 在注意力层里
`00:22:58` 你要从hp m读取QKV矩阵啊
`00:23:03` 然后计算注意力奉先soft max这步其实没那么重要
`00:23:08` 算出值矩阵
`00:23:09` 再把结果写回HPM
`00:23:11` 谢谢好
`00:23:12` 那么FS是B乘S乘T乘D
`00:23:18` 传输的字节数就是上面这个公式
`00:23:21` 所以你看所有的全都是矩阵乘法对吧
`00:23:24` 所以它的多项式阶数
`00:23:26` 总会比传输的字节数高一阶
`00:23:29` 因为这是矩阵乘法
`00:23:30` 关键在于这个系数长什么样
`00:23:32` 对于注意力机制
`00:23:34` 该系数为S3体分之一
`00:23:36` S加体分之好
`00:23:38` 咱们来看看预填充阶段嗯
`00:23:42` 预填充阶段当T等于S10
`00:23:47` 计算强度是二分之S嗯
`00:23:50` 这很好对吧
`00:23:51` 只要序列够长
`00:23:52` 对于注意力机制
`00:23:54` 你能维持很高的计算强度
`00:23:58` 注意这里没有批处理维度
`00:24:01` 我待会解释为什么等到了生成阶段
`00:24:04` 这就麻烦了
`00:24:06` 所以生成阶段的强度是S除以S加Y
`00:24:09` 这个值小于一
`00:24:10` 或者咱们就当它是一吧
`00:24:14` 记住强度是一的话就糟了啊
`00:24:17` 我们希望它达到two hundred five左右
`00:24:19` 好让h one hundred把算力跑满
`00:24:21` 所以谢谢呃
`00:24:23` 这确实就是瓶颈所在啊
`00:24:25` 所以嗯我们刚才对MOP注意力机制
`00:24:30` 做了所有这些分析
`00:24:32` 还有预填充生成
`00:24:35` 我们发现这里存在瓶颈
`00:24:37` 所以咱们来琢磨一下为什么它会是个瓶颈啊
`00:24:42` 啊跟MOP不一样
`00:24:44` 其实MOP用来做生成是没问题的
`00:24:46` 只要你的序列抱歉是你的啊
`00:24:49` 嗯P处理足够大就行
`00:24:52` 所以问题在于嗯说到机器学习
`00:25:00` 你知道呃注意力机制就是这么回事
`00:25:04` 咱们来看看MLP
`00:25:05` 每个序列用的都是同一组MLP权重好的
`00:25:09` 所以这些不依赖B嗯
`00:25:11` 但在注意力层里
`00:25:13` 每个序列都有自己的KV缓存
`00:25:15` and所以这些全都依赖于呃
`00:25:19` 然后一所以你可以这么理解嗯
`00:25:22` 这里的情况是在mp这种场景下
`00:25:27` 有了B就意味着批量大其实是有帮助的
`00:25:31` 因为这些mp权重你大概只要加载一次
`00:25:34` 你知道
`00:25:34` 然后就能用它来处理所有的序列
`00:25:37` 这就是你能获得高算术强度的原因
`00:25:40` 简单点说你加载一次权重
`00:25:42` 然后把整批数据都处理掉
`00:25:44` 这样就很好
`00:25:45` 但在注意力机制里就不行了
`00:25:48` 你知道这些全都依赖于B
`00:25:51` 所以增加B没什么用
`00:25:54` 对每个序列来说
`00:25:55` 你其实就是在做一次矩阵乘法
`00:25:58` 因为他们彼此独立
`00:26:00` 所以多做几次矩阵乘法也没什么用
`00:26:03` 还记得吗
`00:26:03` 刚开始的时候给你们看过这个例子
`00:26:06` 它的算术强度也很低
`00:26:08` 这不是矩阵乘法
`00:26:09` 因为我们本质上是在做批处理
`00:26:11` 是按坐标来的
`00:26:12` 这本质上跟做点基是一样的
`00:26:15` 而点击的算术强度糟糕透了
`00:26:18` 记住这是在注意力机制里
`00:26:20` 这个蓝色的B正是算术强度
`00:26:23` 不随B缩放的原因所在
`00:26:25` 这就是个瓶颈
`00:26:26` 好咱们总结一下
`00:26:28` 预填充阶段是计算受限的
`00:26:30` 生成阶段是内存受限的
`00:26:33` 所以如果你看预填充阶段的MLP算术强度
`00:26:37` 它是B乘以S
`00:26:39` 这就对了
`00:26:39` 预填充阶段的注意力强度是S除以二
`00:26:43` 没那么理想
`00:26:44` 但还能用到了生成阶段
`00:26:46` MOP强度虽然也能应付
`00:26:49` 但这要求请求并发量很大
`00:26:51` 真正棘手的根本瓶颈是生成阶段
`00:26:54` 那极高的注意力强度就是这么回事
`00:26:57` 只要你还用transformer
`00:26:58` 这个问题就没法真正解决
`00:27:00` if否先停一下
`00:27:01` 大家有问题吗
`00:27:03` 接下来每当你们听到大家常说
`00:27:06` inference是内存受限的
`00:27:08` 现在你们应该明白为什么了
`00:27:10` 谢谢好
`00:27:12` 咱们接着用
`00:27:13` 刚才的直觉和计算来聊聊我们的嗯
`00:27:17` inference指标啊
`00:27:19` 吞吐量延迟
`00:27:21` 还有TTFT
`00:27:22` 谢谢好
`00:27:23` 所以inference是内存受限的嗯现在这么看
`00:27:28` 其实把很多事都简化了
`00:27:29` 因为咱们考虑耗时的时候
`00:27:31` 只要看需要传输多少内存就行
`00:27:35` 因为咱们假设通信和计算是重叠进行的
`00:27:39` 那瓶颈就只取决于你得处理多少内存好
`00:27:44` 所以这某种程度上挺好
`00:27:46` 因为更简单了
`00:27:47` 但另一方面也挺让人头疼的
`00:27:49` 加速器就在那干
`00:27:50` 坐着什么也不干哦
`00:27:51` 嗯好那咱们用一个例子走一遍
`00:27:55` 比如lama two thirteen b跑在h one排上
`00:27:58` 什么延迟和吞吐量各是多少
`00:28:00` 好记得lama4FB有个特定的结构形状啊
`00:28:04` 抱歉
`00:28:05` 这是序列长度模型维度
`00:28:08` 前馈维度查询投诉还有键值投数
`00:28:12` 所以N等于K还有头维度啊
`00:28:16` 层数词表大小以及h one百的内存带宽
`00:28:20` Thank you
`00:28:20` 谢谢好
`00:28:22` 咱们继续嗯
`00:28:23` 谢谢好
`00:28:24` 咱们用这个配置来算一下
`00:28:27` transformer的性能数据
`00:28:29` 好这些是我们的输入嗯
`00:28:32` 接下来我要算的统计指标如下
`00:28:34` 参数数量
`00:28:35` 内存占用延迟
`00:28:38` 还有吞吐量好
`00:28:40` 那首先啊咱们来看看到底是什么占用了资源
`00:28:46` 什么东西占内存是参数
`00:28:48` 所以参数会占用内存
`00:28:50` 所以我们先算参数数量啊
`00:28:52` 大家来看嗯
`00:28:54` 嵌入层MOP层KQV的投影
`00:28:58` 把这些加起来
`00:28:59` 你就得到了总的参数数量啊
`00:29:01` 假设我们用的是b f sixteen啊
`00:29:04` 做inference时一向如此
`00:29:06` 那参数就得占这么多字节
`00:29:09` 另外内存里还有KV缓存
`00:29:12` dot k v缓存的大小就是序列里的token数
`00:29:16` 再乘上后面这几项
`00:29:19` 具体就是你的head数
`00:29:21` 再乘以kv head数乘以head维度
`00:29:24` 最后乘以层数
`00:29:25` 你得存一份key
`00:29:27` 一份value
`00:29:28` 然后因为用的是bf16
`00:29:30` 还要再乘以二好
`00:29:32` 这就是KV缓存的大小
`00:29:35` 而总的内存占用就是这是单个序列的
`00:29:39` 你有B个序列
`00:29:40` 所以要乘以B再加上参数大小
`00:29:43` 这就是你需要的内存量
`00:29:45` 现在嗯延迟是什么
`00:29:49` 它主要取决于内存IO对
`00:29:53` 因为推理过程受限于内存带宽
`00:29:57` 通常情况下
`00:29:58` 如果你能把通信和计算重叠起来
`00:30:01` 内存开销主要花在参数数据的来回搬运上嗯
`00:30:05` 那数据要在HBM和s RM之间来回传输
`00:30:09` 这就是移动数据所花的时间
`00:30:13` 所吞吐量是延迟的倒数
`00:30:16` 不过我们是并行生成big token
`00:30:19` 所以这里涉及两个指标
`00:30:20` 一个是每个token的秒数
`00:30:23` 另一个是每秒token数
`00:30:25` 另外因为你要处理一个大小为B的批次
`00:30:28` 数值上还会多出一个B的因子好
`00:30:31` 那我们来算一下
`00:30:33` 针对这个配置
`00:30:36` 这些指标的实际数值是多少
`00:30:39` 好参数总量是啊130亿嗯
`00:30:44` 所以有个很好的合理性检验
`00:30:47` 它号称是个130亿参数的模型呃
`00:30:50` 内存就是呃这一项嗯
`00:30:55` 大概是i m thirty a我想是百万乘以B
`00:30:58` 所以它基本上就是B的线性函数
`00:31:01` 再加上嗯其他一些项
`00:31:04` 所以这是k v cash
`00:31:05` 它随B增长
`00:31:07` 这是参数是参数数量的两倍
`00:31:10` 现在呃延迟就是呃把这一项按内存带宽缩放
`00:31:15` 所以它的形式和内存相同诶
`00:31:18` 啊吞吐量就是B除以那个值好的
`00:31:22` 所以注意当你增大B时
`00:31:24` 延迟会随之增长
`00:31:27` 因为KV缓存的大小会不断增加
`00:31:29` 而处理数据时需要反复复制KV缓存
`00:31:33` 谢谢接下来看吞吐量啊
`00:31:36` 它更有意思
`00:31:37` 随着批量大小B的增加
`00:31:39` 吞吐量确实会提高呃
`00:31:41` 但这有个上限
`00:31:42` 吞吐量之所以提升
`00:31:44` 是因为现在你把成本分摊到了更大的批次上啊
`00:31:49` 另外你处理这些数据的速度
`00:31:52` 显然也在随时间提升
`00:31:54` 所以这就是所谓的渐进线
`00:31:55` 吞吐量不可能无限增长啊
`00:31:59` 谢谢好
`00:32:00` 目前为止大家有什么问题吗
`00:32:03` 随便你只需要记住一点
`00:32:06` 延迟是B的线性函数啊
`00:32:09` 这里的常数项是KV缓存的大小
`00:32:12` 接着是参数量
`00:32:13` 然后就是吞吐量确实存在
`00:32:16` 其比例大致相当于B除以B加上某数值好
`00:32:21` 接下来咱们针对各种场景
`00:32:23` 把这个模型具体化一下好
`00:32:25` 假设你的batch size是one呃
`00:32:29` 那结果就是这样的
`00:32:30` 你的latency会是0.008秒
`00:32:35` Mo slash token
`00:32:36` 而throughput则是每秒hundred and twenty
`00:32:39` four个token
`00:32:40` 那么要是你增大batch size呢
`00:32:42` 你会发现latency变高了
`00:32:44` 不过THROOPER也会随之提升
`00:32:46` 也就是说latency变差了
`00:32:49` 但through put却改善了对所以这挺有意思的
`00:32:52` 因为我们总想着让它跑快一点
`00:32:54` 但这里的快其实有两层意思
`00:32:57` 具体看你看重哪一面
`00:32:58` 这两者甚至截然相反
`00:33:01` 如果你想tell batch sizer
`00:33:02` 其实关系很大啊
`00:33:04` 好会决定你想要的是lacy还是through pert啊
`00:33:09` 那如果我们把它调大呢
`00:33:10` 咱们现在确实需要高吞吐量
`00:33:12` 因为要处理海量文档
`00:33:14` 咱们再把batch size往上提一提好
`00:33:16` 这下latency就更差了
`00:33:18` 我是说对latency是更差了
`00:33:20` 但throughput反而更高了
`00:33:22` 但主要问题是内存
`00:33:24` 你会把内存耗尽
`00:33:26` 那没存这个kv cash占用的内存啊
`00:33:31` 会超过h one台的显存上限
`00:33:34` 就算你有b two hundred rs
`00:33:36` 能把batch size调的更大
`00:33:38` 但最终还是会碰到瓶颈
`00:33:40` 所以你的吞吐量其实是有上限的
`00:33:43` 你没法达到那个理论极限
`00:33:45` 因为内存会成为瓶颈
`00:33:47` 还有随着吞吐量提升
`00:33:49` 你的收益也在递减
`00:33:51` 谢谢所以增大batch size会让延迟变差
`00:33:57` 因为现在你要读写更大的kv cash
`00:34:01` 记住因为试批处理
`00:34:03` 所以如果你只有一个查询
`00:34:04` 你得等所有人都跑完啊
`00:34:06` 所以基本上的呃你就像在等公交啊
`00:34:11` 延迟挺高的
`00:34:12` boy的很deep o
`00:34:13` 然后一起走
`00:34:14` 而公交的吞吐量很好
`00:34:15` 因为它能一次运送
`00:34:16` 很多人也可以选和大家同时启动
`00:34:19` 所以batch size越大
`00:34:21` 吞吐量越高
`00:34:22` 这是因为参数是共享的
`00:34:24` 你只需要把模型加载到内存里一次int
`00:34:28` 这样就能处理很多序列好
`00:34:31` 咱们再确认一下
`00:34:32` 这里存在延迟和吞吐量之间的权衡关系
`00:34:36` 小batch size延迟低
`00:34:38` 但吞吐量差大
`00:34:39` batch size吞吐高
`00:34:41` 但延迟差好
`00:34:44` 关于并行性我就不展开讲了啊
`00:34:47` 推理还有个维度
`00:34:48` 就是你可以把模型切分放到多个设备上去跑
`00:34:52` 嗯要想深入了解的话
`00:34:54` 可以去看看scaling book里关于推理的那一章
`00:34:57` 我就举个都特别简单的例子
`00:34:59` 比如你启动M个模型
`00:35:01` 副本延迟不变
`00:35:02` 但吞吐量能提升到M倍
`00:35:05` 嗯还有个指标我们没提到
`00:35:07` Time to first token
`00:35:09` 这基本上就是做PREFIL所花的时间
`00:35:12` 因为PREFILE1结束
`00:35:13` 你基本上就能开始生成了好
`00:35:17` 那如果你想这么做的话
`00:35:21` 想要更快的TTFT就得用较小的batch size
`00:35:24` 而想提升throughput就得用较大的batch size lea
`00:35:27` 好
`00:35:29` 希望这点讲清楚了
`00:35:31` 关于throughput和latency
`00:35:32` 还有他们之间为什么相互制约
`00:35:34` 大家有什么问题吗
`00:35:35` 好现在咱们有了一个概念框架
`00:35:38` 用来理解怎么高效地做inference
`00:35:41` 也就是arithmetic intensity
`00:35:43` throughput和latency这几个方面
`00:35:47` 接下来咱们试试怎么让它更快
`00:35:50` 怎么让inference提速
`00:35:51` 方法很多
`00:35:52` 差异也很大
`00:35:53` 从改模型架构到做系统优化
`00:35:56` 中间各种做法都有
`00:35:58` 所以某种意义上说
`00:35:59` inference算是个内容相当丰富
`00:36:02` 有横跨多个领域的话题
`00:36:05` 你好
`00:36:05` 那最怎么说呢
`00:36:07` 啊也许现在回过头看
`00:36:10` 最显而易见的一点就是
`00:36:12` 希望我已经把这一点反复讲透了
`00:36:14` memory是influence的瓶颈
`00:36:17` 找kv cash很占内存
`00:36:19` 如果batch size足够大
`00:36:21` 它占用的量甚至可能超过参数本身的规模
`00:36:26` 所以试着把kv cash的大小降下来
`00:36:30` 不过你得小心操作
`00:36:32` 因为得确保在这个过程中别损失太多精度
`00:36:36` 你对这里有个办法啊
`00:36:39` 咱们之前聊过就是group query attention
`00:36:43` 简单回顾一下
`00:36:44` 往后大佛发assession and嗯
`00:36:46` 基本逻辑是啊
`00:36:48` 对于每个token
`00:36:49` 你都要有一个key
`00:36:50` 一个value和一个query were shorder
`00:36:54` 如果你用group query attention
`00:36:56` 你依然计算相同数量的queries
`00:36:59` 但你只有其中一部分分组数更少
`00:37:02` 并且你要为每个分组分别计算key和value好
`00:37:07` 所以这里的K就是分组的数量
`00:37:09` 所以在多头注意力里
`00:37:10` K等于N
`00:37:11` 也就是不做任何缩减啊
`00:37:14` 还有个叫多查询注意力的东西
`00:37:17` 因为效果太差
`00:37:18` 没人用
`00:37:19` 它的K等于一介于两者之间的某个地方
`00:37:23` hopefully能让我们找到精度和速度之间的平衡
`00:37:27` 就是推小伦死了年那篇介绍GQA的论文
`00:37:32` 他们的结果表明
`00:37:33` 只要你去看耗时就会发现啊
`00:37:36` 每个样本的时间
`00:37:37` 这要跟延迟和吞吐量都有关
`00:37:40` I uh
`00:37:41` how decouthose space your one could wah啊
`00:37:45` 多头注意力也就是全注意力
`00:37:47` 它的耗时非常高
`00:37:48` 要是你从K等于一起步
`00:37:50` 那速度可就快多了
`00:37:52` 接着你可以把K往上加
`00:37:54` 像K1等于八
`00:37:55` 这样效果还挺好
`00:37:56` 但到最后你的也就到顶了
`00:37:59` Thank you
`00:37:59` 耗时就会明显变长
`00:38:02` 谢谢好
`00:38:03` 那嗯为啥GQA能降低延迟
`00:38:07` 提升吞吐量呢
`00:38:08` 哎嗯他把KV缓存的大小
`00:38:11` 缩减到了原来的K分之N分之一
`00:38:14` 另外提一句哈
`00:38:15` 省内存就能提速
`00:38:17` 因为咱们这系统是内存瓶颈
`00:38:19` 谢了好
`00:38:20` 咱们再来看看这个浪漫模型啊
`00:38:22` 记得吧
`00:38:23` 最开始咱们设的是K等于N
`00:38:26` 所以既没有多头注意力
`00:38:27` 也没减少key和value的数量
`00:38:30` 所以还是刚才那个例子
`00:38:33` 大家记得吗
`00:38:34` 用64的batch size
`00:38:36` 咱们得到的吞吐量和延迟是这样的嗯
`00:38:40` 现在要是你用了GQA
`00:38:42` 咱们可以假设sparsity设为烟比五嗯
`00:38:46` 这能减少呃VERS内存啊
`00:38:50` 能省不少
`00:38:51` 这样一来延迟改善了
`00:38:53` 吞吐量也上去了
`00:38:55` 所以你看延迟和吞吐量并不总是冲突的
`00:38:58` 你要是把内存占用降下来
`00:39:00` 这两个指标都能变好啊
`00:39:02` 嗯主要的矛盾点其实还是在batch这个维度上嗯
`00:39:07` 行好的
`00:39:08` 这效果不错
`00:39:09` 还有咱们干脆再把batch size调大一点
`00:39:13` 试试看看会怎样
`00:39:15` 之前要是batch size设成two five se内存就爆了
`00:39:20` 但现在装得下内存了
`00:39:22` 你会发现batch size变大
`00:39:23` 让延迟稍微高了点
`00:39:25` 但吞吐量反倒上来了
`00:39:26` 然而会成比例地往上涨
`00:39:30` 好诶
`00:39:31` 所以有时候你得把这些参数一起调
`00:39:33` 比如可减小kv cash
`00:39:35` 这样就能把batch size提上去
`00:39:36` 顺便做做其他取舍
`00:39:38` 谢谢啊
`00:39:39` 所以最后一件要做的事
`00:39:41` 不管你做哪种有损改动
`00:39:44` 都得保证accuracy不掉
`00:39:46` 嗯这篇论文显示啊
`00:39:49` GQUA在时间上表现更好啊
`00:39:52` 不过啊跑了一堆100哦
`00:39:55` 基本上效果都不错哦
`00:39:56` 所以有了这些accuracy评估
`00:39:58` 我觉得咱们还是得打个折扣看
`00:40:00` 毕竟这只是针对特定模型的结果
`00:40:03` 后来deep seek的论文表明它确实会带来负面影响
`00:40:07` 所以除了纯数学的部分
`00:40:09` 其他内容咱们都得打个折扣看
`00:40:12` 谢谢好
`00:40:13` 说到deep sick
`00:40:14` 这里还有一个进一步压缩的思路
`00:40:17` 得KB缓存好
`00:40:18` 这部分的主题就是减少KB缓存
`00:40:21` 同时降低延迟
`00:40:23` 提升吞吐量
`00:40:24` 这是多头注意力机制
`00:40:26` 每个token对应的queries
`00:40:28` keys和values数量都是一样的啊
`00:40:31` 还有GQA大家还记得吧
`00:40:33` 我们减少了每个token
`00:40:35` 对应的keys和values的数量啊
`00:40:37` 抱歉我说错了
`00:40:38` 不是每个token
`00:40:39` 我们其实是减少了values和keys的数量
`00:40:42` 现在deep sick的多头前注意力MLA则表示
`00:40:47` 我们其实要保留keys和values的数量不变
`00:40:51` 每个token对应一个
`00:40:53` 本质上就是每个token一个
`00:40:55` 但我会对他们做参数化处理
`00:40:57` 把这些压缩一下
`00:40:58` 意思一般来说你会有一些东西啊
`00:41:01` 咱们是怎么算键和值的呢
`00:41:04` 咱们先拿到激活值
`00:41:06` 然后乘上一个矩阵得到K
`00:41:08` 再乘另一个矩阵得到V
`00:41:10` 这通常是N乘H的维度
`00:41:12` 就跟你的模型维度差不多大
`00:41:15` 这个维度很大
`00:41:16` 所以MA的做法是
`00:41:18` 把这些激活值投影压缩到C位
`00:41:21` 打个比方
`00:41:22` deep sc v two就把这个数从roll脑般降到了五法
`00:41:26` 所以这里的压缩相当激进
`00:41:29` 接着我就从这个压缩表示里算出K和V
`00:41:33` 现在我只存C就行
`00:41:34` 这小多了
`00:41:35` 等需要键和值的时候
`00:41:36` 我再把它们还原出来
`00:41:38` 谢谢啊
`00:41:39` 不过这里有个细节要注意呃
`00:41:41` MLA和rope兼容
`00:41:43` 因为rope是直接作用在键和值上的
`00:41:46` 所以呃他们的做法是呃
`00:41:49` 增加额额外的维度来处理rope嗯
`00:41:54` 不过总的来说啊
`00:41:56` 缩减幅度还是挺大的
`00:41:58` 所以延迟和吞吐量的提升
`00:42:01` 算笔简单的账就清楚了
`00:42:04` ky cash越小
`00:42:05` 跑起来就越快啊
`00:42:07` 行在某个点之前
`00:42:09` 这基本是线性扩展啊
`00:42:11` 谢谢另外记得检查一下你的模型准不准啊
`00:42:15` 所以首先这个结果跟GQA那篇论文有点冲突
`00:42:18` 或者说存在张力啊
`00:42:20` 他们指出GQA其实没那么好好
`00:42:23` 这是MHA
`00:42:24` 这是GKA
`00:42:25` 这几个数字比那几个数字要小
`00:42:27` 不过你看啊
`00:42:28` 他们的MLA多
`00:42:29` 潜在注意力方法甚至比MH还略好一点
`00:42:32` 但咱们大体上可以认为两者差不多
`00:42:35` 所以这一列和这一列要好得多
`00:42:37` 不过我想这张表上没有GQA
`00:42:40` Thank you
`00:42:40` 咱们在这儿做个对比
`00:42:42` 谢谢好
`00:42:44` 那我接着讲
`00:42:45` 这跟减少模型里的维度数量相比
`00:42:47` 效果怎么样啊
`00:42:49` 谢谢啊
`00:42:50` 这个问题是是跟缩减模型维度相比效果如何啊
`00:42:55` 我的哦
`00:42:56` 呃不过这些消融实验没体现出这一点
`00:42:59` 我猜单纯缩减模型维度只会让效果更差
`00:43:02` 因为你是在不加区分地削减所有东西
`00:43:05` 我觉得这里面的关键是呃
`00:43:08` 在模型里找到那些可以压缩的地方
`00:43:11` 至于先验知识
`00:43:12` 我觉得你很难一开始就确定咱们得靠大量实验
`00:43:16` 看看哪种方法管用
`00:43:18` 所以这里有个新点子来缩减kv cash
`00:43:21` 叫做跨层注意力
`00:43:23` 咱们先看一下常规情况
`00:43:25` 每一层本来都有kv kids和vs
`00:43:27` 咱们假设不这么做了
`00:43:29` 只去计算其中一部分层级的KV
`00:43:32` 然后呢对于这一层
`00:43:34` 我就直接用上一层的kb cash ring
`00:43:36` 好的
`00:43:37` 所以这算是另一种形式的共享
`00:43:40` 就像GQA是在head之间共享KV一样
`00:43:44` 现在咱们是在layer之间共享KV到啊
`00:43:47` 从实证结果来看
`00:43:48` 这篇论文显示这么做能优化perial frontier
`00:43:52` 所以这些模型每一个都更好
`00:43:55` 一个比一个强
`00:43:56` 给另一个方法
`00:43:57` 你其实可以通过调整K和head dimension嗯
`00:44:02` 来便利k v cash的不同大小啊
`00:44:05` 我想这跟你之前提到的
`00:44:07` 改变模型维度的观点有点关系
`00:44:09` 不过要是你用了COA跨层注意力
`00:44:13` 效果就更好了
`00:44:15` 好咱们接着往下讲啊
`00:44:18` 咱们快速过一遍
`00:44:19` 减少kv cash的各种技术手势啊
`00:44:22` 啊比如局部注意力或滑动窗口注意力
`00:44:26` 这个想法挺老的
`00:44:27` 但也特别自然
`00:44:29` 所以你看完整的注意力矩阵
`00:44:31` 它的规模是N的平方
`00:44:34` 与其那样
`00:44:35` 如果你要生成一个token
`00:44:36` 只需看最近的K个token就行好
`00:44:39` 所以简单来说
`00:44:40` 你生成的每个token都有一个滑动窗口
`00:44:43` 只依赖最近的K个
`00:44:45` 现在要是你这么做
`00:44:48` 那么kv cat现在就跟序列长度没关系了
`00:44:50` 它其实就是批次大小乘以其他变量
`00:44:53` 这太好了
`00:44:54` 尤其对于长上下文场景嗯
`00:44:57` 你看因为层数的关系啊
`00:44:59` 实际的有效上下文长度比标称的要大
`00:45:03` 因为信息顺着层往下传能传得更远
`00:45:06` 现在你可以做些更复杂的
`00:45:08` 比如不一定非要密集的选层
`00:45:10` 但你可以把它们间隔开
`00:45:12` 你也可以用全局加滑动窗口的方式
`00:45:15` 既关注固定网格上的不同token点
`00:45:17` 又关注局部的滑动窗口
`00:45:20` 所以你可以尝试各种方法
`00:45:22` 不过有个问题
`00:45:23` 这样做其实还是会牺牲准确性
`00:45:26` 也就是说表达能力下降了
`00:45:29` 天下没有免费的午餐
`00:45:31` 或者说这顿午餐其实挺贵的
`00:45:34` 大家想到的解决办法是
`00:45:36` 把局部注意力和全局注意力交替使用
`00:45:40` 所以这类混合模型的做法是某些层用全注意力
`00:45:44` 另一些层则用局部注意力
`00:45:47` 而且这样基本上就能稍微减少一点KV缓存
`00:45:50` 你一直在努力平衡准确性和效率速度
`00:45:53` 谢谢谢谢
`00:45:55` 问线性注意力和滑动窗口注意力
`00:45:57` 在权衡取舍上有区别吗
`00:45:59` 对
`00:46:00` 这个问题是关于线性注意力和滑动窗口注意力
`00:46:03` 线性注意力我就不展开讲了啊
`00:46:06` 简单提一下啊
`00:46:07` 有一些方法不去存储KB缓存
`00:46:11` 而是直接进行计算
`00:46:13` 对全部历史信息的某种压缩表示啊
`00:46:17` 最简单的线性注意力做法就是
`00:46:19` 直接把KV加起来
`00:46:21` 把这些values加总成一个向量
`00:46:23` 这样一来就肯定与序列长度无关了啊
`00:46:26` 你也可以用更高级的方法
`00:46:28` 比如gannet delta nets和mamba
`00:46:34` 所以现在的问题是
`00:46:35` 这些方法之间该怎么比较
`00:46:37` 它们也被用来替代滑动窗口注意力
`00:46:40` 人们也已经有了一些探索啊
`00:46:43` 你也可以把全注意力
`00:46:45` 滑动窗口注意力和线性注意力结合起来用
`00:46:48` 因为它们各自捕捉的侧重点不同
`00:46:50` 如果你更在意局部高分辨率的信息
`00:46:54` 那滑动窗口注意力会更合适
`00:46:56` 如果你只是想要对过往内容的宏观概括
`00:47:01` 那就用另一种方法更好啊
`00:47:03` 不线性注意力可能更好
`00:47:05` 那么对于长上下文
`00:47:07` 你觉得线性注意力会更好吗
`00:47:09` 嗯针对这种情况的设定
`00:47:12` 所以啊我的问题是对于长上下文
`00:47:15` 线性注意力会更好吗
`00:47:17` 呃我来说说嗯
`00:47:18` 总之天下没有免费的午餐对吧
`00:47:21` 打个比方
`00:47:22` 假设上下文极长
`00:47:23` 而你正在处理一个大海捞针的问题对吧
`00:47:27` 如果你必须把整段历史压缩进很小的一段
`00:47:30` 上下文里
`00:47:31` 信息就一定会丢失
`00:47:34` 而且你可能就根本检索不到它了
`00:47:37` 呃我想我的意思是呃
`00:47:39` 看起来大家都在采用混合架构啊
`00:47:42` 因为似乎总需要某种更长城的注意力机制
`00:47:45` 我只是想在自己脑子里把这件事理清楚
`00:47:48` 那么用滑动窗口和用某种mobile internet层相比
`00:47:53` 二者的取舍是什么
`00:47:55` 在user使用高分两层
`00:47:56` 是不是始终都比华为窗口更好
`00:47:58` 那实际上有代表性的取舍到底是什么呢
`00:48:03` 啊我想也许可以这么说
`00:48:05` mama和delta更为强大
`00:48:07` 嗯比滑动窗口注意力更强大
`00:48:10` 也许你可以这样来理解
`00:48:11` Mua
`00:48:12` 你应该是能想明白的
`00:48:13` it它当然能体现出滑动窗口注意力的某些特性
`00:48:17` 因为在做递归的时候
`00:48:18` 他只要看上一个状态就行啊
`00:48:21` 对也许对
`00:48:23` 也许你可以把线性注意力或者它的扩展形式
`00:48:26` 看作是不对
`00:48:28` 至少他们还有更多回旋的空间
`00:48:30` 而一旦你把它做成滑动窗口
`00:48:32` 注意力就到此为止了
`00:48:34` sick没有别的能做的了
`00:48:35` 对好那我快速过一下嗯
`00:48:38` 重点讲一下这个deep seek
`00:48:39` deep seek还在不断的嗯创新出不同类型的啊
`00:48:44` 注意力机制
`00:48:45` 大家记得他们提出了多潜在注意力
`00:48:48` Multilatent attention
`00:48:49` 用来压缩减值
`00:48:51` 这里有个概念叫压缩系数注意力
`00:48:53` 现在还有deep sk系数注意力和重度压缩注意力
`00:48:56` 这些缩写词的具体含义我一时想不起来
`00:48:59` 不过咱们来看这张图呃
`00:49:01` 正常情况下你会用到kv token和query token
`00:49:06` 压缩注意力的做法
`00:49:08` 基本上就是把每M个token压缩成一个token
`00:49:12` 接着是deep seek吸收注意力
`00:49:14` 它主要就是从里面挑出一部分子集嗯
`00:49:19` 用来保留挑选子集的方法是
`00:49:23` 先算一些轻量级的queries和keys
`00:49:25` 再做一次小规模的attention来拿到索引分数
`00:49:29` 这样你就知道该留哪些了
`00:49:31` 这就是一种极快的确定该保留哪些token的方法
`00:49:35` 然后你就用这些token啊
`00:49:37` 后面还有更进一步的压缩
`00:49:39` 为了节省时间
`00:49:40` 我直接往下讲
`00:49:41` 这一节的目标是减少kv cash啊
`00:49:44` 因为kv cash跟内存有关
`00:49:46` 而我们前面已经看到inference是memory bounder
`00:49:50` 这能直接提升吞吐量
`00:49:52` 降低延迟
`00:49:53` 关键是要在啊不损害准确性的前提下
`00:49:56` 做到这一点
`00:49:57` 所以你可以跨层啊
`00:49:59` 跨head啊
`00:50:00` 跨head dimension来做低维度的kv cash啊
`00:50:04` 你可以用local attention
`00:50:05` 也可以用linear attention
`00:50:06` 这个前面讲过
`00:50:08` 还有很多其他方法
`00:50:10` 另外还有diffusion models
`00:50:11` 这是一种非次回归的生成方式
`00:50:13` 速度能快很多
`00:50:14` print好
`00:50:16` 咱们再来聊几个其他的重要思路
`00:50:18` 所以quantization更多是从系统层面
`00:50:21` 而不是架构层面来讨论怎么让模型变小
`00:50:25` 所以核心思路就是降低数字的精度
`00:50:28` 内存占用更少
`00:50:30` 就能带来更高的吞吐量和更低的延迟啊
`00:50:33` 显然你得担心准确性问题
`00:50:37` 量化嘛就是嗯选项很多范围
`00:50:41` 从BFC六一直到n t four
`00:50:44` 所以啊如果你怕量化把模型搞砸
`00:50:48` 可以试试带着量化意识去训练模型
`00:50:52` 这叫量化感知训练
`00:50:54` 训练时在正向传播过程中
`00:50:57` 你会先量化再反量化
`00:50:59` 你基本上是在训练时模拟这些量化误差
`00:51:03` 就这样权重会适应量化
`00:51:05` 效果通常更好
`00:51:06` 但代价是训练成本高昂且规模大
`00:51:09` 一般大家会先训练好模型
`00:51:12` 然后再做量化
`00:51:13` 这叫训练后量化
`00:51:15` 通常便宜得多啊
`00:51:16` 有一种比较直接的做法
`00:51:18` 针对每一层或每个张量啊
`00:51:21` 分别确定它的缩放因子和零点
`00:51:23` 然后单独做量化嗯
`00:51:25` 不过这种做法通常效果没那么好啊
`00:51:29` Q你可以用一种叫GPTQ的方法
`00:51:32` 它利用一些海森矩阵信息逐层量化
`00:51:35` 同时追踪那些传播到未量化权重里的误差
`00:51:39` 这样你就能修正这些误差嗯
`00:51:42` 现在呃更高级的做法叫激活感知量化
`00:51:47` 它的核心观察是有些激活通道的值很大
`00:51:51` 跟这些通道打交道的权重就显得更重要
`00:51:55` 所以咱们给这些权重多分配点精度
`00:51:59` 来看这张图啊
`00:52:01` 一般情况是
`00:52:02` 如果你拿f p six t的权重矩阵做量化
`00:52:05` 比如把它量化成in3
`00:52:07` 咱们接下来要做的是找出哪些通道是活跃的
`00:52:13` 比如这些激活通道
`00:52:14` 其中有些通道规模还挺大
`00:52:17` 所以如果某个通道整体规模较大
`00:52:19` 咱们就直接给它分配FP16精度
`00:52:22` 其余部分都保持in three in
`00:52:24` 只有少数几个重要通道
`00:52:26` 咱们采用更高的精度好
`00:52:28` 所以这里还有另一种思路叫模型减脂
`00:52:33` 简单来说就是拿一个大模型
`00:52:36` 把其中一部分切掉
`00:52:37` 然后再修补一下呃
`00:52:41` 这方法挺粗糙的
`00:52:42` 但确实管用
`00:52:43` 比如NVIDIA有篇论文基本上嗯让我先看一下啊
`00:52:48` 谢谢呃
`00:52:50` 咱们看看啊
`00:52:51` 所以啊你得先评估模型各部分的重要性
`00:52:55` 挑出最关键的那些呃
`00:52:58` 然后你就把不同的隐藏单元
`00:53:00` 甚至某些层直接移除掉
`00:53:02` 现在我们就得到了一个模型
`00:53:05` 不过它的效果还不太行
`00:53:06` 所以你要对它做后训练
`00:53:09` Post training
`00:53:09` 用你关心的那些数据或任务再多训练一下
`00:53:13` 把它的性能修复回来好
`00:53:15` 所以某种意义上说
`00:53:17` 这其实也算是一种训练
`00:53:19` 这是一种减少kv cash用量的方法
`00:53:23` 只不过你是用一个好模型的一部分
`00:53:25` 来给它做初始化
`00:53:26` well好看起来这个方法效果还挺不错的啊
`00:53:32` 所以啊
`00:53:33` 他们成功把一个15B的模型压缩到了8B
`00:53:37` 准确率没怎么受影响
`00:53:39` 而且训练这个模型
`00:53:41` 走这整个流程所需的数据量要少得多
`00:53:44` 谢谢谢谢好
`00:53:47` 那咱们来总结一下
`00:53:48` 我们要做的就是在不损害准确率的前提下
`00:53:52` 降低推理inference复杂度啊
`00:53:55` 你可以把它理解为啊
`00:53:57` 主要就是减少参数量或者kv cash
`00:54:00` 你可以定义一种更快的模型架构来训练它
`00:54:03` 或者你也可以定义一种更快的模型架构
`00:54:07` 然后做初始化
`00:54:09` 用原始模型的权重来初始化
`00:54:12` 虽然它的架构可能不一样
`00:54:14` 基本上就是拼出一个弗兰肯斯坦式的怪物
`00:54:18` 然后再用蒸馏把这个更快的模型修好啊
`00:54:22` 区分重要层和非重要层
`00:54:25` 那么咱们怎么区分哪些是重要层
`00:54:28` 哪些不是呢
`00:54:29` 呃一般来说你会准备一个校准集
`00:54:33` 把输入跑一遍
`00:54:34` 模型主要看的就是激活值
`00:54:36` ACTIVATIONS的大小
`00:54:39` 至于那些确实存在的激活值
`00:54:42` 你知道其中一些尤其是那些呃死单元呃
`00:54:47` 数值会接近于零
`00:54:48` 而那些数值较大的我们要保留下来
`00:54:51` 这就是一个宏观层面的思路
`00:54:54` 我猜你可能会问
`00:54:55` 为什么激活值高就很重要
`00:54:57` 他也可能只是
`00:54:58` 所以说要是他在其余样本里压根儿就不出现呢
`00:55:02` 所以总的来说好问题在于
`00:55:04` 如果所有激活值都很高
`00:55:06` 那又怎么样
`00:55:07` 所以总的来说
`00:55:08` 这是一种经验性的观察
`00:55:10` 某些通道的激活值会远高于其他通道
`00:55:14` 如果事实并非如此
`00:55:16` 这些技术就不一定有效啊
`00:55:18` 但事实碰巧就是如此
`00:55:20` 这是有原因的
`00:55:21` 这正是这些模型最终被训练出来的方式
`00:55:26` 然后你就可以利用这一点
`00:55:28` 它在所有样本中的值始终是100
`00:55:32` 这是否意味着它一定有意义
`00:55:34` 还是说它可能只是训练过程中产生的某种伪影
`00:55:38` 我明白了嗯
`00:55:40` 那么问题来了
`00:55:41` 如果一个神经元的值始终为100呢
`00:55:44` 这种情况下你也可以考察与方差相关的问题
`00:55:47` 比如如果值为hundred
`00:55:49` 你就不能直接移除它
`00:55:50` 否则会导致整体出错
`00:55:52` 但如果均值高
`00:55:53` 方差低
`00:55:54` 或许还有别的处理思路
`00:55:56` 也就是本质上把偏执bias
`00:56:00` 纳入进来的一种方法啊
`00:56:02` 我快速过一下另一个想法
`00:56:03` 到目前为止
`00:56:04` 我们讨论的都是有损方法
`00:56:07` 这类方法能大幅压缩KV缓存
`00:56:10` 但可能会牺牲准确性
`00:56:12` 还有一种非常优雅的方法可以实现无损处理
`00:56:15` 这被称为推测采样
`00:56:17` spective sampling或推测解码
`00:56:19` Spective decoding
`00:56:20` 回想一下
`00:56:21` 在啊执行预填充PREFIL阶段
`00:56:24` 你可以啊并行的对所有token进行编码
`00:56:28` 这同样能给出概率
`00:56:29` 该方法速度很快
`00:56:31` 属于计算受限
`00:56:32` computer down型具备诸多优点
`00:56:35` 而生成则是逐个进行的好
`00:56:38` 咱们来说说检查
`00:56:41` 检查比生成快多了
`00:56:42` 比如我给你一段序列
`00:56:44` 你能很快告诉我它有多好
`00:56:46` 比逐个生成要快得多
`00:56:47` 所以咱们可以利用这种不对称性
`00:56:50` 思路如下
`00:56:51` 我们先用一个便宜的草稿模型
`00:56:54` 让草稿模型先生成几个token
`00:56:57` 比如四个
`00:56:58` 接着用我们真正在乎的目标模型Q来审查
`00:57:02` 这些token
`00:57:03` 决定接受还是拒绝
`00:57:04` 懂了吗
`00:57:05` 这样两边就平衡了
`00:57:06` 草稿模型更小更便宜
`00:57:08` 虽然它受内存限制
`00:57:10` 必须逐个生成
`00:57:12` 但这问题不大
`00:57:13` 目标模型虽然大且贵
`00:57:16` 但咱们让他并行处理一批token
`00:57:18` 所以他也不会太吃力
`00:57:20` 咱们来看个视频
`00:57:21` 直观的演示一下这背后的工作原理
`00:57:24` 呃
`00:57:24` 你看要是让大模型一个一个token d生成
`00:57:27` 那效率可就太低了
`00:57:29` All right
`00:57:29` 但要是你用了推测解码speculative decoding
`00:57:34` 情况就不一样了
`00:57:36` 嗯这时候你能看到小模型先生乘出一批token
`00:57:40` 接着交给大模型
`00:57:43` 基本上大模型会对这些token进行校验和评估
`00:57:48` 这样一来你就获得了一连串token的批量输出
`00:57:52` 接着是下一批
`00:57:54` 以此类推
`00:57:55` 好咱们来看看具体的算法流程
`00:57:59` 啊当时有好几篇论文
`00:58:01` 几乎同时发表了关于推测解码的研究哦
`00:58:06` 这就是其中之一
`00:58:08` 所以这里的思路是为了生成内容
`00:58:10` 我们会先生成K个token
`00:58:12` 直接从这个草稿模型里采样啊
`00:58:15` 所以P1就是那个草稿模型
`00:58:17` 我们要从中采样出K个token
`00:58:19` 然后我们会并行的算出这些草稿
`00:58:22` token的LODGETS用的是Q
`00:58:25` 现在我们需要决定是接受还是拒绝
`00:58:29` 这时候就得用到一些数学
`00:58:31` 概率和统计学的知识了
`00:58:33` 你基本上会以mino v q four
`00:58:36` 分之Q这个概率来接受B
`00:58:39` 也就是说如果Q比P大很多
`00:58:43` Q越大
`00:58:44` 我们就越倾向于接受它啊
`00:58:47` 否则你就从那个残差分布里采样啊
`00:58:50` 然后退出
`00:58:51` 这基本上就是拒绝采样
`00:58:53` 只不过在拒绝采样里
`00:58:55` 有时候你被拒绝了
`00:58:56` 就什么都得不到
`00:58:57` 但在这儿
`00:58:57` 我们总能保证从目标模型里拿到精确的样本啊
`00:59:01` 这部分我跳过不讲证明挺简单的啊
`00:59:04` 论证逻辑跟拒绝采样基本一样
`00:59:08` 用来证明它是精确的
`00:59:09` 也就是目标模型给出的那些概率
`00:59:13` 最初那篇论文表明这种方法速度很快
`00:59:17` 一般来看
`00:59:17` 如果你的草稿token太少
`00:59:19` 就没怎么利用上目标模型那边的批处理
`00:59:24` 如果数量太多
`00:59:25` 被拒绝的概率就大了
`00:59:27` 所以得找个平衡点
`00:59:28` 比如这种情况下的3~4个
`00:59:31` 谢谢好
`00:59:32` 一般来说drift model比target model小得多
`00:59:35` 理想状态下
`00:59:36` 你希望drift model尽量贴近target model
`00:59:38` 这就得靠蒸馏来实现
`00:59:40` 也就是说咱们刚才聊的那些思路
`00:59:42` 其实也适用于speculum encoding
`00:59:44` 简单来说
`00:59:45` 就是想办法用各种手段把kv cash降下来
`00:59:50` 要是结果满意就直接上线
`00:59:52` 要是不满意
`00:59:53` 他至少能当个draft model
`00:59:55` 再用主模型来修补一下
`00:59:57` 现在关于speculative decoding有很多新研究
`01:00:00` 效果都比最初的好
`01:00:02` 这部分我就先跳过了啊
`01:00:04` 谢谢好
`01:00:04` 咱们快速过一下嗯
`01:00:06` 动态负载这个应用场景是这样
`01:00:09` 你有一个线上网站用户过来跟你的模型聊天嗯
`01:00:13` 这些请求到达的时间各不相同啊
`01:00:17` 共享的前缀也不一样
`01:00:20` 而且长度参差不齐啊
`01:00:22` 所以情况挺乱的
`01:00:24` 完全不像那种
`01:00:25` 一次性把同样数量的token块都塞进去的
`01:00:28` 简单训练
`01:00:29` 那这种情况下该怎么办呢
`01:00:31` 有这么个思路
`01:00:32` 有人构建了一个叫ORCA的系统
`01:00:35` 这其实是呃挺早的一个做法
`01:00:38` 他提出了continuous spatching
`01:00:40` 连续批处理这个概念
`01:00:42` 咱们这么想
`01:00:43` 你手头有一堆这样的请求
`01:00:45` 比如这是第一个请求的前缀
`01:00:47` 你现在要生成这个token
`01:00:49` 这是第二个
`01:00:50` 这是第三个
`01:00:51` 这是第四个
`01:00:51` 因为每个前缀长度都不一样
`01:00:54` 所以看起来参差不齐
`01:00:55` 咱们一步步来抵扣解码
`01:00:59` 每一步里你为所有序列各解码出一个token
`01:01:03` 接下来你要为所有序列解码下一个token
`01:01:07` 一旦某个序列生成结束
`01:01:08` 就直接把它剔除掉
`01:01:10` 接着当有新请求进入批次时
`01:01:14` 你把它加进去
`01:01:15` 然后继续处理啊
`01:01:17` 这就是为什么叫连续批次
`01:01:19` 你的批次是动态更新的
`01:01:21` 旧的序列完成后被踢出
`01:01:23` 新的序列
`01:01:24` 随即进入哎这里有个问题
`01:01:26` 之前我们看到的批量处理
`01:01:28` 前提是所有序列的维度必须一致对吧
`01:01:31` 张亮里的每一个切片维度都一样
`01:01:33` 但这里每个请求的长度不同
`01:01:36` 你该怎么处理
`01:01:37` 这就引出了选择性批次的概念
`01:01:41` 比如假设你有长度分别为39和五的序列
`01:01:45` 3S啊
`01:01:46` 在注意力计算中
`01:01:47` 这确实没法直接处理
`01:01:49` 因为注意力机制依赖于序列的长度啊
`01:01:53` 比如如果是CEXA的运算和啊9X9的运算
`01:01:59` 你就没法有效的共享张量
`01:02:02` 但对于非注意力的部分
`01:02:04` 也就是占用大量浮点运算的MLP层
`01:02:07` 你其实可以把所有序列拼接成一个超大序列
`01:02:10` 然后一起处理好
`01:02:11` 最后一个思路是分页注意力
`01:02:13` Page attention
`01:02:14` 这一机制在vim论文中提出
`01:02:17` 当然现在的VOM拥有许多其他功能
`01:02:19` 但这是当时的核心思想
`01:02:21` 那么问题是
`01:02:22` KV缓存是如何存储的
`01:02:25` 对你想想看
`01:02:26` 请求进来了
`01:02:27` 你得把它们存到内存的某个地方
`01:02:29` 而这通常会带来一个问题
`01:02:31` 就是碎片化嗯
`01:02:33` 这就像以前你的硬盘会遇到的情况
`01:02:37` 那时候你得给硬盘做碎片整理
`01:02:39` 对吧好
`01:02:40` 所以碎片化主要有两种
`01:02:42` 第一种是你必须预留足够的缓冲区
`01:02:45` 这是因为你根本不知道什么时候会停下来
`01:02:48` 所以你可能得设一个最大token上限
`01:02:50` 比如win624
`01:02:52` 所以你得把所有内存都分配出去
`01:02:55` 但里面什么也放不进去
`01:02:56` 因为系统会一直生成
`01:02:58` 直到token数达到上限
`01:03:00` 这就太浪费了
`01:03:02` 这叫内部碎片
`01:03:03` 另外不同请求之间也可能留出一些空隙
`01:03:07` 但这些空隙往往太小
`01:03:09` 没法有效利用
`01:03:10` 也就成了浪费的空间啊
`01:03:13` 所以解决办法是这样的
`01:03:14` 毕竟他们是搞系统的人
`01:03:17` 对操作系统很熟悉
`01:03:18` 他们说好吧
`01:03:20` 既然这个问题
`01:03:20` 我们以前解决过
`01:03:21` 那就照老办法来
`01:03:23` 所以我们要把一个序列的kv cash
`01:03:26` 切成不连续的小块
`01:03:28` 比如果你有这样一个序列
`01:03:29` 87年前我们的先辈创立了这个国家
`01:03:32` 我们就把它切成大小为四的小块儿块
`01:03:36` 具体放在哪并不重要
`01:03:37` 但他们会按块对齐
`01:03:39` 这样整体就有了一定的统一性
`01:03:41` 所以当两个请求共享相同的时
`01:03:46` 它们实际上可以共用同一个kv cash啊
`01:03:50` 所以你可能有这一个块
`01:03:51` 还有那一个块
`01:03:52` 比如这块儿可能放在这里
`01:03:54` 那里那块可能放在那边嗯
`01:03:57` 它们有点交错分布
`01:03:58` 但只要你有索引
`01:04:00` 能追踪到每样东西的位置就没问题嗯
`01:04:03` 特别是嗯如果你有system prompt
`01:04:07` 你其实可以只把这些system prompt的kv cash
`01:04:11` 缓存一次
`01:04:12` 你这个所有查询都很有用
`01:04:14` 这非常关键
`01:04:14` 因为如果很多人用的是同一个system prompt
`01:04:18` 你就不用每个请求都重新计算ky cash啊
`01:04:22` 另外在很多应用场景里
`01:04:24` 你用的是同一个prompt
`01:04:26` 但其实想生成多个回复
`01:04:29` 这种情况下你也可以共享这个prompt的啊
`01:04:33` kv cash只让各自独特的回复输出出来
`01:04:38` 谢谢好
`01:04:39` 那嗯
`01:04:40` 比如如果你要从87年前这个开头
`01:04:44` 生成多段续写R而空白
`01:04:47` 然后嗯这里的情况是
`01:04:50` 你已经有了fourth scorn seven
`01:04:52` 接下来从years ago或者hour开始往下生成for day
`01:04:58` 然后嗯这就叫写实复制
`01:05:01` copy on write语义啊
`01:05:02` 我们保留这部分
`01:05:04` 然后看这两个样本
`01:05:05` 如果他们恰好采样到了相同的token
`01:05:08` 就直接沿用它继续往下
`01:05:10` 但如果他们采样到了不同的token
`01:05:12` 就把这个块拆开
`01:05:13` 然后各自往下继续
`01:05:15` 也就是说
`01:05:16` 你本质上是在尽可能多地共享前缀缓存
`01:05:19` Prefix cash
`01:05:21` 此外还有一系列其他优化
`01:05:24` 比如kernel层面的改进啊
`01:05:26` 时间关系我就不展开讲了
`01:05:29` 那但核心思路就是借用操作系统里的这些概念
`01:05:34` 来管理你的inference
`01:05:36` 好总结一下
`01:05:38` inference真的非常非常重要
`01:05:42` 这和训练非常不同
`01:05:44` 虽然是同一个模型
`01:05:46` 但你让模型做的事情完全不同
`01:05:50` 它最终会变得严重受内存限制
`01:05:53` 而且在实时拆包这种场景下
`01:05:56` 负载还是动态变化的啊
`01:05:58` 我们看到了多种不同的技术
`01:06:01` 为了提升influence
`01:06:02` 你可以做量化
`01:06:03` 可以设计新架构
`01:06:04` 可以做减脂和蒸馏
`01:06:06` 也可以使用推测采样
`01:06:08` 但所有这些方法都遵循同一个原则
`01:06:11` 减少kv cash的占用
`01:06:13` 同时尽量不损害精度
`01:06:15` 此外还可以引入来自系统领域的思想
`01:06:18` 比如啊分页和推测执行
`01:06:21` 针对实际运行的inference服务器
`01:06:25` 有一点我们没怎么来得及展开
`01:06:27` 只是简单提了一下
`01:06:28` 那就是我觉得新架构其实有巨大的改进潜力啊
`01:06:34` 比如状态空间模型
`01:06:35` 线性注意力或者扩散模型
`01:06:37` 在某种程度上KB缓存以及注意力的构建方式
`01:06:42` 从根本上决定了这一切
`01:06:44` 这种架构对inference不太友好
`01:06:47` 所以如果你能设计出一种
`01:06:48` 专为inference而生的新架构
`01:06:50` 而transformer当初并不是这样设计的
`01:06:53` 也许就能释放出巨大的潜力
`01:06:56` 好我就先讲到这下节课
`01:06:59` touch我回来接着讲skating loss的第二部分
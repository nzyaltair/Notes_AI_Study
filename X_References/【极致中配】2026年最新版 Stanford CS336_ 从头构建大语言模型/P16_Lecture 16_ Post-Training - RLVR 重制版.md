---
title: "【极致中配】2026年最新版 Stanford CS336: 从头构建大语言模型"
url: "https://www.bilibili.com/video/BV11LEA6eEuj/?p=16"
bvid: "BV11LEA6eEuj"
cid: "39163398564"
author: "常青藤名校课"
upload_date: "2026-06-05"
created: 2026-08-17
tags: ["bilibili", "clippings"]
---

## 简介

如果这个中文配音版对您理解 CS336 有帮助，欢迎点赞、投币、收藏支持一下，也可以点个关注。
我会持续更新更多 Stanford / AI / 大模型相关课程的中文配音课程，尽量帮大家用更低的理解成本看懂这些高质量课程。你的支持会让我更有动力把后续内容继续做完、做好。

## 字幕

`00:01` 好咱们开始吧
`00:02` 这是后训练系列讲座的第二讲
`00:04` 今天聊聊RLVR
`00:07` 也就是可验证奖励
`00:08` 强化学习这方面进展很令人兴奋
`00:12` 我觉得这事儿挺及时
`00:14` PPT没来得及改
`00:16` 不过OpenAI刚宣布
`00:18` 他们解出了一个earn his难题
`00:21` 他们用思维模型解出了一个未解的数学难题
`00:25` 他怎么做到的
`00:26` 讲的正是这个思维模型
`00:28` 也就是RLVR
`00:29` 目前我们处在图示的左侧对吧
`00:32` 上节课讲完
`00:34` 做了指令微调
`00:35` 有了RL hf
`00:36` 成果不错
`00:37` 都chat gbt
`00:38` 剩下的就是思维模型的前沿进展
`00:41` 模型能做超长思维链推理
`00:43` 解决数学编程这类可验证的难题
`00:46` 记得我上次结尾有点丧
`00:48` 因为过优化问题
`00:49` RL hf没法带我们到想去的地方
`00:52` 所以咱们要去收集一堆偏好数据
`00:55` 这东西很有用
`00:56` 我们建个奖励模型
`00:57` 然后对着它做RL
`00:59` 但这事根本上卡在标注瓶颈上
`01:01` 对吧哦
`01:02` 因为我们有了奖励模型
`01:04` 可不能一直往同一个模型里堆算力
`01:06` 最后你会把奖励模型给过拟合了
`01:09` 正则化做的再好
`01:10` 也躲不过这个问题
`01:11` 所以这挺让人沮丧的
`01:13` 也不是强化学习真正的潜力所在
`01:16` 作为强化学习的坚定信徒
`01:19` 你会看到ALPHAGO这类案例
`01:21` 在这些领域
`01:22` RL确实表现出色
`01:23` 那么我们在ra chef里做的事和那些更原生
`01:27` 更自然的RL领域取得的成果到底有什么区别
`01:30` 我觉得alpha go和RLHF之间有一个主要区别
`01:34` 就在于他们获得反馈的方式不同哦
`01:37` 在ALPHAGO里
`01:38` 我们优化的目标非常明确
`01:39` 比如围棋的胜负条件
`01:41` 这个定义没有任何模糊之处
`01:43` 所以你可以随意投入算力
`01:44` 只要目标在提升
`01:46` 就说明进展良好
`01:47` 某种意义上
`01:48` 这是搜索问题
`01:50` 而前者更像学习问题
`01:52` 这不算精确的区分
`01:53` 但可以这样去理解
`01:55` 所以形式数学甚至自然语言
`01:57` 数学等领域可能也具备这种可验证性
`01:59` 因而更适合强化学习
`02:01` 这就是我们研究这些内容的动机
`02:04` 算法在根本上没太大区别
`02:06` 但最终的成果会出乎意料的不同
`02:08` 今天讲座分两部分
`02:10` 这和咱们课程其他部分的安排差不多
`02:12` 第一部分我讲核心算法
`02:13` 这是大家必备的基础知识哦
`02:15` 呃作业里会用到这些
`02:17` 我会讲讲G是什么
`02:19` 而是PPO怎么运作
`02:21` 接着我会深挖一些开源发布
`02:23` 看看第一部分讲的内容怎么体
`02:26` 现在这些大模型的技术报告里
`02:28` 模型发布
`02:29` 咱们深入聊聊方法和基础部分
`02:32` 我先从PPO讲起
`02:33` 对吧
`02:33` 聊语言模型的RL绕不开PPO虽然讲过一次
`02:37` 但PPO挺绕的
`02:38` 我觉得再讲一遍
`02:39` 大家能受益更多
`02:41` 在语言建模的RL里
`02:43` 最该技术的核心概念是策略梯度
`02:46` 尤其是这种强化梯度技巧
`02:48` 我们本质上一直在对奖励做梯度下降
`02:51` 我们通过加权SFT更新来实现权重可正可负
`02:55` 今儿我们会定义R是什么
`02:57` 这个公式很重要
`02:59` 后续会反复出现
`03:01` 这在某种意义上是衍生出一切的核心
`03:04` 基于此
`03:04` 回想上一讲的直觉
`03:06` 我们想一次多走几步
`03:08` 策略梯度
`03:09` 要求每次梯度更新时都从策略中采样
`03:12` 能复用roll out吗
`03:13` 你可以用TRPOPPO等啊
`03:17` 我稍后会讲PPO
`03:18` 之前讲过TRPO
`03:19` 这里不展开
`03:20` 有上过RL课的话
`03:22` PPO你自然很熟悉这个算法
`03:25` 你是懂的
`03:26` 不过我不会假设大家都上过RL课
`03:29` 或者都理解RF
`03:30` 所以要是你不熟悉PPO
`03:32` 它是RL领域的一种主力算法
`03:35` dot RL它已被用于各种高难度的RL场景嗯
`03:40` OpenAI在早期非常依赖RL时
`03:43` 做过不少这类工作
`03:44` 这是open a gm
`03:46` 他们当时讨论PPO算法
`03:47` 说我们可以做到让人形机器人用强化学习行走
`03:51` 更惊人的演示是他们的OpenAI bar
`03:53` 用PPO训练它在深度RL中表现优异
`03:56` 尤其在高危动作和状态空间那里
`03:59` RL通常更复杂
`04:01` 还有那个到处跑的OpenAI人形机器人
`04:04` 从概念层面看
`04:05` PPO其实非常简单
`04:07` 我记得我提过去掉PPO是这一切的重要一环
`04:10` 这激励了很多人
`04:12` 但你看PPO的伪代码
`04:14` 它来自OpenAI的RL入门文档哦
`04:17` 你会觉得这不难
`04:18` 甚至挺简单
`04:19` 我能一次性实现你采样一些轨迹
`04:22` 我用某种优势估计方法算出优势
`04:25` 我用这种有点怪但还行的方式裁剪优势
`04:28` 然后在裁剪后的优势下更新策略
`04:30` 这没问题
`04:31` 接着再拟合一个价值函数行
`04:33` 这些在实操中看起来都相对简单
`04:37` P a x
`04:37` 所以我觉得大家会觉得这没什么
`04:40` PPPO本身没问题
`04:41` 但看到这类博文
`04:42` 你得心里发毛
`04:43` right博文若列出PPPO的37个实现细节
`04:47` 说明该算法对实现决策极敏感
`04:50` 嗯你可以做各种调整
`04:52` 也有不同的库和实现方式
`04:54` 他们给出的结果截然不同
`04:55` 取决于你用的组合
`04:57` 事实证明许多人实现有误报
`04:59` 有论文指出
`05:00` PPO中某些人用的基线根本不算基线
`05:03` 他们从根本上改变了优化问题
`05:06` 所以这堂课
`05:07` 接下来我不得不带你们看看PPO的一些实现
`05:09` 语言模型的PPO实现
`05:11` 说实话并不令人愉快
`05:13` 这张图很典型
`05:14` 基本把PPU长什么样讲清楚了
`05:16` 但是优势估计中间那个大方块就是优势项
`05:20` 而且有点点缓冲区
`05:21` 因为得留点旧数据
`05:22` 你要训练价值模型
`05:23` 注意优势
`05:24` 估计你用的也是这个模型
`05:26` 绿色方框出现了两次
`05:28` 注意目标函数里的KL项是逐token计算的
`05:32` 所以这不只是个赌博机问题
`05:34` 而是个完整的多步强化学习问题
`05:37` 唉这一切都很困难
`05:39` 很复杂
`05:39` 所以就引出了许多你必须处理的棘手实现细节
`05:44` 你看各种实现会发现PPO有些实现细节挺怪
`05:48` 挺棘手的
`05:49` 我找找看
`05:50` 之前我有个学生做RNHF项目时实现了PPPO
`05:54` 大家还记得PPO在RLHF1的应用吧
`05:56` 所以我们手头有个很稳的PPO
`05:58` 参考实现交通花了挺久
`06:00` 我研究细节到底做了啥
`06:02` 才跑起来的
`06:02` 外层循环挺好
`06:04` 就是便利roll out
`06:05` 然后跑几步PPO把算算损失裁减梯度
`06:09` 再更新参数
`06:10` 这外层循环挺合理的
`06:12` 但细看的话
`06:13` 算损失那部分基本就是照搬PPO的更新逻辑
`06:17` 这也看着没问题
`06:18` 我们再算优势函数和裁减比率
`06:21` 接着就更新模型
`06:22` 所以整体看下来都挺好
`06:24` 但后面有些乱
`06:25` roll out先跳过
`06:27` 得用kl惩罚
`06:28` 把模型往参考模型上拉
`06:30` 不是这得把kl散度在零处阶段才行
`06:32` 懂kl散度的都知道
`06:33` 这完全违背了他的本意对吧
`06:35` 正负值都在累加
`06:37` 要是去掉这个限制
`06:38` 数值立马就炸了
`06:40` 这类情况都可能发生
`06:41` 但这不代表你做PPO实现时就该这么搞哦
`06:44` 提这个是想说RL算法的实现挺棘手
`06:47` 很敏感
`06:48` 毕竟PPO组件多梯度估计的方差又大
`06:52` 这些细节都非常微妙
`06:53` 处理起来很复杂
`06:55` 还有个很常见的现象
`06:57` 不光我们这么干
`06:58` 大家都这么用
`06:58` 原始PPO论文里有个广义优势估计器
`07:01` 主要看GMA折扣奖励
`07:03` 用值函数估算每个token生成步骤的奖励
`07:06` 在但大家长直接把gamma和lambda都设成一
`07:09` 这其实是个退化的设置哦
`07:12` 这就变成了多臂老虎机
`07:13` 问题相当于你丢掉了PPO原本的结构
`07:17` 所以只要工程到位跑
`07:19` 这个算法就能达到预期效果
`07:21` 抛RL作业时
`07:22` 你会看到奖励上升
`07:23` 若做RLHF奖励模型得分也会升
`07:26` 而负KL正则化项则下降
`07:28` B呃
`07:29` Just just alex now
`07:31` 好
`07:31` 但总之PPO的实现挺折磨人
`07:33` 尤其看环境得靠技巧才能稳定
`07:36` 整个过程都很折磨人
`07:38` 当然不是不可能
`07:39` 很多人用PPO都训出了好模型
`07:41` 很多实验室都有现成方案
`07:43` 让PPO能大规模跑通
`07:45` 所以这事儿能做
`07:46` 但对从零实现的研究者来说
`07:49` PPO既挑剔又复杂
`07:51` 嗯PPO在很多情况下不受青睐
`07:54` 还有一个原因
`07:55` 它需要一个价值模型
`07:56` 边生成边估算每个token的价值
`07:59` 价值模型有多大
`08:00` 跟原始模型一样大
`08:01` 就会占内存
`08:02` 而这些内存你本想留给模型
`08:04` 或推理服务器等用途
`08:06` 你现在可能会说
`08:07` 上一讲你提到了DPO
`08:08` 看起来挺不错
`08:09` 我那为什么不干脆全用DPO呢
`08:12` 问题是DPO是针对特定问题的特定解法
`08:16` sense DP o适用于bradley terry形式的成对反馈
`08:20` 这太具体了对吧
`08:22` 如果我要解数学题
`08:23` 题目并不是天然的成对比较形式
`08:25` 由别的DPU变体想打破这种成对结构
`08:28` 但这其实是在用错工具对吧
`08:30` For
`08:31` 别想着用DPO去做PPO通常做的事
`08:34` PPO是更通用的工具
`08:36` 几乎什么问题都能解决
`08:37` DPO一般是离线的
`08:39` 不过我觉得这个区别被过分夸大了
`08:41` 因为反复迭代DPO也能把它变成在线的
`08:44` 所以我觉得这差别没那么本质好了
`08:47` 这就是聊这些的背景
`08:49` 同样研究界也特别希望不必再用PPO了
`08:53` DPO和gr po能被广泛采用
`08:56` 这本身就说明很多时候让PPO跑通有多痛苦
`09:00` 所以gr po和DPO一样是另一种选择
`09:04` 它是给可验证任务做强化学习的更简单的办法
`09:07` 在开源开放知识的社区里
`09:09` RLVR基本已经被gr po接管了
`09:12` 他骨子里跟PPO非常像
`09:14` 只是砍掉了几个最复杂的部分
`09:16` 这样一来就让算法简单多了
`09:18` dog r p l出自deep seek的一篇论文
`09:21` 我不想是他们那篇deep seek数学论文
`09:23` 从PPO起步
`09:25` 思路上是说PPO挺好
`09:27` 但我们只想改几个地方
`09:28` 真的那改了什么
`09:30` 改的是PPO里最复杂最烦人的部分
`09:33` 价值函数加之函数就是作为基线减去的那个量
`09:37` 用来降低梯度更新的方差
`09:39` 价值函数是一整个神经网络
`09:41` 会让训练不稳定
`09:42` 我们不想要它
`09:43` 那怎么办
`09:44` 去掉价值网络
`09:46` 但我们还需要优势
`09:47` 估计我不想用普通的ryan force
`09:49` 因为它的方差会非常非常高
`09:51` Group
`09:52` 所以我们要做的是
`09:53` 在组内把优势算成一个Z分数
`09:56` 我稍后解释这是什么意思
`09:58` 基本思路是这样的
`09:59` 你通常你只看获得的奖励
`10:02` 如果有价值函数
`10:03` 你会拿它和你的预测值比较
`10:05` 也就是预测值
`10:07` 你会说我的神经网络说我本该得五分
`10:10` 结果我得了六分
`10:11` 所以这是个不错的展开
`10:12` 通常你会这么做
`10:13` 但换个做法
`10:14` 比如你拿到一个得五分的展开
`10:16` 然后你再采样另外十个展开
`10:18` 问自己比起这十个我表现得怎么样
`10:20` 嗯啊如果我比均值好
`10:23` 优势值就高
`10:24` 某种程度上
`10:26` 这是计算优势值很自然的一种方式
`10:28` 也适用于能从同一个提示词多次采样的场景
`10:33` 嗯GRPO正是这个思路啊
`10:35` 如果你看deep seal文里GRPO最初的定义
`10:38` 会发现这就是被优化的目标
`10:40` 那么会做标准的PPU更新
`10:42` 加上最小阶段优势项和kl项
`10:45` 让模型别偏离参考模型太远
`10:47` 接着是kl项目具体怎么算的倒不重要
`10:51` 关键是这个优势值是算一组输出样本
`10:55` O1到OG的Z分数
`10:56` 不就是在组内
`10:57` 你算第I个样本的优势
`10:59` 这就是算优势的地方
`11:01` 做法是取组内roll out奖励减去均值
`11:04` 再除以标准差
`11:06` 所以每组得到一个Z分数
`11:08` 在线情况下
`11:09` 这点很关键
`11:10` 因为很多场景下你其实是在线跑
`11:12` 不是离线嗯
`11:13` 在线情况下截断向其实没用了
`11:16` 因为新旧策略的比率就是一
`11:18` 这个截断操作根本没用
`11:20` 你直接取main ini AI a就行
`11:22` 这就是优势函数减去KL惩罚
`11:25` 所以我要用这个定义的优势函数做强化学习
`11:29` 在在线roll out场景中
`11:30` 这是个非常简单的对象
`11:32` 很适合作为强化学习的目标
`11:34` Here
`11:34` 要是gr b o的某些概念没搞清
`11:36` 我可以先停一下
`11:37` 某种程度上
`11:38` 它是那些精彩数学内容的基础
`11:40` 你甚至能用GERPU复现它们
`11:43` 所以在深入那些细节之前
`11:45` 咱们最好先透彻理解一下
`11:47` 好不错
`11:48` google简单
`11:49` 大家一眼就懂了
`11:51` 这评价我收下了GRPO确实非常简单
`11:55` First day
`11:56` 因为没有价值函数
`11:57` GRPO的代码很简单
`11:59` 只需做case roll out
`12:00` 把结果和这K个参考值Z对比就行
`12:03` 对结果打分
`12:04` 然后针对这些权重做强化梯度更新
`12:07` 作业里就是几步简单操作
`12:10` 算出kids roll out的奖励并归一化
`12:13` 再加上KL项
`12:14` 最后把它们放一起更新梯度得动点脑筋
`12:18` 因为用自动微分实现时得在某处加个stop grad
`12:21` 不过其实没那么复杂
`12:23` 你看这个核心思想
`12:24` 一页代码就能实现
`12:25` 对吧啊
`12:26` 这是我从麦吉尔大学几位同事那里拿到的
`12:30` gr po参考实现
`12:31` 有个很棒的简化版
`12:33` 足索引和标准差
`12:34` 这些计算半夜幻灯片就讲完了
`12:37` 跟论文写法唯一不同的是
`12:39` 他们在标准差计算里加了个极小的1E4
`12:42` 防止只有单个样本或样本奖励刚好相同
`12:45` 时数值爆炸
`12:45` 比如在某些领域奖励会精确为零
`12:48` 像没解出数学题
`12:49` 这种情况确实会发生的
`12:51` 希望这能让你看到GRPU有多简单
`12:53` 也明白为什么几乎所有开源工作都基于它
`12:56` 他好写也好懂
`12:59` 结果也显示
`13:00` 他给出的效果相当有说服力
`13:03` 他模拟了闭源实验室用PPO做过的那些事
`13:08` 他到底行不行
`13:10` 如果你搞数学AI
`13:11` 强烈建议读读那篇deep sc数学论文
`13:14` 结果显示
`13:15` gr po黄线和蓝线比RFT拒绝微调强得多
`13:20` 你会把它当基线
`13:21` 只拿模型生成的正确答案来训练
`13:24` 其他的都扔掉次其他呢研究表明
`13:27` 过程监督即不仅看答案
`13:30` 还看中间步骤能带来提升
`13:33` 我后面细说
`13:34` 这很重要
`13:36` 因为这是这类强化学习问题的关键设计决策
`13:39` 现阶段大家只要记住GRPO有效就行
`13:42` 你看蓝线和黄线表现都优于其他组好
`13:47` 咱们再深入聊聊GRPO的目标函数啊
`13:50` 这目标函数里到底发生了什么
`13:52` 我们真的在用策略梯度吗
`13:54` 这几个关键概念咱们得好好想透
`13:57` 咱们回想下GRPO和PPO的区别啊
`14:00` PPO里有个价值函数啊
`14:02` 用来计算优势的
`14:03` 而G2PO去掉了那部分
`14:05` 改用Z分数
`14:06` 也就是用奖励减均值再除以标准差来算优势
`14:09` 对这优势估计算好吗
`14:11` 上过二幺课的同学估计心里已经有答案了
`14:15` 那到底什么样的优势函数才算好
`14:17` 或者说有效的优势函数标准是什么
`14:19` 咱们看看SUTTONBARTO那本强化学习经典教材
`14:22` 里面提到了一个带基线的r in def or cs算法
`14:26` 做策略梯度时
`14:27` 你可以沿奖励方向更新参数
`14:30` 你可以用奖励减去所谓的状态依赖基线
`14:36` 在老虎机问题里
`14:37` 状态就是提示词
`14:39` 你可以减去依赖提示词的基线
`14:42` 只要这么做就是有效的策略
`14:45` 梯度方向不变
`14:47` 但基线选择会影响梯度下降的方差
`14:50` 诶注意我们不只是做减法
`14:53` 我们不只是减去一个常数
`14:55` 还除以标准差
`14:57` 这算个问题
`14:58` 但也未必bird
`15:00` 如果你真想要一个概念清晰
`15:02` 名副其实的算法
`15:03` 它其实是在让奖励下降
`15:06` 特g r p o boot这么做
`15:08` 总之我用红色标出的GRPO除以标准差
`15:12` 这违背了基线原则
`15:14` 我们不仅减去基线
`15:15` 还要除以标准差做归一化啊
`15:18` 针对我的奖励
`15:19` 嗯对了
`15:20` GRPO还有个细节
`15:21` 我之前没提
`15:22` 有点棘手
`15:23` 它几乎是针对每个token操作的对吧
`15:25` 所以他们会用序列总长度作为归一化因子
`15:29` 来除本
`15:31` 但如果你遵循策略梯度和极限定理
`15:34` 从第一性原理推导GRPO结果会不同啊
`15:38` 你既没有长度归一化
`15:39` 也没有标准差归一化对吧
`15:41` 那GRPO刚出不久
`15:43` 有人注意到这点
`15:44` 并发表论文指出
`15:46` 如果不做这两处改动
`15:47` 结果会很不一样
`15:49` 甚至可能表现更好
`15:51` 等讲到DPC高一论文时
`15:53` 我也会聊这个呀
`15:54` 不过核心在于
`15:55` GRPO并不是从第一性原理推导出来的
`15:58` 他其实做了些不一样的事
`15:59` 利弊兼有
`16:00` 那这两项怎么起作用
`16:02` 现在我们知道
`16:03` GRPO并没有直接对奖励目标做下降
`16:06` 所以我们知道它有两个修正因子
`16:08` 干嘛
`16:09` 用的长度归一化比较好懂
`16:11` 我们在干嘛
`16:12` 是拿较长的序列去除以一个较大的数
`16:16` 就会鼓励模型生成长输出对吧
`16:18` 因为当你错了
`16:19` 他会鼓励你生成错误输出
`16:21` 比如我知道这道数学证明会做错
`16:23` 我会引入负值
`16:24` 比如一的负奖励
`16:25` 我就生成无限长的字符串
`16:27` 这样我就能除以无穷大
`16:29` 彻底抵消那个负惩罚
`16:30` 我们这是极端情况
`16:32` 但道理很清楚
`16:33` 除以输出长度会鼓励模型在解不出题时
`16:36` 一直废话
`16:37` 这个是你最终要面对的长度问题啊
`16:40` 解决了它
`16:41` 你会发现GRPO里COT4位链的长度
`16:44` 往往最后会稳定在一个常数
`16:47` 而不是无限增长
`16:48` 就是看起来是好事
`16:49` 特别是在答错时
`16:50` 你肯定不想让输出越来越长
`16:53` 然后是标准差归一化
`16:55` 这个稍微复杂点
`16:57` 但你想我其实是在除以标准差
`17:00` 所以这相当于在强调那些标准差较小的问题
`17:04` 那什么时候二元奖励问题的标准差会小呢
`17:08` 就是问题太简单或太难的时候
`17:11` 如果问题很简单
`17:12` 我总是全对
`17:14` 那我就要做除法了
`17:15` 我基本上是在除以零对吧
`17:17` 因为我总是答对就没有波动
`17:19` 同理如果我总是答错
`17:21` 奖励也就没有波动了
`17:23` 我会显著加大它的权重对吧
`17:25` 所以标准差项放大了两端
`17:27` 也就是简单和难题的权重
`17:29` 这显然不是我们想要的
`17:31` 因为我们希望模型在他能力范围内的题目上
`17:34` 学习好了
`17:35` 以上就是RLVR的算法组件啊
`17:38` 接下来我会拆解几个最近的模型发布
`17:41` 聊聊我觉得关键的算法部分
`17:43` 比如我今年新增的智能体功能
`17:46` 这对模型部署和使用越来越重要
`17:48` 讲之前我先停一下
`17:51` 谁有算法问题
`17:52` 我先回答再继续往下讲
`17:54` 行没问题
`17:55` 那咱们先聊deep seek r1
`17:57` 毕竟他现在挺火的呃
`17:59` 大家都该了解一下deep seek r1
`18:01` 这论文很棒
`18:02` 开启了开源RLVR模型的浪潮
`18:05` 它最牛的地方是首个在表现上追平了OpenAI o e
`18:11` open AI o e的关键在于它有很长的思维链
`18:15` 这显然是经过RL训练的
`18:17` 在难题上表现很好
`18:19` 他们还提供了一套任何人都能用的RL方案
`18:22` 从研发角度看
`18:24` 这很重要
`18:24` 要是只有deep sick能跑那套糟糕的PPO
`18:27` 影响就小多了
`18:28` 至少对我们研究者是这样子
`18:30` 这这种GRPO谁都能玩
`18:32` 哪怕你做作业时也能用
`18:35` 最后啊他们有些有趣的蒸馏见解
`18:38` 到现在依然管用的啊
`18:40` deep seek r1的起点
`18:42` 你能看出他的专长是怎么一步步搭起来的
`18:44` 我们发了篇deep seek数学论文
`18:46` 搞GRPO早就把RL的门道摸透了
`18:49` 针对数学题
`18:51` 所以他们就从deep seek数学的那些组件起步呃
`18:55` 有个重要区别
`18:56` 他们放弃了过程监督
`18:58` 而这种方法在deep sk数学任务中曾很管用啊
`19:02` 他们只用结果监督
`19:03` 简单说就是只看最终答案对错来给奖励
`19:07` 过程监督则是用评分标准
`19:09` 比如检查证明中间步骤是否有效
`19:12` 很多人以为过程监督很重要
`19:15` 结果发现他对很多任务其实没那么关键词
`19:19` 所以RED很有意思
`19:20` 因为它包含不少不同的组件
`19:23` 但有个特别有趣的组件是R102in
`19:26` 他们在那没做太多后训练
`19:28` 他们有个基础模型
`19:30` 当然大家现在都知道
`19:31` 这个基础模型也经过中期训练哦
`19:33` 所以这个模型能做一些指令遵循
`19:37` 然后在这个基础模型之上
`19:39` 他们主要做RLVRGRPO算法的奖励
`19:42` 就是看能不能正确解出一堆数学题
`19:44` 问题与格式奖励
`19:46` 这很关键
`19:47` 因为格式奖励能让他们后续剥离思维链
`19:49` 他们希望模型规范使用思维标签来包裹COT
`19:54` 但这套方案很简单
`19:55` GRPO很简单
`19:56` 准确性奖励也很简单
`19:57` 事实上你基本能浮现这些结果
`20:00` 只要照做
`20:01` 结果会怎样嗯
`20:02` 效果只比OpenAI o e略差一点对吧
`20:05` 所以这个非常纯粹
`20:06` 我喜欢这个结果
`20:07` 因为他没有真实训练流程的那种杂乱
`20:09` 但没有把生产后训练流程混进来
`20:12` 你无需纠结是RLHF起了作用还是别的因素
`20:15` 极作模型加GRPO方案很简单
`20:17` 数学能力相当不错
`20:19` 我认为R1的走红部分
`20:22` 也源于论文中声称观察到的有趣现象
`20:25` 而他们声称观察到的现象之一是
`20:27` 随着训练时间延长
`20:29` 模型能思考更久
`20:30` 哈奇越来越长
`20:31` 他们还提到了网上疯传的顿悟时刻
`20:34` 训练足够久后
`20:35` 模型的COT就会出现顿悟tile
`20:38` 我觉得这一点挺有意思的
`20:39` 但我不确定这两种现象是否真的存在
`20:44` COT其实是GRPO
`20:46` 算法长度归一化的自然结果
`20:50` 至于顿悟时刻
`20:52` 别人已经证明基础模型里就有
`20:55` 所以这肯定不只是强化学习算法的功劳
`20:58` 嗯很多人说顿悟这能帮我解数学题
`21:02` 模型在预训练时就学会了
`21:05` 因为它生成了很多数学token
`21:07` 强化学习时顺手提取出来
`21:09` 这太正常了
`21:10` 我觉得那个现象没那么特别或刺激
`21:12` 但R1确实是个重要里程碑
`21:14` 他展示了RLVR有多简单
`21:16` 这算是一种很干净的算法
`21:18` 也是解决难题的好方法
`21:20` 所以R1.0是个很干净受控的环境
`21:24` 接着re e是他们想把系统投入实际应用的尝试
`21:28` R1有个好处
`21:29` 待会我会拿QUEN来对比诶
`21:31` 你能看到怎么把这些模块拼起来对吧
`21:34` 我们之前都孤立研究过这些部分
`21:36` 知道预训练RLHF和SFT在哪
`21:39` 但怎么把它们组合成最终系统呢
`21:41` 通常做法是我们先用中期训练好的模型诶
`21:44` 然后做推力训练和长上下文扩展
`21:47` 最后做RLAJEFF
`21:48` 毕竟这是直接面向用户的关键步骤
`21:50` 我们要确保格式规范
`21:51` RLAJEFF能很好的辅助这一过程
`21:54` 另外他们给生产模型的coo t加了语言
`21:57` 一致性奖励
`21:58` 我记得报告提过
`22:00` 没加一致性奖励的话
`22:01` R10风格的训练在COT里会乱切语言
`22:04` just他们觉得这太难解释
`22:07` 看着也让人有些不安
`22:08` 所以想把它弄清楚
`22:10` 就让他统一输出一种语言
`22:12` 主要是出于可解释性考虑
`22:15` 他们还把一些不可验证的奖励塞进GRPO
`22:18` 几乎都融进ROHF流程里了
`22:20` 但除此之外两者非常相似
`22:23` 我觉得整个流程很清晰哦
`22:26` 嗯做SFT时他们的做法相当简单
`22:30` R1.0里他们完全不想做SFT
`22:33` 但R1愿意用长CUT数据
`22:36` 读读这些开源技术报告时
`22:38` 琢磨字里行间的意思挺有意思
`22:41` 这儿也能看到他们说
`22:43` 我们构建并收集了少量长coo t数据
`22:46` 你可能会琢磨
`22:47` 我猜这是不是从别的模型蒸馏来的
`22:49` 这对开源模型倒不算坏事啊
`22:52` 但大家都这么干
`22:53` 但这措词写的这么小心
`22:54` 我觉得挺有意思
`22:55` 收集少量长COT数据啊
`22:58` 这是用来微调模型的
`23:00` 我们知道基础模型够强
`23:02` 光靠长cut做监督微调
`23:04` 就能激发大量类OE的能力
`23:06` 这是强化学习的绝佳起点
`23:09` 他们从这里起步
`23:10` 加入验证来过滤这些COT再继续处理
`23:13` 例如此后许多论文
`23:15` 包括我学生的部分工作表明
`23:18` 只要蒸馏流程和基础模型合适
`23:21` 光靠监督微调就能获得长COT推理能力
`23:25` 并光靠监督微调就行
`23:27` 我觉得这很有趣
`23:28` 因为有个味觉问题是你真需要强化学习吗
`23:31` 谈到强化学习在语言建模中的作用
`23:34` 一种思路是它是极好的监督信号来源
`23:37` is若解决前沿数学问题
`23:40` 你根本拿不到详细长COT的监督信号
`23:43` 强化学习能让你自我生成
`23:45` 但一旦有了这些长COT
`23:47` 你也可以从模仿中学习
`23:48` 这似乎部分印证了那些蒸馏结果
`23:52` R1的强化学习部分跟20基本一样
`23:54` 只是去掉了我提到的语言一致性损失
`23:56` 我们主要用gr po做强化学习训练
`24:01` 这就构成了最后阶段
`24:04` 这就是上一讲的内容
`24:06` 先做基础指令
`24:07` 微调式的SFT
`24:08` 再做ROHF
`24:10` 对于不可验证的任务
`24:12` 他们基本还是沿用DPCV3那套方法
`24:15` 这里没什么好惊讶的
`24:16` 那效果到底怎么样哦
`24:18` 效果真的非常好
`24:19` 我想这就是deep seek引发恐慌广受关注的原因
`24:22` 毕竟R1确实是个很好的模型啊
`24:25` 它在很多类别上超过了O1
`24:27` 也符合大家预期的测试时扩展表现
`24:30` 而且它的配方非常简单
`24:32` 所以增益来自哪里
`24:33` 很容易理解
`24:34` 关于之前讨论的蒸馏
`24:36` 你可以把R1的思维链coo t输入给quin
`24:39` 2.5boss
`24:40` 这样做能显著提升这些模型的性能
`24:44` 在某些情况下能媲美许多专用模型I
`24:47` 也就是说
`24:48` 只要给模型能读懂的合适
`24:51` 长思维链通常就能让它长时间推理
`24:54` 基础模型本身就出奇的好
`24:57` 这对lama模型也适用
`24:58` 我觉得挺有意思
`25:00` 最后总结一下deep seek
`25:02` 我很喜欢他们的技术报告
`25:04` 因为既做消融实验
`25:06` 也坦诚公布失败的尝试
`25:08` 对
`25:08` 如果你一直关注他们的动态
`25:10` deep sick math里这些都有哦
`25:12` 比如过程奖励模型说要验证每一步
`25:15` 可你读R1时会问过程奖励模型哪去了
`25:18` 其实论文里讲了过程奖励模型的下落
`25:21` 我们试过让它生效
`25:22` 他们没帮上什么忙
`25:24` 事实证明
`25:25` 结果奖励模型效果很好诶
`25:27` 数据量足够
`25:28` 也更容易扩展
`25:29` 过程奖励模型的主要争议在于分布
`25:32` 评分标准从哪儿来
`25:33` 这很难扩展
`25:34` 现在很清楚
`25:35` 结果奖励模型很好
`25:37` 这也是当下的重点所在啊
`25:38` 同样如果你关注过出版一
`25:41` 很多人曾猜测OpenAI的一内部到底用了什么
`25:44` 大家当时想哦
`25:45` 他们是用过程奖励模型吗
`25:47` 还是像ALFGO那样做数搜索
`25:49` 他们也试过尝试了大量蒙特卡洛树搜索
`25:52` 但表示没能很好地跑通
`25:54` 我想强调的是
`25:56` 他们很坦诚的公开了所有探索和失败的尝试
`26:00` 而不是说他们根本没试过好
`26:02` 那我先停一下
`26:03` 大家有问题可以提
`26:04` 虽然我也不确定有没有解释了
`26:07` 当内部处理不再适用时
`26:09` 阳性案例的情况
`26:12` 就像你说的阴性案例里
`26:13` 我们鼓励除以无穷大和零对阳性案例
`26:17` 我们倾向于这样做
`26:18` 问题是长度归一化器起什么作用
`26:20` 当然这能鼓励缩短COT
`26:22` 如果你想省退理成本
`26:24` 这很好啊
`26:25` 但要是准确率下降就不好了
`26:27` 主要问题是阴性案例会生成超长回复
`26:30` 阳性案例说不了那么多
`26:32` 因为解决特定问题的COT有长度下限
`26:35` 我想我是想弄懂刚才那个图表的解释
`26:39` 对DOCTRGRPO的图更直观
`26:41` 你看R1的图
`26:42` 这里把所有数据都汇总了嗯
`26:45` 这里囊括了评估里的所有回复
`26:47` 不只有那些正确的
`26:49` 咱们再看DOCTRGRPO的图
`26:52` 这条是平均长度
`26:53` 这条是错误长度则啊
`26:56` 你看正确长度在这
`26:58` 所以正如我们预期的增长
`27:00` 主要是由错误输出驱动的
`27:01` 所以在这些受控对比中
`27:03` 结论非常清晰哦
`27:05` 好呃
`27:08` 接下来我还会讲一种替代方案
`27:11` Kimi k1.5
`27:12` 也就是另一篇论文的方法
`27:14` 聊到二一时
`27:15` 我觉得必须提一下KIMIK1.5
`27:17` 因为他们几乎是同时出的
`27:19` 他们还击败了O1
`27:21` 然而大家都在追捧deep seek
`27:23` KIMI却很少被提起
`27:25` 尽管他们的模型非常出色
`27:27` 我说这些并不只是出于同情
`27:29` 我觉得KIMI有个不错的地方
`27:32` 他们在好些方面做的很不一样
`27:34` 跟deep sk不一样
`27:36` 这两套都行得通
`27:37` 我们能从中学到不少东西
`27:39` 这也是咱们这门课的主题之一
`27:41` 读读技术报告
`27:42` 看看更大的趋势
`27:44` 我们就能大致摸清哪些路子
`27:46` 能让这些算法跑通
`27:47` 所以他们在数据集构建和强化学习
`27:51` 课程生成上讲的很细
`27:53` 而很多人觉得这一点特别关键
`27:55` 他们用的强化学习算法也不一样
`27:58` 虽然有些思路相似
`27:59` 但并不是GRPO
`28:01` 所以这又是一个很好的验证
`28:03` 技术上讲
`28:04` 我觉得你未必非得用GRPO
`28:05` 才能让这些模型跑起来
`28:07` 谢谢好
`28:08` 我想先讲第一部分数据诶
`28:10` 数据对这些过程依然至关重要
`28:13` 而对RL来说还多了一层课程式设计的考量
`28:17` 你想投入什么难度的问题
`28:19` ST里你不太用考虑难度
`28:21` 直接把数据塞进去就行对吧
`28:23` 不管你想要什么数据
`28:25` 都用预训练损失模型就被迫从中学习啊
`28:28` SFT就要更微妙一点
`28:30` 我们聊过不少幻觉的问题
`28:32` 有时候你并不想用某些数据来训练right
`28:35` 但对RL来说还有个额外的麻烦
`28:38` 如果问题太难
`28:39` 你就拿不到奖励对吧
`28:40` 没有奖励就没有信号
`28:42` 没有信号就学不了
`28:44` 所以广泛覆盖正确类型的事例非常重要
`28:47` 那么KIMI团队的做法是
`28:49` 首先他们力求获得一大批广泛的数据覆盖
`28:53` 这不意外
`28:54` 他们排除多选题
`28:55` 因为很多领域都涉及了
`28:57` 他们想要需要深度思考的内容
`29:00` 理由可能是耶多选题没这效果
`29:03` 但最后这点我觉得可能是最重要的理念
`29:06` 而这在很多RL论文里都有对吧
`29:08` 你基本上是根据最佳K筛选器来过滤所有样本
`29:11` 看是否通过啊
`29:12` 抱歉是用最佳K筛选器来过滤
`29:15` 所以这里你拿样本来说
`29:17` 如果在最佳八次里成功了
`29:19` 比如采样八次模型至少成功一次
`29:22` 那它就不是好的RL样本
`29:24` 因为它已经卡在模型能力的边缘了
`29:27` 对吧
`29:27` 你教不了他什么特别新的东西
`29:30` 所以你只看那些没通过这关的样本
`29:33` 因为就能让我们挑出合适类型的问题
`29:35` 所以这能省算力
`29:36` 它让你能直接跳过某些问题
`29:38` 你也可以两头过滤
`29:39` 挑出既不太难也不太简单的问题呃
`29:43` 研究界普遍认为
`29:44` 如果你真想让RL稳步进步
`29:47` 做这种中等难度的筛选是非常好的
`29:50` 和deep sick一样
`29:51` 没提到SFT
`29:53` 我们可以推测那是什么
`29:54` 但没有具体信息哦
`29:56` KIMI有趣之处在于
`29:57` 他用了受DPU启发的论证
`29:59` 但最终落点和GRPU非常相似
`30:02` 它们结果相似
`30:03` 说明设计新2L算法时
`30:05` 哪些组件可能有用啊
`30:07` 他们的起点和大家完全一样
`30:09` 所以这些你们现在应该都很眼熟了
`30:11` 也就是在策略下最大化期望奖励
`30:14` 右边的kl正则画像
`30:16` 让策略贴近基础策略
`30:18` 甚至贴近上一轮迭代的策略
`30:20` 具体取决于你怎么做
`30:21` RL他们沿用DPO风格的推导
`30:24` 假设能解析D最大化
`30:26` 再解除对应的奖励模型
`30:29` 这就得到策略的比率
`30:31` 把它带回上面的目标函数
`30:33` 看看会得到什么
`30:34` 他们现在的思路是试着去匹配奖励模型
`30:38` 要让这个等式成立
`30:40` 它在最小值处确实成立
`30:42` 这是个大胆的启发式
`30:44` 既然在最小值数相等
`30:46` 何不直接对它加平方损失来最小化优化
`30:49` 专家看了会震惊
`30:50` 但这直觉在某些方面完全合理
`30:53` 模型好时
`30:54` 两者应该接近
`30:55` 所以把显示拉近二者当做代理目标也行
`30:58` 你看这推导和GRPO完全不一样
`31:01` 我们不走PPU路线来推导这个更新
`31:04` 对L求梯度结果惊人
`31:06` 内向加了正则项的GRPO
`31:08` 而最终得到待机线r bar的策略梯度
`31:12` our bar其实就是各类X条件的平均值
`31:16` 有你还加了一个正则画像
`31:19` 本质上就是GRPO里的kl项
`31:22` 所以某种意义上
`31:23` 我们用完全不同的方法
`31:25` 重新发明了阻均值归一化基线
`31:30` 谢谢我觉得KIMI团队对长度问题的看法更透彻
`31:35` 我觉得GRPO团队会觉得
`31:37` 长度不受控的增长挺好啊
`31:39` 亚都can need this job
`31:41` 我敢肯定我们的模型变聪明了
`31:43` 这说法有点刻薄
`31:44` 但当你把这张图当成好事来展示时
`31:46` 潜台词就是我们的模型表现很出色
`31:49` 思考的更久了
`31:51` 但KIMI团队看长COT问题会说
`31:54` 生成这么长的COT太浪费了
`31:57` 我们其实不想要长coo t
`31:59` 毕竟coo t很烧推理成本
`32:01` 所以他们没长度问题
`32:03` 因为这个目标函数不按序列长度归一化啊
`32:06` 但他们还想更进一步压缩回复长度
`32:09` 这也是语言模型开发中常见的一个重要特征
`32:13` 比如我们希望COT越短越好
`32:15` 我们要解决难题
`32:16` 而我们不想想太久
`32:18` 否则本质上等于补贴用户来说
`32:20` 你是OpenAI用户
`32:22` 买了200美元的专业版模型
`32:24` 却思考一小时啊
`32:25` 这处境可不好对吧
`32:26` 要是每次只思考5分钟
`32:28` 那就很理想啊
`32:29` ITH这些原因KIMI想压缩COT怎么做
`32:32` 最简单的RL做法是加个启发式长度奖励
`32:37` 并入RL流程
`32:39` 这个奖励机制复杂的出乎意料
`32:41` 他们做的是让更长的序列变得越来越短
`32:45` right正确答案也应该短
`32:47` 这里的微妙平衡在于
`32:49` 如果把错误答案压得太短
`32:52` 他们就再也无法翻身了对吧
`32:54` Serve
`32:55` 那我举个例子
`32:56` 假设我几何很差对吧
`32:57` 就像我是个AI
`32:58` 几何很差
`32:59` 会算错很多题
`33:00` 错误答案
`33:01` 而惩罚机制让我的几何COT变得非常短
`33:05` 现在我的几何COT归零
`33:07` 我几何太差
`33:08` 再也翻不了身了
`33:09` 我永远拿不到正的几何奖励
`33:11` 彻底卡死
`33:12` 所以你要做的是
`33:13` 别硬把错误答案压得太短啊
`33:15` 一直激励他们比平均水平稍短一点
`33:18` 这样错误内容就不会无限增长
`33:20` 因此最终结果出奇的合理
`33:22` 就像我之前说的
`33:23` 他们在做数据集筛选时
`33:25` 有一套很棒的RL思路给
`33:28` 所以他们不仅有筛选后的数据集
`33:30` 还关注成功率的
`33:32` 他们统计解决特定问题的频率
`33:35` 一旦模型掌握某问题
`33:37` 该问题就会被移出题库
`33:38` 我家能省点算力
`33:40` 我觉得做2L的都会用成功率来过滤
`33:42` 免得浪费算力
`33:43` 或者去啃模型
`33:44` 根本搞不定的难题
`33:45` 他们基本设有奖励机制
`33:47` 写代码的话
`33:48` 就用标准答案再生成一些新测试用例
`33:50` 做数学题的话
`33:51` 他们其实是用奖励模型来检查答案
`33:55` 是否等价
`33:56` 这挺有意思的
`33:57` 毕竟咱们讲座一开始就说要搞形式化
`34:00` 数学这类真正能验证的带的C
`34:02` 让编译器来检查数学
`34:03` 对不对
`34:04` 结果我们讲完了大半
`34:05` 最后呢咱们到底走到哪一步了
`34:08` 我们最终得到了一个奖励模型
`34:09` 用来检查数学答案
`34:11` 对不对啊
`34:12` 至于为什么
`34:13` 你做作业时或者动手试试就会明白了
`34:16` 你很快就会发现
`34:17` 判断答案是否等价是个特别难的问题
`34:21` 因为在数学里
`34:22` 同一个意思往往有很多种写法
`34:24` 不仅如此
`34:25` 语言模型给出答案的方式也五花八门
`34:27` 哪怕你让他用light te把答案框起来
`34:29` 他有时也会忘了加框
`34:31` 稍后者在框里多加些东西
`34:33` 严格的检查期会判错
`34:35` 哪怕模型的思路其实是对的
`34:37` 就是正因为这些原因
`34:38` 大多数RL项目都用了很复杂的检查器
`34:41` 比如正则或模型
`34:43` 就是为了避免这类问题
`34:45` 要把RLVR里呃
`34:48` 验证这部分做对真是个深坑
`34:50` 以one of the necessarily desigch carry against
`34:53` 最后一点就跟前面聊过的
`34:55` 系统架构和推理环节挂钩
`34:57` 就是强化学习
`34:58` 优化和底层基建真的相当难训练
`35:01` 难推理也难强化学习
`35:04` 把这两者揉在一起
`35:05` 难怪它既棘手又困难
`35:08` 呃
`35:09` 更麻烦的是
`35:10` 大家一开始往往低估了
`35:12` 让强化学习高效起来有多难
`35:16` 咱们看个例子好不
`35:18` 假设你有一批ROLOUT生成序列
`35:21` 但其中有道题特别难
`35:23` Make sure you feed them big thing
`35:24` 一道是黎曼猜想
`35:26` 你的模型正在这道题上死磕
`35:29` 它生成了超长的COT思维链
`35:31` 这时候如果你用朴素推理
`35:33` 其他人只能干
`35:34` 等这一个roll跑完才能进入下一阶段
`35:37` 所以作弊处理时常roll out会严重拖慢进度也在
`35:41` 所以RL确实得处理所有这些棘手的底层细节
`35:45` 你得应对COT可能极长的情况
`35:47` 要截断他们吗
`35:49` 怎么把它们放到别的机器上啊
`35:51` 谁知道呢
`35:52` 他都是你能做的决策对
`35:54` 如果你在训练和roll out间切换
`35:56` 比如跑一次
`35:57` 训一次
`35:58` 你就得想办法处理这个切换啊
`36:00` 要么你的部分机器是纯roll out机
`36:03` 器部分是训练机器
`36:04` 要么你一直在切换框架right
`36:07` 两者成本都很高
`36:09` 最后你还面临一个极其棘手又糟糕的权衡
`36:12` 策
`36:13` 略内方法
`36:14` 在数学和训练动力学上都非常理想对吧
`36:17` 所以G2PO在简单的策略内形势下表现很好
`36:20` D你会在作业中体验到这一点
`36:23` 然后你会变得有点贪婪
`36:25` 你会说系统利用率太低
`36:27` 要是能复用roll out就好了
`36:29` 这样就能并行推理和计算
`36:31` 还能做很多优化复用roll会引发离侧问题
`36:34` 导致训练不稳定
`36:35` 非常棘手
`36:36` 所以这些环节最后都会变得非常棘手
`36:38` 现在大多数开源技术报告都会专门讲
`36:42` 他们的RL基础设施
`36:44` DORL基础设施得包含训练部分
`36:47` 左边篮框和推理部分
`36:50` 右边绿框
`36:51` 你得把权重从训练部分移到推理部分
`36:54` 所以需要设计传输方案
`36:56` 他们得紧密配合
`36:57` 有时甚至共用机器
`36:59` 因为推理运行时训练可能闲置
`37:02` 这些都是复杂的地方
`37:03` 你得处理一大堆复杂的系统工作
`37:06` 我们已经知道KIMI胜过open a l o e
`37:08` 而且能看到很好的现象
`37:10` 随着2L推进
`37:11` 模型能想的更久
`37:12` 性能也在提升
`37:14` 但有些情况下
`37:15` 我们并不是无限的对
`37:16` token army map就是个很好的例子
`37:19` 思考时间没怎么变长
`37:21` 但性能还在涨
`37:22` 这正是长度控制起作用的好例子
`37:24` 最后我想聊聊
`37:25` 你可能会好奇
`37:26` 真正的RL是不是比光用正确答案训练更好
`37:30` 这叫专家迭代
`37:32` 过去很多论文里效果都很好
`37:34` 在处理特别不稳定的情况时
`37:36` 你可能反而更想用专家迭代啊
`37:39` KIMIKE的大规模消融实验证实了这一点
`37:43` 这类RL方法的表现一直优于专家迭代
`37:46` 也就是橙色线在这里赢了蓝色线
`37:49` 像榨干性能RL必不可少
`37:52` 关于KIMI部分
`37:53` 大家有问题可以打断
`37:54` 接下来我咱们接着讲
`37:55` 最后一个也就是quin3
`37:58` 最后一个我想聊的是QUEN3
`38:00` 还有quin3.5next coder
`38:03` 名字越来越绕
`38:04` QUEN3虽然晚出
`38:05` 但扩展和数据结果很有意思啊
`38:10` queen3.5next coder很有意思
`38:11` 我觉得他披露了主流报告中
`38:13` 关于AGGARLVR训练的最多细节
`38:16` Q文用的是一套成熟的RLVR方案
`38:21` 好和deep sick一样
`38:23` QN报告的一个亮点是整体组织非常清晰
`38:27` 这和DPC的结构很像
`38:29` 咱们先拿基础模型做SFT再做推理
`38:31` RL他们还有思维模式融合要到一块细说
`38:34` 接着他们做RAJEFF得到一个模型模型上限
`38:38` 但他们当然不想直接拿它来服务
`38:41` 所以做蒸馏来得到小模型
`38:43` 说真的quin3是个挺靠谱的啊
`38:46` 开源模型
`38:47` 你可以把这当成前言语言模型构建的图景
`38:51` 把组件拼起来
`38:53` 这就挺合理了
`38:54` 宽基本用了现在这套经过验证的RLV2打法
`38:58` 我觉得他们吸收了KIMI和deep sig的长处
`39:01` 做的挺好啊
`39:02` 他们大量过滤难度
`39:04` 毕竟这省算力也是拿好数据的好路子
`39:07` 他们还会剔除模型
`39:09` 不用coo t就能答对的题
`39:10` 毕竟这不算思维问题
`39:12` 他们还会剔除跟验证数据太像的内容
`39:15` 给数据去污染
`39:16` 接着他们会对参考coo t做一点人工筛选
`39:20` 昆山最厉害的是他们的LOVR
`39:22` 只用极少量样本才4000个
`39:25` 不过只要其他环节没问题
`39:27` 效果往往能超出预期啊
`39:29` ROVR的核心没啥新鲜的
`39:31` 毕竟它确实基于deep seek r1和KIMI构建
`39:35` 而QU3有些特有做法虽然部分被放弃了
`39:39` 但思路挺有意思
`39:40` 他们有个做法
`39:41` 用标签把思维和非思维内容混在一起
`39:44` 这就是为什么模型会有思考模式
`39:48` 并生成很长的coo t
`39:49` 他们能把这两种模式混用
`39:51` 所以即使响应和长
`39:53` CUT模型其实都在同一个模型里
`39:55` 很多情况下并非如此
`39:57` 甚至在OpenAI思考模式和非思考模型也是分开的
`40:01` 而还有个挺有意思的机制
`40:03` 叫提前退出思考
`40:05` 输入特定字符串模型就会中断
`40:08` coo t直接给出答案
`40:10` 比如CHARGDT界面就有这功能
`40:12` 你可以中途打断思考
`40:13` 直接要个计时答案
`40:15` 我挺惊讶的是
`40:16` 当你调整思考预算
`40:19` 用这种提前终止技巧时
`40:21` 你会发现模型的性能PRISING是平滑下降的
`40:26` 哪怕coo t被截断
`40:28` 他们仍能给出让人惊讶的啊合理回答
`40:31` 哪怕在那种时候
`40:32` 我们一致观察到
`40:34` 即使思维预算很小
`40:35` 思维模式模型在数学和编码任务上
`40:38` 也远胜即时响应模式
`40:40` 后者更像经典的指令遵循加
`40:43` 也就是传统的RL hf模型
`40:46` 因此他们帮我们做了一件好事
`40:48` 就是分析各个不同组件对性能的贡献
`40:52` 这其实不太令人意外
`40:54` 先做推理强化学习
`40:56` 再做通用强化学习就能逐步获得提升
`40:59` 在各种不同的任务里
`41:00` 比如arena hard或COUNTERFEQA
`41:04` 这些通用任务通常需要更常规的RL hf
`41:07` 我们看到它们的性能全面显著提升
`41:10` 数学和编码上确实有些下降
`41:12` 因为我们融合了非思维组件
`41:14` 但下降幅度并不严重啊
`41:16` 我觉得虽然这些绝对数值不大
`41:19` 但在后续版本
`41:20` 比如某些宽3.5里
`41:22` 他们又回到了
`41:23` 把思维和非思维融合进单一模型的做法
`41:26` 不他们以前管这叫混合模型
`41:28` 因为觉得这种下降难以接受
`41:30` 他们想彻底挖掘思维模式的潜力
`41:32` 所以现在把这些模型分开了
`41:35` Two
`41:35` 最后要讲困三
`41:37` 抱歉我刚才顺序说反了
`41:38` 它叫coder next
`41:40` 不是next
`41:40` Coder
`41:41` 是比较新的模型啊
`41:43` 如果你对通用智能体感兴趣
`41:44` 当然还有其他点
`41:45` 比如harness de
`41:46` 如果你想了解怎么构建智能体
`41:48` 这份报告值得读
`41:50` Quit quarter next
`41:51` 一篇优秀的智能体后训练论文
`41:54` 智能体的后训练
`41:56` 其实跟我们之前讲的内容差别不大
`41:58` 并不是有什么新的智能体训练算法
`42:00` 这一课要贯穿整节课去真正内化数据才是关键
`42:05` 所以你要训练或后训练智能体时会怎么做
`42:09` 你得先把数据凑齐
`42:11` 数据分为两部分
`42:12` 要获得大量能力
`42:14` 不能只在最后注入
`42:15` 你可能得更早开始
`42:17` 所以有个很长的中间训练阶段
`42:19` 尽量把代码和类智能体能力塞进模型
`42:21` 比如他们会把仓库里的文件拼接起来
`42:24` 生成很长的上下文
`42:25` 数据喂给模型对吧
`42:26` 模型最终会看到很长的cot轨迹
`42:29` 比如智能体打开一堆文件
`42:31` 这种预训练风格的数据很有用
`42:33` 他们拿来拉取请求
`42:34` 用rag生成合成上下文
`42:36` 帮模型理解这个请求
`42:38` 这些都在中期训练中处理
`42:40` 他们通过自动化方法检测
`42:42` 含有文本和代码的文档
`42:44` 然后用大语言模型转换为规范的markdown格式
`42:47` 一并纳入训练
`42:48` 最后他们让大模型聊聊那些跟编程有关的网页
`42:51` 以此生成一些类似编程的合成数据
`42:54` 然后他们拿公开的编程智能体
`42:57` 在各种环境里跑一跑
`42:58` 这些运行记录全都放进中期训练哦
`43:01` 他们也会做一些常规操作
`43:03` 比如指令遵循
`43:04` 还有个我觉得对我们关系不大的额外任务
`43:06` 补全中间部分
`43:07` 比如编程师补全代码中间部分很有用
`43:09` 所以他们就把这类数据也加进中期训练
`43:12` 总的来说
`43:12` 这是一套标准
`43:13` 但深入的数据收集流程
`43:15` 目的是把一些智能体能力塞进中期训练
`43:18` 而接着科研团队搞了个我头回见的操作
`43:21` 他们拿中期训练好的Q3next的模型
`43:24` 专门去训练一堆
`43:26` 针对各类编程相关任务的专家模型
`43:29` 最后得到四个专家智能体
`43:31` 再把它们全部蒸馏回同一个模型
`43:33` 我只能推测他们这么做的原因
`43:35` 我好像没在其他研究里见过three
`43:38` 我知道最接近的是deep sv3还是V3
`43:41` 是V3.5
`43:43` deep sk v3.5或者3.2
`43:48` 抱歉可的嗯
`43:50` 这是3.2诶
`43:51` 他们做过类似的事
`43:53` 让专家专门负责数据格式和处理
`43:55` 也就是数据处理专家负责生成全量训练数据
`44:00` 但我没见过这种生成不同模型
`44:03` 专家在蒸馏回去的做法啊
`44:05` 这其实更像分支训练合并之类的学术研究
`44:09` 这确实是人们常做的事
`44:10` 但在前沿模型训练中并不常见
`44:13` 不过这些专家其实都挺有意思
`44:15` 他们主要采用完整的RL和S
`44:18` 在他们定义的众多子任务上进行SFT训练
`44:22` 比如他们设有web开发专家
`44:25` 基于各类检查对有效web代码进行sf t诶
`44:30` 他们会构建UX专家训练于多种工具格式
`44:33` 并设有QA
`44:35` 所以啊QA代理只是针对代码的更多
`44:38` 合成数据进行训练
`44:40` 但对我们来说
`44:41` 最有趣也最复杂的是他们的软件工程代理
`44:46` 他们所做的是大规模构建代理环境
`44:49` 因此skid bench是这类任务的黄金标准
`44:53` 他们想要升级版的sv bench
`44:55` 用GITHUB自动化生成海量问题
`44:57` 因此他们想在这些环境里做强化学习
`45:00` 某种程度上并不意外
`45:02` 结果如何
`45:03` 强化学习的表现确实提升了
`45:06` 这里有一点很重要
`45:07` 我们之所以敢不断往强化学习里投入算力
`45:10` 是因为相信奖励模型很难被攻破
`45:14` 一旦这个假设失效
`45:15` 强化学习就会找到越来越隐蔽的作弊方式
`45:19` 偷走你的性能
`45:20` 他们有个很棒的图表
`45:22` 在get里
`45:23` 你可以查看后续的提交或不同的提交
`45:26` 如果有问题且有后续提交
`45:28` 你就能直接查到修复方案
`45:30` 模型很容易学会这种钻空子的手段
`45:34` 他们设了一个奖励
`45:35` 专门用来防止agent搞乱记历史记录
`45:39` 如果不这么做
`45:40` 你会看到模型一直在学
`45:41` 一直在学
`45:42` 然后突然性能跳涨
`45:44` 其实他是学会了操纵get调用去拿到历史记录
`45:48` 有时候它甚至能绕过你设定的某些限制
`45:52` 所以你禁止它用get lock
`45:53` 他可能就去加一个远程仓库
`45:55` 再查特定提交里发生了什么
`45:57` 所以套路五花八门
`45:58` RLDR稳不稳
`46:00` 全看你的奖励设得好不好
`46:02` 而奖励有时根本靠不住你啊
`46:04` 顺便说个事儿
`46:05` 我和一个学生做过一个项目
`46:07` 类似在LIN上做强化学习
`46:09` LIN是个形式化数学验证语言
`46:13` 当时我们天真的觉得
`46:14` 这绝对不可能出问题
`46:16` 很多人都搞过lean lean编译器很稳
`46:19` 结果发现它并不具备对抗鲁棒性
`46:22` 可以插入一些字符串
`46:24` 验证那些在特定模式下本不该被验证的证明
`46:28` 因此我认为可验证奖励这个概念
`46:31` 其实比很多人最初想的要棘手得多
`46:33` 无论如何
`46:34` 当你大规模构建这些GITHUB仓库啊
`46:37` 精力强化学习过程
`46:38` 看到分数稳步上升
`46:40` 最终你会得到一个系统哦
`46:42` 他的表现惊人的好
`46:43` 尽管基本上是个很小的模型
`46:46` 30亿激活参数
`46:47` 它在sw bench上却能达到约70.6%
`46:51` 表现令人惊讶
`46:52` 经在这些任务上做得很好
`46:55` 不过某些方面我也说不准
`46:56` 这是否真的令人意外
`46:58` 毕竟是做RL
`46:59` 你在训练过的环境里表现好
`47:01` 这很正常
`47:02` 你甚至可能像这里一样把能力泛化到验证集上
`47:05` 但对比性能时你总得小心点
`47:07` 比如在特定任务上表现好
`47:09` 不代表它能泛化到更广泛的领域
`47:12` 到RLVR这部分内容我就讲这么多
`47:14` 记得抓住这次讲座的核心要点
`47:17` 关键在于RL的奖励
`47:19` RL hf和RLV2解决的问题其实很像
`47:22` 区别在于我们想要更难被钻空子的奖励
`47:25` 这样我们就能投入更多算力
`47:27` 让系统变得更好
`47:29` 在研究圈
`47:30` GRPO确实推动了这些进展
`47:32` 大家得熟悉gr po
`47:34` 清除它的函数形式和更新方法
`47:36` 这就像预训练损失是你们必须掌握的嗯
`47:40` 最后我觉得现在很多人都会做RLVR了
`47:43` 可惜RL还是很敏感
`47:45` 噪声大
`47:45` 用起来挺让人头疼
`47:47` 其实没那么难
`47:48` 不像以前在各种极受环境里调P
`47:52` 其实比你想的顺多了
`47:54` 好我停一下
`47:54` 最后答几个问题
`47:55` 再三会大家要是还有问题的话
`47:58` 都刚才问的是思维模式和后台原理
`48:01` 这模式有趣
`48:02` 在它其实就一个模型
`48:03` 只要加个提示标签
`48:05` 就能在长COT和几乎无COT模式间切换
`48:09` 要是光设个API开关什么的
`48:11` 那就不难了啊
`48:13` 两个这个结合使用挺有意思的
`48:15` 你可以不选coo t
`48:16` 但自己加提示词来引导coo t对
`48:20` 这里的控制机制在提示磁力
`48:22` 而不在API或服务层
`48:25` 对假的哦
`48:27` 对你提到了中期训练阶段
`48:30` 我就在想中期训练里有多少格式化的内容
`48:33` 这一过程怎么影响
`48:35` RLV2后训练能学到的东西
`48:37` 另外如果中间缺了正确的数据
`48:40` 那是不是可以说强化学习采不到真知剪
`48:43` 所以学不会对
`48:45` 这问题是关于中期训练在强化学习里的作用
`48:49` 嗯我觉得这就是经验性的细节很多
`48:53` 我没有那种放之四海皆准的答案bridge
`48:56` 但细节上我认为预训练和sf t承担了主要工作
`48:59` 前提是数据覆盖到位
`49:01` 预训练没代码
`49:02` 数据就麻烦了
`49:03` 得靠中期训练
`49:04` 但若预训练多样
`49:05` 覆盖广
`49:06` 咱们看回中期训练的页
`49:07` 扫阿SA训练涵盖了文本代码以及大量GITHUB数据
`49:12` 那当然好
`49:13` 但未必关键
`49:14` 因为后续总要做SFT
`49:15` S f t
`49:16` 足以让模型接近到
`49:18` 能开始拿到一些RL奖励的程度
`49:21` 要是没有SFT那就麻烦大了
`49:23` 但好在RL前我们总会做一定量的SFT
`49:26` 这帮助很大
`49:27` 而且但愿预训练覆盖足够
`49:29` 让你能获得合理的泛化
`49:32` 所以中期训练很有用
`49:34` 对略微提升泛化很重要
`49:35` 但我觉得未必是成败
`49:36` 关键好
`49:37` 对于KIMI模型
`49:39` 你说过训练完每个专家模型后
`49:41` 还有一步是把它们再蒸馏回单个模型之前
`49:45` 那这种蒸馏是怎么做的
`49:47` 需要设计某种混合流程吗
`49:50` 这是很多流程需要你准备一组提示词
`49:53` 专家模型将基于这些提示词
`49:54` 蒸馏到最终模型里的
`49:56` 这方法的优势很明显
`49:58` 就是可以让不同团队分别负责各个专家模块
`50:01` 这样团队分工容易得多
`50:03` 而且只要算力充足
`50:05` 聚合过程也可能很简单
`50:07` 既然有了所有目标
`50:08` 不如直接扔进大训练循环里跑神
`50:11` 通常这才是大家偏好的做法
`50:13` 这样就不用处理蒸馏带来的复杂性了
`50:15` 你只需定一个大训练目标
`50:17` 跑一次训练循环就行
`50:18` 你看到了吗
`50:19` 嗯问问题是长推理训练算不算中期训练
`50:22` 我觉得R1和KIMI1.1里都有长coo t监督
`50:26` 微调
`50:27` 长COT本身并非传统中期训练的一部分
`50:31` 但长COT类数据常用于长上下文扩展
`50:34` 所以这点更微妙些
`50:36` 我完全没提倡上下文扩展
`50:38` 挺后悔的啊
`50:39` 通常这是RNHF之前的一个额外阶段啊
`50:43` 这里也是长COT
`50:45` 即用任何长上下文数据来扩展上下文
`50:49` 通常这是书籍代码合成数据的组合
`50:53` 因为这些内容足够长
`50:55` 适合做扩展
`50:56` 所以结合当前这张图在做RLVR时
`51:00` 比如你有数学
`51:01` 化学等很多领域啊
`51:03` 好这些训练是该病情好
`51:06` 还是说你得按顺序一个个来啊
`51:09` 为了避免以往前面学到的内容
`51:12` 而回答是顺序还是并行处理
`51:15` 最贴切的答案就是这类图
`51:16` 你会把问题分成推理问题和非推理问题两类
`51:20` 推理问题归为一类
`51:22` 在第二阶段执行
`51:23` 非推理问题
`51:24` 则在最终的RLAJF阶段处理
`51:26` 这个包括话多之类的方面啊
`51:28` 这些都归入最后的第四阶段
`51:31` 好的谢谢
`51:33` 下周见
# 动作表征与 Robot Transformer

动作表示决定模型实际学到的控制语义。常见选择包括关节位置/速度/力矩、末端位姿增量、离散动作 token 与技能参数。动作分块（action chunking）可改善时序协调、降低视觉推理频率，但块太长会放大时延与状态误差。

Robot Transformer 以时序注意力融合视觉、语言、本体和动作历史。无论架构如何，训练与部署必须一致定义参考系、单位、采样频率、归一化、夹爪编码和裁剪规则。
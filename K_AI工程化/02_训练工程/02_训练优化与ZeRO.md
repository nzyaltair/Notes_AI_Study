# 训练优化与 ZeRO

训练优化围绕计算、显存、通信和 I/O 的瓶颈展开。

- **混合精度**：FP16/BF16 降低计算和显存，配合 loss scaling 与数值监控。
- **梯度累积/微批次**：用小显存获得大有效 batch，但会延迟更新。
- **激活重计算**：以额外计算换取激活显存。
- **通信重叠**：将梯度规约与反向计算重叠，减少等待。
- **ZeRO**：阶段 1/2/3 分别切分优化器状态、梯度和参数；Offload 可把部分状态移至 CPU/NVMe。

优化必须用 tokens/s、MFU、显存、通信等待和收敛质量共同评估，不能只追求局部吞吐。
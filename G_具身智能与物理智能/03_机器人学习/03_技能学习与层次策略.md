# 技能学习与层次策略

长程任务应分解为可验证技能，而非由一个低层策略输出数百步动作。技能可表示为 option：起始集合、内部策略、终止条件、前后置条件与失败语义。

```text
任务目标 → 任务规划器 → 技能选择/组合 → 局部策略或控制器 → 结果核验
```

技能表示可来自动作片段、潜变量、语言命名、目标条件策略或运动原语。组合泛化的关键是接口：`抓取(object)` 必须明确所需感知、可达条件、成功证据、超时与恢复路径。

技能库将 VLA/LLM 的语义泛化与规划器的可验证性、低层控制的稳定性连接起来。
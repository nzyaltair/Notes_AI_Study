# JEPA 与联合嵌入预测

JEPA 在表示空间预测目标，而不是重建目标的全部原始细节。上下文编码器从可见区域产生上下文表示，预测器据此推断目标区域/未来的目标编码器表示。

## 核心价值

- 避免为纹理等不可预测细节付出生成代价。
- 鼓励对象、布局、时序和语义等抽象规律。
- 可通过视频和多模态数据进行大规模自监督预训练。

## 关键约束

目标编码器常使用 stop-gradient 或动量更新；配合掩码策略和方差/协方差正则，防止所有表示收敛为常数。预测目标的选择决定模型会学到什么。

## 与可行动世界模型的差距

V-JEPA 等视频表征模型可学习时序结构，但机器人或 Agent 还需要动作条件、可供性、奖励/安全约束、长时域不确定性和规划接口。因此 JEPA 可以是世界模型的表征骨架，而非自动等同完整世界模型。

## 延伸

- [[04_Predictive_Learning|预测学习]]：目标设计的总览。
- [[../../03_生成式世界模型/00_生成式世界模型_综述|生成式世界模型]]：需要视觉细节时的另一条路线。
- [[../../05_世界模型驱动智能体/00_世界模型驱动智能体_综述|世界模型驱动智能体]]：从预测表征到行动。
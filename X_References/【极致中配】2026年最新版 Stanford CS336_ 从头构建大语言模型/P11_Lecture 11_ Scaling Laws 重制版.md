---
title: "【极致中配】2026年最新版 Stanford CS336: 从头构建大语言模型"
url: "https://www.bilibili.com/video/BV11LEA6eEuj/?p=11"
bvid: "BV11LEA6eEuj"
cid: "39163200628"
author: "常青藤名校课"
upload_date: "2026-06-05"
created: 2026-08-17
tags: ["bilibili", "clippings"]
---

## 简介

如果这个中文配音版对您理解 CS336 有帮助，欢迎点赞、投币、收藏支持一下，也可以点个关注。
我会持续更新更多 Stanford / AI / 大模型相关课程的中文配音课程，尽量帮大家用更低的理解成本看懂这些高质量课程。你的支持会让我更有动力把后续内容继续做完、做好。

## 字幕

`00:00:00` 好咱们继续我们的scaling之旅
`00:00:02` 今天的内容有点杂呃
`00:00:04` 具体来说
`00:00:04` 咱们要聊聊scaling laws里更进阶的细节
`00:00:07` 还有模型扩展的具体做法
`00:00:09` 在这个过程中
`00:00:10` 咱们会讲到一些我觉得很关键的细节
`00:00:13` 也就是实际操作中怎么扩展一个language model
`00:00:16` 好咱们可以问几个问题
`00:00:18` 比如你两节课前讲的那个scaling laws
`00:00:21` 在真正构建open source model的规模下
`00:00:23` 它真的管用吗
`00:00:25` 而如果能优化其中一些环节就好了
`00:00:28` 人们用了很多学习率技巧之类的手段
`00:00:30` 我觉得作为了解怎么构建language model的人
`00:00:33` 这些你们都该知道
`00:00:35` 所以咱们会聊聊这些优化细节嗯
`00:00:38` 最后我想讲的一点是
`00:00:40` 模型规模变大后
`00:00:42` 有些东西调起来会稍微麻烦一点
`00:00:44` 我们知道优化器及其行为
`00:00:47` 对规模变化其实非常敏感
`00:00:48` 有因此像初始化学习率批次大小这些
`00:00:53` 我们需要把它们作为规模的函数来仔细考量
`00:00:57` 接下来我们就来讲讲嗯
`00:00:59` 该怎么仔细思考这些问题不好
`00:01:02` 到目前为止
`00:01:03` 我们聊的大多是我所说的经典scaling laws
`00:01:05` 正点我们聊过KAPLAN
`00:01:07` 聊过hastiness啊
`00:01:09` 还有CHANCHER那篇论文嗯
`00:01:11` 所以我们讲到的内容大概到twenty
`00:01:14` twenty two年左右嗯
`00:01:16` 从那以后
`00:01:17` 不少训练大模型的人发表了一系列scaling论文
`00:01:21` 不过我觉得近几年这类论文的数量有所减少
`00:01:25` 而且大多出自中国的open source社区
`00:01:27` 所以今天我想挑几篇来讲讲
`00:01:30` 这些都是我特意筛选出来的
`00:01:31` 想让大家感受一下前言
`00:01:34` 至少是开源前沿的scaling是什么样子
`00:01:37` 这些人关心或担心的是什么
`00:01:39` 如果你想从chinchilla1路速通到chemicate two
`00:01:42` 也就是最新发布的那个
`00:01:44` 你需要做些什么事
`00:01:45` 这里有一些SKILLING的细节好
`00:01:47` 这就是本次讲座的第一部分
`00:01:49` 咱们会过几篇论文
`00:01:51` 把细节都讲一遍
`00:01:51` 第二部分讲OPTIMIZERS
`00:01:53` Conditializations
`00:01:54` 大家记了吧
`00:01:55` 我两节课之前花了不少时间讲怎么选batch sizes
`00:01:58` 这点很重要
`00:01:59` 还有咱们还得想想怎么调learning rates这类参数
`00:02:02` 这也挺关键的
`00:02:03` 这些参数很敏感
`00:02:04` 设对了特别重要
`00:02:05` 所以你看大家确实花了不少功夫把它们调对
`00:02:09` 咱们会讲一些相关技巧
`00:02:11` 说实话
`00:02:11` 这对你们平时训练各种language model挺有用的
`00:02:14` 因为它能让你们知道规模放大或缩小时
`00:02:17` 实验里的hyper primary该怎么调好
`00:02:20` 我打算花不少时间重点讲两篇论文
`00:02:23` 这两篇不算最新
`00:02:25` 其实也不算最顶尖
`00:02:27` 不过说真的
`00:02:28` deep seek那篇可能算是讲的比较细的啊
`00:02:33` 整体上它不算最详尽的那篇SKILLING论文
`00:02:35` 但我觉得他正好踩在了一个最佳平衡点上
`00:02:38` New
`00:02:39` 所以这两篇论文都是来自一线模型构建者的
`00:02:43` 严肃scaling研究
`00:02:44` 它们都达到了相当高的性能
`00:02:47` 更重要的是
`00:02:48` 针对我们今天要探讨的一些关键问题
`00:02:51` 他们采取了截然不同的方法
`00:02:53` 所以我觉得用它们来引出后面课程的很多内容
`00:02:57` 很合适
`00:02:58` 那我就来讲讲这两篇
`00:03:00` 第一篇是mini cp m
`00:03:01` 我想这是twenty twenty four年的
`00:03:03` 是一个高性能的small language model
`00:03:06` 还有deep seek
`00:03:06` 大家现在都很熟悉了
`00:03:08` 我不用再解释为什么要讲deep seek
`00:03:09` 这很好
`00:03:10` 不过我们还是先从迷你CPM讲起
`00:03:12` 我挺喜欢这篇论文的
`00:03:14` 尤其是他刚发表的时候
`00:03:16` 当时感觉非常新颖
`00:03:18` 很有前沿感
`00:03:19` 即便到了twenty in twenty16年
`00:03:20` 我觉得迷你CPM团队做的很多工作
`00:03:22` 对我们依然很有启发
`00:03:24` 这个我后面讲到新论文时会细说
`00:03:26` 不过我感觉现在大家似乎已经默认了
`00:03:29` 一些经典的scaling操作
`00:03:30` 比如怎么设学习率
`00:03:31` 怎么做成车的优化
`00:03:33` 这些基本都成了标配
`00:03:35` 我觉得这类东西
`00:03:36` 现在大家都觉得是理所应当的了
`00:03:38` 就像大家默认你肯定知道该怎么操作一样
`00:03:41` 所以现在论文里很少再提这些了
`00:03:44` 正因如此
`00:03:45` 回过头来聊聊这些早期的论文
`00:03:46` 其实挺有意义的那种啊
`00:03:49` mini cpm是中国open source社区里一群来自业界学界
`00:03:53` 还有合作伙伴的朋友一起搞出来的项目
`00:03:56` 目的是做一个高性能参数
`00:03:59` 规模中等的language model
`00:04:01` 当时他刚出来的时候
`00:04:02` 在迟E到团11参数这个档位里
`00:04:05` 基本上算是顶尖水平了
`00:04:07` 虽然现在GEMA和其他to b或者3B的模型
`00:04:11` 性能更强
`00:04:11` mini cpm依然是训练良好
`00:04:13` 具备竞争力的模型
`00:04:15` 至少放在2024年的标准来看
`00:04:17` 是这样
`00:04:18` 那这些模型怎么训练呢
`00:04:20` 正如我之前提到的
`00:04:22` 咱们得把一些scaling参数调对
`00:04:24` 我今天想重点强调的一点是
`00:04:27` 初始化这事儿特别重要啊
`00:04:29` 初始化之所以重要
`00:04:30` 是因为它能让某些超参数不那么依赖模型规模
`00:04:34` 那么我想先从这篇迷你CPM论文里
`00:04:36` 提到的第一个技术聊起
`00:04:38` 他是一类特殊的初始化方法
`00:04:40` 叫做mp
`00:04:41` 那么new p的核心思想就是
`00:04:44` 无论模型规模如何放大或缩小
`00:04:47` 我都希望最优学习率保持不变
`00:04:50` 我在两次课之前简单提过这个概念
`00:04:53` 希望大家还有点印象
`00:04:54` 但这是我们今天要讨论的
`00:04:55` 第一个真正的具体实现
`00:04:58` 它具体长什么样呢
`00:04:59` 迷你CPM这篇论文列出了一系列操作
`00:05:02` 他们会缩放嵌入embedding的输出
`00:05:05` 按层数的平方根来缩放残差
`00:05:07` 连接对所有矩阵形状的张量
`00:05:09` 则依据fanny flash art te比率来缩放它们的初始化
`00:05:14` 他们还会调整张量的学习率
`00:05:16` 大家请注意这一点
`00:05:18` 这一点之所以显得特别
`00:05:20` 是因为如果你平时不习惯按参数设置学习率
`00:05:24` 会觉得有点奇怪
`00:05:25` 另外他们还会对language model的head做缩放
`00:05:28` 咱们后面会聊为什么要这么做
`00:05:30` 以及这些缩放比例是怎么推导出来的
`00:05:33` 简单说
`00:05:33` 你只需要把初始化和学习率的设定稍微改一改
`00:05:37` 希望能让训练过程更稳一点啊
`00:05:41` 当然我们这训练的是个小模型
`00:05:43` 但还是想搞一套像样的缩放策略
`00:05:45` 做这个研究的一个目的
`00:05:47` 就是不想靠训练几百个模型去暴力穷举
`00:05:51` 从而得到那个1.5B的模型
`00:05:56` 咱们希望第一次就把超参数调准
`00:05:58` 所以会先训练一堆小得多的模型
`00:06:00` 然后搭一个缩放阶梯
`00:06:01` 一路扩展到目标大小大概五倍左右
`00:06:04` 所以他们阶梯上最大的那个模型
`00:06:08` 和最后真正发布的那个模型之间差了大概五倍
`00:06:11` 他们的目标是想把那些大家普遍认为
`00:06:15` 很敏感的参数定下来
`00:06:17` 比如最佳批量大小学习率
`00:06:19` 还有咱们在change cha里提过的
`00:06:21` token与模型规模的比率
`00:06:22` 他们其实也想复现一下陈秋L的分析
`00:06:25` 这事儿现在对我们来说没那么重要了
`00:06:27` 对你们估计也一样
`00:06:28` 哪怕你们正在做训练
`00:06:30` 不过我还是得聊聊这个
`00:06:32` 因为他们做这件事的方式里有个关键点
`00:06:34` 我觉得特别有价值
`00:06:35` 也很实用好就像我刚才说的
`00:06:38` 搞这种叫mp的奇怪初始化
`00:06:40` 或者说非标准初始化
`00:06:42` 主要目的就是想让最优学习率保持稳定是吗
`00:06:46` 至少在mini cpm这个例子里
`00:06:48` 答案是肯定的
`00:06:49` 事实上他们的实验结果非常干净漂亮
`00:06:51` 他们在各种模型上扫了学习率
`00:06:54` 所以每条线就是一个模型的大小啊
`00:06:56` 最小损失基本就在十的二次方
`00:06:59` 那我猜对于最小的那个模型
`00:07:01` 最小值稍微偏了一点
`00:07:03` 但即便如此
`00:07:04` 也挺接近最小值的啊
`00:07:06` 所以啊这是MUP方法的一个很好的成功案例
`00:07:09` 接下来我们会聊聊其他几个
`00:07:11` 用了mp的模型和论文
`00:07:13` 这不是唯一的方法
`00:07:14` 但确实是一种有效的方法
`00:07:16` 如果你能做到类似这样的效果
`00:07:19` 基本上就不用再去调学习率了
`00:07:21` 诶不过当然哪怕你有个漂亮的参数化方案
`00:07:24` 把学习率锁定在某个最优值上
`00:07:26` 你的最优batch size也还是不会保持不变
`00:07:29` 所以你看batch size
`00:07:31` 其实是随着数据集规模和模型规模
`00:07:33` 变化而变化的
`00:07:35` 在他们的案例里
`00:07:36` 他们跑了一批这样的模型训练
`00:07:38` 你在这些图上看到的一列点就代表一次训练
`00:07:42` 他们把batch size固定
`00:07:44` 然后增加处理的token数量
`00:07:46` 我猜是从下往上递增的
`00:07:49` 然后他们问好
`00:07:50` 我们做了这么多次训练
`00:07:51` 那把这些二次曲线拟合到每一条线
`00:07:54` 也就是这些等损失等高线上最优的best size是多少
`00:07:58` 结果他们发现
`00:08:00` 最优batch size大致是随着处理的token数量
`00:08:03` 按比例变化的
`00:08:04` 他们在不同规模的模型上都试了一遍
`00:08:07` 考虑到我们在capital论文里看到的临界bad size
`00:08:10` 那些漂亮的缩放规律
`00:08:12` 这个结果你应该不会觉得意外
`00:08:14` 不过最优的batch size相对于目标loss来说
`00:08:17` 确实呈现出一种密率结构
`00:08:20` 所以你看这就很像capital那套分析逻辑
`00:08:23` 如果你的loss更低
`00:08:24` 也就是在图的左边
`00:08:26` 那back size就得更大对吧
`00:08:28` 这就是临界batch size的缩放规律
`00:08:30` 所以既然你已经通过scaling loss
`00:08:32` 反推出了一个特定的loss目标
`00:08:34` 那你基本上就能定下那个最优
`00:08:36` 或者说临界batch size了
`00:08:38` 这样能帮你找到最佳的权衡点对吧
`00:08:40` 这里有个很清晰很干净的规律
`00:08:43` 有了它
`00:08:44` 你就能非常轻松精确的把batch size定下来了
`00:08:47` and谢谢好
`00:08:48` 那咱们刚才说的这两点
`00:08:50` batch size和学习率的缩放
`00:08:52` 后面还会反复提到
`00:08:54` 可以说他们是做scaling lah
`00:08:56` 最重要也最敏感的两个参数嗯
`00:08:59` 不过我最后还想讲讲mini cp m论文里
`00:09:03` 我觉得很重要的一点
`00:09:04` 所以大家还记得吧
`00:09:06` 要做CHANTEL的话
`00:09:07` 咱们得训练好多个不同的模型
`00:09:09` 咱们举个例子
`00:09:10` 这次要做的是ice of flops实验
`00:09:13` 也就是说我把浮点运算量预算固定下来
`00:09:16` 然后去调整token数量和模型大小之间的取舍
`00:09:20` 具体怎么做呢
`00:09:21` 我会增加训练数据的量
`00:09:23` 也就是增加序列的数量
`00:09:26` 不过随着序列数量增加
`00:09:27` 大家要记住
`00:09:28` 每个序列得到的学习率调度是不同的
`00:09:31` 你们用的都是余弦学习率
`00:09:32` 但他们的曲线长得不一样
`00:09:34` 你得在训练前就清楚最终的总预算
`00:09:37` 才能定出余弦调度boss
`00:09:40` 这就带来一个挺让人头疼的问题
`00:09:42` 你不得不反复重新训练模型
`00:09:45` 比如你想训练一个200万序列的模型
`00:09:47` 你不能接着400万序列模型的末尾继续训
`00:09:50` 你必须从头开始
`00:09:52` 因为学习率调度从根本上就不同
`00:09:54` 这真的很让人头疼
`00:09:55` 从某种意义上说
`00:09:56` 这么做的成本相当于二次方增长
`00:09:59` 因为你每次都得从头开始重新跑训练啊
`00:10:02` 这太浪费了
`00:10:03` 我想很多人也是这么觉得的
`00:10:05` 我觉得mini cpm论文里有个很不错的方案
`00:10:07` 现在一般叫它预热稳定衰减学习率
`00:10:10` 这是个挺通用的叫法
`00:10:11` 它的形状大体上是个大梯形
`00:10:13` 首先是一个预热阶段诶
`00:10:15` 呃预热阶段通常按固定的部署来定义
`00:10:18` 而不是按训练总量的比例来定义
`00:10:20` 它只是跟你的训练总时长无关
`00:10:21` 接下来会有相当长的一段时间
`00:10:23` 把学习率保持恒定
`00:10:24` 这就是稳定阶段
`00:10:26` 然后在最后学习率会比较快的衰减到零
`00:10:29` 这就是衰减阶段
`00:10:31` 一般来说
`00:10:32` 衰减阶段大约占整个训练总量的10%
`00:10:35` 到20%
`00:10:36` Here
`00:10:37` 所以在训练的绝大部分时间里
`00:10:40` 学习率都保持稳定
`00:10:42` 然后在最后阶段迅速衰减到
`00:10:44` 最大学习率的10%左右
`00:10:46` 这就是典型的预热稳定衰减学习率策略好
`00:10:51` 那我们为什么要这么做呢
`00:10:52` 这么做的意义何在
`00:10:53` 我们之所以用WSD
`00:10:55` 是因为现在只要稳定阶段一结束
`00:10:57` 我们就能随时重启训练
`00:10:59` 所以如果我想复用之前的训练并继续跑下去
`00:11:03` 我只需要把训练回滚到
`00:11:04` 上一个稳定的检查点就行
`00:11:07` 接着我就接着稳定阶段的学习率继续往下跑
`00:11:11` 然后再把模型重新衰减到零
`00:11:13` 所以这让我们能很方便地做数据规模实验
`00:11:16` 基本上不用每次都重新跑一遍
`00:11:18` 你每次都得重新衰减
`00:11:20` 但这可能只占总成本的10%左右
`00:11:23` 反复做衰减可比反复重跑预训练划算多了
`00:11:28` 这是一个很常见的技巧
`00:11:30` 很多地方都在用
`00:11:31` 我给大家看个性能对比吧
`00:11:33` 以前有很多这种形式的图
`00:11:35` 我想最近是谁做的来着
`00:11:38` 也许是martin jaggy他们
`00:11:40` 我记得是在ETHH
`00:11:41` 他们对WSD的性能做过深入的研究
`00:11:44` 不过我觉得很多论文都展示了类似的情况
`00:11:47` 你会有一个调得很好的余弦学习曲线
`00:11:49` 并这是这条呈线
`00:11:52` 然后还有各种WSD曲线
`00:11:54` WSD曲线看起来总有点怪怪的
`00:11:56` 因为在进入衰减阶段之前
`00:11:58` 你的表现看起来差远了
`00:11:59` 直到进入衰减阶段
`00:12:00` 你突然又把之前落下的全追了回来
`00:12:02` 你的收益都追回来了
`00:12:04` 然后你的表现大致能赶上
`00:12:06` 甚至有时超过余弦学习率的表现
`00:12:08` 选择凭经验来说
`00:12:10` 要是你去问那些训练模型的人
`00:12:12` 很多人会说余弦调度在很多情况下稍微好一点
`00:12:16` 但WSD在不少其他场景下其实也差不多好
`00:12:20` 它就像一个非常通用的默认学习率
`00:12:23` 如果你只是想接着跑之前的实验
`00:12:25` 通常不需要重新跑
`00:12:26` 或者啊不过如果你以前没试过学习率衰减
`00:12:30` 看到它带来的影响有多大
`00:12:31` 真的挺震撼的
`00:12:33` 学习率衰减这一步对于搞定训练
`00:12:36` 最后那个退火阶段真的特别关键
`00:12:38` 好这就是之前的对比嗯好了
`00:12:41` 既然咱们有了WSD风格的学习率
`00:12:43` 请求了风格的分析其实就很简单了
`00:12:46` 你主要要做的就是跑一次长周期的训练
`00:12:49` 然后把检查点回退
`00:12:51` 反复做衰减
`00:12:53` 这样你就能在数据维度上扫一遍
`00:12:55` 同时也在模型维度上扫一遍
`00:12:57` 而你可以做等于FOOPS分析
`00:13:00` 或者任何你想做的其他分析
`00:13:02` 迷你CPM的作者
`00:13:03` 不知为啥选了方法一和方法三来复现
`00:13:06` 我觉得这两个在chinchilla方法里最不可靠
`00:13:08` 但他们出来的scaling曲线确实挺好看的
`00:13:10` 你看mini cp m那个chinchilla方法一
`00:13:13` 得到的下包络线挺漂亮的
`00:13:15` 基本都呈线性
`00:13:16` 这挺好的嗯
`00:13:17` 然后是方法三
`00:13:19` 在计算量和非嵌入参数的权衡上
`00:13:22` 大部分时候他们得到的SKILLING曲线都很平滑呃
`00:13:26` 看起来大多数情况下
`00:13:28` 他们得到的拟合结果都挺符合前期老规律的啊
`00:13:32` 有一点我得提一下
`00:13:34` 就是他们得到的指数差别挺大的
`00:13:37` 就他们整体的联合拟合而言
`00:13:39` 跟陈TRA比的话
`00:13:41` 论文里说
`00:13:42` 这意味着他们应该用比chinchilla
`00:13:44` 多得多的文本来训练
`00:13:46` 说实话我不太确定这是不是真的成立
`00:13:48` 还是说他们的请求的拟合结果
`00:13:50` 跟原始请求的论文比有点奇怪
`00:13:52` 好
`00:13:53` 我先停一下
`00:13:53` 看看大家有没有关于mini cp m的问题
`00:13:55` 我觉得主要有两点值得记住
`00:13:57` 第一用牛批是个挺有用的技巧
`00:14:00` 能帮你稳定学习率
`00:14:01` 随规模变化时的表现
`00:14:03` 第二点要记住的是WSD学习率
`00:14:06` 这也就是个很常见的技巧
`00:14:08` 这种学习率你们都得知道
`00:14:10` 这是基础内容
`00:14:11` 好不错
`00:14:13` 那咱们接着看deep seek
`00:14:14` 这是deep seek早期的一篇原始论文
`00:14:17` 那时候他们还没开始聊MOE这些很酷的东西
`00:14:20` When you were just don't have a so like
`00:14:21` 哪怕看DEPC克的第一篇论文
`00:14:23` 也能看出这帮人做事非常认真
`00:14:25` 就拿DPCGLM来说
`00:14:27` 我觉得即便到现在
`00:14:29` 这也是开源世界里
`00:14:30` scaling分析做得最漂亮的研究之一
`00:14:34` 我觉得他们做实验的品味特别好
`00:14:37` 所以哪怕R湾这些还没出来
`00:14:40` 大家就都看出来这帮人是要成大事的好
`00:14:44` 所以deep seek用的scaling策略
`00:14:46` 跟mini cpm那几个不太一样
`00:14:48` 他们主要是想优化超参数
`00:14:50` 他们不搞那个牛皮
`00:14:51` 也不搞这种挺有意思的SKILLING调整
`00:14:54` 相反他们打算直接拟和一条scaling lass
`00:14:57` 用来估算最优的batch size和learning rate哦
`00:15:01` 咱们都知道learning rate和batch size其实得跟着规模变
`00:15:05` 但如果他们的变化规律是可预测的
`00:15:08` 就跟scaling laws那样
`00:15:09` 那咱们心里就有底了
`00:15:10` 我们其实不用太担心
`00:15:12` 他们主要是在不同规模下做广泛的网格搜索
`00:15:15` 好找出最优解
`00:15:16` 这就是他们正在做的一类事情
`00:15:18` 他们会便利learning rate
`00:15:19` 便利batch size
`00:15:20` 然后看看处理一定数量
`00:15:22` token时的终端损失是多少
`00:15:24` 他们会先固定计算量
`00:15:26` 然后变动这两个参数
`00:15:27` 而且他们会针对几个不同的规模都这么做
`00:15:31` 而接着他们主要会去识别最低损失
`00:15:34` 也就是图里颜色最浅的那个点四
`00:15:37` 它们会在对应位置标个小型号
`00:15:39` 然后针对各个规模都这么做
`00:15:41` 所以如果你在足够多的不同规模上都这么做
`00:15:43` 你就会得到像这样的图表
`00:15:46` 这张图的意思是好吧
`00:15:47` 我要在某个范围内变动我的非嵌入训练EPHLOX
`00:15:52` 你能看到这儿这些灰点对吧
`00:15:54` 当我变动这个参数时
`00:15:55` 我的最优batch size也会跟着变
`00:15:57` 记住最优batch size是从这些大型网格中找出来的
`00:16:02` 也就是找到那个损失最小的点对吧
`00:16:05` 我会得到一条看起来像直线的东西
`00:16:08` 最优批次大小应该随非嵌入计算量
`00:16:11` not embedding flop缩放
`00:16:12` 同样地对于最优学习率
`00:16:14` 我会在非嵌入计算量范围内得到一堆灰点
`00:16:18` 接着我可以用一条线去拟合这些数据
`00:16:21` 以此估算出最优学习率大概该是什么样
`00:16:24` 而嗯这结果挺不错的
`00:16:26` 我想提一下他们那些完整的运行实验
`00:16:28` 在图上是标了星号的
`00:16:30` 这次这些是他们在计算量较高时选定的实验
`00:16:34` 他们的批次大小要大得多
`00:16:35` 学习率则要低得多
`00:16:37` 诶那些星号就是他们真正跑过的大型实验
`00:16:40` 这些拟合线对P4大小来说挺合理的对吧
`00:16:43` 这确实看起来像条直线
`00:16:45` 不过学习率那条线我不太确定
`00:16:47` 这算不算我这辈子见过最好的线性拟合
`00:16:50` 但既然他能跑通
`00:16:51` 那应该就没问题对吧
`00:16:52` 模型确实训练起来了
`00:16:53` 确实如此
`00:16:55` 你能得到合理的结果吗
`00:16:57` 嗯我觉得这是学习率方法的一个短板
`00:17:00` 如果你做这种网格搜索
`00:17:02` 除非网格找对了地方
`00:17:03` 否则量化误差会很大
`00:17:04` 最后scaling laws的拟合效果也不好
`00:17:06` 最后出来的结果
`00:17:08` 看起来就像是个随意的线性关系
`00:17:10` 行对切切
`00:17:13` 为什么同样的训练算力下
`00:17:17` 你会得到多个数据点
`00:17:20` 而且最优学习率的跨度还那么大
`00:17:24` 为什么不同的训练算力下
`00:17:26` 最优学习率的范围差别会这么大
`00:17:29` 你是问Y轴这里的问题吗
`00:17:31` 对我觉得这是因为模型规模这些参数变了嗯
`00:17:35` 所以twenty twenty four year那会儿
`00:17:37` 我觉得每个做open source模型的人
`00:17:40` 都在复现整套scaling loss体系啊
`00:17:44` Iting
`00:17:44` 所以DEPC也在补充学习这方面的内容啊
`00:17:47` 做的是learning rate分析
`00:17:49` 基本上就是把chinchilla复现一遍
`00:17:51` 为了做到这一点
`00:17:52` 他们也会像mini cpm那样沿用WSD风格的learning rate
`00:17:56` 他们有个稍微有点特别的小改动
`00:17:58` 就是做了两个衰减阶段
`00:18:00` 而不是通常的一个啊
`00:18:02` 我其实不太清楚为什么要这么做
`00:18:04` 这后来也没怎么流行起来
`00:18:06` 但他们确实就是这么做的
`00:18:08` 不管怎样
`00:18:09` 这里想说的核心观点是
`00:18:11` WSD是个有效的方法
`00:18:12` 你可以用它来复现成车
`00:18:14` Means it all flop
`00:18:16` 所以deep seek the scaling curves比mini cp m漂亮多了
`00:18:19` 结果就是在固定的flops范围内
`00:18:22` 他们得到了一组非常漂亮清晰的sweeps
`00:18:25` 接着他们也得到了相当漂亮清晰的请求
`00:18:30` A风格权衡跟之前一样
`00:18:33` 所以他们借此基本复现了chinchilla的结果
`00:18:36` 并得到了类似的数据模型
`00:18:38` Scaling laws
`00:18:39` 我讲这个主要是想说明
`00:18:41` chinchilla los已经在相当大的计算规模下
`00:18:44` 被其他人复现了
`00:18:45` 所以这个分析大体上是靠谱的啊
`00:18:49` 最后我觉得挺遗憾的
`00:18:50` 现在open source论文里很少见到这种图了
`00:18:53` 但这其实是很多scaling law研究工作的最终结论
`00:18:56` 那在较小的计算规模下
`00:18:58` 训练一堆精心挑选的小模型
`00:19:01` 然后画出密律
`00:19:02` 最后得到的结果跟现实非常接近
`00:19:04` 只要你扩展的足够大对吧
`00:19:06` 那两个星号是他们实际训练的模型
`00:19:09` 灰色圆点则是scaling law的拟合结果
`00:19:12` 他们对那些星号标记的模型预测的相当准
`00:19:15` 我是说虽然还能更好
`00:19:17` 但已经相当不错了
`00:19:18` 所以他们对自己训练的那些真实open source模型
`00:19:22` 做出了相当准确的scaling law预测好
`00:19:25` 那我就先讲到这里
`00:19:26` 如果大家对deep sc的内容有疑问
`00:19:28` 随时提问
`00:19:29` 你觉得这里可以用一句话概括
`00:19:31` 这是处理敏感超参数的另一种思路
`00:19:33` 诶
`00:19:34` 你要么试着稳定这些参数
`00:19:36` 要么直接用scaling laws去拟合它们对吧
`00:19:39` 然后就直接按scaling laws来
`00:19:42` deep sick走的路线
`00:19:43` 更像是通过拟合skilling laws来处理这个问题
`00:19:46` 好行
`00:19:47` 还有一些其他模型
`00:19:48` 尤其是比较新的
`00:19:49` 他们提到了自己拥有不错的scaling laws
`00:19:52` 咦不过就像我之前说的
`00:19:54` 我觉得现在好多这类工作大家都已经知道了
`00:19:56` 所以没人会把这类东西
`00:19:59` 专门写进论文的大章节里
`00:20:00` 你看q one two关于我的论文里面
`00:20:02` 大概就是说好
`00:20:03` 我们需要调优一些最佳超参数
`00:20:06` 比如批量大小和学习率
`00:20:07` 所以我们做了一堆scaling实验来找最优值
`00:20:10` 然后我们有了scaling lass就能预测出最优质了
`00:20:13` 然后我们甚至能对most easy也这么做对吧
`00:20:15` 所以这基本上是一回事啊
`00:20:17` 就像他们在twenty two
`00:20:18` point five里讨论的那个deep seek分析那样
`00:20:20` QUTHREE也是同理
`00:20:21` 他们说到了QUTHREE这个阶段
`00:20:23` 我们已经在QUTWO动work里把该做的都定好了
`00:20:26` 我们只是照搬之前的做法
`00:20:28` 所以这已经变成了一套标准流程
`00:20:31` 用来确定最优的学习率和批次大小嗯
`00:20:34` Kimi k two
`00:20:35` 大概是我接下来要讲的这一系列论文里
`00:20:39` 最新的一篇
`00:20:40` 接下来我会快速过几篇论文
`00:20:42` 主要是想给大家一个概览
`00:20:44` 看看大家在open source模型发布的scaling
`00:20:47` 工作上都做了什么
`00:20:48` 所以kicking to和其他几篇论文
`00:20:50` 关注的基本上就是MOE的scaling lass
`00:20:53` 你想啊
`00:20:54` 现在都20twenty45年了
`00:20:56` 大多数人应该都知道怎么做chanchilla scaling law
`00:20:59` 甚至是学习率的scaling law
`00:21:01` 那有什么新东西呢
`00:21:02` 嗯很多公司现在都在转向MOE
`00:21:04` 而且是全面转向MOE对吧
`00:21:06` 如果你在做这类实验
`00:21:08` 你得搞清楚一件事
`00:21:09` 你训练的激活参数数量到底对不对
`00:21:13` 所以他们会做这类实验调整f ops
`00:21:16` 看看损失怎么变
`00:21:17` 还要在不同稀疏度下都跑一遍
`00:21:20` 好搞清楚稀疏度到底是帮了忙还是拖了后腿
`00:21:24` 老人他们的结论很明确啊
`00:21:26` 你看不同稀疏度下的表现
`00:21:28` 网络越稀疏
`00:21:29` 验证损失就越低哦
`00:21:31` 给定flop下的损失
`00:21:33` 这其实并不意外
`00:21:34` 但关键在于
`00:21:35` 他们现在能定量算出稀疏度和损失之间的关系
`00:21:39` 这样就能更理性地决定模型里该塞进多少
`00:21:42` 稀疏度了
`00:21:43` 嗯他们看完数据后说行
`00:21:45` 我们就用SD的稀疏度吧
`00:21:46` 因为再往上加
`00:21:47` scaling laws带来的收益就开始递减了
`00:21:50` 所以你看这种分析常被用来
`00:21:53` 为不同open source版本里的架构决策提供依据
`00:21:57` 唉再比如恒源
`00:21:58` 这是几年前发布的另一个open source moe
`00:22:01` 他们做的也差不多对吧
`00:22:02` 我觉得每家公司一旦转用MOE
`00:22:04` 都得做这套分析
`00:22:06` 这就像是一种m o e scaling law
`00:22:08` 用来搞清楚激活参数数量或稀疏度的变化
`00:22:11` 怎么影响训练损失
`00:22:12` 然后他们就会把这些分析重新跑一遍
`00:22:15` 举个例子
`00:22:16` 会员最后算出来
`00:22:17` 每个激活参数对应大概96个数据点
`00:22:20` 他们那边把稀疏都固定住了
`00:22:22` 所以这也不是他们要搜索的参数嗯
`00:22:25` lama three也有一些不错的scaling loss
`00:22:28` 不过遗憾的是
`00:22:29` 这些规律其实没多大用
`00:22:31` 他们做的是ice of flop风格的缩放
`00:22:34` 得出的token和模型规模比例虽然略有不同
`00:22:37` 但大体相似
`00:22:38` Of lam three
`00:22:40` 在缩放方面最有趣的地方就是右边这张图
`00:22:44` 左边那个面板展示的是常规的scaling law
`00:22:46` 也就是说如果我投入更多算力基准测试
`00:22:49` 答案的对数概率就会稳定下降诶右边则展示了
`00:22:53` 你看对数损失越低
`00:22:55` 下游准确率也就越高对吧
`00:22:57` 所以他们大概的意思是
`00:22:59` 咱们可以对这些紫色数据点做个signal拟合
`00:23:02` 这样就能大致看出这些不同模型的效果如何
`00:23:06` 大家可以看到
`00:23:07` 数据点和这条曲线有系统性的偏离
`00:23:10` 所以我真不敢说这条曲线就是绝对真理
`00:23:12` 不过很明显
`00:23:13` 它符合从损失到准确率的S型缩放映射
`00:23:16` 我觉得这点很有用对吧
`00:23:17` 因为咱们之前一直只在聊对数损失
`00:23:21` 但在某些甚至很多情况下
`00:23:23` 对数损失和下游准确率其实是紧密绑定的好
`00:23:27` 最后我想再聊一篇关于缩放的论文
`00:23:30` 去年mini max01做了一组很棒的缩放实验
`00:23:35` 他们做了我在两次讲座前提到的那种
`00:23:39` 利用缩放来探索架构变体的操作
`00:23:41` 诶他们想搞清楚
`00:23:43` 我有lightning attention
`00:23:44` 这是一种类似线性注意力架构
`00:23:47` 我有soft max版本
`00:23:48` 也就是全注意力
`00:23:49` 还有一个混合版本
`00:23:51` 把两者结合起来
`00:23:52` 这些东西怎么缩放
`00:23:53` 是不是说如果我扩大规模
`00:23:55` 其中一种方法的表现会比另一种差很多
`00:23:58` 所以你可以看到他们基本上是在复现CAPLAN图
`00:24:01` 看看随着FOP次增加
`00:24:03` 学习曲线是怎么变的
`00:24:05` 那么参数数量是不是得变
`00:24:07` 也就是说针对不同架构和不同的算力水平
`00:24:10` 我们需要调整参数数量吗
`00:24:12` 大体上答案是否定的
`00:24:13` 混合架构lightning和soft max在大多数情况下差不多
`00:24:17` 他们需要的模型规模也类似
`00:24:20` 这组图就是用来证明
`00:24:22` 为什么他们在实际部署的系统里选了混合架构
`00:24:26` 你看这就是基于scaling laws做决策的典型例子
`00:24:30` 好了
`00:24:30` 这就是我想讲的关于各家近期skilling law配方
`00:24:34` recipes的全部内容
`00:24:35` 就像我之前说的
`00:24:36` DEPC的配方主要看两点
`00:24:38` 核心在于对batch size和learning rate scaling分析嗯
`00:24:41` 这是一种基于scaling的做法
`00:24:44` 而mini cp m走的是更偏new p风格的路子
`00:24:47` 试图获得不变性
`00:24:48` 接着我们看到了一些趋势
`00:24:50` 比如KIMIQUAN
`00:24:51` 还有会员
`00:24:52` 他们在切换到ME架构时
`00:24:54` 都在做这类moe scaling实验
`00:24:56` 我们看到了mini max在架构方面的一些工作
`00:24:59` 另外我们也看到其他几家机构复现了
`00:25:01` 等f ops分析
`00:25:03` 咱们回到刚才那个点
`00:25:04` 我觉得吧现在很多公司没那么注重细节了
`00:25:08` 因为像chinchilla或者学习率缩放
`00:25:10` 这些核心的scaling机制
`00:25:12` 大家其实都搞得挺明白了
`00:25:14` 所以把这些实验或者部分发布内容拿出来展示
`00:25:18` 其实没啥额外的价值
`00:25:19` 或者这大概就是我猜咱们发布里
`00:25:22` 这类实验变少的原因吧
`00:25:24` 行我再讲一分钟
`00:25:25` 然后停一下
`00:25:26` 大家消化消化
`00:25:27` 要是有什么问题
`00:25:28` 随时提问
`00:25:29` 念念念好的
`00:25:30` 刚才说的都是预训练阶段
`00:25:32` 那我想问一下
`00:25:33` 现在后训练成了所有数据的标准流程
`00:25:37` 这中间有哪些变了
`00:25:38` 哪些没变
`00:25:39` 短视问题问的是
`00:25:40` 当你大致把后训练也考虑进来时
`00:25:42` 这方面会有什么变化
`00:25:44` 我觉得这还是个悬而未决的大问题
`00:25:46` 要想把post training也考虑进去
`00:25:48` 做那种完全整合的scaling
`00:25:50` 目前还没什么好办法
`00:25:52` 因为post training有时候真的会改变
`00:25:54` pre training该怎么做
`00:25:55` 而当pose training是缠绵的
`00:25:57` 是有些研究在探讨
`00:25:59` PRETRAINING里的覆盖率或者多样性概念
`00:26:01` 那想用它来预测post training会怎样
`00:26:04` 不过这还是个很新的领域
`00:26:06` 至于该怎么处理跟put training的协同效应
`00:26:09` 我觉得现在也没什么靠谱的答案
`00:26:11` 好那咱们接着聊另一块内容
`00:26:13` 我觉得这堂课剩下的两部分正好能对应上
`00:26:16` 咱们刚才说的两个例子
`00:26:18` 所以我要讲一种方法
`00:26:20` 或者说一类方法
`00:26:21` 就是关于learning rates
`00:26:22` batch sizes和OPTIMIZERS的scaling laws
`00:26:25` 这在某种程度上对应了deep seek看待世界的方式
`00:26:31` 然后我要聊聊mp那些东西
`00:26:33` 是在某种程度上对应了mini cp m看待世界的方式
`00:26:37` 咱们能不能把所有东西重新参数化一下
`00:26:40` 这样就不用操心scale的问题了
`00:26:41` 接下来我们聊聊deep c的做法嗯
`00:26:44` 学习率到底该怎么按比例放大啊
`00:26:46` 这主要分两块
`00:26:47` 一是超参数
`00:26:48` 二是优化器的选择啊
`00:26:50` 我们会大量参考
`00:26:51` STEPHONE团队最近发的一篇预印本嗯
`00:26:54` 嗯我觉得目前并没有一项真正可靠
`00:26:57` 稳健的针对超参数
`00:26:59` 尤其是学习率的研究
`00:27:01` 但这篇大概已经比较接近了
`00:27:03` step fund团队训练出可信的大模型
`00:27:05` 并把他们公开发布在一项scaling loss研究中
`00:27:08` 他们用网格搜索便利超参数空间
`00:27:11` 烧掉了大量算力
`00:27:13` 所以我会带大家过一遍
`00:27:14` 看看一些有趣的经验现象
`00:27:16` 学习率P大小这些因素是怎么影响下游损失的
`00:27:20` 好嗯很多人已经讨论过各种不同的方式
`00:27:24` 大致根据算力来缩放学习率和P大小
`00:27:29` 另外step on那篇关于超参数调优的论文
`00:27:32` 注意是研究论文
`00:27:34` 不是综述
`00:27:34` 里面有张表做得特别棒
`00:27:36` 把这些内容都涵盖进去了哦
`00:27:38` 我来指出几个不同的点
`00:27:40` 有些东西咱们其实早就知道了
`00:27:42` 你偏爱都要上S看最上面那一行
`00:27:46` 这是OpenAI的scaling laws
`00:27:48` 来自KAPLAN
`00:27:50` 你看那个batch size上面写的是toe eighteen
`00:27:54` 那个L是大写的
`00:27:55` 作为指数
`00:27:56` 这就是所谓的临界batch size对吧
`00:27:58` 意思基本上就是说你要根据最终的loss来调整
`00:28:02` Batch size
`00:28:02` 嗯不错对吧
`00:28:04` deep c咱们刚才也看过了
`00:28:05` 这就是针对learning rate和batch size的
`00:28:08` 基于计算量的scaling laws
`00:28:09` 还有其他人也提出过各种scaling方案
`00:28:12` 比如咱们之前聊过的mini cp m
`00:28:14` 还有个重要的点
`00:28:15` 我想提一下
`00:28:16` 就是这些算learning rate和batch size的公式
`00:28:19` 用的输入变量都不一样
`00:28:20` 哈哈大家似乎连哪些变量会影响这些learning r
`00:28:24` 都还没达成共识
`00:28:25` OPENAI用的是终端损失
`00:28:27` 而step fund那边人则认为
`00:28:29` batch size的正确缩放方式取决于数据量
`00:28:32` 在超参数缩放的本质这个问题上
`00:28:35` 大家有很多不同的分歧啊
`00:28:37` 我顺便说一句
`00:28:37` 大家看最后这一行时
`00:28:41` 这些方法在很多方面其实挺脆弱的
`00:28:43` 但里面的实验和现象
`00:28:45` 咱们还是能学到不少东西的
`00:28:47` 好我就讲这些
`00:28:48` 谢谢这篇论文的设计跟deep i那篇很像
`00:28:51` 他们就是训练一大堆模型
`00:28:54` 把learning rate空间和back size空间都做成网格搜索
`00:28:59` 在不同规模的模型
`00:29:00` 和不同大小的数据集上都跑一遍
`00:29:03` 这么做的目的
`00:29:04` 本质上就是想把超参数空间给填满
`00:29:06` S针对一组小模型
`00:29:08` 然后把那个最优值给敲定下来
`00:29:10` Right
`00:29:10` 这和deep sk的设计很像
`00:29:12` 他们把空间网格化之后
`00:29:14` 画出了等高线图
`00:29:15` 其实底层就是跑一堆模型
`00:29:17` 目标就是尽量把损失降到最低对吧
`00:29:20` 这等高线图挺直观的
`00:29:21` 看着很舒服
`00:29:22` 他们用了很细的网格
`00:29:24` 所以我们能看出来
`00:29:25` 他们其实能画出分辨率相当高的等高线matters
`00:29:28` 看看超参数空间长什么样
`00:29:30` 这里切出来的是10亿参数下的那一片
`00:29:32` Po one thousand
`00:29:33` 1token
`00:29:34` 我记得是你看右边那个曲面
`00:29:35` 就是超参数的地形图
`00:29:38` 横轴是批量大小
`00:29:40` 纵轴是学习率
`00:29:40` 如果我们固定模型的总数据量
`00:29:42` 看看损失是怎么随着两个变量变化的
`00:29:45` 现在你看每个切片
`00:29:47` 比如你固定了批量大小或者学习率
`00:29:49` 形状都很凸
`00:29:50` 甚至几乎是平滑的理
`00:29:52` 这挺好的
`00:29:53` 因为这意味着你找最优超参数会容易得多
`00:29:56` 这样你就能放心最小值不会跑太远
`00:29:58` 要知道以前那种曲线可是锯齿状的
`00:30:01` 你可能会怀疑这种把搜索空间网格化的方法
`00:30:04` 到底行不行得通
`00:30:06` 另外这应该能让你对超参数空间整体的平滑性
`00:30:10` 有个直观的认识啊
`00:30:11` 我觉得这里其实有一个特别有意思的
`00:30:13` 通用scaling趋势
`00:30:14` 我看了跟这个主题相关的其他论文
`00:30:16` 我不太确定有没有见过跟这个观察结果
`00:30:19` 相矛盾的证据
`00:30:20` 所以我觉得这项工作最有趣的一点
`00:30:23` 也是核心结论之一
`00:30:24` 如果你看最优bad size
`00:30:26` 它似乎只取决于你训练数据的总量
`00:30:30` 所以咱们看右边这张图对吧
`00:30:32` 这里的横轴代表的是训练数据量
`00:30:34` 不同颜色的点代表不同的模型
`00:30:37` 你看这些点大致都沿着同一条趋势线走
`00:30:40` 挺有规律的
`00:30:42` 因为这是双对数坐标
`00:30:43` 所以你应该能预期到某种关于D的密率关系
`00:30:46` 对吧
`00:30:46` 左侧面板展示的是对学习率的依赖关系
`00:30:50` 可以看到这里的情况很不一样
`00:30:52` 以模型规模为例
`00:30:54` 颜色从蓝色一直过渡到粉色
`00:30:57` 代表模型越来越大
`00:30:59` 你会发现模型越大
`00:31:01` 最优学习率反而越低
`00:31:03` 而数据量越大
`00:31:04` 最优学习率则越高
`00:31:05` 这其实有点反直觉
`00:31:07` 所以这个结论可能不太稳固
`00:31:10` 也有另外一些论文认为
`00:31:12` 对D的依赖关系应该是反过来的
`00:31:14` 不过至少就这项实验而言
`00:31:16` 这里的趋势是相当清晰的好
`00:31:20` 最后我想指出一点
`00:31:22` 学习率其实是相当稳健的
`00:31:24` 我认为这一点
`00:31:25` 也得到了大量实践经验和实验结果的支持
`00:31:29` is哈我想大家心里大概都有个谱呃
`00:31:33` 顺便做个小调查
`00:31:34` 大家清楚什么样的超参数才算好吗
`00:31:36` 而假如你明天就要去训练一个language model
`00:31:39` 你会选压1-3还是11-3
`00:31:42` 这种学习率
`00:31:43` 我觉得咱们大概心里有数
`00:31:45` Learning rate
`00:31:45` 这个超参数范围通常是个不错的起点
`00:31:48` 大体上确实如此
`00:31:50` 所以他们得出的scaling law
`00:31:51` 在一定程度上也能用到MOE上
`00:31:54` 那些黄色的星号可能稍微有点看不清
`00:31:57` 就是根据他们的scaling lass做出的预测
`00:31:59` 那些红色的X呢
`00:32:01` 基本上就是直接从moo is里推导出来的最优值
`00:32:06` 所以只要你大致把控好active parameters的数量
`00:32:09` 换成MOE架构的话
`00:32:11` scaling law其实不会有太大差别
`00:32:14` 唯一看起来可能稍微有点悬的地方是
`00:32:17` 当你换训练数据的时候
`00:32:18` 最优的learning rates和最优的batch size确实会有些变动
`00:32:22` 对吧
`00:32:23` 这说明这些现象很可能都取决于数据
`00:32:26` 像这些scaling lawy的具体数字
`00:32:28` 很大程度上是依赖于你用来训练的数据的
`00:32:31` 比其他因素更依赖数据
`00:32:33` 所以在我接着往下讲之前
`00:32:36` 我想把这个画面再放出来看看
`00:32:38` 最后你会得出一个简化的scaling loss结论
`00:32:41` back size大致等于数据量乘以某个常数
`00:32:45` 再开平方
`00:32:46` 你的learning rate也得跟着往上调
`00:32:48` 有二数据多了
`00:32:50` learning rate往上调
`00:32:51` 模型大了往下调
`00:32:52` 这效果挺反直觉
`00:32:53` 但也不至于太疯狂
`00:32:54` 因为很多实验都默认用了chinchilla h的scaling
`00:32:57` 那样这两个因素刚好抵消算力上去了
`00:32:59` learning rate就得降下来
`00:33:01` 刚才关于chinchilla的那段大家应该听明白了吧
`00:33:03` 哎我是说按china原则做scaling的时候
`00:33:07` N和D其实都跟着算力走
`00:33:09` 算力一涨
`00:33:10` learning rate就得降对吧
`00:33:12` 所以你会得到类似deep c定律的结论
`00:33:15` learning race随算力增加而降低
`00:33:17` backside随算力增加而升高
`00:33:19` 只是指数跟DPC定律的不太一样啊
`00:33:22` 不错
`00:33:22` 某种程度上说
`00:33:24` 这就是deep sk分析最新规模最大的一版
`00:33:28` 这是一种思路
`00:33:30` 如果你用scaling loss那套方法处理learning rate
`00:33:33` 拟合问题
`00:33:34` 就是这么做的好
`00:33:35` 以上就是超参数部分
`00:33:36` 大家如果有疑问
`00:33:37` 随时可以打断我
`00:33:38` 接下来我们要接着聊聊优化器
`00:33:42` 我觉得这部分内容
`00:33:43` 我在讲架构时没来得及展开呃
`00:33:46` 之前那次讲座内容已经很满了嗯
`00:33:48` 但要是我不讲优化器
`00:33:50` 我觉得对你们不太公平
`00:33:51` 而且优化器是依赖于规模的
`00:33:53` 所以这里很适合拿来聊聊
`00:33:55` 讲讲优化器的一些最新进展
`00:33:57` 以及它们和规模之间的关系
`00:33:59` 嗯对嗯
`00:34:01` 我是想弄明白该怎么理解这个
`00:34:03` 比如这是不是说
`00:34:05` 我们直接用现有的scaling law就行
`00:34:07` 不用自己再去跑网格搜索了
`00:34:09` 我的意思是
`00:34:10` 这得看是什么情形
`00:34:12` 抱歉这个问题我得给线上的朋友重复一遍对吧
`00:34:15` 我们是直接拿现成的scaling laws来用
`00:34:17` 还是自己跑一遍网格搜索
`00:34:19` 这得看你的计算预算有多少
`00:34:21` 还有你需要把它调到多准
`00:34:24` 我觉得比如step fund那个scaling law
`00:34:26` 要是你的算力规模刚好合适
`00:34:28` 差不多就在他们做网格搜索的那个水平
`00:34:30` 那这些参数大概就是很好的默认选择
`00:34:33` 我肯定会选这些
`00:34:34` 而不是我自己凭空想出来的东西
`00:34:37` 不过话说回来
`00:34:38` 要是你在做自己的大规模预训练
`00:34:41` 多半会有一些细微的差别
`00:34:43` 有人可能会说
`00:34:43` 我真的觉得咱们应该用比较大的权重衰减
`00:34:46` 我的架构里权重衰减就设得很大
`00:34:48` This
`00:34:49` 咱们不一定知道这些因素会按同样的规律扩展
`00:34:52` 所以你可能得把这里面很多东西重新做一遍
`00:34:56` 这就是为什么我觉得之前给你们看的那些报告
`00:34:59` 都在重新跑进去了
`00:35:00` 重新验证各种东西
`00:35:01` 因为细节上总有些差别
`00:35:03` 大家想确认一下
`00:35:04` 这些结果在主要量级上大致是对的
`00:35:07` To them
`00:35:07` 我觉得接下来讲优化器的时候还会提到这个
`00:35:09` 我之前也聊过这一点
`00:35:11` 就是skilling lass
`00:35:12` 给人的感觉特别像严谨的科学呃
`00:35:15` 这感觉就像哦对
`00:35:16` 把这些线拟合上去
`00:35:17` 然后往外推
`00:35:18` 但说到底
`00:35:19` scaling laws里有很大一部分还是靠感觉
`00:35:22` 我就会想
`00:35:23` 第一步
`00:35:24` 那个调查的实验设置真的和我的足够相似吗
`00:35:26` 相似到结果能迁移过来
`00:35:28` 我们根本没法确切知道总会有一些细微的差别
`00:35:32` 好好接下来咱们聊聊优化器
`00:35:35` 我觉得过去这一两年
`00:35:36` LM优化器
`00:35:38` 甚至整个深度学习优化器领域
`00:35:40` 都有一些特别让人兴奋的进展
`00:35:42` Here
`00:35:43` 所以现在聊这个正合适
`00:35:45` 我想先从这个比较讲起
`00:35:47` 他也许能让我们明白这一小节为什么值得讲啊
`00:35:51` 或者咱们就先从左边这张图开始
`00:35:53` 这是来自nano gbt speed run
`00:35:55` 他启发了咱们的第一次作业
`00:35:57` 在那个例子里
`00:35:58` 你要训练一个非常小的模型
`00:36:00` 训练时间也相当短
`00:36:01` 你想尽快达到三点几的对数损失
`00:36:04` 具体是多少
`00:36:05` 我忘了哦
`00:36:05` 嗯很多人一直在针对这个基准测试做局部优化
`00:36:10` 我觉得这里有个挺有意思的现象
`00:36:11` 你看这个叫MUON的紫色曲线
`00:36:14` 它相对于ADAM蓝色取得了非常显著的增益
`00:36:17` 它就在那条绿色线下面
`00:36:19` 所以你看只要对比一下
`00:36:21` 绿色和紫色这两条线之间的差距
`00:36:23` 你就会觉得哇仅仅把优化器换成MIUI
`00:36:26` 提升就这么大
`00:36:27` 而且它甚至不比ADD慢
`00:36:28` 更谈不上比阿德慢很多
`00:36:30` 这很棒对吧
`00:36:31` 但接下来有个问题
`00:36:33` 这东西在大规模扩展时真的管用吗
`00:36:36` 然后你去跑一些扩展性研究
`00:36:38` 结果可能就没那么好了
`00:36:39` 这是做研究是一个很棘手也很重要的部分
`00:36:43` 我们做个小规模实验
`00:36:44` 比如优化器这种肯定依赖规模的东西
`00:36:47` 结果在小规模下看到了巨大的提升
`00:36:49` N那大规模情况下该怎么办呢
`00:36:52` 这不仅仅是给出一个扎实具体的方案那么简单
`00:36:56` 我想咱们在这里不妨讲个故事
`00:36:58` 这样你们就能大概明白我们到底能做什么
`00:37:00` 以及哪些事情是我们不一定能提前知道的
`00:37:03` DE好嗯
`00:37:05` 在切到下一张幻灯片前
`00:37:06` 大家常遇到这种情况
`00:37:08` 小规模时想出一个很棒的方案
`00:37:09` 可以放大规模就不灵了
`00:37:11` 哎呃差别在哪
`00:37:12` 嗯接下来几页我会讲讲kayer tenor
`00:37:16` 还有PERCY和DAVID的那篇论文
`00:37:18` 他们基本上做了一组大规模实验
`00:37:20` 用来对比优化器
`00:37:22` 并且把这种对比做得更到位
`00:37:24` 我的意思是
`00:37:25` 除了能从中学到关于scaling
`00:37:27` 以及其他方面的东西
`00:37:28` 这里面还有很多标准的深度学习知识值得了解
`00:37:32` 嗯有些很基础的问题
`00:37:34` 比如不同模型的超参数是不一样的哦
`00:37:37` 所以当你扩大规模时
`00:37:39` 他们的scaling指数可能都不一样
`00:37:41` 这时候你必须把ADAM调得很到位
`00:37:44` 所以如果你调得不好
`00:37:46` 就会得到这条深褐色的线
`00:37:48` 你会想哇
`00:37:49` 这东西比左边那些算法差远了
`00:37:51` 但只要你稍微改一下学习率
`00:37:53` 你就会发现哇之前所有的收益都没了嗯
`00:37:56` 同样在右边也有这种情况
`00:37:58` 也许你已经把学习率调好了
`00:38:00` 但其他东西比如权重衰减
`00:38:02` WADECAY却没调到最优
`00:38:04` 所以要是你用同样的权重衰减
`00:38:07` 那这条绿线的结果肯定会差很多
`00:38:09` 要是你选了这个偏小的权重衰减
`00:38:12` 那跟选最优权重衰减比起来
`00:38:14` 这条绿线的结果会差很多
`00:38:16` 所以以后有各种挺棘手的情况
`00:38:19` 严格来说这不算scaling层面的问题
`00:38:22` 但这是个非常重要的机器学习通用经验规律
`00:38:24` 我想特别指出来
`00:38:27` 谢谢
`00:38:27` 不过可能跟咱们现在课上讨论更贴切的是
`00:38:32` 这些因素对规模的依赖关系
`00:38:35` 我觉得这是个很好的案例
`00:38:37` 能帮我们真正学会
`00:38:38` 怎么把算法开发当成规模的函数来思考
`00:38:41` 因为在我看来
`00:38:43` 思考规模的时候
`00:38:45` 有两个变量始终很重要
`00:38:47` 这篇论文在很大程度上把这两点都揭示了出来
`00:38:50` 所以其中一个维度是算力
`00:38:53` 要是你固定了模型大小和数据量的比例
`00:38:56` 让这两者绑定在一起
`00:38:58` 那这本质上就是横轴上的算力维度啊
`00:39:01` 所以做研究的时候
`00:39:03` 你总得把算力拉上去
`00:39:05` 看看各项指标是怎么随规模变化的
`00:39:08` 也就是随规模变化
`00:39:10` 我们看这里
`00:39:11` 如果把ADAM的加速比定为1.0
`00:39:15` 再对比其他所有算法
`00:39:17` 会发现迷惘在小规模时特别强
`00:39:19` 但随着规模增大
`00:39:21` 这种优势就慢慢减弱了
`00:39:23` 这往往不是你想看到的情况
`00:39:25` 谢谢要是你在做算法开发
`00:39:27` 还有另一个维度
`00:39:28` 得时刻留意
`00:39:29` 你得警惕的另一个维度就是chinchilla比例
`00:39:32` 简单说
`00:39:32` chinchilla比例
`00:39:33` 就是你的数据量和模型参数量之比
`00:39:36` 这个参数特别关键
`00:39:37` 因为有些算法在过参数化的情况下跑得挺好
`00:39:40` 也就是模型比数据大得多
`00:39:41` 你的算法是不是引入了某种影视正则化
`00:39:45` 又或者它数据效率不高
`00:39:46` 但靠算力撑着效果照样不错
`00:39:48` 所以有些算法在亲切er比例低的时候效果好
`00:39:51` 反过来有些算法则适合请求比例高的情况
`00:39:54` 也就是每个参数能分到很多数据
`00:39:56` 因为你能把知识更高效地塞进参数里对吧
`00:40:00` 所以这是一个特别特别大的混淆因素
`00:40:02` 哪怕是我觉得那些在模型规模这条轴上
`00:40:05` 做scaling实验规范做得挺不错的论文
`00:40:08` 往往也忽略了
`00:40:09` 或者压根没算力
`00:40:10` 去研究那种陈秋老师的token与参数比例
`00:40:14` 结果发现这一点很多时候出人意料的重要
`00:40:17` 但在这种情况下并非如此
`00:40:20` 你看这种情况下
`00:40:21` 优化器带来的提升是相当一致的意思
`00:40:24` 不管前车的比例怎么变
`00:40:26` 这几条线之间的比例关系都一样
`00:40:28` 但情况并非总是如此
`00:40:30` 所以这两个SCALLING因素你都得时刻留意
`00:40:33` 这点挺重要的
`00:40:34` 那我还要提一句
`00:40:35` 要是你搞了个新算法或者新配置
`00:40:38` 想确立scaling规律可没那么简单
`00:40:40` 这个例子来自will help
`00:40:42` 他参与过marine项目
`00:40:43` 也就是PERCY那个open source language model训练项目
`00:40:46` AMINERS嗯
`00:40:47` 在那个实验里
`00:40:48` 他们主要是在验证一组特定的超参数
`00:40:50` 所以这根本不算什么新算法
`00:40:52` 它是谨慎版
`00:40:53` ADAMC算是ADAM的一个变体
`00:40:54` 用了平方根batch size缩放
`00:40:56` 还有一堆其他看起来很常规的设置
`00:40:58` 他们做的本质上就是
`00:41:00` 你在其他所有论文里都见过的那种
`00:41:03` 请CHA分析嗯
`00:41:04` 嗯但因为这不一定算是一篇论文
`00:41:07` 而是一群人在做实验
`00:41:09` 所以你不会只把成功的那个发表出来
`00:41:11` 他们有个好处
`00:41:12` 就是愿意把失败的实验记录也公开出来
`00:41:15` 这里的例子是他们做的
`00:41:16` 基本上就是标准的请求了
`00:41:17` 那一套结果看起来很棒
`00:41:21` 如果你看这条在池的串
`00:41:22` 你多次方出的竖直虚线
`00:41:25` 在它之前
`00:41:25` 这条scaling law画的真的非常漂亮
`00:41:27` first可随着规模继续往上扩
`00:41:30` 就变成嗯先是差了一点点
`00:41:33` 然后差很多
`00:41:34` 接着哇
`00:41:35` 那个实验现在就不行了
`00:41:36` 整个就在你手里崩掉了
`00:41:37` 有这里想说的是
`00:41:39` 即便在很多个数量级上
`00:41:41` SCALLING趋势看起来都不错
`00:41:43` 也可能突然反咬你一口
`00:41:45` 曲线可能会偏离线性
`00:41:47` 然后立刻就崩掉
`00:41:48` 针对他们的情况
`00:41:49` 解决办法就是选用更谨慎的mp风格
`00:41:52` 参数化的优化器方面
`00:41:53` 他们换了一种优化器
`00:41:55` 结果在多个数量级上都得到了更好的
`00:41:57` scaling表现
`00:41:57` 因此但我想强调的是
`00:42:00` 当我回过头来说好吧
`00:42:01` 你们都应该去做scaling实验室
`00:42:03` 你们得对所有学习率等参数做scalling in
`00:42:06` 这并不容易
`00:42:07` 最后你会发现有相当一部分时间
`00:42:09` 你得到的图表都是长这样啊
`00:42:12` 看着挺糟心的
`00:42:13` 而且你也不太确定到底哪里出了问题
`00:42:15` 好了很好
`00:42:16` 那么最后我想稍微聊聊me网
`00:42:18` 大致收尾一下关于迷惘的话题
`00:42:21` 所以我觉得有必要比之前更详细的讲讲MIO
`00:42:24` 因为它开始变得越来越重要了
`00:42:25` 他被整合进了大规模模型训练流程啊
`00:42:29` 这也是咱们这门课很多新研究课题的入选标准
`00:42:32` 他有没有参与过大规模训练
`00:42:34` 现在mi网达标了啊
`00:42:36` 而且它本身也是个挺有意思的算法
`00:42:38` 所以我简单提一下
`00:42:39` 聊几分钟
`00:42:40` 然后咱们接着往下
`00:42:41` 所以诶在梯度下降里通常发生的事情比较简单
`00:42:46` 先别管D法庭
`00:42:47` 这样你得到的大致就是个标准的
`00:42:50` 基于动量的优化器
`00:42:51` 我取个梯度嗯
`00:42:52` 把它加到B的T上
`00:42:53` 这就是我的动量步骤
`00:42:55` 所以我用参数mu延续之前的动量
`00:42:58` 然后基于这个动量梯度来更新参数
`00:43:01` 这是非常标准的动量梯度操作
`00:43:04` 但我认为真正有意思的
`00:43:06` 至少对我来说是一套跳出常规的思路
`00:43:09` 梯度有个特性
`00:43:11` 就是对所有参数都一视同仁
`00:43:13` 但我们要知道
`00:43:14` Language model
`00:43:15` 甚至深度神经网络里的参数并不都是一样的
`00:43:18` 有些参数是向量参数
`00:43:20` 比如属于RMS归一化的那些
`00:43:22` 有些参数是矩阵参数
`00:43:24` 比如用于注意力机制或MLP的参数对吧
`00:43:27` 而矩阵参数可能跟向量参数不一样诶
`00:43:30` 这就是这个想法的起点
`00:43:32` 我觉得挺有意思的
`00:43:33` 所以针对矩阵参数
`00:43:35` 你能做的一件事就是开始看看他们的谱对吧
`00:43:38` 毕竟他们是矩阵吗
`00:43:39` 矩阵有特征值和极意值的
`00:43:42` 所以你可以对奇异值做些操作
`00:43:44` 你在迷惘里用的就是这个挺简单的想法
`00:43:46` 你算出梯度走一步动量
`00:43:48` 现在我拿到T时刻的更新量
`00:43:50` B不是像第六行那样直接把它用到参数上
`00:43:53` 而是要做一下这个newton shots操作
`00:43:55` 具体来说是newton shots for
`00:43:57` 这样做的作用就是把B太太T矩阵正交化换
`00:44:02` 所以我把这个操作写在下面了
`00:44:04` 咱们假设取BIT把它做机翼值分解
`00:44:06` 写成USV的TT的形式
`00:44:08` 然后把所有G1值都设成一
`00:44:11` 如果极易值特别大
`00:44:12` 我就把它缩回到一
`00:44:13` 如果特别小
`00:44:14` 就把它扩回到一
`00:44:15` 然后我取UV的T4
`00:44:17` 这就是我要应用的新更新量
`00:44:20` 如果你想找个直观的理解
`00:44:22` 而且你对ATRAH或者ADAM
`00:44:23` 这些优化器已经有概念了
`00:44:25` 那么在ATGRAD或ADAM里
`00:44:27` 你是除以梯度的某种大小
`00:44:29` 对吧啊
`00:44:30` 这样每个坐标的大小就大致相同了
`00:44:32` 而在MON里你操作的是谱范数
`00:44:35` 所以每个方向的大小都是单位值
`00:44:37` 注意这只有对矩阵才有意义
`00:44:40` 我只能对矩阵值的BT定义
`00:44:42` 及异值分解这个概念
`00:44:44` 所以你往有意思的地方在于
`00:44:46` 它是一个矩阵优化器
`00:44:48` 所以你把它用在这些矩阵参数上
`00:44:50` 如果是向量值的参数
`00:44:52` 你大概还是会用ADAMW这类优化器
`00:44:55` 因为它们跟牛顿舒尔茨项基本没什么交互
`00:44:59` 还有牛顿舒尔茨是对那个运算的一种近似
`00:45:01` 而且只用到矩阵乘法
`00:45:03` 所以他在系统层面很高效
`00:45:04` 但严格来说并不算正交化
`00:45:06` 那我为什么花这么多时间讲木耳呢
`00:45:08` 首先他在像NANOGPT速度竞赛
`00:45:12` 这类任务里表现非常强
`00:45:13` 他本来就是为这类任务提出的
`00:45:15` 所以你可能会觉得他效果不错
`00:45:17` 就像我刚才那张幻灯片里说的
`00:45:19` 有些扩展性研究指出
`00:45:21` 随着模型规模变大
`00:45:23` me on带来的收益好像会慢慢变少
`00:45:26` 对啊我想给这个故事做个收尾
`00:45:28` 你往刚出来那会儿
`00:45:29` 我觉得他挺有意思的哦
`00:45:31` 我当时在想
`00:45:32` 会不会有人把它放大到更大规模
`00:45:34` 后来开由他们真的把魔王做了大规模实验
`00:45:37` 结果说它的效果可能没那么神
`00:45:40` 我当时觉得这事就算翻篇了
`00:45:42` 心想估计也就这样了
`00:45:44` 谁会砸那么多算力
`00:45:46` 把它扩到完整的预训练规模啊
`00:45:48` 结果kimi kt出来了
`00:45:50` 而且他整个模型都是用迷惘训练的
`00:45:52` 就全套都用米王
`00:45:54` 只是加了一些小手段
`00:45:56` 防止冥王训练时数值爆炸
`00:45:58` 他们确实发现了一大堆不稳定的问题
`00:46:00` 但kimi k two是个非常出色的模型对吧
`00:46:03` 模型效果很好
`00:46:04` 训练曲线看起来也挺合理的
`00:46:06` 所以这其实说明moon在大规模场景下是行得通的
`00:46:10` 它比ADAM好吗
`00:46:12` kimi k two肯定没做消融实验
`00:46:14` 所以我们在那个规模下没法确切知道
`00:46:16` 但他确实是个有效能用的方案
`00:46:19` 那这个故事想说明什么呢
`00:46:21` 就是想说
`00:46:22` 很难确定某样东西在大规模下到底行不行
`00:46:25` 这真的很难
`00:46:27` 那最后把time is the ga啊
`00:46:29` 不过到目前为止
`00:46:30` 我们做科研的主要路子还是先做小规模实验
`00:46:32` 再扩展到大规模
`00:46:34` 所以像NGBT急速运行
`00:46:36` speed run这种工作
`00:46:37` 咱们还是得重视
`00:46:39` 从中能学到不少东西
`00:46:40` 也能得到不少好点子
`00:46:42` 然后这些方法最终会用到大模型上
`00:46:44` 咱们看看大家是不是
`00:46:46` 还会在大规模上做消融实验
`00:46:48` 真正搞清楚
`00:46:49` MIU网在大规模上到底是不是比ADAMW好
`00:46:52` 诸如此类
`00:46:52` 这些都是挺有意思的问题
`00:46:53` 咱们现在还没答案
`00:46:54` 行good啊
`00:46:56` 我先停一下优化器这部分讲完了
`00:46:58` 我想确认一下
`00:46:59` 大家都听懂了没
`00:47:01` 另外因为我刚才提到了迷惘
`00:47:02` 这是个全新的话题
`00:47:03` 我之前没怎么细讲
`00:47:04` 里面水挺深的
`00:47:05` 或者说你能做到比啤酒beer更快的速度
`00:47:08` 不错对问题是
`00:47:10` SVD在GPU上跑的快吗
`00:47:12` SVD在GPU上其实不快
`00:47:14` 但他们根本没用SVD
`00:47:15` 他们用的方法是牛顿舒尔茨
`00:47:18` Newton shorts
`00:47:18` 这是一种只做矩阵乘法的有限迭代算法
`00:47:22` 用来近似实现矩阵的正交化啊
`00:47:25` 这里有好几个巧妙的地方
`00:47:27` 比如把当做一个独立的矩阵来处理
`00:47:29` 他有正交化这个思路
`00:47:31` 另外就是用牛顿舒尔茨方法
`00:47:33` 在系统层面去实现它
`00:47:34` Now i was just
`00:47:35` 所以这些都是挺重要
`00:47:38` 挺有意思的
`00:47:38` 点
`00:47:39` 对后排那位
`00:47:40` 谢谢亚当
`00:47:41` 别客气
`00:47:41` 超参数是共享的吗
`00:47:43` 嗯不也不一样
`00:47:44` 对
`00:47:44` 我觉得你知道
`00:47:45` 其实有几个人一直在做类似MUON风格的研究
`00:47:49` 其中一位比如杰里米伯恩斯
`00:47:52` Jeremy bernstein
`00:47:52` 你知道他好像一直主张
`00:47:54` 每一层都应该有自己的学习率啊
`00:47:57` 我觉得这件事的极限大概是这样
`00:47:59` 甚至更进一步
`00:48:01` 抱歉是每一层都该有自己的优化器
`00:48:04` 嗯把这个推到极致
`00:48:05` 就是说
`00:48:06` transformer里的每一个参数都是不一样的
`00:48:08` 为什么不给他们
`00:48:09` 每一个都配一个独立的优化器呢
`00:48:11` 那你也得为每一个单独调超参数
`00:48:13` 这我可不想折腾
`00:48:14` 但这说不定就是优化未来的方向
`00:48:17` 把明亮节又不老的想哭
`00:48:20` 怎么像fry和bash size
`00:48:23` 他们好像就盯着这两样挑出最好的
`00:48:26` 然后再拿它去调别的参数
`00:48:28` Bn in
`00:48:30` 但这些变量会和learning rate互相影响吗
`00:48:32` 对或者我理解的那个版本是
`00:48:35` 很多人就是在网格搜索里调the learning rate和BSLIDES
`00:48:38` 但按理说这些变量会和一大堆like
`00:48:40` 其他超参数产生交互
`00:48:42` 你难道不担心这个吗
`00:48:43` 我觉得一般来说
`00:48:43` 确实得担心超参数之间的相互作用
`00:48:45` 但现实是组合数量是指数级增长的对吧
`00:48:49` 就说你不可能把整个空间都拿来做网格搜索啊
`00:48:53` 所以我觉得大家通常的做法是
`00:48:55` 只对那些最让人头疼
`00:48:57` 最敏感的参数做网格搜索
`00:48:59` 而learning rate特别重要
`00:49:01` 我觉得只要你跳过惨
`00:49:02` 就知道learning rate是最该调的那个
`00:49:05` 所以他们在这上面会非常激进地下功夫
`00:49:08` 然后像weight decay这种参数
`00:49:10` 他们之后可能会单独扫一遍
`00:49:11` 好让这几个参数达到局部最优好行
`00:49:14` 谢谢好
`00:49:15` 那这堂课的最后部分
`00:49:16` 我想聊聊MIUEP牛批
`00:49:18` 这东西有点神秘
`00:49:19` 虽然有不少论文写过
`00:49:20` 他实际上也有不少人做了实现
`00:49:22` 但我觉得大家连底层的数学是什么
`00:49:25` 底层实现该怎么做都没能达成一致
`00:49:28` 不过啊大家还是有一套共享的核心理念的
`00:49:32` 而且这套方法看起来确实挺管用
`00:49:34` 所以我来聊聊我对这整套方案的理解
`00:49:37` 咱们姑且就这么叫它吧
`00:49:39` 那么这个目标希望已经讲得很清楚了
`00:49:42` 我觉得这张图就代表了我们的目标
`00:49:45` 这张图是说如果我们增大模型的规模
`00:49:48` 这里特指只增加宽度
`00:49:50` 模型变大时
`00:49:52` 最优学习率通常会变
`00:49:54` 但理想状态下
`00:49:55` 我们希望不管模型多大
`00:49:57` 最优学习率都别变
`00:49:58` 这就是我们想做的事儿
`00:50:00` 而为了做到这一点
`00:50:01` 我愿意动的旋钮是我愿意按层去调整初始化
`00:50:06` 我也愿意按每一个参数去调整学习率
`00:50:10` 唉WOR按钮的时候
`00:50:11` 我还愿意根据模型的大小
`00:50:12` 把残差连接这类东西放大或缩小好了
`00:50:16` 这就是我想玩的游戏
`00:50:17` 那它在实际中效果如何呢
`00:50:20` 已有好几篇论文
`00:50:22` 对这些内容做了相当广泛的研究
`00:50:25` SYRIVERS其实是一家芯片公司
`00:50:27` 他们还有一个语言模型训练部门
`00:50:29` 由HASSNESS负责
`00:50:31` 就是那位因scaling laws啊论文而出名的人
`00:50:34` 呃我觉得他们做的事挺有意思的
`00:50:36` 因为他们最近发表了很多关于语言模型
`00:50:38` new p scaling的论文啊
`00:50:40` 啊比如他们几年前推出的three rivers g b t
`00:50:44` 就是用标准的china配方来训练
`00:50:47` 0.1RB到3ZB参数的模型
`00:50:50` 另外他们还用了一种mp的变体
`00:50:52` 主要是为了更有效的调优超参数
`00:50:56` 我觉得挺不错的一点是
`00:50:58` 他们发现要是用mp参数化scaling laws的拟合
`00:51:03` 结果通常会更稳定
`00:51:05` 他们预测的mp趋势跟实际模型几乎严丝合缝
`00:51:10` 相比之下
`00:51:10` 我觉得那些非牛P模型的损失
`00:51:13` 预测波动要大得多哦
`00:51:14` 啊所以他们的结论是
`00:51:16` new p也许真是个很好的思路
`00:51:19` 这一点已经在大规模场景下得到了验证
`00:51:22` 大家看到了mini cpm这些例子
`00:51:24` 其实很多人已经用mp的变体训练过模型了
`00:51:27` 我其实也可以多讲讲训练方面的内容
`00:51:30` 或者举几个用mp训练出来的模型例子
`00:51:34` 不过我觉得现在更有意思的
`00:51:37` 其实是聊聊mop的概念基础和背后的数学原理
`00:51:41` 如果你对这块感兴趣
`00:51:43` 想找篇相对好懂的论文读读
`00:51:45` 我推荐这篇birth
`00:51:46` 这篇论文我觉得是对我来说最清晰的一篇
`00:51:49` 相关的论文有好几篇嗯
`00:51:52` 发起这项研究项目的GREGAN写了一系列论文
`00:51:55` 叫做tensor programs
`00:51:57` 我觉得这些资料有点晦涩啊
`00:51:59` JEREMYBERNSTEIN也参与了new one那部分的研究
`00:52:02` 他写了一篇很不错的文章
`00:52:03` 不是那种简单的回顾
`00:52:05` 而是一篇综述
`00:52:06` 把它放到了一个不同的框架里
`00:52:07` 我觉得很好理解
`00:52:08` 另外还有几位物理学家写的文章
`00:52:11` 在解释mp是什么
`00:52:13` 这方面也挺有意思的
`00:52:14` right seriverse人其实也做点类似的工作
`00:52:16` 好吧
`00:52:17` 不管怎样
`00:52:18` 咱们回到这篇论文
`00:52:19` 母P的核心想法
`00:52:20` 基于两个断言对吧
`00:52:21` 哎这是很典型的物理学家思维
`00:52:24` 比如如果我让网络变大
`00:52:26` 假设我现在只改宽度对吧
`00:52:28` 我把网络的宽度变大
`00:52:30` 我要提出两个不变量
`00:52:31` 在做扩展极限时
`00:52:33` 这两个量应该保持不变
`00:52:34` 首先是初始化时的激活值
`00:52:37` 也就是说如果我初始化模型输入一些随机数据
`00:52:41` 激活值的大小应该大致保持不变
`00:52:44` 不随网络规模变化
`00:52:45` 是如果他们随网络变大而爆炸
`00:52:48` 或者随网络变大而缩到零
`00:52:50` 那就是我参数化方法选错了
`00:52:52` 这听起来挺合理的
`00:52:54` 不管网络多大
`00:52:55` 激活值的大小都应该大致维持在同一个量级上
`00:52:58` 好咱们做一次梯度更新
`00:53:00` 激活值的变化量应该是ogle one
`00:53:03` 所以这就叫特征学习
`00:53:05` 也就是说做一次梯度更新
`00:53:07` 网络里的东西会发生显著变化
`00:53:09` 而且这个变化量应该是网络宽度的固定函数
`00:53:13` 嗯在座的可能有些是搞理论的对吧
`00:53:16` 你可能听说过神经正切和neural cangent kernels
`00:53:19` 他的表现会非常不一样
`00:53:21` 在那种情况下
`00:53:22` 随着网络变宽
`00:53:23` 激活值的变化量反而会趋近于零
`00:53:25` 咱们不希望看到这种情况
`00:53:27` 因为即便网络规模很大
`00:53:31` 好这两个就是我们要找的不变量
`00:53:34` 这说得通
`00:53:35` 基本上就是说无论网络宽度如何变化
`00:53:38` 激活值及其变化量都应保持大致稳定
`00:53:40` Entals while
`00:53:42` 尤其是会在单个激活值和范数之间切换
`00:53:45` 如果每个激活值大致是o one
`00:53:47` 那么整个激活向量的范数
`00:53:48` 应该是隐藏单元数量的平方根
`00:53:50` 因此在那一层里好
`00:53:53` 这就是关键所在
`00:53:55` 说到mp它其实挺有意思的
`00:53:57` 因为它一开始基于的是一组非常简单
`00:54:01` 也很容易让人接受的假设
`00:54:03` 你们可能都会认同这么做是合理的
`00:54:05` 然后我们会再追加一些很强的假设
`00:54:07` 但到最后我们会得到一套
`00:54:09` 非常简单的scaling规则
`00:54:11` 他们就这么从这些假设里推导出来了
`00:54:14` 咱们回想一下
`00:54:15` 目标是要确保初始化时的激活值
`00:54:18` 大致保持在单位两极X那咱们该怎么做呢
`00:54:22` 假设我们有一个简单的深度线性网络
`00:54:25` 把权重都初始化为按sigma平方缩放的高斯分布
`00:54:29` 那这个矩阵的谱范数spectral norm大概是多少
`00:54:32` 那就把它写下来
`00:54:33` 我不要求你们去验证
`00:54:35` 但这属于标准的矩阵浓度
`00:54:36` matrix concentration论证of
`00:54:38` 这是关于随机高斯矩阵算子范数的论证
`00:54:42` 接着请注意
`00:54:43` 在某些情况下
`00:54:44` 当fan out不比fan in大太多时
`00:54:47` 你会得到该层的输出
`00:54:49` 大致等于输入乘以该算子范数
`00:54:52` 这始终是一个上界
`00:54:53` 但在某些高危情况下
`00:54:54` 它只是近似成立
`00:54:56` 如果这成立
`00:54:57` 那我们现在能做什么呢
`00:54:59` 我们可以求解sigma
`00:55:00` 努力让激活值在所有层所有宽度上都保持一致
`00:55:04` 设想一下
`00:55:05` 在DLY层
`00:55:06` 我大约有根号下FE的激活值
`00:55:10` 记住这与我们的记号一致
`00:55:12` 那至少对整个向量而言
`00:55:14` 结果应该是根号下NL
`00:55:16` 这就是我们归纳法的基础情形
`00:55:18` 然后归纳步骤说
`00:55:19` 记住我在DL层有这个算子范数
`00:55:22` 它是fan in和fan out的函数
`00:55:24` 所以如果我选取某个特定的sigma
`00:55:27` 这一项就会等于那一项
`00:55:30` 我们就把它当作拟设
`00:55:32` that嗯
`00:55:33` 我先假定它就是答案
`00:55:34` 然后看看归纳法会怎么样
`00:55:36` 如果我把这个作为假设代入sigma
`00:55:38` 注意我得到算子范数之比
`00:55:40` 就是fan out除以根号下fan in好把它带回这里
`00:55:44` 于是我会得到输出的范数
`00:55:46` 等于层中单元数的平方根
`00:55:48` 再加上一些我懒得细究的低阶项
`00:55:51` 后边那这个sigma是怎么来的呢
`00:55:54` 说实话
`00:55:55` 这乍一看像是凭空变出来的
`00:55:58` 但只要把这种噪声初始化代入我前面的式子里
`00:56:01` 大致就能验证这一点
`00:56:04` 根据公式
`00:56:05` 本质上
`00:56:06` 每一层的激活值规模
`00:56:08` 大致都等于该层隐藏单元数的平方根
`00:56:11` 这里确实做了一些假设
`00:56:13` 比如这个近似相等
`00:56:14` 但这套假设基本上是站得住脚的
`00:56:16` 所以条件a one很简单
`00:56:18` 但说实话条件A2就要稍微棘手一些了
`00:56:22` 所以咱们现在得来处理更新这一块了
`00:56:25` 我只讨论深度线性网络
`00:56:27` 在SGD下批次中单个样本的情况
`00:56:30` 那它具体会是什么样呢
`00:56:32` 想想看对于特定层L权重的变化
`00:56:35` 其实就是损失梯度和激活值之间的一种质疑
`00:56:38` 更新
`00:56:39` 这纯粹是由反向传播的性质决定的
`00:56:42` 前向传播是质一乘法
`00:56:44` 而反向传播则表现为质疑外机
`00:56:47` 好了
`00:56:48` 刚才说的是权重的变化
`00:56:51` 那接下来这一层激活值的变化又是怎样的呢
`00:56:55` 哎那么关于激活值的变化
`00:56:58` 咱们取H等于w passion hl减完反的展开对
`00:57:03` 把它展开成这个公式
`00:57:06` 这就是DL层激活值的精确变化量
`00:57:09` 它包含两项
`00:57:10` 一项是直接向源于DLY层激活值的变化
`00:57:14` 另一项是交叉项
`00:57:15` 反映的是权重变化带来的影响
`00:57:17` 而不仅仅是前一层激活值变化带来的影响
`00:57:21` 所以这个更新涉及两项
`00:57:23` 这就是第L层激活值的变化量
`00:57:25` 接下来咱们来看看这三项中的每一项
`00:57:30` 我希望这三项的数量级保持一致
`00:57:32` 而且这个数量级都是根号下NN分
`00:57:34` 为什么呢
`00:57:35` 大家还记得前面两页幻灯片提到的条件吗
`00:57:38` 对于A2
`00:57:39` 我们希望经过一次梯度更新后
`00:57:41` 激活值的变化量保持在o quin one a great in mind
`00:57:45` 所以为了满足这个条件
`00:57:48` 该层激活值的范数应当是根号下NL对
`00:57:52` 所以为了做到这一点
`00:57:54` 我希望这三个项都达到那个数量级
`00:57:56` 只要假设成立
`00:57:58` 且各项之间没有相互抵消
`00:58:00` 那么这个估算的数量级就是准确的
`00:58:03` 你可以看到这些粗略的估算
`00:58:05` 是如何一点点累加起来的对吧
`00:58:08` 这里我们做了一些假设
`00:58:09` 比如假设不存在相互抵消的情况
`00:58:12` 这我们提供了一种有趣的视角
`00:58:14` 让我们去思考
`00:58:15` 这些对象各自的数量级该如何评估嗯
`00:58:19` 所以此刻我们思考这个问题的正确方式
`00:58:22` 并不是说他是正坐在这里进行严谨的数学推导
`00:58:25` 哎
`00:58:26` 我在这里做的
`00:58:27` 本质上就是在追踪所有这些项的数量级B
`00:58:30` 我的意思是基于我们之前的假设
`00:58:33` 这些项的规模大致应该是多少
`00:58:35` 就有点像物理学家似的数学嗯
`00:58:38` 没有冒犯物理学家的意思
`00:58:40` 这种方法正是物理学家们这样发展出来的好
`00:58:43` 那我们现在哦抱歉
`00:58:45` 先退回到上一张幻灯片
`00:58:46` 把这些内容讲清楚嗯
`00:58:48` 第一项基本是假设得来的对吧
`00:58:50` 也就是所谓的归纳
`00:58:51` 假设
`00:58:52` 我们假设delta h holding l one的数量级是正确的
`00:58:56` 对吧
`00:58:56` 所以这项是自然成立的嗯
`00:58:59` 另外两项就比较棘手了
`00:59:01` 这里涉及几项相乘嗯
`00:59:03` 权重在算子范数下的变化量
`00:59:06` 再乘以LN的平方根诶
`00:59:08` 所以既然这两项分别代表了各自的数量级
`00:59:11` 我们就能大致推算出delta l需要多大对吧
`00:59:14` 我们可以解除它
`00:59:15` 因为我们知道左边的值需要是skirt a h or
`00:59:18` 接着我们处理这一项
`00:59:20` 于是我们解除这个算子范数
`00:59:22` 得到的正是这个量
`00:59:23` 所以这就是我们下一张幻灯片要达成的目标
`00:59:27` 我想搞清楚什么样的学习率能让参数更新量
`00:59:30` 大致正比于fan out的平方根
`00:59:32` 反比于fin的平方根
`00:59:34` 所以目标就是这个
`00:59:36` 刚才我说了
`00:59:36` 选好学习率后
`00:59:38` 这个关系应该大致成立
`00:59:40` 那具体怎么做呢
`00:59:42` 好咱们再做一个假设
`00:59:44` 这个假设可能是我觉得最不太能接受的
`00:59:47` 我们假设损失更新是按单位缩放的
`00:59:50` 换句话说
`00:59:50` 我们假设模型随着规模扩大
`00:59:53` 确实取得了显著且非频繁的进展
`00:59:56` 且这种进展模式大致相似
`00:59:59` 呃说实话我自己也不太觉得这个假设很合理
`01:00:03` 但咱们先把它作为假设写在这儿
`01:00:05` 但这个假设的关键在于
`01:00:07` 如果它成立
`01:00:08` 后面的数学推导就会非常清晰简洁
`01:00:10` 因为损失的变化量等于梯度与权重
`01:00:13` 变化量的乘积
`01:00:15` 这就是泰勒近似
`01:00:16` 因为他是质疑的
`01:00:17` 所以我们可以把它改写为
`01:00:19` 弗罗贝尼乌斯范数和算子范数
`01:00:20` 按cc右边的这一项
`01:00:23` 就能帮我们得到想要的结果了
`01:00:25` 所以这一项是o o one
`01:00:27` 我们希望这一项是sq RT r n l
`01:00:31` 除以SQRL免因就是右边这一项
`01:00:36` 最后我们来看L这一项的梯度
`01:00:38` 这将是scared aim of our team and all minus one two
`01:00:43` 那我现在需要什么样的ea呢
`01:00:45` 我们需要解除这个量
`01:00:46` 也就是这两者的比值
`01:00:49` 大家看也就是an l除以part
`01:00:51` 所以我们做了一个假设
`01:00:52` 认为左边这一项是over one
`01:00:54` 然后解除了所有变量
`01:00:55` 最终得出了某一层学习率的fan out与fan in之比
`01:00:58` 另外补充一点
`01:01:00` 对于ADAM优化器
`01:01:01` 由于其中一些变化
`01:01:03` 你会得到已换人Z1分
`01:01:05` 在已喷的so放比例
`01:01:06` 我在这里提一下啊
`01:01:08` 那么我刚才带大家过了一遍
`01:01:11` 针对各个组件的scaling论证
`01:01:13` 希望这些论证看起来比较直观
`01:01:16` 但说到底你可能更关心的是
`01:01:18` 这到底意味着什么
`01:01:20` 对你有什么用
`01:01:21` 这意味着我们在设置初始化和学习率时
`01:01:25` 会稍微调整一下方法度
`01:01:27` 所以针对new p也就是蓝色部分
`01:01:31` 我们的初始化方式如下
`01:01:32` 蓝色方框展示了我们的具体做法
`01:01:34` 初始化公式为一
`01:01:36` 除以根号下df index乘以一个系数
`01:01:38` 该系数为一除以根号下fan out
`01:01:41` fainsane on monster的后果
`01:01:43` 这个比值是in
`01:01:44` 那就等同于我们常规的初始化方式
`01:01:47` 不这是标准参数化
`01:01:49` 但如果这个比值不小于一
`01:01:51` 得到的结果基本上就不一样了
`01:01:54` 你会得到更接近根号下fan out与find in的比值
`01:01:57` 更有趣的是
`01:01:59` ADAM的情况有点不一样
`01:02:01` 它的推导我就不细讲了
`01:02:02` 主要就是把那个20分子想去掉
`01:02:04` 这样就得到our enger air far in
`01:02:07` 把它当做一种针对特定层的学习率
`01:02:09` 所以简单来说
`01:02:10` 对ADAM而言
`01:02:11` fan underscore in比较大的层得到的学习率会更小
`01:02:15` 这样你能实现这种层次适应的学习率
`01:02:18` 挺有意思的
`01:02:19` 嗯好嗯
`01:02:21` 以上就是new underscore p的推导
`01:02:23` 刚才我讲了不少细节
`01:02:25` 所以我想聊聊这背后的核心要点到底是什么
`01:02:30` 核心要点在于
`01:02:31` Move on underscore p
`01:02:33` 这个论点本身非常有趣
`01:02:35` 这不仅是因为里面有实际有用的东西
`01:02:38` 更是因为推导出这个算法的过程本身就很有趣
`01:02:42` 因为这里用到的数学方法很不一样
`01:02:45` 他的思路是好吧
`01:02:46` 咱们要取一个缩放极限
`01:02:49` 针对我的网络
`01:02:51` 我先断言它具备某种不变性
`01:02:53` 接着做一些额外假设
`01:02:55` 然后去推导这类超参数需要满足什么约束
`01:02:57` Well
`01:02:58` 这些约束本质上给你的超参数
`01:03:01` 也划定了一些scaling limits
`01:03:03` 嗯我觉得这是一个通用的原则
`01:03:05` 用来推导不同的算法或超参数的scaling
`01:03:09` 这大概跟你们在cs或ml训练中
`01:03:11` 习惯的做法很不一样
`01:03:12` 好以上就是mp的推导
`01:03:14` 我想用几个实证例子来收尾
`01:03:16` mp这部分
`01:03:17` 有个独立研究者发了篇挺有意思的论文
`01:03:20` 基本上是用各种不同的方式
`01:03:22` 对mp做了压力测试啊
`01:03:24` 你可以把mp看作是一种调整超参数的流程
`01:03:27` 不参数
`01:03:28` 所以取决于你怎么做
`01:03:30` 比如ADAM的学习率就是网络不同组件的函数
`01:03:33` 所以你会涉及到嵌入参数
`01:03:35` 注意力参数输入输出的MLP或者是soft max线性层
`01:03:39` 这些情况在mp下会有不同的缩放表现
`01:03:43` 同时也有一些遵循非mp规则的缩放方式
`01:03:47` 有意思的是
`01:03:48` 那篇论文复现了不同规模下训练的language model的不少
`01:03:52` Miur a p
`01:03:53` 结果就会不出所料
`01:03:55` 他得出的主要结论和mini cpm等研究一致
`01:03:58` 只要new underscore p设置正确
`01:04:01` 且仅进行缩放啊
`01:04:03` 比如在这里用一种非常受控的方式
`01:04:05` 你就能得到和基线new p一样的学习率
`01:04:08` 最优性不变性
`01:04:09` 或者看看那种追踪注意力啊
`01:04:11` 投影偏差的new p变体
`01:04:13` 这两种情况都内地都能很好的迁移
`01:04:16` 但你可能会问
`01:04:17` 现实中其实有很多mute perience的没涵盖的情况
`01:04:21` 比如技术上讲
`01:04:22` SWIGLOW就不在new underscore p范围内
`01:04:25` 初始化或r ms norm的变动也不行
`01:04:28` 要是你大幅改动优化器
`01:04:30` 那也会破坏规则
`01:04:31` 所以你可能会问这些理论不允许的因素里
`01:04:35` 哪些真正破坏了new pp内
`01:04:37` 其实大部分时候这些都能和new underscore p兼容
`01:04:41` 但有些情况不行
`01:04:42` 比如在r max norm中学习增益项
`01:04:44` 事实证明这会让MUSIMP失效
`01:04:47` 虽然很多时候去掉它们并不会影响效果
`01:04:50` 所以这倒不算个坏主意
`01:04:51` 但MUP确实会因此出问题
`01:04:53` and另外如果你用一些更冷门的优化器
`01:04:58` 比如lion
`01:04:59` 它基本上依赖梯度的符号
`01:05:01` more underscore p似乎也会失效
`01:05:04` 符号梯度在理念上和miu on挺像的
`01:05:06` 所以mu underkey
`01:05:07` 可能还会在其他一些有趣的地方失效
`01:05:10` 最后最让人担心的是
`01:05:12` 如果你做较大的解耦权重衰减
`01:05:14` mop最终会严重失效啊
`01:05:16` 所以这可能是他唯一没通过的压力测试好了
`01:05:20` 为了收尾这最后一节
`01:05:21` 你最终可能会问mp到底有没有用
`01:05:24` 如果你真心想让学习率稳定下来
`01:05:26` 他其实挺管用的
`01:05:28` 有很多这类实验都显示
`01:05:30` 当你增加宽度时
`01:05:32` 最优学习率会随规模发生巨大但可预测的偏移
`01:05:36` 而用了MUP之后就不会这样啊
`01:05:38` 所以它是工具箱里的众多工具之一
`01:05:40` 用来控制那些随规模变化而漂移的超参数
`01:05:44` 我觉得这是个很有意思
`01:05:46` 有待探索的研究方向
`01:05:47` 还没到盖棺定论的程度
`01:05:48` 也没说这就是唯一正确的做法
`01:05:50` 不过MUI风格的初始化方案很有前景
`01:05:52` 对另外光是去拟合scaling laws也很有前途行
`01:05:58` 所以我想用最后一点来收尾
`01:06:00` 真实环境中的扩展其实非常棘手
`01:06:03` 就像我在某条回复里说的
`01:06:05` 我觉得scaling laws最初被介绍的方式
`01:06:09` 会让人感觉它像一门科学
`01:06:11` 好像只要画出这条线
`01:06:13` 按步骤操作
`01:06:14` 你就能确切的知道规模变大后会发生什么
`01:06:17` 但实际上现实情况比这要混乱的多
`01:06:20` 未知因素也多得多
`01:06:21` 唉大家确实会用scaling laws来做很多事
`01:06:25` 比如选架构
`01:06:25` 选优化器
`01:06:26` 选超参数
`01:06:27` 虽然大家拿scaling laws做这些事
`01:06:30` 但这其实挺讲究技巧的
`01:06:31` 因为你根本没法确定它能不能无限外推下去
`01:06:34` 哎你会想做一些合理的事
`01:06:35` 尽量提高成功率对吧
`01:06:37` 所以大家想出了不少办法
`01:06:39` 比如用mp或者去搜最优学习率
`01:06:42` 诸如此类
`01:06:43` 这些都是控制超参数漂移的方法
`01:06:45` 但目前还没有万全之策
`01:06:47` 也许明年会有个模块告诉我们问题解决了
`01:06:50` 不过现在还差点意思
`01:06:52` 太好了
`01:06:52` 非常感谢之后我可以回答大家的问题
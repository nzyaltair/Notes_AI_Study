# Sim2Real 迁移

Sim2Real 的目标不是让仿真看起来真实，而是让部署策略对真实系统的关键差异保持鲁棒。

主要差距包括质量/摩擦、执行器回差、视觉外观、传感噪声、控制时延、柔性接触和任务分布。常用手段是域随机化、系统辨识、真实数据微调、残差控制、硬件在环和课程式迁移。

推荐验收阶梯：日志回放 → 单元仿真 → 集成仿真 → 限速空载 → 受限真机 → 扩大场景。每步应量化成功率、超力/碰撞、时延、恢复率和可回滚性。
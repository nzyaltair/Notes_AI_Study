---
title: "【极致中配】2026年最新版 Stanford CS336: 从头构建大语言模型"
url: "https://www.bilibili.com/video/BV11LEA6eEuj/?p=14"
bvid: "BV11LEA6eEuj"
cid: "39163268285"
author: "常青藤名校课"
upload_date: "2026-06-05"
created: 2026-08-17
tags: ["bilibili", "clippings"]
---

## 简介

如果这个中文配音版对您理解 CS336 有帮助，欢迎点赞、投币、收藏支持一下，也可以点个关注。
我会持续更新更多 Stanford / AI / 大模型相关课程的中文配音课程，尽量帮大家用更低的理解成本看懂这些高质量课程。你的支持会让我更有动力把后续内容继续做完、做好。

## 字幕

`00:00` 好咱们开始
`00:01` 今天是数据系列的第二天
`00:04` 上次咱们聊过
`00:05` 数据可不是天上掉下来的
`00:07` 你得想想数据从哪来
`00:09` 一般来说互联网是由实时服务组成的
`00:12` 你得把这些服务里的数据抓下来或者导出来
`00:15` 然后还得做一步数据处理
`00:17` 咱们还聊了社会层面的考量
`00:19` 比如服务条款
`00:20` 版权
`00:21` 你要么拿到授权
`00:22` 要么原因合理使用原则
`00:23` 数据处理这事挺复杂的
`00:25` 今天咱们重点看看data processing pipeline
`00:28` 也就是怎么转换过滤去虫和混合数据
`00:31` 就是数据转换过滤去种和混合这些环节
`00:35` 最后咱们简单聊聊后训练数据
`00:37` 特别是现在大家是怎么用合成数据的
`00:40` 另外到底什么才算内容
`00:41` 这里面还有一些细微的讲究
`00:43` 一般我们会把页脚
`00:45` 页眉
`00:46` 还有菜单这些给删掉
`00:47` 只盯着正文内容提取
`00:49` 你要是看过common call里的东西
`00:52` 就会发现它根本不是文本
`00:54` 要么是HTML
`00:55` 有时候是PDF
`00:56` 如果是GITHUB的数据
`00:58` 那就是目录
`00:59` 所以数据转换的重头戏就是处理HTML
`01:02` 毕竟网上大部分内容都是HTML
`01:04` 在这类处理中
`01:05` 许多方法其实相当依赖经验法则
`01:08` 例如移除导航栏
`01:10` 广告等冗余内容
`01:11` 并提取页面的主体内容
`01:13` 此外关于内容的定义
`01:15` 其中还存在一些细微的差别
`01:17` 通常我们会删除页脚
`01:20` 页眉和菜单
`01:21` 仅提取正文内容
`01:22` 不过在某些场景下
`01:24` 部分导航元素或许有助于理解网页的整体布局
`01:28` 因此究竟哪些属于内容
`01:30` 哪些不属于往往界限模糊
`01:33` 那么网页中的图片和表格又该如何处理呢
`01:36` 本质上这是一个有损过程
`01:38` 因为需要将HTML线性化
`01:40` 而HTML至少是层级结构的
`01:43` 或者说从渲染后的视觉效果来看
`01:45` 它是可视化的
`01:46` 最终输出并转换为token序列
`01:48` 特别是表格处理起来较为复杂
`01:51` 简单表格可以使用markdown渲染
`01:53` 但遇到嵌套表格时
`01:54` 挑战则显著增加
`01:56` 你总需要在某个环节做出妥协或采用近似处理
`02:00` 因此HTML转文本的处理通常基于规则
`02:03` 原因在于基于规则的处理方式速度极快
`02:07` 而且你看在这儿你不用搞得太复杂
`02:11` 你不需要太高的智能水平
`02:13` 所以基于规则的方法通常就行得通
`02:15` 不过我觉得
`02:16` 现在咱们可以考虑用基于模型的方法了
`02:19` 他们得跑得飞快
`02:20` 还得干点儿更智能的活儿
`02:22` 但只要你看看数据
`02:23` 就会发现里面总有些瑕疵
`02:25` 因为基于规则的处理方式难免会有出错的时候
`02:29` 就像我们上次讲过的
`02:30` 准确度确实关键
`02:32` 不过这得看你用的是什么工具
`02:34` 比如你用brazil parts或者TRANSLERR的话
`02:37` 在那些扩展的DCOM评估里啊
`02:40` brazio pass的效果
`02:41` 我觉得是比其他工具都要好的
`02:43` 我还会简要谈谈PDF
`02:45` hugin face发布了一个名为FPDF的数据集
`02:49` 你看PDF文件要是没用专门的阅读器打开
`02:52` 显示出来大概是这个样子的
`02:54` 这些内容得先渲染出来才行
`02:56` 毕竟PDF就像网页一样
`02:59` 满互联网都是common crow里也有收录
`03:02` 一般来说common crow主要抓文本
`03:04` 但有时候光看个链接
`03:05` 要是没后缀
`03:06` 你下载前根本不知道它是不是个PDF
`03:08` 这篇博文细节很多
`03:09` 我就不一一展开了
`03:10` 感兴趣的朋友可以直接阅读原文
`03:12` 举个例子
`03:13` common crowd里很多PDF因为文件过大被强行截断
`03:16` 这就带来了一些麻烦
`03:18` 因此需要重新抓取
`03:19` 此外还有一个问题
`03:20` 拿到PDF文件后究竟该如何将其转换为文本呢
`03:23` 另外有些PDF其实只是扫描件
`03:25` 本质上就是图片
`03:26` 因此需要重新抓取
`03:28` 此外还有一个问题
`03:29` 拿到PDF文件后
`03:31` 究竟该如何将其转换为文本呢
`03:33` 另外有些PDF其实只是扫描件
`03:36` 本质上就是图片
`03:37` 这篇论文测试了多种工具
`03:39` 但核心思路仍是利用视觉语言模型进行OCR
`03:42` 这肯定比之前处理纯文本的成本高得多
`03:45` 好在或者说不好
`03:47` 在PDF在整个互联网中其实只占很小一部分
`03:49` 但他们很有价值
`03:51` 因为通常愿意制作PDF意味着内容经过精心整理
`03:54` 不像普通网页那样随意
`03:56` 因此PDF的平均质量通常高于HTML
`04:00` 文件过滤后的数据和PDF需要大量清洗
`04:03` 甚至多于网页数据
`04:04` 因为在HTML中HEP等标签能提供语义信息
`04:08` 而在PDF中这些信息往往丢失了
`04:10` 现在你手里有了文本
`04:12` 但这还远没结束
`04:13` 咱们还差得远呢
`04:15` 下一步就是过滤
`04:16` 我先稍微抽象的讲讲过滤到底是什么
`04:20` 咱们先看这个基础模块
`04:22` 假设你有一些想要获取的目标数据
`04:25` 这通常包括一小部分高质量数据
`04:28` 以及大量的原始数据数据
`04:30` 这是从上一轮转换过来的
`04:32` 刚出炉的token批次
`04:34` 咱们的目标是从这些原始数据里挑出一个子集
`04:37` 这个子集要和目标数据相似
`04:40` 这就是过滤的一般框架
`04:42` 几乎所有过滤方法都套用这个模式
`04:45` 过滤的原因有很多
`04:46` 比如你在训练英语或德语模型时
`04:48` 得先识别语言
`04:49` 把不匹配的给过滤掉
`04:51` 过滤的核心目的是筛选质量
`04:52` 我们要找的是高质量数据
`04:54` 剔除那些低质量的
`04:55` 咱们不要那些垃圾信息
`04:57` 要的是像百科全书一样有深度的内容
`04:59` 还有另一个应用就是毒性过滤
`05:02` 毕竟网上有很多让人不适的内容
`05:04` 你肯定不想拿这些去训练语言模型
`05:07` 谢谢好
`05:09` 针对过滤算法
`05:10` 我们需要做到上述几点
`05:11` 算法必须能从目标数据中泛化对吧
`05:14` 毕竟目标数据是一致的
`05:16` 算法
`05:16` 不能只盯着目标数据
`05:18` 还得运行高效
`05:19` 因为你需要在全互联网数据上执行一遍
`05:22` 这意味着数据量可能高达10000000000000token
`05:25` 总体而言
`05:26` 过滤后的数据量极小
`05:28` 仅占原始数据总量的个位数百分比
`05:30` 谢谢好
`05:31` 让我们回顾一下针对目标数据的通用框架
`05:34` 给定原始数据R和目标数据
`05:37` T任务是寻找一个子集
`05:39` 基本思路是先根据R和T训练模型
`05:44` 建立评分标准
`05:45` 然后根据R中数据的得分进行筛选
`05:48` 保留高分数据
`05:49` 因此分类器主要有两种类型
`05:52` 第一种是生成模型
`05:53` 你看你手头有目标数据
`05:56` 你可以直接基于这些数据来估算模型
`05:59` 注意这一步必须低成本且高效
`06:02` 因此我们通常不会训练大型语言模型
`06:04` 例如km m7
`06:06` 基本做法是训练一个五元语法模型
`06:08` 如今更常见的做法也是咱们最近经常看到的
`06:10` 就是直接训练一个分类器
`06:12` 分类器会给出预测结果
`06:15` 将其标记为正类
`06:16` 对于属于目标及T的样本标签设为负
`06:20` 对于在原始数据R中
`06:23` 但不在T中的样本标签设为正
`06:26` 所以你看T其实就是你的正样本
`06:29` 而从R中随机抽取的一部分则是你的负样本
`06:33` 然后再去训练分类器
`06:35` 大家通常使用fast tx
`06:36` 主要是因为它的运行速度快
`06:38` 它通常是一个线性分类器
`06:40` 采用磁带模型
`06:41` 拿到这个模型后
`06:43` 面对每个新闻档
`06:44` 你都可以给它打一个分数
`06:45` 然后根据你设定的质量标准
`06:47` 确定一个阈值
`06:49` 保留符合条件的样本
`06:50` 筛选方式有时是随机的
`06:52` 有时则不是对
`06:54` 所以你看这其实是一种基于模型的过滤方法
`06:57` 大家还记得上一节课吗
`06:59` 那时候很多数据集还没有采用这种
`07:02` 基于模型的过滤方法
`07:03` 主要是担心引入过多的偏差
`07:06` 现在嘛我觉得大家基本上都会进行一定程度的
`07:09` 基于模型的过滤
`07:10` 因为除非你的算力非常充足
`07:13` 否则可能没必要做那么多过滤
`07:15` 理论上你可以直接用所有数据来训练
`07:17` 但大多数人的算力都不够
`07:19` 必须想清楚过滤策略
`07:21` 否则就是在低质量内容上白白浪费算力
`07:24` 谢谢
`07:25` 接下来我们具体讲讲过滤方法是如何落地的
`07:28` 大家还记得我之前提过的语言识别吗
`07:31` 他的目标很明确
`07:32` 给定一段文本
`07:33` 判断它是否属于某种特定语言
`07:35` meta训练了一套快速的文本语言识别模型
`07:39` 大家通常可以直接拿来用
`07:41` 它支持176种语言训练
`07:43` 数据来自多个多语言网站
`07:45` 比如维基百科上有各种语言的内容
`07:47` 还有翻译网站以及各语言专属网站
`07:49` 与其他任务相比
`07:51` 语言识别其实相对简单
`07:53` 用一个简单的分类器看几个词
`07:55` 就能分辨出是西班牙语还是日语
`07:57` 不过由于文本中常存在语言混用和方言情况
`08:01` 细节上还是有点复杂
`08:02` 因此说它已经完全解决
`08:04` 可能有些夸张
`08:05` 但这确实不是训练语言模型的关键瓶颈
`08:08` 不过由于文本中存在语码转换现象
`08:10` 且涉及一些方言
`08:12` 细节上较为复杂
`08:13` 因此说他已彻底解决
`08:15` 可能言过其实
`08:16` 但这确实并非训练高质量语言模型的关键瓶颈
`08:19` 打个比方
`08:20` 假设你想精通数学
`08:21` 那就需要去搜集大量的数学数据
`08:23` open mattex是2023年发表的一篇论文
`08:27` 其目标是整理一个大规模的数学文本语料库
`08:30` 这个数据处理流程包含几个步骤
`08:33` 它并非仅训练单个分类器
`08:35` 首先你会使用一些基于规则的方法
`08:38` 检查文本是否包含latex命令
`08:39` 他们还使用了CANN
`08:41` 这是一种生成式方法
`08:43` 他们在proof pile
`08:45` 一个已知的数学数据集上对其进行了训练
`08:49` 若困惑度低于阈值
`08:51` 则予以保留
`08:51` 接着你还训练一个fast tax分类器
`08:54` 用于判断文本是否属于数学写作
`08:56` 此外他们还使用了CANNM
`08:58` 这是一种生成式方法
`09:00` 他们在proof pile
`09:01` 一个以已知的数学数据集上对其进行训练
`09:04` 若困惑度低于特定阈值
`09:06` 则予以保留
`09:07` 接着你还训练一个fast text分类器
`09:09` 用于判断文本是否属于数学写作
`09:12` 结果他们得到了150亿个token
`09:15` 用来训练模型
`09:17` 论文里显示这种更有针对性的数据筛选方法
`09:20` 能让模型在数学方面表现更好
`09:23` 相比之下
`09:24` 那些在未经过滤
`09:26` 数据量大20倍的语料上训练的模型
`09:28` 效果就没这么好
`09:29` 因此我们将质量过滤视为一种工具
`09:32` 你可以自行定义何为质量
`09:33` 质量并无统一定义
`09:34` 若将质量定义为数学能力
`09:36` 则收集数学数据即可提升模型在该领域的表现
`09:39` 以GPT-3为例
`09:41` 此前虽已提及
`09:42` 但未将其纳入本框架
`09:44` 我们再次简要说明
`09:45` 例如正样本包括维基百科和网络文本
`09:48` 这些页面源自高权重链接
`09:50` 例如正样本包括维基百科和网络文本
`09:53` 这些页面源自高权重链接
`09:55` 他们训练了一个线性分类器
`09:57` 仅当分类器评分超过阈值时才保留该文道
`10:00` 以早期的拉玛论文为例
`10:02` 其参考的是维基百科引用的页面
`10:04` 而非维基百科文章本身思路类似
`10:07` 以微软发布的five福一为例
`10:10` 有趣的是
`10:11` 其效果相当不错
`10:13` 该方法同样适用于此框架
`10:15` 因此他们原始数据并非预先切分的stack overflow
`10:19` Python子集
`10:20` 而是包含所有代码的完整数据集
`10:23` 他们定义了一个prompt
`10:24` 用于评估数据是否具备交易价值
`10:28` 接着他们使用g p t-4
`10:30` 配合该提示词
`10:32` 对R中的10万个样本子集进行分类
`10:36` 以观察结果
`10:37` 那些被判定为正向的样本会被保留
`10:41` 并称为目标
`10:43` 因此这里的目标实际上就是
`10:45` 高成本分类器的输出结果
`10:47` 随后你再去训练一个成本更低的分类器
`10:50` 例如他们使用的随机森林
`10:51` 不过fast text也是不错的选择
`10:53` 接着筛选出被该分类器判定为正向的数据
`10:59` 他们还展示了使用该数据集的效果显著提升
`11:02` 远优于直接使用原始数据
`11:04` 这意味着模型能够以更少的训练部署
`11:08` 达到更高的性能指标
`11:10` 谢谢接下来毒性检测的原理也类似
`11:14` 有一个名为exo toxic comments的数据集
`11:17` 它源自一个旨在改善网络交流环境的项目
`11:21` 谢谢接下来毒性检测的原理也类似
`11:24` 有一个名为jx saw toxic comments的数据集
`11:27` 它源自一个旨在改善网络交流环境的项目
`11:31` 我们也可以采用同样的方法
`11:32` 明确定义正立和复利
`11:34` 然后利用这些数据训练一个分类器好了
`11:37` 现在我想你已经掌握了一些工具案例
`11:41` 也获得了一些灵感
`11:43` 确定好目标数据后
`11:44` 构建相应的分类器
`11:46` 即可用它来过滤common call数据集
`11:48` 这里需要补充一个细节
`11:50` 稍微复杂一些
`11:51` 具体来说
`11:52` 所需数据的类型取决于你计划训练的模型类型
`11:56` 这主要取决于训练时使用的token数量
`11:58` 因此并不存在所谓的最优阈值
`12:01` 之前观察分类器时
`12:02` 它会输出一个分数
`12:04` 不能仅凭0.9的分数就断定其最优
`12:06` 因为这取决于具体的应用场景
`12:08` 直观来看
`12:09` 如果训练周期较长
`12:11` 可以容忍较低质量的数据
`12:13` 如果训练时间较短
`12:14` 则通常情况下你肯定希望数据质量更高
`12:19` 打个比方
`12:20` 如果训练时间充足
`12:22` 当然更倾向于高质量数据
`12:24` 但这受限于现有的数据时
`12:26` 并非完全由你选择
`12:28` 这里展示了一项初步实验
`12:31` 该实验由MICHAELRYAN完成
`12:33` 基本上
`12:34` 这张图展示了针对一个1.57亿参数的模型
`12:39` 我们使用了一个极小的数据池
`12:41` 记100个工作单元
`12:43` 这仅占common crow的极小部分
`12:46` 接着我们观察训练过程
`12:49` 接下来我们观察训练损失
`12:51` 蓝色曲线代表DC o m损失值
`12:54` 从这一位置开始
`12:55` 随后呈现下降趋势
`12:57` 图中的每一条线代表模型完成了一个数据轮次
`13:00` e park的训练
`13:01` 这是第一轮
`13:02` 随后的蓝色线对应第二轮
`13:04` 以此类推
`13:05` 由于数据总量有限
`13:06` 最终只能对数据进行重复使用
`13:08` 损失值仍在下降
`13:10` 是因为模型在重复学习过程中继续获取信息
`13:13` 达到某个临界点后
`13:14` 模型开始出现过拟合
`13:16` 以RESILIPARTS为例
`13:18` 其数据基本未经过滤
`13:20` 因此其初期表现较差
`13:23` 但随着训练持续再次遍历数据时
`13:26` 损失值下降的速度会放缓
`13:29` 由此可见
`13:30` 这一趋势若不重复使用数据
`13:32` 高质量数据的效果更为显著
`13:35` 然而当token数量达到海量级别时
`13:38` 高质量数据的优势便不再绝对
`13:40` 当然弱数据质量极高
`13:42` 我们应避免进入此阶段
`13:43` 因为极易导致过拟合
`13:45` 通常在此处停止训练即可
`13:47` 但即便处于这一阶段
`13:48` 其效果仍不如使用低质量数据
`13:51` 进行更长时间训练的结果
`13:52` 没错
`13:54` 我们来计算一下指标
`13:55` 这不仅仅是看图的问题
`13:57` 图中的每一个点都代表一次独立的训练运行
`13:59` 在做这些预训练实验时
`14:01` 有必要计算执行区间吗
`14:03` 目前我们只看单次结果
`14:05` 也就是说图中的每个点仅代表一次训练运行
`14:09` 是否应该多次重复实验并计算执行区间
`14:13` 理想情况下
`14:14` 这确实是更好的做法
`14:16` 但在论文中
`14:17` 这类数据往往较少
`14:18` 因为每次训练运行的成本都很高
`14:21` 不过在实际操作中
`14:23` 实验完成后
`14:24` 结果通常比较稳定
`14:25` 我是这么认为的
`14:26` 至少在预训练阶段是这样好
`14:28` 对你刚才提到了我们的延长训练时间
`14:32` 并结合高质量数据
`14:33` 这并不是我们考虑的方案
`14:35` 例如如果我们使用这种高质量数据
`14:38` 延长训练时间
`14:39` 效果会出现收益递减吗
`14:41` 延长训练时间就能获得高质量数据对
`14:43` 所以你的问题是
`14:44` 如果获得更高质量的数据并延长训练时间
`14:47` 效果还会出现收益递减吗
`14:48` 任何数据集最终都会出现收益递减
`14:50` 毕竟数据量是有限的
`14:52` 不过曲线可能会在这里趋于平缓
`14:55` 然后继续缓慢下降
`14:56` 好不错
`14:57` 我们继续数据过滤
`14:59` 对于构建优质模型至关重要
`15:01` 不过曲线可能会在这里转折
`15:03` 随后继续缓慢下降
`15:05` 好我们继续数据过滤
`15:07` 对于构建高质量模型至关重要
`15:09` 从理论上讲
`15:10` 如果你拥有无限的算力
`15:11` 就不需要过滤
`15:12` 可以直接使用全部数据进行训练
`15:15` 从而训练出一个庞大的模型
`15:17` 但在现实中我们必须进行过滤
`15:20` 因此我们需要先明确
`15:22` 什么样的数据才算优质数据
`15:23` 接着你可以训练一个分类器
`15:25` 将其应用于模型的其他部分
`15:28` 如何确定什么是好数据呢
`15:29` 一种方法是
`15:31` 在现有数据中发现你特别喜欢的部分
`15:33` 然后尽可能多的获取同类数据
`15:36` 或者你可以向语言模型提供提示
`15:40` 让他先对大量数据进行初步筛选
`15:42` 再用这些优质数据训练一个小规模的分类器
`15:46` 最后将其推广到剩余数据上
`15:51` 至于其他数据
`15:52` 接下来我们讨论去重
`15:54` 然而数据中往往仍会存在重复内容
`15:58` 重复项主要分为两类
`16:00` 完全重复和近似重复
`16:02` 例如查看镜像站点时
`16:05` 由于镜像站点本身就是为了复制内容而存在的
`16:08` 这就会导致重复
`16:09` 有时爬虫不够智能
`16:11` 无法区分镜像站点与原站内容相同
`16:14` 于是他爬取了多个站点
`16:16` 最终你获得的内容完全一致
`16:18` 此外当你fork一个仓库时
`16:21` 这也构成了重复
`16:22` 即便你进行了修改
`16:23` 通常也只涉及少数文件
`16:25` 因此该仓库中
`16:27` 99%的内容可能并未改变
`16:29` 由此可见
`16:30` 重复数据无处不在
`16:32` 是的有时还会出现近似重复
`16:35` 它们既非镜像
`16:36` 也非衍生内容
`16:37` 但内容恰好相似
`16:38` 文本相同
`16:39` 仅相差几个token
`16:41` 这通常源于复制行为或来自共同的来源
`16:45` 接下来我们来看几个近似重复数据的例子
`16:48` 例如服务条款和许可证
`16:50` 我想MIT许可证应该会频繁出现
`16:53` 他在很多地方都能见到
`16:56` 因此很多时候它其实是完全相同的副本
`17:00` 但仅限于许可证本身
`17:02` 除非有人在复制时打错了字
`17:04` 但页面的其余部分及许可证所在的部分
`17:08` 可能各不相同
`17:09` 我们之前讨论过
`17:10` 许多网站的页眉和页脚都是相同的
`17:12` 这些也算重复数据
`17:14` 另外还有一种情况
`17:15` 出于各种原因
`17:16` 这是来自LM1B及那个10亿规模的基准测试
`17:20` 有些文章之间仅存在排版上的细微差别
`17:24` 其中一个版本带了逗号
`17:25` 另一个则没有
`17:26` 我不太清楚具体原因
`17:27` 但这种情况确实存在
`17:29` 有时你会看到这类模板里面充斥着低质量内容
`17:32` 看起来就像某种广告
`17:34` 有人只是简单套用模板
`17:36` 把加拿大替换成了美国
`17:38` 所以如果你用这类数据来训练模型
`17:41` 而且只是用包含不同实体的不同变体进行训练
`17:44` 那其实是在浪费GPU算力
`17:46` 还有更极端的案例
`17:48` 所以仔细检查你的数据是非常有必要的
`17:50` 在对C4数据集进行的这项审计中
`17:53` 我们发现某款产品的描述
`17:55` 在数据集中出现了6万1000次
`17:58` 追溯起来
`17:59` 这非常奇怪
`18:00` 我也不知道为什么
`18:01` 这是一段关于某种防毒面具的描述
`18:03` 它就这样出现在了数据集中
`18:05` 追溯起来
`18:06` 这真的很奇怪啊
`18:08` 我不知道为什么
`18:09` 这看起来像是对某种防毒面具的描述
`18:12` 它就这样出现在了数据集中
`18:14` 首先动机很明确
`18:16` 为了更高效地进行训练
`18:18` 去重能在不损失信息的前提下缩减数据及规模
`18:21` 因为你移除的只是重复内容
`18:23` 此外它还能防止模型记忆训练数据
`18:26` 正如这篇论文所指出的
`18:27` 举个例子
`18:28` 如果大量受版权保护的内容被复制
`18:31` 模型在训练时可能会直接记住这些数据
`18:34` 从而引发隐私问题
`18:35` 不过我觉得最主要的原因是为了避免浪费
`18:39` 计算资源
`18:40` 与去重相关的是去污染
`18:42` 这甚至可能更重要
`18:43` 原理类似
`18:45` 你要确保测试集的数据没有出现在训练集中
`18:49` 那么具体该怎么进行去重呢
`18:52` 所以咱们得先理清设计空间
`18:55` 首先你打算对哪些内容进行去重
`18:58` 是看句子段落还是整篇文档
`19:01` 那你怎么判断两条内容算匹配呢
`19:04` 是要求完全一致
`19:05` 还是只要包含相同的子项就行
`19:07` 如果是做近重复去重
`19:10` 是不是看共同子项占多大比例
`19:13` 那找到重复内容后
`19:15` 你打算怎么处理
`19:16` 是把所有重复的都删掉
`19:18` 还是只保留一份
`19:19` 所以这里有个核心的算法难点去重
`19:23` 本质上就是在做项目间的两两比对
`19:26` 你看通常做过滤的时候
`19:28` 咱们是针对单个项目处理的
`19:30` 大家看通常做过滤时
`19:32` 我们是针对单个项目处理的
`19:34` 即便在线性时间复杂度下
`19:35` 我们也试图利用基于规则的方法
`19:37` 或极小的模型来加速处理
`19:39` 但在驱动任务中显然不能采用o on的复杂度
`19:42` 也就是避免将所有数据两两进行比较
`19:45` 为了适应这种网页规模
`19:46` 你需要线性时间复杂度的算法来实现扩展
`19:49` 谢谢
`19:50` 通常去重领域的文献
`19:52` 会使用哈希函数来解决这个问题
`19:54` 因此我们将探讨其中一些思路
`19:57` 这些是非常出色的思路
`19:58` 这要归功于算法社区
`20:00` 我想大家都了解哈希函数是什么
`20:02` 它将某种输入值
`20:04` 如字符串映射为类似字符串或整数形式的输出
`20:08` 哈希值远小于原始数据项
`20:10` 哈希冲突是指两个不同的数据项
`20:13` 映射到了同一个值
`20:14` 好的大家看哈希函数里确实有一些比较花哨的
`20:19` 本质上适用于密码学的这类函数
`20:21` 利用密码学来防止碰撞
`20:23` 比如比特币里就用到了
`20:24` 而另一类是速度更快的哈希函数
`20:27` 用于哈希表
`20:28` 在这些场景中
`20:29` 哈希冲突并非不可接受
`20:31` 所以我们用这类函数打个比方
`20:33` 把字符串映射成一个值
`20:35` 这就是哈希函数的作用精确
`20:37` 却重在概念上特别简单
`20:39` 你拿一个字符串看看有没有完全一样的
`20:43` 如果有
`20:43` 就只留一个
`20:44` 把其他的都删掉
`20:46` 所以这里有一堆元素
`20:48` 你对它们做哈希处理
`20:49` 然后去重复
`20:51` 所以这是一种精确重复
`20:53` 处理起来非常理想
`20:55` 这里的操作逻辑很清晰
`20:56` 但这对于混乱的真实世界数据来说还不够好
`20:59` 因为通常存在大量近似重复项
`21:02` 需要注意的是
`21:03` 实现这一目标有多种写法
`21:05` 这段代码采用了一种类似my produce的风格
`21:09` 这使得它更易于并行化和扩展
`21:12` 接着看C4
`21:13` 这是来自T5论文的那篇文献
`21:15` 该过程对common call执行了精确去重
`21:18` 其操作对象为三个句子的跨度
`21:20` 你们进行精确匹配
`21:22` 并保留其中一项
`21:24` 移除其余所有项
`21:26` 所以如果你稍微留意一下
`21:28` 就会意识到这里有点奇怪
`21:29` 你们查看的是三个句子的跨度
`21:31` 如果找到两个包含三个句子跨度的文档
`21:33` 你们会移除所有
`21:35` 但有一种情况比较特殊
`21:37` 如果你只是从文档里删掉三句话
`21:40` 这其实挺奇怪的
`21:41` 因为会破坏文本的连贯性
`21:43` 他们当时就是这么做的
`21:44` 你看所以这叫精确去种
`21:48` 那我们要怎么做近似曲中呢
`21:51` 首先得明确一下这里的近似匹配具体指什么
`21:55` 为此我们引入一个概念
`21:56` 叫做JACCARD的相似度
`21:58` 这是一个很标准的定义
`21:59` 两个集合的JCR相似度
`22:01` 基本上就是交集的大小除以并集的大小
`22:04` 假设给定这两个集合
`22:06` 十1234十和十1235十
`22:11` 计算j car相似度时
`22:13` 先取交集
`22:14` 也就是123
`22:15` 再取并集
`22:16` 即12345
`22:17` 然后相除得到的这块尔相似度是0.6
`22:20` 这car系数的取值范围在0~1之间
`22:23` 零代表没有交集
`22:24` 一代表完全一致
`22:25` 这个概念很直观
`22:27` 如果两个文档的j car相似度超过某个阈值
`22:30` 比如0.99
`22:31` 我们就认为它们是近似重复的
`22:33` 接下来我们要解决的是
`22:35` 如何在线性时间内把它们找出来
`22:37` 好在这是个已经解决的算法问题
`22:39` 解决办法是用最小哈希mean hash
`22:42` 因此第一步是应用最小哈希
`22:45` 但这还没完
`22:46` 最小哈希本质上是一种随机哈希函数
`22:50` 它具有一个特性
`22:51` 发生哈希碰撞的概率
`22:53` 恰好等于集合A和B的解卡的相似度
`22:55` 这是一个非常有用的性质
`22:57` 因为哈希函数能将处理复杂度降低至线性时间
`23:01` 然CORE是我们使用的指标
`23:03` 我们现在正将两者结合起来
`23:05` 或者更准确地说是期望值
`23:07` 有趣的是
`23:08` 通常情况下
`23:09` 我们希望哈希函数
`23:10` 让每个元素映射到不同且独立的值上
`23:14` 你通常不希望出现碰撞
`23:16` 但在这里你实际上需要碰撞
`23:18` 这种碰撞不是随机的
`23:20` 而是需要以特定方式控制
`23:22` 使其与相似度保持一致
`23:23` 也就是说
`23:24` 相似的元素比差异大的元素更容易产生碰撞
`23:28` 那么最小哈希本质上是什么
`23:30` 我们来梳理一下
`23:31` 这就是最小哈希它很简单
`23:34` 你取出这个集合
`23:35` 对其中每个元素进行哈希计算
`23:38` 然后取最小值
`23:39` 如果你是第一次接触
`23:40` 可能会觉得有些奇怪
`23:42` 原理很简单
`23:43` 取出集合中的每个元素进行哈希计算
`23:45` 然后取最小值好
`23:47` 那么呃如果是第一次接触
`23:49` 可能会觉得有点奇怪嗯
`23:51` A集合记得包含1234
`23:54` 而B集合包含123
`23:57` 所以这就是这两个集合的特征矩阵表示对吧
`24:01` 那么随机哈希函数究竟在做什么
`24:03` 它在元素上诱导出一个排列
`24:05` 比如可能是43152或者其他顺序
`24:10` 然后你看在这个排列下
`24:11` R中哪个元素排第一
`24:13` 以及B中哪个元素排第一
`24:15` 因此每个元素成为首位的概率是相同的
`24:18` 好假设随机哈希函数将某个元素排在第一位呃
`24:24` 那么A中的第一个元素
`24:26` 就等于B中的第一个元素
`24:28` 还有如果是二排在第一位
`24:31` 结果也一样
`24:32` 三排在第一位
`24:33` 结果也一样
`24:34` 但如果随机排列
`24:35` 让四排在第一位
`24:39` 情况五同理
`24:40` 接下来我们看这种表示法
`24:42` 简而言之
`24:43` 随机哈希函数的作用就是从这些行中
`24:46` 随机选取一行作为首行
`24:47` 接着最小值的作用仅仅是A的最小值
`24:52` 等于B的最小值
`24:54` 这基本上意味着这两行是相同的
`24:56` 这就是最小哈希MHASH的证明
`24:59` 解释了该性质为何成立
`25:02` 即当你拥有一个哈希函数时
`25:04` 你看无论怎样随机选择哈希函数
`25:07` 其碰撞概率的期望值正是我们所需的相似度
`25:11` 度量好
`25:11` 我们在代码中验证一下
`25:13` 我会生成100个不同的哈希函数
`25:16` 我只检查最小哈希值是否相等
`25:20` 以及估计的解卡的指数
`25:22` 所以我主要关注看这个比例
`25:24` 结果是0.6
`25:26` 最小哈希的关键在于
`25:28` 我不不需要进行N平方级别的运算
`25:31` 我可以直接计算的最小哈希
`25:33` 以及集合B的最小哈希值
`25:36` 还有另一个集合的最小哈希值
`25:37` 我只是在寻找碰撞
`25:39` 谢谢好的
`25:42` 关于MAHASH还有什么问题吗
`25:52` 现在我们可以对物品进行哈希处理
`25:55` 但还没完
`25:56` 因为出现碰撞
`25:58` 并不意味着JCR的指数超过了某个阈值
`26:01` 这才是我们真正想要的
`26:02` 我们要找出A和B
`26:03` 使他们的JCR的指数大于0.99
`26:06` 我们刚才做的只是说明一点
`26:07` 如果发生了碰撞
`26:09` 这两个集合发生碰撞的概率
`26:11` 就等于他们的j car的指数
`26:13` 但光靠这个其实没啥用
`26:14` 因为这是随机的
`26:15` 我没法从中得到任何可靠的结果
`26:18` 对那咱们接着看下一个
`26:20` 这里有一种叫局部敏感哈希的方法
`26:23` 它基本上能解决这个问题
`26:25` 这是理论计算机里很经典的一个思路
`26:27` 挺巧妙的
`26:28` 咱们这么想
`26:30` A和B发生碰撞的概率
`26:32` 就等于他们的j car的相似系数
`26:35` 你看越相似的数据确实越容易撞在一起
`26:38` 不过这种方法随机性很强
`26:40` 方差很大
`26:41` 结果不太稳定
`26:42` 所以我们的目标是
`26:44` 只要j ca相似系数超过某个阈值
`26:47` 就确保A和B1定能发生碰撞
`26:50` 因此我们需要想办法把这些概率分布锐化一下
`26:53` 让分布更集中
`26:55` 概率值无法直接等同于JK2系数
`26:58` 所以解决办法是使用更多的哈希函数
`27:01` 这些哈希函数之间相互独立
`27:04` 这部分稍微有点技术性
`27:05` 咱们一步步来拆解
`27:07` 也就是说你把N个哈希函数分成B个代
`27:10` 每个代里包含二个哈希函数
`27:12` 比如如果你有12个哈希函数
`27:14` 可以分成三个代
`27:16` 每个带包含四个哈希函数
`27:18` 哈希函数好
`27:19` 这里有一个带
`27:20` 从H1到H4
`27:21` 这是第二个带
`27:22` 从H5到H8
`27:24` 第三个代从H9到H12好
`27:26` 接下来我们要尝试做的是好
`27:29` 接下来我们要尝试的是好的
`27:32` 也就是说每个哈希函数都对应一个特定的概率
`27:36` 记一下
`27:36` 重点在于我们需要计算A和B发生碰撞的概率
`27:40` 如果一个band里的所有哈希函数
`27:43` 都返回了相同的值
`27:44` 我们就说A和B发生了碰撞
`27:47` 比如如果H5H6
`27:49` H7和H8都返回了相同的值
`27:52` 抱歉我是说如果A的H5等于B的H5
`27:56` A的H6等于B的H6
`27:58` 以此类推
`27:59` 那就说明这个band被触发了
`28:02` 然后我会说他们发生了碰撞
`28:04` 也就是说
`28:05` 如果这个band里的所有哈希函数结果一致
`28:08` 我就认为发生了碰撞
`28:10` 但你不需要检查所有的哈希函数
`28:12` 然后我会说他们发生了碰撞
`28:14` 也就是说
`28:15` 如果这个BANDA里的所有哈希函数结果一致
`28:19` 我就认为发生了碰撞
`28:21` 但你不需要检查所有的哈希函数
`28:25` 好假设你有一个特定的j car相似系数A那B呢
`28:29` 按这个定义
`28:30` A和B发生碰撞的概率是多少
`28:33` 好我们来算一下
`28:34` 假设是这种情况
`28:35` JCR相似系数是0.8
`28:38` 我们有五个袋
`28:39` 每个袋里有十个哈希函数
`28:42` 那么我们可以算一下
`28:43` 某个固定带匹配的概率是多少
`28:45` 因为每个带包含R个哈希函数
`28:53` 好的
`28:53` 所以某个固定带匹配的概率通常非常低
`28:55` 这个概率随R呈指数级变化
`28:58` 现在发生碰撞的概率即为
`29:01` 至少有一个带匹配的概率
`29:03` 这等于一减去的概率
`29:05` 也就是说这是代不匹配的概率
`29:07` 然后如果你将其自称必次
`29:09` 因为有big个代
`29:10` 这就是的概率
`29:12` 然后如果你将其自成B次
`29:14` 因为有big个波段
`29:15` 这就是的概率
`29:17` 他们都不匹配
`29:18` 因此用一减去这个数
`29:20` 得到的就是发生匹配的概率
`29:22` 好的
`29:23` 所以碰撞概率是0.4
`29:25` 你的预期值应该更高
`29:28` 好的
`29:29` 谢谢咱们把它画出来
`29:32` 大概长这样
`29:33` 好的X轴代表这里的相似度
`29:36` 我们看的是0.8
`29:38` 然后你看这儿
`29:39` 这是碰撞概率
`29:40` 所以如果相似度是零
`29:42` 我们预期结果也是零
`29:43` 如果是一结果就是一
`29:44` 不过大家注意这个曲线呈现出一种有趣的S型
`29:47` 这很好
`29:48` 因为这正是我们想要的效果
`29:50` 简单来说
`29:51` 我们的想法是
`29:52` 一旦相似度低于某个阈值
`29:54` 匹配概率就要尽可能低
`29:56` 反之如果相似度高于某个阈值
`29:58` 匹配概率就要尽可能高
`30:00` 所以我们试图让这种变化
`30:02` 呈现出类似相变的效果好
`30:05` 如果你把相似度作为横轴来绘制这个函数
`30:09` 就会得到这样的结果
`30:11` 好抱歉
`30:13` 我们继续好
`30:16` 那我们直接看几个例子
`30:18` 我们具体一点
`30:20` 把相似度范围设定在0.7到0.98之间
`30:23` 好
`30:24` 我们来看看
`30:25` 当B等于十
`30:26` R等于十时会发生什么
`30:28` 因此我们根据定义来考察碰撞概率
`30:31` 我们发现这给出了一个范围范围
`30:34` 从0.25到一好的
`30:36` 如果你要设定一个阈值
`30:38` 那么其实这样也行
`30:39` 我们可以把阈值设在这儿
`30:41` 然后那些有一定概率的仍存在假阳性
`30:45` 因为即使这些值低于阈值
`30:47` 例如0.9
`30:48` 它们仍可能发生碰撞
`30:50` 但如果我们愿意
`30:51` 可以将它们过滤掉
`30:52` 而高于阈值的主要会被保留
`30:55` 那么当你增加R时会发生什么
`30:58` 所以R代表每个band的中哈希函数的数量
`31:01` 因此当你增加R时
`31:03` 阈值变得更加尖锐
`31:05` 导致曲线整体向右移动
`31:09` 匹配变得更难了对吧
`31:10` 因为每个band里有更多的哈希函数
`31:12` 这使得指数项变得更陡峭
`31:14` 因此概率分布曲线在这里变得更陡峭
`31:17` 整体向右偏移
`31:19` 也就是说
`31:20` 如果某对数据的展开相似度很低
`31:23` 它们被匹配到的概率就微乎其微了
`31:25` 比如概率从之前的0.25
`31:27` 降到了现在的0.008
`31:29` 我们是在让概率分布变得更陡峭好
`31:33` 接下来看增加B的情况
`31:34` 这也会使曲线向左移动
`31:36` 从而降低匹配门槛
`31:38` 让匹配更容易
`31:39` 我们是在让概率分布变得更陡峭好
`31:42` 接下来看增加B的情况
`31:44` 这会使曲线向左移动
`31:46` 从而降低匹配门槛
`31:48` 让匹配更容易
`31:50` 因此观察这一指标在实施去重之前
`31:53` 数值仅为0.72
`31:55` 而目前它已提升至0.92
`31:57` 当然这也带来了一定的增长
`32:00` 但幅度并不显著
`32:02` 我们可以通过增大B和R的值
`32:04` 让下面过程尽可能陡峭
`32:06` 但要注意
`32:07` 如果B和R取值过大
`32:09` 可能会导致成本上升
`32:11` 谢谢
`32:11` 接下来我们来看一个更贴近实际应用的场景
`32:14` 在这篇关于去重的论文中
`32:17` 作者提出了
`32:19` 在这篇关于去重的论文中
`32:21` 作者提出了通常相变发生在临界点
`32:25` 该临界值是B的1R次方的倒数
`32:29` 也就是B的e sin2次方的倒数
`32:31` 所以如果你想根据大于0.9的JCR的相似度
`32:35` 来过滤数据
`32:36` 就需要调整B和R
`32:38` 使阈值恰好为0.9
`32:40` 接着你可以通过调整B和R
`32:43` 让这种相面现象更加陡峭
`32:45` 大家记住
`32:46` 如果固定带匹配的概率是一败比
`32:48` 且你正好处于这个临界点
`32:50` 那么A和B发生碰撞的概率是大概是这个数值
`32:56` 这是一个常数
`32:57` 所以碰撞概率为0.64
`32:59` 这很合理
`32:59` 基本上相变的中心点就在0.64左右
`33:02` 随着BNR增大
`33:03` 下方的值会趋近于零
`33:05` 上方的值会趋近于一
`33:06` 关于LSH
`33:07` 大家有什么问题吗
`33:09` 谢谢
`33:10` 因此这种方法被称为最小哈希LSH
`33:13` 因为LSH实际上适用于任何哈希函数
`33:16` 在处理原模型的驱动时
`33:18` 我们使用的是最小哈希
`33:20` 它可以近似计算你的JCR相似度
`33:23` 所以嗯基本上这就是你应该在这里使用的方法
`33:26` 关于LSH
`33:30` 还有问题吗
`33:31` 关于器重
`33:35` 有一点需要注意
`33:37` 关于LH还有问题吗
`33:40` 关于驱虫
`33:41` 有个点得注意一下
`33:42` 有时候大家会跳过这一步
`33:44` 但其实应该做好
`33:46` 咱们接着看下一个话题
`33:48` 数据混合
`33:49` 到目前为止
`33:50` 我们已经完成了从原始HTML或PDF到文本的转换
`33:56` 并筛选出了高质量的内容
`33:58` 经过取证处理
`34:00` 我们得到了一组规模更小的高质量文档
`34:03` 谢谢这种情况很常见
`34:06` 经过去种处理
`34:07` 我们得到了一组规模更小的高质量文档
`34:10` 谢谢这种情况很常见
`34:11` 以MARY为例
`34:13` 该网站目前会追踪下一个模型
`34:16` 可能使用的各类数据源
`34:19` 大家可以看到这里涵盖了多种类型
`34:22` 比如nebula
`34:23` 还有我们之前提到的find Pdf以及机构出版的书籍
`34:29` 部分代码等等
`34:30` 面对这些不同的数据源
`34:33` 关键问题在于该如何将它们整合
`34:36` 如果我们回顾一篇较早的论文
`34:39` The pile
`34:40` 就能看到当时他们使用的数据源集合呃
`34:44` 他们基本上是为每个部分分配特定的权重
`34:49` 施加到各个组件上
`34:51` 换句话说
`34:52` 这本质上是一种资源分配嗯
`34:54` 他们基本上是为每个部分分配特定的权重
`34:59` 谢谢咱们简化一下
`35:01` 假设你有三个数据源
`35:03` 你需要找到一个数据混合方案
`35:05` 说白了就是为你那些数据源确定一个分布好
`35:08` 要是你试着从第一性原理来思考这个问题
`35:11` 嗯好吧
`35:12` 不算严格的
`35:13` 第一性原理就是你平时可能会怎么做
`35:15` 靠感觉wives
`35:16` 我想这跟第一性原理正好相反
`35:19` 就是你凭某些手动调整一下直觉嘛
`35:22` 这其实挺常见的
`35:24` 大家平时大多就是这么干的
`35:26` 所以我觉得这确实挺靠感觉的
`35:29` 甚至更近期的论文你直接看就行对
`35:32` 也许你会用某种方法
`35:33` 然后就像这样微调一下嗯
`35:35` 你也可以做均匀采样
`35:37` 也就是在你的数据源上应用均匀分布
`35:39` 在你的样本中
`35:40` 每获取一个chunk
`35:42` 你只是采样一个数据源
`35:43` 你也可以做按比例混合
`35:45` 也就是根据原数据中的token数量来采样
`35:48` 所以如果某个数据集包含更多token
`35:51` 你就给他更高的权重
`35:52` 这通常是个合理的做法
`35:54` 但你可能会担心
`35:55` 如果有一个体量巨大但质量低下的数据集
`35:57` 它会消耗掉大量的token
`35:59` 所以这看起来也不太理想
`36:01` 直觉上你应该给更高质量的原数据更高的权重
`36:06` 所以这看起来也不太理想
`36:09` 第一你得保证数据具有一定的多样性
`36:12` 你看很多数据源之间其实没法直接比较
`36:15` 拿文献代码和论文来说
`36:17` 你很难说某篇论文比某段代码质量更高
`36:20` 因为它们根本不是同类的东西
`36:22` 没法直接比
`36:23` 如果你想让语言模型效果好
`36:25` 就不能把所有的质量权重都只压在论文上
`36:28` 谢谢第二点
`36:29` 咱们后面会细说
`36:31` 就是每个数据源的数据量其实都是有限的
`36:34` 所以要是你在一个数据量小的圆上
`36:37` 分配太多权重
`36:38` 那你很快就会把数据用光
`36:40` 不得不进行多轮训练
`36:42` 也就是在完全相同的token上反复训练
`36:44` 这会有负面影响
`36:46` 原因咱们待会看好
`36:47` 那咱们具体拆解一下
`36:49` 最后这一点
`36:49` 打个比方
`36:50` 假设你手头只有两个圆
`36:52` 一个是低质量员有10万亿个token
`36:54` 另一个是高质量元
`36:56` 只有100亿个token好
`36:58` 那咱们具体拆解一下
`37:01` 最后这一点
`37:02` 打个比方
`37:03` 假设你手头只有两个来源
`37:05` 一个是低质量来源
`37:06` 有10万亿个token
`37:08` 另一个是高质量来源
`37:09` 只有100亿个token
`37:10` 通常高质量的数据源规模较小好
`37:14` 那如果我们做个最简单的数据混合
`37:16` 比如均匀采样
`37:17` 也就是各占一半哦
`37:19` 会训练1万亿个token嗯
`37:21` 所以当我提到训练2000000000000个token时
`37:25` 指的并不是唯一token的数量
`37:27` 那只是指训练那么多步呃
`37:29` 这本质上等于总步数除以批次大小
`37:32` 或者说乘以批次大小
`37:34` 嗯好那我们来看这个指标
`37:37` 它表示低质量数据集中的某个特定样本
`37:41` 被训练的次数
`37:42` 也就是说这是某个数据点经历的epoch数量
`37:45` 简单来说
`37:46` 这意味着在这个低质量数据集中
`37:48` 只有5%的token会被我接触并用于训练
`37:52` 谢谢接着看高质量数据部分
`37:56` 你会发现epoch数是50
`37:58` 没错
`37:59` 因为我只有100亿一个token
`38:02` 如果数据量减半
`38:04` 那就是5000亿个token
`38:05` 我需要训练5000亿个token的高质量数据
`38:08` 但我没有那么多高质量数据
`38:10` 所以每个数据点我得重复使用50次
`38:13` 这其实非常关键
`38:13` 你知道有些大模型训练项目在这方面搞砸了
`38:17` 也就是说你不能简单地查看数据集
`38:20` 定义一个分布
`38:21` 然后进行采样
`38:22` 因为如果你只看质量
`38:24` 只看数据
`38:25` 你实际上忽略了数据点的数量
`38:27` 好这样讲清楚了吗
`38:29` 大家要是有问题
`38:29` 可以暂停一下
`38:30` 这点挺重要的
`38:39` 好对嗯
`38:41` 我想是的
`38:42` 谢谢
`38:46` 所以问题是为什么要跑50个E炮
`38:49` 其实重点是你根本不需要50个E跑
`38:50` 运气好的话只是浪费算力呃抱歉
`38:54` 运气好是浪费算力
`38:56` 运气不好就是过拟合了
`38:57` 设为50的原因是如果直接设定数据混合比例
`39:01` 除非全程监控
`39:03` 否则很容易在不知不觉中跑满50个epoch
`39:06` 因此关键在于确认数据实际运行的E炮数量
`39:10` 没错
`39:10` 那么在训练过程中
`39:12` 这些混合比例具体是如何体现的呢
`39:14` 比如如果每步都切换
`39:15` 是像训练师部那样
`39:17` 既需要十个Python代码样本和十个C语言样本吗
`39:21` 我有些疑惑
`39:22` 在做决策时是否有直观的逻辑可循
`39:26` 对好问题
`39:27` 你的意思是
`39:28` 训练时这些混合比例具体是如何落实的
`39:31` 嗯通常情况是这样的
`39:33` 我们采用的是采样方式
`39:35` 也就是要填满一个batch
`39:37` 因此你可以对每个元素进行采样
`39:40` 确定其具体来自哪个混合分量
`39:43` 接着用来自该混合分量的序列将整个批次填满
`39:47` 一般来说每个序列都源自同一个混合分量
`39:50` 我们不是按token采样的
`39:51` 所以我认为混合策略是在批次层面设立的
`39:55` 并为此进行权衡
`39:56` 对通常为了降低方差
`39:58` 需要从多个来源进行训练
`39:59` 你看一个批次里最好还是包含一些混合成分
`40:03` 对
`40:04` 谢谢好
`40:05` 这个思路
`40:06` 或者说这个问题其实早就有人注意到了
`40:09` 所以在那篇论文里
`40:11` 他们提出了unit max这个方法
`40:12` 当时他们正在训练多语言模型
`40:15` 很明显某些语言的数据资源非常稀缺
`40:19` 因此必须想办法解决这个问题
`40:22` 这篇论文指出
`40:23` 以往的一些做法其实是按比例混合数据
`40:26` 然后进行幂运算
`40:28` 这样可以让数据分布更加平滑
`40:29` 但这篇论文的思路是把这件事做得更明确一些
`40:33` 我们均匀地采样各个数据源
`40:35` 但为每个数据源的epoch数设定上限
`40:38` 所以核心思路就是针对某个数据源
`40:41` 只训练20个EPO
`40:43` 我们对各数据源进行均匀采样
`40:45` 但为每个数据源的epoch数设定上限
`40:48` 因此核心思路是针对特定数据源
`40:51` 仅训练20个EPOK
`40:52` 从某种意义上说
`40:53` 这涉及网络层面的分配
`40:55` 具体而言
`40:56` 若观察分配给某数据源的概率
`40:59` 乘以训练token数量
`41:00` 这一指标其值应小于或等于设定上限
`41:03` 此外存在一种简单流程
`41:05` 可在满足该约束的前提下实际确定混合比例
`41:09` 好的我们稍微换个话题
`41:11` 我们最初是如何定义混合比例的
`41:14` 假设有50个数据源
`41:16` 就需要确定50个数值
`41:18` 我们发现简单的按比例混合并不足够
`41:20` 或许可以尝试估算某种质量指标
`41:23` 并据此进行采样
`41:25` 但这依然属于启发式方法
`41:27` 因此最具理论依据且易于理解的方法
`41:31` 既能清晰阐明原理的方法
`41:34` 是基于回归的混合方法
`41:36` 例如remix omix及其他相关论文
`41:39` 就像这样
`41:40` 其核心思路相对简单
`41:42` 首先假设你正在尝试训练大规模模型
`41:46` 我们先缩小规模
`41:48` 假设我们将参数量设定为3亿
`41:50` 然后你尝试不同的数据混合比例
`41:55` 假设你有三个组成部分
`41:56` 你可以尝试不同的混合方案
`41:58` 接着你训练这些小型模型
`42:01` 也就是训练一组小型模型
`42:03` 每个模型会在某个目标指标上给出一个损失值
`42:07` 这可能是某些下游评估指标
`42:09` 也可能是困惑度
`42:10` 由你决定
`42:11` 谢谢好的
`42:12` 简单来说
`42:13` 你是用这些数据点来训练一个回归模型
`42:16` 以数据混合权重作为输入预测对应的损失值
`42:20` 好的
`42:21` 这样你就有了一个低成本模型
`42:23` 能帮你预估
`42:24` 如果用这种数据混合来训练
`42:26` 损失会是多少
`42:28` 接着你就可以对这个数据混合方案进行优化
`42:32` 也就是通过优化这个函数
`42:34` 找到最佳的数据混合比例
`42:36` 最后你就用这个数据混合
`42:38` 去训练那个大规模模型
`42:39` 谢谢好的
`42:41` 这就是那个简单的流程
`42:42` 你会发现它和缩放定律有点像
`42:45` 都是先做点低成本计算来探路
`42:47` 然后再大规模扩展好的
`42:49` 这里涉及几个设计上的选择
`42:52` 比如你打算尝试哪些数据混合方案
`42:56` 此时你需要对分布本身进行建模
`42:58` 因此许多人会选择迪利克雷分布
`43:00` 此外还需确定回归方法
`43:02` 实践中大家既尝试过线性模型
`43:04` 也尝试过提升决策树
`43:06` 首先要明确目标
`43:07` 通常参考下游评估指标
`43:09` 但务必警惕过拟合
`43:11` 毕竟我们进行的是预训练
`43:13` 理论上预训练旨在训练通用模型
`43:16` 而非迎合特定的下游任务
`43:18` 评估数据方面需格外谨慎
`43:20` 例如若混入大量代码类评估数据
`43:22` 结果往往不尽如人意
`43:24` 模型会将更高的权重分配给所有代码数据
`43:27` 这一逻辑不难理解
`43:29` 最终当你希望模型创作诗歌时
`43:31` 却发现其已经过拟合
`43:33` 因此这一点需格外注意
`43:35` 按比例混合或均匀混合
`43:36` 可避免此问题
`43:37` 因为这些方法不依赖下游评估
`43:39` 最后我们探讨一下
`43:40` 小规模模型与大模型之间的差异究竟有多大
`43:44` 这本质上是成本与精度的权衡
`43:46` 训练极小规模模型时
`43:48` 结果可能缺乏代表性
`43:49` 若训练大模型
`43:50` 则无需采用此类策略
`43:52` 直接进行训练即可
`43:54` 这实质上是在最大规模上进行超参数调整
`43:56` 成本过高
`43:57` 谢谢这篇组学论文做得不错
`43:59` 里面有个表格
`44:00` 展示了针对多种不同那些属于这类框架的方法
`44:04` 代理模型的大小通常都在数千万或呃
`44:07` 数千万个参数
`44:08` 你采样多少个点
`44:10` 采用多少个混合比例
`44:12` 代理模型的大小通常为数千万或数E
`44:16` 你可以使用迪利克雷分布
`44:17` 也可以使用指数分布
`44:19` 谢谢接下来是回归模型
`44:21` 你可以拟合一个对数线性模型
`44:24` 这通常很有效啊
`44:26` 效果相当不错
`44:28` 然后你可以用不同的方法来解决这个优化问题
`44:31` 好的
`44:31` 所以这里有两个你需要特别小心的假设前提
`44:34` 也就是说
`44:35` 这个回归模型是在一堆这样的小规模
`44:38` 代理实验上训练出来的
`44:40` 而你正在对它进行优化
`44:42` 因此我们希望该回归模型找到的最优
`44:46` 数据混合方案依然是可靠的
`44:48` 并且该回归模型本身也是准确的
`44:51` 这有点
`44:52` 而这个回归模型依然有效
`44:54` 这有点通常问题不大
`44:55` 毕竟有经典的泛化能力作为保障
`44:57` 当你采样混合数据并拟合函数时
`45:00` 模型理应能在数据分布范围内做出准确预测
`45:03` 但在优化过程中
`45:04` 你本质上是在逼近某些极端情况
`45:07` 而那里的数据覆盖可能不足
`45:09` 这是一个需要注意的点
`45:11` 另外一点是关于最优数据配比
`45:13` 你主要希望这种关系能从小规模迁移到大规模
`45:17` 一般来说至少在开源社区常用的规模下
`45:21` 这种假设通常是成立的
`45:23` 或者说并不明显错误
`45:24` 但显然存在规模依赖效应
`45:27` 回想一下刚才讲过滤时的那张幻灯片
`45:29` 如果你要训练更多的token
`45:31` 那那么低质量数据或许是可以接受的
`45:33` 显然最优解并不一样
`45:34` 但在这种情况下
`45:36` 咱们只能尽量争取最好的结果
`45:37` 谢谢好
`45:38` 稍等有个之前聊过的跟规模有关的效应
`45:41` 咱们得在这儿处理一下
`45:42` 咱们得在这解决这个问题
`45:44` 谢谢打个比方
`45:46` 假设我们有海量的低质量数据
`45:48` 10000000000000token
`45:49` 但高质量数据很少只有10000000000token
`45:52` 如果咱们用较少的token数量去训练一个小模型
`45:58` 大家应该还记得
`45:59` 此时你可能会将大部分注意力
`46:01` 集中在高质量数据上
`46:03` 这是一个假设性的例子
`46:04` 但谢谢
`46:05` 因为如果token数量较少
`46:07` 模型无法进行多次EPOKE训练
`46:09` 这时模型就会产生一种倾向哇
`46:11` 维基百科真好
`46:12` 于是我们决定就在维基百科上进行训练
`46:15` 但是如果你继续使用这种混合数据来训练
`46:19` 大型混合模型
`46:20` large模型
`46:21` 你就会对高质量数据进行反复训练
`46:24` 结果就是过拟合法嗯
`46:26` 因此一种解决方案
`46:29` 也就是组学研究中采用的那种嗯
`46:32` 你可以限制epoch的数量
`46:35` 好
`46:35` 这是一种解法
`46:36` 这里还有另一种方法
`46:38` 它可能有多个名称
`46:39` 其中一种叫做模拟epoch
`46:41` 其原理是让你的小规模实验
`46:43` 看起来像大规模实验
`46:44` 这算是这门课的一个通用主题
`46:46` 我们之前台词讲解UP时也见过这个思路啊
`46:49` 你对模型进行参数化
`46:50` 这样你的超参数就能迁移过去
`46:52` 所以同样的原理在这里也适用
`46:55` 而这种简单粗暴的缩放方法的弊端在于嗯
`46:58` 你不能在小规模上跑完epoch
`47:00` 然后在大尺度上又跑一遍
`47:02` 同样的原理在这里同样适用
`47:04` 但这种简单粗暴的缩放方式存在弊端
`47:07` 你不能先在小规模数据上跑完一个epoch
`47:10` 再在大尺度数据上重新跑一遍
`47:12` 比如小模型训练使用10000000000token
`47:16` 大模型训练使用1000000000000token
`47:18` 你就需要进行一1%的下采样
`47:21` 接着直接使用经过下采样的混合数据
`47:26` 这样做的逻辑是
`47:27` 既然使用了下采样后的数据
`47:30` 那么这种做法不可行
`47:31` 因为你无法使用全量数据进行训练
`47:34` 只能使用维基百科的数据
`47:36` 你只能利用维基百科中极小的一部分数据
`47:39` 届时你会发现必须反复训练很多个epoch
`47:41` 这会导致损失值老死
`47:43` 表现很差
`47:44` 因此在优化过程中
`47:46` 你需要寻找更平衡的方案
`47:48` 因此你实际上是在小规模下模拟
`47:50` 大规模高容量阶段会遇到的数据稀缺情况
`47:53` 好
`47:54` 我们来总结一下这部分核心问题是
`47:57` 如何为不同的数据源分配权重
`48:00` 例如你拥有维基百科
`48:02` come on row代码数据
`48:04` 以及从其他来源爬取的数学数据
`48:06` 那么如何平衡这些来源呢
`48:08` 我认为采用基于回归的混合方法
`48:11` 是一个不错的思路
`48:12` 先在小规模数据上拟合一个函数
`48:15` 将混合权重映射到损失值
`48:17` 优化完成后再推广到大规模场景
`48:20` 这一点必须格外谨慎
`48:22` 因为涉及轮次控制和过拟合风险
`48:24` 建议采用截断轮次或模拟轮次
`48:26` 所以这里有一个教训
`48:28` 如果要优化某个目标
`48:29` 务必小心
`48:30` 否则很容易优化到错误的方向
`48:32` 好在继续之前
`48:35` 关于数据混合
`48:36` 大家有什么问题吗
`48:38` 谢谢没错
`48:39` 确实存在这个风险
`48:41` 所以我觉得对万一token数量极少呢
`48:45` 我觉得到时候最优解
`48:47` 基本就不会太关注那些数据了
`48:49` 等你扩大规模后
`48:50` 也许就能凑齐正确的token集合了
`48:54` 原则上这事儿能成
`48:56` 但可能会有舍入误差
`48:58` 导致最终结果出现偏差
`49:01` 本来打算在这个数据集上训练一次
`49:04` 结果搞不好一次都没训练成
`49:06` 不过你可以稍微规避这些问题
`49:08` 不过我觉得你反正还是得在这个数据上
`49:11` 训练一次对行
`49:12` 那就这样
`49:13` 不过你可以稍微绕开这些问题
`49:15` 我觉得你反正还是得在这个数据上训练一次
`49:19` 对啧行
`49:23` 那就这样
`49:24` 不过你可以稍微规避这些问题
`49:26` 我想你反正还是得用这份数据训练一次对行
`49:30` 那就这样是指不同领域
`49:32` 还是说就在一个数据集内部
`49:33` 这其实是我之前漏讲的
`49:35` 很高兴提出来了
`49:36` 比如在NETN论文里或者欧盟的一些工作中
`49:40` 你可以这么想
`49:41` 假设我直接给你一堆common crowd的数据
`49:44` 对你可以把它拆开
`49:45` 比如按领域分组
`49:46` 像I团队搞的那个web organizer
`49:48` 就能让你按主题来归类
`49:50` 你也可以做质量过滤
`49:51` 这样一来你就构建了一个二维网格
`49:53` 一边是领域
`49:54` 另一边是质量
`49:55` 而每一个格子里的内容
`49:57` 都是你可以用来做数据混合的素材
`50:00` 所以这就是确定领域的一种自动化方式
`50:03` 除此之外
`50:04` 你还要加上别人提供的那些数据源
`50:06` 谢谢好
`50:08` 咱们快速过一下post training数据
`50:11` 到目前为止
`50:12` 这些基本上算是pre training或者METRAINING数据
`50:15` 这些数据通常跟具体任务关系不大
`50:18` remix除外
`50:19` 虽然remix是为了最小化损失
`50:21` 但数据本身还是相对任务无关的
`50:23` 主要是为了培养基础技能
`50:25` 所以你看到了post training阶段
`50:27` 很多数据就变得非常依赖具体任务了
`50:30` 我就不做全面回顾了
`50:32` 只想提几个最近发布的比较有趣的post training
`50:35` 数据集
`50:36` 特别是编程领域的
`50:37` 我觉得现在大家对这个都很感兴趣
`50:39` 通用的做法是这样的
`50:41` 即定义一组环境
`50:42` 以代码为例
`50:43` 可能涉及GITHUB仓库
`50:45` 你定义一组任务或提示
`50:47` 接下来你会从一个强大的模型或教师
`50:50` 那里收集回复
`50:51` 所以至少在开源社区中
`50:54` 影视的几乎所有后训练数据
`50:57` 也就是大部分数据都是合成生成的
`50:59` 此外你也可以用人类来替代强大的模型
`51:03` 但那样做既缓慢又昂贵
`51:05` 不过如果你处于技术前沿
`51:07` 我想几年前你确实不得不花大价钱
`51:10` 雇佣很多人来提供回复
`51:12` 而且如今即便在技术前沿
`51:14` 你也可以采用人机混合的方式
`51:16` 总之关键在于那里有一个教师在为你提供回复
`51:20` 好的
`51:20` 那么我只简要介绍几项相关工作
`51:23` 其中一项是名为open thoughts的工作
`51:26` 接下来简要介绍几项相关工作
`51:29` 其中一项是open thoughts项目
`51:31` 我们如何为这一目标
`51:32` 获取高质量的后训练数据集
`51:34` 它们最终利用该教师模型
`51:37` 生成了120万个实例
`51:39` 具体步骤如下
`51:40` 首先我们需要明确环境任务以及提出的问题
`51:44` 他们从多种不同的来源中提取了数据
`51:47` 数据来源包括sec exchange或new man mass等人工来源
`51:51` 同时也包含部分合成数据
`51:53` 这里仅展示了编程类数据的一个子集
`51:55` 此外还有数学和化学领域的相关数据
`51:58` 这是一个大型项目
`51:59` 汇聚了来自多方的大量贡献
`52:02` 大家可以看到其中一些是编程练习
`52:05` 另一些则是更贴近现实的应用场景
`52:07` 当然像code golf这类任务
`52:09` 可能并不具备现实代表性
`52:11` 这是一个涉及多方贡献的大型项目
`52:14` 他们发现针对这个数据集到底该怎么生成
`52:17` 回复
`52:17` 他们做了一套挺全面的分析
`52:19` 结果显示只用几个来源
`52:21` 其实比把所有来源都拿来用效果更好
`52:23` 多采样几组生成结果挺有用
`52:26` 比如采16个
`52:27` 一个有趣的发现是模型的表现更好
`52:30` 这并不一定意味着他是更好的老师
`52:32` 打个比方
`52:33` Q w q32 b
`52:35` 虽然现在算是一个较老且较小的模型
`52:37` 但它的教学效果比deep sk r1更好
`52:40` 当时他大概是当时最强的开源模型之一
`52:43` 此外他们还发现简单的答案过滤效果有限
`52:46` 因此整个流程大致如下
`52:48` 先将这些来源的数据全部输入
`52:50` 然后进行去重和随机采样
`52:53` 大致就是这样
`52:54` 我们对问题进行了下采样
`52:56` 生成多个答案
`52:57` 最后将这些内容整合到最终的数据集中
`53:00` 所以120万指的是样本总数
`53:03` 但除以16之后才是实际问题的数量
`53:07` 谢谢
`53:08` 接下来还有另一个
`53:09` 刚才讲的是数学科学以及一些代码相关的内容
`53:14` 我觉得最近大家特别感兴趣的是
`53:17` 开发一种基因编码模型
`53:19` 也就是说不只是能生成代码的模型
`53:22` 而是真正能进行软件开发
`53:25` 所以sweet smith这篇论文他提出了一个想法
`53:29` 你拿到一个代码仓库
`53:31` 然后用语言模型自动生成任务
`53:33` 他们设计了一个智能体
`53:35` 专门处理代码仓库
`53:37` 使其具备可运行状态
`53:39` 例如安装各类依赖项
`53:41` 随后生成任务
`53:42` 通常通过修改代码实现
`53:44` 比如故意引入bug
`53:46` 这些修改经过验证后便成为具体的任务实例
`53:49` 这些属于合成任务
`53:51` 但你们能获得5万个样本
`53:52` 要知道去年这个数量已经相当可观
`53:55` 此后又涌现出一系列其他研究
`53:58` 我接着谈谈刚才提到的
`54:01` 英伟达有一篇关于sweet zero的论文
`54:05` 其中提到了这种
`54:07` 实际上好的
`54:08` 我也会提及那个这个想法很有意思
`54:11` 大家发现与数学题不同
`54:13` 3B任务通常包含大量重型依赖项
`54:16` 大多数GITHUB仓库甚至无法直接运行
`54:19` 你还得安装一堆过时的依赖项
`54:21` 尤其是当你回退到某个P2提交时的状态时
`54:24` 这简直是一团糟啊
`54:26` 这简直是噩梦
`54:28` 所以他们当时就在想
`54:29` 究竟该如何获取一个数据集
`54:31` 就是那种针对所有代码仓库
`54:34` 他们发现与其为每个仓库单独配置docker镜像
`54:37` 不如直接使用模型
`54:39` 因为现在的模型实力强劲
`54:41` 能处理不少这类任务不需要代码执行反馈
`54:44` 你看如果允许模型执行代码得分能有80左右
`54:47` 要是禁止执行呢
`54:49` 得分也能接近70
`54:50` 代码都跑不通
`54:52` 能拿到这个成绩已经很不错了
`54:54` 这说明这些模型对代码的内部语义
`54:56` 确实有一定的理解
`54:57` 因此他们成功生成了30万个智能体轨迹
`55:00` 这些都是真实的
`55:02` Githupr
`55:03` 非常贴近实战
`55:04` 不像swiss smith那个例子
`55:05` 因此他们成功生成了30万个智能体轨迹
`55:08` 这些都是真实的
`55:10` GITHUBPR非常贴近实战
`55:12` 不像sweet smith那个例子
`55:15` 比如set和grab这类基础操作
`55:18` 谢谢好
`55:19` 接下来
`55:20` 他们从一个大编码模型中蒸馏并过滤数据
`55:23` 因为有时编码模型会无视指令强行尝试执行
`55:26` 然后他们展示了这一点
`55:27` 对了
`55:28` 他们还有1.3万条需要执行反馈的代理轨迹
`55:31` 所以他们在训练模型时
`55:33` 先在这些SEMBLY减bench样本上做微调示例
`55:37` 然后再在这些sweet bench样本上微调
`55:40` 他们取得了一些进展
`55:41` 不过我想前沿水平依然很高
`55:43` 我快速过一下sub1减REBENCH
`55:46` 这本质上又是另一个试图抓取大量pr的尝试
`55:50` 这类方法的核心思路是获取一批GITHUB仓库
`55:55` 然后尝试安装
`55:56` 比如大多数尝试可能会失败
`55:58` 这时你会加大尝试力度
`56:00` 或者使用一个用语言模型来生成回复
`56:04` 这项工作
`56:05` 实际上今天刚发布
`56:06` 你可以沿用死无1zero的思路
`56:10` 将规模扩展至1200万个智能体轨迹
`56:14` three zero的一个显著优势
`56:16` 使它非常清亮
`56:17` 这里他们使用的是CBUE减bench中的REBENCH
`56:21` 任务
`56:21` 回顾一下他们之前尝试让代码执行
`56:23` 最终只有3万2000个成功执行
`56:26` 另有120个未能执行
`56:28` 这里他们使用的是SWFBV简bench
`56:31` 中的REBENCH任务
`56:32` 回顾一下他们之前尝试让代码执行
`56:35` 最终只有3万2000个成功执行
`56:38` 另有120个未能执行
`56:40` 你看我们从数学这类无需环境的数据集
`56:43` 过渡到了现在的编程领域
`56:45` 而且编程数据集的增长幅度相当大
`56:47` 但总体思路是你拥有这些提示词
`56:51` 这其中存在某种权衡
`56:53` 你可以使用完全合成的数据
`56:55` 也可以使用半合成数据
`56:57` 即环境是真实的
`56:58` 但任务为合成或者任务是真实的
`57:00` 但环境为合成
`57:01` 这些回复通常来自能力较强的模型
`57:05` 但他们同时也需要具备良好的教学能力
`57:09` 代码环境处理起来比较麻烦
`57:11` 还涉及很多过滤和其他细节问题
`57:13` 我们没时间一一展开
`57:14` 好的总结一下本次讲座我们讨论了过滤
`57:17` 首先定义什么是好的标准
`57:19` 然后训练一个轻量级分类器
`57:21` 代码环境处理起来比较麻烦
`57:23` 还涉及很多过滤和其他细节
`57:25` 我们没时间处理
`57:27` 好的总结一下
`57:28` 这次讲座我们讨论了过滤
`57:30` 首先定义什么是好的数据
`57:32` 然后训练一个轻量级分类器
`57:34` 接着我们看了一些后训练数据
`57:36` 说实话很多数据工作挺琐碎的
`57:39` 而且特别依赖具体领域
`57:41` 得盯着具体例子
`57:42` 才能做出高质量的数据集
`57:44` 所以这堂课其实没法完全
`57:46` 代表真实世界里的数据工作
`57:48` 但希望能帮大家理清一下上面的数据全貌好
`57:51` 今天就讲这些
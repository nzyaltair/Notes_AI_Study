---
title: "【极致中配】2026年最新版 Stanford CS336: 从头构建大语言模型"
url: "https://www.bilibili.com/video/BV11LEA6eEuj/?p=7"
bvid: "BV11LEA6eEuj"
cid: "39162939037"
author: "常青藤名校课"
upload_date: "2026-06-05"
created: 2026-08-17
tags: ["bilibili", "clippings"]
---

## 简介

如果这个中文配音版对您理解 CS336 有帮助，欢迎点赞、投币、收藏支持一下，也可以点个关注。
我会持续更新更多 Stanford / AI / 大模型相关课程的中文配音课程，尽量帮大家用更低的理解成本看懂这些高质量课程。你的支持会让我更有动力把后续内容继续做完、做好。

## 字幕

`00:00:00` 好我们开始吧
`00:00:01` 欢迎大家回来
`00:00:02` 今天我们要讨论PARALYSM
`00:00:05` 还记得吗
`00:00:06` 上周我们介绍了如何通过编写kernels
`00:00:09` 让单个GPU跑得更快
`00:00:11` 并且深入看了GPU的内部结构
`00:00:14` 而这周我们要讨论如何利用多个GPU
`00:00:17` 让你的代码跑得更快
`00:00:19` 你脑海中应该有这样一幅画面
`00:00:21` 上周我们聚焦于这些方框中的一个
`00:00:26` 也就是你的GPU
`00:00:28` 还记得吧
`00:00:29` 里面有high bandwidth
`00:00:30` Memory h b m l tar cash l in cash registers
`00:00:34` 以及一大堆streaming multiprocessors
`00:00:37` As far after streaming multi processors
`00:00:40` 现在这幅画面要扩展了
`00:00:42` 因为你不再只有一个GPU
`00:00:44` 而是可能有四个甚至1000个GPU
`00:00:47` 而且它们是相互连接的啊
`00:00:50` 稍后我会讲这些GPU是如何连接起来的
`00:00:53` 然后你就得想办法利用所有这些computer
`00:00:58` 来训练模型啊
`00:01:00` 所以无论是单GPU还是多GPU
`00:01:03` 这也是我们今天要讨论的内容
`00:01:05` 这两种情况下
`00:01:07` 如果把视角拉远看情况大致相似
`00:01:10` 那就是compute
`00:01:12` 当arithmetic logic units
`00:01:14` Tensor
`00:01:15` cores等等
`00:01:16` 离你的数据很远
`00:01:17` 在单GPU场景下
`00:01:19` 这意味着数据远在HBM里
`00:01:21` 而在多GPU场景下
`00:01:23` 你需要的数据可能在另一块GPU上的problems
`00:01:27` 你不得不想办法把它搬过来
`00:01:29` 但核心原则不变
`00:01:30` 因为关键在于编排计算
`00:01:32` 尽量避免数据转移带来的瓶颈问题
`00:01:35` 瓶颈用上一大堆GPU很容易
`00:01:39` 但要高效的用好它们却很难啊
`00:01:42` 啊所以我们可以稍微灵活的看
`00:01:45` 考虑一个广义的层次结构
`00:01:47` 在最局部的层面紧邻的是单个节点单GPU
`00:01:51` 其中你and new l one cash shared memory
`00:01:55` 这是最快的
`00:01:56` 然后是HBM
`00:01:57` 我们之前曾抱怨他太慢
`00:02:00` 但在这节课里
`00:02:01` HBM会被当作高速存储来看待啊
`00:02:05` 现在啊我们将考虑单节点多GPU的场景
`00:02:09` 其中GPU通过m v link和m v switch连接
`00:02:13` 最后是多节点多GPU的场景
`00:02:16` 这是我们必须根据你所拥有的网络选择
`00:02:20` 使用infinite band或ETHERNET好
`00:02:22` 那么啊
`00:02:23` 上周啊我们讨论了减少内存访问的各种技巧
`00:02:28` 也就是fashion和THAILIN读入short memory
`00:02:31` 尽可能在其中完成所有操作
`00:02:34` 然后再写回
`00:02:35` 而本周我们将讨论如何减少通信量
`00:02:37` 也就是通过在GPU之间进行适当的复制和分片
`00:02:41` 来实现
`00:02:42` 那么为什么要使用多GPU
`00:02:44` 显而易见的答案是你想要扩展规模
`00:02:47` 不过说的更具体一点
`00:02:49` 其实有两个原因
`00:02:51` 首先你的参数ACTIVATIONS
`00:02:54` gradients和optimize the state
`00:02:57` 装不进单张GPU的HBM内存
`00:03:00` 比如啊b two hundred有one
`00:03:02` i need to give goby显存
`00:03:04` 如果你训练一个one train参数的模型
`00:03:07` 单张GPU根本装不下
`00:03:09` 其次即便模型能装进单张GPU
`00:03:12` 你可能也想通过拆分任务来利用更多的GPU
`00:03:16` 为了加快训练速度
`00:03:18` 所以有时候你得做个权衡
`00:03:19` 因为虽然能把所有东西放进GPU
`00:03:21` 但核心数不够多
`00:03:23` 但如果你把任务分散开
`00:03:25` 就得承担通信带宽的代价
`00:03:27` 所以你得算一笔账
`00:03:29` 看看该怎么做并行化好
`00:03:31` 这里提一点
`00:03:32` 到目前为止
`00:03:33` 这门课用的是Python
`00:03:35` 你执行代码就能把所有结果展示出来哦
`00:03:38` 这一讲里如果你直接运行这段代码
`00:03:40` 会用到motor processing啊
`00:03:43` 不过我一步步演示的时候用的是某种特殊模式
`00:03:46` 也就是single process mode啊
`00:03:49` 如果你想看这一讲的代码
`00:03:51` 在multiprocessing模式下的standard out
`00:03:53` 点这里就行
`00:03:54` 我会边讲边演示啊
`00:03:56` 不过大家记住我们一步步讲解的时候
`00:03:59` 其实并没有真的在跑moto processing
`00:04:01` 因为只是在逐行执行代码好
`00:04:04` 这一讲分为两个部分
`00:04:06` 第一部分
`00:04:07` 我们来学习
`00:04:08` 分布式通信与计算的基本构建模块嗯
`00:04:12` 先从编程模型讲起
`00:04:13` 简单说说硬件
`00:04:14` 然后开始在touch里实现一些东西
`00:04:16` 这也是你在作业里要做的
`00:04:18` 第二部分
`00:04:19` 我们来看看实际的训练过程
`00:04:21` 我们会看三种PARALYSM
`00:04:23` Data
`00:04:23` Paralysm
`00:04:24` Tensor
`00:04:24` PARALYSM和pipeline paralysm
`00:04:26` 每一种都会以不同的方式切分我们的模型
`00:04:30` 我们拿MLP而不是完整的transformer来举例
`00:04:33` 但这里展示的其实就是核心的计算过程
`00:04:38` 好咱们深入看看
`00:04:39` 首先得聊聊这些叫集合操作
`00:04:42` collective operations的东西啊
`00:04:44` 集合操作是分布式编程里的一些基础言语
`00:04:48` 这得追溯到A级年代了
`00:04:50` 所以并行编程的想法其实非常古老
`00:04:53` 他们当初并不是为了训练大语言模型才发明的
`00:04:57` 而且直到今天我们用的还是这些言语
`00:05:00` 这里的集合意思是
`00:05:02` 你指定的是跨多个设备的通用通信模式或模板
`00:05:06` 而不是去管理点对点通信
`00:05:08` 也就是不用管这块GPU具体怎么跟另一块GPU通信
`00:05:12` 首页会简单的多系统也能帮你做更多事情
`00:05:16` 所以啊
`00:05:17` 这是一种非常成熟可靠的并行编程接口啊
`00:05:22` 好的总体设置如下
`00:05:23` 这里的术语我觉得有点奇怪
`00:05:25` 但这是并行编程里的标准说法
`00:05:28` 核心思想是你有一组ranks
`00:05:31` 每个rank对应一个特定的设备
`00:05:33` 在我们的例子中可以是GPU
`00:05:35` 也可以是TPU
`00:05:37` 但重点是你这里有四个ranks
`00:05:39` the world size对应于设备的数量
`00:05:41` 所以这里的world size是四好的
`00:05:44` 我们将要介绍几种操作
`00:05:46` Broadcast scatter
`00:05:47` Gather reduce
`00:05:48` All gather reduce
`00:05:49` scatter or reduce和all to all
`00:05:52` 这些操作中的每一个都将指定
`00:05:55` 这一组ranks或设备
`00:05:56` 如何与另一组设备传输一定量的数据
`00:06:00` Slash computer
`00:06:01` 那么前三个操作
`00:06:02` Broadcast gather reduce
`00:06:04` 我认为他们其实只是热身
`00:06:06` 他们让你能够大致理解
`00:06:08` 这些集合通信是如何工作的
`00:06:11` 集合操作确实有效
`00:06:18` 在语言模型的分布式训练中
`00:06:20` All gather reduce
`00:06:21` gather和all reduce
`00:06:22` 这些操作会反复出现嗯
`00:06:25` 最后是auto
`00:06:26` 我简单提一下
`00:06:27` 它对某一很重要
`00:06:29` 但这节课我们不会花太多时间讲它好
`00:06:33` 那我们就从最简单的操作开始啊
`00:06:35` 深入看看哦
`00:06:36` 也就是broadcast广播
`00:06:39` 在广播操作中
`00:06:40` 你有一个rank zero
`00:06:42` 它可以是任何rank
`00:06:44` 但为了举例
`00:06:45` 我们假设rank zero有一个tensor0123
`00:06:50` 然后把它广播给所有的ranks
`00:06:52` 所以在这个操作结束后
`00:06:55` 每个rank都拥有相同的数据
`00:06:58` 它上面的张量好
`00:06:59` 这应该相当直接明了
`00:07:01` 而且同样的
`00:07:02` 这其实并不会出现在训练的核心路径中
`00:07:05` 一般来说广播用于初始化阶段
`00:07:08` 比如你加载一个初始检查点
`00:07:09` 然后把它广播给所有的ranks
`00:07:12` 所以这类操作大概只做一次好
`00:07:14` 第二个操作是散射scatter
`00:07:17` 散射的基本意思是我有一个张量在rank zero上
`00:07:22` 这个张量被切分成versize face
`00:07:25` 然后我会把我的张量散射到其他各个ranks上
`00:07:29` 所以rank zero得到第零个分量
`00:07:31` rank one得到这个rank two
`00:07:33` 得到这个rank3
`00:07:35` 得到那个好的
`00:07:37` 再说一遍
`00:07:38` scatter本身并不直接使用
`00:07:40` 但它是理解reduce scatter的重要基石
`00:07:42` 看顾名思义
`00:07:44` scatter就是把一个大张量从一处分散到多处
`00:07:47` 你可以看出这为什么有用
`00:07:49` 因为你希望所有参与scatter的GPU
`00:07:52` 对各自的部分做局部计算
`00:07:55` 谢谢谢谢
`00:07:56` 好scattering逆操作是gather
`00:07:59` 这应该不难猜到
`00:08:01` 输入是一堆数据片段
`00:08:03` 每个片段都存放在特定的rank上
`00:08:06` 然后当你执行gather时
`00:08:08` 这是针对特定的rank
`00:08:10` 也就是rank zero而言的
`00:08:12` 他只是把所有片段拼接在一起
`00:08:15` 好再说一遍
`00:08:16` gather本身并不直接使用
`00:08:18` 但它是理解all gather的基石
`00:08:20` 接下来是reduce
`00:08:22` 做过函数式编程的人
`00:08:23` 可能对reduce很熟悉
`00:08:25` 但完全一样
`00:08:26` 思路是从和gather一样的起点开始啊
`00:08:29` 当数据分散到多个设备上时
`00:08:32` 你在每个不同的rank上都有部分数据
`00:08:34` 接着你对这些数据执行规约操作
`00:08:37` 并把结果放到rank zero上
`00:08:41` 所以如果你执行某种规约
`00:08:44` 把所有数加起来
`00:08:45` 结果就是六呃
`00:08:47` 所以你可以把gather看作是一种规约
`00:08:49` 他的操作是也就是所谓的拼接好的
`00:08:53` 当然reduce对于理解or reduce很重要
`00:08:55` 好先停一下嗯
`00:08:57` 这些只是热身
`00:08:59` 以防大家对什么是集体操作还有疑问嗯
`00:09:02` 广播散射收集规约
`00:09:05` castle对撑爆谢天谢地
`00:09:10` 但正是这样吗
`00:09:11` 那么问题是
`00:09:13` 这与广播broadcasting和N画有关吗
`00:09:17` 我是说我认为他们在概念上是一样的
`00:09:20` 思路也相同
`00:09:21` 就是一个东西分发给多个东西
`00:09:23` 比如在number派里
`00:09:24` 如果你有一个标量
`00:09:25` 它会被广播成张量
`00:09:27` 在实例化这方面
`00:09:28` 因为这是用于集体通信
`00:09:30` 所以略有不同
`00:09:31` 嗯好那我们继续聊点更有趣的
`00:09:34` 那么我来做收集gather
`00:09:36` 你所做的基本上就是对所有rank执行收集操作
`00:09:41` 不只是rank zero
`00:09:42` 还记得gather是做什么的吗
`00:09:44` 它基本上就是把所有不同的部分都收集起来
`00:09:48` 他把数据放到一个rank上
`00:09:50` 也就是rank zero
`00:09:52` 接下来就是all gather
`00:09:54` 他对每个rank都这么做
`00:09:56` 这就是all gather的意义所在
`00:09:59` all的意思是把所有输出发到所有rank上
`00:10:03` 而gather就是你对所有rank所做的操作
`00:10:07` 好的这个概念后面会频繁出现
`00:10:09` 你不需要完全搞懂这句话
`00:10:11` 但之后我们会看到每个rank只持有部分参数
`00:10:16` 接下来你需要做的是用all gather把参数收集起来
`00:10:21` 从而得到完整前向传播所需的完整参数
`00:10:25` 所以在训练过程中
`00:10:27` 我们通常会看到这样的模式
`00:10:29` 先gather来做点事
`00:10:30` 然后scatter再gather
`00:10:32` 再scatter
`00:10:33` 好的reduce
`00:10:34` scatter就是在每个维度上执行reduce操作
`00:10:39` 然后把结果scatter出去
`00:10:40` 好的假设你有啊
`00:10:43` 比如四个设备
`00:10:44` 每个设备里都有一些向量
`00:10:48` 所以当我们这么做的时候
`00:10:51` 之前做reduce的时候
`00:10:53` 我们只有ZZ123
`00:10:55` 结果被reduce成了六
`00:10:57` 但现在reduce scatter的意思是
`00:11:00` 对于这个张量的每个分量我都会做一次reduce
`00:11:04` 然后啊我会把它们放到不同的
`00:11:08` 你知道
`00:11:08` 然后ins对于第一个维度
`00:11:12` 我把这些加起来得到六
`00:11:14` is现在对于第二个维度
`00:11:17` 我把这些加起来得到十
`00:11:18` 对于第三个维度我来处理这些诶
`00:11:22` 对于第四个维度
`00:11:24` 我处理内线好的好
`00:11:27` 先做个铺垫
`00:11:27` 说一下这个会出现在什么地方
`00:11:30` 反向传播之后
`00:11:31` 当你做求和时
`00:11:32` 你要做的是这样的
`00:11:33` 每个大GPU会处理不同的数据
`00:11:37` 对你要做的是把所有的梯度加起来
`00:11:40` 来自不同分片的梯度
`00:11:42` 然后你将重新分配这些存储好的
`00:11:45` 最后是all reduce
`00:11:47` 如果你理解orreduce gather和ALGATHER
`00:11:50` 本质上就是先做一个再做另一个好的
`00:11:54` 所以这个操作执行的是reduce scatter输入
`00:11:57` 和我们之前的一样
`00:11:59` 还有啊回想一下
`00:12:07` 这些元素分散在不同的rank上
`00:12:11` 接着all reduce it的all gather步骤
`00:12:14` 会把它们全都收集到同一个节点上
`00:12:18` 所有元素都在同一个节点上
`00:12:20` 所以从这个角度看
`00:12:22` ORREDUCE其实是最容易理解的
`00:12:24` 你有一堆张量对它们做规约
`00:12:27` 比如这里是求和
`00:12:29` 然后把结果复制到所有节点上
`00:12:32` 好的我们在做数据并行时
`00:12:34` 会先看到这个操作对梯度求和
`00:12:36` 然后复制完整的参数啊
`00:12:38` 谢谢
`00:12:38` 所以啊我们实际上要从这里开始讲
`00:12:42` 大家不妨先把注意力集中在reduce上
`00:12:45` 之后我们会看看如何进阶到更复杂的技术
`00:12:48` 比如zero或FSDP
`00:12:50` 我们需要把all reduce拆解成reduce
`00:12:54` scatter和all gather这几个步骤
`00:12:57` 因为这样你就能介入并更好地掌控局面
`00:13:00` 不过对于基础版来说
`00:13:01` 用all reduces就够了哦
`00:13:04` 谢谢好
`00:13:05` 最后讲一下auto all
`00:13:07` 这在某种程度上是最通用的
`00:13:09` 你基本上要指定每个rank
`00:13:11` 如何向另一个rank发送特定的消息啊
`00:13:14` 所以这里有个简单的例子
`00:13:15` 输入和之前一样
`00:13:17` 他的意思是我想把位置零的元素发给rank0
`00:13:22` 意思是这个元素我自己留着
`00:13:24` 然后把位置一的元素发给rank yon
`00:13:27` 把位置二的元素发给rank2
`00:13:29` 位置三的发给rank3
`00:13:31` 然后假如我是rank1
`00:13:33` 我想把位置四的元素发给rank0
`00:13:36` 位置五的发给rank1
`00:13:38` 把位置six的元素发给rank two
`00:13:41` 位置七的发给rank three
`00:13:43` 所以基本上这里的位置表示
`00:13:46` 哪个rank是最终接收方啊
`00:13:48` all these啊
`00:13:49` 所以看看输出结果你就明白了
`00:13:52` 第一个rank会收到所有在列零
`00:13:55` 发送了数据的rank
`00:13:56` 因为所有这些rank都在向rank0发送数据
`00:13:59` 同理对rank one
`00:14:01` 所有rank都在向他发送这一列
`00:14:04` 以此类推
`00:14:04` 对好的
`00:14:05` 这在训练中会很有用呃直观的理解是
`00:14:10` 每个rank既持有数据的一个切片
`00:14:13` 也持有一组专家子集
`00:14:15` 那一
`00:14:16` 基本上MOE的核心思想是某种动态路由嗯
`00:14:20` 你需要查看自己的数据
`00:14:21` 才能确定要把哪些ACTIVATIONS录入到哪些专家
`00:14:25` 所以它最终就变成了一种auto通信pose
`00:14:29` 假设一切都是平衡的
`00:14:31` 也就是每个rank
`00:14:32` 向其他每个rank发送相同数量的字节
`00:14:35` 那么自始至终
`00:14:37` 你都可以把它本质上看作一次转制
`00:14:40` 如果你把它看作一个矩阵
`00:14:42` 你所做的无非就是转制这个矩阵啊
`00:14:45` 不过一般来说auto all也能处理负载不均的情况
`00:14:48` 虽然这里没展示
`00:14:49` 但你完全可以配置它
`00:14:51` 向任意其他rank发送任意数量的字节哦
`00:14:54` 不过总的来说最好还是让划分尽可能均衡
`00:14:57` 所以回想一下touch讲MOE的那节课
`00:15:01` 里面提到了负载均衡
`00:15:02` 目的就是确保各部分尽可能均衡
`00:15:05` 所以说到底理想目标是让auto all的形态
`00:15:09` 大致像这样啊
`00:15:10` 咱们来总结一下
`00:15:11` 我刚才讲了不少不同的概念
`00:15:13` 这里有些小贴士帮你记住这些术语操作
`00:15:18` 首先是reduce
`00:15:19` 他做的就是reduce
`00:15:21` 执行某种满足结合律和交换律的运算
`00:15:24` 可以是some max
`00:15:25` 也可以是门
`00:15:26` scatter是gather的逆操作
`00:15:28` scatter负责分散数据
`00:15:31` gather负责汇聚数据
`00:15:33` 而all则表示目标是所有节点
`00:15:36` 也就是所有设备
`00:15:37` 这就解释了or reduce or gather
`00:15:40` 好的
`00:15:40` 我先停一下
`00:15:41` 大家有什么关于集合通信的问题吗
`00:15:44` 都比如在gather这类操作中
`00:15:46` 我们通常希望
`00:15:48` 我们基本上是想把所有rank的数据
`00:15:51` 汇聚到rank10
`00:15:53` 这个ranks音是固定的吗
`00:15:55` 也就是说每次都是同一个特定的GPU
`00:15:59` 还是说这个ranks音可以变动
`00:16:01` 是的
`00:16:02` 你的问题是
`00:16:03` 当你执行gather或者对目标做reduce的时候
`00:16:06` 你呃把输出写在哪里
`00:16:10` 我刚才说是rankling嗯
`00:16:14` 稍后看代码你就会明白
`00:16:16` 你只需指定GPID或rank数据
`00:16:19` 就会发往对应位置
`00:16:21` Crunch terror
`00:16:22` 因此它不必提前很久就定死
`00:16:24` 但必须确定so
`00:16:26` 基本上在执行那个调用时才确定好
`00:16:32` 还有别的吗
`00:16:33` 对这些只是概念层面的
`00:16:36` 他们并不是啊
`00:16:37` 实际上你问的是是否存在
`00:16:41` 像他才是实际的交付者
`00:16:43` 对
`00:16:44` 所以问题是这些只是概念性的构建块还是代码
`00:16:47` 所以我现在展示这些只是概念构建块
`00:16:50` 但我们很快会看到他们在代码中如何实现好的
`00:16:54` 所以在进入代码之前
`00:16:55` 我想先谈谈硬件
`00:16:57` 尤其是GPU之间是如何连接的
`00:16:59` 因为我们已经知道GPU内部的结构了
`00:17:02` 那么来我们来谈谈网络
`00:17:05` 大体上是这样
`00:17:06` 所以这算是一张非常经典的图
`00:17:09` 从这张看起来很旧的图片中啊
`00:17:11` 你可以看出计算机过去大致就是这样工作的
`00:17:14` 所以你有这个呃
`00:17:16` 你知道服务器
`00:17:17` 然后你有一堆CPU
`00:17:19` 有一个PCIE总线
`00:17:20` 你用它来连接各种设备
`00:17:22` 或者过去用来连接诸如鼠标和键盘之类的东西
`00:17:27` 嗯然后有一堆GPU挂机在上面
`00:17:30` 你还有一些ram
`00:17:31` 然后啊
`00:17:32` 这台计算机通过EASNET连接到另一台计算机
`00:17:35` 如此等等哦好的
`00:17:37` 所以这是一种特定的配置
`00:17:39` 一种特定的拓扑结构
`00:17:42` 同一节点上的GPU用PCIE通信
`00:17:45` 而不同节点上的GPU就得另走一条路了嗯
`00:17:49` 一路通过internet好
`00:17:52` 这就好比你买了游戏GPU
`00:17:54` 然后跟朋友把它连了起来
`00:17:56` 说我要训练个大模型嗯
`00:17:59` 那你就只能这么干
`00:18:00` 但如果你真的认真想做训练
`00:18:03` 那情况就更像这样了
`00:18:04` thing比如说我一开始展示的那张图上面有GPU
`00:18:08` 还有一些东西S叫做envy link
`00:18:12` envy switch和gentant band
`00:18:14` 所以典型的配置是这样的
`00:18:17` 而这些数字里八是典型值
`00:18:20` 但这个256是我随便举的例子
`00:18:22` 所以通常每个节点配备八个GPU
`00:18:26` 这些GPU通过NVIDIA的NVLINK连接到交换机
`00:18:31` 为了有个概念
`00:18:32` 如果你用envy link five
`00:18:34` 总带宽能达到每秒万兆瓦Q爆dolls fell了
`00:18:38` 别忘了b two five的HBM带宽是每秒8es as
`00:18:41` 所以啊n v link five大约是它的14
`00:18:45` 麻了大约四倍
`00:18:46` 不过我的意思是
`00:18:48` 如果考虑到这是在设备之间传输数据
`00:18:51` 这个速度其实已经相当快了
`00:18:53` 只是显然比不上HBM
`00:18:55` HBM要快得多
`00:18:57` 可比共享内存或ln cash慢的多好的
`00:19:01` 所以简单来说啊
`00:19:03` ENVLINK连接到交换机啊
`00:19:06` 这意味着从编程的角度看
`00:19:08` 你可以把任意一个GPU都当做直接连到了
`00:19:11` 其他任何GPU上
`00:19:12` 你从一个GPU发送数据到任意另一个GPU
`00:19:16` 由硬件负责把数据传送到交换机
`00:19:18` 再由交换机进行路由好的
`00:19:20` 所以通常你还会遇到这样的情况
`00:19:22` 到某个时候你没法只靠n vs witch和nv link
`00:19:26` 你必须这样做
`00:19:27` 因为随着集群里GPU数量的增长
`00:19:30` 你就不得不把这些节点放进pause里
`00:19:33` 它们之间通过infinite band连接
`00:19:35` 而infinite band的工作方式是
`00:19:38` 现在情况有点不一样啊
`00:19:40` 也就是说现在GPU不再直接连接到另一个G
`00:19:44` 而是必须经过PCIE
`00:19:46` 还有其他环节
`00:19:47` 他要通过这种专用的infinite band线缆连接
`00:19:50` 你会发现速度要慢得多
`00:19:52` twin out it要低得多
`00:19:54` 最后如果你的infinder band也用完了
`00:19:57` 而你又有这些庞大的pods
`00:19:59` 那么你就需要通过internet把它们连接起来
`00:20:02` 而在internet中数据必须经过PCIE
`00:20:06` 还要实际经过CPU
`00:20:08` 正如我们后面会看到的
`00:20:10` 这甚至更慢好的
`00:20:12` 所以这有点类似于内存的情况啊
`00:20:14` 节点越多
`00:20:15` 你没法让一个n vs witch去处理大约10万个GPU
`00:20:19` 谢谢还有一点我要说明
`00:20:21` 我之前暗示过绕过CPU这件事
`00:20:24` 从硬件角度来看
`00:20:26` 这将是一个重要的方面
`00:20:29` 所以如果你用的是传统意internet情况是
`00:20:33` GPU必须与CPU通信才能完成它的数据复制啊
`00:20:38` base所以他基本上得把数据复制到这儿
`00:20:42` CPU里有个内核套接字缓冲区
`00:20:45` 这里的kernel是指内核
`00:20:48` 不是GPU kernel
`00:20:49` 而是CPU传统概念里的内核
`00:20:51` 接着他得组装一些网络数据包
`00:20:54` 复制到网络接口再发出去free
`00:20:57` 所以这通常会带来很大的延迟
`00:21:00` 于是有了远程直接内存访问RDMA这项技术
`00:21:04` 它让GPU能直接读写
`00:21:06` 直接读写另一个GPU的内存
`00:21:09` 整个过程完全不需要CPU参与
`00:21:11` 所以显然如果你用的是NVLINK和m v switch
`00:21:16` 那就具备r d ma功能啊
`00:21:18` infinite band也支持r d ma
`00:21:20` 所以如果你通过infinite band的连接
`00:21:23` GPU就能直接互联
`00:21:25` 无需CPU介入
`00:21:26` 但标准1internet就不行了
`00:21:29` 我要提两个值得注意的进展
`00:21:31` 这就是说NVIDIA一直在大力突破
`00:21:35` 用越来越大的集群能实现什么的极限
`00:21:37` 所以针对b two盘和B3排
`00:21:39` 他们推出了一种叫NVL72的产品
`00:21:42` 也就是说他们用的是装八个GPU的托盘
`00:21:45` 但一共有九个这样的托盘
`00:21:47` 因此啊归根结底你就有了70TU个GPU
`00:21:51` 它们全部通过m v switch连接到同一个n v link域中
`00:21:56` 呃
`00:21:56` 如果你还记得mv link的速度非常快呃
`00:22:00` 所以通常来说
`00:22:02` 如果你只是个普通用户
`00:22:04` 你会想好吧
`00:22:05` 我有八个GPU
`00:22:06` 它们之间以极快的速度互联
`00:22:08` 然而在这个范围之外
`00:22:10` 速度就会大幅下降
`00:22:11` 但如果你有足够的钱
`00:22:13` 就可以购买这种非常高端的硬件
`00:22:16` 你就能在多达70
`00:22:17` 出个GPU之间实现极快的互联啊
`00:22:21` 另外我要提到的一点是
`00:22:22` 我之前说标准以太网不支持RDMA
`00:22:25` 但在以太网方面也有进展
`00:22:28` 所以有一种叫rose e的技术
`00:22:29` 也就是基于融合以太网的r d ma
`00:22:32` 它能让以太网直接绕过CPU
`00:22:35` and这算是他们针对infinite band给出的解决方案
`00:22:39` infinite b通常价格不菲
`00:22:42` NVIDIA的许多产品也是如此啊
`00:22:45` 但你可以获得相当不错的性能
`00:22:47` 用的就是基于融合以太网的rd ml bil
`00:22:51` 这个MATT发过一些论文
`00:22:53` 显示他们正在研究这个方向
`00:22:55` 拉玛的训练可能用了融合以太网
`00:22:58` 也可能没用到
`00:22:59` like好了
`00:23:00` 以上就是对硬件大致情况的简要概览
`00:23:03` 打GPU之间通过envy link连接到envv switch
`00:23:07` 这个连接范围可能跨越八个或70差个节点
`00:23:10` 随后再通过infinite band互联
`00:23:13` 谢谢接下来我们谈谈如何对它进行编程
`00:23:18` 在最底层有一个名为nvidia collective communications library
`00:23:21` and ccl读作NSL的库
`00:23:23` 他负责转换几何通信操作
`00:23:26` 把all reduce
`00:23:27` Reduce
`00:23:27` broadcast等操作
`00:23:29` 转换为在GPU之间实际传输的底层数据包
`00:23:33` 当你使用NC cl时
`00:23:35` 它本质上就像是在说我想要执行all reduce
`00:23:39` 然后NCCL会分析硬件的拓扑结构
`00:23:42` 确定不同GPU之间的通信路径
`00:23:45` 并实际启动GPU kernels来发送和接收数据
`00:23:48` 因为归根结底
`00:23:49` 请记住在GPU上运行的所有内容
`00:23:52` 本质上都是一个kernel
`00:23:54` 另外还有一些专门负责与其他GPU通信的通信
`00:23:58` Kernels
`00:23:59` 好
`00:23:59` 我们就不多讲NICO了
`00:24:01` 只要知道它存在就行
`00:24:03` 嗯接下来我们要讲PYTORCH
`00:24:05` 在这之前大家有什么问题吗
`00:24:07` 对就是关于硬件的
`00:24:09` 你能具体描述一下吗
`00:24:10` 它看起来像机架和托盘那样吗
`00:24:14` 我能从物理结构上描述一下计价和托盘吗
`00:24:17` 就是计价和托盘分别指什么啊
`00:24:20` 关于n v o seven two
`00:24:21` 我不是硬件专家啊
`00:24:23` 但机价说白了我见过数据中心
`00:24:25` 就是这么一个机架
`00:24:26` 而每个托盘大概就是这样
`00:24:28` 里面装着一些东西
`00:24:29` G代表grace
`00:24:31` 所以里面有两个
`00:24:33` 每个CPU连接四块GPU
`00:24:37` 因此每个托盘上有八块GPU
`00:24:39` 它们堆叠在一起
`00:24:41` 所有设备都连接到这个n vs witch
`00:24:44` 谢谢是的
`00:24:45` 所以问题是区别究竟在哪里
`00:24:48` 在RDA和RDM之间
`00:24:50` 是的
`00:24:50` 你可以把RDA理解为
`00:24:52` 更像是一种一种期望特性
`00:24:54` 而DA意味着一个GPU可以读取和写入
`00:24:58` 另一个GPU的内存
`00:25:00` 实现rd ma有多种方式
`00:25:02` 一种是使用envy link和MVSWITCH
`00:25:05` 另一种是使用infinite band
`00:25:07` 因此infinite band envy switch和envy link更多是指硬件
`00:25:13` 也就是有哪些组件
`00:25:14` 比如有哪些线缆和交换机
`00:25:17` 而RDMA更多是指操作层面
`00:25:20` 也就是通信时发生了什么
`00:25:23` 也就是你通信的时候
`00:25:25` 所以总希望你能在网上读到那些事件
`00:25:28` 就像一周一样诶啊所以还有其他途径
`00:25:31` 我们其实不需要深究这些细节
`00:25:33` 比如基于融合以太网的RDA
`00:25:34` 就是另一种实现r d ma的方式
`00:25:36` 用的人多吗
`00:25:38` 好的好的对
`00:25:39` 那NC cl是专门针对什么优化的
`00:25:42` 是多节点集群
`00:25:44` 虽然这听起来很直观
`00:25:46` 但它其实适用于更通用的场景
`00:25:48` 但如果是针对RDMANCCL是可选的吗
`00:25:51` Is is
`00:25:52` 所以问题是NCCL是不是针对多节点集群
`00:25:57` 谢谢啊
`00:25:59` 所以我的我不太清楚
`00:26:01` 他们具体有没有针对这一点做优化
`00:26:04` 细节我不了解啊
`00:26:06` 我只能说英伟达基本上一直在啊
`00:26:10` 优化其整个软件站
`00:26:12` 用于这些大型模型的推理和训练
`00:26:14` 因为他们的主要客户都是那些大公司
`00:26:17` 语言模型提供商
`00:26:19` 所以如果他们没想过
`00:26:20` 针对这类工作负载进行优化
`00:26:22` 我反倒会感到意外
`00:26:27` 谢谢是的
`00:26:29` 你遇到的工作负载里
`00:26:31` 最常见的
`00:26:33` 你能完成大约90%工作的是哪种情况
`00:26:37` 好玩
`00:26:37` 谢谢你正对着镜头说话
`00:26:40` 问题是如果你有九块GPU会怎样
`00:26:43` 你如何在这些GPU之间分配工作负载
`00:26:47` 我想这取决于这九块GPU
`00:26:49` 在你的配置里是如何分布的
`00:26:51` 比如很多时候你会有假设每个节点有八块GPU嗯
`00:26:57` 那第九个就会在另一个节点上
`00:26:59` 要是你没把H3R3那糟了
`00:27:01` 要是没有env link把它们连起来
`00:27:04` 那情况就会变成另一个样子
`00:27:06` 那就太糟糕了
`00:27:07` 因为那个节点提供的computer很少
`00:27:09` 而且跟他通信的成本极高
`00:27:11` 但如果你比方说所有东西都有m v switch软件
`00:27:15` 那情况就会是另一个样子了
`00:27:17` 那就合理多了
`00:27:19` 好再问一个问题
`00:27:20` 然后我接着讲
`00:27:21` 好的
`00:27:21` 那么这跟TPU有什么区别
`00:27:23` 让TTO来多讲讲TPU啊
`00:27:26` 所以嗯我是说让我们看看
`00:27:30` 我该怎么快速解释这个
`00:27:33` 所以TPU通常是结构简单的多的硬件
`00:27:38` 我对这些组件具体如何对应的细节不太熟悉
`00:27:41` 但我们可以在课后讨论
`00:27:46` 好的
`00:27:47` 让我们实际编写一些代码来利用这些硬件
`00:27:51` Pytorch
`00:27:51` 很方便的提供了一个torch distributed的库
`00:27:54` 该库为这些集合操作提供了简洁的接口
`00:27:57` 因此你无需显示的考虑NCCL
`00:28:00` 事实上该库还支持针对不同硬件的不同后端啊
`00:28:06` 因此如果你使用的是GPU
`00:28:08` 就使用NCCL后端
`00:28:10` 如果你使用的是CPU
`00:28:11` 则有一个名为glue的工具
`00:28:13` 正如我所说的
`00:28:14` 并行处理在GPU出现之前就已经存在很久了
`00:28:18` 你也可以在CPU上执行这些集合操作啊
`00:28:22` 这个库还支持更高级的模型和算法
`00:28:24` 比如不过我们这门课不用那些
`00:28:27` 因为我们要从头开始
`00:28:29` 搭建好了
`00:28:30` 我们来过几个集合操作的基本例子
`00:28:34` 这里有个叫SPOND的函数
`00:28:36` 它接收另一个函数啊
`00:28:38` 意思是把这个函数在word size为四的情况下
`00:28:42` 复制运行四次啊
`00:28:44` 我们来看看他做了什么
`00:28:46` 这其实是我写的一个封装函数
`00:28:49` 主要是为了绕过我在本次讲座中
`00:28:52` 无法进行multiprocessing的限制
`00:28:54` 正常情况下你会调用torch dot
`00:28:56` Multiprocessing dots bd
`00:28:57` 然后传入那个函数
`00:29:00` 按照我会用这个分支
`00:29:02` 它禁用了分布式功能
`00:29:04` 我们过一遍流程
`00:29:06` 现在我在这个函数里
`00:29:08` 它本该为每个进程异步执行啊
`00:29:11` 请记住world size是进程的数量是吧
`00:29:14` ranked则是zero12
`00:29:17` 一直到world sized减一
`00:29:19` 所以会有和world size一样多的这些函数
`00:29:23` 同时在各自的进程上运行好
`00:29:26` 我现在是rank zero
`00:29:27` 那我该怎么做
`00:29:29` 先有个setup步骤
`00:29:31` 基本上你要配置master的PGDP端口啊
`00:29:35` 注意这其实不是GPU之间通信的方式啊
`00:29:39` 这更多是为了通用的原数据和协调实际数据
`00:29:43` 走NCCL
`00:29:44` 否则速度会非常慢
`00:29:45` 如果你有STUDER
`00:29:46` 就可以用NC c l back end
`00:29:48` 我在笔记本上
`00:29:49` 所以打算用glue back end好
`00:29:51` 现在我在这里运行
`00:29:52` 假设我有四个这样的进程在跑
`00:29:54` 这里有个屏障函数
`00:29:56` 它可以用作同步屏障
`00:29:58` 简单来说
`00:29:59` 一旦遇到这个
`00:30:01` 他就会等所有进程都走到这一步啊
`00:30:05` 基本上你可以把所有进程看作是异步运行的
`00:30:09` 我没法保证某个进程一定比另一个先跑完
`00:30:12` 它们的完成顺序可能是任意交错的
`00:30:14` 所以为了确保某些代码先于其他代码执行
`00:30:18` 我会加上这些同步屏障
`00:30:20` 所以多加屏障的坏处是
`00:30:23` 你可能会陷入不必要的等待
`00:30:26` 好我们来试一下all reduce诶
`00:30:28` 好接下来我会创建这个张量进入到v two3嗯
`00:30:32` 我设置了rank
`00:30:34` 只是为了让例子更有趣一些
`00:30:37` 每个rank将拥有不同的张量嗯
`00:30:40` 我会在or reduce之前把我手上的内容打印出来
`00:30:44` 那现在我跳到这里都看看会打印出什么嗯
`00:30:49` 那么i'm not all reduce
`00:30:52` 之前rank zero是o d one two three
`00:30:56` rank one是one two three four
`00:30:58` 以此类推
`00:30:59` 这和我之前展示的示例一样
`00:31:02` 注意打印语句是按硬件决定的任意顺序出现的
`00:31:06` 因为它在异步运行
`00:31:07` 但所有数据都在那里好
`00:31:10` 那么现在如果我执行all reduce
`00:31:13` 你将这些作为参数传入
`00:31:15` 这是一个派出函数
`00:31:16` 它接收该张量
`00:31:19` 你传入规约操作
`00:31:21` 这里是求和我要说明不要进行异步操作啊
`00:31:25` 它的作用是调用
`00:31:26` 在这种情况下是glue
`00:31:27` 但也可能是NCCL
`00:31:29` 此时它会启动gua kernels执行通信
`00:31:32` 并为你处理好一切
`00:31:34` 然后他基本上就地写入数据
`00:31:37` 因此在all reduce之后
`00:31:39` 我得到了all reduce操作的结果
`00:31:41` 记住这是每一列的和
`00:31:43` 但复制到了所有ranks上
`00:31:45` Thank you
`00:31:46` 谢谢好的
`00:31:48` 这就是all reduce
`00:31:49` 如果你想更高级一些执行异步操作
`00:31:52` 可以设置a sync equals true
`00:31:55` 但那样会搞乱所有这些print语句
`00:31:57` 所以我试着插入比平时更多的barriers
`00:32:00` 对有个问题你在骗人吗
`00:32:03` 谢谢
`00:32:03` 那么问题是rank到GPU的对应关系
`00:32:08` 在这门课里
`00:32:09` rank就是GPU啊
`00:32:11` 是的
`00:32:11` 那我们在这里是另一个例子嗯
`00:32:14` 我要做一个reduce scatter操作
`00:32:17` 那么在这里我要创建一个输入
`00:32:19` 从零到world size
`00:32:22` 然后我要嗯把输出处理好
`00:32:25` 我要为输出分配好空间啊
`00:32:28` 那么在我执行reduce scatter之前
`00:32:31` 它看起来是什么样
`00:32:32` 它看起来像呃嗯我看看嗯
`00:32:37` 这个其中每一块的情况都差不多
`00:32:41` 输入和输出碰巧都是零
`00:32:43` 但其实可以是任何值
`00:32:44` 然后我执行一下reduce scatter
`00:32:48` 张亮操作
`00:32:49` 这里我不做原地写入
`00:32:51` 而是分别用了一个输出张量和一个输入张量
`00:32:55` 我指定要做求和
`00:32:58` 然后接下来我得到的结果是
`00:33:01` 输入基本没有被改动
`00:33:03` 但在输出里
`00:33:04` 我把每个分量的规约结果写进了对应的rank
`00:33:09` 当然嗯嗯谢谢他的问题
`00:33:13` 谢谢呃
`00:33:14` 这本来是不需要他的
`00:33:15` 谢谢好的问题是这样
`00:33:17` 异步执行or reduce是怎么做到的
`00:33:19` 也就是说
`00:33:20` 这在某种程度上就像是一个整体式的操作
`00:33:24` 你说去执行or reduce
`00:33:27` 它就启动quit kernel s去执行通信啊
`00:33:29` 而且要记住
`00:33:31` JUDY本身相对于进程就已经是异步的
`00:33:34` 现在我们让所有进程也都异步了
`00:33:37` 关键在于这段代码执行后会立即返回嗯
`00:33:42` 然后然后你就可以去做其他事情了啊
`00:33:45` 所以有一种典型的做法
`00:33:47` 这门课我不会展开讲
`00:33:48` 就是让计算和通信相互重叠
`00:33:51` 比如说你可以执行这个操作
`00:33:54` 然后接着去加载下一步所需的其他数据
`00:33:57` 因为这些数据和这个操作是相互独立的
`00:34:00` 然后当你想要确认嗯
`00:34:02` 当这些确实都完成时
`00:34:04` 你就可以调用一个WA草
`00:34:05` 等待或者barrier屏障好
`00:34:09` 我们来处理最后一个也就是all gather
`00:34:11` 至此我想你已经大致理解了其中的思路
`00:34:14` 在这里我把reduce scatter的输出设为输入
`00:34:18` 我可以先分配一个输出缓冲区
`00:34:20` 所以在之前the all gather的过程是这样的
`00:34:24` 这里我有来自reduce scatter的结果
`00:34:27` 这个输出缓冲区刚刚分配好
`00:34:29` 里面碰巧带有一些值
`00:34:31` 但不用担心这一点嗯
`00:34:33` 然后在我把数据all gather到tensor之后嗯
`00:34:40` 然后我会得到嗯
`00:34:45` 你知道所有不同的输入啊
`00:34:48` 你知道数据被聚集到所有不同的rank上
`00:34:54` 好的
`00:34:55` 在这里你可以看到确实如此
`00:34:58` 我们通过一个例子来证明
`00:35:00` all reduce其实就等于reduce scatter加上all gather
`00:35:04` 好的
`00:35:05` 最后做个收尾
`00:35:07` 就像我开头先做了设置那样
`00:35:10` 然后我把环境清理掉啊
`00:35:13` 你知道的嗯
`00:35:14` 就是清理环境是个好习惯
`00:35:17` 命令好的
`00:35:19` 所以这就是你第一个torch
`00:35:21` distribute程序的例子
`00:35:23` 你知道的程序好的
`00:35:25` 那么我们来做一些嗯
`00:35:27` 做个基准测试嗯
`00:35:29` 这会很快
`00:35:30` 因为我其实想继续进入第二部分
`00:35:32` 那么通信速度有多快呢
`00:35:33` 我们来做一次all reduce嗯
`00:35:36` 这里我要对银亿个元素做all reduce
`00:35:41` 哎呀好吧抱歉我弄错了好吧
`00:35:45` 那我做一下reduce
`00:35:46` 所以我要创建一个你知道的
`00:35:49` 包含这么多元素的张量啊
`00:35:51` 记得跟之前一样
`00:35:52` 做基准测试前先预热一下啊
`00:35:55` 这里我会调用could a synchronize和barrier啊
`00:35:58` 只是为了确保因为这里存在两种异步情况
`00:36:02` coda kernels和不同的processes
`00:36:04` 我想确保一切都同步到位
`00:36:06` 即使开始前有些操作其实没在跑
`00:36:09` 已经完成了
`00:36:10` 接下来我要做all reduce
`00:36:12` 然后再次通过barrier同步等待停止计时好
`00:36:17` 所以记住这是在每个rank上运行的啊
`00:36:22` 所以啊如果我看一下这里的输出
`00:36:25` 针对rank0213
`00:36:27` 我得到的时间可能不同
`00:36:30` 因为它们都是独立的进程
`00:36:32` 每个进程都会报告某个嗯你知道的测量值
`00:36:37` 如果你想报告一个数值
`00:36:38` 可以取平均值
`00:36:40` 比如嗯
`00:36:42` 所以现在有一件有用的事要做
`00:36:46` 就像我们算MFU时那样
`00:36:48` 就是计算我们来测一下有效带宽
`00:36:52` 思路是这样的
`00:36:54` 刚才花了1.6ms嗯
`00:36:56` 具体是多少
`00:36:57` 这算好还是不好
`00:36:58` 所以要算有效带宽
`00:37:00` 我们得先算出实际发送了多少字节
`00:37:02` 以及理论上应该在这段计算期间传输的数据量
`00:37:07` 然后用这个数据量除以总耗时
`00:37:10` 就能得到有效带宽
`00:37:12` 嗯好
`00:37:13` 那我发送的数据量大小是每个元素的大小
`00:37:17` 乘以元素个数
`00:37:19` 这基本上就是这个数据张量的字节总数嗯
`00:37:22` 实际发送了多少字节
`00:37:24` 这里需要稍微拆解一下
`00:37:26` 所以对于all reduce操作嗯
`00:37:29` 为了简化起见
`00:37:30` 假设我们要把rank zero
`00:37:32` Rank one
`00:37:33` rank two和rank three的值相加
`00:37:35` 由于总共涉及word size减一次加法运算
`00:37:38` 因此需要迭代word size减一步
`00:37:40` 这就是这个因子的来源
`00:37:42` 之所以乘以二
`00:37:43` 是因为既需要发送
`00:37:45` 也需要规约
`00:37:46` 然后乘以负载的大小
`00:37:48` 这就得到了发送的总字结束
`00:37:50` 总持续时间指的是强中时间
`00:37:53` 也就是实际消耗的时间
`00:37:55` 然后乘以VERSIZE
`00:37:57` 因为这是所有rank加权后的总量
`00:38:00` 不带宽是这个即发送的字节数除以总持续时间
`00:38:06` 好的
`00:38:07` 所以在这种情况下
`00:38:08` 结果大约是每秒400GBB好的
`00:38:11` 那么这里有几点要说明
`00:38:13` 第一点看这个表达式
`00:38:15` 有效带宽等于数据大小乘以二
`00:38:18` 再乘以世界大小减一
`00:38:20` 然后除以世界大小与持续时间的乘积
`00:38:24` 所以随着世界大小增加
`00:38:26` 世界大小减一除以世界大小
`00:38:28` 这一项基本趋近于一
`00:38:30` 所以实际上就只剩下数据大小
`00:38:33` 字节乘以二
`00:38:34` 再除以持续时间
`00:38:36` 所以这本质上就是带宽
`00:38:38` 注意它与世界大小无关
`00:38:41` 嗯这是好事
`00:38:42` 也就是说当你增加GPU数量时
`00:38:45` 带宽保持不变
`00:38:46` 它也独立于拓扑结构
`00:38:48` 而拓扑结构是环形还是树形
`00:38:51` 由NCCL来确定
`00:38:52` 嗯所以就是这样好
`00:38:54` 这就是all reduce
`00:38:55` reduce scatter的操作也非常类似
`00:39:00` 所以我会先创建输入和输出
`00:39:02` 做个预热执行操作并计时啊
`00:39:05` 注意看啊
`00:39:06` reduce scatter有这些这些计时数据
`00:39:10` 你也可以在这里测量有效带宽
`00:39:13` 也就是输入数据的字节数
`00:39:15` 你统计的是发送出去的字节数
`00:39:18` 注意这里没有乘以二啊
`00:39:21` 嗯然后用总耗时去除就得到了带宽
`00:39:25` 所以这里的带宽就是这么算出来的
`00:39:28` 嗯应该非常相似
`00:39:30` 我想有时会有一些随机性
`00:39:32` 但结果大致在400多
`00:39:35` 所以这里说明几点
`00:39:37` 请记住
`00:39:38` All reduce
`00:39:39` 正如我们所说
`00:39:41` 由reduce gather加上all gather组成
`00:39:44` 因此all reduce自然要移动两倍的数据量
`00:39:48` 因为reduce gather我就有开销
`00:39:51` all gather也有开销啊
`00:39:53` or reduce执行了双倍的工作量
`00:39:55` 但他花费的时间也是双倍
`00:39:58` 不过两者相互抵消
`00:40:00` 因此你得到的带宽是相同的
`00:40:03` OK嗯好吧
`00:40:07` 你你你好
`00:40:09` 第一部分到此结束
`00:40:10` 也许在我进入第二部分之前
`00:40:14` 大家有什么问题吗
`00:40:16` 为什么你必须同步执行那个操作
`00:40:19` 对其他人来说
`00:40:21` 谢谢谢谢谢谢谢谢
`00:40:23` 那么为什么我们必须为ker kernel做同步
`00:40:27` 所以归根结底我们仍然是在执行could的操作
`00:40:31` 对我们只是有多个process
`00:40:33` 每个process都配有一个GPU
`00:40:35` 正在执行一些coder操作
`00:40:38` 然后如果你在执行Q的操作
`00:40:41` 请记住默认情况下它是异步的
`00:40:44` 所以当你执行到Python中的下一行时
`00:40:46` 那个good的操作可能尚未完成
`00:40:49` 所以我们需要始终等待
`00:40:52` 通过同步来确保它已完成
`00:40:55` 是的
`00:40:56` 所以反过来
`00:40:57` 那么你是必须先做barrier
`00:40:59` 然后再做synchronize吗
`00:41:00` 呃不是不太确定吧
`00:41:04` 所有设备读取代码的设计
`00:41:06` 然后等待所有哦
`00:41:08` 对我觉得有个问题
`00:41:10` 如果你先调用barrier to列
`00:41:13` 可能还没跑完
`00:41:14` 正在运行
`00:41:15` 而你却立刻去执行barrier
`00:41:17` 这样你只是在各自独立的同步
`00:41:19` 不同的科勒克劳斯
`00:41:20` 这意味着并没有真正同步
`00:41:23` 比如barrier
`00:41:24` 如果那些操作都返回了
`00:41:26` Barrier
`00:41:26` 其实并没有起到作用
`00:41:28` 好接着往下讲
`00:41:30` 现在我们来谈谈如何训练模型好
`00:41:33` 我们接下来会过一遍
`00:41:34` 一个非常基础的MLP训练
`00:41:36` 实现多层MLP呃
`00:41:38` 我想这有点重复
`00:41:39` 因为MOP本身就已经是多层感知机了
`00:41:42` 别忘了
`00:41:43` MOP才是transformer里真正的computer瓶颈
`00:41:47` 所以这个例子很能说明问题
`00:41:49` 好数据PARALISM
`00:41:52` 张亮PARALYSM
`00:41:53` 流水线
`00:41:54` Paralysm
`00:41:54` 请你在脑海里想象这张图
`00:41:56` 这是个示意图
`00:41:58` 不用想的太复杂
`00:41:59` 它主要是帮你理解
`00:42:00` 如何切分数据和参数的一种方式
`00:42:03` 数据PARALYSM的做法是把数据拆开分成几块
`00:42:08` 然后每个GPU负责一部分数据
`00:42:10` 我照常我会维护所有参数
`00:42:13` 进行正常的模型训练
`00:42:15` 接下来我需要同步
`00:42:17` 我来解释一下这是怎么运作的
`00:42:19` 所以我要生成一些样本
`00:42:21` 数据批量大小是128
`00:42:23` 维度数是124
`00:42:25` 所以这是一个批量大小乘以维度数的矩阵
`00:42:28` 比如数据矩阵
`00:42:30` 咱们直接开始
`00:42:32` 这就是数据PARALISM
`00:42:34` 明白吗
`00:42:35` 他的工作方式是你有这个数据矩阵
`00:42:38` 我会把形拆开
`00:42:39` is分成数量等于world size的若干份
`00:42:44` 比如这里就是四份
`00:42:45` 每个rank会分到一份哦
`00:42:47` 好的就是维度数和批量大小写了
`00:42:51` 所以每一份我基本上就管它叫本地批量大小
`00:42:54` 你知道就是批量大小除以what size
`00:42:57` 也就是每一个GPU看到的会有30two行
`00:43:00` DOS数据点嗯
`00:43:02` 这里指的是索引的起始位置嗯
`00:43:06` 用从起始到结束的索引
`00:43:08` 就能取出该数据的一个切片
`00:43:11` 他们会把它放到那里啊
`00:43:13` 放到那个rank上
`00:43:14` 好至此每个GPU都持有一个独立的数据tensor
`00:43:19` 即他负责处理那部分数据
`00:43:22` 实际上啊每个rank通常会加载自己的数据
`00:43:26` 以避免这个瓶颈
`00:43:27` 不过这里只是为了演示方便好
`00:43:30` 接下来我们实例化一个MLP
`00:43:33` 我现在开始操作
`00:43:34` 那假设我们设定了层数numb嗯
`00:43:38` 以及层数呢
`00:43:39` 对于每一层我将使用一个尺寸为nam dim
`00:43:44` CHINESENDM的矩阵嗯
`00:43:47` 所以我只是初始化一组随机参数
`00:43:51` 然后我会将其输入优化器
`00:43:53` 好的这就是训练循环
`00:43:56` 在前向传播中
`00:43:57` 我获取数据
`00:43:59` 请记住数据并非全部数据
`00:44:02` 如果我是rank two
`00:44:03` 那么我只获取beat
`00:44:04` 也就是数据的一部分
`00:44:07` 我会便利这些层执行前向传播
`00:44:10` 那么我会执行反向传播
`00:44:13` 通常到这里就结束了
`00:44:15` 但是现在请记住
`00:44:17` 每个rank拥有不同的数据
`00:44:21` 因此gradients也会不同
`00:44:23` 同样地
`00:44:25` 所以这正是让数据并行
`00:44:27` data paralysm生效的关键步骤
`00:44:29` 我们要在所有工作节点之间同步梯度
`00:44:32` 这是标准训练和DDP之间唯一的区别
`00:44:35` 其实这挺优雅简洁的
`00:44:37` 基本上对所有参数我都要做一次ORREDUCE
`00:44:41` 也就是最primeter grad做
`00:44:43` 然后取平均这个ORREDUCE做完之后
`00:44:47` 此时每个rank拿到的梯度就完全一样了
`00:44:51` 然后我就直接更新参数
`00:44:53` 好的
`00:44:54` 我觉得这挺优雅的
`00:44:55` 因为它本质上就是标准训练
`00:44:57` 偶尔也就是把它用在你本地的batch上
`00:45:00` 不过你只要把它插在反向传播后面
`00:45:03` 就行我们就用这个把所有梯度平均一下
`00:45:06` 我做一次reduce就影响代码的事儿
`00:45:08` 代码改完后
`00:45:10` 参数就更新了
`00:45:11` 所以训练时
`00:45:12` 每个rank看起来像是在用全部数据做参数更新
`00:45:16` 但其实它只处理了其中一部分
`00:45:18` 这部分数据好
`00:45:20` 这基本上就是DDP
`00:45:21` 也就是第一种数据并行方法啊
`00:45:24` 编写大家对此有什么问题吗
`00:45:27` 有谢谢
`00:45:28` 问题是是不是只有batch size大于一才能这么做对
`00:45:32` 所以为了让这招真正有意义
`00:45:35` 你的batch size至少得等于word size
`00:45:38` 而且通常应该更大一些的话
`00:45:40` 对谢谢对
`00:45:42` 问题是bad size是不是得是VERSIZE的倍数
`00:45:45` 要是倍数当然更好
`00:45:47` 对我说
`00:45:48` 如果不是的话
`00:45:49` 你可以补零或者做点填充
`00:45:51` 所以确实是有办法的
`00:45:52` 不过如果是倍数的话
`00:45:54` 对大家都会更省事
`00:45:56` What would be the possibility for silver
`00:45:58` 能聊聊针对transformer的DDP吗
`00:46:00` 还有做成应急方案的可能性有多大
`00:46:04` 那基于transformer的情况会怎样
`00:46:06` 嗯我也是这个formerly
`00:46:08` 具体是什么样子
`00:46:09` 其实基本是一样的
`00:46:11` DDP有个好处
`00:46:12` 就是它的模块化做得很好
`00:46:14` 你做前向传播
`00:46:15` 我的意思是DDP在这里只是对参数做平均
`00:46:19` OK他并不在意你的前向传播具体是什么样子
`00:46:23` 好我接着往下讲好
`00:46:26` 这就是DDP
`00:46:27` 所以总结一下
`00:46:29` 各个rank之间的损失值是不一样的
`00:46:32` 梯度在一开始也是不一样的
`00:46:34` 但它们都会经过规约
`00:46:35` 在各个rank之间变得一致
`00:46:38` 因此参数在各个rank之间也保持相同
`00:46:41` 好的下一节课
`00:46:43` TSU将讲解更高级的数据并行技术
`00:46:46` 也就是FSDP和zero那里的思路
`00:46:49` 正如我刚才提到的
`00:46:50` 这里我们使用all reduce
`00:46:52` 它就像一个非常简单的单体操作
`00:46:54` 但它确实需要把模型的所有参数
`00:46:57` 都保留在内存中
`00:46:59` 但如果模型参数太大
`00:47:01` 内存装不下怎么办
`00:47:03` 那你就得想些更巧妙的办法
`00:47:05` 这正是下节课的主题
`00:47:07` 好哇
`00:47:08` 下面我来讲讲tensor parallelism
`00:47:10` 所以这里的思路是我们沿这个方向切
`00:47:15` 我的意思是我们不切数据
`00:47:18` 我们切的是这其实每一层
`00:47:22` 所以每个rank都会分到每一层的一部分
`00:47:26` 数据给的好的
`00:47:29` 通常来说
`00:47:30` 这意味着我们需要传输的数据量会大得多
`00:47:33` 我们稍后会再讨论一下嗯
`00:47:36` 那tensor parallel具体长什么样呢
`00:47:39` 好那么我们先做个简化
`00:47:42` 假设每个rank都持有全部数据嗯
`00:47:46` 这里记住数据维度是batch size乘以NONDEM
`00:47:50` 我来定义一个local num m
`00:47:52` 对于这个rank
`00:47:53` 我只负责其中一部分
`00:47:54` 不得一部分维度
`00:47:55` 所以这里的示意图是说
`00:47:57` 每个模型仍然包含所有层
`00:47:59` 但对于每一层
`00:48:02` 现在参数的数量就是nb dim乘以一个局部数值
`00:48:07` 好
`00:48:08` 假如这是某一层的参数矩阵
`00:48:10` 我会沿着列方向来做切分
`00:48:13` 这也叫做列式张量并行
`00:48:15` 其实也可以按行切分
`00:48:17` 但我们现在先不讨论
`00:48:18` 谢谢好
`00:48:20` 那前向传播具体是怎么进行的呢
`00:48:24` 我应该是遍历所有层并计算ACTIVATIONS
`00:48:27` 所以嗯我是从啊X也就是数据开始
`00:48:32` 然后去访问d layer层的参数啊
`00:48:36` delayer层
`00:48:37` 注意看
`00:48:38` 这仅仅是参数的一部分对吧
`00:48:40` 所以如果我是rank one
`00:48:42` 我就只能拿到矩阵的这一部分
`00:48:45` 但我仍然可以继续
`00:48:47` 我可以应用这个非线性函数
`00:48:50` 因为它本来就是逐元素进行的啊
`00:48:53` 但现在我要做的事也就是把ACTIVATIONS通信出去
`00:48:58` 所以如果我有一个数据矩阵
`00:49:01` Rank one
`00:49:02` 拥有这部分矩阵对应的部分
`00:49:03` Activations
`00:49:04` drank one拥有这个矩阵的部分
`00:49:06` Activations
`00:49:07` 以此类推
`00:49:08` 而我基本上需要把所有ACTIVATIONS都放到
`00:49:11` 所有rank上
`00:49:12` 而且我们知道该怎么做
`00:49:14` 我们之前介绍过all gather呃
`00:49:17` 作为一种集合通信言语
`00:49:19` 所以我拿到了这就是所有的ACTIVATIONS
`00:49:25` 所以这就是HAPITS大小乘以局部维度数
`00:49:30` 即ACTIVATIONS的形状
`00:49:32` 然后我执行一次all gather
`00:49:35` 抱歉
`00:49:36` 这里是在为ACTIVATIONS分配内存
`00:49:38` X是activation的实际部分
`00:49:40` 所以这是批次大小乘以局部维度数
`00:49:44` all gather的意思是每个rank都持有X
`00:49:47` 每个rank都会分配ACTIVATIONS
`00:49:51` 它是一个列表元素数量与what size相等
`00:49:55` 然后在all gather之后
`00:49:58` X会被复制到各自对应的ACTIVATIONS中
`00:50:02` 谢谢好
`00:50:03` 一旦我收集了所有ACTIVATIONS
`00:50:06` 我就把它们拼接起来
`00:50:07` 形成全维度的X
`00:50:09` 其形状是批次大小乘以总维度数
`00:50:13` 好
`00:50:14` 关于列张量并行
`00:50:15` 大家有什么疑问吗
`00:50:17` 所以每一层都要这么做
`00:50:19` 嗯我们现在大家注意数据并行和这里有个区别
`00:50:25` 我们现在得对模型本身动点手脚
`00:50:29` 数据并行非常优雅
`00:50:31` 因为它是按数据切分的
`00:50:33` 模型被当做一个模块来处理
`00:50:35` 但现在我们必须改动模型啊
`00:50:38` 这很大程度上利用了这样一个事实
`00:50:40` 如果你要做矩阵乘法
`00:50:42` 可以把它拆成一组小矩阵乘法
`00:50:44` 我们可以在不同的rank上分别执行这些运算
`00:50:47` 然后把结果汇总起来
`00:50:49` 而等到了反向传播的时候呢
`00:50:52` 猜猜看
`00:50:53` 因为你的radiant sock还是谢谢对
`00:50:57` 那现在问题是反向传播时会发生什么
`00:51:02` 现在enjoy在反向传播里
`00:51:05` 你有ACTIVATIONS
`00:51:06` 你需要做reduce scatter分发到所有不同的部分
`00:51:09` 但是从某种意义上说
`00:51:11` all gather和reduce scatter具有这种双重性
`00:51:14` 在前向传播中
`00:51:16` 如果你执行的是all gather
`00:51:18` 那么在反向传播中
`00:51:19` 你执行的就是reduce scatter
`00:51:21` 那么问题是这些是由autograph自动完成的吗
`00:51:26` 啊这些都不行
`00:51:28` 明白吧
`00:51:29` 如果你只是调用DOBACKWARD
`00:51:31` 他不会这样做
`00:51:32` 因为其中没有PARALYSM
`00:51:33` 就但是你知道的
`00:51:36` 拍照是自带了所有这些功能
`00:51:38` 很多事情都会自动为你完成
`00:51:44` 如果我换成另一种做法
`00:51:46` 我大概需要添加什么或者做些什么呢
`00:51:49` 需要在多大程度上修改我的代码
`00:51:51` 又在多大程度上可以保持原样
`00:51:54` 直接沿用
`00:51:55` 那么在这里哪些是自动为你完成的
`00:51:58` 哪些不是呢
`00:51:59` 嗯在这里我们相当明确的手动管理这些事物
`00:52:03` 这意味着我不去做反向传播
`00:52:05` 而你必须自己管理并调用reduce scatter
`00:52:08` 好的是的
`00:52:09` 谢谢
`00:52:10` 这是特意这样设计的
`00:52:11` 因为这门课是336从零开始构建语言模型吧
`00:52:15` 实际上你通常不必这么做好
`00:52:18` 那我们快速讲一下流水线并行
`00:52:20` 流水线并行的思路是我们把网络这样拆分
`00:52:25` 这样每个rank会分到一部分层
`00:52:27` 每层内部他会拿到所有的维度
`00:52:30` 他还会拿到所有的嗯
`00:52:32` 算了
`00:52:33` 那其中一个rank会持有全部数据
`00:52:36` 每个rank都会以某种形式看到所有数据
`00:52:39` ok so所以这就是他的工作方式好
`00:52:44` 那么我们来看看我们拥有所有数据
`00:52:49` 其大小还是批次大小乘以number on
`00:52:52` 所以我要把层拆开哎
`00:52:54` 这样local number layers就是每个rank负责处理的层数
`00:52:59` 接下来会有local problems啊
`00:53:02` 本质上就是那些局部层对应的参数
`00:53:05` 不过在每一层内部
`00:53:07` 我会做NONDAM乘NONDAM的计算好
`00:53:11` 所以有件事我也许TSU周三会再细说这个操
`00:53:16` 这是micro batches的概念吗
`00:53:18` 嗯那我就直接展示一下
`00:53:20` 并解释我为什么要这样安排
`00:53:22` 所以除了拆分层之外
`00:53:25` 我还要把batch拆成许多micro batches
`00:53:28` 那么如果我是rank zero
`00:53:31` 我就获取数据
`00:53:32` 我会把它拆分成若干个micro batches
`00:53:35` 每组四个
`00:53:38` 对不是micro batch
`00:53:39` 我要做的是呃接收嗯
`00:53:45` 从上一个rank接收
`00:53:47` 然后执行前向传播
`00:53:51` 只对分配给这个rank的那些层
`00:53:54` 然后再发送给下一个啊
`00:53:56` 你知道的rank s好
`00:53:58` 所以这里我其实用的是接收和发送操作
`00:54:02` 他们是点对点通信
`00:54:04` 所以我之前没细讲这些
`00:54:06` 但他们的意思其实挺直观的
`00:54:09` 这基本上是说我是rank
`00:54:12` 我会把这个tensor发给啊
`00:54:15` 我会从rank减一那里接收这个tensor
`00:54:19` 这表示我会把tensor x发给rank加一好吧
`00:54:25` 所以所以基本上原因是嗯
`00:54:28` 我在讨论micro batches utto
`00:54:31` 周三会更多地讲这个啊
`00:54:33` 这是在pipeline parallem里吗
`00:54:34` 所以你有一个rank获取数据啊
`00:54:37` 他处理一部分层
`00:54:38` 然后把它传给下一个啊GPU
`00:54:42` 它处理一部分层
`00:54:43` 然后把它传给下一个GPU啊
`00:54:45` naser so啊
`00:54:48` 这是一种拆分深度网络的很自然的方法
`00:54:51` 但问题是会产生所谓的pipeline bubbles
`00:54:54` 也就是在等待其他张量处理时
`00:54:57` 你其实是在空等
`00:54:59` 并没有在计算
`00:55:00` 这最终导致效率相当低下
`00:55:02` 所以微批次的想法是把数据拆成更小的批次
`00:55:06` 这样你就能快速处理并传给下一步
`00:55:09` 从而减少那些pipeline bubs
`00:55:11` 另外我要提一下
`00:55:12` 在这个很初级的版本里
`00:55:14` 没处理的一个概念是计算和通信的重叠
`00:55:18` 这其实对pipeline paralysm非常重要
`00:55:20` 这基本上给了你正确的结构
`00:55:23` 如果你在这些前面加一个I
`00:55:25` 它就变成异步的了
`00:55:27` 你得加些东西来管理代码
`00:55:29` 这里的想法是
`00:55:30` 当你在这儿计算的时候
`00:55:32` 你应该你可以接收或发送数据
`00:55:35` 所以计算和通信应该重叠起来
`00:55:38` 这样就能减少你实际花在等待上的时间
`00:55:41` 好这里有些内容没讲到
`00:55:44` 希望下次能补上
`00:55:46` 即通信与计算的重叠
`00:55:48` 这在pipeline paralysm中尤为关键
`00:55:51` 我之前没提data paralysm的情况其实也一样
`00:55:54` 因为我只做了一次前向传播
`00:55:56` 最后再做所有这些all reduces
`00:55:58` 但如果你处理得当
`00:56:00` 在后向传播中
`00:56:02` 梯度一算完就可以开始发送
`00:56:05` 这样你就能更充分的把通信和计算重叠起来啊
`00:56:10` 有人问到通用模型的问题
`00:56:12` 同样我觉得这个mp基本上涵盖了你所需的一切
`00:56:16` 也就是理解基础知识所需的大部分内容
`00:56:21` 一些规模更大的模型需要做更多的布计工作
`00:56:25` 因此更难看清核心的核心算法
`00:56:29` 还有一些其他类型的PARALYSM
`00:56:31` 我们还没讲到
`00:56:32` 比如sequence paralysm
`00:56:34` 它把整个序列拆分成小块
`00:56:36` 从而实现attention计算的并行化
`00:56:39` 还有expert paralism
`00:56:40` 它可以并行处理M1中的experts
`00:56:44` 这正是我之前提到的auto or通信派上用场的地方
`00:56:48` 而此外不同PARALYZATION技术的各种组合
`00:56:52` 也会出现这些内容在作业中也会涉及把嗯
`00:56:57` 所以要注意一点
`00:56:59` 你选择哪种PARALISM技术
`00:57:01` 很大程度上取决于取决于硬件
`00:57:05` 例如tensor paralysm涉及大量的通信
`00:57:08` 因为对于每一层
`00:57:09` 你都需要发送这些相当庞大的ACTIVATIONS
`00:57:12` 所以一般来说
`00:57:13` tensor paralism是在节点内部进行的
`00:57:16` 也就是在拥有高带宽的mv link连接处
`00:57:20` 相反你不会在超出mv link域的范围
`00:57:24` 使用tensor paralysm啊
`00:57:27` 至于pipeline paralysm
`00:57:28` 你会看到人们在使用它
`00:57:31` 而且它通常能容忍啊慢得多的啊
`00:57:35` 互联网络
`00:57:36` 所以一些去中心化训练工作
`00:57:39` 会使用pipeline parallelism
`00:57:41` 因为你的节点GPU实际上分布在世界各地
`00:57:44` 但你不会想在那种情况下使用tensor paralysm
`00:57:47` 就是这样
`00:57:49` 所以当你看这些组合时
`00:57:51` 通常是节点内部的tensor paralysm嗯
`00:57:55` 然后是接着是pipe data parallel或FSDP
`00:58:01` 如果需要的话
`00:58:02` 最后是pipeline paralysm
`00:58:04` zhenis be pure jjagger啊
`00:58:06` red其他因素
`00:58:07` 比如如果你做data parallel
`00:58:09` 你可能能进行大量的data parallel
`00:58:12` 但随后你会遇到所谓的critical batch size
`00:58:14` 如果你把batch size增加的太多
`00:58:16` 但像这种啊对你没有帮助
`00:58:19` 在这种情况下
`00:58:20` 你只是在浪费compute
`00:58:22` 那么使用tensor paralysm会更好
`00:58:24` 所以这里有很多啊需要考虑的因素啊
`00:58:28` 我们之后在课上会详细展开啊
`00:58:31` 最后补充一下
`00:58:32` 刚才稍微提到了TPU
`00:58:34` 有一点要注意
`00:58:35` 我们特意选用PYTORCH
`00:58:37` 而且不只是用PYTORCH
`00:58:39` 更是用一种很基础的方式去调用collective operations
`00:58:44` 这样你们能看得更清楚
`00:58:46` 另一种做法
`00:58:47` 尤其是如果你在用JACKSONPU
`00:58:49` 你可以直接定义模型以及分片策略
`00:58:53` 编译器实际上会处理很多决策
`00:58:56` 比如基本确定你需要什么样的通信操作
`00:58:59` 你基本上就是说这块数据得放在这儿
`00:59:02` 这儿和这儿
`00:59:03` 然后编译器就会通过一些魔法来搞定其余部分
`00:59:08` 这确实很吸引人
`00:59:09` 但显然这样会少了很多
`00:59:12` 从零开始构建东西的乐趣
`00:59:14` 好简单总结一下并行化的方法有很多种
`00:59:18` 你可以按数据张亮或专家来切分
`00:59:22` 也可以按流水线或序列来切分
`00:59:25` 我们之前讨论了数据PARALYSM
`00:59:27` 当时只涉及DDP
`00:59:28` 下次我们会讲FSDP和zero
`00:59:31` 正如我提到的
`00:59:33` tensor parallelism对互联速度要求极高
`00:59:37` 而pipeline要求稍低
`00:59:39` 但你得下功夫减少pipeline起泡
`00:59:41` 从更宏观的角度看
`00:59:43` 这种模式很常见对吧
`00:59:46` 所以你可以选择重新计算或者存储在内存中
`00:59:51` 当我们讨论activation checkpointing哈
`00:59:54` 或者在使用GPU时做某些操作时
`00:59:58` 或者在这种情况下
`00:59:59` 你可以把它看作一种扩展
`01:00:01` 即你可以存储啊
`01:00:03` 存在不同的GPU上
`01:00:04` 从这个角度看
`01:00:06` 在数据parallel中
`01:00:07` 你做的是冗余工作
`01:00:08` 从某种意义上说
`01:00:10` 因为每一个rank实际上都在更新其参数
`01:00:13` 并跟踪所有参数S之所以这么做
`01:00:16` 是因为你无需将optimizer state在不同设备间传输
`01:00:21` 嗯所以一方面硬件变得更快了
`01:00:25` 但从某种意义上说
`01:00:26` 我们总是需要更大的模型
`01:00:29` 所以采用分层结构这一思路将一直适用好
`01:00:35` 今天的内容
`01:00:36` 就这些
`01:00:36` 下周三他就会深入讲解更多的POALISM
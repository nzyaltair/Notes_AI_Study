# 分词与Tokenization

## 1. 概述

分词（Tokenization）是将原始文本转换为模型可处理的基本单元（token）的过程，是大语言模型处理输入的第一步。分词质量直接影响模型的词表大小、多语言能力、推理效率和上下文窗口利用率。

现代LLM几乎统一采用**Byte-level BPE（BBPE）**算法，以字节为基本单元迭代合并高频对，可表示任意UTF-8文本，彻底消除未登录词（OOV）问题。分词器的设计是LLM工程中常被低估但对用户体验影响巨大的环节。

## 2. 发展历史

| 时间 | 里程碑 | 特点 |
|------|--------|------|
| ~2010 | 词级分词 | 简单但OOV严重，词表巨大 |
| ~2014 | 字符级分词 | 无OOV但序列过长，语义信息稀疏 |
| 2016 | **BPE**（Sennrich et al.） | 子词分词，平衡OOV和序列长度 |
| 2016 | WordPiece（Schuster & Nakajima） | 基于似然的子词合并 | 
| 2018 | **BBPE**（GPT-2） | 字节级BPE，彻底消除OOV |
| 2018 | **SentencePiece**（Google） | 直接处理原始文本，语言无关 |
| 2018 | Unigram LM（Kudo） | 概率模型，从大词表逐步删除 |
| 2020 | tiktoken（OpenAI） | 高效BPE实现 |
| 2023 | 多语言优化分词 | Qwen 152K词表，多语言均衡 |
| 2024 | 压缩比优化 | 更高效的分词设计 |

## 3. 核心概念

### 3.1 子词分词（Subword Tokenization）

在字符级和词级分词之间找到平衡：
- **词级**：序列短、语义完整，但OOV严重、词表巨大
- **字符级**：无OOV、词表小，但序列过长、语义稀疏
- **子词级**：高频词保留为整体，低频词拆分为子词，兼顾两者优势

### 3.2 关键术语

- **Token**：分词后的基本单元
- **Vocabulary（词表）**：所有token的集合
- **OOV（Out-of-Vocabulary）**：不在词表中的词
- **Token效率**：单位文本对应的token数量（越少越高效）
- **压缩比**：原始字节数与token数的比值

## 4. 技术原理

### 4.1 分词算法对比

| 算法 | 原理 | 方向 | 代表模型 |
|------|------|------|---------|
| **BPE** | 从字符开始，迭代合并最高频pair | 自底向上 | GPT系列、LLaMA |
| **WordPiece** | 类似BPE，基于似然增益选择pair | 自底向上 | BERT |
| **Unigram** | 从大词表逐步删除，最大化语料似然 | 自顶向下 | T5、ALBERT |
| **SentencePiece** | 框架，支持BPE/Unigram | - | LLaMA、T5 |

### 4.2 BPE算法流程

1. **初始化**：将所有词拆分为字符序列
2. **统计**：统计所有相邻字符pair的频率
3. **合并**：合并频率最高的pair，加入词表
4. **迭代**：重复步骤2-3直到达到目标词表大小

```python
vocab = list(all_characters)
while len(vocab) < target_vocab_size:
    pairs = count_pairs(corpus)
    best = max(pairs, key=pairs.get)
    corpus = merge_pair(corpus, best)
    vocab.append(best[0] + best[1])
```

### 4.3 Byte-level BPE (BBPE)

GPT-2引入的关键改进：以**字节**而非字符为基本单元。

**优势**：
- 字节只有256种，词表基础可控
- 可表示任意UTF-8文本，彻底消除OOV
- 语言无关，天然支持多语言

**代价**：
- 非ASCII字符（如中文）消耗更多token（每个中文字符=3字节）
- 需要更大的词表才能达到合理的压缩比

### 4.4 SentencePiece

Google开发的分词框架，核心特点：
- **直接处理原始文本**：不需要预分词（空格分割），对无空格语言（中文、日语、韩语）更友好
- **将空格视为特殊字符**：`▁`（U+2581）表示词首空格
- **支持BPE和Unigram**两种算法
- **语言无关**：同一套工具处理任何语言

### 4.5 WordPiece vs BPE

| 方面 | BPE | WordPiece |
|------|-----|-----------|
| 合并标准 | 最高频率 | 最大似然增益 |
| 方向 | 自底向上 | 自底向上 |
| 代表 | GPT、LLaMA | BERT |

### 4.6 词表设计考量

- **词表大小**：通常32K-152K
  - GPT-2: 50K
  - LLaMA: 32K
  - LLaMA 3: 128K
  - Qwen: 152K
- **多语言覆盖**：多语言模型需更大词表覆盖不同字符集
- **特殊token**：`<bos>`, `<eos>`, `<pad>`, `<unk>`及Chat模板token（`<|im_start|>`, `<|im_end|>`等）
- **数字处理**：单独数字token（T5将数字拆为单个数字）vs 完整数字
- **代码处理**：缩进、符号的特殊token设计

### 4.7 Token效率分析

不同语言的token效率差异显著，直接影响推理成本和上下文窗口利用率：

| 语言 | 字符/Token | 说明 |
|------|-----------|------|
| 英文 | ~4 | 压缩比最高 |
| 中文 | ~1-2.5 | 取决于词表是否包含中文字符 |
| 日文/韩文 | ~1-2 | 类似中文 |
| 代码 | 变化大 | 取决于缩进和符号 |
| Emoji | ~2-5 | 通常消耗多个token |

**影响**：同一内容，中文版可能消耗英文版2-3倍的token，直接影响API成本和上下文利用率。这是多语言LLM词表设计的重要考量。

## 5. 关键方法/模型

### 5.1 主流分词器对比

| 分词器 | 算法 | 词表大小 | 特点 | 代表模型 |
|--------|------|---------|------|---------|
| GPT-2 tokenizer | BBPE | 50K | 首次字节级BPE | GPT-2/3 |
| LLaMA tokenizer | SentencePiece BPE | 32K | 多语言但中文效率低 | LLaMA/LLaMA 2 |
| LLaMA 3 tokenizer | BBPE | 128K | 大幅提升多语言效率 | LLaMA 3 |
| Qwen tokenizer | BBPE | 152K | 多语言优化，中文效率高 | Qwen系列 |
| tiktoken | BBPE | 50K-100K | OpenAI高效实现 | GPT-3.5/4 |
| Claude tokenizer | BBPE | - | 多语言均衡 | Claude系列 |

### 5.2 重要论文与工具

| 名称 | 作者 | 年份 | 贡献 |
|------|------|------|------|
| Neural Machine Translation of Rare Words with Subword Units | Sennrich et al. | 2016 | BPE用于NMT |
| SentencePiece: A simple and language independent subword tokenizer | Kudo & Richardson | 2018 | SentencePiece工具 |
| tiktoken | OpenAI | 2020 | 高效BPE实现 |

## 6. 优势与局限

### 优势

- **子词分词**：平衡OOV问题和序列长度，兼顾灵活性和效率
- **BBPE**：彻底消除OOV，支持任意文本
- **SentencePiece**：语言无关，多语言友好
- **可扩展性**：词表可通过追加新token扩展（如领域专用token）

### 局限

- **多语言不均衡**：英文效率通常高于中文等其他语言
- **分词边界模糊**：不同分词器对同一文本产生不同token序列，影响可比性
- **数字与代码**：数字拆分和代码缩进的处理不统一
- **词表冻结**：预训练后词表难以修改，扩展需特殊处理（如渐进式扩展）
- **对抗样本**：精心构造的输入可能导致异常分词

## 7. 应用场景

- **LLM预处理**：所有LLM输入处理的第一步
- **词表设计**：影响模型多语言能力和推理效率
- **上下文窗口**：分词效率直接影响可用上下文长度
- **成本控制**：token数量直接决定API计费
- **多语言模型**：词表设计决定语言覆盖和效率
- **领域适配**：医疗、法律等领域可能需要专用分词器

## 8. 与其他技术关系

- 分词是[[01_LLM整体架构|LLM整体架构]]的输入处理环节
- [[05_向量表示与Embedding|向量表示与Embedding]]建立在分词之上：token → embedding
- Token效率影响[[07_长上下文与记忆机制|长上下文与记忆机制]]的实际可用长度
- 分词一致性影响RAG系统中检索和生成的对齐
- 是[[08_大模型设计模式|大模型设计模式]]中预处理模式的基础

## 9. 前沿发展

### 9.1 大词表趋势

LLaMA 3将词表从32K扩展到128K，显著提升多语言token效率。趋势表明大词表（100K+）成为多语言LLM的标配，代价是embedding层参数增大。

### 9.2 动态分词

- **多模态分词**：文本、图像、音频统一token化
- **自适应分词**：根据输入内容动态调整分词粒度
- **语义感知分词**：基于语义而非频率的分词

### 9.3 Token效率优化

- **压缩比竞赛**：各模型厂商竞相提升token效率
- **中文优化**：增加中文高频词的直接编码
- **代码优化**：针对编程语言结构设计专用token

### 9.4 无分词架构

- **Byte-level模型**：直接处理原始字节，跳过分词步骤（如ByT5、MegaByte）
- **字符级模型**：完全在字符层面建模
- 优势：完全语言无关；劣势：序列过长，计算成本高

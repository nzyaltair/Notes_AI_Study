---
title: "【极致中配】2026年最新版 Stanford CS336: 从头构建大语言模型"
url: "https://www.bilibili.com/video/BV11LEA6eEuj/?p=18"
bvid: "BV11LEA6eEuj"
cid: "39162547881"
author: "常青藤名校课"
upload_date: "2026-06-05"
created: 2026-08-17
tags: ["bilibili", "clippings"]
---

## 简介

如果这个中文配音版对您理解 CS336 有帮助，欢迎点赞、投币、收藏支持一下，也可以点个关注。
我会持续更新更多 Stanford / AI / 大模型相关课程的中文配音课程，尽量帮大家用更低的理解成本看懂这些高质量课程。你的支持会让我更有动力把后续内容继续做完、做好。

## 字幕

`00:00` 大家好
`00:00` 非常感谢各位前来
`00:01` 我觉得你们这门课很棒
`00:03` 但也谢谢PERCY邀请我来做这个演讲
`00:06` 我想这门课主要讲的是训练怎么训练语言模型
`00:10` 最终做出这些东西
`00:12` sir and they啊
`00:13` 这今天我想讲讲
`00:15` 当你有了这样一个模型之后
`00:16` 从另一面看是什么样的
`00:17` 也就是真正去部署这些模型
`00:20` 做推理是什么样的
`00:21` 把这些东西转化一下
`00:22` 也就是把这些东西
`00:23` 比如说从电力变成token
`00:26` 再变成智能
`00:27` 以及从这个角度看
`00:28` 有哪些有意思的研究问题和研究方向好
`00:32` 我先从一些宏观的动机讲起
`00:34` 我想有一点我们都已经看得非常清楚
`00:38` 那就是这些模型和他们的能力
`00:44` 这些幻灯片是我两年前求职演讲时做的
`00:47` 所以具体的例子已经有点旧了
`00:49` 但你可以做到人类水平的文本生成代码生成啊
`00:53` 比如cursor cloud code g p t5.5
`00:56` 而不只是GPT4
`00:58` 你既能生成也能理解处理图像和视频
`01:02` 很能开始理解新模态啊
`01:04` 哎对生物健康领域就出现了DNA模型
`01:07` 这类应用我一直很关心一个问题
`01:10` 这些突破是怎么做到的
`01:11` 下一代又能怎么改进
`01:13` 规模是这些能力的主要驱动力
`01:15` 看这张旧图
`01:16` 模型规模已经大幅扩展了一二千零一十八年
`01:20` 我刚开始读博
`01:21` 那时最大的模型才1亿
`01:23` Parameter
`01:24` 我们都觉得太疯狂了
`01:25` 到2019年
`01:27` 我们觉得他太危险
`01:28` 不敢发布
`01:29` 这就是GPT2这门课
`01:32` 你们应该能训出GPT2水平的模型
`01:34` 不知道做到没啊
`01:35` 努力的话
`01:36` 你们肯定能啊
`01:37` 如今开源模型都有万亿参数甚至更多嗯
`01:41` 前沿模型参数大概五到10万亿
`01:44` 挺让人兴奋的
`01:46` 有了这些
`01:46` 你就能对话
`01:48` 写代码
`01:48` 分析复杂文本
`01:50` 还能帮你做作业等等
`01:52` 真正了不起的是
`01:53` 这一转变比我们想象的更快
`01:55` 也比我几年前读博初期预想的快得多
`01:58` 我觉得有个lady很贴切
`02:00` 而且日期也很有意思的吻合啊
`02:02` 1902年
`02:03` 曼哈顿有13万匹马
`02:05` 它们不是养着玩的
`02:06` 而是承担着关键作用
`02:08` 近每匹马每天产生好几磅粪便
`02:10` 乘以13万匹
`02:12` 数量惊人
`02:14` 这也成了严重的粪便问题
`02:16` 事实上还真开过专门的学术会议
`02:19` 讨论怎么处理这些麻烦
`02:21` 1898年
`02:22` 纽约就开过一次这样的会
`02:24` 结论是马粪问题
`02:26` 没辙
`02:26` 只能捏着鼻子忍着
`02:28` 10年后到1912年
`02:30` 曼哈顿的汽车数量已经超过马匹
`02:33` 所以在这10年的过渡期里
`02:35` 你看那些存在了几个世纪的东西
`02:38` 开始被汽车取代了
`02:39` 我觉得对咱们做语言模型的人来说
`02:43` 那个1912时刻大概就是去年啊
`02:47` 所以至少对我来说
`02:48` 去年我开始用这些语言模型来写大部分代码了
`02:52` 嗯我团队里大多数人都这么干
`02:55` 我也让所有学生都这么干啊
`02:57` 除了做作业的时候呃
`03:00` 这确实是个令人兴奋的过渡期
`03:02` 咱们正身处其中
`03:05` 推动这一切的一个关键因素是
`03:08` 很多规模效应都由GPU驱动
`03:10` 所以说真的你可以把GPU看作新的石油
`03:14` so free也是较早的消息
`03:17` 但能看到数千亿美元甚至更多资金投入GPU呃
`03:21` 呃历史啊总是惊人的相似
`03:24` 但啊
`03:25` 各国主权基金正把这类投资
`03:27` 作为国家资产的重头戏子
`03:30` 有一点非常清楚哦
`03:32` 推理才是关键
`03:33` 你可以把它看作把电力转化为智能的引擎
`03:37` 嗯就像没有引擎啊
`03:40` 石油无法转化为动能一样啊
`03:43` 推理引擎也至关重要
`03:45` GPU内核让GPU从沙子变成今天我们能用的东西
`03:50` 机器学习模型
`03:52` 其实就是些存在于抽象空间的运算
`03:54` 有向无环图推理引擎和GPU内核
`03:57` 就是要把模型映射到ML运算的关键
`04:00` 这门课会涉及部分实现
`04:01` 比如flash attention
`04:03` 不过在推理这一侧其实藏着巨大的复杂性
`04:07` 所以哪怕今天的内容你都忘了
`04:10` 也请记住一点
`04:11` 只要搞懂推理推理引擎和底层的GPU内核
`04:14` 你就能推动if啊
`04:16` 从而在机器学习算法上实现全站创新
`04:19` 今天我先给大家宏观讲讲token的生命周期
`04:23` 比如当你向魔性发出请求时
`04:25` 请求会经历什么
`04:27` 它如何贯穿整个推理服务
`04:29` 你会面临哪些有趣的选择
`04:32` 杨素我会深入讲两个偏研究性的项目
`04:34` 如果你抽取这个系统的某些部分
`04:36` 你能提出哪些问题
`04:38` 又能做些什么
`04:39` 在讲技术细节前
`04:41` 我先简单做个推介
`04:43` 今天我代表两个机构来到这里
`04:45` 一个是UCSD啊
`04:47` 我在那儿有个小实验室
`04:48` 今天演讲里也涵盖了他们的一些工作
`04:51` 另一个是together
`04:52` 他们做了很多其他工作
`04:54` 今天也会一并介绍
`04:55` together是个AI云平台
`04:57` 涵盖GPU推理微调等全套服务
`05:01` 研究背景很强
`05:02` 比如PERCY
`05:03` 他可能看不见我的鼠标
`05:04` 但就在屏幕上
`05:06` 背后还有强大的研究团队支撑
`05:08` 你接下来在演讲中看到的各项内容
`05:11` 我先大致讲讲一个token的完整生命周期嗯
`05:16` 当你向推理系统发请求时
`05:18` 数据会经过哪些环节
`05:20` 这些幻灯片参考了我学生AUSTIN的演示
`05:23` 以及他在together的经历
`05:26` 顺便说下啊
`05:27` 幻灯片全是nano banana pro生成的
`05:30` 只要别盯着文字细看
`05:32` 效果其实不错
`05:33` 大体上没错
`05:34` 但要是细看就会发现不少地方错的离谱
`05:37` On of for the pandhours after boston
`05:40` 好
`05:40` 咱们来看看推理引擎主要由哪些部分组成
`05:44` 简单说下它怎么运作
`05:45` 请求进来之后
`05:47` 第一步
`05:47` 系统会把请求分配给不同的G
`05:50` 预填充和解码阶段
`05:52` 可能跑在不同的机器上
`05:54` 接着拿请求去查KV缓存
`05:56` 看以前是否处理过嗯
`05:58` 看看能不能省点算力
`06:00` 接着就开始跑核心机器学习代码还算新
`06:04` token和算子优化手段
`06:06` 包括跨机器拆分
`06:07` 多节点并行或节点内GPU并行
`06:10` 具体看取决于模型大小和拆分方式
`06:14` 随着硬件发展
`06:15` 我们会看到更多可选方案
`06:17` 选好跑完代码
`06:18` 最后就能拿到token了啊
`06:20` 你可以问hey模型
`06:21` PERCY说的线性回归是什么意思
`06:24` 因为我那节课缺了
`06:25` 所以你能体验整个全流程啊
`06:27` 值得我们开始思考的是
`06:29` 这些不同的负载到底长什么样
`06:32` 情况多种多样
`06:33` 这张图你要是看的太细
`06:35` 有些舞看着就像S了
`06:36` 对you're doing
`06:37` 但你要想到要真正跑生产流量时
`06:40` 未必也肯定不像你现在做的这样
`06:44` 不像训练时看到的那种token
`06:46` 也不太像你在脑子里凭空编出来的负载哦
`06:50` 那我们通常会看到什么特定负载下啊
`06:54` 输入和输出token会呈现特定的分布呃
`06:57` 拿代码生成来说
`06:59` 比如你用cursor智能体能访问你的代码库
`07:02` 你只管提问嗯
`07:03` 一般情况是输入量很大
`07:06` 得有上万个输入token
`07:07` 接着看模型怎么训练的
`07:10` 它可能输出一些思考
`07:11` token也可能只给简短回复
`07:13` 第二取决于工作负载和模型
`07:17` 比如代码生成跟摘要生成差别就很大啊
`07:22` 要是你把整本书贴进对话框来回聊
`07:25` 那跟普通对话完全不一样
`07:26` 要是你直接问给我讲
`07:28` 讲一阶微积分负载形态就完全不同了
`07:31` 呃
`07:31` 现在用语言模型大多是这种一问一答的智能体
`07:36` 工作流
`07:37` 写代码时
`07:37` 你和代码agent来回沟通
`07:39` 做这个不不是这意思
`07:41` 或者做那个代码agent
`07:43` 本身就能对语言模型做不少迭代
`07:47` 比如我调用工具让grab在代码库里搜点东西
`07:50` 再把结果喂回模型
`07:52` 我也可能上网搜一下用户问的东西
`07:55` 所以对话通常会有好几个来回
`07:59` 另一个有趣的点是不同应用的节奏各不相同
`08:02` 比如你在快速对话循环里
`08:05` 或用语音模式跟ChatGPT聊天
`08:08` 响应通常很快啊
`08:10` 反之如果你run agent自己跑流程
`08:13` 替我做这个
`08:13` 我不打扰你自己
`08:14` 迭代节奏就不同了
`08:17` 要是代理卡住喊帮帮我
`08:19` 我得问问你却没注意到回合间可能又多个空档
`08:24` 所以这些就决定了你的工作负载是什么样的
`08:28` 每次能拿到多少新token啊
`08:30` 你要生成多少token
`08:31` 会画有多长
`08:32` 我是那种反复跟cod代理来回聊的年轻用户吗
`08:36` 还是我只问个问题就走
`08:37` 第二天再回来
`08:38` 还有回合间隔
`08:39` 比如我曾用HGPT代理聊
`08:42` 怎么安排每周的锻炼
`08:44` 我大概每两周才聊一次
`08:46` 这跟其他流量模式差别很大啊
`08:49` 还有要看你的应用
`08:51` 交互式应用可能要求一秒内返回show token
`08:53` 让代理能提示我正在想
`08:56` 或者我知道要生成500个token
`08:59` 希望整个响应在规定时间内返回
`09:02` 让用户能快呃
`09:03` 谢谢请求进来时包含几个基本部分
`09:06` Z稍后详述预填充和解码
`09:09` 假设输入一些文本啊
`09:11` 第一步是分词
`09:12` 我想大家都熟悉啊
`09:14` 然后进入复杂的调度机制
`09:16` 比如是否见过这些token
`09:19` 能否从缓存直接查找激活值
`09:22` 这是机器学习计算的两个主要部分
`09:25` 叫预填充和解码
`09:27` 节奏差别很大
`09:29` 预填充是说假设你手上有这样一段东西
`09:33` 你亚托1万从未见过的token
`09:36` 得算出激活值和逻辑
`09:37` 即输入10000token
`09:39` 输出一个token
`09:40` 这是计算密集型操作
`09:43` 这和你们训练时看的指标差不多
`09:45` 训练时你处理大量token
`09:47` 写好flash tension
`09:48` 内核跑训练循环
`09:50` and pfl差不多
`09:51` 只是不反向传播
`09:53` 然后是decodes deep soldier
`09:54` 逐个生成token
`09:55` 比如输入10000token
`09:57` 问ABC函数是干什么的
`09:59` 模型处理完prom就开始逐个生成
`10:02` 用了推测解码的话
`10:03` 一次能出三四个
`10:04` 每生成一个新token都得重新过一遍
`10:07` 模型算一算就知道
`10:09` 其实需要计算的浮点运算并不多
`10:12` 就是计算量不大
`10:13` 但瓶颈在内存带宽呃
`10:15` 也就是说每次生成一个token都要重新加载模型
`10:19` 模型跑完后你会得到一个代表该token的数值
`10:22` 再转成字符串
`10:24` 接着你会做点处理
`10:26` 比如检查停止token或做安全检查
`10:29` 看用户是不是想恶意入侵系统之类的啊
`10:32` 最后输出token推理引擎就在这一循环里运行
`10:36` 等着接受请求
`10:38` 他就在调度执行和token采样的循环里不断重复
`10:43` 咱们再深入一步
`10:45` 看看系统同时处理多个请求时是什么样
`10:48` 这里有个技术叫连续批处理
`10:51` 看这张图时间是从上往下走的啊
`10:55` 往下走会有新请求进来
`10:57` 比如到第一步
`10:57` 你有一些长请求啊
`10:59` 有用户让你分析一份很长的文档引擎
`11:03` 就一直生成一大堆token
`11:05` 可能还有另一个请求进来
`11:07` 开始占用资源嗯
`11:09` 这些资源首先是计算资源
`11:11` 你得同时跑多个请求
`11:12` 如果你在填kv cash
`11:14` 那就是内存资源
`11:15` 你会看到多个请求同时进行
`11:18` 走一步后
`11:19` 那个短请求可能结束了
`11:22` 又来个新请求
`11:23` 可能要跑好几步卫星来新请求
`11:25` 比如又是个特别长的
`11:27` 就是第四部里那个橙色的
`11:29` 但显存不够
`11:30` 就得把全部k v cash存到GPU上
`11:33` 那现存耗尽时
`11:34` 请求可能会开始排队
`11:36` 等那个场景球跑完就能启动新请求
`11:40` 以此类推
`11:41` 你可以看着它运行
`11:42` 所以你看系统里有一堆不同请求时
`11:45` 运行起来就已经挺复杂了啊
`11:47` 其中一个挺重要的部分叫做kv cash
`11:50` 你可以这么想
`11:51` 很多用户可能都在说
`11:53` hi ChatGPT或hi cloud
`11:55` 理论上不用重新计算激活值
`11:58` K不用给每个用户都再跑一遍
`12:00` 要是用户输入了长文本
`12:02` 只需对它做一次PREFL
`12:03` 下一轮对话里再追加内容时
`12:05` 你不用把整个东西重新算一遍
`12:08` 所以有了叫kv cash的机制
`12:10` 利用前缀共享新来一组token
`12:13` 用基础数这种很传统的数据结构
`12:16` 看看哪些token见过哪些是新的
`12:19` 然后查一下这些激活值大概长什么样啊
`12:22` 算力到了GPU上拆分的方式就有很多种
`12:25` 假设你有一个万亿参数的模型
`12:27` 跑在280GB显存的GPU上
`12:30` 单个GPU根本装不下
`12:32` 整个模型拆分方式有好几种
`12:36` 比如把每个tensor拆成四份
`12:38` 分给四个GPU
`12:40` 或者叫tensor paralysm
`12:41` 还有现在很多SOTA模型是mixture of experts啊
`12:46` 这些独立的expert会根据不同的token
`12:48` 被选择性激活
`12:50` 你可以把它们分到不同GPU
`12:53` 这些选择决定了瓶颈在哪
`12:55` 需要多少GPU
`12:56` 能同时服务多少session等等
`12:58` 他一个常见做法是
`13:00` 把PREFIL和decode分到不同的GPU
`13:03` 不同的机器组上
`13:05` 因为啊他们的计算瓶颈和计算特性截然不同啊
`13:09` 预填充很像你们训练时做的事
`13:11` 直接运行非常消耗算力
`13:13` 你能充分利用GPU
`13:15` 而另一方面它极度依赖内存带宽
`13:17` 因为计算量不大
`13:18` 却得同时加载所有模型
`13:19` 权重耗时也各不相同
`13:22` 预填充通常比单次解码久的多
`13:24` 但解码步骤多得多
`13:26` 因为提示词只预填充一次
`13:27` 每生成一个token就解码一次啊
`13:30` 一个基础优化是我们都开始采用的
`13:32` 让不同的worker组分别处理预填充和解码
`13:35` 从而针对计算站的不同环节做专门优化啊
`13:39` 事实证明
`13:40` 有了这种拆分可以做很多创新
`13:43` 比如你可能听说过NVIDIA收购grog
`13:46` 也就是GPU之王
`13:47` NVIDIA买下了这种新型推理芯片
`13:51` 原因之一是解码负载和预填充负载差异巨大
`13:55` 针对解码
`13:56` 你可以使用完全不同的芯片
`13:58` 比如下一代硬件
`13:59` NVIDIA打算用GPU处理与填充
`14:02` 用grog的LPU芯片做解码
`14:04` it's super brass也是OpenAI和CEREBRAS有算力合作
`14:09` 它解码更强
`14:10` Sabova
`14:11` 这些公司也在这一领域的不同环节下注
`14:13` Non is skin so
`14:16` 当你真正大规模部署这些推理引擎
`14:18` 每天处理上万亿token甚至更多时
`14:21` 就会遇到一些相当嗯棘手的bug
`14:25` 这组bug大多发生在去年年底
`14:27` 涉及一些开源推理引擎
`14:28` 有大规模系统有个特点
`14:31` 小规模下正常的大规模下必然出错
`14:34` 我们说的是发生概率
`14:35` 低于0.001%的事件啊
`14:38` 有些bug极细微
`14:39` 内核略有错误
`14:41` 但触发条件极罕见
`14:42` 算到一半部分
`14:44` LODGETS就会变成NANON
`14:47` 这时模型就开始反复输出同一个token
`14:51` 有个模型后来只输出hi hi hi或感叹号
`14:54` 陷入死循环
`14:54` the hoo模型有人改了处理工具调用的逻辑
`14:58` 工具调用就是模型说嘿我要搜个网之类的模型
`15:03` 说完去搜一下
`15:04` 后端就得用老代码去执行
`15:06` 嗯后来某个引擎处理工具调用出错了
`15:10` 变折线就是输出变长模型一致函去搜一下模型
`15:15` 正常时通常会说he去搜一下pay
`15:18` 我说搞定了
`15:19` 去回复用户吧
`15:20` 但这回他没正确返回
`15:22` 他会一直说去搜一下
`15:24` 去搜一下
`15:24` 怎么还没搜
`15:25` 他会陷入死循环
`15:27` 跑上数万个token嗯
`15:29` 还有个案例很有趣
`15:30` 他同时搞挂了多个推理提供商
`15:33` 还被赖在量化问题上
`15:35` 嗯其实是个更微妙的bug
`15:38` 模型没理由的突然开始蹦出中文字啊
`15:41` 有人猜模型可能用中文微调过
`15:43` 因为问他英文
`15:44` 他却回中文
`15:45` 其实是某个kernel里的一个差异错误
`15:48` 是个很微妙的bug
`15:49` 有时候你会从GPU里读到一些
`15:52` 没初始化的内存数据
`15:54` 经过注意力机制
`15:56` 最后会得到一个随机的中文字模型
`15:59` 会纳闷怎么突然开始想中文了
`16:01` 以为你在问中文问题
`16:02` 结果就顺着聊到中文去了
`16:04` 嗯有时候这是因为模型确实被训练过
`16:07` 让他能想中文
`16:08` 有时候只是别人的代码里有个off by one的小bug啊
`16:12` 在一些更高级的推理框架里
`16:15` 最近开始冒出一些挺有意思的现象
`16:17` so啊跑大型生产系统时
`16:20` 关键的一点就是kv cash越大越好
`16:23` 最好能把不同用户或者同一用户
`16:26` 在不同绘画里的请求都缓存起来
`16:29` 这样能跑的任务就更多是为了处理更多绘画
`16:32` 先把kv cash存到GPU上
`16:33` 结果GPU内存很快就不够了
`16:35` 嗯接着你可以开始存到CPU内存里
`16:38` 如果你留意JENSEN的主题演讲
`16:41` 会发现他最近特别看重CPU性能
`16:44` One fuse
`16:45` 原因之一在于上一代CPU and solo其实很慢
`16:49` 结果成了很多重要任务的瓶颈啊
`16:52` 于是你开始关注
`16:54` 因为如果你50万美元的机器
`16:56` 被你配的1000美元CPU拖了后腿
`16:59` 那就很糟糕了
`17:00` 原因之一是你可能把k v cash存在了CPU内存里
`17:05` 所以你会很在意把k v cash读回来的速度啊
`17:09` 再往后你可能把更多kv cash放到磁盘上
`17:13` 这时就得关注SSD和他的空间了
`17:15` 再说你大概听过OpenAI抢购全球SSD和内存的传闻
`17:21` 原因之一就是为了这种场景
`17:23` 你得在KV缓存里尽可能多地存数据
`17:27` 当然真正构建引擎时
`17:29` 你得处理各种复杂情况
`17:30` 比如这些token我好久没见了啊
`17:34` 我可能会把他踢出去啊
`17:36` 转到CPU磁盘或者其他全局存储里
`17:39` 先请求一来
`17:40` 我得去查去
`17:41` 等再把他们取回来
`17:43` it's kind of加载进系统里
`17:46` 这事就妥了
`17:47` 这感觉有点儿
`17:51` CSD卸载不是指把某种特定负载卸下来
`17:55` 比如要快的时候
`17:57` 你肯定不想这么干对吧
`17:58` 毕竟读SSD太慢了
`18:00` 能再问一遍吗
`18:01` 行
`18:01` 你是问这种卸载
`18:02` 是不是只针对某类特定的工作负载
`18:05` 嗯咱们聊聊这个
`18:07` 我知道你们可能没修过操作系统课
`18:09` 但我强烈建议你们去上这里射击
`18:11` 很经典的调度逻辑
`18:13` 除了右边GPU那块
`18:15` 这张图看着就跟七八十年代操作系统课的图
`18:18` 一模一样
`18:19` 以前咱们也遇到过这问题
`18:21` 电脑里开太多应用
`18:22` CPU内存已满
`18:24` 你就得想办法应对
`18:26` 把这些应用挪到磁盘里
`18:27` 这工作负载其实是一回事
`18:29` 理想状态下
`18:30` 左边那个nana banana模拟了L2U驱逐
`18:33` 这启发式策略其实挺靠谱的
`18:35` 估计有篇操作系统论文说过
`18:37` L2U的效果最多也就比最优解差两倍
`18:41` 那才是最优解
`18:42` 当然如果你能预知未来
`18:44` 知道马上会有特定请求进来
`18:46` 那就完美了
`18:47` 这是我先预去一下内存
`18:48` 其实预测未来是有可能的
`18:51` 比如你打开聊天应用
`18:53` 翻出一个月前的旧对话
`18:55` 这说明你大概率要针对他提问
`18:58` 接着你可能得把它加载到GPU上啊
`19:01` 理想情况下你能预测未来
`19:03` 若不能就靠各种启发式策略啊
`19:06` ch这其实是个的问题
`19:09` 你要把多少嗯流量压到GPU上
`19:12` 我没见过谁想把更少的流量压给GPU
`19:16` 所以啊只要满足咱们开头提的那些SA
`19:20` 你肯定想尽可能多的承载流量呃
`19:24` 有些有趣的事开始了
`19:25` 在上一代black wgpu中
`19:27` NVIDIA开始打造新的GPU及MVL
`19:29` 72gr black qu芯片啊
`19:31` 这就是72块GPU通过高速互联连接起来
`19:34` 于是你开始考虑
`19:35` 怎么把万亿参数的模型分摊到72块GPU上嗯
`19:40` 这有意义吗
`19:41` 能带来什么
`19:42` 会发生什么嗯
`19:44` 怎么考虑容错呢
`19:46` 这些设备常因各种原因出故障
`19:48` 比如连接器很脆
`19:50` 是塑料做的
`19:51` 不是金属插太紧线了
`19:53` 就会one mv链接就变得不稳定
`19:55` 这本身就很麻烦
`19:57` 这是AI时代
`19:57` 所以给芯片加了风扇和TIMEJIA
`20:00` 如果把模型分到64块GPU上
`20:03` 为数百万用户
`20:04` 数万亿token提供生产流量
`20:07` 当某一块GP出故障时
`20:09` 该怎么办
`20:09` 有办法做到容错吗
`20:11` 这里有很多有趣的问题值得琢磨
`20:13` 接着你会发现
`20:14` 这些模型的上下文已经达到百万级甚至更多
`20:17` 那具体怎么处理
`20:19` 是把上下文拆分到多个GPU上吗
`20:22` 那具体怎么做
`20:24` 谢谢我想简要提一种优化方案
`20:26` 当你从系统层面审视整个流程时
`20:29` 就可以着手实施
`20:31` 这是我们几个月前一起发布的一项工作
`20:34` 叫做cash wpfid decode segregation friends
`20:37` 这优化很简单
`20:39` 路由层只需改两行代码
`20:41` 但效果显著
`20:42` 基本思路是近来的大量请求
`20:45` 多维对话中的逐轮交互及用户已经开启了对话
`20:48` 假设平均一段对话持续十轮
`20:50` 随后用户离开
`20:52` 这意味着10%的请求是全新的请求
`20:55` 新请求包含数千token
`20:57` 看起来很不一样
`20:58` 计算成本也高得多
`20:59` So you may
`21:00` 土味
`21:01` 也想把这种预填充
`21:02` 放在一个进行到一半的短对话上
`21:04` 那么比如有人将贴近一本书
`21:06` 说跟我聊聊这本书
`21:08` 你不想让他和另一个人同时跑
`21:10` 不
`21:11` 而那个人正聊到一半
`21:12` 比如问为什么1+1=2
`21:14` 对方答1+1=2
`21:16` 因为数字之类的
`21:17` 你说我没懂
`21:18` 你不会想让这种很短的问答和那个很长的请求
`21:23` 同时跑在同一批GPU上
`21:25` thanks呃
`21:26` 所以你可以做一个很简单的路由
`21:27` 新请求进来缓存
`21:29` 命中率很低
`21:29` 就发到一组GPU上去处理
`21:32` 把这些内容合并
`21:34` 再把其他预热请求发给另一组预填充节点嗯
`21:37` 事实证明
`21:38` 用这种简单的优化服务速度
`21:40` 最高能提升40%
`21:42` 就呃这是第一组内容
`21:44` 我觉得咱们在研究和这些技术上的现状
`21:48` 可以用非常早期来形容
`21:50` 这就是那种类型的事
`21:53` 十到20年后人们回头看可能会想
`21:56` 当时他们为啥聊这个
`21:58` 这难道不是显而易见的吗
`22:00` 其实是因为我们开始在生产环境中
`22:03` 用新方式运行这些东西
`22:05` 看到了新方式下的新流量模式
`22:07` 好了刚才大致介绍了内核推理的生命周期
`22:11` 接下来我要讲两个有趣的研究项目
`22:13` 他们深受我们推理服务中一些现象的启发啊
`22:16` 第一个咱们聊聊语言模型解码
`22:19` 以及怎么用所谓的mega kernel
`22:20` 想让它快得多
`22:21` 这是斯坦福和together的合作运行
`22:24` 解码时根本难点在于你得先处理prompt
`22:28` 再逐个生成token啊
`22:29` 难点在于生成一个token就得跑完整个模型
`22:33` 这意味着你没法像预填充或训练时那样
`22:37` 利用GPU的大规模并行能力
`22:39` 反而把这套高性能系统
`22:41` 变成了单纯的内存读取工具
`22:44` 难点还在于我们写内核和跑模型时
`22:47` 通常是一次只写一个操作
`22:50` 大家大概能理解为什么了
`22:52` 毕竟写内核挺难的
`22:54` 我猜大家写flash attention是肯定没少折腾
`22:57` 所以通常的做法是看看语言模型里有哪些操作
`23:01` 然后一次只跑一个操作对应的内核
`23:04` 这样编程就简单多了
`23:06` 你只需要写好归一化内核
`23:08` 映射内核和注意力内核就行
`23:11` 但你会发现这最终会给系统引入大量停机时间
`23:15` Turns
`23:16` 所以这是你在两个内核上运行
`23:20` 推理时的一个示例
`23:21` 这显然是个示意图
`23:22` 但例子取自某个特定的注意力推理
`23:25` 内核读图的方法是横轴是时间
`23:28` 时间从左向右流动
`23:30` 纵轴是GPU上所有不同的流式多处理器
`23:33` H100上有132个
`23:36` B200上我记得是148个
`23:39` 以此类推
`23:41` 调薪代表有效工作
`23:43` 有调薪时就说明GPU上的某个处理器正在干活
`23:46` 空白处则是在等待
`23:48` 那你是在等其他操作做完好接着干别的都
`23:52` 所以你会发现
`23:53` 不管内核写的再好
`23:54` GPU总有停摆的时候
`23:56` 比如内核启动和拆卸
`23:58` 也就是洪荒区域里那些大空隙
`24:00` 这叫尾部效应
`24:01` 就像短prompt和很长的prompt一起处理时一样
`24:04` 这影响会一直传到最基础的注意力操作
`24:07` 处理一批输入时
`24:08` 如果有长有短
`24:09` 你就得等那个最长的处理完
`24:12` 而且因为跨多个内核运行
`24:14` 这些内核之间的间隙会慢慢叠加起来
`24:17` 都为了解决这个问题
`24:18` 我们做了个叫MAGGIKERNEL的东西
`24:20` 也就是不单独为每个操作写内核
`24:22` 而是用一个内核一次搞定多个操作
`24:26` 这有点像flash tension里的融合
`24:29` 只是覆盖更广
`24:30` 更激进
`24:31` 嗯嗯具体来说它让GPU不再只是单一设备
`24:35` 执行单一操作
`24:37` 你要开始把GPU看作一个庞大的分布式系统形象
`24:41` 好吧
`24:41` 我有大量工作要处理
`24:43` 有些任务存在依赖关系
`24:45` 比如这些红色部分依赖于某些绿色任务嗯
`24:49` 该怎么调度
`24:50` 怎么分配任务才能最大化GPU利用率啊
`24:54` 如果只对注意力推理内核
`24:56` 这样做能获得30%到70%的加速
`25:00` 嗯好处是这能应用到整个模型
`25:04` 比如这是LIMAEB模型的一层
`25:07` 这里我们基本上把整个层合并成了一个内核
`25:11` 你看这些不同的条
`25:13` 他们以非常奇怪的方式重叠在一起
`25:15` 才开始让前一层的权重加载与注意力机制重叠
`25:19` 或者说在注意力操作结束前
`25:21` 就开始执行部分规约
`25:22` 举个有趣的例子
`25:23` 在现代大语言模型中
`25:25` 有注意力层
`25:27` 还有QKV投影
`25:28` 你会给他加入一些rope scaling
`25:30` 也就是这些蓝线
`25:31` 这些蓝线就是QKV加rope橙色线
`25:34` 是注意力的开始
`25:35` 一个关键认识是在QKV完成前
`25:38` 你就能把kv cash加载进注意力
`25:41` 尤其是在DECO阶段带圆圈的这些橙色条
`25:44` 表示QKV加roll还在跑时
`25:46` 你就开始加载kv cash了
`25:48` QKV完成后
`25:49` 你拿到新的query tokens
`25:51` 就能跑完注意力操作的剩余部分
`25:54` 基本上当你能这样精细控制GPU时
`25:58` 就可以在其他操作结束前启动某些操作
`26:01` 再看一个例子
`26:01` 这里的橙色同样代表注意力计算的第一部分
`26:05` 红色是注意力之后的O投影
`26:07` 在注意力运算结束前
`26:09` OTO影就已经开始加载权重了
`26:12` as swan pology学整合进一个相对复杂的code框架
`26:16` 用基于指令的抽象
`26:18` 让每个字内核都能独立写进各自的文件里
`26:21` 再搭建一个大型虚拟化共享内存系统
`26:25` 来协调这些操作的运行啊
`26:28` 为此我们做了一个叫thunder kittens的COU
`26:30` 它属于编写内核的那类库
`26:33` 你也可以把它想成更底层的TRITON
`26:36` 对细节的控制要精细的多
`26:37` 就是推理解码速度接近光速啊
`26:41` 现在这数据已经好多了
`26:43` 青色这根柱子就是mega kernel的表现啊
`26:46` 你看他比其他一些顶尖引擎跑的快多了
`26:51` 在H100上带宽利用率达到72%
`26:54` 几乎跑满了GPU的物理极限
`26:57` 抛开我们这里做的所有复杂处理
`26:59` 只看GPU执行这个操作的物理极限速度
`27:02` 我们已经接近那个极限的72%了
`27:05` 这部分的一个启示是
`27:06` 如果你能深度掌控内核
`27:08` 理解硬件
`27:08` 就能解锁全然不同的计算范式
`27:11` 这些细节只有当你深入到推理的底层去摸索时
`27:16` 才会发现
`27:17` 那下面我要讲点新东西
`27:19` 这就进入新架构的领域了
`27:21` 我要介绍一个新模型叫per se
`27:24` 这是我UCSD实验室的成果
`27:26` 由hidden主导
`27:27` 还和ZACHARYTAYLOR两位合作完成
`27:29` 我待会再回到这点
`27:31` 开场时我讲过这些新能力之所以出现
`27:34` 是因为你开始扩大模型参数和数据的规模
`27:37` 在parse这个工作里
`27:40` 我们想问另一个问题
`27:42` 规模扩张是唯一的路吗
`27:44` 还是说有没有别的办法能达到同样的质量
`27:48` 做PARSECTION
`27:49` 我们想试试循环transformer技术
`27:52` 就是把transformer的某些模块拿出来循环跑
`27:55` 所以与其让token1层层过模型
`27:59` 不如让他们在处理过程中反复在循环里跑
`28:02` space设置里面有几块内容
`28:03` 首先我们用了一些状态空间模型理论
`28:07` 还有SM理论
`28:09` 主要是为了稳住这个操作啊
`28:11` 要是直接刨直接训
`28:13` 我们发现模型会直接崩掉啊
`28:16` 另外我们发现一些有趣的缩放定律
`28:18` 说明数据多了
`28:19` 得增加这些循环模型的循环次数
`28:23` 所以为了把perimeter用好
`28:25` 你得在一定程度上复用他们呃
`28:28` 先说说动机
`28:29` 为什么我们觉得这个循环问题很有意思
`28:32` 简单来说
`28:33` 假设有个激活值穿过模型的一部分
`28:35` M它会碰到循环块
`28:38` 反复穿过该层
`28:39` 多次运行相同的激活值
`28:42` 紫色块是循环块
`28:43` 最后会返回结果
`28:45` 这有好处
`28:46` parameter保持不变
`28:47` 却能调高FLOS嗯
`28:49` 如果觉得flops越多
`28:51` 质量越高
`28:51` 这就能不增加parameter成本来提升质量
`28:54` Drive
`28:55` 虽然还有几年前的旧作
`28:57` 就是相对的表明实际能获得更高表达能力
`29:01` 有些东西用相同parameter数表达不了
`29:05` 而这些循环模型却能表达
`29:07` 我们一直在思考怎样让每个参数发挥最大价值
`29:10` 也就是在数据里
`29:11` 这些模型能展现出怎样的智能水平啊
`29:15` 起初有些不错的结果
`29:16` 马里兰大学tom goldstein团队有篇论文说
`29:19` 这东西说不定比transformer更强
`29:22` 这是YORK任务上的一些结果
`29:24` twitter上也有不少热议
`29:25` 因为发布parse前一周
`29:27` Open ai
`29:28` 有人说cloud matos是个循环语言模型
`29:30` 嗯我觉得他说的不对
`29:32` 他就是在瞎编
`29:32` 结果那会儿twitter上全是各种猜测闹得沸沸扬扬
`29:38` 最后他只能发博文认错
`29:40` 说那都是他瞎编的啊
`29:43` 这些都不是真的
`29:44` 但我们觉得挺有意思
`29:45` 所以早在那波twitter热议之前就开始研究了
`29:50` 不过我觉得这里有些地方挺值得琢磨的
`29:53` 这类循环模型有个问题
`29:55` 你训练他们的时候
`29:56` 只要稍微改动一下训练算法
`29:58` 比如把学习率调一点点
`30:00` 模型就会突然开始崩掉model
`30:02` 比如说我个学习率扫描十次
`30:04` 有九次
`30:04` 这模型都会啊
`30:05` 模型不收敛会爆掉
`30:07` 冒出name或损失激增
`30:09` 训练大模型是若见损失暴涨
`30:11` 说明训练出问题了
`30:12` 对深究到底是怎么回事
`30:14` 以前靠土办法
`30:16` 比如每层都加归一化来看情况
`30:18` 或者干脆只用2E4的学习率
`30:20` 别的都不悬
`30:21` 这损失暴涨说明底下还有更深层的原因
`30:25` 我们用类似状态空间模型的思路
`30:28` 来解决稳定性问题
`30:30` 我们的核心发现是这个过程是可以分析的
`30:34` 直接硬算太复杂
`30:35` 因为R模块参数实在太多
`30:38` 模型里有很多非线性操作
`30:40` 比如soft max gu和rope sason
`30:43` 太复杂了
`30:43` 我们的思路是
`30:45` 直接看看这个残差到底在发生什么
`30:47` 就那激活值在各模块之间变化其实不大
`30:50` 这是我们第一个发现啊
`30:52` 这些参差块只是微调向量
`30:55` 影响其实没那么大
`30:56` 一那么我们接下来该怎么做呢
`31:00` 我们试着建个模
`31:02` 深挖一下里面的细节
`31:04` 我们基于这个残差建立了一个动态系统模型
`31:08` 这么做之后
`31:09` 我们发现模型里有几个部分
`31:11` 写出来是看着不起眼
`31:12` 实际上却影响巨大
`31:14` 我们把这些非线性组件
`31:16` 比如注意力机制GLU和带中间层的大前馈网络
`31:21` 先打包处理
`31:22` 我们把它们装进一个盒子
`31:24` 叫做RR是某种复杂的非线性结构
`31:28` 我们把它放到一边的
`31:30` 剩下的就是A和B矩阵
`31:32` B矩阵负责对你初始的向量做某种变换
`31:36` 循环开始前初始向量是什么
`31:38` 而AH阵决定了
`31:40` 如何在每次循环中变换这个残差
`31:42` I will hold this visuits
`31:43` right about transformer的复杂性都放到一边
`31:46` 就能用一种相对简单的方式来看待之前的循环
`31:49` 在以前的案例里
`31:50` 你确实会做一些挺常规的决定啊
`31:54` 比如一种情况
`31:55` 你直接把它当成恒等变换
`31:56` 就是简单相加
`31:58` 另一种情况
`31:59` 它是一个完全可学习的矩阵
`32:02` 我们接着想
`32:03` 要是直接嗯经验显示
`32:05` A和B矩阵其实主导了方程的量级
`32:08` 直接去掉那个复杂的非线性部分
`32:11` 结果会怎样
`32:12` 结果呢
`32:13` 你会得到一个很简单的系统
`32:14` 用微积分就能解出答案
`32:16` 就是EC
`32:17` 你可以算出给定值初始激活和初始注入
`32:21` 你甚至可以直接经验性的算出t plus
`32:23` 一布什的激活状态
`32:25` 你会注意到几点
`32:26` 这由A矩阵主导
`32:28` 尤其是被大幅密次的那个A矩阵sir
`32:32` 由此我们意识到有个量叫A的谱半径啊
`32:36` pro半径基本等同于范数
`32:38` 一种看法是你把矩阵大幅幂次
`32:41` 咱们打个比方
`32:42` 假设这个矩阵就是个标量
`32:44` 2T大概是16啊
`32:47` 你把机会值放大到二的16次方
`32:50` 数值变得巨大啊
`32:51` 这解释了为何损失值会剧烈波动
`32:54` 我们发现前任论文里A和B矩阵的选择
`32:58` 要么编辑稳定
`32:59` 要么就不稳定啊
`33:01` 这样会让系统不稳定
`33:02` 所以在PC里我们想如果让A和B随便怎么变
`33:06` 系统就会爆炸
`33:08` 那咱们约束一下A和B
`33:10` 让它们在数学上不会爆炸呢
`33:12` 对于A咱们直接把它设成负对角矩阵
`33:16` 这样他的幂次项最终会趋于零
`33:18` 不会发散
`33:19` 对于B矩阵
`33:20` 我们加了个简单的线性范数约束
`33:23` 因为它只应用一次
`33:24` 不会发散
`33:25` 只要铺半径小于一系统就稳定了
`33:28` 训练时你会发现损失曲线很稳定
`33:31` dot pass就是我们对A和B重采书画后的稳定版
`33:34` 哪怕用614这种让别的模型崩掉的学习率
`33:38` 最后模型还是纹的啊
`33:40` 这样就能自然约束激活值的状态
`33:42` 范数票那条橙色基线就是完全没加约束的模型
`33:47` 它直接炸到十的19次方
`33:50` 蓝色线则是加了范数约束的模型
`33:53` 而啊模型想扩张激活值
`33:55` 因为他觉得空间越大
`33:57` 表示效果越好
`33:58` 咱们把这些概念尽量拉开距离
`34:00` 再用范数约束
`34:01` 把向膨胀的数值压回到一
`34:03` 这两种力量互相拉扯
`34:06` 导致损失出现剧烈波动
`34:09` 虽然右侧范数控制的很好
`34:11` 激活没炸
`34:12` 但损失值确实出现了很糟糕的波动
`34:15` 所以激活值稍微改一下
`34:17` 就能稳定训练和这些循环过程啊
`34:21` 系统不仅更稳了
`34:22` 模型质量也更高
`34:24` 这张表对比了per se模型和之前的循环
`34:28` transformer模型
`34:29` recurrent depth models啊
`34:31` 可以看到它在各种应用里表现都更好
`34:34` PERSE不仅胜过上一代循环模型
`34:37` 还强于那些很强的transformer极限啊
`34:40` 这种transformer就像那些微型对话模型
`34:43` 一群人都想让他尽快学会拿这种模型来说
`34:47` 用同样的基础transformer架构做循环并稳住它
`34:51` 困惑度会更低
`34:52` 端到端质量也更好
`34:54` 接着我们跑了一些基础的扩展定律
`34:56` 我就在想这事儿挺有意思啊
`34:58` 循环操作确实能提升效果
`35:01` 但咱们得想想有没有证据显示
`35:04` 我们应该更激进的这么干哇
`35:06` 先退一步说
`35:07` 几年前当我们开始大规模扩展这些模型时
`35:11` 一个自然的问题是是该把模型做大
`35:14` 还是只是多训练些数据
`35:16` 几年前大家就开始琢磨
`35:18` 这两个量该怎么一起扩展啊
`35:21` 初二参数还是扩数据
`35:22` 咱们搞出了一堆复杂的密率曲线都长这样嗯
`35:26` 这些图挺漂亮
`35:27` 颜色也多
`35:28` 咱们主要得看的是
`35:30` 抱歉你要注意的是
`35:32` 曲线向右下倾斜
`35:33` 就说明要同时扩展数据和参数
`35:36` that啊
`35:37` 如果曲线垂直向下
`35:39` 那就只要增加训练数据嗯
`35:41` 不用加参数
`35:42` 如果曲线水平向右
`35:43` 那就完全别加数据啊
`35:45` 只加参数
`35:46` 曲线向右下倾斜
`35:48` 说明要同步扩展数据和参数
`35:50` 还用35000000000000token训练万亿参数模型啊
`35:54` 效果更好
`35:55` 我们想问循环机制怎么融入
`35:58` 其中有两种可能向右下倾斜
`36:01` 意味着要扩展数据和参数
`36:03` 结合循环机制
`36:04` 有几种
`36:05` 可能比你听的会觉得根本不该用循环机制
`36:09` To be u
`36:10` 最好还是用同一个循环模型
`36:12` 你可能会觉得要么多做循环
`36:15` 要么只对一小部分做循环
`36:16` 我们这里展示的是
`36:18` 至少在初步的缩放定律里
`36:20` 这些曲线都保持计算量和参数规模不变
`36:24` 所以那条曲线就是一个模型
`36:26` 左右两边的参数数量是一样的
`36:29` 往下走
`36:30` 颜色变化
`36:30` 意味着
`36:31` 我们通过增加数据量来提升训练模型的算力
`36:34` 所以这里我们在调整数据量和循环次数
`36:38` 我们发现这两种模型都再次呈现出
`36:41` 右下倾斜的趋势呃
`36:43` 这说明对于这些固定参数的训练
`36:46` 随着数据量增加
`36:47` 你也应该相应增加循环计算的量
`36:49` 我们发现这些循环遵循经典的密律关系
`36:53` 现在你其实可以开始预测了
`36:55` 随着循环和token同步扩展
`36:58` 这些缩放定律就能预测质量额
`37:01` 头部运动声音大小
`37:02` 对循环呢对底部缩放的联合权重呢啊
`37:06` 我们之前有个很复杂的3D图
`37:09` 展示了不少东西啊
`37:10` 比如循环数据和参数
`37:12` 它大致指向右下方
`37:14` 同时也指向那个方向
`37:15` 所以如果你相信这个图
`37:18` 它表明你应该同时扩展这三者
`37:20` 对啊
`37:21` 但这个图真的很难看
`37:22` 因为它是3D的
`37:23` 有点怪啊
`37:24` 这些命率表明增加数据时你应该增加循环
`37:28` 还有其他命率表明增加数据时
`37:30` 你应该增加参数嗯
`37:32` 显然该增加条件允许的话
`37:35` 这三项最好都提上去
`37:38` 但有一点
`37:39` 如果你固定模型大小
`37:40` 同时增加数据量
`37:42` 你也得增加RECURREN
`37:43` 这挺有意思
`37:44` 因为据我所知
`37:45` 现在的模型都不带RECURREN
`37:47` 所以他们都在曲线最左边
`37:50` 数据量巨大
`37:52` 说明训练时我们或许能做的更好
`37:55` 都没错
`37:56` 这只是换个角度展示这个实验
`37:59` 看橙色曲线
`38:00` 这里我们固定了模型规模
`38:02` 橙色曲线代表固定深度的模型
`38:05` 也就是传统的transformer模型
`38:06` 蓝色曲线是固定flop预算的情况
`38:09` looping模型在曲线上该取哪个点
`38:11` 这里橙色和蓝色的点训练用的算力相同
`38:14` 但数据量不同
`38:15` 模型大小是一样的
`38:17` 如果是靠增加循环次数
`38:20` 而不是只堆数据来达到这个算力验证
`38:23` 损失会更低
`38:24` 这说明我们或许应该
`38:26` 让所有大规模预训练都循环起来
`38:29` For goes
`38:29` 那say theis accurt
`38:31` 我稍微退一步
`38:32` 回到这次演讲的核心要点
`38:34` 希望今天我让大家多少明白了
`38:36` 如果你理解推理
`38:37` 都理解这些模型
`38:39` 理解GPU内核
`38:40` 理解构成它们的每一个部分
`38:42` 你就真的能在机器学习算法上实现全站创新
`38:45` 无论是通过新的路由算法来处理更多流量
`38:49` 或换种方式分配流量
`38:51` 还是用新的内核让系统的一部分跑得快得多
`38:54` 又或是用新架构让参数少很多时也能装进
`38:58` 让他们以不同方式装进部分GPU
`39:01` 减少通信量
`39:02` 这些都是这个问题
`39:04` 也就是我们今天看到的这个研究
`39:07` 难题的不同侧面
`39:09` 也希望今天我至少能激发在座的一位
`39:12` 去更深入地探究其中一些内容好了
`39:17` 谢谢
`39:18` 很高兴回答几个问题
`39:19` 我们有大约五到10分钟的提问时间
`39:22` 有问题的请举手啊
`39:24` 我想第一天比如你是好的
`39:27` 从头开始在步子上训练
`39:30` 我想知道你能不能展示一下其中的一些内容
`39:33` 好的好问题
`39:34` 问题是关于PARC
`39:36` 我们是不是从头开始训练
`39:37` 你能用预训练模型做什么嗯
`39:40` 几个月前有人发了篇很恶搞的博客
`39:42` 他说嘿我没训练任何东西
`39:44` 就赢了一个排行榜比赛嗯
`39:46` 他其实是在一个QUAN模型里循环了两三层
`39:50` 然后发现在一些数学问题上
`39:52` 它的质量开始变高啊
`39:55` 我们有一些相关研究在做
`39:57` 可能很快就会发布啊
`39:58` 但有些模型只要对预训练模型做一点循环迭代
`40:01` 就能得到更高质量的结果
`40:03` 嗯这真挺怪的
`40:05` 有点让我想不通
`40:06` 不知道为啥会这样
`40:07` 我们对这个挺感兴趣的
`40:08` 要是能说服hidden
`40:11` 他会去盯着激活值和实际的权重
`40:13` 看看循环为啥能让效果变好
`40:16` 顺着刚才说的
`40:17` 你提到了循环模型在算力上的最优性
`40:20` 是能聊聊推理方面的影响和显存吗
`40:23` 这怎么帮你跑得更快得
`40:25` 我对这些循环方法特别兴奋的一点是
`40:28` 高效跑推理的主要瓶颈往往是GPU显存参数少了
`40:32` 就能塞进更多kv cash
`40:34` 或者减少跨GPU拆分带来的通信开销
`40:38` 所以啊小模型能带来很大的灵活性
`40:41` 嗯我有个想法
`40:42` 要是把循环块缩的足够小
`40:44` 就能写个微型巨型内核
`40:46` 用超快循环跑这些计算嗯
`40:49` 目前还没法把块儿做的那么小
`40:51` 但这挺有意思
`40:52` 当然下一代LPU来了
`40:54` growth芯片会结合NVIDIA的技术
`40:56` 他们大概有250m by内存
`40:59` 所以能塞进去的东西极少
`41:01` 不过可以设计适配方案
`41:02` 让权重常驻内存极速跑
`41:04` 激活值
`41:05` 跨过这些阈值会有非线性收益
`41:08` 希望我们很快能做到对
`41:10` 在那边你刚才提到对的伤害性颤动
`41:15` 我想权红贯日被封号
`41:19` 这总是严格最优的
`41:20` 但人们不直着走啊
`41:22` 超大内核的代价是什么
`41:24` 是啊工程师的血汗啊
`41:27` 事实证明
`41:27` 编写超大内核非常耗费人力
`41:30` 打个比方
`41:31` 顶尖工程师一年可能也就为一种硬件
`41:34` 做出两三个模型的超大内核啊
`41:38` 批量大小1~16还行
`41:40` 一旦到17就得推倒重来
`41:44` 编写难度极大
`41:45` 我们正试着用编译器把其中一些工作自动化
`41:48` 这个过程做起来确实很难
`41:50` 在GPU编程里
`41:52` 超大内核这个概念过去几十年一直起起落落呃
`41:57` 但一旦搞定速度就顶天了
`41:59` 再也快不了
`42:00` 但这确实得耗费大量精力
`42:02` 行啊
`42:03` 我能问个问题吗
`42:04` 好咱们能多聊聊协同设计吗
`42:07` 比如你提到的GRC和SUBRAS
`42:09` 这些推理新硬件是如果你设计模型
`42:12` 且知道他要在特定平台服务该怎么调架构
`42:15` 对有几件事得注意
`42:17` 首先内存是主要瓶颈
`42:19` real在特定SEREBR芯片服务得看晶圆算内存
`42:23` 调整模型大小
`42:24` 以容纳KV缓存
`42:25` a real chess6点余量
`42:28` 看最近的中国模型
`42:29` 他们的一些选择
`42:30` 暗示了他们可能在考虑华为的新芯片
`42:34` 你会看到一些量化选择
`42:36` 不比如你要在英伟达显卡上跑模型
`42:39` 像他们的nemo tr
`42:40` 就得用NVFP4格式来训练
`42:43` 这是英伟达芯片专有的FP4格式
`42:47` 如果你不用英伟达
`42:48` 比如用AMD
`42:49` 就得用另一种叫MXIP4的格式
`42:51` 它们各有优劣
`42:52` 所以啊
`42:53` 你得根据选用的硬件来做这些细微的选择
`42:58` 谢谢VERCEL
`42:59` 想问
`43:00` 如果只在乎计算最优训练
`43:02` 循环迭代是不是比增加参数更好
`43:04` Pute optimal training
`43:06` 还是说这主要是为了降低推理成本的技巧
`43:10` 对关于计算最优训练的技巧是计算最优
`43:13` 通常是指给定FOP预算
`43:16` 麻烦重复一下问题
`43:17` 抱歉问题是when percel
`43:19` 你会选择循环迭代
`43:20` 而不是增加参数吗
`43:22` 计算最优的技巧就是在给定f log预算下
`43:25` 搞清楚你想达到什么目标
`43:28` 这么说有点牵强
`43:29` 毕竟想提升模型质量
`43:31` 直接增加IPHLOX预算就行
`43:34` 定好模型规模后就拉长训练时长
`43:37` 要是规模受限
`43:39` 那就多跑几轮循环
`43:41` 数据用完了就挑个你觉得合适的模型
`43:45` 规模在选定规模下尽量练好
`43:48` 我觉得选择很多
`43:49` 当然得看这东西到底能不能被采纳
`43:51` 我该怎么部署它
`43:52` 真要搞开源
`43:53` 现在大家笔记本上跑得动多大的模型
`43:57` 我觉得这些考量最终都归结为一个决定
`43:59` 你打算训练多大的模型
`44:01` 嗯我觉得只要模型更大
`44:04` 数据更多
`44:05` 性能肯定更好
`44:06` 对我觉得这主要看那些呃设计细节
`44:10` 谢谢
`44:12` 也许还有一个关于协同设计的问题
`44:14` so你开头提到了不同的用例啊
`44:17` 比如代理式代码生成
`44:19` 还有批量数据处理
`44:20` 就是那么不同用例之间
`44:23` 你看到的最佳架构最显著的区别是什么
`44:26` 有想归根结底
`44:27` 模型开发者得选一个
`44:29` 还得尽量做到合理
`44:31` 中国有很多是的好问题
`44:33` 我认为一个巨大的区别在于
`44:35` 在这些代理循环工作流里
`44:37` 有一点很重要
`44:38` 就是要让你的KV缓存尽量保持um or there
`44:42` 越热越好
`44:44` 所以如果你做的是大规模批量处理
`44:46` 每个文档只看一次
`44:48` 然后翻译KV缓存就没那么重要了啊
`44:51` 比如deep seek的ma啊
`44:53` 注意力机制相比其他模型
`44:55` 它对KV缓存做了激进的压缩
`44:58` 或者如果模型能在FP8或者F4下
`45:01` 处理KV缓存
`45:02` 这在缓存大小上都是相当大的差异
`45:05` 如果你关注智能体工作流
`45:07` 就会注意到这点
`45:08` 当然最关键的是因果注意力还是非因果注意力
`45:11` 对吧嗯
`45:12` 所以如果只是做大批量处理
`45:14` 比如谷歌过去长期只用bird模型
`45:16` 我想搜索里现在可能还在用
`45:18` 但呃因为另一端其实不需要生成大量token哦
`45:22` 所以做一次大的双向注意力
`45:26` 就能得到向量输出结果
`45:28` 存入数据库
`45:29` 随你怎么用
`45:30` 但对话工作流的处理总会包含解码这部分
`45:33` 比如T5这类这种方案嗯
`45:36` 人们曾选择用它先做双向处理
`45:38` 再做生成
`45:39` 谢谢好
`45:40` 最后再问一个问题哦
`45:41` 对我主要想问关于mega kernels的问题
`45:45` 当时是想把所有东西融进一个内核
`45:49` 但说到可移植性
`45:50` 其实你做的是训练芯片好嘞
`45:52` 就是想问问这些超大内核怎么跨多台机器通信
`45:57` 还是说必须扩展到训练环节
`45:59` 问得好
`46:00` 你是问当多个GPU在循环里通信时
`46:03` 超大内核怎么运作对吧
`46:05` 我们早期做过一些初步研究
`46:07` 发现只要设置得当
`46:09` 也能把NC cl调用融进超大内核
`46:12` 我觉得还没找到特别好的杀手级应用场景
`46:15` 有时候瓶颈就在NC co调用本身的延迟上
`46:19` 我觉得这些也能融合啊
`46:21` deep six c出来时
`46:22` 他们为MOE推理层做了个超大内核
`46:25` 专门跑这一块
`46:26` 还真把部分通信也融了进去
`46:29` 我觉得现在越来越能看到的趋势是
`46:32` 模型越来越多时
`46:33` 你会为一部分计算做个小的超大内核
`46:37` 但不一定为整个模型做
`46:38` All the time
`46:39` We have
`46:39` Let's
`46:40` Thank dan again
`46:40` Thanks so much for having me
`46:43` 除非你愿意拼尽全力把整套真正跑起来
`46:45` 好时间差不多了
`46:47` 再次感谢DAN
`46:48` 谢谢邀请
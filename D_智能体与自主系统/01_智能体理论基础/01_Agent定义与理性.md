# Agent 定义与理性

Agent 根据观测历史 \(h_t\)、目标 \(g\) 和约束 \(c\) 选择动作：\(\pi(a_t\mid h_t,g,c)\)。自主性是连续属性，体现在目标解释、状态维护、计划、执行、恢复和学习中减少逐步人工指令的程度。

理性 Agent 以结果效用、风险、成本和约束共同评价行动。奖励不是唯一目标：权限、隐私、时限、不可逆副作用与用户意图必须显式进入决策。

LLM 提供语言推理先验，但不自动具备稳定状态、可靠预测或行动授权；这些能力由后续环境模型、规划、运行时和治理模块补足。

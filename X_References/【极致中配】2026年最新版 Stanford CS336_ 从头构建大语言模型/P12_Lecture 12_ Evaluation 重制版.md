---
title: "【极致中配】2026年最新版 Stanford CS336: 从头构建大语言模型"
url: "https://www.bilibili.com/video/BV11LEA6eEuj/?p=12"
bvid: "BV11LEA6eEuj"
cid: "39163203344"
author: "常青藤名校课"
upload_date: "2026-06-05"
created: 2026-08-17
tags: ["bilibili", "clippings"]
---

## 简介

如果这个中文配音版对您理解 CS336 有帮助，欢迎点赞、投币、收藏支持一下，也可以点个关注。
我会持续更新更多 Stanford / AI / 大模型相关课程的中文配音课程，尽量帮大家用更低的理解成本看懂这些高质量课程。你的支持会让我更有动力把后续内容继续做完、做好。

## 字幕

`00:00` 那今天我们要讲的是评估到目前为止
`00:04` 这门课里啊
`00:06` 训练一个language model需要做的我们都已经做过了啊
`00:10` 我们定义了架构优化器
`00:12` 还有主训练循环
`00:14` 我们讲了怎么用kernel paralism
`00:17` 让训练跑得更快
`00:18` 然后还看了skiing laws go啊
`00:22` 我们还稍微看了一点
`00:23` 怎么让infancy快起来
`00:25` 所以现在唯一还没讲的就是用来训练的数据
`00:28` 我们下周会专门花时间讲
`00:30` 这个数据会塑造模型的行为
`00:33` 如果你用代码来训练
`00:35` 那你的模型多半就会擅长处理代码
`00:38` 如果你只用DNA序列来训练
`00:40` 它
`00:40` 估计连英语都不会说
`00:42` 不过在讲数据之前
`00:44` 我们得先说清楚
`00:45` 我们希望模型具备什么样的行为
`00:47` 而这正是评估要讨论的主题
`00:50` 所以评估其实就在问一个很简单的问题
`00:54` 咱们训练好的这个模型到底有多好
`00:57` 那到底什么叫好呢
`00:59` 乍一看评估好像就是个挺机械的流程
`01:02` 你定好一些prompt发给模型
`01:05` 拿到回复
`01:06` 然后算算准确率
`01:07` 那为啥我们要花整整一节课来讲评估
`01:10` 其实评估是个很深奥的话题
`01:12` 而且特别关键
`01:13` 因为正式评估在塑造AI的发展
`01:18` 不管是做开源模型还是闭源模型的开发者
`01:21` 大家都盯着它看
`01:23` 它就像是衡量我们进步的一个尺子
`01:26` 所以你认真琢磨怎么评估
`01:29` 其实就是在无形中决定你的模型
`01:31` 将来能做成什么样
`01:33` 那评估难在哪儿呢
`01:35` 通常一开始你脑子里只有一个比较抽象的想法
`01:38` 我到底想让模型干啥
`01:40` 比如我可能希望我的模型
`01:42` 在对话或推理方面表现出色
`01:45` 好的这些都是抽象概念
`01:47` 而评估的过程
`01:48` 其实就是把这些抽象概念转化为具体指标
`01:52` 这通常要依靠一些具体的prompt或环境来实现
`01:56` 所以呃这就是我们要面对的核心挑战
`02:00` 接下来我们会看到很多例子
`02:02` 展示这种从抽象概念到具体指标的转化过程
`02:06` 是怎么发生的
`02:08` 那么让我们回到什么样的模型才算好
`02:11` 这个问题上
`02:12` 表面上看
`02:13` 如果一个模型在基准测试上表现不错
`02:17` 那它可能就算是个好模型啊
`02:19` 这里有一个网站叫artificial analysis
`02:21` 它现在基本上成了衡量模型
`02:23` 所谓智能啊水平的一个标准
`02:26` 所以如果你打开artificial analysis diai
`02:29` 就能看到
`02:30` 从GBT法电网往下排列的各种模型排名
`02:32` 其实在这之后还有很多模型呢
`02:34` 总之这是一个衡量标准
`02:37` 它包含了许多不同的数据集啊
`02:40` 其中一些我们后面会讲到
`02:42` 又或者如果一个模型在基准测试中表现优异
`02:45` 同时运行成本低
`02:47` 那它就算好模型
`02:48` 毕竟成本是个重要考量
`02:50` 就这张图展示了之前的intelligence index
`02:54` 以及运行该模型所需的费用
`02:57` 这是基于inference cost运行特定模型的情况啊
`03:01` 你可以看到
`03:01` 正如预期的那样
`03:03` 这两者确实存在相关性
`03:04` 运行花费越高
`03:06` intelligence index通常也越高
`03:08` 但两者并非完全对应
`03:10` 又或者如果人们只是单纯更喜欢某个模型
`03:14` 那它就算好模型嗯
`03:16` 稍后我们会深入探讨
`03:18` 这就是arena AI前身为chat out arena所持的观点
`03:21` 我们可以根据人们是否喜欢某个模型
`03:24` 来对模型进行排名
`03:27` 又或者如果人们选择使用某个模型并为此付费
`03:31` 那它就算好模型
`03:33` 所以open router就是这样一种服务
`03:36` 让你能通过一个统一的端点
`03:39` 来调用多种不同的模型
`03:42` 这样一来
`03:43` 他们就收集了大量
`03:44` 关于大家都在用哪些模型的统计数据
`03:47` 他们还把这些数据公开了
`03:49` 看看通过open brother用的最多的那些模型
`03:53` 是挺有意思的
`03:54` 当然这并不能代表所有模型的使用情况
`03:58` 但它能让你大致了解
`04:00` open router上的用户都在用什么
`04:02` 所以这更像是一种经济学视角
`04:05` 我不知道什么才算好
`04:06` 但如果有人愿意为他付费
`04:08` 那他一定不错
`04:09` 好咱们接着往下说
`04:12` 这些答案未必都对
`04:13` 我也不确定到底有没有标准答案
`04:16` 我只是想让你换个角度思考一下
`04:18` 究竟什么才算好
`04:20` 那咱们现在就开始来看看
`04:22` 评估language model有哪些不同的具体的方法
`04:26` 要开始的话
`04:27` 没有什么比回到language model的核心思想更合适了
`04:31` 简单来说它就是在token序列上的一个分布
`04:35` PX从这个角度来看
`04:37` 评估分布有一个很自然的方法
`04:39` 比如困惑度
`04:40` 自然或者对数损失
`04:42` 这些概念其实都挺相关的
`04:45` 基本上就是说我拿个测试数据集
`04:47` 第一看看我的language model给他分配了多少概率质量
`04:51` 然后我做了归一化和除法
`04:53` 这样数值就更容易理解了
`04:55` 但核心问题就是我的概率
`04:57` language model到底给这个数据集分配了多少概率
`04:59` 质量好的
`05:00` 这挺自然的
`05:01` 实际上我们在训练时
`05:03` 最小化的就是训练集上的困惑度
`05:06` 好的那最直观的做法就是在测试集上测控活度
`05:11` 这也是过去很多年里
`05:12` 语言建模研究里大家传统上都在做的事
`05:15` 在整个2000年代
`05:17` 你随便拿篇语言建模的论文来看
`05:20` 做的基本都是这件事
`05:21` 一那时候呃常用的数据集有pantry bank和wiki
`05:25` Text one of three
`05:26` 还有个10亿词规模的基准
`05:28` 它的数据是从别处采集来的
`05:30` 机器翻译的原数据
`05:32` 这全都属于经典范式
`05:33` 也就是做分布内评估
`05:35` 你用数据集的训练部分来训练模型
`05:38` 再用测试部分来评估效果
`05:40` 大家确实进步挺大
`05:42` 不过主要就看困惑度降了多少
`05:44` 嗯比如twin sixteen年有篇很有名的论文
`05:47` 他证明了把CNN和LSTM用到这个实义词基准上
`05:52` 困惑度能大幅降低
`05:55` 呃记住那时候人们还在聊angry模型
`05:58` 可能还有那种混合模型
`05:59` 这是第一个决定性的结果
`06:01` 证明纯神经网络这条路显然才是正确方向
`06:04` 那时候挺简单的
`06:06` 你在训练集上训练
`06:07` 在测试集上看困惑度嗯
`06:10` two thousand nineteen年
`06:12` OpenAI推出g b d two的时候
`06:14` 他们用了个叫web text的数据集
`06:16` 就是从REDIS上链接过来的40GB网页文本
`06:20` 不过他们的评估方式其实不太一样
`06:22` 他们在别人都在用的标准数据集上
`06:25` 做了零样本评估啊
`06:27` 这属于分布外评估啊
`06:29` 这是他们论文里的一张表
`06:30` 展示了他们训练不同模型的效果
`06:32` 最大的那个模型有15亿参数
`06:35` 他们取得了相当明显的提升
`06:38` 特别是在那些呃小数据集上
`06:41` 我啊举个例子
`06:42` PDP是个很小的数据集
`06:44` 跟save2比起来
`06:45` 它的困惑度是35%
`06:47` 现在如果你手头有足够多的分布内数据效果
`06:52` 其实还不如那些最先进的方法
`06:55` 不过话说回来
`06:56` 这个模型压根没在10亿
`06:57` 此规模的数据集上训练过
`06:59` 能做到这样已经挺厉害
`07:00` 也许有点数据重叠吧
`07:02` 我也说不准
`07:02` 毕竟我不确定他们有没有认真做过
`07:04` 训练集和测试集的去无BUTION
`07:07` 所以人也开启了一种看待language model的新范式
`07:12` 不再只盯着分布内的设定
`07:15` 而是我们想要在大数据集上训练
`07:19` 然后在标准基准上评估
`07:21` 这在今天我觉得早就成了标配
`07:24` 但在当时还被看作挺新鲜的做法
`07:27` 谢谢那嗯
`07:30` 既然咱们聊到困惑度
`07:31` 我还想提一点
`07:32` 你需要的其实就是困惑度啊
`07:35` 这话别太当真
`07:36` 不过我觉得这确实是推动许多language
`07:40` model研究的一种思维模式
`07:42` 基本逻辑是这样的
`07:44` 假设存在一个真实分布
`07:46` 我们叫它T而你正在训练模型P
`07:50` 那么你能获得的最佳困惑度是多少
`07:53` 嗯最佳困惑度就是那个真实分布的熵
`07:57` 只有当P与T完全一致时
`08:00` 才能达到这个值
`08:01` 所以啊通过最小化困惑度
`08:05` 你本质上是在不断压低困惑度
`08:08` 而唯一的极小值点就出现在
`08:11` 你真正得到真实分布的时候
`08:13` 一旦掌握了真实分布
`08:15` 你就通关了
`08:16` 你只需对万事万物建模
`08:18` 就能解决所有问题
`08:20` 比如根据问题生成解法
`08:21` 或者根据提问给出答案
`08:23` 所以只要不断压低困惑度
`08:26` 我们最终就能实现通用人工智能
`08:28` 这就是这个论点
`08:30` 说实话
`08:31` 这听起来可能有点不切实际
`08:34` 但在我看来
`08:36` 这确实是推动许多人不断扩展language model的核心动力
`08:41` 尤其是在那样一个时期里
`08:43` 效果提升没那么显著
`08:46` 在GPC出现之前
`08:47` 大家其实并不确定
`08:49` 语言模型能带来这么大的影响
`08:52` 当时大家坚信只要把困惑度降下来
`08:56` 好事自然会发生哦
`08:59` 所以当然困惑度可能并不是全部嗯
`09:02` 换种说法
`09:03` 困惑度提供的信息可能超出了你的实际需求
`09:06` 打个比方
`09:07` 如果句子是斯坦福大学
`09:09` 成立于1885年
`09:12` 困惑度会对所有token的预测结果都进行惩罚
`09:16` 具体来说他会去计算在已知其他词的情况下
`09:20` 一般I85法出现的概率
`09:22` 这个token其实挺有用的
`09:24` 它本质上就是一个问答示例
`09:26` 只是伪装成了句子补全的形式
`09:28` 所以它能捕捉到关于世界的知识
`09:31` 不过像句首词或者像founded这种词
`09:35` 我觉得就没那么有意思了
`09:37` 但perplexity可不管这些
`09:40` 只要你的输出偏离了真实分布
`09:43` 它就会按每一个bit的偏差向你计费
`09:46` 所以这里有个解决办法
`09:48` 就是你可以去测量条件
`09:49` Perplexity
`09:50` 你可以给定一个prompt作为条件
`09:53` 然后去测剩下的response部分的perplexity
`09:56` 这样一来
`09:56` 你大体上就可以把重点放在
`09:59` 你认为相关的某些token上
`10:01` 而少去关注那些偶然出现的TOS
`10:04` 在the thing外还得记住一点
`10:05` 有些benchmarks虽然表面上看并不是明确的说
`10:10` perplexity度量
`10:11` 其实只是伪装起来的perplexity罢了
`10:14` And feea
`10:15` 想想那些封闭任务
`10:16` 有一篇托尼sixteen年的网吧的论文
`10:18` 基本就是做填空题
`10:20` 你有了上下文
`10:21` 也有了句子
`10:22` 你真觉得我会让你留个空白吗
`10:24` 啊我们的目标是猜出这个词
`10:27` 虽然评估标准是准确率
`10:29` 但它本质上是一个next token prediction问题
`10:32` 这里指的并不是所有词的perplexity
`10:34` 而是经过精心挑选的词
`10:36` 如果你读过这篇论文
`10:38` 就发现
`10:39` 他特意挑选了那些必须依赖大量长上下文
`10:43` 才能推断出来的词
`10:45` 所以早期的gt论文之所以特别看重这一点
`10:49` 是因为他们对此很感兴趣
`10:51` 也就是long context moeling
`10:53` 他们认为这是解锁推理等一切能力的关键
`10:56` 而且正是通过聚焦
`10:59` 那些必须借助长距离才能确定的位置
`11:02` 也就是依赖关系
`11:04` 用这种方式把perplexity
`11:06` 聚焦到你真正关心的某个现象上
`11:08` 咱们再看另一个在language model评估里
`11:10` 很经典的热门数据集
`11:11` 叫hello swag
`11:12` 那HELLWIG到底是什么
`11:14` 它是从各种不同的数据集和来源里整合出来的
`11:18` 形式是多项选择题
`11:20` 都说白了它其实就是句子补全
`11:22` 某种程度上也是困惑度
`11:24` Perplexity
`11:25` 比如这句话
`11:25` 一个女人带着水桶和狗在户外
`11:28` 狗在四处乱跑
`11:29` 她怎样怎样
`11:30` 然后你要从选项里挑出
`11:32` 最能把这句话补全的那一句
`11:34` 所以它严格来说不是困惑度
`11:37` 而是道选择题
`11:39` 但某种程度上也算是困惑度
`11:41` 谢谢这里得提醒一句
`11:44` 要是你认定困惑度就是真正的衡量标准
`11:47` 还打算搞个排行榜让大家来比啊
`11:50` 比的就是困惑度
`11:52` 也就提醒这么一句
`11:53` 那基于困惑度的排行榜会长什么样呢
`11:56` rob大家会把language model提交给你
`11:59` 然后你得去算log prop对数概率
`12:02` 你把测试数据发过去
`12:04` 他们返回一个对数概率
`12:05` 你一看心想嗯
`12:07` 这看着有点不对劲
`12:08` 可能是有点不对劲
`12:09` 嗯有点可疑啊
`12:11` 因为你需要信任他们
`12:12` 返回给你的概率是有效的
`12:14` 概率之和必须为一
`12:16` 不然的话我只要实现一个语言模型
`12:18` 让它永远返回一
`12:19` 或者说对数概率为零
`12:21` 我会得到非常好的困惑度
`12:24` 但显然这不是一个有效的分布ability
`12:27` 所以有了这个契约
`12:28` 很难检查对方内部是否真的计算了对数概率
`12:32` 所以这里涉及一些信任问题
`12:35` 也许你得去审查代码什么的
`12:37` 不过对于下游任务
`12:38` 这就直接多了
`12:39` 因为你可以把它当作黑盒语言模型来用
`12:41` 我给你一个prompt
`12:42` 你给我一个回复
`12:43` 我直接用那个回复算一下准确率就行
`12:46` 所以困惑度在根本上是不同的
`12:48` 因为它处理的是分布
`12:50` 你得稍微小心一点
`12:52` 另外自回归模型在这方面的问题相对较小
`12:55` 但如果你用的是变分自编码器
`12:58` 或者有些模型连分布都算不出来
`13:00` 只能算出一个边界
`13:01` 那你就得真正相信数学推导
`13:04` 确认这个边界确实有效
`13:06` 谢谢好
`13:07` 总结一下困惑度
`13:09` 它在language model开发中其实仍然被大量使用啊
`13:13` 它有一些不错的特性
`13:14` 咱们之前看到它一般用来做scaling laws
`13:17` 因为它随规模平滑变化
`13:19` 这一点对推导skinning loss很重要
`13:22` 但说到底
`13:22` 尤其是如果你不信这一套的话
`13:25` 那你就得靠一些基准测试来反映真实场景
`13:29` 好让大家相信你的language moo确实不错
`13:32` 对于信徒
`13:33` 而言
`13:33` 要是我拿出一个低得惊人的困惑度
`13:37` 他们就会幸福好
`13:39` 关于困惑度还有什么问题吗
`13:41` 谢谢谢谢哈好了
`13:44` 希望现在大家都幸福了
`13:46` 嗯好咱们来聊聊考试基准啊
`13:50` 考试嘛就像你们这些学生都知道的
`13:53` 是我们用来测试人类的一种方式
`13:55` 所以咱们也可以用同样的思路来测试language model啊
`14:00` 考试的好处在于
`14:01` 你能对考什么有多难进行非常严格的把控
`14:05` 你可以设计出有明确正确答案的题目
`14:08` 这样评分起来就很简单
`14:10` 所以考试有很多优点
`14:12` 正因如此
`14:13` 很多language model的基准测试文化
`14:16` 都是基于这个理念建立的啊
`14:18` Have eye
`14:19` 所以大家知道啊
`14:20` 现在肯定有不少基准测试了
`14:23` 不过我就挑几个我觉得比较有代表性的
`14:25` 简单说说嗯
`14:26` 比如这个呃m mu全称是massive multitaslanguage
`14:30` Understanding
`14:31` 出自HENRICKS等人二长零团一年的研究
`14:34` 那时候大家还不太确定
`14:36` language猫到底能不能充当通用的任务求解器
`14:39` 嗯这大概是gp d three
`14:41` 刚出来的时候
`14:42` 我想很多人那会儿还是觉得language model嘛
`14:45` 无非就是掌握语言知识
`14:47` 能生成通顺的英语
`14:49` 但他们当时有个想法说
`14:51` 咱们不如试着走在前面抢占先机
`14:53` 我们要涵盖fifty个不同领域的七个主题
`14:56` 还要组织一对学生去网上搜集各种来源的资料
`15:00` 真正搭建起这套非常全面的刷剧集啊
`15:05` 虽然m mu这个名字里带着语言理解
`15:07` 但它真正考察的其实是知识和推理能力
`15:10` 都它并不只是纯粹的语言理解
`15:14` 他们评估他的方式是利用g b d three rain
`15:17` 进行future p prompting
`15:19` 是要ky araround the house
`15:22` 所以他们当时基本就是在说我做到了民科
`15:27` 怎么构建这样一个prompt
`15:29` 这里有一些高中数学题
`15:31` 前面先给几个带上下文的问答示例
`15:34` 最后再出一道题
`15:36` 让模型来预测答案
`15:37` 比如这事儿现在看挺平常的
`15:40` 但在当时我觉得挺激进的
`15:43` 咱们构造这么个相当复杂的题目
`15:45` 塞进上下文示例之类的
`15:47` 还指望language model真的能做出合理的反应
`15:50` 果然一开始模型还做不到嗯
`15:53` 我会这么说
`15:55` 你知道有些小模型勉强比随机猜测好一点
`15:59` 但那个大模型也就是GPT3
`16:02` 表现确实远超随机水平
`16:04` 所以这在当时还挺让人受鼓舞的
`16:07` 然后这些年过去啊
`16:09` 就像咱们知道的这个基准测试
`16:13` 基本上已经饱和了
`16:15` 或者是这个
`16:16` 所以这就是那个网站
`16:17` 我不确定里面是不是全部数据
`16:19` 但大概是3.5TURB
`16:22` 这就像twenty twenty年
`16:23` 我才是twenty twenty four年哦
`16:26` 其实是twenty twenty3年嗯
`16:28` 到了gp four
`16:29` 然后现在你知道分数都到90多分了
`16:35` 另外还有个链接
`16:36` 你之后可以点开浏览这些模型的一些预测
`16:40` 看看他们到底在哪儿出错哪
`16:42` 答对了
`16:42` 挺有意思的
`16:44` 谢谢好
`16:45` 大概在twenty twenty four年
`16:47` EMMU看起来进步很大
`16:49` 那时候大家就觉得行这个套路
`16:51` 你后面还会再看到好
`16:53` 这指标现在太简单了
`16:54` 咱们得让它难一点嗯
`16:56` 首先他们发现有些题噪声大又琐碎
`17:00` 那就把这些去掉嗯
`17:01` 四个选项可能太简单了
`17:03` 那就改成十个选项吧
`17:04` 那时候大家用思维链来解决
`17:07` 这些基准测试已经很普遍了
`17:09` 而gbt three刚出来时还没这回事
`17:11` 很明显有些题目用几部推理来解会更好
`17:14` 所以这个新基准更难了
`17:16` 现在模型的准确率又回到了33%
`17:19` 不过从目前的进展
`17:21` 你们大概能看出来
`17:22` 他起初分数很低
`17:24` 现在又回到了
`17:25` 我猜是嗯
`17:26` 大概是ADA快哪去吧
`17:29` 行那这个基准测试可能有点太简单了好
`17:34` 那咱们再看另一个叫GPQA的基准测试
`17:37` 全称是google proof q a啊
`17:40` 那这里的核心思路是
`17:42` 咱们怎么才能搞出一个真正好的基准测试呢
`17:47` 思路是这样的
`17:48` 如果你搜一下google就能解题
`17:52` 而language model又是基于全网数据训练的
`17:54` 所以要是你能直接解出来
`17:57` 那这题大概就太容易了
`17:58` 所以他们现在搞了些博士水平的题目二
`18:01` 我觉得MMU之前大概是
`18:05` 其实有些题可能是本科水平
`18:07` 也许是研究生水平
`18:08` 但现在更明确的指向那些经过刻意设计的题目
`18:11` 难的题目
`18:13` 到了这一步
`18:14` 你可能会问
`18:14` 那我该怎么设计出真正有挑战性的题目呢
`18:19` 要拿到真正有难度的题目
`18:20` 其实得投入大量的人力
`18:22` 比如我们雇了UPWORK上的sixty one way
`18:25` 博士来做合同工啊
`18:27` 有人出题
`18:28` 经过专家审核并给出反馈
`18:30` 出题人据此修改
`18:32` 再由另一位专家来复合这个问题
`18:35` 接着你把题目发给一些拥有google访问权限的
`18:38` 非专家
`18:39` 看看他们能不能解出来啊等等等
`18:42` 所以这里有一类子集叫钻石级
`18:45` 你也许会在别人的排行榜里看到
`18:48` 如果两位专家意见一致
`18:51` 且最多只有一位非专家能答对
`18:53` 这就被认定为一道合格的钻石题
`18:56` 所以这套审核流程其实相当严谨
`19:00` 但连博士专家也才拿到四十百%的分数啊
`19:04` 我想这主要是四选一的多选题
`19:07` 虽然比瞎猜强
`19:08` 但说实话
`19:09` 考试考65%可不算什么好成绩
`19:12` 非专家就算有third分钟还能用google搜
`19:15` 表现也就比瞎猜强一点点多
`19:18` 不了多少
`19:18` 那时候g b t four才30
`19:20` 讲percent左右
`19:21` 咱们来看看他现在怎么样嗯
`19:24` Ninety four
`19:25` 所以GBQA现在又到94%了
`19:29` 现在男人four percent
`19:31` 所以说这些基准测试都是有保质期的
`19:34` 我觉得不会太高啊
`19:37` 不过我得说
`19:38` 有些基准测试虽然已经把最大的模型测饱和了
`19:42` 但他们对开发小模型
`19:44` 你和scaling laws之类的还是有用的行
`19:48` 那嗯对
`19:51` 当然你怎么知道这些测试被用来训练
`19:55` 而且实际上你的模型是经过代码验证的
`19:59` 对这是个很关键的问题
`20:01` 你们怎么确认这些问题
`20:03` 没被包含在训练数据里呢
`20:05` 呀简单说就是我们没法确定
`20:08` 因为我也不知道训练集里到底有什么
`20:10` 我待会儿再回过头来聊聊数据污染的问题
`20:14` 以及该怎么应对它
`20:15` 很高兴你问了这个问题
`20:17` 不过你们拿到这些数字时
`20:18` 最好还是打个折扣
`20:20` 毕竟这主要是基于信任
`20:21` by谢谢我再补充一句吧
`20:24` 我觉得数据污染这事其实挺微妙的对吧
`20:26` 因为模型大概率不是
`20:28` 直接拿这些数据集来训练的
`20:30` so问题可能源自其他渠道
`20:33` 而这些渠道的数据可能已经被模型学过了
`20:36` 所以数据污染通常不是那种啊
`20:39` 不小心把测试题放进训练集的简单情况
`20:43` 而是一个更隐蔽的过程
`20:45` 还有别的问题吗
`20:47` 行很好
`20:49` 保持批判性思维总是好的好
`20:52` 我要讲的最后一个考试基准啊
`20:55` 有个听起来有点不详的名字
`20:57` 叫人类最后的考试
`20:59` Humanities last exam
`21:01` 这是去年的事
`21:02` 他们说现在这些模型已经变得非常厉害
`21:05` 咱们干脆全力以赴
`21:07` 搞点真正难的东西出来啊
`21:10` 这个测试是多模态的
`21:12` 涵盖很多学科题型
`21:13` 主要是多选
`21:14` 我想还有一些简答呃啊题目是众包收集的
`21:18` 他们出钱激励那些为了奖金的人参与
`21:22` 为了出题
`21:23` 他们也用署名权来激励那些在乎这个的人啊
`21:27` 整个过程经过多轮审查
`21:29` 并用前沿模型进行了筛选
`21:31` 这里有一套完整的流程
`21:33` 对你这个问题还有另一个答案
`21:35` 他们特意保留了一部分未公开的数据集
`21:38` 就是为了确保这些数据不会混进训练里
`21:41` 然后你只能祈祷了
`21:43` 毕竟你还得把这些发给模型
`21:45` 只能希望这些prom最后别被拿去训练
`21:47` 但愿如此吧
`21:48` 啊所以你看基本上每篇数据集论文都长这样
`21:51` 以前的基准测试里
`21:52` 你看模型表现都好得不得了
`21:54` 但在我的数据集上
`21:56` 你看所有模型都差得一塌糊涂
`21:58` 所以这算是个常见套路
`22:00` 这里的分数尤其低
`22:02` 甚至是个位数
`22:02` 表现简直惨不忍睹
`22:04` 嗯咱们来看看到了twenty
`22:07` twenty6年我们表现如何
`22:09` 所以这个还有一些提升空间
`22:12` 所以分数很低
`22:14` 即便是MYTHOS也只有64版七分好
`22:18` 这确实是一道看起来相当难的考题
`22:20` 这所以总结一下
`22:22` 这一节大家可以看到
`22:23` 随着模型变强
`22:24` 现有的基准测试也差不多被刷爆了
`22:27` 我们就开始出更难的问题
`22:28` 有意思的是
`22:30` 多选题这种形式居然还活着
`22:33` 因为用多选题的话
`22:35` 你想出多难的题都行
`22:37` 嗯所以有人觉得多选题太简单了
`22:40` 嗯我是说我完全可以给你出一道很难的多选题
`22:44` 这倒不是重点
`22:46` 我觉得重点在于多选题限制了你能问的问题
`22:50` 范围
`22:51` 正在难度上
`22:52` 多选题没问题
`22:53` 基于考试的题目
`22:54` 主要问题在于
`22:55` 它没法捕捉到全貌建设里的实际用法啊
`22:59` 除了你在做HOE评估的时候
`23:01` 没人会拿HOE问题去问language model
`23:03` 其实大家问的大多是开放式问题
`23:06` 甚至不一定有标准答案
`23:08` 有时候问题本身都没表述清楚
`23:10` 而现有的基准测试根本抓不住这些情况好
`23:14` 那么这个问题
`23:16` 可那模型输出和标准答案到底怎么比呢
`23:21` 比如在HOE里说到准确率
`23:24` 我们是怎么把模型输出和标准答案做对比的
`23:27` 就是为了算出准确率
`23:29` 那为了算准确率
`23:30` 这标准答案到底是怎么拿来比对的
`23:32` 或者是多选题
`23:34` 那就简单了
`23:35` 看看你是不是选对了
`23:37` 那咱们是去检查每个选项的概率分布
`23:41` 还是说language model会直接生成出像C这样的答案
`23:45` 而我的答案可能是BCA或D中的一个
`23:49` 我只需要检查它们是否一致
`23:51` 我们只生成一个token对
`23:53` 所以与其说是在讨论评估
`23:56` 不如说是在问模型到底是怎么生成答案的
`24:00` 偶尔对这里有几种做法
`24:02` 一种就是字面意义上的生成
`24:04` 你以采样它生成一个字母或者更多
`24:08` 现在更常见的做法是生成思维链
`24:10` 也就是先生成推理过程再给出答案
`24:13` 有时还会附带一些解释
`24:15` 不过提取答案的方式确实很关键
`24:19` language model的评估器对这点可能很敏感
`24:22` 不过我这里就不展开讲了
`24:23` 谢谢好
`24:25` 接下来我们聊聊另一类完全不同的基准测试
`24:29` 也就是聊天基准测试
`24:31` 到目前为止我们聊的都是多项选择题任务啊
`24:35` 这类任务在衡量某种智力水平和难度方面
`24:39` 依然相当有效
`24:40` 但大多数人不会拿多项选择题去考AI助手
`24:43` 除非是想让AI替自己考试
`24:46` 这种事没人会真这么做好
`24:48` 咱们来看个例子
`24:49` 这个灵感部分来自一个真实的使用场景
`24:53` 比如我想做个甜菜根配奶酪的沙拉
`24:55` 你觉得哪些香草搭起来好
`24:58` 哪些不好
`24:59` 这是个很开放的问题
`25:00` 得到的回答往往也是开放的
`25:03` 对
`25:03` 那怎么评估这个回答呢
`25:05` 不能光靠看他是不是和标准答案一字不差
`25:08` 毕竟连个标准答案都没有
`25:09` 所以咱们得想几个办法
`25:11` 咱们包挨个细说了
`25:12` 其中一个思路是arena AI提出的
`25:15` 嗯啊呃思路很简单
`25:17` 就是问人好
`25:19` 他们问人的方式挺巧妙的
`25:22` Basically
`25:22` 就是搞了个网站
`25:24` 让网上随便什么人都能进去聊聊天
`25:26` 然后不像普通助手那样只给一个回复
`25:30` 你会同时拿到两个匿名回复
`25:33` 分别来自两个不同的模型
`25:36` 接着由人来打分
`25:38` 看哪个更好
`25:39` 比如你输入提示词
`25:40` 拿到助手A和助手B的回复后
`25:43` 你需要判断A更好
`25:45` 两者都好
`25:46` 两者都差
`25:47` 还是B更好
`25:49` 好通过这个过程
`25:50` 你就能收集到大量成对比较的数据
`25:53` 比如对比模型A和模型B看哪个更优
`25:57` 然后你可以用这些数据来计算yellow排名不
`26:01` 其实IYCORPON使用for totype viwing
`26:03` The probability of oppremi
`26:06` Watch kidding
`26:07` 大家对这个是否熟悉
`26:09` 但为了让大家有个共同的认识
`26:11` 在U排名体系里
`26:13` 评级模型被定义为这样一个概率
`26:15` 即某个模型击败另一个模型的概率
`26:19` 是
`26:20` 模型B和模型A这两个yellow分数的某种函数
`26:24` 也就是说模型A的yellow分数越高
`26:27` 它获胜的概率就越大
`26:29` 这是一个平滑的映射关系
`26:31` 然后你再来拟合这个模型
`26:33` 这里的参数就是各个一楼评级
`26:36` 我们通过调整它们来最大化成对比较的概率
`26:40` 好啦
`26:41` 这样一来
`26:41` 你基本上就能为每个模型得到一个评级
`26:44` 谢谢好的
`26:46` 这就是CHAPEARENA如今成为arena AI的做法
`26:50` 这样就能得到一份相当不错的排名啊
`26:53` 我们觉得COPUS在这里表现挺不错的嗯
`26:58` 不过列表里还有其他一些模型好了
`27:02` 那么这套做法有几个优点
`27:04` 第一他用的是真实场景下的prompt
`27:06` 也就是说任何人都可以访问这个网站
`27:09` 大家愿意来这个网站是因为能免费使用language model
`27:13` 所以这里其实有个隐含的前提
`27:15` 这些人是真的想拿它来做点有用的事
`27:18` 那么所以啊
`27:19` 你拿到的往往就是那种真实场景下的prompt
`27:24` 不过有一件事你得留意
`27:26` 你知道这些使用者到底是谁吗
`27:30` 打个比方
`27:31` 那些在网上随便刷到l a marina的用户啊
`27:35` 我真不知道他们是个什么分布
`27:36` 我觉得他们论文里倒是列了一些用户画像数据
`27:40` 但这并不能说明全部情况
`27:42` 你总得担心不同模型带来的各种偏差啊
`27:46` 比如那些发垃圾信息的
`27:48` 或者有人想故意刷分的啊
`27:50` 甚至可能有人自己提交了模型
`27:52` 就想让自己家的模型看起来更出彩啊
`27:55` 反正你根本摸不清底细
`27:56` 这地方有点像法外之地
`27:58` 这里还有另一个问题
`27:59` 就是二元偏好
`28:01` 也就是判断A和B哪个更好
`28:04` 这种方式很直观简单
`28:06` 也能用来打分
`28:07` 但他确实会把风格和正确性混为一谈
`28:10` 所以国际象棋的一楼评分很合理
`28:13` 因为唯一重要的是你赢没赢
`28:15` 但哪个更好
`28:16` 就没那么非黑即白了
`28:18` 另外这里还有个问题
`28:20` 就是由人类来当裁判
`28:23` 输入prop的人
`28:24` 其实是在回答这个问题
`28:26` 这挺好的
`28:27` 因为从某种程度上说
`28:28` 这个人有自己的意图
`28:30` 他可以判断自己的意图有没有被满足
`28:32` 而在某种程度上是这样
`28:35` 只不过他们之所以提问
`28:37` 大概是因为他们自己也不知道答案
`28:39` 所以如果我给你两个答案
`28:42` 他们又怎么判断哪个才是对的呢
`28:44` 还有个问题是阿谀奉承
`28:46` 也就是说比起正确但诚实的回答
`28:49` 那些更讨喜的回答啊
`28:52` 可能会被给予更高的权重
`28:55` 所以这里有不少潜在问题
`28:58` 不过这种方法的好处是
`29:00` 你不用给所有模型为一样的提示词
`29:04` 这就是yellow评分法的妙处对吧
`29:06` 下棋时你不用跟每个人都对弈
`29:08` 而是大家互相之间对弈
`29:10` 它可以只是其中一个系数的采样
`29:13` 只要这个图是连通的
`29:14` 你照样能推导出排名
`29:16` 这很重要
`29:17` 毕竟是人类在打分
`29:18` 你不可能指望一个人去评所有模型
`29:21` 甚至连超过两个都难
`29:23` 还有一个好处是
`29:24` 随着新的提示词和新模型不断加入
`29:27` 这套机制
`29:28` 能很自然地随时间更新排名嗯
`29:31` 好那咱们来聊聊另一种不同的方法嗯
`29:34` 这是twenty twenty three年的apark evolve啊
`29:37` 这些指令是从各种不同的来源整理出来的
`29:41` 那这里的指标就是说
`29:44` 也就是跟机械模型比的胜率
`29:47` 比如你有个模型
`29:49` APARK1到会拿一个提示词让它生成回答
`29:52` 假设机械模型是当时的gbt four preview
`29:56` 然后用gpd four preview来当裁判
`29:58` 看看哪个回答更好
`30:00` 这马上就会让人想到潜在的偏差问题
`30:03` 不过咱们先放一边
`30:04` 所以这其实就是让language model来当裁判
`30:08` 在现在评估语言模型时非常流行啊
`30:12` 你可以通过用多个裁判做集成之类的办法
`30:17` 来缓解这种偏差
`30:19` 所以早期IPAD表有个问题
`30:22` 就是大语言模型
`30:23` 裁判更偏好长回答
`30:25` 这就引来了一堆刷排行榜的操作
`30:29` 不少微调过的模型提交上去
`30:31` 光靠回答长奥派克医疗分数就特别高啊
`30:36` 后来有一篇论文解决了这个问题
`30:38` 他们用了一种很简单的回归方法
`30:40` 去掉了这个指标的偏差
`30:42` 好不过这里其实引出了一个更普遍的问题
`30:45` 这不只是l park Eva的问题
`30:47` 咱们得想想到底该怎么去评估一个指标
`30:50` 好不好啊啊咱们一直在聊怎么评估模型
`30:53` 要是我手里有个指标
`30:55` 我就能用它来评估模型
`30:57` 但要是我提出了一个新指标
`30:59` 我怎么知道它好不好呢
`31:01` 这是个挺难的问题
`31:03` 这儿可没有现成的答案
`31:06` 你可以做个简单的合理性检查
`31:08` 看看它跟其他指标的相关性怎么样
`31:12` 这里的想法是数值越高并不一定就越好啊
`31:17` 你看APARKEVO和CHARINA的相关性
`31:20` 高达0.9败
`31:21` 这数值相当高
`31:23` 嗯也也就是说你是可以的
`31:26` 比方说假如你确实想这么做的话
`31:28` 比如你想在chapout arena上跑分
`31:32` 但你可能不好意思把自己的模型放上去
`31:34` 或者不想干
`31:35` 等人类标注
`31:36` 这时候你就可以用apark evil来代替
`31:39` 不过要注意
`31:40` 这个相关性是基于当时那批模型得出的
`31:44` 所以如果模型比gt for preview更强
`31:46` 这个结论可能就不适用了
`31:48` 谢谢好
`31:49` 说到这个排行榜
`31:51` 我觉得他现在应该已经停更超过一年了
`31:54` 不过这是他当时的样子啊
`31:56` 好还有另一个基准测试
`31:58` 我要讲讲叫wild bench
`32:00` 这个是这样的
`32:01` 它是从人类和聊天机器人的对话记录里
`32:04` 收集了一大堆例子
`32:05` 跟CHARA一样
`32:07` 他们也搞了个免费服务
`32:09` 让大家跟机器人聊天
`32:10` 然后把那些提问都收集起来了
`32:12` 再比如APARKEVO
`32:14` 他们直接用语言模型来当裁判啊
`32:18` 为了做评估
`32:20` 这里最大的创新是用了一份检查清单
`32:24` 这份清单是专门针对某个具体任务生成的
`32:28` 提示或任务
`32:30` 这么做的思路是
`32:32` 如果你只让语言模型去判断某个回答好不好哦
`32:37` 这在某种程度上是个定义很模糊的任务
`32:39` 我也说不准
`32:40` 这得看你在乎什么
`32:42` 所以思路是有了检查清单或者某种评分标准
`32:45` 就能大大缩小范围
`32:47` 让评估任务更清晰
`32:49` 他们就是这么做的
`32:50` 另外他们还展示了与chat boerra的相关性
`32:53` 说实话这一点是有点微妙的
`32:56` 有点循环论证
`32:57` 因为接着你就会问你
`32:58` chap2RENA真的就是那个标准答案吗
`33:01` 嗯但至少咱们都在同一条船上好
`33:04` 那么这一节就总结到这里嗯
`33:06` 咱们怎么评估开放式回答呢
`33:09` 嗯你知道这方面没有什么明确的解决办法
`33:11` 但有一些思路深
`33:14` 并且呢做对比通常是个好办法
`33:16` 把参考答案和模型的回答放一起比
`33:19` 因为尤其是对于很相似的回答
`33:21` 这样能给出方向性的判断
`33:24` 嗯这个比那个稍微好一点
`33:26` 但要是论绝对评分
`33:27` 比如这到底是十分里的七分还是八分
`33:30` 这种判断的参考价值往往就低多了
`33:32` 嗯咱们得时刻留意偏见问题
`33:36` 不管评判者是人类还是语言模型
`33:39` 两者的偏见啊各不相同
`33:42` 但不管怎样
`33:43` 嗯这一点很重要
`33:45` 得留意这一点
`33:46` 也许你唯一能做的就是找多个评判者来评估
`33:50` 不管是真人还是模型评判者
`33:53` 如果大家都说你的模型更好
`33:56` 那也许你的模型确实更好
`33:58` 最后我想强调一下
`33:59` 开放式回答的评估问题
`34:01` 其实并没有定义的很清楚
`34:03` 所以我觉得啊现在越来越重要的是
`34:07` 定好评分标准或检查清单
`34:09` 这样能提高实际评估的可靠性
`34:12` 也让它更明确
`34:13` 不管评判者是人类还是裁判
`34:15` 道理都一样
`34:17` 做过众包的人都知道
`34:18` 如果你只是让人打分
`34:20` 却不给一套评分标准
`34:21` 得到的结果很可能非常混乱
`34:23` 没什么意义好
`34:25` 那咱们接着聊聊代理agent这个领域吧
`34:28` 所以你可以这样理解
`34:30` 咱们到目前为止做的其实是在评估language model
`34:34` 说了什么
`34:35` 也就是聊天chat
`34:37` 而现在我们要评估的是language model做了什么
`34:42` 也就是代理agent
`34:43` 所以你知道如果你一直在关注AI热潮
`34:47` 就会发现代理agent无处不在
`34:50` 而且这些我觉得
`34:52` 这肯定会彻底改变我们看待language model的呃方式
`34:57` 所以agent基本上就是language model加上某种脚手架
`35:01` 也就是一套用来决定language model行为方式的逻辑
`35:05` 它会被怎样调用
`35:07` 能用上哪些工具
`35:08` 诸如此类啊
`35:10` 所以我想提几个考察agent能力的benchmark啊
`35:15` 有一个特别火叫SWEBENCH
`35:18` 简单来说
`35:19` 任务就是给你一个代码库和一个github shoe的描述
`35:22` 让你提交一个pr评估
`35:24` pr的标准就是看它能不能通过单元测试
`35:28` 这些测试是这样设计的
`35:30` 在你提交pr之前
`35:32` 有些单元测试是过不了的
`35:34` 你的目标就是修好它
`35:36` 让那些测试通过
`35:37` 同时别把别的地方搞坏
`35:39` N这里有一些指令的例子
`35:42` 就是issue
`35:44` OK了
`35:44` 上下文里的代码
`35:46` 你的目标是生成一个patch
`35:47` 拿到一个patch后
`35:49` 就看你过不过得了嗯
`35:51` 所以这么做有个好处呃
`35:53` 就是评估起来非常直接
`35:55` Three bench
`35:56` 咱们来看看在store bench上做得怎么样啊
`35:59` 其实这次用的是three bench verified
`36:02` 待会我给你们展示一下
`36:03` 顺便说说为什么这事情是这样的
`36:05` S w bench
`36:06` 还有我猜twenty
`36:09` twenty four年的时候才sixteen percent左右
`36:12` 现在都涨到3%了哦
`36:15` 干得漂亮
`36:15` Methods
`36:17` 所以这是一个很典型的generic benchmark
`36:20` 因为大家关注agent的时候
`36:22` 主要都放在编码能力上了
`36:24` 所以so1bench其实开创了一种新思路
`36:27` 用来评估agents在真实环境里写代码的能力
`36:31` 所以这里还有另一个叫terminal bench的项目
`36:34` and这类的思路是它会更通用一些对吧
`36:37` 咱们把环境设定为一个电脑终端
`36:40` 咱们会定义一堆能在终端里完成的任务
`36:44` 就是敲一堆命令
`36:45` 这个环境的好处是特别简单
`36:47` 也很通用
`36:48` 这些任务都是你有从全世界找人众包来的啊
`36:52` 一共找了长西森格
`36:54` 来自世界各地的人嗯
`36:56` 其中eighty nine个任务组成了terminal bench
`36:58` two point neo啊
`36:59` 这些任务一般要花四个小时
`37:01` 你看时间跨度从一小时到一周以上
`37:06` 主要看你是专家还是新手
`37:08` 咱们来看看截至今天排行榜长什么样
`37:13` 所以呃排在最前面的是前沿模型嗯
`37:16` 有意思的一点是
`37:17` agent在这里也很关键啊
`37:20` 你看哪怕用同一个模型
`37:23` 如果有两个不同的agent
`37:25` 他们的准确率也会不一样些
`37:27` 好
`37:28` 接下来我要聊的另一个基准测试是网络安全啊
`37:32` 这里有苏里格夺旗任务
`37:34` 场景是这样的
`37:35` 你有一个agent啊
`37:37` 这个agent会被分配一个环境
`37:39` 它能运行各种命令
`37:41` 比如查看源代码
`37:42` 它还可以执行别的操作
`37:44` 它能访问外服务器
`37:46` 主要目标是入侵该服务器
`37:48` 并提取一个flag
`37:50` 也就是一个能证明你成功入侵的唯一字符串啊
`37:54` 所以这些通常是人类来做的
`37:56` 网络安全竞赛和演习
`37:58` 这是一个例子
`37:59` 给大家看看agent的scaffold大概长什么样啊
`38:03` 这是一个非常简化的版本
`38:05` 基本流程是你有一段连续的记忆
`38:08` 生成某种动作
`38:09` 拿到环境反馈
`38:10` 然后把结果放进这个缓冲区并拼接起来
`38:14` 我待会儿会演示这段历史记录显然会越积越多
`38:17` 所以你需要有更好的方法来管理上下文
`38:20` 好的
`38:21` 这是排行榜刚出来时的样子嗯
`38:24` 最好的模型当时准确率也就10%左右
`38:28` 但随着时间推移
`38:30` 现在这个问题实际上已经完全解决了
`38:32` 那一类基准测试就讲到这里
`38:35` 还有另一类问题涉及ML1工程
`38:38` 这个基准测试包含一堆cargo竞赛
`38:41` 主要涉及数据处理模型训练之类的
`38:44` 智能体可以查看数据集阅读说明
`38:47` 编写代码训练模型
`38:49` 最后你提交结果就能得到一个评分
`38:52` 那么这就是排行榜现在的样子
`38:55` 还是那句话
`38:55` 你会看到language model这边依然是那些老面孔
`38:58` 但不同的智能体脚手架之间差异相当大啊
`39:03` 说到智能体脚手架
`39:05` 我得提一句
`39:06` 这东西非常重要
`39:07` 所以我虽然说的是language model评估
`39:10` 但其实它的范围比language model评估要广得多啊
`39:14` 原因在于
`39:15` 跟我之前提到的那个早期CBENCH智能体脚手架
`39:19` 不一样
`39:19` 现在你得用复杂的多的方法
`39:22` 才能解决真正复杂的任务
`39:24` 大家意识到一点
`39:25` 显示规划很有用
`39:27` 你不能只是用意识流啊
`39:29` 用思维链去处理
`39:30` 然后就这样堆积上下文
`39:32` 那样智能体很容易就搞不清
`39:34` 自己进行到哪一步了
`39:36` 所以咱们列个待办清单
`39:38` 做完一项划掉一项
`39:42` 在调用其他子智能体时
`39:44` 应该先把上下文清理好
`39:46` 子智能体干活后只返回结果
`39:49` 主智能体没必要看那些繁琐的细节
`39:51` 这样做能起到封装的作用
`39:54` 说到记忆
`39:54` 大家一直在试验显示的读写文件
`39:57` 特别是上下文变长的时候
`40:00` 你总不能把所有东西都塞进上下文窗口里吧
`40:04` 最后还有一种上下文工程
`40:07` 侧重于流程管理
`40:09` 什么时候该把活儿派给子智能体
`40:11` 什么时候用这一招
`40:13` 什么时候用
`40:14` 另一招
`40:15` 该往持久记忆里写什么
`40:17` 这些决定通常都不是那么简单啊
`40:20` 他是专门为某个特定的language model优化的好智能体
`40:26` 之所以让人兴奋
`40:27` 是因为他们拓展了language models的能力
`40:30` 边界脚手架非常关键
`40:32` 所以咱们在评估智能体时
`40:35` 其实是在同时评估language model和它的脚手架结构好
`40:40` 咱们接下来换个话题
`40:42` 聊聊这种风格完全不同的另一种基准测试
`40:46` 我管他们叫纯推理
`40:49` 毕竟找不到更好的词
`40:51` 这么做的动机是
`40:52` 之前的所有任务
`40:54` 都依赖于一定的语言和世界知识
`40:56` 所以问题来了
`40:57` 咱们能不能把推理能力
`40:59` 或者说纯粹的流体智力
`41:01` 从知识和事实记忆里单独剥离出来
`41:04` 你可以认为这代表了一种更纯粹的智力形态
`41:08` 比如之前有个项目叫ark a g i
`41:11` 这个项目得从twenty009年说起
`41:13` 当时的目标正是冲着这个去的
`41:16` 所以这任务本来是要让人百分之百能解出来
`41:19` 但对AI来说却很难
`41:21` 每个任务都挺独特的
`41:23` 或者说这就是想达到的效果
`41:25` 所以你要是光靠死记硬背或者做过类似的题
`41:29` 其实帮不了你多少
`41:31` 如果你们还记得language model的发展历史
`41:34` 2019年还是pre alum时代
`41:36` 而我是说那是g p t two的时代
`41:40` 这么看的话
`41:40` 当时还挺有前瞻性的
`41:42` 举个例子
`41:43` 这是这道题最早的一版
`41:44` 给你看几张图
`41:45` 让你猜接下来是什么
`41:47` 你要是花个十秒钟左右
`41:49` 基本上就能解出来好了
`41:51` 大家应该猜到了
`41:52` 这其实就是把黄色填满
`41:54` 拼成一个矩形
`41:56` 嗯好那去年出了个升级版
`41:59` 稍微难一点
`42:00` 嗯嗯大家看这个嗯这些
`42:05` 所以这就是轨迹2K a g i one出来的时候嗯
`42:09` 你们知道的那个thing
`42:12` 我觉得这其实就是零分
`42:14` 基本上模型什么都没做出来
`42:16` 这就跟GBT推一样
`42:17` 预训练模型根本没带来任何提升
`42:20` 对而这完全在出题方的预料之中
`42:23` 预训练模型只是啊
`42:26` 比如说去学互联网上有哪些事实
`42:29` 或者学一些语言规律
`42:31` 但这些其实都没什么直接的帮助
`42:33` 至少对于这些RCHI任务来说是这样
`42:36` 然后到了toy突然4年发生了一件事
`42:39` 对等学说在这个时候
`42:40` OpenAI发布了他们的零一和零三模型
`42:43` 随后局面就开始迅速打开了
`42:46` 压的你相当突然
`42:47` 而现在这就是我们的rk g i one
`42:50` 基本上算是解决了嗯
`42:52` 而且到了twenty twenty five年左右
`42:56` a ark g i two出来之后
`42:58` 他也是这样
`42:59` 虽然还没完全解决
`43:00` 但你看他正一步步走向那一步
`43:03` 嗯最终被彻底解决
`43:06` 嗯被解决
`43:07` 所以真正被解锁的其实就是推理能力X嗯
`43:12` 就这样
`43:14` 现在大家可能会想对这些任务来说
`43:18` 预训练里那些知识真的有必要吗
`43:21` 哦当然要是没有预训练
`43:23` 就不会有今天推理模型的这种爆发啊
`43:26` AK说它依然重要
`43:28` 但在RKGI发展的很长一段时期里
`43:31` 这种重要性并不明显
`43:33` 就在上个月
`43:34` l c a g i3发布了
`43:36` 大家可以看到这种熟悉的模式啊
`43:38` RC的开发者一看心想哎呀
`43:40` 该出新的基准测试了
`43:42` 于是现在的测试变成了一个交互式环境
`43:45` 你可以上网玩这个游戏
`43:47` 同样这里没有语言
`43:48` 你只是试图通过观察模式来找出规律
`43:51` 挺有意思的
`43:52` 不过现在的得分非常低啊
`43:54` 明年我上这门课时啊
`43:56` 肯定得更新这张幻灯片
`43:58` 咱们走着瞧吧
`43:59` 觉得ark g i这条发展线挺有意思的
`44:02` 值得琢磨
`44:02` i believe in the mirojan seventeen米尔
`44:05` 你之上目标是将推理与知识解耦
`44:09` 这真的很难
`44:10` 但你知道我觉得这大概是目前最好的尝试了
`44:14` 目前还不太清楚
`44:15` 是不是真能把这些东西完全解耦啊
`44:18` 当然了
`44:19` 要是大家真在意这个benchmark
`44:21` 我觉得他们多半是能做到的
`44:24` 我觉得基本上什么都能拿来做benchmark
`44:26` 所以你完全可以设计出这类问题
`44:29` 这些还是来自某种鲜艳嗯
`44:33` 而且我也说不准是不是真存在所谓纯粹的推理
`44:38` 这也只限于人类能做的推理
`44:40` 它的明确目标就是在合理时间内
`44:42` 让人100%分之百能做出来
`44:44` 所以这其实没法延伸到超人类的推理上
`44:48` 比如拿到国际数学奥赛IO金牌
`44:51` 或者攻克数学上那些尚未解决的难题
`44:55` 这些可以说仍然非常有用
`44:57` 很重要
`44:58` 但很明显
`44:59` 这暴露了当前模型的一些短板
`45:01` 所以如果你把目标理解成
`45:03` 基本上能解决所有任务
`45:05` 那确实还有不少工作要做
`45:07` Perfea s t i c v
`45:10` 尤其看起来挺图形化的
`45:13` 那么这里面是涉及某种多模态前端
`45:16` 还是说呃它只是一种文本表示对
`45:20` 问题在于这看起来非常图形化
`45:23` 很有画面感
`45:24` 那输入给language model到底是什么呢
`45:27` 我不太确定具体的分辨率
`45:29` 我猜是64×64的
`45:31` 所以你既可以直接提供这张图
`45:34` 也可以用asy art或者某种文本形式来表示
`45:38` 不管用哪种方式
`45:39` 这里都有一种空间属性
`45:42` 是模型必须去推理的
`45:44` Thank you
`45:45` 这真的不算英语或者自然语言吗
`45:48` 谢谢好
`45:49` 那咱们换个话题
`45:50` 聊聊另一类跟安全性有关的基准测试
`45:53` 所以关于安全和language model的讨论已经有一阵子了
`45:58` 那你再想想其他领域
`45:59` 比如汽车啊
`46:01` 这里安全指的是什么
`46:02` 就很清楚了的
`46:04` 汽车有安全评级
`46:06` 这涉及到把车撞向墙壁
`46:08` 测试安全气囊的效果
`46:10` 看它保护的怎么样
`46:11` 保护车里的假人
`46:13` 这是经过几十年游说
`46:15` 才一点点搞清楚车辆安全到底指什么的结果
`46:19` 那这对AI又意味着什么呢
`46:21` 说实话
`46:22` 这里没有现成的好答案
`46:23` 但我可以举些例子说说大家都是怎么想的
`46:26` 就比如有个叫harm bench的基准测试
`46:29` 主要就是用那些有害的内容去prompt模型
`46:33` 期望模型基本上会拒绝回答
`46:35` 这就是一种类型的安全
`46:37` 我觉得大家一提到安全最核心的想法可能是
`46:41` 我想防止那些不怀好意的人利用language model干坏事
`46:45` 但事实证明这并非唯一关键的因素
`46:49` 所以有一项叫AIRBENCH的研究
`46:51` 他试图通过审视欧盟
`46:54` 中国以及美国的公司政策
`46:57` 从整体角度来考量安全性
`46:59` 并建立了一套涵盖所有不同潜在风险点的
`47:03` 分类体系
`47:04` 接着利用这套体系构建一组特定的提示词
`47:08` 并进行评估
`47:09` 以此类推
`47:10` 嗯不过安全性方面有个问题是越狱
`47:14` 这属于一个独立的子问题
`47:17` 也就是说language model通常被训练去拒绝有害指令
`47:21` 但正如大家所知
`47:22` 只要方法得当
`47:24` 就能绕过这一限制啊
`47:26` 因此早期有一些研究采用自动方法来优化
`47:29` 提示词
`47:30` 已绕过啊
`47:32` 使用GCG
`47:33` 它本质上是一种逐坐标优化算法
`47:37` 而且值得注意的是
`47:39` 在开源模型上进行优化
`47:42` 使我们能够将这些效果迁移到其他模型
`47:45` 我希望这些攻击现在不管用了
`47:47` 但在那会儿
`47:48` 咱们确实能生成一堆BASSICALLY胡言乱语的东西
`47:51` 然后一步步规划怎么毁灭人类
`47:54` 而OPENAI的模型居然就照做了
`47:56` 当然你可能会说
`47:57` 这算不算一个毁灭人类的有害计划
`47:59` 但这至少说明当时的模型并没有拒绝
`48:03` 正常情况下他应该拒绝才对好
`48:06` 这就带出了一个更大的问题
`48:09` 到底什么是安全性
`48:11` 让这事儿变得棘手的是
`48:13` 安全性的很多方面都高度依赖具体情境
`48:16` 毕竟卡尔牵扯到政治
`48:18` 法律
`48:19` 还有社会规范嗯
`48:20` 这在不同的国家
`48:21` 各不相同
`48:22` 所以谈到安全性
`48:23` 你就得先搞清楚风险到底有哪些
`48:26` 风险实际上也非常多样
`48:28` 可能需要分别给予不同的关注啊
`48:31` 所以幻觉是个风险
`48:33` 尤其是在医疗法律或者金融这类场景下
`48:36` 不过这其实也跟能力挂钩
`48:38` 打个比方
`48:39` 要是你把模型做得更准
`48:41` 那他就跟少出现幻觉有关
`48:44` 阿谀奉承是个问题啊
`48:46` 教唆犯罪是个问题
`48:48` 但这可能跟能力是反着来的
`48:50` 不平等
`48:51` 或者因为能力提升而丢掉批判性思维
`48:53` 这也算个风险啊
`48:55` 如果你想想啊
`48:56` language model呃对一个世界产生的呃社会影响
`49:00` 再想想
`49:01` 所谓安全就是确保AI能造福于人
`49:05` 这其实是个挺复杂的话题
`49:07` 我时间有限
`49:08` 没法展开细说
`49:10` 最后language model还有个双重用途的问题
`49:13` 网络安全agent既可以用来入侵系统
`49:16` 也可以做渗透测试
`49:17` 让系统更安全
`49:19` 所以讨论他们到底是安风险还是真正有益
`49:23` 这其实是个双刃剑
`49:25` 好咱们接下来咱们聊聊更宏观的一些考量
`49:31` 关于评估啊
`49:32` 我觉得评估物理很重要的一点就是现实性
`49:36` 或者说生态消毒评估
`49:39` 能在多大程度上反映真实世界的使用
`49:41` 比如考试就跟真实场景差得挺远的
`49:45` 像chat polarry呢
`49:46` 数据都来自真实用户
`49:48` 但咱们可能会想
`49:50` 这种用户分布或者使用场景的分布
`49:53` 到底对不对呢
`49:54` 我后面会提到几个benchmark
`49:56` 他们试图去衡量生态效度
`49:58` 尤其是在用力层面
`50:00` 至少不是针对单个query这个层面哦
`50:02` 所以OpenAI做了一个叫GDP value的benchmark create
`50:07` 然他们挑了美国GDP排名前九的行业
`50:11` 涵盖44种不同职业
`50:14` 找了一批专业人士来啊
`50:16` 来创建任务
`50:17` 这些专业人士大概有14年的经验
`50:20` 你能看到里面有护士李斌接待房产中介
`50:25` 影视剪辑师等等啊
`50:27` 他们创建了一堆任务
`50:30` 这是医学领域的另一个例子
`50:32` 在此之前
`50:34` 很多医学benchmark都是基于标准化考试的
`50:36` 其实对咱们人类也一样
`50:38` 你得考这些标准化考试
`50:40` 从医学院毕业
`50:41` 然后才能上手做手术
`50:42` 诶
`50:43` 其实流程更长
`50:44` 你不会直接给病人做手术
`50:46` 所以重点在于对于language model
`50:49` 你也不能只是让它通过医学考试就直接部署
`50:53` 所以这是一个项目
`50:54` 你知道的这安创创泥one治任务
`50:57` 由创泥男为临床医生提供
`50:59` 目的是模拟临床医生真正会向language model提出的问题
`51:03` 这和那种多项选择提示的考试完全不一样
`51:06` 这里还有另一个项目
`51:08` 他用的是真实数据
`51:09` 那么咱们想想
`51:11` 谁手里真正掌握着人们行为的数据
`51:13` 是模型开发者吗
`51:15` 所以这个项目挺有意思
`51:16` 因为他用language models本身来分析真实数据
`51:19` 所以尤其是出于隐私考虑
`51:21` 你也没法直接去查看那些人的数据
`51:25` 但你可以让language models去查看数据
`51:27` 并分析总结出一般规律
`51:30` 所以language models就能告诉你人们都有哪些类型
`51:34` 以及大家一般用claw来做什么
`51:36` 嗯有时候真实性和隐私会相互冲突
`51:41` 因为理想情况下
`51:42` 你其实希望能拿到真实查询流里的样本
`51:46` 这对评估能了解一个模型的运行效果
`51:50` 并深入理解错误究竟出在哪
`51:52` 是非常有价值的
`51:53` 但这又会触及隐私方面的考量好
`51:56` 那么还有一个要考虑的点
`51:57` 也就是另一种效度
`51:59` 那就是嗯总的来说
`52:01` 我们怎么知道这些评估在科学上是有效的呢
`52:05` 这一点其实很早就被提出来了
`52:11` 所以在机器学习入门课里
`52:13` 你会学到不要在自己的测试集上训练
`52:17` 希望如此
`52:18` 嗯在基础模型出现之前
`52:20` 这其实是相当明白吧
`52:22` 以前用image net s squad这些数据集做基准
`52:26` 测试时
`52:26` 都有明确的训练集和测试集
`52:29` 大家玩的都是同一套规则
`52:31` 你在训练集上训练
`52:33` 在测试集上测试事情很简单
`52:36` At
`52:37` 但现在我们面对的是那些
`52:39` 在网络上训练出来的模型
`52:42` 而且远不止于此
`52:44` 还有你根本不知道数据里到底有什么
`52:47` 所以我觉得这让评估变得很不一样
`52:50` 那咱们能怎么办呢
`52:52` 首先你可以试着推断一下
`52:54` 模型是不是真的见过这些测试数据啊
`52:58` 这个想法挺巧妙的
`53:00` 这是TSU团队提出的
`53:01` 他们利用了一个事实
`53:03` 基准测试里问题出现的顺序本应是随机的
`53:07` 但如果模型偏好某种与基准测试一致的
`53:11` 特定顺序
`53:12` 那很可能他只是在那个基准上训练过
`53:15` 另一种办法是
`53:16` 你可以鼓励大家主动报告呃
`53:19` 就呃你知道就是实际训练集和测试集有重叠
`53:24` 所以这其实是个规范问题对吧
`53:27` 我觉得吧可能在机器学习里不太常见
`53:31` 但在统计学里
`53:32` 大家都会报告置信区间miss得出一个估计值
`53:36` 但我总想知道这个估计值靠不靠谱
`53:39` 这就是标准做法
`53:40` 所以这篇立场论文的想法是
`53:43` 你们这些模型提供商
`53:44` 如果打算声称拿到了某个GPQA分数
`53:48` 那就应该给出依据
`53:50` 证明你没拿测试集去训练up呃
`53:54` 所以还有个路子就是干脆放弃
`53:58` 就是说好吧
`53:59` 所有这些评估
`54:01` 咱们就假设大家都拿他们去训练过了
`54:04` 先假设最坏的情况
`54:05` 所以呢我们网站上有个功能
`54:07` 就是你可以随时定义全新的评估
`54:10` 所以这里有一堆点子
`54:11` 比如life code bench
`54:13` Untreatable eval
`54:14` 他们主要就是去抓取新网页
`54:16` 归档论文或者GITHUB上的内容
`54:19` 然后定义那些超出截止日期的评估
`54:22` 也就是超出language model截止日期的那些
`54:26` 所以这看起来挺靠谱
`54:28` 效果也不错
`54:29` 但有一点要记住
`54:30` 时间戳也不一定靠谱
`54:32` 因为啊比如我可能会发布一个带代码仓库
`54:36` 你懂的
`54:37` 举个例子
`54:37` 我可能往GITHUB上放点东西
`54:39` 但它其实是从别的某个仓库衍生来的
`54:42` 那我怎么知道那东西是不是真的全新的呢
`54:45` 谢谢第四条路是用私有评估
`54:48` 像那些公司模型开发者就是这么干的对吧
`54:51` 所以如果你是google或者OpenAI
`54:53` 你有自己的内部代码库
`54:55` 你可以认为它不在互联网上
`54:57` 而且你显然不会拿这些数据来训练
`54:59` 所以你可以拿它来做评估
`55:01` 这通常是相当可靠的
`55:03` 而如果你不是一家公司
`55:04` 你可以用你个人写的东西
`55:06` 我读研的时候有一堆被拒的论文
`55:09` 从来没放到网上
`55:10` 所以我可以用那些
`55:11` 而这类评估我觉得对perplexity评估非常有利
`55:15` 因为呃做PERPLEXI
`55:17` 你只需要一个好的数据集
`55:18` 就能评估对数概率
`55:20` 谢谢我要再提的另一个顾虑是数据及质量
`55:24` 还记得吧
`55:25` 我之前展示过sweet bench verify的排行榜啊
`55:28` 这是因为a three bench有一堆问题
`55:30` 有些任务的单元测试不够严谨
`55:33` 还有其他一些毛病后来都被修正了
`55:35` 这你知道其实还有很多别的benchmark的
`55:38` nark或者m mu
`55:40` 这些benchmark都经历过这个过程
`55:43` 大家做了更仔细的核查
`55:46` 然后发现等等等等
`55:48` 这道题其实有点不对
`55:50` 它本身是不是哪里出了问题
`55:53` 谢谢好
`55:54` 比如这个例子
`55:55` 文本里没给曲线
`55:56` 所以你没法回答这个问题
`55:58` 谢谢嗯
`55:59` 你看这婴儿穿袜子了吗
`56:01` Possible
`56:02` 谢谢嗯
`56:04` 还有篇论文指出
`56:05` agenic benchmarks问题很多
`56:07` 这可以说更难评估
`56:09` 因为它不只是看问题和选项那么简单
`56:11` 而是一整个环境答案你可以看一看
`56:15` 但它是一整个环境嗯
`56:17` 而且有些情况是这样的
`56:19` 你知道写代码时有能跑通或跑不通的
`56:24` test cases当然很好
`56:26` 但咱们都知道test cases可能不全
`56:29` 所以你可能过了所有测试代码却还是没法用啊
`56:34` TALBENCH基准测试里还有另一种情况
`56:36` 如果agent直接输出空响应
`56:38` 得分能到38分三左右
`56:40` 这些基准测试有些琐碎的问题
`56:43` 咱们得小心避开这些坑
`56:44` 我要提一下DAWSON这个工具
`56:47` 他用language model来检查agent的运行轨迹
`56:50` 从而发现问题
`56:52` 所以这算是对我们这种重度量化的基准测试方
`56:57` 法的一种定性回应吧
`56:59` Measuring
`56:59` 我总建议大家
`57:01` 不管是你自己搞基准测试还是跑模型
`57:03` 一定要看看输出结果
`57:05` 试着审核一下
`57:06` 确保你测的真实
`57:07` 你想测的东西
`57:08` 你是真正在测的东西
`57:10` 谢谢好
`57:11` 那么你知道
`57:12` 咱们接下来也许会要聊得更有哲学意味一些
`57:15` 评估到底是为了什么好
`57:18` 我刚才聊了各种评估类型
`57:19` 其实根本不存在啊
`57:22` 一种能通吃所有情况的评估方法
`57:25` 我觉得你在考虑评估的时候得把目的想清楚
`57:29` 我觉得这点经常没说清楚
`57:31` 打个比方
`57:32` 你可能是个用户或者公司正在纠结要不要买
`57:35` 你是要为某个具体场景选A还是选B
`57:40` 或者你是做研究的
`57:41` 凭直觉觉得智能是怎么回事
`57:44` 想找个法子测一测
`57:45` 或者你是出于商业或政策上的考虑
`57:48` 想搞清楚它到底有什么好处和坏处
`57:51` 或者你是搞模型开发的
`57:52` 想听听反馈好
`57:54` 把模型改的更好
`57:55` 所以不同的目标会让你去选不同的基准测试
`58:00` 或者把几种评估方法混着用
`58:03` 最后还有件事得说清楚
`58:04` 我前面稍微提过
`58:05` 就是咱们到底在评估什么基础模型出来之前
`58:09` 大家评估的是方法
`58:10` 因为咱们有训练集和测试集
`58:12` 唯一变的就是算法本身
`58:14` 而今天咱们主要评的是模型和系统
`58:17` 这里面什么因素都可能变啊
`58:19` 其实也有例外
`58:20` 比如NANOGBT速度竞赛
`58:22` 这是一种刻意设计的评估方式
`58:24` 主要用来衡量算法性能
`58:26` 尤其是模型训练的速度有多快
`58:28` 不过大多数language model评估其实都是针对最终成品的
`58:34` 这很有用
`58:35` 毕竟这才是真正要上线供用户实际使用的东西
`58:39` 但我觉得关键就在于
`58:41` 你得始终非常明确的去界定对
`58:45` 也就是明确你的目标好吧
`58:47` 所以不存在唯一的标准评估
`58:51` 关键在于你选择去测量什么啊
`58:54` 我们聊了不少不同的benchmark
`58:56` 从困惑度开始
`58:57` 包括基于考试的聊天场景
`59:00` 智能体交互
`59:02` 还有推理和安全方面的这些benchmark
`59:05` 在难度真实感和有效性上各不相同
`59:08` 而且往往需要权衡取舍
`59:11` 很难同时做到既高度真实
`59:14` 极具挑战性
`59:16` 又具备生态效度
`59:18` 同时还完全避免训练数据和测试数据的污染
`59:22` 你通常得在这些因素之间做个取舍
`59:25` 具体取决于你的目标
`59:27` 你得决定自己愿意在哪一方面让步好了
`59:31` 这一讲就到这里
`59:32` 下周我们开始聊训练数据好
`59:35` 下次见
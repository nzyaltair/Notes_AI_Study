# 变分推断

变分推断选择可计算的 \(q_\phi(z)\) 近似真实后验 \(p(z\mid x)\)，通过最小化 \(D_{KL}(q\|p)\) 或最大化 ELBO 学习参数：

\[
\log p(x)\ge \mathbb E_{q_\phi(z)}[\log p(x,z)-\log q_\phi(z)]
\]

它适合大规模可微模型，但近似族限制和 KL 方向可能低估不确定性。VAE 是其与深度生成模型结合的典型实例，详见 [[../../B_连接主义与深度学习/04_生成模型/00_生成模型_综述|生成模型]]。
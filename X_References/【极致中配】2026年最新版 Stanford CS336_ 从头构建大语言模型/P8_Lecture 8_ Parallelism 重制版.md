---
title: "【极致中配】2026年最新版 Stanford CS336: 从头构建大语言模型"
url: "https://www.bilibili.com/video/BV11LEA6eEuj/?p=8"
bvid: "BV11LEA6eEuj"
cid: "39163006174"
author: "常青藤名校课"
upload_date: "2026-06-05"
created: 2026-08-17
tags: ["bilibili", "clippings"]
---

## 简介

如果这个中文配音版对您理解 CS336 有帮助，欢迎点赞、投币、收藏支持一下，也可以点个关注。
我会持续更新更多 Stanford / AI / 大模型相关课程的中文配音课程，尽量帮大家用更低的理解成本看懂这些高质量课程。你的支持会让我更有动力把后续内容继续做完、做好。

## 字幕

`00:00:00` 上一节课PERCY讲了并行的一些底层机制
`00:00:03` 咱们暂且这么叫他
`00:00:04` 今天我想做我最爱做的事
`00:00:06` 聊聊各种你能了解的东西
`00:00:08` 比如关于现代语言模型训练并行是怎么运作的
`00:00:11` 知识细节和各种琐碎冷知识
`00:00:14` 咱们这次要真正搞懂
`00:00:15` 当你不得不在超大规模集群上训练超大模型时
`00:00:19` 会面临哪些复杂的状况啊
`00:00:21` 你会发现
`00:00:22` 其实可以同时使用好几种不同的并行策略
`00:00:25` 最后我们会看到一张幻灯片
`00:00:28` 上面我会说4D并行
`00:00:30` 因为你可以同时做四件事
`00:00:32` 其实我还会讲到更多
`00:00:33` 你会发现要在最大规模上实现并行
`00:00:36` 你几乎得用上所有或者说大部分这些策略
`00:00:40` 咱们也会讨论一下
`00:00:41` 为什么你们作业里有一项
`00:00:44` 就是根据给定的网络拓扑和某个具体模型
`00:00:47` 找出最优的并行策略
`00:00:49` 最后我会结合一些已公开的细节
`00:00:52` 带大家看几个最近的大规模训练案例
`00:00:55` 让你们看看这些东西在实际中是怎么用的
`00:00:58` 谢谢好
`00:00:59` 咱们先把第一部分快速过一遍
`00:01:01` 这部分算是对基础集合操作
`00:01:04` 基础网络言语的一个复习对吧
`00:01:06` 咱们之所以需要搞这些并行技术
`00:01:09` 是因为得解决两个瓶颈
`00:01:10` 办法就是扩展到多台机器和多块GPU
`00:01:13` 一个是计算能力
`00:01:15` 单块芯片提供的算力不够
`00:01:17` 我们需要更多解决办法
`00:01:19` 就是搞很多机器
`00:01:20` 然后把它们连起来
`00:01:21` 全球最快的超级计算机
`00:01:23` 算力能达到埃及浮点运算的水平
`00:01:25` 而单块GPU跟这完全不在一个量级
`00:01:28` 另一个咱们得继续并行扩展的
`00:01:30` 原因是内存问题对吧
`00:01:32` 模型太大了
`00:01:33` 塞不进一块GPU
`00:01:34` 所以咱们得想办法把它们切分成小块啊
`00:01:37` 所以当我们在多台不同的机器上部署大量GPU时
`00:01:40` 就会面临一些复杂的问题
`00:01:42` 今天讲座中我们会重点讨论一个核心概念
`00:01:45` 那就是节点内并行和节点间通信的区别
`00:01:49` 节点内并行的连接速度非常快啊
`00:01:52` 这意味着你可以执行那些通信开销很大的操作
`00:01:55` 但节点间的通信速度会比较慢啊
`00:01:59` 因为通信速度较慢
`00:02:00` 所以我们必须采用更尊重通道
`00:02:03` 通信限制的处理方式
`00:02:04` 对吧啊
`00:02:06` 所以这两者我们都要处理
`00:02:07` 现在请记住
`00:02:08` 今天我们讨论的所有内容
`00:02:10` 实际上并不涉及发送数据包之类的操作
`00:02:12` 我们是在集合通信言语的层面上
`00:02:15` 来处理这个问题的
`00:02:16` 所以每当我们要计算通信量时
`00:02:19` 我可能会说好
`00:02:20` 我们需要执行一次all reduce或者all gather
`00:02:22` 来实现某些操作
`00:02:23` 为了高效实现这些操作
`00:02:26` 你得一直深入到硬件层面去
`00:02:28` 不过咱们今天主要聊算法
`00:02:30` 所以我提到的那些网络言语
`00:02:31` 都是指集合操作这一层面的
`00:02:34` 大家还记得PERCY提过这个吧
`00:02:35` 这点很重要
`00:02:36` 因为咱们后面要讲all reduce和reduce
`00:02:38` scatter加all gather是等价的啊
`00:02:40` 所以会有这样一个算法
`00:02:42` 一边做all reduce
`00:02:43` 另一边则分两步走
`00:02:45` 既然这两者在某种意义上的成本是一样的
`00:02:48` 那右边的算法咱们就算白得了
`00:02:51` 后面咱们还会再看到啊
`00:02:53` 这里快速复习一下
`00:02:54` 确保你记得PERCY之前讲的内容
`00:02:56` FRANCE好基础回顾就到这里
`00:02:59` 在咱们进入那些不依赖特定硬件或底
`00:03:01` 层的算法之前我想简单提一下区别
`00:03:03` Any particular reason
`00:03:04` Why you're saying that
`00:03:05` 哦抱歉是的
`00:03:06` 那么你说这个有什么特别的原因吗
`00:03:08` 哪些reduce可以拆成reduce scatter之类的
`00:03:11` 好问题是我为什么特意讲这个是的
`00:03:14` 确实还有其他分解方式
`00:03:15` 但这一种对某个算法特别有用
`00:03:17` 所以我才特意强调这种分解没错
`00:03:20` 当然还有其他可行的做法好
`00:03:22` 那么在正式开始之前
`00:03:23` 我还要讲另一件事
`00:03:24` 我们今天讲的大部分内容跟具体硬件无关
`00:03:27` 也就是说基本上只要理解了这些基本概念
`00:03:30` 就能把它用到任何硬件平台上
`00:03:33` 不过在正式开始之前
`00:03:34` 我确实想简单聊一下底层的硬件战
`00:03:38` 一方面是因为这本身挺有意思
`00:03:39` 另一方面也是因为
`00:03:40` 我觉得这会让不同的模型训练公司
`00:03:43` 采用不同的并行策略
`00:03:44` 大家还记得TPU吧
`00:03:46` TPU是google的加速器
`00:03:48` 我在讲GPU那节课时提到过
`00:03:50` TPU有点像轻量级的GPU
`00:03:52` 但不同之处在于网络
`00:03:54` so那么网络有什么不同呢
`00:03:55` 简单用一页幻灯片来说
`00:03:57` TPU的网络是一种叫环形网格的结构对吧
`00:04:01` 你可以把它想象成一个网格
`00:04:02` 然后让网格的两端能够互相通信
`00:04:05` 这个稍微有点复杂
`00:04:06` 这是个3D图
`00:04:07` Marcel
`00:04:07` 你那边有可视化效果对吧
`00:04:09` 你要把它发到讲座频道吗
`00:04:11` 行有个挺有意思的网页可视化
`00:04:13` 你可以看看
`00:04:14` 你看他把three d环形网格可视化出来了
`00:04:17` 挺酷的
`00:04:18` 不过我觉得你脑子里大概可以想象成这张图
`00:04:21` 你可以忘掉那个更复杂的所有芯片都连向邻居
`00:04:24` 而且邻居是首尾相接的对吧
`00:04:26` 那这有什么好处呢
`00:04:27` 它能让你用一个极简的网络拓扑
`00:04:30` 就能非常轻松的无限扩展对吧
`00:04:32` 那不管网络规模多大
`00:04:34` 你连接的邻居数量基本保持不变
`00:04:36` 相比之下
`00:04:37` GPU的网络思路就很不一样了
`00:04:39` 它更像是一种全对全的模式
`00:04:42` 它的网络连接方式有点像一棵树
`00:04:44` 通常被称为胖树factory
`00:04:46` 在最底层GPU之间的连接速度极快
`00:04:49` 接着GPU会组成一个个part在part的内部进行连接
`00:04:53` 随后pod可能会配备几交换机
`00:04:56` 让它们能够互相通信
`00:04:58` 这意味着随着节点数量的增加
`00:05:00` 这种树状结构会变得越来越大
`00:05:02` 随着你把所有这些节点进行全互联
`00:05:05` 通信成本会上升
`00:05:06` 通信拓扑结构也会变得更加复杂
`00:05:09` 这种设计理念的差异意味着
`00:05:11` 如果你只跟相邻节点通信
`00:05:14` TPU的表现非常出色
`00:05:16` 它们性价比高
`00:05:17` 而且可以在相同功耗下让每个连接更强大对吧
`00:05:24` 那么GPU的这种网络就会更灵活对吧
`00:05:27` 所以你会看到
`00:05:27` GPU和GPU之间有着截然不同的设计理念
`00:05:31` 在接下来的并行课程中
`00:05:32` 我会尽量指出这种差异具体体现在哪里
`00:05:35` 我把这段内容发到了上一讲的聊天区
`00:05:38` 因为有人问这两者有什么区别等等
`00:05:40` What do in my stater
`00:05:41` My chilyour sk far
`00:05:43` Uh about visit reflex
`00:05:44` Stina
`00:05:45` bill allee和jeff dean之前聊过
`00:05:47` 不同硬件各自的适用场景
`00:05:49` dog pu很适合处理
`00:05:51` 像专家混合
`00:05:52` mixture of experts这种通信量比较小的任务
`00:05:55` of因为token会路由到不同的专家
`00:05:58` TPU很适合处理这种密集模型
`00:06:00` 它的分区方式非常可预测
`00:06:03` 我稍后会在讲座中讨论一种叫张量并行
`00:06:05` tensor parallelism的概念啊
`00:06:08` 所以这些硬件适用于不同的工作负载
`00:06:11` 这也是我原本打算在讲座里收尾的地方
`00:06:14` 不要
`00:06:15` 今天早上google宣布要发布TPUBI和RT讲座
`00:06:19` 才接着往下讲
`00:06:20` 理的备课时查了一下
`00:06:22` 心想天哪
`00:06:22` T p u8 i
`00:06:23` 这是一种树状拓扑结构
`00:06:25` 他们现在改用了一种更彻底的out to all连接
`00:06:29` 你仔细想想
`00:06:30` 这很合理
`00:06:31` 现在的语言模型都是MOE
`00:06:33` 你要跑MOE的话
`00:06:34` 把token路由到不同专家时
`00:06:36` 带宽流量会到处乱窜
`00:06:37` X推理的时候
`00:06:39` 这些通信可是真正的瓶颈
`00:06:41` 所以你可能需要那种更彻底的全互联连接
`00:06:44` 有你可以想办法搞定它们
`00:06:46` 你看哪怕是训练的时候
`00:06:48` 他们的训练芯片tpu at那个跨机架的连接
`00:06:52` 看着也挺像GPU的版本啊
`00:06:55` 就比如说他们正在搭建一种更偏向out to all
`00:06:59` 风格的交换结构
`00:07:00` 叫做VERAL网络
`00:07:01` 这是更高层的网络协议站
`00:07:04` 我觉得现在发生的事儿挺有意思的
`00:07:06` 这有点像TPU和GPU的驱动进化
`00:07:08` 说白了是工作负载
`00:07:10` 决定了我们需要什么样的网络gram好
`00:07:13` 最后聊聊硬件
`00:07:14` 我挺喜欢讲讲中国AI圈子里的事
`00:07:17` 有个观点我觉得挺有意思的
`00:07:18` 就像在讲GPU的时候
`00:07:20` 有人问过
`00:07:20` 既然s ram这么好
`00:07:22` 干嘛不全用它
`00:07:23` 比如只用SRAM芯片那种
`00:07:25` 那就是rock对吧
`00:07:26` 效果确实不错
`00:07:27` 这时候你们可能会想
`00:07:28` 既然连接性都这么好了
`00:07:30` 那为什么不把所有东西都连起来
`00:07:32` 用超快的光纤芯片互联呢
`00:07:34` 呃这样你就会得到类似华为S3
`00:07:37` nine ten的方案对吧
`00:07:38` 看看华为S3的规格
`00:07:39` 其实挺有意思的
`00:07:40` 因为单看芯片
`00:07:41` 它比H200差了不少
`00:07:42` 比如它的矩阵乘法速度慢很多
`00:07:44` 你知道很多方面都偏慢
`00:07:46` 但如果你愿意专门拿一个巨大的机架
`00:07:48` 装满光纤交换机去连接数量多得多的芯片
`00:07:52` 就像他们那样
`00:07:52` 用了384个装在一个打机架上
`00:07:55` 那也许你就能通过连接多得多的芯片
`00:07:59` 来弥补单个芯片速度较慢的问题
`00:08:01` 对吧啊
`00:08:02` 所以我觉得
`00:08:03` 这给我们展示了一个非常有趣的权衡
`00:08:05` I
`00:08:06` 因为一旦你审视这种架构
`00:08:07` 就会发现它的功耗高得离谱
`00:08:09` wortan envied系统的四倍
`00:08:11` 也就是说
`00:08:12` 如果你愿意承担电力成本
`00:08:13` 就可以用蛮力解决很多通信问题
`00:08:16` 从而实现横向扩展
`00:08:18` 你可以更激进的扩展到three hundred的芯片
`00:08:20` 但代价就是功耗对吧
`00:08:22` 这就好比之前讲的SRM粒子
`00:08:24` 如果你想设计一款既省电又好造的硬件
`00:08:28` 最终就会落到某种特定的结果上
`00:08:30` 如果你想用蛮力硬解
`00:08:32` 最终结果就会大不相同对吧
`00:08:33` 所以我觉得这是个很有意思的例子
`00:08:35` 能让我们学到硬件设计中的这些权衡
`00:08:37` 好不错
`00:08:38` 咱们把这些串起来看
`00:08:39` 你首先得明白
`00:08:40` 现在的计算单元已经不是GPU了
`00:08:42` 而是整个数据中心
`00:08:43` 咱们得搞定几件事
`00:08:45` 首先得能控制咱们用了多少显存诶
`00:08:48` 其次得能控制咱们能调动多少算力
`00:08:52` 而且咱们希望所有这些环节都是无损的
`00:08:55` 对吧
`00:08:55` 咱们得把现有资源都用上
`00:08:57` 接下来咱们要讲的就是那些算法
`00:08:59` 他们能借助这些通信源于组件来实现这一点嗯
`00:09:03` 好不错
`00:09:04` 那么讲座的第二部分就是今天的重头戏了
`00:09:08` 这些就是咱们用来并行化语言模型的算法啊
`00:09:11` 接下来会讲到几个不同的重要概念
`00:09:14` 其实这些内容或许可以归为一组
`00:09:17` 数据并行是一种思路
`00:09:19` 简单来说
`00:09:19` 就是把要训练的数据分发到不同的GPU上啊
`00:09:23` 也就是说流动的是数据嗯
`00:09:25` 而模型并行则是把模型拆分成不同部分
`00:09:29` 分散到各处
`00:09:30` 实际上这两者的界限有点模糊啊
`00:09:33` 因为其中一种算法也会切分参数
`00:09:35` 各位大家稍后会明白
`00:09:37` 我为什么把这些算法分门别类嗯
`00:09:40` 从概念上说
`00:09:41` 我们既要扩展计算能力
`00:09:43` 也要扩展内存
`00:09:44` 所以才需要用到这一整套不同的技巧好
`00:09:48` 那么我觉得数据并行是最容易理解的
`00:09:51` 它的概念非常简单
`00:09:52` 这也是并行化语言模型训练
`00:09:54` 这类算法的标准做法
`00:09:56` 所以先别管ADAM
`00:09:57` 我们暂时只用最基础的SGDN
`00:10:00` 咱们有一个批次大小G为B对吧
`00:10:02` 所以每次更新我都会取B个样本
`00:10:04` 我会把梯度加起来
`00:10:06` 然后执行更新
`00:10:07` 所以针对这种情况
`00:10:09` 最基础的并行做法就是把批次B拿来用
`00:10:12` 我可以把它分给M台机器
`00:10:14` 如果B能被M整除
`00:10:16` 每台机器就能分到大小为M的小批次
`00:10:20` 接着每台机器分别计算梯度
`00:10:22` 最后把所有梯度同步起来求和好不错
`00:10:26` 从计算扩展性的角度看
`00:10:28` 这非常完美
`00:10:29` 只要每个GPU上的样本量足够就没问题
`00:10:32` 那通信开销呢咱们得互相传多少数据啊
`00:10:36` 每个批次
`00:10:37` 咱们基本上得传输两倍于参数量大小的数据
`00:10:40` 对吧
`00:10:41` 因为我要做的就是把梯度在机器间来回传输好
`00:10:45` 至于内存扩展性
`00:10:47` 那完全没有
`00:10:47` 每个GPU都存有模型的相同副本
`00:10:50` 激活值的大小得保持一致
`00:10:52` 所以在这种方案里
`00:10:54` 你其实一点内存都没省下来
`00:10:56` 那现在的问题出在哪儿
`00:10:58` 咱们虽然多少解决了计算问题
`00:11:00` 但内存问题压根儿没碰
`00:11:02` 咱们来看看这个内存问题
`00:11:04` 做一点简单的核算
`00:11:05` 只要一看内存情况好像挺糟
`00:11:08` 但事实是咱们的内存状况简直糟糕透顶
`00:11:10` 诶内存为啥这么糟
`00:11:11` 你稍微算算参数就知道了
`00:11:14` 唉看事实证明你得存好多东西
`00:11:16` 而且要存的东西其实比参数本身还多
`00:11:19` 所以按经验法则
`00:11:21` 具体取决于精度
`00:11:22` 我们大概需要五份权重副本
`00:11:25` 以及每个参数16字节来存模型诶
`00:11:28` 所以咱们肯定得存模型
`00:11:30` 参数还得有个地方放梯度对吧
`00:11:33` 再把它们放进模型参数之前
`00:11:35` 这些东西算是某种累加器
`00:11:36` 所以咱们得把这些东西存下来
`00:11:38` 不过实际上你可能还得存点别的东西
`00:11:40` 比如你可能需要一个更高精度的累加器
`00:11:43` 用来往SGD里累加数据
`00:11:45` 这可能只是临时的
`00:11:47` 但你确实得留着
`00:11:48` 要是你再用adamant
`00:11:49` 麻烦正是从这冒出来的
`00:11:51` 你得同时跟踪梯度的一阶距和二阶距
`00:11:54` 而且你得一直跟踪他们对吧
`00:11:56` 所以你得备两份
`00:11:57` 不幸的是
`00:11:58` 视稳定性的要求而定
`00:12:00` 你可能得用高精度来存
`00:12:01` 这开销可不小
`00:12:03` 我们一般管这些叫优化器状态
`00:12:05` 也就是你用来累加一阶局和二阶局的那些数据
`00:12:09` 这些开销其实挺大的
`00:12:10` 你要是细算一下账
`00:12:11` 会发现做SGD更新时内存成本的大头就在这
`00:12:15` 所以我觉得这地方有个特别优雅的做法
`00:12:18` 就是想办法省点内存
`00:12:20` 把它们分散到各个加速器上
`00:12:23` 而且是用一种结构化的方式
`00:12:25` 这么干的话
`00:12:26` 你几乎是白捡了这些收益
`00:12:28` 就像我刚才在上一张幻灯片里说的
`00:12:30` 优化器状态占了你绝大部分的内存
`00:12:32` 大家看优化器状态是绿色的
`00:12:34` 占了大头
`00:12:35` 橙色是梯度
`00:12:36` 蓝色是参数cross
`00:12:37` 你看参数和梯度一样大
`00:12:39` 但状态数据更大
`00:12:40` 要是用朴素的数据并行
`00:12:42` 就是把这一堆东西全复制到每个GPU上
`00:12:45` 跨GPU复制
`00:12:46` 这不太妙对吧
`00:12:47` 毕竟你占用的显存远超实际需要
`00:12:50` 而且显存占用会随着加速器数量线性增加
`00:12:54` 确实不太好
`00:12:55` 我们可以把优化器状态
`00:12:56` 分片放到不同的GPU上
`00:12:58` 这样能省不少空间
`00:12:59` 因此如果我们把其他需要跟踪的部分
`00:13:01` 也分片处理
`00:13:02` 那也能省很多
`00:13:04` 比如梯度也可以分片到不同的GPU上
`00:13:07` 或者极端点
`00:13:08` 把所有东西都分配到不同的加速器上
`00:13:12` 这样你看随着数值从YY01路降到一边九
`00:13:15` 右边显示的显存节省效果会非常惊人
`00:13:18` 接下来咱们主要研究一下这么做代价有多大啊
`00:13:23` 大家可能觉得天下没有免费的午餐
`00:13:25` 基线方案肯定是最快的
`00:13:27` 当我们往下走到这一步时
`00:13:29` 为了省下这些显存
`00:13:31` 就得付出巨大的通信代价
`00:13:33` 我觉得最棒的一点是
`00:13:34` 很多部分其实是免费的
`00:13:36` 不过我会一步步带大家过一遍好
`00:13:39` 咱们先从大家常说的零阶段一开始
`00:13:41` 对应这几个步骤会有三个阶段
`00:13:43` 所以第一阶段是最简单的
`00:13:45` 我只对优化器状态做分片
`00:13:47` 并把它分散到各个GPU上
`00:13:50` 每个人手里都有参数和梯度
`00:13:52` 你可以这么想
`00:13:53` 每个工作进程负责更新参数中的一块儿
`00:13:56` 比如如果我是0号GPU
`00:13:58` 那我就只管更新这里最前面的这一块
`00:14:00` 其他的更新跟我没关系等
`00:14:01` 那具体怎么运作呢
`00:14:03` 我们要做的是大家各自计算
`00:14:05` 每个人处理不同的数据项
`00:14:07` 所以第一步是在各自的数据项上
`00:14:09` 算出完整的梯度
`00:14:11` 接下来我会把自己算好的梯度拿出来
`00:14:13` 用reduce scatter操作分发给其他所有机器
`00:14:15` 毕竟大家都需要我的梯度
`00:14:17` 不过他们不需要全部的梯度
`00:14:19` 只需要那些跟他们自己参数更新有关的部分
`00:14:23` 所以每个工作节点会从我这儿拿到梯度
`00:14:27` 但只拿参数空间里属于他们的那一块儿
`00:14:30` 这样一来
`00:14:31` 这部分的通信开销就相当于一整套参数的量级
`00:14:34` 因为我虽然发的是完整梯度
`00:14:36` 但会把它拆开
`00:14:37` 分别发给每个GPU
`00:14:38` 现在每台机器都可以进行参数更新了
`00:14:41` 他们手里有自己的参数
`00:14:42` 从别人那拿到的梯度
`00:14:44` 还有自己跟踪维护的状态
`00:14:46` 参数更新完之后
`00:14:47` 我会用all gather操作把它们都收回来
`00:14:50` 我负责的比方说1/4的参数
`00:14:53` 所以我会把这些更新后的参数
`00:14:55` 发回给其他所有机器啊
`00:14:58` 所以走啊
`00:14:59` 这就是一个all gather
`00:15:00` 你看这种等价关系其实挺有用的
`00:15:03` 我到底做了多少种操作嗯
`00:15:05` 在朴素的数据并行方案里
`00:15:08` 我得做一次all reduce
`00:15:10` 我得跟所有人交换梯度
`00:15:12` 然后再做更新
`00:15:13` 所以通信开销是参数量的两倍
`00:15:17` 在zero stage one阶段
`00:15:18` 我做了两个操作
`00:15:20` 先用reduce scatter把梯度发出去
`00:15:22` 再用all gather把更新后的参数收回来
`00:15:24` 不过我们之前提过reduce scattered加上all gather
`00:15:28` 其实就等价于一个all reduce
`00:15:29` 所以zero stage one的通信特性
`00:15:32` 跟朴素DDP是一模一样的
`00:15:34` 这意味着我们白捡了一笔内存节省啊
`00:15:36` 太棒了
`00:15:37` 再看看内存占用
`00:15:38` 我们基本上是把参数
`00:15:39` 以及所有非优化器状态的东西都除以了GPU数量
`00:15:42` NGPU太棒了太棒了太棒了
`00:15:44` 那我们还能更进一步吗
`00:15:47` 嗯到了zero stage two
`00:15:49` 我会把梯度拆开
`00:15:50` 然后同样对它们做分片
`00:15:52` 现在这一步会更棘手
`00:15:54` 因为之前我一直依赖一个前提
`00:15:56` 我能算出完整的梯度
`00:15:58` 现在我连完整的梯度都没法物化出来
`00:16:00` 那怎么办呢
`00:16:01` 其实这只是个系统层面的小技巧
`00:16:05` 我在网络里反向遍历的时候
`00:16:07` 并不需要算出完整的梯度
`00:16:08` 我可以边走边把梯度发给对应的工作节点
`00:16:11` 所以这里的关键步骤是
`00:16:14` 不再一次性物化整个梯度向量
`00:16:16` 而是沿着计算图反向遍历
`00:16:20` 算完某一层的梯度后
`00:16:21` 我会马上对它做规约
`00:16:23` 把结果发给对应的工作节点
`00:16:25` X方向图里不再需要某个梯度时
`00:16:26` 就立刻把它释放掉
`00:16:27` 所以你现在其实是在边算边发梯度
`00:16:30` 大家可以看到逐步去做还是一次性做完
`00:16:32` 结果都一样
`00:16:33` 所以这真的没什么区别
`00:16:35` 最后你可以再次用all gather把参数收集起来
`00:16:39` 从某种意义上说
`00:16:40` 我已经把梯度分票了
`00:16:41` 而且没多花什么额外成本
`00:16:43` 对吧好
`00:16:44` 最后一点也是最棘手的一点
`00:16:46` 我第一次学这个的时候
`00:16:48` 觉得简直像变魔术
`00:16:49` 到目前为止
`00:16:50` 我们已经免费得到了不少好处
`00:16:52` 所以咱们继续往下推
`00:16:53` 我打算把参数也分片
`00:16:55` 一直横跨到所有的状态
`00:16:57` 咱们还是用之前在zero stage two里用过的那个思路
`00:17:01` 大家还记得这个思路吧
`00:17:02` 就是我要一步步来
`00:17:03` 所以我的做法是在便利计算图的时候
`00:17:06` 按需发送接收参数
`00:17:08` 这挺难搞的
`00:17:09` 因为任何时候
`00:17:10` 每个GPU只能看到参数
`00:17:12` 梯度和优化器状态的一小块对吧
`00:17:14` 这就是zero stage three
`00:17:15` 也叫FSDP
`00:17:17` 如果你以前在PYTORCH里做过并行化
`00:17:19` 那你估计早就打过交道了
`00:17:20` DU它底层是怎么跑的呢
`00:17:22` 你要做两件事
`00:17:24` 四过程中会做多次all gather和一次reduce scatter
`00:17:28` 咱们从左往右一步步来
`00:17:29` 这大致就是整个更新流程
`00:17:31` 首先你要加载模型
`00:17:32` 你看顶部和底部各有一块GPR
`00:17:35` 第一步是收集这一层的权重
`00:17:38` 我先跑一下这一层的前向传播
`00:17:40` 既然前向传播做完了
`00:17:41` 我就可以把权重释放掉了
`00:17:42` 对吧
`00:17:43` 唉这些参数我就不需要了
`00:17:45` 打个比方
`00:17:46` 假设我只有一层
`00:17:47` 那我现在就可以进入反向传播阶段了
`00:17:49` 做反向传播的时候
`00:17:51` 我需要之前保留的激活值
`00:17:52` 还有这一层的参数
`00:17:54` 这个参数我可以按需通过all gather拿过来的
`00:17:57` 我先all gather做完反向传播后
`00:18:00` 拿到梯度就立刻把它们分散回去
`00:18:02` 拿到梯度后
`00:18:03` 我执行reduce scatter
`00:18:04` 把梯度分散出去
`00:18:06` 接着就把权重释放了
`00:18:07` 反正也不需要了
`00:18:08` 然后我就重复这个过程
`00:18:09` 所以我是按需抓取参数
`00:18:11` 算完反向传播
`00:18:13` 把梯度发出去
`00:18:14` 循环往复
`00:18:14` 最后再更新权重好了
`00:18:17` 那通信成本是多少
`00:18:18` 咱们一共做了多少次all gather
`00:18:20` 咱们做了两次all gather
`00:18:21` 还有一次reduce scatter
`00:18:22` 而且你看多了一次all gather
`00:18:24` 这看起来不太好time
`00:18:25` 这是其一
`00:18:26` 其二是从开销上看
`00:18:28` 这有点夸张
`00:18:28` 因为咱们一直在通信
`00:18:30` 你看每一层咱们都在做这种通信
`00:18:32` 你可能会觉得这太贵太糟糕了啊
`00:18:34` 但我觉得有一点既反直觉又很重要
`00:18:37` zero stage three用了两个很关键的想法
`00:18:39` 让这种开销基本上可以忽略不计
`00:18:42` 第一个想法咱们已经见过了
`00:18:44` 就是咱们会扫一遍计算图
`00:18:46` 做完必要的通信
`00:18:48` 然后立马释放内存
`00:18:49` 嗯这是咱们已经见过的一个思路
`00:18:51` 还有个我还没讲过的思路
`00:18:52` 就是咱们要把这个过程里
`00:18:54` 通信和计算的开销重叠起来对吧
`00:18:56` 这一开始挺难解释的
`00:18:58` 所以我从这篇论文里拿了这张图
`00:19:00` 我觉得这篇论文讲的是PYTJFSDP
`00:19:02` 那种架构人
`00:19:03` 因为恰好其实大概是这样的啊
`00:19:05` 你看我们这有几条不同的流
`00:19:07` 这大家大致就是CPU里发生的事
`00:19:10` 这是GPU里负责计算的部分
`00:19:12` 而最底下则是GPU里负责通信的部分
`00:19:15` 那FSDP是怎么工作的呢
`00:19:17` 我得先拿到参数才能开始计算啊
`00:19:20` 所以我会对第零层做all gather
`00:19:21` 一旦搞定
`00:19:22` 我就能开始前向传播了
`00:19:24` 不过在我做前向传播的时候
`00:19:26` 你看我正在请求下一层的参数
`00:19:28` A我在算第几层层的时候
`00:19:30` 也在对第一层做all gather
`00:19:32` 现在第一层的数据齐了
`00:19:33` 我就能做第一层的前向传播了对吧
`00:19:36` S接着我开始这个过程时
`00:19:39` 就可以去对第二层做all gather了
`00:19:41` 所以你看我在做这些操作时
`00:19:43` 把计算和通信重叠在一起了
`00:19:46` 我可能还得花点时间释放参数
`00:19:48` 也可能再花点时间算
`00:19:49` 下一次前向传播讲了1divides
`00:19:53` 如果你复用那个W0
`00:19:54` 我这边相当于把第零层的前项计算做了两次
`00:19:57` 对吧
`00:19:58` You see
`00:19:58` 这么做的好处是
`00:19:59` 只要你算力够
`00:20:01` 网络够快
`00:20:01` 把通信和计算重叠起来
`00:20:04` FSDP的成本几乎可以忽略不计对吧
`00:20:07` 诶你会看到一些气泡
`00:20:08` 那是额外开销
`00:20:09` 但要是通信极快
`00:20:11` 计算量极大对吧
`00:20:13` 你很容易就能看出计算速度可以超过通信速度
`00:20:17` 也就是说计算耗时可以长于通信耗时
`00:20:20` 这样你就能大幅优化内存使用
`00:20:22` 却几乎不增加额外的内存开销
`00:20:25` 好咱们来看看不同的zero阶段
`00:20:27` DDP也就是朴素的数据并行及开销
`00:20:30` 是参数量的两倍
`00:20:32` 它只涉及一次or reduce and zero阶段一和二
`00:20:36` 也就是对梯度和优化器状态进行分片的阶段
`00:20:39` 这在字面意义上是免费的
`00:20:41` 因为他们的通信成本是一样的
`00:20:43` 这仅仅是因为all reduce本质上等同于两个操作
`00:20:46` 现在zero阶段三在技术上需要更多的通信
`00:20:50` 你需要额外进行一次all gather对吧
`00:20:53` 不过情况没那么糟
`00:20:54` 因为这些库能用非常巧妙的方式
`00:20:56` 把通信开销藏在计算过程里
`00:20:59` 所以实际跑起来的话
`00:21:00` FSDP的GPU利用率会非常接近单卡的性能
`00:21:04` 说实话FSDP的效果相当出色
`00:21:06` 而且概念上也非常简单啊
`00:21:08` 你的作业里其实就需要写一个FSDP的实现
`00:21:11` 你写个包装器就行
`00:21:13` 它能把任意模块包一下
`00:21:14` 变成对应的FSDP版本
`00:21:17` 概念上你要做的就是做一堆all gather计算释放
`00:21:21` 然后再反向传播时重复这个过程
`00:21:25` 好的很好
`00:21:26` 对我的理解是
`00:21:28` 我们从下一张GPU拿梯度来计算上一张GPU的梯度
`00:21:31` 对吧
`00:21:32` 嗯你说的那个嘛
`00:21:33` 其实我们不是这样的
`00:21:34` 我觉得你刚才描述的那种
`00:21:36` 从一张GPU把梯度传给下一张
`00:21:38` 那叫流水线
`00:21:39` 那流水线并行
`00:21:40` 打个比方
`00:21:40` 一个层放在一个GPU上
`00:21:42` 另一个层放在另一个GPU上
`00:21:43` 并不是那样
`00:21:44` 所以你看在这些情况下
`00:21:46` 每个GPU都会从头到尾跑完整个模型
`00:21:48` 但区别在于
`00:21:50` 没有任何一个GPU会同时存下所有的参数
`00:21:53` 所以咱们这么改
`00:21:54` 假如我手里这部分网络里没有第零层
`00:21:57` 那我就先把第零层的参数拿过来
`00:21:59` 做完整套计算
`00:22:00` 再去处理下一层
`00:22:02` 所以计算本身没变
`00:22:04` 它跟朴素数据并行里的单GPU计算是一样的
`00:22:07` 只不过你可以理解为
`00:22:08` 在每次计算操作之间去请求参数
`00:22:11` 用完再释放对
`00:22:12` 所以你的意思是
`00:22:13` 每个GPU都只存在每个层的一部分
`00:22:16` 这就是你们要做all gather的原因吗
`00:22:18` 对就是这样
`00:22:19` 你看这张图
`00:22:20` 参数
`00:22:20` 梯度和优化器状态都是分散在不同的GPU上的
`00:22:23` 错对很好哦
`00:22:25` 对
`00:22:25` 那为什么通信开销也没有随着层数成倍增加呢
`00:22:30` 对因为运算量会随层数成倍增加
`00:22:32` 所以这是对的
`00:22:33` 不过每一块通信的数据量其实更小对吧
`00:22:36` 毕竟这只是一个很小的MOP之类的东西
`00:22:38` 而would not something that
`00:22:39` 得整个网络做all reduce的开销做对比
`00:22:41` 所以从某种意义上说
`00:22:43` 这些开销都会累加起来
`00:22:45` 对很好嗯
`00:22:45` 好好的很好
`00:22:47` Of the way
`00:22:48` Their best russions
`00:22:49` 所以实际应用中
`00:22:50` 你看比如a one hundred GPU能装下的最大模型
`00:22:53` 如果用极限方法
`00:22:54` 连7B的模型都塞不下
`00:22:56` 但用了zero stage three现在就能塞进去了
`00:22:58` 比如five hundred这一参数的模型等等
`00:23:00` 所以用了FSDP
`00:23:01` 要装下这些大模型就轻松多了
`00:23:04` 它在节省内存方面带来的提升非常显著
`00:23:07` 嗯能切回上一张幻灯片吗
`00:23:10` 那么关于ABXAN他这上的这个情况
`00:23:14` 到了H500或B200上限制会是什么啊
`00:23:18` 行我没法先算乘以one forty one
`00:23:19` 再除以80
`00:23:20` 不过h two by three对应的就是这个数
`00:23:22` 后面的以此类推
`00:23:23` 对它是线性的对吧
`00:23:24` 也就是说不管换哪个加速器
`00:23:26` 底层的数学逻辑其实没怎么变
`00:23:28` 嗯好我觉得FSDP设计的很干净
`00:23:31` 特别优雅
`00:23:31` 我个人挺喜欢他的
`00:23:33` 我真想在这就把课讲完
`00:23:34` 但不巧的是
`00:23:35` 咱们还得接着往下走
`00:23:36` 去碰那些更复杂更棘手的部分啊
`00:23:40` 咱们得这么做
`00:23:41` 主要有两个原因
`00:23:42` 这里涉及并行化里的一个关键概念
`00:23:45` 某种程度上
`00:23:46` 数据并行正在消耗一项重要资源
`00:23:49` 这项资源说白了就是批次大小
`00:23:51` 如果你的批次大小是八
`00:23:52` 那你最多就只能用八个加速器
`00:23:55` 你可能会想
`00:23:56` 既然咱们有越来越多的加速器
`00:23:57` 那干脆把批次大小设的越来越大不就行了
`00:24:00` 而也行不通
`00:24:01` 因为到了某个临界点
`00:24:03` 会出现所谓的临界批次大小
`00:24:05` 简单来说
`00:24:06` 这时候你增加批次带来的收益
`00:24:08` 还不如多跑一轮SGD划算哦
`00:24:10` 毕竟你还是要针对那个单独的元素
`00:24:12` 跑一次SGD
`00:24:13` 所以在小批次时多加点样本确实很有帮助
`00:24:16` 这跟多跑几轮SD是一样的道理
`00:24:18` 但到了某个点收益就开始递减了
`00:24:21` 批次无限大
`00:24:22` 效果并不会比无限次单步更新更好
`00:24:24` 所以这就带来了一个两难的选择
`00:24:27` 咱们是想用小批次让GPU多歇会儿
`00:24:30` 还是说用大批次
`00:24:31` 但得忍受优化效果上的损失
`00:24:33` 这确实挺难抉择的
`00:24:34` 对吧啊
`00:24:35` 还有个老问题
`00:24:36` 就是内存zero的阶段一和阶段二
`00:24:39` 其实没法真正解决内存扩展的问题
`00:24:41` 阶段二确实能把参数内存切开
`00:24:44` 但不幸的是
`00:24:45` 激活内存和其他类型的内存占用
`00:24:47` 它管不了
`00:24:48` 也没法减少
`00:24:49` 所以我们需要更细致的模型切分方法
`00:24:52` 这样才能真正突破这些限制
`00:24:54` 进一步降低内存占用啊
`00:24:56` 所以这就把我们带到了
`00:24:57` 各种模型并行的思路上嗯
`00:25:00` 所以模型并行其实就是像FSDP那样
`00:25:03` 把参数拆分到各个GPU上
`00:25:05` 不过我觉得我们之前讲的和接下来要讲的
`00:25:08` 有个重要的概念区别
`00:25:10` 现在我们要开始通信了
`00:25:12` 在FSDP里
`00:25:14` 我们把参数切开了
`00:25:15` 但某种意义上它只是个包装器而已
`00:25:18` 计算过程还是照常进行
`00:25:19` 我们只是在计算前把参数来回传一下
`00:25:22` 所以之前飞来飞去的是参数
`00:25:24` 而现在我们要改成来回传激活值
`00:25:28` 再打个比方
`00:25:28` 假如一层在GPU zero上
`00:25:30` 下一层在GPU one上
`00:25:32` 我需要传的是这两层中间的激活值
`00:25:35` 这就是最大的不同之处
`00:25:37` 就会用到流水线定型
`00:25:39` 也就是把层切开
`00:25:41` 张亮并行
`00:25:42` 就是把矩阵切开
`00:25:43` 还有专家并行
`00:25:44` 把专家分散到不同的地方
`00:25:46` PERCY之前讲过张量并行了
`00:25:48` 所以你对这个应该有个大概的概念了
`00:25:50` 我先来讲讲流水线并行
`00:25:52` 也就是逐层并行
`00:25:53` 我觉得这在概念上挺简单的
`00:25:54` 我刚学并行那会儿心想哦对
`00:25:56` 这很简单嘛
`00:25:57` S就是把层切开放到不同的GPU上
`00:25:59` 这事特别简单
`00:26:01` 然后在前向传播时
`00:26:02` 你把激活值往前传
`00:26:04` 接着在反向传播时把部分梯度往回传好
`00:26:08` 那如果我这么干会发生什么
`00:26:10` 你会得到一张很难看的图
`00:26:11` 就像这样
`00:26:12` 特别特别让人沮丧
`00:26:14` 每个处理模型的1/4
`00:26:16` 所以计算是随时间变化的
`00:26:18` 你看时间从左往右走
`00:26:20` 不同的加速器就是不同的行right
`00:26:23` 首先最底下这个0号加速器
`00:26:25` 也就是处理第一层的那个会先算一会儿
`00:26:27` 然后就停下来对吧
`00:26:29` 接着他把计算任务交给第二个加速器之后
`00:26:32` 他就一直闲着
`00:26:33` 直到最后我基本上就是在来回传数据
`00:26:37` 同一时间只有一个GPU在工作
`00:26:39` 然后到了反向传播阶段
`00:26:41` 也是一次
`00:26:41` 只有一个GPU在工作
`00:26:43` 专门处理反向传播
`00:26:44` 所以大部分时间我的GPU都闲着
`00:26:47` 利用率真的非常差啊
`00:26:49` 谢谢这个叫气泡
`00:26:51` 就是流水线里你什么都没做的那一段啊
`00:26:54` 解决办法其实就是做某种批处理或者流水线化
`00:26:57` 所以不再是简单的一层接一层的做
`00:27:00` 而是要形成一个流水线对吧
`00:27:03` 在这张图里
`00:27:03` 咱们以这个例子来看
`00:27:04` 有四个元素要处理
`00:27:06` 刚处理完一个
`00:27:07` 紧接着就处理下一个
`00:27:08` 接着你把处理好的元素传给下一层
`00:27:10` 这样就能实现元素在层级间的上下流动
`00:27:14` 4spin
`00:27:15` 当然这个方案合理
`00:27:16` 关键就是看气泡时间和有效计算的比值
`00:27:19` 也就是某种意义上的利用率
`00:27:21` 它大致等于阶段数除以VP4的数量
`00:27:24` 所以为了把气泡时间压到接近零
`00:27:26` 咱们得用很大的批次大小
`00:27:28` 所以如果我们希望气泡时间归零
`00:27:31` 它会按VP4大小的倒数下降
`00:27:33` 这就是我之前说批次大小是个有用资源的原因
`00:27:36` 咱们现在可以换个用法
`00:27:38` 比如通过增加流水线并行度
`00:27:40` 来减少流水线并行中的空闲时间
`00:27:43` 谢谢好吧
`00:27:44` 流水线并行确实挺让人头疼的
`00:27:46` 圈子里常说并行化代码看着挺合理
`00:27:49` 大家也都能看懂
`00:27:50` 直到你真正去实现流水线并行
`00:27:52` 那咱们为啥还要搞流水线并行呢
`00:27:54` 因为跟DDP比
`00:27:55` 它能省内存
`00:27:56` 咱们把层切开了
`00:27:57` 而且还能跟数据并行结合起来用
`00:27:59` 所以这两种方法可以一起上
`00:28:01` 另外这也是个很重要的概念
`00:28:03` 你看在某些网络拓扑里
`00:28:04` 你总会用到流水线
`00:28:06` 它跟FSDP以及你能用的其他各种方法
`00:28:09` 比起来
`00:28:09` 通信特性都非常好
`00:28:11` 它只跟激活值有关
`00:28:12` 比如B乘S乘H它是点对点的
`00:28:15` 就是一个层跟另一个层通信
`00:28:17` 不是全对全的那种
`00:28:18` 所以你怎么安排这些操作
`00:28:21` 决定了他们能不能高效利用你的网络
`00:28:24` 所以实际操作中
`00:28:25` 我们会把流水线放在网速最慢的链路上
`00:28:29` 比如你有好几个数据中心
`00:28:31` 或者几个通信比较慢的pod
`00:28:32` 你就用流水线并行来跨他们做并行
`00:28:35` 因为这是你能用上的通信效率最高的并行方式
`00:28:39` 那我刚才说了
`00:28:41` 流水线的性能特别取决于批次大小
`00:28:44` 接下来我会多次提到这篇论文
`00:28:46` 我先说明一下
`00:28:47` 这是NVIDIA出的那篇mega tron论文
`00:28:50` 他们做了一组很漂亮的参数扫描
`00:28:53` 看看在不同配置下
`00:28:54` 利用率会发生什么变化
`00:28:56` 可以如果你用较大的batch size
`00:28:59` 再配上较大的PIPELSIZE
`00:29:01` 利用率能提得很高
`00:29:02` 几乎跟完全不做流水线并行差不多
`00:29:04` 那但问题是你得用大batch size
`00:29:07` 才能把利用率维持住
`00:29:09` 不然的话
`00:29:10` 流水线并行会迅速拉低你的利用率和性能
`00:29:13` 我先停一下
`00:29:14` 回答几个问题
`00:29:15` 因为下一张片子有点复杂
`00:29:16` 对刚才说那个PSP比流水线的通信开销更小
`00:29:21` 对具体是怎么回事
`00:29:23` 是因为要传输的数据量更少
`00:29:26` 所以这个量是批次大小乘以序列长度
`00:29:30` 再乘以隐藏层维度
`00:29:31` 比起通信整个参数矩阵
`00:29:33` 这涉及的数据量通常要小得多
`00:29:35` 对吧嗯好
`00:29:37` 所以大家一直在想办法
`00:29:40` 用更聪明的方式来减小流水线气泡的大小
`00:29:44` 你可以用一些巧妙的办法
`00:29:47` 把不同层不同V批次里正在做的各个阶段
`00:29:51` 切分开来
`00:29:52` 这样你就能把前向传播和
`00:29:55` 后向传播里的不同步骤穿插起来
`00:29:58` 通过更巧妙的调度安排
`00:29:59` 你还能进一步把气泡压得更小
`00:30:02` 这是从deep seek的论文里拿来的
`00:30:04` 你要是能把前项和后项计算的时机
`00:30:07` 安排的特别巧妙
`00:30:08` 气泡大小就能稍微优化一点
`00:30:11` 不过我觉得还有个更巧妙的做法叫
`00:30:14` 零气泡流水线
`00:30:16` 它其实不完全是调度本身的问题
`00:30:18` 而是要仔细琢磨后向传播的结构
`00:30:22` 在后向传播里其实有两件事是必须要做的
`00:30:25` 你想想看计算在计算图中其实是往回走的
`00:30:29` 这时候你要做两件事
`00:30:31` 第一件事把那些偏导数顺着计算图往下传
`00:30:34` 回想一下反向传播
`00:30:36` 就是从最后面开始把这些偏导数往回传
`00:30:39` 所以这步必须做
`00:30:40` 但你还得算一下当前权重的梯度
`00:30:43` 所以在计算图的每个节点上
`00:30:46` 你要做两件事往回传播
`00:30:48` 然后算出权重的导数
`00:30:50` 理解这个计算图的啊
`00:30:51` 重点在于啊
`00:30:53` 这两件事里有一件对流水线特别关键
`00:30:56` 另一件则什么时候做都行
`00:30:58` 把偏导数往回传对吧
`00:31:00` 这很关键
`00:31:01` 因为信号没传回去
`00:31:02` 下一阶段就没法干活
`00:31:04` 另一方面相对于权重的导数
`00:31:06` 可以说它算是这个图里的一个叶节点right
`00:31:09` 我随时都能做对吧
`00:31:10` 所以现在比较巧妙的做法是
`00:31:12` 把这两部分拆开对吧
`00:31:14` 所以这里就是你的B这些B算是反向的对吧
`00:31:16` 嗯也就是反向传播
`00:31:18` 然后是这些W他们负责计算权重的导数
`00:31:21` 这部分操作可以在任何时候进行
`00:31:23` 哎所以你要做的就是尽可能快的算出B对吧
`00:31:26` 所以这部分现做
`00:31:28` 至于W你可以往后放
`00:31:29` 等计算出现空隙的时候再处理
`00:31:31` 这样一来你基本上就能把流水线几乎完全填满
`00:31:34` 那是不能用零气泡
`00:31:35` 流水线技术非常复杂
`00:31:37` 或者说比大家平时想处理的要复杂得多
`00:31:39` 但它能让你几乎彻底解决这些流水线问题
`00:31:42` 具体取决于W和B各自的工作负载如此等等
`00:31:45` 诸如此类
`00:31:46` 好了
`00:31:47` 不过我觉得看完这个之后
`00:31:49` 我就在想哇
`00:31:50` 这招真聪明
`00:31:51` 你能用系统做出这么巧妙的东西
`00:31:53` 好刚才讲的就是流水线并行
`00:31:56` 关于这部分有什么疑问吗
`00:31:57` 好谢谢
`00:31:58` 咱们可以做一个很自然的类比
`00:32:00` 或者说找个类似的对应关系
`00:32:02` 所以流水线并行就像是沿着深度方向切分
`00:32:06` 你有一个深度维度
`00:32:07` 然后沿着这个维度把网络切开
`00:32:09` 但我们有两种扩展方式对吧
`00:32:11` 我们既有深度轴
`00:32:12` 也有宽度轴
`00:32:13` 但如果我们沿着宽度来切分模型呢
`00:32:15` 这就是PERCY之前讲过的张量并行gather
`00:32:19` 这其实是个很简单的想法啊
`00:32:21` 把矩阵乘法拆成更小的矩阵乘法
`00:32:24` 最后再把算出来的部分和加回去
`00:32:26` 这跟分块tiling的思路也是一样的
`00:32:29` 核心原语只不过是反复出现了很多很多次
`00:32:32` 那么实际操作中
`00:32:34` 这具体是怎么个流程呢
`00:32:36` 咱们来看这个JU运算
`00:32:38` 通常你会怎么处理
`00:32:39` 输入X是从幻灯片最右边进来的Z
`00:32:42` 接着我会拿矩阵A去做乘法
`00:32:44` 先过一遍GELU非线性激活
`00:32:47` 再和B相乘做降维投影
`00:32:49` 最后输出Z这是典型的串行计算
`00:32:52` 但在张量并行中
`00:32:54` 我会把A切成两半
`00:32:55` B也切成两半
`00:32:56` 然后并行跑计算
`00:32:57` 最后再把结果汇总起来
`00:32:59` 这里有个关键点
`00:33:00` 前向传播和后向传播之间
`00:33:02` 其实存在一种对偶关系
`00:33:04` 所以在前向传播时
`00:33:06` 我对X做的就是复制两份
`00:33:08` 然后往前跑计算
`00:33:09` 所以F是恒等函数
`00:33:11` 等到并行计算结束
`00:33:12` G函数就是一个all reduce操作
`00:33:14` 把结果聚合回来对吧
`00:33:15` 但在反向传播时
`00:33:17` 计算梯度
`00:33:18` 这个过程会反过来
`00:33:19` 这时候G就变成了恒等函数
`00:33:21` 因为偏导数是反向传回来的
`00:33:23` 有接下来F就是那些部分梯度
`00:33:26` 我需要把它们加起来
`00:33:28` 才能得到本模块开头的偏导数
`00:33:30` 所以他们会翻转过来
`00:33:31` 如果你要写张量并行
`00:33:33` 这种对偶性还挺重要的好
`00:33:35` 你可能已经注意到了对吧
`00:33:37` 不过当我们拆分内容时
`00:33:39` 有些切法是按列来的诶
`00:33:41` 所以按列的张量并行会发生在输入端对吧
`00:33:45` 也就是说MLP的输入和注意力的投影
`00:33:48` 在每个transformer块里都会按列切分
`00:33:51` 这就是按列切分
`00:33:52` 然后在相应的第二阶段会有按行切分
`00:33:56` 所以在mp的下投影和注意力的输出部分
`00:33:59` 你会看到暗堂切分
`00:34:01` 觉得像层规一化
`00:34:02` 非线性激活或者某一里的路由器
`00:34:04` 这些比较小的层
`00:34:06` 都会在每台机器上完整复制一份
`00:34:08` 咱们真没必要去折腾
`00:34:10` 拆分他们带来的那些额外开销啊
`00:34:12` 谢谢好
`00:34:13` 那咱们什么时候用张量并行呢
`00:34:15` 你看张量并行特别吃计算资源
`00:34:17` Right
`00:34:17` 你看你每次做矩阵乘法的时候
`00:34:20` 都得搞点通信操作对吧
`00:34:22` 你会用到all reduce
`00:34:23` 或者在反向传播时
`00:34:25` 这里得做一个all reduce
`00:34:26` 前向传播时
`00:34:27` 在GE操作里也得做一个good
`00:34:29` 这些操作的数据量跟激活值差不多
`00:34:32` 而且执行的特别频繁
`00:34:33` 所以这非常耗通信带宽
`00:34:35` 咱们一般只在节点内部做这个
`00:34:37` 所以对于GPU来说
`00:34:39` 八个GPU通常会在一个机箱里紧密连在一起
`00:34:42` 所以最多到八个的时候
`00:34:44` 你可能还愿意搞点张量并行
`00:34:46` 但一旦超过这个数
`00:34:47` 你就得跨界电通信了
`00:34:49` 行节点和互联的速度要慢得多
`00:34:51` 所以一旦跨出单台机器的范围
`00:34:53` 性能就会大幅下跌
`00:34:55` 所以咱们得记住
`00:34:57` 张量并行非常吃通信带宽
`00:35:00` 一般咱们会把它用在手头最快的互联网络上
`00:35:03` 接下来我想回到之前提到的TPU的话题
`00:35:07` It's cb
`00:35:08` 记住对TPU来说
`00:35:10` 不存在那种前八个GPU很快
`00:35:13` 后面机器就慢的区别
`00:35:15` well它就是一个大的网格结构
`00:35:17` 所以TPU团队常强调的一个优势是
`00:35:20` 他们能很好的支持张量并行
`00:35:23` 相比GPU它们能支持大得多的规模
`00:35:26` 因为在这个网格上
`00:35:27` 这种高度规则的通信模式能提供高带宽对吧
`00:35:31` 所以用TPU还是GPU做并行差别很大
`00:35:34` 你分配给张亮并行和流水线并行的比例
`00:35:36` 实际上会截然不同
`00:35:38` 那么咱们怎么比较这两种方案呢
`00:35:41` 它们都属于模型切分策略
`00:35:43` 他们都能帮咱们节省参数和激活值的开销
`00:35:46` 张量并行的好处是通常不会出现流水线气泡
`00:35:49` 如果你的网络够快
`00:35:50` 咱们就能实现充分利用
`00:35:52` 它的复杂度也低
`00:35:54` 就是把一些矩阵乘法切分一下
`00:35:55` 不过缺点就是通信量会大很多啊
`00:35:58` 以前咱们做的是B乘S乘H的点对点通信B神
`00:36:02` 现在我要做的是大约八乘B乘以S乘H的一次
`00:36:07` all reduce通信
`00:36:09` 所以这不只是点对点通信
`00:36:11` 还得进行全对圈通信
`00:36:13` 所以只要互联速度够快
`00:36:15` 张亮并行就是个很好的方案
`00:36:18` 其他情况下咱们大概就得用流水线并行了
`00:36:21` 好最后我想聊聊内存使用这块
`00:36:24` 也就是得再仔细琢磨一下内存
`00:36:26` 我觉得在深度学习和语言建模里
`00:36:29` 对内存最天真的理解就是内存不就是存参数吗
`00:36:32` 就像这儿这块绿色区域对吧
`00:36:34` 你觉得只要存下参数所需的那些位就行了
`00:36:36` 完事儿
`00:36:37` 那我之前提过优化器状态
`00:36:39` 这挺重要的
`00:36:40` 就是图里这个黄色方块儿里嗯
`00:36:42` 但要是你看这张图
`00:36:44` 这是用某个PYTORCH分析器测出来的
`00:36:46` 实际内存占用
`00:36:47` 你会发现事情其实没那么简单
`00:36:49` 现啊计算过程中还得留出不少动态内存
`00:36:52` 在存下所有激活值
`00:36:54` 就是图上这些大红峰值
`00:36:55` 然后算梯度的时候
`00:36:56` 反向传播阶段也得把它们存起来
`00:36:59` 所以实际上内存占用峰值
`00:37:01` 会出现在激活值最大的那个点之后不久一次
`00:37:05` 比如当你开始反向传播梯度时
`00:37:07` 你仍需计算并保存大量激活值
`00:37:10` 这往往就是内存占用的峰值时刻
`00:37:12` 所以为了降低内存占用
`00:37:14` 也许我们得处理这个巨大的红色隆起
`00:37:17` 也就是激活值嗯
`00:37:19` 如果你去看语言模型
`00:37:21` 更现代的一些工作负载
`00:37:22` 你会发现随着参数量越来越大
`00:37:24` 先别管标准
`00:37:25` 当前工作的那一列
`00:37:26` 我稍后会讲到
`00:37:27` 但如果只看这张图里的基线
`00:37:28` 那几列
`00:37:29` 对于中等长度的序列
`00:37:30` 随着模型越来越大
`00:37:31` 激活值占用的内存
`00:37:33` 往往会远远超过你所需的参数内存对
`00:37:35` 所以任何内存优化策略都必须充分考虑激活值
`00:37:40` 才能真正发挥作用
`00:37:42` 比如我们不能只处理参数内存
`00:37:45` 那对我们来说是远远不够的
`00:37:46` 谢谢
`00:37:47` 那么如果我们把所有东西都存下来
`00:37:50` 这其实是个不错的起点
`00:37:51` 或者说一个通用的经验法则
`00:37:53` Freeze
`00:37:54` 如果我们需要存储所有内容
`00:37:56` 那么所需的激活量大概是下面这样
`00:37:59` twenty four乘以S乘以B乘以H
`00:38:02` 其中S代表序列长度
`00:38:04` H乘以P4大小
`00:38:06` 再乘以隐藏层维度大小
`00:38:08` 然后加上五乘以a as除以H
`00:38:11` 因此a as就是注意力投数乘以序列长度
`00:38:15` 再除以隐藏层维度大小
`00:38:17` 这种SBH依赖关系是根本性的
`00:38:20` 因为我们预期要为序列中的每个元素
`00:38:23` 存储一些内容
`00:38:24` 为每个批次元素存储一些内容
`00:38:26` 也要为隐藏层维度的每一维存储一些内容
`00:38:29` 因此在我们常是用不同方法减少激活内存
`00:38:33` 并核算激活内存总量的过程中
`00:38:35` 会反复看到这个SBH项
`00:38:37` 如果你好奇这些像是怎么来的
`00:38:39` 这个看起来有点怪的5vs处H
`00:38:41` 其实来自二次的注意力项
`00:38:42` 包括drop out
`00:38:43` 像而如果我们使用flash attention
`00:38:45` 就可以通过重计算把这一项省掉
`00:38:48` 谢谢嗯好
`00:38:50` 那如果咱们用张量并行呢
`00:38:51` 之前讲模型并行时
`00:38:53` 我跟你们说过
`00:38:53` 咱们能大幅降低激活值的显存开销
`00:38:56` 所以咱们来看看具体会发生什么
`00:38:59` 张亮并行
`00:39:00` 会把注意力机制和MLP里的矩阵乘法给拆开啊
`00:39:04` 具体是哪些部分呢
`00:39:06` 我之前提过34个部分
`00:39:07` 其中24个是MLPJULI不对
`00:39:10` 注意力头这部分是five dollars a s HT对吧
`00:39:13` 哎就是这儿的第二项
`00:39:16` 现在我可以用张量并行大小T把它降下来
`00:39:19` 这挺不错的
`00:39:20` 这还不错
`00:39:21` 因为我搞定了大部分显存
`00:39:22` 让它能随张量并行大小T线性扩展有
`00:39:26` 所以这挺好的
`00:39:27` 不过事实证明你还会做一些别的操作
`00:39:29` 好比如你会用到层归一化或者drop out
`00:39:32` 你得把注意力机制和MLP的输入存下来对吧
`00:39:35` 因为每一层的输入都得存成残差
`00:39:38` 这样反向传播的时候才能用
`00:39:40` 但不幸的是
`00:39:41` 这些输入并不会因为用了张量并行就减少
`00:39:44` 张量并行没法拆分层归一化
`00:39:46` 所以这些模块的激活值也就没法拆分
`00:39:48` 所以这挺让人头疼的
`00:39:49` 你本来以为像google那样用1000块GPU做张量并行
`00:39:53` 就能把激活值大幅降下来
`00:39:55` 但现实很骨感
`00:39:56` 你这么做还是会多付出十倍的SBH代价
`00:40:00` 所以通常跟张亮并行搭配
`00:40:02` 使用的最后一个手段就是序列并行
`00:40:06` 这名字起得特别误导人
`00:40:08` 其实说到序列并行
`00:40:09` 有个叫上下文并行的概念
`00:40:11` 用在这个名字上其实更合适
`00:40:13` 序列并行的核心思路是这样的
`00:40:15` 我们要处理的是剩下的那些项
`00:40:17` 也就是第十项
`00:40:18` 像SBH这些像都很清亮
`00:40:21` 比如层归一化基本没什么计算量
`00:40:24` 我会把它们拆开来
`00:40:25` 但不是沿着隐藏轴
`00:40:27` 而是沿着序列轴来拆
`00:40:29` 所以每次我们需要用到这些项式
`00:40:31` 在操作之前都得先做all gather和reduce scatter it
`00:40:36` 你仔细想想
`00:40:36` 这其实跟FSDP很像
`00:40:38` 我们有个需要用到的东西
`00:40:40` 我们在某些时候需要激活值
`00:40:43` 前向传播时要算它们
`00:40:45` 反向传播
`00:40:46` 求导时也得用它们
`00:40:47` 但现在我们用不上它们
`00:40:49` 所以咱们沿序列之后把它们拆开来存
`00:40:51` 等需要的时候再生成出来
`00:40:53` 所以说这在概念上和FSDP的思路很像
`00:40:56` 咱们先按分片格式存着
`00:40:58` 等需要的时候再收集起来
`00:40:59` 接着就像我刚才提到的
`00:41:01` 这里有个对偶关系
`00:41:02` 所以前向传播时G走all gather
`00:41:05` G上走reduce scatter
`00:41:07` 到了后向传播情况就反过来了
`00:41:09` G和G上之间的all gather和reduce scatter
`00:41:12` 会互换位置
`00:41:14` 那咱们现在这么算
`00:41:15` 结果会怎样
`00:41:17` 咱们回想一下
`00:41:18` 在没有并行的情况下
`00:41:20` 第一部分我说过
`00:41:21` 激活内存占用是34倍的SBH
`00:41:23` 这里面还包含了注意力头的部分啊
`00:41:25` 用了张量并行
`00:41:27` MLP和注意力部分都能除以T来分摊
`00:41:29` 但我手里那些主点运算就不行了
`00:41:32` 嗯要是用序列并行
`00:41:34` 我也按加速器分了片
`00:41:35` 只不过是在序列轴上分
`00:41:37` 这样内存依赖就变成完全线性的了
`00:41:40` 要是做激活重计算
`00:41:41` 第二项我甚至能省掉
`00:41:43` 毕竟那只是为了存soft max占用的空间啊
`00:41:46` 以后我顺利算上去弱的减一项
`00:41:47` 最后的结果就是SBH乘以3A4
`00:41:49` 再除以T这一点值得记住
`00:41:51` 因为它是咱们在正常训练中
`00:41:53` 激活内存需求的合理下限number
`00:41:55` 所以如果你想手动算算模型能不能塞进GPU
`00:41:59` 记住这一点很有用
`00:42:00` 激活内存至少得留这么多
`00:42:01` 另外你还需要给优化器状态参数和梯度
`00:42:05` 留点内存
`00:42:06` 有了这些数
`00:42:07` 你就能对模型总共需要多少内存
`00:42:10` 有个大概的数了
`00:42:11` 好我先停一下
`00:42:12` 刚才咱们已经过了一遍
`00:42:13` 流水线并行和张良并行
`00:42:14` 大家可能会觉得有点乱
`00:42:15` 在讲专家并行之前
`00:42:17` 咱们先聊聊几个点
`00:42:18` Yes
`00:42:18` 谢谢你啊
`00:42:19` 哦是的
`00:42:21` R3T1是MOP部分mp这部分不能A
`00:42:24` 你同样可以重新计算MOP
`00:42:26` 实际上你能做的重新计算操作
`00:42:28` 远不止这里列出的这些
`00:42:29` 不过做超出这里列出的那些重新计算计算
`00:42:33` 开销会非常大
`00:42:34` 那就打个比方
`00:42:35` mp的重新计算
`00:42:36` 意味着要在反向传播阶段再跑一遍mp
`00:42:39` 这通常是你不想做的
`00:42:40` attention的重新计算
`00:42:42` 通常开销更小
`00:42:42` 没错
`00:42:43` 因为你可以按图块tile来处理cost对
`00:42:45` 另外你也得避开二次方复杂度带来的开销
`00:42:48` 那代价相当高
`00:42:49` 就是那个二次方成本啊
`00:42:51` 这确实挺让人头疼的
`00:42:52` 行好明白了
`00:42:54` 接下来我要讲讲标准并行
`00:42:56` 工具箱里的最后一块拼图
`00:42:57` 也就是专家并行
`00:42:59` Expert paralism
`00:42:59` 如今M1已经是工具箱里的标配了
`00:43:02` 大多数大模型都采用了ME架构
`00:43:05` 这让我们能充分利用其一大优势
`00:43:07` 也就是专家并行控专家并行
`00:43:09` 我觉得他跟张亮并行有点像
`00:43:11` 意思是你把网络里的MOP
`00:43:13` 也就是FFN组件用各种方式拆开
`00:43:16` 然后这会产生通信开销
`00:43:19` 就像你可能在拆分矩阵那样
`00:43:21` 只不过这次你是把整个MP
`00:43:23` 拆分到不同的设备上
`00:43:24` 专家并行的系统表现跟张亮并行也差不多
`00:43:27` 因为这是一种高带宽的并行言语
`00:43:29` 同时还能减少激活值
`00:43:31` 不过有一点很重要
`00:43:33` 如果你在做MOE
`00:43:34` 你几乎总是更倾向于用专家并行分片策略
`00:43:37` 而不是张量并行啊
`00:43:39` 我截了安普
`00:43:40` 来自那个并行指南啊
`00:43:42` 来自MEGATRON
`00:43:42` 这是NVIDIA出的并行库
`00:43:44` 这个库和文档都做得非常好
`00:43:46` 其中一条建议说
`00:43:47` 如果你要做某种并行
`00:43:49` 不管是1P还是TP
`00:43:50` 你都该选EP而不是TP
`00:43:52` 为什么呢
`00:43:53` 嗯这里其实列出了张亮并行的一些缺点
`00:43:56` 如果矩阵切分的太碎
`00:43:58` 矩阵维度变小
`00:43:59` GPU利用率就会下降
`00:44:01` 所以你希望矩阵乘法尽可能大
`00:44:03` 而张量并行会削弱这一点
`00:44:05` 在moo1层中
`00:44:06` 路由稀疏的token激活值
`00:44:08` 远比路由那些密集又庞大的张量并行矩阵
`00:44:12` 乘法激活值容易得多
`00:44:13` 这样你可以把token精确路由到他们该去的地方
`00:44:16` 从而跳过一些计算
`00:44:18` 或者更准确的说是一些开销
`00:44:20` 既然反正都要用MOE
`00:44:22` 不如让专家数量在所有设备上分散开来
`00:44:26` 嗯那么这个抱歉好的
`00:44:28` 这可能会让人觉得好吧
`00:44:30` 专家并行一批很棒
`00:44:31` 大家都应该一直用它cult
`00:44:33` 可惜专家并行依然非常复杂
`00:44:36` 也依然很难
`00:44:37` 我觉得讲完FSDP之后
`00:44:39` 我提到的张量并行
`00:44:40` 流水线并行
`00:44:41` 还有系统层面的那些东西
`00:44:43` 要想真正跑得好
`00:44:44` 其实挺不容易的啊
`00:44:45` 咱们来看两个不同的库
`00:44:47` 我不细讲里面的细节
`00:44:49` 主要是想让大家知道这活其实挺复杂的
`00:44:52` deep sick有个库叫啊DBP
`00:44:54` 我觉得这是deep sk v three时代我最喜欢的东西
`00:44:57` 基本上是他们自己做的库
`00:44:59` 用来做专家并行
`00:45:01` 你的路由和分发
`00:45:02` 他们盯着底层的GP网络言语
`00:45:04` 想把各种操作结合起来
`00:45:06` 或者让硬件去处理某些操作
`00:45:08` 好让hand moe的路由尽可能高效
`00:45:11` 类似的NVIDIA也有个库叫hybrid p
`00:45:14` 也是在做类似的事
`00:45:16` 用非常高效的底层硬件实现
`00:45:18` 来做专家并行的分发
`00:45:20` 简单说一下这事儿为什么难
`00:45:22` 因为你要做大量的auto分发
`00:45:24` 你看M1的通信模式跟张亮并行很像
`00:45:27` 每次碰到mop
`00:45:28` 你都得把token路由到各种各样的地方
`00:45:31` 而且这得特别在意延迟
`00:45:33` 因为计算得等着token到了才能接着跑
`00:45:36` 所以降低这种分发的延迟非常非常关键呃
`00:45:40` 另外关于DPP库还有个挺有意思的冷知识
`00:45:43` deep seek那帮人太较真了
`00:45:45` 为了榨干最后一点性能
`00:45:46` 他们真的去翻出了一些没写进文档的这个啊
`00:45:50` 比如PTX
`00:45:51` 也就是GPU机器码或者类似机器码的东西
`00:45:53` 他们发现可以用这些没写进文档的指令
`00:45:56` 来进一步加速网络通信啊
`00:45:58` 所以这真的就是你为了达到并行效率前沿
`00:46:01` 所需要处理的那种层级的工作
`00:46:04` 最后到现在为止
`00:46:06` 我一直是在单独讲各种并行原语
`00:46:09` 比如数据并行之类的
`00:46:10` 默认你可以像搭乐高一样把它们拼在一起
`00:46:14` 对于基本上其他所有的并行策略来说
`00:46:17` 这在大多数情况下都是成立的
`00:46:20` 比如把数据并行和张量并行结合起来
`00:46:23` 就非常容易
`00:46:25` 专家并行有一些额外的约束
`00:46:27` 你需要稍微注意一下
`00:46:29` 通常来说
`00:46:30` 实现数据并行和专家并行的朴素做法
`00:46:33` 我觉得很多旧库里都是这样
`00:46:34` 是让这两种并行的副本数保持一致
`00:46:37` 换句话说
`00:46:38` 打个比方
`00:46:39` 假设我的数据并行度是八
`00:46:41` 那我就把这八个专家进行分片
`00:46:44` 专家会分布在这八个副本上
`00:46:46` 数据也会在这八个副本间分割
`00:46:48` 并以各种方式路由
`00:46:50` 也就是说你先按数据并行来划分GPU
`00:46:54` 然后专家并行只是这个划分里的一个子集
`00:46:56` 这么做很自然
`00:46:58` 因为你在对token做路由
`00:47:00` 但如果这样做
`00:47:01` 专家并行能达到的并行程度就有一个上限
`00:47:04` 而且它会约束数据并行和张量并行之间
`00:47:06` 如何配合
`00:47:07` 我觉得这就是最后一层复杂性了
`00:47:10` 专家并行在很多方面确实挺乱的
`00:47:13` 这点我得说声抱歉啊
`00:47:14` 不过正因为有这些复杂性
`00:47:17` 在结束专家并行这个话题之前
`00:47:20` 我想跟你们讲一个有趣的系统层面的事实
`00:47:23` 记住混合专家模型只改多层感知机对吧
`00:47:26` 它完全不改变注意力机制对吧
`00:47:29` 因为我们把多层感知机切开了
`00:47:31` 但没切注意力机制
`00:47:32` 所以专家并行在模型上的应用就不太均匀
`00:47:35` 所以当我在专家层面做并行时
`00:47:38` 我并行的是多层感知机
`00:47:40` 而不是注意力机制
`00:47:42` 所以为了并行化注意力机制
`00:47:44` 我可能还得用上张量并行啊
`00:47:47` 所以为了切分注意力机制
`00:47:50` 我需要较高的张量并行度
`00:47:53` 但这会反过来影响我的多层感知机
`00:47:56` 因为如果我张亮并行和专家并行都设得很高
`00:47:59` 矩阵就被切得太碎了
`00:48:01` 利用率会非常差
`00:48:02` 所以这就有个挺别扭的权衡
`00:48:04` 注意力机制想要高张量并行
`00:48:07` 但多层感知机又想要低张量并行啊
`00:48:11` 所以这就产生了冲突
`00:48:12` 那咱们该怎么办呢
`00:48:14` 我过去几年里
`00:48:15` 大家搞出了更复杂的系统方案
`00:48:18` 把MLP里的张量并行
`00:48:20` 和注意力机制里的张量并行给解耦开了
`00:48:23` 所以注意力层用一种张量并行
`00:48:25` MOE层
`00:48:25` 用另一种
`00:48:26` 你再你可以彻底解耦并行性了
`00:48:29` 这就带来了EEP张量并行和数据并行之间
`00:48:31` 更复杂但也更高效的组合方式好了
`00:48:34` 最后我想在这个部分讲讲上下文并行
`00:48:38` 上下文并行
`00:48:39` 也就是环形注意力
`00:48:40` 基本思想就是把超长序列里的激活值
`00:48:43` 分到不同的加速器上
`00:48:45` 你可以按TPU的网格拓扑
`00:48:47` 用环形的方式把激活值传给需要的设备
`00:48:51` 环形注意力我觉得是最早做这个的论文
`00:48:54` 显示这在TPU上效果特别好
`00:48:56` 上下文并行就是一种标准的并行策略
`00:48:59` 专门干这事儿
`00:49:00` 这两种方法都用在长上下文
`00:49:03` 扩展阶段和模型服务里
`00:49:05` 我就不展开讲这个了
`00:49:06` 主要是因为它在概念上
`00:49:07` 和咱们之前聊过的很多内容有重叠好
`00:49:11` 那么第二部分到这里就结束了
`00:49:13` 为了把这些内容都整合到一起
`00:49:15` 我做了一张并行策略的大表
`00:49:17` 包括前两行的数据并行
`00:49:19` 还有下面的各种模型
`00:49:20` 并行策略
`00:49:21` 我还用红色标出了我主观上的看法
`00:49:24` 这些就是不同方法的缺点所在
`00:49:27` 我之所以用红色标出来
`00:49:29` 是想告诉大家
`00:49:31` 没有哪一种并行策略是绝对占主导地位的哦
`00:49:35` 其实就是一大堆权衡取舍
`00:49:37` 你得巧妙地把它们都处理好
`00:49:39` 才能拿到好结果啊
`00:49:41` 举个例子
`00:49:41` FSDB很棒
`00:49:42` 是一种很好的策略
`00:49:44` 但它对激活值没什么帮助
`00:49:46` 它得靠占用全局批次大小来实现并行
`00:49:49` 所以它也有局限
`00:49:50` 为了解决这些问题
`00:49:51` 你可能需要把它和手头的其他一些技巧
`00:49:54` 结合起来用
`00:49:55` 你可能想通过张量并行来减少激活内存啊
`00:49:59` 这也不会影响全局P4大小
`00:50:01` 但这么做的话
`00:50:01` 你就需要更快的网络和更高的带宽
`00:50:04` 另外为了利用那些较慢的连接
`00:50:06` 你可能想采用其他一些并行策略
`00:50:08` 所以你可以看到这些不同的优势
`00:50:10` 表明每种策略都有其适用场景
`00:50:12` 虽然并非每种架构都适用着每一种并行策略
`00:50:16` 但在许多大规模架构中
`00:50:17` 通常会组合使用多种策略
`00:50:20` 请问这些方法还适用于MOE吗
`00:50:23` 还是说MOE有严格限制
`00:50:25` what对
`00:50:26` 所以刚才的问题是流水线并行和什么来着啊
`00:50:31` FSDP适用于MOE吗
`00:50:32` 是的绝对适用
`00:50:33` 要说有什么讲究的话
`00:50:34` 标准建议是专家并行
`00:50:36` 有点像张亮并行
`00:50:36` 所以你会想扩展到大约八路
`00:50:38` 也就是高速连接的范围内
`00:50:40` 嗯不过现在人们不太严格遵循那个建议了
`00:50:44` 但即便如此
`00:50:45` FSDP数据并行
`00:50:46` 流水线并行和张量并行
`00:50:48` 在所有前沿模型中都被广泛使用
`00:50:50` 对不错
`00:50:51` 好你们会在作业中做这个或者类似的操作
`00:50:54` 不过并行有个很酷的地方
`00:50:56` 我觉得也挺好
`00:50:56` 就是你可以做一些数学推算对吧
`00:50:58` 你其实可以这样问好
`00:51:00` 针对我不同的分片策略
`00:51:02` 每一层需要做多少计算
`00:51:03` 针对我不同的通信策略
`00:51:05` 每一层又需要多少通信量
`00:51:07` 如果把不同的设备分配给不同的通信策略
`00:51:11` 这些东西的扩展性如何
`00:51:13` 接着你就能搞清楚这些策略组合在一起时
`00:51:17` 扩展情况是怎样的
`00:51:19` 你可以开始画一些图表了
`00:51:20` 比如如果我设定了特定的批次大小
`00:51:23` 和特定的并行策略
`00:51:24` 这里我列出了三种不同的并行策略
`00:51:26` 看看我的GPU利用率是多少
`00:51:29` 也就是计算时间与通信时间的比率啊
`00:51:33` 只要计算耗时比通信耗时更长
`00:51:35` 原则上你就能把通信开销隐藏在计算过程里
`00:51:41` 唉所以你可以画出这样的图
`00:51:43` 最上面那条虚线大致就是效率的临界点
`00:51:47` 线以上的区域
`00:51:48` 说明你的计算资源得到了充分利用
`00:51:50` 线以下的区域则处于屋顶线的低效区域对吧
`00:51:53` 奇国人你得等着通信结束才能继续计算
`00:51:56` 这地方有个挺有意思的点对吧right
`00:51:59` 所以结合我之前讲的
`00:52:01` 这应该很直观
`00:52:02` 如果P4大小够大
`00:52:03` 只用FSDP就行对吧
`00:52:05` 比如要是每个芯片的批次大小是you tan对吧
`00:52:09` 那你能扩展多远呢
`00:52:11` 这时候FSDP表现非常好
`00:52:13` 你的瓶颈完全在计算上对吧行
`00:52:15` 但关键在于随着批次大小减小
`00:52:18` 只用FSDP会遇到一个临界点
`00:52:20` 这时候限制你的就是通信了
`00:52:23` 那具体该怎么做
`00:52:24` 为了进一步提升效率
`00:52:25` 你得把mp加进来
`00:52:27` 这里指的就是tensor parallel
`00:52:28` 把它和其他策略混用
`00:52:30` 这样你就能把这条曲线再往外推一点
`00:52:32` 哪怕batch size变小了
`00:52:34` 依然能保持计算受限
`00:52:35` 你得不断叠加策略
`00:52:37` 把这条曲线再往外推
`00:52:39` 这样才能拿到更好的效果
`00:52:41` 这就是大家常说的CD或ford并行
`00:52:44` 我觉得应该没有FD并行
`00:52:46` 也就是把这些策略全用上
`00:52:48` 不管通信拓扑怎么变
`00:52:50` 都能让计算单元满负荷运转啊
`00:52:53` 其实有个很简单的办法
`00:52:54` 我觉得到目前为止
`00:52:55` 我一直讲的比较笼统
`00:52:56` 所以你可能觉得挺复杂的
`00:52:58` 但其实并行化的策略很简单
`00:53:00` 只要模型装不进内存
`00:53:02` 你就得想办法把它切开
`00:53:04` 怎么切
`00:53:04` 要么用tensor parallel
`00:53:06` 要么用expert parallel
`00:53:07` 针对高速互联的情况
`00:53:09` 如果每台机器有八个GPU
`00:53:11` 你就用阿陆的tensor或expert parallel
`00:53:13` 剩下的部分再用pipeline parallel或者zero three
`00:53:15` 也就是FSDP把模型塞进去
`00:53:17` 模型放得下之后
`00:53:19` 剩下的部分就全用数据并行搞定对吧
`00:53:22` 剩下的GPU也都拿数据并行填满对吧
`00:53:24` 如果P4大小实在太小
`00:53:26` 可以用梯度累积来提升GPU利用率对吧
`00:53:29` 策略很简单
`00:53:30` 而且你实际会用到的那些大规模并行训练库
`00:53:33` 也证实了这一点
`00:53:35` 所以我把链接放这了
`00:53:36` 是mega tron关于怎么并行化MOE的指南
`00:53:40` 不过这个思路对稠密模型也适用
`00:53:42` 你会看到跟我说的完全一样的东西
`00:53:44` 只是顺序反过来了
`00:53:45` 哎所以第一条准则就是能少用模型并行就少用
`00:53:50` 尽量把数据并行拉满
`00:53:52` 所以能用的话
`00:53:53` 尽量用大部分GPU来分片数据
`00:53:56` 另外如果你在用GPU
`00:53:58` 尽量别出NBLINK的范围
`00:54:00` 意思就是一台机器或者说一个机箱对吧
`00:54:03` 把expert pl和TENSORPL
`00:54:05` 都限制在这个机箱里对吧
`00:54:07` 然后如果你要扩展到多节点
`00:54:09` 就用流水线并行来实现来
`00:54:12` 还有如果是MOE
`00:54:14` 那就首选专家并行
`00:54:15` 最后要是做长序列
`00:54:17` 就用上下文并行对吧
`00:54:20` 环形注意力
`00:54:21` 这其实就是我讲座里一直想跟你们讲透的东西
`00:54:23` 只不过这里把它变成了实操版好
`00:54:26` 最后一个部分我在这儿停一下
`00:54:28` 万一有人对4D并行化那些东西好奇呢
`00:54:30` 对我不那么叫序列并行
`00:54:32` 其实叫上下文并行更合适
`00:54:34` 但我发现序列并行没在这个推荐名单里
`00:54:37` patience啊
`00:54:39` WO对对
`00:54:40` 我是斯拉
`00:54:40` 序列并行
`00:54:41` 通常就是跟张亮并行一起用
`00:54:43` 为了减少激活值
`00:54:44` 那更像是一个附加组件啊
`00:54:46` 加进去是为了减少激活值
`00:54:47` Thank you
`00:54:48` 很多时候它本身并不是一个独立的东西
`00:54:51` 谢谢谢谢对
`00:54:53` 既然大家猜测
`00:54:55` METHOS类似循环transformer这类架构
`00:54:57` 这会让事情变简单还是变复杂
`00:54:59` 谢谢
`00:55:00` 我不太相信那些关于循环transformer的传闻
`00:55:03` 也许吧这也挺有意思的
`00:55:05` 不过回到刚才的问题
`00:55:06` 系统结构会因此发生什么变化
`00:55:09` 好问题
`00:55:10` 我觉得这可能会影响FSDP这类机制
`00:55:13` 比如你没法直接丢弃权重
`00:55:15` 但好处是你不需要反复获取权重
`00:55:18` 它的参数效率要高得多
`00:55:19` 所以很多模型并行
`00:55:21` 相关的东西可能就没那么重要了
`00:55:23` 这挺有意思的
`00:55:24` 对于FSDP相关的很多机制来说
`00:55:27` 情况肯定会不一样
`00:55:28` 为FSDP策略本质上依赖于取权重
`00:55:31` 丢权重
`00:55:32` 再取权重再丢权重这样一个循环
`00:55:34` 而在这里你其实什么都没有丢弃
`00:55:36` 谢谢很好好的
`00:55:38` 这篇论文虽然有些年头了
`00:55:40` 但它其实可能是最好的资源之一
`00:55:42` 本外网络底层的基础原理
`00:55:45` 其实并没有发生太大改变
`00:55:47` 所以我觉得这篇论文里的经验教训
`00:55:49` 放到今天依然很有参考价值
`00:55:52` 这是一篇NVIDIA的论文
`00:55:53` 作者MATT和d park
`00:55:55` 以前可惜是斯坦福的人
`00:55:58` 他们针对很多种不同配置
`00:55:59` 做了大量大规模并行训练实验
`00:56:02` 主要想告诉你
`00:56:03` 随着规模扩大
`00:56:04` 你的并行策略应该如何相应调整
`00:56:06` 这正好印证了我之前一直在讲的那套方案
`00:56:09` 数据并行基本上已经拉满
`00:56:11` 接下来就是张量并行
`00:56:13` 张量并行度会逐步增加
`00:56:14` 直到达到八
`00:56:15` 然后就停下来
`00:56:16` 因为你不想再往上加了
`00:56:17` 从那以后
`00:56:18` 流水线并行的规模就一直在往上加对吧
`00:56:20` 规模够大的时候
`00:56:22` 数据并行的占比就会降下来
`00:56:23` 到最后数据并行度就只剩六了诶
`00:56:26` 因为只要用这些张样并行和流水线并行
`00:56:29` 就能把模型塞进去对吧
`00:56:31` Thank you
`00:56:31` 剩下的预算就全拿去做数据并行了
`00:56:33` 谢谢把这些策略结合起来看
`00:56:35` 就算GPU数量多到离谱
`00:56:37` 利用率依然能保持的很平稳
`00:56:39` 很高效
`00:56:40` 这就是为什么
`00:56:42` 你会看到那么多超大规模的数据中心拔地而起
`00:56:45` 大家想出来的并行策略特别管用
`00:56:47` 通信硬件也够好
`00:56:48` 所以哪怕是在超大规模数据中心
`00:56:51` 甚至是跨数据中心训练
`00:56:52` 你在各种不同模式下都能拿到很高的利用率
`00:56:55` 他们有一些很扎实的定量数据
`00:56:58` 告诉你针对不同的批量大小
`00:57:01` 张量并行做到什么程度就该停了
`00:57:04` 很明显
`00:57:05` 张亮并行度就是R1旦超过这个数
`00:57:07` 你就会陷入麻烦啊
`00:57:09` 在流水线并行里
`00:57:10` 你同样需要比较大的批量大小
`00:57:11` 这样才能把机器有效利用起来
`00:57:13` 这差不多就是为什么会有蓝城之间的差距
`00:57:16` 这是流水线并行
`00:57:17` 而且流水线并行度越大
`00:57:18` 橙色和蓝色之间的差距就越大
`00:57:21` 另外我觉得你可能已经从flash attention里学到了
`00:57:24` 这一点
`00:57:25` 或者还没学到
`00:57:26` 那就是你可能应该做大量的激活值重计算S
`00:57:30` 因为如果你把激活值重计算做的足够巧妙
`00:57:33` 你就能用更大的批量大小
`00:57:34` 而更大的批量大小又能让你把机器用的更充分
`00:57:38` 这点起初让我觉得很反直觉
`00:57:40` 其实你应该通过重计算多做一些计算
`00:57:43` 这样反倒能最终获得更好的利用率
`00:57:45` 因为它能帮你省下内存
`00:57:47` 而省下的内存又可以转化成批量大小
`00:57:51` 好
`00:57:51` 以上就是关于如何选择并行策略的
`00:57:54` 一种更偏定量的视角啊
`00:57:56` 最后我想简单看几个训练运行的例子
`00:57:59` 咱们看看这些训练运行的例子
`00:58:01` 大概就能明白
`00:58:02` 现代并行策略在实际中是怎么用的
`00:58:04` 以及他们是怎么演进的啊
`00:58:06` 比如AI to的DA
`00:58:07` 那是一个70亿参数的开源模型啊
`00:58:09` 抱歉我说的是OEO
`00:58:11` 他是用多A数据集训练的
`00:58:13` 具体细节大家可以去看多A那篇论文啊
`00:58:16` 这是个seventy e参数的模型
`00:58:19` 完全用FSDP在一大堆加速器上训练呃
`00:58:22` 具体多少我记不太清了
`00:58:23` 但这就是个例子
`00:58:25` 说明FSDP的扩展性出奇的好
`00:58:27` 哪怕扩展到非常非常多的GPU也没问题
`00:58:30` 只要你训练的是小模型
`00:58:32` FSDP其实是个挺不错的并行策略
`00:58:34` 我觉得好多1/3参数左右的模型
`00:58:37` 都是纯靠FSDP来训练的
`00:58:39` Deep sick
`00:58:40` 因为系统功底深厚
`00:58:41` 确实搞出了一些挺独特也挺实用的东西
`00:58:44` 所以deep sv one的训练只用了数据并行
`00:58:47` GCL第一阶段张量并行和流水线并行
`00:58:50` 这跟我之前跟你们讲过的一样
`00:58:52` 另外deep seek v three是个mo1模型
`00:58:54` 你要是还记得我之前讲过MOE的话对
`00:58:56` 所以他们不仅有流水线并行
`00:58:58` 还用专家并行取代了张亮并行啊
`00:59:01` 他们的专家并行其实挺特别的
`00:59:03` 因为搞了个64路并行啊
`00:59:05` 所以他们把八台不同的机器归为一组
`00:59:08` 这就构成了专家并行的预
`00:59:10` 那为了实现这种大规模的专家并行
`00:59:13` 他们基本上沿用了流水线并行里的那套技巧
`00:59:16` 目的就是尽量保证专家并行
`00:59:18` 不会出现利用率偏低的时段啊
`00:59:21` 所以为了搞定这些超大的专家并行划分
`00:59:24` 他们其实用了挺复杂的一套技巧
`00:59:26` 再举个例子
`00:59:27` 比如E模型
`00:59:28` 它来自中国一个开源权重的训练场景
`00:59:31` 他们又一次用了零阶段
`00:59:33` 一张量并行和流水线并行对吧
`00:59:35` 这很合理
`00:59:36` 就是经典的数据并行
`00:59:37` 张量并行
`00:59:38` 流水线并行的组合
`00:59:39` 同样一旦用上MOE
`00:59:41` 你就要把张亮并行换成专家并行
`00:59:44` 他们的目标差不多
`00:59:45` 但专家并行效率稍微高一点啊
`00:59:47` 而在大型稠密模型的另一个极端就是lammar in
`00:59:50` 我挺喜欢lama three的这份报告
`00:59:51` 因为他是少数几份
`00:59:53` 真正把并行策略完整拆解出来的报告
`00:59:55` 不只是泛泛而谈
`00:59:57` 而是覆盖了各个不同阶段
`00:59:58` 而这里有个有意思的地方
`01:00:00` 他们有一个预训练之前的阶段
`01:00:02` 就像在最开始做个小小的预热
`01:00:04` 第一行可以忽略
`01:00:06` 中间这一行展示的才是他们主要的预训练阶段
`01:00:09` 这是一种非常标准的并行策略
`01:00:11` 八卡张量并行
`01:00:12` 一种上下文并行
`01:00:13` 16G流水线并行
`01:00:14` 还有十八二十八个数据并行对吧
`01:00:16` 比如说那种大规模的数据并行划分
`01:00:18` 然后在最后因为他们想要一个长上下文模型
`01:00:21` 所以做了长上下文扩展
`01:00:23` 为了这个他们拉高了上下文并行
`01:00:25` 降低了数据并行
`01:00:26` 这就是他们对这个极度吃内存的
`01:00:28` 长上下文扩展阶段做并行的办法
`01:00:31` 注意下
`01:00:32` 我们现在是在张亮并行
`01:00:34` 而不是专家并行
`01:00:35` 因为lama three four five b是个巨大的稠密模型
`01:00:38` 它不是贸易模型
`01:00:40` 顺便说一句
`01:00:41` 有个我没聊到的点
`01:00:42` 但如果你在这些公司里做基础设施工程师
`01:00:45` 你会发现GPU老是坏
`01:00:48` 显然在训练lama three four five b的时候
`01:00:51` GPU坏了
`01:00:52` 阿豪哥forty杯次
`01:00:54` 所以你需要大量的并行
`01:00:56` 不只是快并行
`01:00:57` 还需要冗余
`01:00:58` 这样才能应对训练中可能出现的各种糟糕状况
`01:01:02` 所以这也带来了分布式系统的挑战
`01:01:04` game to是google的一个开源模型
`01:01:07` 他用了FSDP加上张量并行和序列并行
`01:01:11` 就只靠这两种策略
`01:01:13` 我提一下gem two的原因在于
`01:01:15` 据我所知
`01:01:16` 这基本印证了google的观点
`01:01:18` 在TPU上其实根本不需要做流水线并行
`01:01:21` 你只需要搞一个很大的环形网格
`01:01:24` 然后在那个大型网格网络上做张量并行
`01:01:26` 目前还不太清楚
`01:01:27` 这能不能无限扩展
`01:01:28` 这一点一直都有些含糊
`01:01:30` 说实话这点对我来说不太清楚
`01:01:33` 但在java这种规模下
`01:01:34` 这肯定是没问题的嗯
`01:01:36` 谢谢
`01:01:36` 最后我看了几个今年训练的模型
`01:01:38` 比如mixtro it x x twenty two b
`01:01:40` 我调查时发现
`01:01:41` 如果你对实际中的模型训练和并行配置感兴趣
`01:01:44` and video有个叫mega tron bridge的代码库
`01:01:47` 他们在里面发布了不少推荐配置哦
`01:01:50` 针对各种不同模型大小和设置的训练配置
`01:01:54` 你可以看看他们最终推荐的
`01:01:55` 比如bodex starting out b moe
`01:01:58` 或者deep CV three模型的正确配置
`01:02:00` 第
`01:02:00` 所以对于mixture他们用的是八的专家并行
`01:02:05` 四的流水线并行
`01:02:06` 外加四的张量并行
`01:02:07` 这是针对注意力层的
`01:02:09` 你可以看到这遵循的那个建议
`01:02:10` 把专家并行
`01:02:12` 大致保持在八左右啊
`01:02:14` neutron three super大致沿用了deep seek v three的模型设计
`01:02:18` 用了大量专家并行以及上下文并行
`01:02:20` 因为他们在做扩展
`01:02:21` 至少这篇文章里是这样嗯
`01:02:23` 最后我想聊的是换three嗯
`01:02:26` 他沿用了deep sick的方案
`01:02:27` 也就是相当大量的专家并行达到32
`01:02:31` 此外他们还用了阿格流水线并行和果张杠并行
`01:02:34` 来拆分注意力矩阵这类东西
`01:02:36` 另外看看NVIDIO团队
`01:02:38` 针对不同模型并行化所做的各种基准测试
`01:02:40` 其实挺有意思的啊
`01:02:42` 细节我就不展开了
`01:02:43` 如果你想看
`01:02:44` 可以去nvidio bridge仓库里找意思
`01:02:46` 你可以选择各种子配置
`01:02:48` 即便是在张量并行内部
`01:02:50` 这些配置也会显著影响性能啊
`01:02:53` 所以系统集成相当重要啊
`01:02:55` 为了把各方面内容串联起来啊
`01:02:57` 我就想着也许可以做个整体概览啊
`01:03:00` 其实这些模型的共同点是
`01:03:03` 尽可能多地采用数据并行
`01:03:05` 把数据并行域扩大到它们所能达到的极限
`01:03:08` 然嗯张亮并行几乎总是保持在I以下嗯
`01:03:12` 而专家并行现在有时规模可以很大
`01:03:14` 我想这在一定程度上是因为deep seek v rn
`01:03:18` 以及他们为大规模专家并行训练
`01:03:20` 构建的大量基础设施
`01:03:21` 好好的
`01:03:23` 那么为了把所有内容整合起来嗯
`01:03:26` 这门课最核心的观点是我们需要考虑多GPU
`01:03:30` 多节点甚至多数据中心的并行
`01:03:33` 而要在这种情形下考虑效率对吧
`01:03:35` 我们就需要切实调动手头所有的方法
`01:03:37` 因为你们既有高速链路
`01:03:39` 也有低速链路
`01:03:40` 还有利用P大小的各种技术
`01:03:42` 以及所有这些你们都想用上的不同资源啊
`01:03:45` 尽可能多地利用
`01:03:46` 但鉴于所有这些啊
`01:03:48` 事实证明
`01:03:49` 要把这些不同形式的并行组合起来
`01:03:51` 其实有一些相当简单的经验法则
`01:03:53` 最终能实现对计算硬件的有效充分利用好
`01:03:57` 接下来下周我们来讲缩放定律
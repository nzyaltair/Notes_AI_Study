# 训练稳定性与容错

长周期训练的失败既可能来自硬件，也可能来自数值和数据。

## 关键措施

- 监控 loss、梯度范数、激活统计、学习率、吞吐和 NaN/Inf。
- 使用 warmup、梯度裁剪、稳定优化器和异常批次隔离处理 loss spike。
- 分片、异步、可校验的 checkpoint；记录数据位置、随机种子和并行拓扑。
- 定期恢复演练，明确抢占、节点故障和损坏检查点的降级策略。

恢复后须验证训练曲线、数据游标和权重一致性，避免“任务成功但模型悄然退化”。
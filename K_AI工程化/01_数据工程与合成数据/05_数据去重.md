# 数据去重

去重降低训练重复、数据泄漏和记忆风险，并减少无效算力消耗。

## 层次

- 精确去重：内容哈希，适合完全相同的文档或文件。
- 近似去重：MinHash、SimHash、n-gram Jaccard，识别改写、镜像和模板页。
- 语义去重：向量近邻或交叉编码器，处理意义相近但表面不同的样本。
- 跨集合去重：训练集与验证集、基准集、检索库之间的污染检查。

## 工程实践

以文档、段落和样本三种粒度执行；记录保留代表、相似阈值和簇大小。去重应在切分训练/验证集之前完成，并对代码、表格、多模态数据采用专用指纹。

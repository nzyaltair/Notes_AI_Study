# 时间表示

世界是连续变化的。时间表示要追踪对象身份、动作持续时间、事件因果次序和长期任务进展。

## 方法与要求

RNN/SSM 以紧凑递推状态维持短期动态；Transformer 和记忆检索适合长上下文；层级模型将高频运动与低频任务事件分开。好的时序状态应避免身份交换、支持多步预测，并能在新观测到来时修正过去的错误估计。

长期预测不能仅依赖滚动生成：应引入事件级抽象、地图/记忆写入和不确定性增长机制。返回 [[00_世界表示学习_综述|综述]]。
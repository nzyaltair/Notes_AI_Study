---
title: "【极致中配】2026年最新版 Stanford CS336: 从头构建大语言模型"
url: "https://www.bilibili.com/video/BV11LEA6eEuj/?p=2"
bvid: "BV11LEA6eEuj"
cid: "39162612765"
author: "常青藤名校课"
upload_date: "2026-06-05"
created: 2026-08-17
tags: ["bilibili", "clippings"]
---

## 简介

如果这个中文配音版对您理解 CS336 有帮助，欢迎点赞、投币、收藏支持一下，也可以点个关注。
我会持续更新更多 Stanford / AI / 大模型相关课程的中文配音课程，尽量帮大家用更低的理解成本看懂这些高质量课程。你的支持会让我更有动力把后续内容继续做完、做好。

## 字幕

`00:01` 希望大家都没淋雨
`00:03` 我倒是淋湿了
`00:04` 上次说过moon项目有一次one1twenty three
`00:06` 浮点运算次数的训练
`00:08` 之前在跑
`00:08` 现在跑完了
`00:09` 结果其实和预测的一样
`00:11` 大家记得我们跑的这些啊
`00:13` 每条曲线代表的是啊
`00:16` 本质上是一条等浮点运算次数曲线
`00:19` 也就是一批小规模模型运行
`00:21` 你要从中找到计算最优的那个点
`00:24` mini和一个缩放定律
`00:26` 这就是我们预测损失
`00:28` 然后实际跑出损失的那个点模型
`00:32` 跑出来的损失在0.0瓦以内
`00:33` 我觉得这挺酷的
`00:35` 如果外推到g p t five的性能水平
`00:37` 这就是你会得到的损失值
`00:38` 当然具体结果可能因
`00:40` 这些缩放定律的具体情况而异
`00:43` 好的简单分享一下这个消息
`00:45` 在上一讲中我概述了整门课程
`00:48` 我们谈到了分词
`00:49` 这将是第一次作业的内容
`00:52` 今天我想谈谈资源核算
`00:54` 这部分会更偏向系统层面
`00:57` 可回顾一下
`00:58` 我们的核心目标是在有限资源下
`01:01` 训练出尽可能好的模型
`01:02` 资源包括算力内存
`01:04` 有时也包括数据
`01:06` 不过数据在本课中并不会成为限制因素嘛
`01:09` 我们的目标就是单纯的最大化训练的绩效率
`01:12` 所以在优化计算效率之前
`01:15` 我们需要理解某一具体计算的效率
`01:17` 而为此我们需要理解算力和内存特征啊
`01:20` 先让大家大致感受一下
`01:22` 到课程结束时
`01:23` 你们有望能回答哪类问题
`01:25` For h one hundred
`01:26` Yelson
`01:26` Twenty
`01:26` Four on h one hundred
`01:28` 在师傅万一个token上
`01:29` 训练一个700亿参数的模型需要多长时间啊
`01:33` 其实这里指的还是h one派
`01:35` 那我们要怎么回答这个问题呢
`01:37` 这里有个公式
`01:38` 能算出浮点运算次数是参数量的六倍
`01:41` 再乘以token数
`01:42` 我们可以去查一下h one hundred的规格表
`01:44` 看看它有多快啊
`01:46` 我们有个叫MFU的指标
`01:47` 取值为0.5
`01:49` 然后你就能估算出需要多少浮点运算次数
`01:52` 硬件每天能提供多少
`01:54` 最后就能算出需要多少天action
`01:56` 所以总共需要安排force3天好
`01:59` 接下来看另一个问题
`02:00` 如果用八块h one缀配合ADAMW
`02:03` 你能训练的最大模型有多大
`02:06` 你可以看一下h one hundred
`02:07` 配有80几个bytes的hp m内存
`02:11` 也就是每个参数占用的字节数嗯
`02:15` 加起来是two plus
`02:16` two plus four plus four啊
`02:18` 我们稍后解释这些数字是怎么来的
`02:21` 参数总数大约是531
`02:24` 这里有个注意点
`02:25` 我们不计算激活值
`02:27` 因为它取决于批次大小和序列长度
`02:30` 所以这些都是非常粗略的估算
`02:33` 但希望上完这门课
`02:34` 你们能明白这些数字是从哪儿来的
`02:37` 重点不是要把每个数字都算得精确
`02:40` 而是大概搞清楚整体情况啊
`02:43` 谢谢好
`02:45` 上次我讲了知识
`02:46` 以及你们能从这门课里学到什么嗯
`02:50` 机制也就是系统是怎么运行的
`02:53` 所以今天的内容会很简单直接啊
`02:56` 原理就是PYTORCH和张亮是怎么工作的
`02:59` 没什么花哨的
`03:00` 很直接啊
`03:01` 我想让大家建立一种思维
`03:03` 资源核算非常关键
`03:04` 希望大家养成习惯
`03:06` 每写一行代码都想想性能特性
`03:09` 最后是直觉
`03:10` 我们要大致了解一下资源有多少
`03:13` 以及他们是怎么被消耗的
`03:15` 我们既然不会讲机器学习里的魔法
`03:18` 这部分留给touch
`03:19` 下次可讲
`03:20` so咱们开始吧
`03:21` 从底层入手
`03:22` 层层往上搭建
`03:24` 底层是什么
`03:25` 是张亮
`03:25` 张亮是存储一切的基础单元
`03:28` 参数
`03:29` 梯度优化器状态数据激活值本质上都是张量
`03:33` 比如大家可以看看deep secups模型
`03:36` 你有看到模型本身其实是一堆不同的张量啊
`03:40` 每个张量都有各自的形状和进度
`03:42` 我后面会讲到我
`03:44` 你知道的张量包含了向量和矩阵
`03:47` 并且能推广到任意维度好
`03:50` 那我们来聊聊存储张量需要占用多少空间
`03:53` 这得看张亮的类型呃
`03:57` 所以一般来说我们处理的是存储浮点数的张量
`04:01` 不过张量也能存整数或其他类型嗯
`04:03` 说到浮点数啊
`04:04` 通常你提到float
`04:06` 我想大家指的都是标准的float32
`04:09` 所以float thirty two拆开来看
`04:11` 有32位
`04:13` 其中一位是符号位
`04:14` 八位是指数位
`04:15` 用来表示动态范围
`04:17` 剩下的是尾数或小数部分
`04:19` 用来表示数值的细微变化啊
`04:21` 这也被称为f p thirty two或单精度
`04:24` 单精度这个词的由来是早期做科学计算时
`04:27` float thirty two算是一种基准
`04:29` 就好比你预期的那样
`04:31` 如果有人给你一个浮点数
`04:32` 你会默认它至少是单晶
`04:35` 如果想要更高的精度
`04:36` 可以用双精度
`04:38` 也就是float64
`04:40` 但在深度学习中
`04:41` 我们走的方向恰恰相反
`04:43` 因为即便是32位精度也已经很高了
`04:46` 我们要做的这类计算
`04:47` 并不需要像某些数值模拟那样的高精度
`04:51` 好
`04:52` 在介绍其他类型之前
`04:53` 我们先来看float3C二
`04:55` 好的
`04:55` 那么我们来构建一个4×8的矩阵
`04:58` 默认情况下
`04:59` 你创建的张亮的类型是float32
`05:01` 所以如果你需要其他类型
`05:03` 应当显示声明
`05:04` 内存占用就是元素数量乘以元素大小
`05:07` 这里30诸位数的元素大小为四字节
`05:10` 因此总共是128字节啊
`05:13` 为了给你一个直观的概念
`05:15` 在g b three这个相对较旧的模型中
`05:18` 前馈层中的一个矩阵
`05:20` Train like extings
`05:22` And uki has hans
`05:23` Who are ping children sums
`05:25` 因此这些张量可能变得相当大
`05:28` 而这甚至还不是人们能想象到的最大尺寸啊
`05:31` 好的
`05:31` 既然我们关注效率
`05:33` 通常我们希望减少存储量
`05:35` 我们将看到随着精度的降低
`05:37` 你实际上既节省了内存
`05:39` 也节省了时间啊
`05:40` 因为在excel位上进行运算会更快
`05:43` 假设速度能快一倍
`05:44` 不过这不总是成立
`05:45` 具体还得看情况
`05:47` 还有通过降低内存需求
`05:49` 我们后面会看到
`05:50` 这其实也能节省时间
`05:52` 这点可能没那么明显
`05:54` 但希望后面能讲清楚
`05:55` 所以最直观的做法就是行
`05:58` 我们把位数砍掉一半
`06:01` 现在你用的是float sixteen
`06:02` 它的格式是一位符号位
`06:04` 五位指数
`06:05` 剩下的全是尾数嗯
`06:07` 所以好吧
`06:08` float to sixteen还不错
`06:10` 除了一点
`06:11` 它的动态范围比较差
`06:13` 所以就算你试着构造一个央译检测R的丈量
`06:18` 结果实际上就是零
`06:20` 所以没法表示特别大的数
`06:22` 也没法表示特别小的数
`06:24` 原因在于这怎么CONFEI就只有这么多位
`06:27` 你知道指数位只有五位
`06:29` 相比之下FPSERA是八位
`06:31` 所以如果你用fp sixteen训练以前
`06:35` 大家确实这么做
`06:36` 就会遇到不稳定性啊
`06:38` 出现下意上衣和NAM
`06:40` 这相当让人头疼
`06:41` 谢谢
`06:42` 于是b float sixteen被发明出来
`06:45` 它实际上是在托尼A7年开发的
`06:48` 就是为了解决这个问题
`06:49` 当时的思路是好
`06:51` 我们不在位数上做妥协
`06:53` 总位数和f p sixteen保持一致
`06:56` 但我们会把一部分位从尾数挪到指数
`06:59` 也就是说它的动态范围比浮点数更大
`07:03` 比如sixteen尾嗯
`07:05` 它的动态范围其实和float32是一样的
`07:08` 但分辨率确实更差了
`07:10` 毕竟天下没有免费的午餐啊
`07:13` 但事实证明
`07:14` 在很多深度学习应用里
`07:16` 这种取舍是值得的
`07:18` 你需要确保动态范围不会溢出或下溢
`07:22` 而且因为数据本身就比较粗糙
`07:25` 且带有随机性
`07:26` 你并不需要那么高的分辨率
`07:28` 好嗯
`07:30` 让我直接跳过这部分
`07:32` 好总结一下这对训练有什么影响
`07:34` 所以用thirty two位进度训练完全没问题
`07:38` 要是你训练的是个小模型
`07:39` 也不想操心这事
`07:41` 直接用flow third tire就行
`07:42` 不过每个参数得占四个字节的内存
`07:46` 这挺占内存的啊
`07:49` 就是用flow sixteen训练
`07:51` 风险有点大
`07:52` 所以BF46算是个比较平衡的选择
`07:55` 哪怕BF sixteen也有点风险
`07:57` 我们待会就知道了
`07:57` 谢谢现在大家普遍的做法是用混合精度训练
`08:03` 让我整理下这些点好
`08:05` 我再重新编译一次
`08:07` 这些看起来是旧版本
`08:09` 所谓混合精度训练就是让一部分计算
`08:12` 用一种精度
`08:12` 另一部分计算用另一种精度
`08:14` 一般来说BF sixteen用于参数激活值和梯度
`08:19` 而优化器状态则使用f p thirty two
`08:22` 这点我们后面再详细讲
`08:24` 也要启用混合精度训练
`08:25` 拍torch提供了一个amp库
`08:28` 我们不会过多讨论这一点
`08:30` 你基本上只需把代码包装一下
`08:32` PYTORCH的库会自动处理
`08:34` 在安全的情况下
`08:35` 他会把数据转成bf16
`08:37` 例如矩阵乘法通常是安全的
`08:39` 但如果你做指数运算
`08:41` 它会把数据保留为FP3.2
`08:44` 好我们可以用b f sixteen
`08:46` 我想这门课大概就讲到这里诶
`08:48` 但如果你想更深入探索
`08:50` 可以再往下走
`08:51` 所以FPA是4年前推出的
`08:54` 并且目前已经实现了标准化
`08:56` 所以FPA其实有两个版本
`08:59` 具体取决于你是更需要动态范围
`09:01` 还是更高的精度
`09:03` 我们就不深入讨论这两个版本了
`09:05` 不过NVIDIA的某些transform engine是支持FT8的
`09:10` 最近甚至可以用到f p four
`09:12` 去年NVIA推出了m v f p four
`09:14` 每个数值仅占四位
`09:16` 我们达成基本共识
`09:17` 四位的数据量很少
`09:19` 我可以在这里的一行里列出
`09:21` 从-6~-6的所有值比
`09:23` 所以这精度并不高
`09:25` 不过这里其实有个技巧对吧啊
`09:27` 因为如果你只是机械地直接使用这些值模型
`09:30` 是没法训练好的
`09:32` 也就是说虽然每个值只有四位
`09:35` 但数据是按块组织的
`09:36` 每个块可以相应地向上或向下缩放
`09:39` 所以虽然能表示的数值变多了
`09:42` 但每个数值都无法覆盖完整的动态范围
`09:46` 今年刚发布了一个叫neo trthree super的模型
`09:49` 它是用f p four训练的
`09:51` 我觉得挺酷的好的
`09:52` 有些内容你大概了解一下就行
`09:55` 还有些东西你根本碰不到
`09:56` 它不像你建一个张量调用接口
`09:58` 说这是f p four
`09:59` 它就真的变成f p so了
`10:01` 这些底层工作大多是由NVIDIA的软件站完成的
`10:04` 好刚才稍微扯远了一点
`10:06` 但这或许有助于你理解精度
`10:08` 这里面的门道好
`10:10` 在我继续之前有什么问题吗
`10:13` 请说不好意思
`10:15` 这是个提问
`10:17` 比如当你处理一个块时
`10:18` 你会对该块涉及的所有数值进行同样的缩放
`10:22` 所以这些数值之间的比例关系
`10:23` 在f p four下依然保持不变
`10:25` 对所以这么做就是为了填入最大值
`10:27` 从而获得更高的精度
`10:29` 对那这个问题问得好嗯
`10:31` 那我再解释一下这个快
`10:32` 假设你有一个快啊
`10:34` 在这个块里你可以在四位的范围内变化
`10:38` 另外所有这些值都可以根据缩放因子的倍数
`10:42` 进行上下缩放
`10:43` 所以如果你看单个数值
`10:45` 实际上得到的动态范围超过了四位
`10:48` 但问题是你不能让这个值极大
`10:51` 而相邻的值极小
`10:52` 好的这关于移位的问题嗯
`10:55` 那如果是一位呢
`10:56` 训练显然有一条单独的路径对对
`10:59` 所以这可能正好是个切入点
`11:01` 所以问题在于一位精度
`11:02` 因为实际上无法再低于这个了
`11:04` 这里需要区分训练和推理quantum
`11:07` 所以很多低位精度的内容
`11:09` 如果我们谈到量化
`11:10` 我想我们之后会讲到量化
`11:12` 量化是指你先以某个精度
`11:15` 甚至是bf16训练模型
`11:17` 然后将其量化到比如一位或两位等
`11:22` 这比训练一个一位的语言模型要容易得多
`11:25` 而后者我认为还没有人做过
`11:27` 也许这是可能的
`11:28` 但我认为还没有人在这方面
`11:30` 训练出可信的成果好
`11:31` 那我们继续
`11:32` 我们讨论了张量
`11:34` 内存计算相当简单
`11:35` 就是元素数量乘以每个元素占用的内存啊
`11:39` 所以默认情况下
`11:41` 你在拍拖之中创建的张亮会位于CPU上
`11:44` 还有当然如果你希望运行速度快
`11:47` 就需要把张亮移到GPU上
`11:49` 其实这里有个小问题
`11:51` 这些幻灯片是在我的笔记本电脑上跑的
`11:53` 所以没有GPU
`11:54` 因此有些代码我只展示不实际运行好
`11:58` 我们来看看啊
`12:00` 这部分可能没那么有意思
`12:02` 但我想大家都知道怎么操作
`12:05` 把张亮移到GPU上
`12:06` 但一定要记得这么做
`12:08` 否则就拿不到加速效果
`12:09` 好刚才我们讨论了张亮的内存
`12:11` 这部分很直接
`12:12` 现在我们来谈谈用张量做计算
`12:14` 在讨论张量操作的浮点运算次数之前
`12:17` 我们先稍微岔开一下
`12:18` 聊聊IOS
`12:20` 在座有多少人熟悉或用过IOS好
`12:23` 大概2/3啊
`12:24` 好的
`12:25` i obs的初衷是对于那些还没用过它的人
`12:28` 原生操作很容易搞错
`12:30` 这或者我觉得这种代码很难看懂
`12:33` 看到X和Y接着是转置里的二和负央
`12:37` 你根本搞不清这两个负数代表什么
`12:40` 所以这大概就是使用变量名而非索引的动机
`12:45` 所以IOS是一个用来处理维度
`12:47` 带有名称的张亮的库啊
`12:49` 这灵感来自爱因斯坦求和约定
`12:52` 有个不错的教程
`12:53` 大家可以看看
`12:54` 我只讲一些基础
`12:55` 基本上可以把ISAM理解为
`12:57` 一种带有良好索引管理的广义矩阵乘法不呃
`13:01` 举个例子
`13:02` 我有一个3×4的矩阵
`13:05` 我有一个速乘三的矩阵
`13:07` 如果做矩阵乘法
`13:08` 这其实是相当不错嗯
`13:11` 这很容易理解
`13:12` 在non los中
`13:13` 基本上你可以说X有两个维度
`13:15` pg和列
`13:16` 我将它们命名为seek one和hidden
`13:19` 然后我会有一个Y
`13:21` 它也是一个矩阵
`13:22` 包含所有行和列
`13:23` 我将它们命名为hidden和c two
`13:26` 我将生成一个张量或矩阵
`13:29` 其维度有seek one和seek two索引
`13:31` 任何未在此处提及的维度
`13:34` 比如hidden将被求和消去
`13:37` 所以其工作原理是
`13:38` 我会遍历此处出现的所有变量
`13:41` 即seek one
`13:42` hidden和sk two的所有可能取值
`13:45` 并求和
`13:46` 我基本上会对X和Y进行索引
`13:48` 将它们相乘
`13:49` 然后累加到结果Z的six one
`13:52` six two位置上
`13:53` 好的好
`13:55` 所以你说这是这可比刚才简单多了对吧
`13:58` 来我们看个更复杂的例子
`14:00` 还是刚才那个例子
`14:02` 现在我们有一个张量
`14:04` 它的形状是two x three x four
`14:06` 另一个张量也是RX3X4
`14:09` 如果我采用旧式做法
`14:12` 基本上我是在转至最后两个维度
`14:16` 然后由于影视地
`14:18` 对非矩阵乘法维度进行了批处理
`14:21` 你就得到了结果
`14:22` 懂了吗
`14:23` 你得稍微推理一下
`14:24` I ops
`14:25` 让这一点变得非常清晰
`14:27` 它明确标出了P处理维度
`14:29` Seek underscore
`14:30` One hidden
`14:31` Seek underscore
`14:32` Two hidden
`14:32` 而我只需要输出batch
`14:34` Seek underscore to
`14:36` 懂了吗
`14:37` 注意这里没有转制操作
`14:38` 因为某种意义上
`14:39` 我通过命名就实现了转制
`14:41` 假如命名里写的是hidden和seek to
`14:44` 那也无需转制
`14:45` 我老是被转制搞糊涂
`14:47` 所以不用考虑转制这件事
`14:49` 让我很开心行
`14:50` 那如果你想写的精简点儿
`14:52` 可以这么说
`14:53` 对于batch我就用省略号代替
`14:56` 也就是说假设我有个质为前的张量
`14:59` 有八个不同的批处理维度
`15:01` 我只要写省略号就行
`15:03` 不用一个个列出来
`15:04` 这在语言建模里很常见
`15:05` 因为你可能有批处理维度
`15:07` 序列维度
`15:08` 头维度
`15:08` 然后你想对这些维度都做矩阵运算
`15:11` 那你肯定不想光为这个发愁
`15:13` 好在你可以写模块化代码
`15:15` 用省略号表示
`15:16` 连输入张亮的形状都不用管好了
`15:19` 这就是i'm on some
`15:20` 它属于i'm on obs school
`15:22` 还有一个reduce操作
`15:24` 简单来说
`15:25` 它是均值最大值和最小值
`15:28` 这些操作的通用形式
`15:30` 比如假设你有这个张亮想对他求和依据
`15:34` dm match one这个维度
`15:36` 也就是说在这里沿着最后一个维度求和
`15:40` 所以我再次强调
`15:42` 我不太喜欢这种写法
`15:43` 但你可以使用一种叫做reduce的操作
`15:46` 简单来说
`15:46` 这里指的是存在某些批处理维度
`15:49` 在这个例子中就是前两个维度
`15:51` 然后还有一些隐藏维度
`15:52` 他们不会出现在等式右边
`15:54` 这意味着他们会被求和掉
`15:56` 这里我用了some表示聚合规约操作是求和
`15:59` 但你也可以换成mean max或M
`16:02` 这样做能带来加速吗
`16:04` 这本质上归结为同一种机缘操作
`16:06` 你可以把它仅仅看作一种语法糖啊
`16:09` 所以性能应该是一样的
`16:11` 谢谢最后我要讲的操作是rearrange
`16:14` 我觉得这个工具相当强大
`16:16` 作业里应该会考到这一点
`16:18` 有时候某个维度其实包含了两个维度的信息
`16:22` 你需要针对其中某一个进行处理
`16:25` 之所以会出现这种情况
`16:26` 是因为我们有时会把矩阵展平之后
`16:29` 你可能需要将其还原或再次展平
`16:32` 嗯它的工作原理是这样的
`16:34` 假设我有一个三乘R的矩阵
`16:36` 但这里的这个维度那个大小为八的维度
`16:40` 其实代表的是一个2×4的矩阵
`16:42` 所以我想把这个2×4的矩阵
`16:45` 和那个4×4的矩阵做乘法
`16:47` 所以我接下来要做的其实是调用rearrange嗯
`16:51` 我在这里的操作是声明看这是若干批次维度
`16:57` 在这里就对应第一个元素
`16:58` 然后我用括号来标记这一点
`17:01` 这里的H其实是head和hidden维度的乘积
`17:05` 就显然分解方式有很多种
`17:07` 比如top乘四或者42
`17:10` 所以我设head数量为two
`17:12` 这意味着hidden维度是四
`17:13` 然后我可以把它拆分成head和hidden这两个维度
`17:18` 好的
`17:18` 所以这就形成了这种结果
`17:20` 在此之前X的形状是这样的
`17:22` 而现在X变成了这样
`17:24` 所以我想这可能会有点难看清
`17:27` 然后你就可以对W进行变换操作了
`17:31` 嗯这就是我们之前见过的情况
`17:34` 执行标准矩阵乘法
`17:36` 其中X带有一些P处理维度
`17:39` 也就是隐藏层乘以隐藏层二
`17:42` 接着完成变换后
`17:44` 你就能把它重新排回去
`17:45` 这很直接
`17:46` 你主要看两个维度
`17:48` 然后把它们合并成一个维度
`17:50` 好对你是能把二维的东西转成一维
`17:53` 还是说有两种做法
`17:54` 我觉得有你可以按散优先或列优先来做对
`17:57` 所以问题是你有一个一维的东西并转换它
`18:00` 抱歉是你有一个二维的东西并转换为一维
`18:03` 你按哪种方式做嗯
`18:06` 执行顺序由这里的order指定诶好的
`18:08` 我是说刚开始适应它确实要花点时间
`18:12` 但很值得
`18:13` 因为一旦用上ANNOS
`18:14` 你的思维方式就会变得不一样
`18:16` 诶啊还有那些转制和规约操作啊
`18:20` 所有这些处理起来都会变得更顺畅对吧
`18:22` 你必须深入思考这类更为定制的机缘
`18:25` 好的那么好
`18:26` 我们接下来要用IOS
`18:28` 我们稍后再细看
`18:30` 现在让我们回到资源核算的问题上
`18:33` 所以我有了张量
`18:34` 我们已经讨论过他们如何占用内存
`18:36` 那么他们需要多少计算量呢
`18:39` 四好的
`18:41` 那么我们将用来衡量计算成本的指标
`18:44` 是浮点运算次数
`18:46` 一次浮点运算就是一次浮点操作
`18:48` 我们假设它是加法或乘法这样的基本运算
`18:51` 那有GPU还能做其他一些操作
`18:54` 但在大多数情况下
`18:55` 我们将忽略它们
`18:56` 因为加法和乘法才是核心
`18:58` 会占用你大部分的时间
`19:00` 所以有件让我有点介意的事是
`19:02` 如果我说flops这个词
`19:05` 它的含义其实是有歧义的
`19:07` 一种是FLOS指浮点运算次数
`19:10` 通常写作小写S的flops
`19:13` 这是衡量计算量的指标
`19:15` 另一种也是flops
`19:17` 指每秒浮点运算次数
`19:19` 有时它也会很容易混淆的写成大写S的FLOS
`19:22` 但我会一律写成LSA
`19:25` C已明确表示这是在衡量硬件速度
`19:28` 所以如果你去查会发现H100有989啊
`19:33` 每秒泰拉flops
`19:34` 而当我说g p t three消耗了300000000twenty5
`19:37` 或者类似数量的flops时
`19:39` 指的是前者flops
`19:40` 先把这个讲清楚嗯
`19:42` 给你一个数量级的概念
`19:44` 当我提到ee tone two tt3或25的flops时
`19:49` 这些隐含指的是计算量或者这些模型的规模
`19:53` 好的
`19:54` 所以如果你看h one hundred啊
`19:56` 如果你查看这份精美的规格说明书
`20:00` 实际上它并不在这一页
`20:02` 呃好吧算了
`20:03` 有一份规格说明书
`20:04` 它会告诉你
`20:05` bf16下每秒的浮点运算次数是多少
`20:09` Nineteen
`20:09` Seven nine
`20:10` 然后你去进行基准测试
`20:12` 结果发现等等
`20:13` 这和我实际得到的数据不符
`20:16` 然后你去阅读细则会看到一条脚注
`20:19` 说这是基于稀疏性的
`20:21` 即稀疏矩阵
`20:23` 而稠密矩阵则要除以二
`20:25` 嗯好的
`20:25` 所以你必须总是将这些数值除以二
`20:28` 这就是为什么你会看到这些除以二的情况
`20:31` 嗯好的
`20:32` 所以呃所以呃这有助于我们建立一种直觉
`20:38` 所以如果你有八张h one hundred
`20:40` 这就是一个节点
`20:41` 持续两周
`20:42` 那就是你知道的8×2周内的描述
`20:45` 嗯实际上这看起来是一周行
`20:48` 没问题
`20:48` 也就是一周乘以每秒的浮点运算次数
`20:52` 所以大约是5E21
`20:55` 这主要是为了帮你建立一种直觉
`20:58` 了解特定硬件能提供多少浮点运算次数
`21:01` 以及特定模型需要消耗多少浮点运算次数啊
`21:05` 没什么复杂的
`21:06` 就是数学粗略估算一下好
`21:08` 那现在我们来做一些更机械的计算啊
`21:11` 假设你有一个线性模型
`21:13` 事实证明很多关于浮点运算次数的计算
`21:16` 核心上其实就是线性矩阵乘法啊
`21:19` 所以这其实并不是一般性
`21:20` 这里有若干端点
`21:21` 每个点都是低维的
`21:22` 我们要进行映射
`21:24` 把这些D维向量中的每一个映射成K为的
`21:27` 输出好
`21:29` B是点的数量
`21:30` D是输入维度数
`21:31` K是输出维度数
`21:33` 好
`21:33` 我们来构建一个X也就是数据矩阵
`21:35` 维度是B乘D啊
`21:37` 权重矩阵是D乘K当你做矩阵乘法时
`21:41` 关键问题是这涉及多少浮点运算次数
`21:45` 结果发现这其实是两倍那么多啊
`21:49` 基本上就是这三个维度相乘
`21:52` 要理解这一点
`21:52` 我们可以这样看
`21:54` 每个三元组都要做一次乘法
`21:56` 然后再做一次加法好了
`21:58` 这里有一
`21:59` 因为如果不提问
`22:00` 你就得实际加上类似的东西啊
`22:04` 这种情况下是D1-1乘以
`22:06` 不过咱们先忽略它
`22:07` 好的
`22:07` 如果觉得讲的太快
`22:08` 我会再回来讲啊
`22:10` 我觉得还有另一种推导方式
`22:12` 谢谢
`22:13` 那其他操作的浮点运算次数是多少呢
`22:16` 是
`22:19` 逐元素操作的浮点运算次数就等于矩阵的大小
`22:23` 我想这很清楚
`22:24` 所以加法也需要NN次浮点运算
`22:27` 所以一般来说对于足够大的矩阵
`22:29` 你遇到的任何其他操作的开销
`22:32` 都不如矩阵乘法大
`22:34` 所以一般来说我们只关注矩阵乘法在做什么
`22:38` 但在讨论内存时有一个重要的例外
`22:41` 对哦就像呃纯粹出于兴趣呃
`22:43` 其实还有一些其他算法可以用来做矩阵乘法
`22:45` 对
`22:46` 这是否只适用于你知道的常规做法
`22:49` 也就是说存在其他进行矩阵乘法的算法
`22:53` 意思呃就是像磁立方算法那样的呃
`22:56` 所以一般来说
`22:57` 人们从矩阵乘法中探索的优化算法
`23:04` 而不是这些呀更偏渐进性的算法算法
`23:07` 对对如果把加法和乘法同等看待
`23:11` 难道加法不能比乘法执行的更高效吗
`23:13` 对所以我认为从硬件构建的方式来看
`23:17` 两者基本上是一样的
`23:18` 但确实直觉上感觉加法应该比乘法做得更快
`23:22` 从硬件构建的方式来看
`23:24` 它们的计算开销是一样的
`23:26` 对好
`23:28` 你可以把这次矩阵乘法理解为
`23:30` B是数据点的数量
`23:32` 而DK就是参数的数量对吧
`23:36` 记住X的维度是B乘D
`23:38` W的维度是D乘KMOR
`23:41` 所以理解这个公式的另一种方式是
`23:43` 完成这个线性矩阵前向传播
`23:46` 所需的浮点运算次数
`23:48` 实际上是token数量
`23:50` 或者说数据点数量乘以参数数量好
`23:54` 事实上这一结论可以推广到transformer
`23:57` 如果你还记得6ND公式
`23:59` 就能看出其结构是如何形成的
`24:01` 好遗憾的是
`24:02` 这些计算结果意义不大
`24:04` 因为我是在CPU上跑的
`24:06` 不过我还是在这里把代码过一遍
`24:08` 到目前为止
`24:09` 我们测量的是浮点运算次数
`24:13` 这与硬件无关
`24:14` 它就是你的模型需要完成的计算量
`24:17` 谢谢
`24:18` 那么现在的问题是在硬件上实际跑一次
`24:21` 需要多长时间
`24:22` 所以一种办法就是直接计时好
`24:25` 在这门课里
`24:25` 我想我们会用即将来深入讨论基准测试诶
`24:28` 啊不过先做个小预告
`24:30` 通常来说啊
`24:32` 计时时啊
`24:33` 尤其是在GPU上
`24:35` 你得调用quer synchronize
`24:37` 因为GPU是异步运行的
`24:38` 要确保有这种同步点啊
`24:41` 然后你执行操作操作之后
`24:43` 你也必须设置同步屏障
`24:45` 如果你省略这一步
`24:46` 你会发现哇你的计时结果非常快
`24:49` 这是因为这是一个非阻塞调用
`24:51` 它直接返回了
`24:52` 通常来说多跑几次
`24:54` 取平均值是个好习惯哦
`24:56` 所以所以你所以实际的每秒浮点运算次数是嗯
`25:02` 基本上是你执行的浮点运算次数
`25:06` 再乘以你在硬件上记录的时间对吧
`25:12` 所以别忘了还有一个数据在GPU的规格表里
`25:15` 我试试能不能把这个链接调出来
`25:17` 你看呃我好像放错链接了
`25:20` 我得课后改一下
`25:22` 他本来会给出这个数字的嗯
`25:25` 一个浮点运算次数是989t f ops
`25:29` 所以实际每秒浮点运算次数
`25:32` 通常和标称的每秒浮点运算次数不一样
`25:36` 好啊还有行嗯
`25:40` 唉呀抱歉
`25:41` 理解这种差异的一个概念叫做模型
`25:44` 浮点运算利用率
`25:45` 简称MFMFU的定义
`25:48` 就是实际每秒浮点运算次数
`25:51` 除以标称的每秒浮点运算次数
`25:54` 这我们暂且忽略了通信和其他开销好
`25:58` 简单来说就是用实际值除以标称值
`26:01` 一般来说你很少能超过标称值
`26:03` 我是说就是这样
`26:05` 没错你不可能超过标称值
`26:06` 通常结果会更低
`26:08` 一般来说
`26:09` 如果现代模型的MFU能达到0.5左右
`26:12` 你就该挺满意了
`26:14` 如果你只是做纯粹的矩阵乘法
`26:17` 可能能达到0.8甚至更高
`26:19` 但通常达不到那么高
`26:21` 有时候如果真的出了大问题
`26:22` 你只能拿到0.1左右
`26:24` 这就说明你得做点什么了啊
`26:26` 好写写好
`26:28` 所以每当你编写模型时
`26:30` 你都可以计算
`26:31` 现在你已经知道如何通过统计模型所需的逻辑
`26:35` 浮点运算次数来计算MFU
`26:37` 然后查看实际耗时
`26:39` 本质上就是做个除法
`26:41` 还有好几票老板好
`26:45` Why you go to the next slide should
`26:49` 那边有人提问吗
`26:50` 哦我要翻到下一页幻灯片呀
`26:52` 所谓的承诺浮点运算次数是多少
`26:55` 在规格表里它已经是nineteen dh9
`26:58` 这个数除以二之后的结果了
`27:01` 此外通常你也只能拿到其中约一半
`27:04` 具体取决于你的计算
`27:06` 谢谢你得到的是50%对吗
`27:09` 是的
`27:10` 那么为什么你只能得到50%的MFU嗯
`27:12` 这确实是个好问题
`27:14` 当量远到内存瓶颈时
`27:15` 我会再提这点好总结一下
`27:17` 矩阵乘法通常是计算的主力
`27:20` 这其实是特意设计的
`27:22` 嗯嗯所以啊每秒能跑多少
`27:27` 浮点运算次数取决于硬件
`27:29` 所以硬件越好
`27:30` 浮点运算次数越高
`27:32` 但这还取决于数据类型
`27:34` 也就是说如果你看规格表
`27:36` 会发现
`27:36` 不同数据类型的浮点运算次数是不一样的
`27:39` 要是现在还用flow thirty two
`27:41` 速度会非常慢
`27:42` 因为厂商根本没针对这种负载做优化
`27:45` 而现在用ba fox3或者FP8会快得多好
`27:48` 现在你该知道MFU是什么了
`27:50` 它就是实际浮点运算次数
`27:52` 除以理论承诺的浮点运算次数
`27:56` 谢谢好
`27:57` 我们回到MFU
`27:59` 为什么是0.5
`28:00` 这个问题
`28:00` 要理解这一点
`28:01` 我得先引入算术强度这个概念
`28:04` 原因在于事情没那么简单
`28:07` 不只是做一堆矩阵乘法就完事
`28:10` 然后看看矩阵乘法花了多长时间
`28:12` 这是我画的一个非常简化的硬件示意图
`28:15` 你有HBM
`28:17` 然后是计算核心所在的位置
`28:20` 也就是加速器芯片的位置
`28:22` 那么计算是怎么进行的呢
`28:24` 嗯你得自己动手算一下参议员
`28:26` 你的输入矩阵和张量都待在这里
`28:28` 从内存流向加速器
`28:30` 你做完计算再把结果传回去
`28:33` 好的
`28:33` 那么如果你想衡量这要花多久
`28:36` 这主要取决于两点
`28:37` 第一是加速器的速度
`28:39` 也就是我们刚才聊过的
`28:40` 你知道具体指什么
`28:42` 但另一个关键因素是硬件的内存带宽
`28:45` 这点我们还没聊过啊
`28:47` 如果你看规格表
`28:48` 我们之前提过每秒浮点运算次数是多少
`28:52` One point nine
`28:53` Seven
`28:53` Nine times
`28:54` ten twelve除以二
`28:56` 再看每秒字结束
`28:57` 也就是内存带宽
`28:58` 这是每秒3.3TUR
`29:00` 好的
`29:01` 还要记住我们为什么要关注内存存储
`29:05` 这些数据需要多少空间
`29:07` 最明显的一点是
`29:09` 如果你的模型太大
`29:10` 它就放不下你的内存
`29:12` 那可不是什么愉快的体验
`29:15` 而且事实证明内存这边也有讲究
`29:19` 你需要移动这些数据
`29:20` 这需要时间
`29:21` 因此实际上数据规模的大小也会影响速度
`29:25` 好的
`29:25` 接下来我来讲讲一些运算
`29:28` 我基本上会计算所需的时间
`29:31` 并引入算术强度这一概念
`29:33` 好的
`29:34` 假设我有一个100万维的bf16向量
`29:37` 我将对其执行real u计算
`29:39` 请记住
`29:39` Real ue
`29:40` 就是对整个向量逐元素
`29:42` 进行X与零的最大值运算
`29:44` 所以我要算两件事
`29:46` 第一是移动了多少字节嗯
`29:48` 写中文啊
`29:49` 所以我得先读取X然后把它拷贝到加速器里
`29:54` 加速器每一份都是two字节
`29:56` 因为b f sixteen每个浮点数占两个字节
`29:59` 再乘以N个浮点数
`30:01` 所以这部分是two n
`30:02` 接下来我要把Y写回去
`30:04` 所以这里又是2N好
`30:06` 这就是需要移动的字节数量
`30:08` 那么完成了多少浮点运算次数呃
`30:12` 对于其中的每个元素
`30:13` 我只是把它和零做个比较
`30:15` 就这些
`30:16` 所以这是N次比较嗯好的
`30:20` 所以现在来看通信时间
`30:22` 也就是字节数量
`30:23` 也就是我移动的距离除以移动速度
`30:27` 这样算出来时间是要微秒
`30:29` 那计算时间呢用浮点运算次数除以每秒
`30:32` 浮点运算次数
`30:34` 所以是万纳秒好
`30:37` 此外还有一个重要假设
`30:39` 我们通常会尽量满足呢
`30:40` 就是我们让通信和计算重叠进行
`30:43` 等后面深入讲GPU时再详细讨论
`30:47` 其核心思想是在这种情况下
`30:49` 我们不会坐等数据传输完成
`30:52` 数据一到就开始计算
`30:54` 紧接着继续往下走
`30:57` 再把数据传回去
`30:58` 这样数据传输和计算就同时进行了
`31:01` 所以从数学上讲
`31:03` 我们就假设总时间是取两者的最大值的
`31:07` 因为我们假设这两部分可以完美重叠
`31:09` 但在实际中重叠不可能那么完美
`31:11` 会有一些开销
`31:12` 但目前这样已经够用了好
`31:14` 所以总时间大家看这里是11-6好
`31:19` 那如果问瓶颈在哪
`31:20` 当通信时间大于计算时间时
`31:23` 我们就称之为算法内存受限
`31:25` 因为你大部分时间都在等数据过来
`31:27` 而当计算时间大于通信时间时
`31:29` 那就是计算受限
`31:31` 因为这时候你的瓶颈其实就在于做计算对啊
`31:33` 那么在这种情况下
`31:34` real6是什么
`31:36` 谢谢哦抱歉
`31:38` memory bound是内存受限还是计算受限
`31:41` 内存受限之后的内存受限
`31:43` 对这很明显对吧
`31:45` 因为计算量远小于通信量好换个角度来看
`31:49` 这里我要定义强度
`31:52` 那么加速器的强度是什么
`31:54` 它本质上是指加速器每传输一个字节
`31:58` 能完成多少工作量
`32:00` 对于任何加速器
`32:01` 从规格表上
`32:02` 你基本上能拿到每秒浮点运算次数
`32:04` 除以每秒的字节数好
`32:07` 那么每搬运一个字节
`32:09` 你能做多少有用的计算
`32:11` 对于h one hundred
`32:13` 这个数值是二小五
`32:15` 也就是说每传输一个字节的数据
`32:19` 可以执行295次
`32:21` 就能进行295次浮点运算
`32:23` 好的
`32:23` 这是个让你心里有数的直观数字
`32:25` 大概300左右
`32:26` 好的
`32:27` 接下来讲算术强度
`32:29` 它衡量的是算法在处理这项任务时
`32:32` 每字节数据背后实际做了多少计算
`32:36` 以RAVENUE计算为例
`32:38` 它的算术强度就是浮点运算次数除以字节数
`32:41` 实际上这个数值我想是1/4
`32:45` 而不是1/2
`32:46` 所以关键在于它非常小
`32:47` 只有浓缩圈好了
`32:49` 现在我们可以用强度这个概念来讨论瓶颈问题
`32:52` 如果算术强度低于加速器的临界值
`32:55` 就是内存受限
`32:57` 如果高于这个值就是计算受限
`33:00` 所以这两者是等价的
`33:02` 你看一下代数推导
`33:03` 其实就是两个分数
`33:04` 通过乘除把项的位置换一下好
`33:07` 那这种情况呢嗯你知道我们是显存受限的
`33:13` 所以一般来说
`33:15` 我们会发现自己处于显存受限的状态
`33:17` 因为数据搬运的代价很高
`33:20` 所以如果能提高算术强度
`33:22` 那就更好了好
`33:24` 所以零点two啊
`33:26` 如果你看到这个数
`33:27` 或者别人告诉你
`33:28` 你的算术强度是0.25
`33:30` 你就该意识到这真的很糟糕
`33:32` 那典型的算术强度大概是多少呢
`33:35` 对我们待会儿会讲到好
`33:36` 那么提升算术强度的一种思路是
`33:40` 我们试着在每搬运一个字节的数据时
`33:44` 多做一些计算
`33:45` 好JU是另一种激活函数嗯
`33:48` 它的公式大概长这样
`33:49` 看起来更复杂一些
`33:51` 里面没有零
`33:51` 要是算一下的话
`33:53` 来回搬运的字节数还是2N加2N
`33:56` 再算浮点运算次数
`33:57` 对于大规模的逐元素U型运算
`34:00` 每个元素大概要20次浮点运算
`34:02` 所以总共是20n and
`34:04` 所以算术强度大概是五
`34:06` 这只是一个粗略的估算啊啊在这种情况下
`34:10` 我们是内存受限还是计算受限对吧
`34:15` 因为五还是比295小小得多
`34:17` 所以虽然GU做的计算比real ue多得多
`34:21` 但按目前的结构来看
`34:23` 它仍然是内存受限的对
`34:27` 也就是说如果你只看value和value
`34:29` 你可能会觉得GU那么复杂
`34:31` 开销一定很大
`34:32` 但实际上两者开销完全一样
`34:34` thank you对
`34:36` 因为瓶颈并不在那里
`34:40` 谢谢好
`34:40` 接下来我们看几个线性运算
`34:42` 比如点击好点击
`34:44` 假设你有一个向量X和一个大小为N的向量W
`34:47` 然后计算它们的点击
`34:49` 那么数据移动量是多少
`34:51` 读取X乘2N字节
`34:53` 读取W2N字节
`34:55` 然后写入Y1个二字节的标量
`34:58` 那浮点运算次数呢
`34:59` 你做了N次乘法和N减一次加法
`35:02` 总共是2N减一次好呢
`35:05` 算数强度是多少哦
`35:07` 这个大约是精秒五
`35:08` 这也相当低
`35:09` 希望你明白了
`35:10` 它属于内存受限型计算
`35:13` 谢谢好
`35:16` 那矩阵向量乘法呢好Y等于X与W的乘积
`35:21` 其中X是向量
`35:23` W是一个M乘N的矩阵
`35:25` 然后你计算这个乘积
`35:27` 大家觉得这会是计算瓶颈还是内存瓶颈
`35:30` 举手示意一下好
`35:32` 我们来看我要读取XTM大小2N
`35:36` 读取WRVR大小2N的平方
`35:38` 然后写入Y大小2N
`35:41` 浮点运算次数基本上就是N次点积的开销
`35:45` 所以总成本是N乘以一次点G的开销
`35:49` 算术强度也只是略高一点嗯
`35:51` 所以这同样是内存瓶颈
`35:54` 好现在我们来讨论矩阵乘法
`35:57` 这才是有意思的地方
`35:58` 我有一个N乘N的矩阵
`36:00` 再乘以另一个N乘N的矩阵
`36:01` 传输的数据量大约是2N的平方
`36:04` 加上2N的平方字节
`36:06` 然后我必须把QN的平方字节写回Y
`36:10` 这里的浮点运算次数是N的平方
`36:13` 差点积对
`36:15` 那算术强度是多少
`36:17` 是300
`36:17` 太棒了
`36:18` 好的
`36:19` 所以是seven four
`36:20` 一般来说它大约是3/3
`36:24` 直觉上这很合理
`36:25` 因为你传输的是N的平方量级的数据
`36:28` 但运算量却是N的立方
`36:31` 所以计算量相对于传输量的比例是N的量级
`36:35` 谢谢而且矩阵越大效果越好
`36:40` and then要run tire remnation后
`36:42` myself要增大批量或使用更大的矩阵
`36:45` 原因就在这里
`36:46` 如果你低于加速器的算术强度
`36:49` 那么缩小规模实际上并不能加速运算啊
`36:53` 情况都差不多
`36:54` 但一旦你超过这个点
`36:55` 你实际上就已经处于饱和状态了
`36:58` G还是GP好
`36:59` 所以到这一步终于是计算密集型了
`37:01` 那好所以只要有大型矩阵
`37:04` 我们的效率其实挺高的
`37:06` 我们处于计算密集型状态
`37:08` 加速器已经满载
`37:10` 那之前提到的transformer问题呢
`37:12` 是证明你们在作业和下一讲中都会看到
`37:15` transformer实际上是本质上就是大型矩阵乘法
`37:19` 中间穿插了一些其他操作
`37:22` 所以从算术强度来看
`37:24` 这是个好消息
`37:25` 这是经过刻意设计的
`37:27` transformer的结构
`37:29` 经过特定设计
`37:30` 以实现高算术强度
`37:31` 这里提一点
`37:32` 为接下来的推理讲座做个铺垫
`37:34` 矩阵向量乘法
`37:36` 本质上大致就是你做transformer
`37:38` 推理时发生的事情
`37:39` 因为推理时你是一次生成一个token
`37:42` 所以这就像是用一个向量与矩阵做点基
`37:46` 正如我们所见
`37:47` 这受限于内存带宽
`37:49` 而在训练时
`37:50` 你会获得整个序列并一次性进行处理好的
`37:54` 此外请注意计算强度取决于精度
`37:57` 因此这里我们默认使用的都是BF16嗯
`38:00` 你好的
`38:02` 那么把这一点和之前关于MFU的问题联系起来
`38:06` 你MFU较低的原因可能是
`38:08` 虽然你看似在做事
`38:10` MFU是承诺浮点运算次数啊
`38:13` 抱歉是实际值除以承诺值
`38:15` 对对对
`38:15` 因此如果你存在严重的内存带宽瓶颈
`38:18` 那么实际上你无法获得理想的吞吐量
`38:21` 尽管承诺的前提是
`38:22` 如果没有内存瓶颈
`38:24` 你本来能一路把所有这些都做完
`38:26` Thank you
`38:27` Thank you
`38:28` 模型的运算
`38:30` 谢谢谢谢好
`38:31` 关于算术强度
`38:32` 最后讲一下屋顶线图
`38:34` 所以有个挺好的办法对吧
`38:36` 我再重复一遍
`38:38` 一般来说
`38:39` 正如大家所说
`38:40` 这些模型在大部分计算中都是受内存限制的
`38:44` 不过如果加速器利用率只有50分三
`38:48` 那就不太理想
`38:49` 要是能到70%甚至80%
`38:51` 那就相当不错了啊
`38:54` 这意味着加速器的规模相对于内存来说过大了
`38:58` 我待会儿会再讲这个问题嗯
`39:00` 为什么会这样
`39:01` 为什么这些加速器做得这么大
`39:03` 结果只是在那闲置或等待内存
`39:06` 所以问题是
`39:07` 我们能不能设计出特性更好的加速器
`39:10` 对也许等我们讨论完GPU
`39:12` 对他们的工作原理有更多了解之后
`39:15` 就能解释为什么会这样了
`39:17` 嗯不过如果你有答案
`39:19` 不妨告诉JENSEN
`39:20` 说不定你还能设计出更好的硬件次好
`39:24` 我们来直观的看看算术强度和性能之间的关系
`39:28` 和这个图基本上是把算术强度画在了X轴上
`39:32` 所以这里每一个垂直切片都对应着
`39:35` 比如说某一种算法
`39:37` 然后我们有这些线
`39:39` 每条线对应一种特定的加速器
`39:42` 比如h one i to b摇摆
`39:44` 因此嗯它展示的是在Y轴上
`39:49` 实际达到的每秒浮点运算次数
`39:52` 所以如果你的算法算术强度较低
`39:55` 比如value或点击
`39:57` 你就处在这个区域
`39:58` 这意味着实际达到的每秒浮点运算次数
`40:02` 会低于加速器的峰值性能
`40:04` 而当你提升内存带宽
`40:07` 增加算术强度时
`40:09` 情况会逐步好转
`40:10` 在达到某个临界点之前
`40:12` 你都能充分利用硬件性能
`40:14` 超过那个点之后
`40:15` 计算就成为瓶颈
`40:17` 显然你无法超过峰值
`40:18` 浮点运算次数好的那我们继续往下讲好
`40:21` 谢谢呃
`40:23` 接下来我要回过头来谈谈内存和计算
`40:26` 也就是我们在训练中需要的那些操作
`40:30` 到目前为止我们做的是张亮操作
`40:33` 基本上就是矩阵乘法
`40:35` 我们看到了它需要多少内存
`40:37` 多少计算
`40:38` 以及两者之间的相互作用
`40:40` 现在我们来具体看看训练到底需要什么
`40:44` 就拿我们刚才那个例子来说吧
`40:46` 啊其实这不是线性网络
`40:48` 而是深度网络
`40:49` 所以我设想这样一个场景
`40:51` 输入是B乘D的矩阵
`40:53` 然后经过若干层
`40:55` 每一层都做D乘D的矩阵乘法路径
`41:00` 然后生成一些B组预激活值
`41:02` 接着做逐元素的value运算
`41:05` 得到第一层的激活值
`41:06` 然后这个过程不断重复
`41:09` 好的输出尺寸保持不变好的
`41:12` 所以这个深度网络呃
`41:14` 为了看看它在PYTORCH里长什么样啊
`41:17` 基本上就是一组模块
`41:19` 每个模块都有一个与之关联的权重向量
`41:22` 抱歉是权重矩阵
`41:23` 好的啊
`41:25` 所以这里的参数量是D的平方乘以层数L
`41:30` 帮你在一个数据批次上运行模型时啊
`41:33` 我们做的是逐层处理
`41:35` 每一层我们基本上先做线性变换
`41:38` 然后再做一个逐点运算啊
`41:40` 使用value激活函数
`41:42` 然后对所有层都这么做好希望这也很清楚
`41:46` 这是一个非常简单的模型
`41:48` 好现在我们来谈谈梯度
`41:50` 所以我们用一个更简单的例子啊
`41:53` 这就是一个简单的线性回归模型
`41:56` 我们有一个向量one two three
`41:58` 一个权重向量one one one
`42:01` 我们计算点击
`42:02` 然后构建这个均方误差
`42:04` MCE损失还能当我们执行反向传播时
`42:08` 计算图中涉及的每个变量
`42:10` 它的梯度要么被赋值
`42:11` 要么没被赋值
`42:13` 所以w grad被赋值为one two three好
`42:17` 这只是PYTORCH的基本机制
`42:19` 这对大家来说应该都很熟悉
`42:21` 那么现在的问题是
`42:22` 计算梯度需要多少浮点运算次数好
`42:26` 我们来计算梯度所需的浮点运算次数
`42:28` 我们考虑一个简化模型
`42:30` 其中你有啊一个B乘D的X乘以w one矩阵
`42:36` 为简化起见
`42:37` 我们暂时忽略这个value再乘以w two
`42:40` 它是一个形状相同的D乘递矩阵好
`42:43` 出于后面会讲清楚的原因
`42:45` 这里我使用ie some
`42:46` 在这个线性网络中
`42:48` 你做的第一件事是取出X和WVN
`42:50` X是P4大小乘输入维度
`42:53` W1是输入维度乘输出维度
`42:56` 得到的是批次大小乘输出维度
`42:58` h two取h one和w two情况大致相同
`43:03` S这些只是矩阵乘法
`43:06` 然后你形成一个损失
`43:09` 我只是把它传给某个任意操作
`43:11` 以便得到一个数值
`43:12` 好的
`43:13` 那么在反向传播过程中会发生什么嗯
`43:17` 为了调试方便
`43:19` 我把这些叫做保留梯度
`43:20` 你后面会看到啊
`43:22` 你对损失执行反向传播问题是那做了多少工作
`43:26` 也就是说那是多少浮点运算次数
`43:29` 好的
`43:29` 让我们放大查看一层
`43:31` 让我们专注于这里的第二层及这一层嗯
`43:34` 第二层接收h one
`43:35` 然后直接乘以矩阵w two得到h two
`43:38` 就这样
`43:39` 所以如果你看前向传播里的浮点运算次数
`43:42` 这仅仅是个矩阵乘法ATHER
`43:44` 我们只需要把这三个维度乘在一起
`43:47` b f sixteen占两个字节
`43:49` 抱歉这个细节不重要
`43:50` 这里算两次
`43:51` 因为包含一次加法和一次乘法
`43:53` 好的那反向传播阶段是怎样的呢
`43:57` 好的
`43:58` 在反向传播中
`43:59` 如果你还记得链式法则和反向传播算法
`44:02` 你需要计算两个量
`44:04` 你需要计算反向信号
`44:06` 也就是损失函数相对于输入的梯度
`44:10` 同时你还需要计算相对于参数的梯度
`44:13` 清楚吗
`44:14` 这些梯度的具体形式是什么
`44:16` 这直接由定义得出嗯
`44:18` 这其实就是用爱因斯坦求和符号写出的
`44:21` 链式法则
`44:22` 你计算损失对h two的导数
`44:26` DHH也就是h two的梯度
`44:29` 这是一个P大小乘以输出维度的矩阵
`44:33` 然后你把它乘以WR
`44:34` and w two是输入维度乘以输出维度
`44:38` 接着这就得到了h one的梯度
`44:41` 也就是啊损失对h one的导数
`44:44` DHR维度是P大小乘输入维度好
`44:48` 我每次看到这个总会搞混
`44:49` 如果你学过微积分里面会出现一个转置
`44:52` 我总是记不清他们该按什么顺序排
`44:54` 我觉得爱因斯坦的技法把这件事讲得很清楚
`44:57` 因为只要你记得标量情形下它应该是什么样子
`45:00` 就知道它必须长成那样
`45:02` h one的梯度就是h two的梯度乘以w two
`45:05` 唯一的问题是你怎么给这些元素加索引
`45:09` 你基本上是通过看这些张亮的形状和命名维度
`45:12` 来给它们加索引的
`45:13` 这种情况下
`45:14` 它就是一个矩阵乘法
`45:16` 你在输出维度上做求和行
`45:19` 照这么操作的话
`45:20` 我们就能验证一下
`45:21` 所以h one dug grad是lost backwards算出来的
`45:24` 这跟我在这儿手写的结果是一样的
`45:26` 就当做个sanity check吧
`45:28` 好那你还得算另一件事
`45:31` 对参数的梯度
`45:32` 也就是d underscore d HT h r乘以HY
`45:37` 所以这其实是同一种反向传播信号
`45:40` 只不过它是乘上了你没求导的那部分
`45:44` 你知道就是对应的那个梯度
`45:46` 你直接把维度列出来就行
`45:48` 这里你要对batch维度求和
`45:50` 谢谢好
`45:51` 那么呃好处在于
`45:54` 如果你只看这里
`45:55` 我们就知道它的计算成本了
`45:57` 浮点运算次数基本上就是所有维度相乘的结果
`46:01` Gregate
`46:02` 不管你有没有做batch
`46:03` 这都不影响
`46:04` 本质上你就是在便利IJK并进行聚合
`46:09` 聚合方式不同
`46:10` 但浮点运算次数是相同的
`46:13` 请注意
`46:14` 反向传播的开销恰好是前向传播的两倍啊
`46:19` 原因在于你需要计算两类梯度
`46:21` 一类是针对每个参数的梯度
`46:24` 另一类是关于输入的梯度
`46:26` 也就是那个非参数的部分
`46:29` 好也许我该停一下
`46:31` 看看有没有问题
`46:32` 天哪谢谢谢谢
`46:34` 嗯好好
`46:36` 那么刚才讲的只是针对w to的情况
`46:39` 你只需要把这套方法
`46:41` 应用到网络中的所有参数上
`46:43` 然后汇总起来就行
`46:44` 你会发现对于该网络前向传播的计算量是两倍
`46:51` 即数据点数量B乘以参数量的浮点运算次数
`46:55` 反向传播的计算量是前向传播的两倍
`46:58` 即数据点数量乘以参数数量的四倍
`47:00` 浮点运算次数
`47:01` 因此总计算量为2+4=6
`47:04` 即数据点数量与参数数量乘积的六倍
`47:07` 这就是你在各种地方可能见过的
`47:09` 63D这一说法的来源
`47:11` 这仅仅是通过统计前向传播和反向传播得出的
`47:15` 因此我们仅针对这些深层网络进行了上述计算
`47:19` 这是可行的
`47:20` Arch
`47:20` 但事实证明
`47:22` 只要上下文长度不太大
`47:23` 这对transformer来说也是一个良好的近似
`47:26` 如果上下文长度过大
`47:27` 就会出现上下文长度的平方向
`47:29` 这属于此类估算中未包含的额外浮点运算次数
`47:33` 切好
`47:34` 那么我们就稍微聊一聊吧
`47:37` 现在我们有了梯度
`47:38` 接下来在训练过程中
`47:40` 另一项必要工作是执行优化
`47:43` 因此这里展示的是我们的深度网络
`47:46` 为了避免只是重复作业一里的内容
`47:49` 我打算使用ADOGRANT优化器
`47:51` 该优化器来自two thousand
`47:53` eleven年
`47:54` 比ADAM还早嗯
`47:56` 你可以把它理解为
`47:57` 介于SGD和ADAMM之间的方法
`48:00` 基本上它是在SGD基础上
`48:04` 考察梯度的二阶局啊
`48:07` 动量momentum考察的是梯度的一阶局
`48:10` 而ADAM则是将两者结合起来
`48:12` 由于我们主要关注使用方法
`48:14` 因此这里不展开优化器的具体细节
`48:17` 因此我们可以定义一个优化器
`48:19` 然后呃当你计算梯度时
`48:22` 接下来计算梯度
`48:24` 然后进行优化器的一步更新
`48:26` 如果你要自定义优化器
`48:28` 比如在第一次作业里
`48:29` 你需要实现ADAM针对每个参数组
`48:32` 比如这里的w one或w two啊
`48:35` 我们要看一个叫优化器状态的东西
`48:38` 它本质上是一种存储
`48:41` 优化器在运行时就会用到这种存储
`48:43` 在adequate里
`48:45` 我们算的是梯度的平方
`48:47` 也就是所有梯度平方的总和
`48:49` my这里我们从优化器状态里取出这个值
`48:53` 我们用当前的梯度去更新它
`48:55` 然后再存回去
`48:56` 接着更新完G2之后
`48:58` 你就去更新参数
`49:00` 懂了吗
`49:00` 在ADIGRADE中
`49:01` 你基本上是用梯度平方的均值或总和的平方根
`49:04` 来做除法
`49:05` Thank you
`49:07` 你哎好呃
`49:10` 谢谢你让我看看
`49:12` 我可能得跳过前面那部分了
`49:14` 你知道的就是训练那块儿看
`49:17` 其实等等等一下
`49:21` 我想我好像漏讲了一点
`49:23` 不是故意的好
`49:25` 刚才说的是优化器状态
`49:27` 现在来看看优化器占用了多少
`49:30` 显存占用了多少
`49:31` 或者更一般的说内存用量是多少
`49:34` 以参数为例
`49:35` 在这个网络中
`49:37` L层每层有D的平方个参数
`49:40` 如果我们用BF6存储
`49:41` 每个参数占两个字节
`49:43` 这就是参数的总量
`49:45` 激活值呃
`49:47` 这个嘛你知道的好两倍呃
`49:50` 这是针对BF sixteen的及批次大小乘以D
`49:55` 再乘以层数
`49:56` 每一层都有激活值以及梯度
`49:58` 它基本上是所有参数的副本
`50:00` 这些都是BF sixteen优化器状态
`50:02` 则是针对每一项保存的哦
`50:05` 实际上这应该是我想这里写错了
`50:08` 抱歉我会修正这个笔误
`50:10` 这里指的应该是参数数量
`50:13` 而不是参数内存
`50:16` 是参数数量
`50:17` 所以是参数数量的两倍
`50:20` 然后是参数数量的四倍
`50:22` 原因在于为了保持稳定
`50:24` 惯例上会使用F1three two来存储优化器状态
`50:29` 显然有人试过用BF sixteen
`50:31` 结果发现因为涉及平方运算和跨部平均啊
`50:35` 这样并不太稳定嗯
`50:37` 并且以AUUNDERGRAD为例
`50:39` 这里每个参数需要四个字节来存储优化其状态
`50:42` ADAM优化器需要存储一阶距和二阶距
`50:46` 因此占用八个字节
`50:48` 因此优化器状态实际上占用了大量内存
`50:51` S这里有一个注意事项
`50:53` 请记住
`50:54` 内存主要承担两种功能
`50:56` 一是你需要在HBM中存储这些数据
`51:00` 二是这些数据必须传输到加速器
`51:05` 总体而言
`51:06` 优化器状态通常不是计算的瓶颈
`51:08` 因此这里的内存量对于速度方面的性能
`51:12` 并非至关重要
`51:13` 但它意味着你无法在内存中容纳大型模型
`51:18` 好的综上所述
`51:19` 有前所述
`51:20` 参数数量为D乘以D乘以L
`51:22` 浮点运算次数为D乘以token数量或数据点数量
`51:25` 再乘以参数数量
`51:26` 因此对于transformer架构情况会稍显复杂
`51:30` 但这将在作业一中涉及
`51:32` 且你会进行更细致的推导
`51:34` 好的
`51:34` 咱们跳过训练循环
`51:35` 这算是个总体回顾
`51:37` Thank you
`51:38` 结束前我想快速提两点
`51:40` 第一点
`51:41` 谢谢
`51:41` 正如所见
`51:42` 显存不仅影响存储大模型的能力
`51:45` 有时还影响速度
`51:47` 所以一般要尽量减少显存占用
`51:49` 大家常做的两件事里
`51:51` 一项叫梯度累积呃
`51:53` 一般来说批次大小要足够大
`51:55` 以提高稳定性
`51:56` 直到达到临界批次大小
`51:58` 他从后面会讲
`51:59` 这个正如我们看到的
`52:01` 激活显存会随批次大小增加
`52:03` 就是所以在某些情况下
`52:05` 你可能会显存不够
`52:07` 如果批次大小太大
`52:09` 梯度累积的做法就是
`52:11` 把一个批次拆成若干个微批次
`52:13` 在每个V批次上计算梯度
`52:15` 然后把梯度累加起来
`52:17` 不用把梯度清零
`52:19` 每隔批次大小除以V批次大小
`52:22` 不更新参数并清零梯度
`52:23` 因此这其实是一个非常简单的代码改动
`52:26` 就能节省算力
`52:27` 比另外我快速提一下激活检查点
`52:30` Activation check point
`52:31` 在训练时我们需要存储
`52:35` 一般来说是存储所有层的激活值
`52:37` 默认就是这么做的
`52:39` 有意思的是
`52:40` 在推理时我们不需要计算梯度
`52:42` 所以只需存储当前层的激活值
`52:45` 但在训练时
`52:46` 内存占用是B乘以D乘以二乘以L
`52:49` 那么问题是能不能把这个降下来
`52:51` 所以激活检查点也叫梯度检查点或重计算
`52:55` Rematerialization
`52:57` 其核心思想是在前向传播中
`52:59` 你只保留其中一部分层的激活值
`53:02` 反向传播时
`53:04` 已从最近的检查点重新计算缺失的激活值
`53:07` 这就是它被称为检查点的原因
`53:11` 这是系统中常见的通用技巧
`53:13` 本质就是一种权衡的呃
`53:16` 如果你想减少内存重新计算就行
`53:19` 所以我们要把这个想法具体实现出来
`53:21` 这其实相当啊
`53:23` 其实很简单
`53:24` 我没可能是我没看到那部分好
`53:28` 基本上你在这里做的是用同一个模型
`53:32` 只是有处不同
`53:34` 你只需在某一层上加上torch util叫checkpoint
`53:38` 意思就是执行这个计算
`53:40` 但不存储任何中间的激活值
`53:42` 只存储需要的部分
`53:44` 所以这意味着啊
`53:46` 如果你存储所有激活值
`53:49` 记住在深度网络里
`53:50` 有real ue之前的值和REUE之后的值
`53:54` 这就占用了大量存储
`53:56` 换个做法
`53:57` 在这些由线性层和RAU组成的块上
`54:00` 使用激活检查点
`54:01` 不保存RAU之前的激活值
`54:03` 基本上就能省下一半内存
`54:06` 这样内存就能省下一半
`54:08` 然后做反向传播时
`54:09` 虽然需要G3
`54:11` 但你可以很容易的从h two算出G3
`54:13` 谢谢好
`54:15` 你还可以做得更彻底
`54:17` 你可以说好吧
`54:18` 我可以把所有层都存下来
`54:20` 但在极端情况下
`54:22` 我也可以一层都不存
`54:24` 这样内存利用率就达到最高
`54:25` 唯一的问题是计算量会变成L的平方
`54:28` 因为每一层都得从头开始算起
`54:32` 也许一个折中方案是在根号L层储存检查点
`54:36` 这样激活值占用的内存是根号L重新计算的
`54:40` 开销也是根号L
`54:41` 这样就平衡了
`54:43` 谢谢好
`54:44` 总结一下这节课所有操作都是在张量上进行的
`54:47` 参数梯度激活值优化状态数据啊
`54:49` 我们介绍了i ops
`54:50` 希望大家能把它当作理解张量运算的一种思路
`54:54` 在数据点数乘参数数再乘六啊
`54:56` 这个公式我们现在已经搞清楚
`54:58` 它代表的是每部的浮点运算次数啊
`55:02` 如果是训练步骤的话
`55:04` 这里的变量应该是指批次大小
`55:07` 然后我们讲了算术强度和物顶线分析
`55:10` 借此我们可以判断计算瓶颈是在内存上
`55:13` 还是在算力上
`55:14` 矩阵VP处理属于计算受限
`55:16` 而基本上其他情况都是内存受限
`55:19` 最后梯度累加和激活值
`55:22` 检查点技术都是用来节省内存的方法
`55:25` 节省了内存呃
`55:26` 你就能够使用更大的批次大小
`55:29` 好今天的课就讲到这里
`55:31` 下周T促会讲架构相关的内容
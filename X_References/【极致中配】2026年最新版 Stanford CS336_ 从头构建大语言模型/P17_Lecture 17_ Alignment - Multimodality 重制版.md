---
title: "【极致中配】2026年最新版 Stanford CS336: 从头构建大语言模型"
url: "https://www.bilibili.com/video/BV11LEA6eEuj/?p=17"
bvid: "BV11LEA6eEuj"
cid: "39163462056"
author: "常青藤名校课"
upload_date: "2026-06-05"
created: 2026-08-17
tags: ["bilibili", "clippings"]
---

## 简介

如果这个中文配音版对您理解 CS336 有帮助，欢迎点赞、投币、收藏支持一下，也可以点个关注。
我会持续更新更多 Stanford / AI / 大模型相关课程的中文配音课程，尽量帮大家用更低的理解成本看懂这些高质量课程。你的支持会让我更有动力把后续内容继续做完、做好。

## 字幕

`00:00` 原本的计划是深入探讨强化学习
`00:02` 但考虑到课时有限
`00:04` 如果不介绍多模态
`00:05` 这门课就显得不够完整
`00:07` 毕竟你看如今的主流模型几乎都具备多模态能力
`00:11` 所以今天咱们就大致overview一下多模态模型
`00:14` 当然这部分内容单开一门课都绰绰有余
`00:17` 但我尽量在这一节课里把能讲的核心内容都涵盖进去
`00:21` 好咱们开始深入多模态模型
`00:23` 到目前为止
`00:24` 这门课一直只关注语言模型
`00:26` 语言模型其实已经非常通用
`00:28` 它能实现从任意文本到任意文本的转换
`00:31` 比如它可以处理各种语言代码
`00:34` 诗歌或者任何你喜欢的文本形式
`00:36` 甚至像DNA序列这样的数据
`00:39` 但现实世界是多模态的
`00:41` 我们面对的数据包括文本图像
`00:44` 音频和视频
`00:45` 咱们想想看最终想要达到的北极星目标
`00:48` 也就是大家所说的全模态模型
`00:51` 它能接收任意模态组合的输入
`00:53` 也能输出任意模态组合的结果
`00:56` 打个比方
`00:57` 我给你一张图像和一个视频
`01:00` 然后问你关于他们的问题
`01:02` 我能生成图像
`01:03` 甚至能把音频转换成图像
`01:05` 有了全模态模型
`01:06` 你就能想到各种各样的应用场景
`01:08` 这门课我不会带大家从头搭建一个全模态模型
`01:11` 但我会给你一些关键组件
`01:13` 让你至少能上手处理一些数据
`01:15` 以图像数据为例
`01:16` 这也是今天讨论的重点
`01:18` 那么如何构建这样的系统呢
`01:20` 这听起来可能有些抽象
`01:22` 但事实是transformer确实行之有效
`01:25` 尽管人们尝试过许多其他方法
`01:27` 但在处理多模态大规模任务时
`01:29` 它依然是我们目前的最优选择
`01:31` 因此我们需要找到充分发挥其优势的方法
`01:35` 需要注意的是
`01:35` transformer最初是为文本任务设计的
`01:38` 它具备处理token的特性及输入
`01:41` 是一组token
`01:42` 输出
`01:42` 也是若干token
`01:43` 在此我将token的概念进行了扩展
`01:46` 它不再局限于文本中
`01:48` 离散的token
`01:49` 还可能包含连续的token
`01:50` 你可以将这些视为token的嵌入形式
`01:53` 核心在于token必须代表某种语义信息单元
`01:57` 例如在自然语言中
`01:59` token是子词
`02:00` 具有一定的意义
`02:01` 但像像素这样的元素单独来看是没有意义的
`02:04` 因此我们需要将音频图像等数据转化为离散或连续的token
`02:11` 值得注意的是
`02:12` 对于文本数据
`02:13` 我们早已采用这种做法
`02:14` 在第一讲中
`02:15` 我们讨论过分词
`02:16` 当时的情况尚可
`02:18` 因为我们使用了BPE分词器
`02:20` 并且大家也亲手实现过
`02:22` 这虽然勉强可用
`02:24` 但大家心中或许都期待有更优秀的分词器出现
`02:29` 或者干脆不用分词器
`02:30` 这倒也不是什么大问题
`02:32` 处理非文本模态时
`02:34` 咱们得费点脑筋
`02:35` 想清楚怎么把BP分词器的逻辑牵引过来
`02:38` 让图像能变成transformer能处理的形式
`02:41` 好这里咱们得解决两个问题
`02:44` 第一面对图像
`02:46` 视频或音频这些非文本数据
`02:48` 咱们该怎么把它们喂给transformer
`02:51` 第二是怎么把它们生成出来
`02:53` 咱们这节课主要先讲第一个问题好
`02:56` 咱们往回倒带
`02:57` 回到2021年QUE模型
`02:59` 大家可能听说过
`03:01` 它是现代v o ms的许多基础
`03:03` 所以咱们可以稍微深入一点来看这个例子
`03:06` KB的全称是对比语言图像预训练
`03:09` 稍微回顾一下历史背景
`03:11` 那时候GPT-3已经问世
`03:13` GBT-2以及所有语言模型都已宣进入了基础模型时代
`03:17` 不过视觉领域当时还荣落有些尝试
`03:21` 但传统做法主要依赖大量标注或者数据集
`03:24` 比如image net
`03:25` 研究人员在这些数据集上训练RESNET等模型
`03:28` 配合数据增强等手段
`03:30` 最终取得了非常出色的性能
`03:32` 因此open night的研究人员提出一个问题
`03:35` 能否利用海量的图像及其文本描述
`03:38` 受语言领域这一思路的启发
`03:40` 只需抓取互联网上的文本
`03:42` 尽管数据噪声很大
`03:43` 但只要训练足够大的模型
`03:45` 就能从中提取意义
`03:47` 并生成有用的内容
`03:49` 那么图像领域的等效方案是什么
`03:52` 于是clip应运而生
`03:54` clip的核心思路其实很简单很直接
`03:57` 那么针对图像对应的做法是什么呢
`04:00` 于是clip就这样诞生了
`04:03` clip的核心思路其实很简单很直接
`04:06` 这样一来我就得到了3万2000个图像编码
`04:09` 对应的文本
`04:10` 我也会做同样的操作
`04:12` 从而得到T1到T1
`04:13` 这些是文本嵌入
`04:15` 那些则是图像嵌入
`04:16` 接下来我要设定一个目标
`04:18` 以图像I为例
`04:21` I和T1是配对的
`04:23` 所以我要让这两个嵌入向量的点击远远大于I坏
`04:28` 与其他所有文本嵌入的点击同理
`04:31` 对于这段文本
`04:32` 我也希望如此
`04:34` 也就是说这个点击的值要大于T1与其余所有图像点击的值
`04:38` 简单总结一下
`04:39` 这就是clip的目标
`04:40` 你可以把它想象成执行了2N次不同的soft max分类来看代码
`04:45` 这里你会得到一个M乘D的矩阵来表示图像编码
`04:50` 另一个M乘D的矩阵表示文本嵌入
`04:54` 先做归一化再计算
`04:56` 点击这里会用到温度系数
`04:58` 接着计算交叉熵好的
`05:00` 所以这基本上就是一个多类分类问题
`05:03` 样本被组织成了M乘N的矩阵形式
`05:06` 好大家有什么问题吗
`05:09` 好那咱们继续接下来讲讲clip构建的一些细节
`05:14` 数据是从哪来的
`05:15` 论文里关于数据部分的细节并不多
`05:18` 简单来说就是先收集一批查询词
`05:21` 从某个来源获取这些查询词后去网上搜索
`05:26` 从中挖掘出大量的图文
`05:29` 谢谢最终得到了4亿个图文
`05:32` 该数据集并未公开
`05:33` 不过有人尝试过复现
`05:36` 有一篇名为open clip的论文进行了复现
`05:40` 并进一步扩展了clip的思路
`05:42` 例如发布了一个名为lion5B的数据集
`05:45` 包含50一张带文本描述的图像
`05:47` 期间还进行了一些数据处理
`05:49` 有趣的是
`05:50` 他们先用clip进行数据筛选
`05:52` 再训练open clip
`05:53` 这里存在一种自取过程
`05:55` 但至少这是一个能明确指明训练数据集的模型
`05:59` 期间还进行了一些数据处理
`06:01` 有趣的是
`06:03` 他们先用clip进行数据筛选
`06:06` 再训练open clip
`06:07` 这里存在一种自举过程
`06:10` 但至少这是一个能明确指明训练数据集的模型
`06:13` 还有代码好
`06:15` 咱们再补充几个细节啊
`06:17` 图像分辨率五花八门对吧
`06:20` 网上随便找张图
`06:21` 长宽比可能很不固定
`06:23` 要么细长
`06:24` 要么瘦长
`06:24` 尺寸完全随意
`06:26` 另外学神经网络时
`06:28` 你会知道他们不喜欢动态尺寸
`06:31` 偏好固定大小的输入
`06:33` 所以通常会做个启发式的预处理
`06:35` 把图像缩放
`06:36` 让短边变成336或者224这类目标尺寸
`06:39` 接着做中心裁剪
`06:40` 把它变成正方形
`06:42` 通常目标位于画面中央
`06:44` 因此只需裁剪掉背景即可
`06:46` 所以这倒不是问题
`06:48` 好那视觉编码器到底是什么
`06:53` 这是通用算法
`06:54` 但我尚未详细解释图像编码器和文本编码器的具体含义
`06:57` 对本本被输入序列的末尾
`07:04` 你是不是能量很大
`07:06` 一分钟内我们要去试摸黑总组件
`07:10` 新车六五背后都有相应的训练过程
`07:13` 这正是他们使用普外科技术训练
`07:16` 他可以坚持用不足下面原因
`07:18` 我认为核心问题在于
`07:20` 为何他们采用图文对训练
`07:22` 而不是单独使用图片
`07:24` 谢谢有一类研究致力于将图像编码为表示形式
`07:29` 谢谢有一类研究致力于将图像编码为表示形式
`07:33` 它们被视为同一张图像
`07:34` 因此应具有相似的嵌入表示
`07:37` 不过我们接着往下说
`07:39` 这种方法对捕捉低级细节很有用
`07:42` 但问题是仅靠数据增强
`07:44` 你无法将一只狗变成另一种狗
`07:47` 比如从一种狗变成另一种狗
`07:49` 引入文本
`07:50` 就能提供更高阶的语义信息
`07:52` 用于表征图像
`07:54` 这是个很好的问题
`07:55` 稍后我们会看到结合具体任务类型
`07:58` 这些语义表征能发挥多大作用
`08:00` 那么clip使用的视觉编码器是什么
`08:02` 他们尝试过多种架构
`08:04` 例如RESNET和当时刚发布的视觉transformer
`08:07` 试验
`08:07` 结果表明
`08:08` 视觉transformer的效果最佳
`08:10` 因此人们提到clip时
`08:12` 通常指的就是基于v it的那个版本
`08:14` 视觉transformer的处理流程如下
`08:18` 我们先取一张图像
`08:20` 然后将其切分为小块
`08:23` 原始论文中采用的是16X16的块大小
`08:26` 而clip使用的是14X14
`08:29` 无论尺寸如何
`08:30` 这些小块本质上都是向量
`08:32` 因此每个小块在某种程度上就是它的token
`08:36` 对于视觉transformer
`08:37` 你会像训练语言模型那样加入位置嵌入
`08:40` 然后将其输入到一个标准的transformer中
`08:43` 如果你要进行分类任务
`08:44` 通常的做法是取出序列的第一个token作为分类表示
`08:49` transformer编码器会输出一组向量序列
`08:53` 以transformer为例
`08:54` 如果你要做分类任务
`08:56` 通常会取出序列的第一个token作为分类表示
`08:59` 通常的做法是
`09:00` transformer编码器会输出一个向量序列
`09:03` 具体来说先对所有激活值做全局平均
`09:07` 再用这个查询向量与每个位置的键和值进行一轮注意力计算
`09:12` 这样得到的向量可能比直接求平均包含更多信息
`09:16` 谢谢好
`09:18` 这就是wait科勒文发现效果最好的模型是i i it减L14
`09:23` 这个名称的意思是这是一个大型weight
`09:26` 我估计它大概有24层
`09:28` 不太确定使用的是1414的patch
`09:31` 每个patch都是RGB的
`09:33` 所以有三个通道
`09:34` 他们使用336×336分辨率的图片进行训练
`09:37` 实际上他们先用低分辨率图片训练
`09:39` 然后再upscale
`09:41` 这主要是为了提速
`09:42` 毕竟高分辨率图片太耗时
`09:44` 在后续训练阶段
`09:45` 他们才使用高分辨率图片
`09:47` 对
`09:47` 那么位置编码在文本处理中是否会更复杂一些
`09:51` 比如三本及以为按左下键包含上下左右等方向信息没错
`09:58` 所以核心问题在于能否设计更智能的位置嵌入方法
`10:02` 因为目前采用的只是0~9的线性编码
`10:05` 我认为clip这篇论文其实尝试过该方法的二维和一维版本
`10:09` 结果显示两者差异并不显著
`10:11` 在解读这些结果时
`10:13` 需要意识到它们主要属于有趣的分类实验
`10:16` 因此对于分类任务而言
`10:18` 这一点或许并不关键
`10:20` 稍后我们会介绍更高级的位置嵌入方法
`10:23` 这些方法会考虑空间结构
`10:25` 接下来我们来看文本编码器
`10:28` 它采用标准的transformer架构
`10:31` 由于开发团队相同
`10:32` 这里使用的是GPT-2风格的transformer
`10:35` 为了从序列中提取一个向量
`10:38` 它们在序列开头添加boss结尾添加EOS
`10:43` 然后取最后一层中EOS位置的激活值
`10:47` 现在我们拥有了视觉编码器和文本编码器
`10:51` 随后即可利用这一流程来训练模型
`10:54` 它代表了整个序列
`10:55` 现在我们有了视觉编码器和文本编码器
`10:59` 明白了吗
`11:00` 因此clip在2021年引发广泛关注的关键成果是
`11:04` 在image net基准测试中实现了零样本分类
`11:08` 它的表现超越了在120万张图像上训练的模型
`11:12` 注意是图像images而非图片
`11:16` 我们可以想一下
`11:17` 处理120万张image图像
`11:19` 需要亚马逊机械
`11:20` 土耳其amazon mechanical turk的工人投入大量工时进行数据清洗
`11:24` 而现在你使用的是在更自然的网络数据上训练的clip
`11:28` 诚然这确实利用了网络上众人的劳动
`11:32` 但既然能直接利用这些线程数据
`11:34` 就能直接进行零样本推理
`11:36` 零样本技术的基本原理是
`11:38` 虽然这里没有幻灯片展示
`11:40` 但逻辑很简单
`11:41` 你拿一张图
`11:42` 再准备一堆文本
`11:44` 然后计算它们与各个标签的点击
`11:47` 看谁的得分最高
`11:49` 我们继续
`11:54` 对确实是这样
`11:55` 抱歉
`11:56` 这部分在侧面好的好问题
`11:58` 所以这是领养本预测嗯
`12:01` 就是你拿一张图片
`12:03` 然后你
`12:07` 对就像我之前说的有问题吗
`12:10` 律师的就这样
`12:11` 我们遇到了一个问题
`12:12` 比如这里有一张狗的图片
`12:14` 对于所有的标题也是不只是这个简单的标题
`12:18` 这只狗也许其他标题也和狗有关
`12:21` 这会让模型产生混淆吗
`12:23` 没错
`12:24` 所以问题是
`12:25` 如果你有一只狗
`12:26` 也许还有其他标题也提到狗
`12:28` 这会混淆模型吗
`12:29` 嗯一般来说这个过程是有噪声的
`12:33` 嗯如果有另一只狗也没关系
`12:35` 因为平均来看嗯不太可能总是出现梗
`12:38` 有时会是苹果或猫之类的东西啊
`12:41` 还有一点是这些图片是嗯嗯如果有另一只狗也没关系
`12:46` 因为平均来看不太可能总是出现狗嗯
`12:50` 还有一点是这些图片是呃
`12:52` 并不一定只是如实描述图像内容没错
`12:55` 因为如果图像是一只狗
`12:57` 你就不需要特意说明这只狗
`12:58` 因此这一过程噪声很大
`13:00` 也许令人有些意外
`13:02` 但有趣的是
`13:03` 它确实有效
`13:04` 我的意思是需要进行大量的数据过滤
`13:06` 因此如果随意选取网络上的图像和文本
`13:09` 噪声可能会过大
`13:11` 还有一点需要注意
`13:12` 这与我将在最后回到的一点有关
`13:14` 他们还尝试了另一种替代方案
`13:16` 既不进行这种排序
`13:17` 而是从图像中预测文本
`13:20` 因此它们可以设定一个目标
`13:22` 尝试从图像中预测文本
`13:24` 既可以作为磁带bag words形式
`13:27` 他们还尝试了另一种思路
`13:29` 放弃排序
`13:30` 改为让模型根据图像直接预测文本
`13:34` 于是他们设定了一个目标
`13:36` 即让模型根据图像预测文本
`13:38` 比如把文本当做磁带bag of words来处理
`13:41` 这和clip使用的磁带方法不同
`13:44` 所以我的观点是对于你真正想学习的表示
`13:49` 或者说看image net的准确率
`13:51` 也就是分类任务的表现
`13:53` 实际上精确建模字幕的token序列
`13:57` 对于获取图像的粗略表示来说并没有那么关键
`14:01` 好我们稍后再回到这一点
`14:04` 那么到目前为止我们学到了什么
`14:06` 我们已经对图像进行了编码
`14:08` 使用clip它能捕捉图像的语义
`14:11` 因为它与文本配对
`14:12` 而文本通常描述的正是羽翼
`14:14` 需要指出的是
`14:15` 这里的设计决策是基于图像分类的
`14:18` 因此它的力度并不十分精细
`14:21` 但正如我们将要提到的
`14:22` 它仍然为我们后续要做的所有事情提供了一个稳健的起点
`14:27` clip在技术上的一个缺点是
`14:29` 它需要较大的批量大小
`14:31` 比如3×000
`14:32` 如果你的批量大小仅为一
`14:34` 显然他无法工作
`14:35` 即使是十也不行
`14:36` 此外soft matt操作是在整个批量上进行的
`14:40` 因此它缺乏良好的可分解性
`14:43` 相比之下
`14:43` 在常规语言模型训练中
`14:45` 一批样本中的所有序列会被并行处理
`14:48` 最后只需进行聚合
`14:50` 这一问题将由谷歌发布的另一篇名为seed clip的论文来解决
`14:55` 因此你可以将其基本视为clip的改进版本
`14:59` 它代表用于语言图像预训练的signal损失
`15:02` 这里的主要区别在于
`15:04` clip执行的是多类别分类
`15:06` 其逻辑是既然拥有匹配的文本和图像对
`15:10` 就将这一对视为正样本
`15:12` 与其他所有不匹配的图像或文本进行对比
`15:15` 从某种意义上说
`15:16` CCL简单的多
`15:17` 他基本只问对于任意给定的图像文本对他们是否对齐
`15:22` 没错
`15:23` 大家看上面这张图
`15:24` 这其实是个二分类问题
`15:26` 对角线上的元素是正样本
`15:28` 对角线外的就是负样本
`15:29` 就这么简单
`15:30` 咱们来看算法流程
`15:32` 拿到嵌入向量后
`15:34` 先做归一化
`15:35` 然后照常计算点击
`15:37` 不过现在咱们是在计算相似度
`15:40` 咱们来设定标签
`15:41` 非对角线位置标为一
`15:43` 对角线位置标为正一
`15:45` 接着执行交叉熵损失计算
`15:48` 也就是对数SIGMOID
`15:50` 所以这个目标函数非常简单
`15:52` 咱们来定义一下标签
`15:54` 所以对角线位置标记为一
`15:56` 对角线位置标记为正一
`15:58` 然后实行交叉熵损失计算也是对四对
`16:01` 最终因此这个目标还是非常简单
`16:06` 对那问题来了
`16:07` 这需要采用复杂的采样策略吗
`16:09` 呃在最早的那篇论文里
`16:12` 他们直接操作的是同一种类型的矩阵
`16:14` 我猜你大概在想
`16:15` 一般情况在进行这种对比学习时
`16:18` 负样本该怎么处理
`16:20` 比如是否需要平衡正负样本
`16:22` 或者让负样本选的更紧一些
`16:24` 以避免模型学到错误的关联
`16:26` 产生偏差
`16:27` 不过在最初版本里
`16:28` 这其实挺简单的
`16:29` 所以咱们来聊聊数据嗯
`16:33` 你知道这是谷歌做的
`16:35` 这是一个数据集
`16:36` 他们参考了另一篇论文
`16:38` 即2022年左右出现的一款语言图像模型
`16:42` 该模型使用了web类数据集
`16:44` 其中包含11集的图像文本
`16:46` 对他们还做了一些额外处理
`16:47` 比如针对图片中包含文字的情况
`16:50` 他们使用了OCR技术
`16:51` 这也是构建图像文本对的一种方法
`16:54` 他们还进行了一些过滤
`16:56` 这个数据集是多语言的
`16:58` 这篇论文的核心观点是其训练效率远高于clip
`17:02` clip是在256个TPUV上训练了十天
`17:06` 而C格勒仅用了32块vi显卡训练了五天
`17:13` 单看每秒浮点运算次数
`17:14` 他们其实并不更快
`17:16` 优势在于你可以在一个pod中部署更多设备
`17:19` 且互联性能更好
`17:20` 但在这个规模下
`17:21` 实际情况是效果非常显著
`17:23` 你知道并没有更快
`17:25` 实际上速度慢了大约60%
`17:27` 所以这种方式之所以更快
`17:29` 我认为clip可能并未针对最大吞吐量优化代码
`17:32` 当时只是想先把模型跑通
`17:34` 不过CIGAE论文展示了
`17:37` 我们可以这样进行并行化
`17:38` 所以大家可以回想一下之前的系统课
`17:42` 把它看作也就是DDP
`17:44` 每个设备各自存储一部分图像文本
`17:48` 对不过这里样本之间存在交互
`17:51` 不像语言模型训练那样
`17:53` 各项完全独立
`17:54` 所以在第一轮中
`17:55` 每个设备只计算他手头那些图像文本队的损失
`18:00` 接着输入文本
`18:02` 这样设备一就能拿到T5到T8
`18:05` 并据此计算这里的负样本
`18:08` 接着获取T9到T12
`18:11` 以此类推
`18:11` 不断轮换
`18:12` 直到覆盖所有非对角线块中的条目
`18:15` 好的cup的一个优势在于
`18:18` 它有效地将批量大小与损失解耦了
`18:22` 相比之下
`18:23` 在clip中
`18:24` 损失是依赖于批量大小的
`18:27` 如果你改变了批量大小
`18:28` 损失函数也就变了
`18:29` 所以他们得以用更小的批量大小做实验
`18:32` 比如小于16K
`18:33` 在这种设置下
`18:34` 它的表现远优于clip
`18:36` 因为在clip中如果批量大小过小
`18:39` 损失函数就会退化
`18:40` 而对于c clip
`18:42` 即使批量较小
`18:43` 会导致方差较大
`18:44` 但其期望值与标准损失是一致的
`18:47` 他们还提到
`18:48` 虽然可以使用更大的批量大小
`18:50` 但这其实并没有带来实质性的帮助
`18:52` 谢谢好
`18:53` 接下来我们看看clip和c clip
`18:56` 这些是图像编码器
`18:58` 负责接收图像
`18:59` 在这个版本中是固定尺寸
`19:01` 例如336×336的图像
`19:03` 并将其映射为向量
`19:05` 好的这个向量里包含了一些语义信息
`19:08` 好的这个向量里包含了一些语义信息
`19:11` 好接下来我们开始构建视觉语言模型
`19:15` 也就是VRMS
`19:16` 我要介绍两类模型
`19:18` lava和鲲
`19:19` 这两类模型在整体架构模板上非常相似
`19:22` 具体细节上会有些不同
`19:24` 我们稍后展开
`19:25` 其基本思路是将这些嵌入向量注入到语言模型中
`19:30` 而不是从零开始训练
`19:31` 谢谢好的
`19:33` 拉A论文发表于2023年
`19:36` 这让大家很兴奋
`19:37` 因为那时候像GPT这样的闭源模型
`19:40` 我觉得是g p t-4已经能做视觉推理了
`19:42` 而且他们展示了自己确实能做视觉推理
`19:45` 当然效果不如g p t-4
`19:47` 但这是个开源模型
`19:49` 大家得以看到它的内部运作机制
`19:51` 所以嗯四个VRM这里有展示了多模态能力的提升
`19:56` 本质上这里主要分几块
`19:58` 一个是视觉编码器
`20:00` 他们用的是clip文本解码器部分
`20:02` 他们用了个叫VIKA的语言模型
`20:04` 这是首个在共享GBT对话数据上微调过的拉马模型
`20:08` 他们用的是clip文本解码器部分
`20:10` 他们用了一个叫VIKA的语言模型
`20:13` 这是首个在共享GPT对话数据上微调过的拉马模型
`20:16` 本质上它主要由几个部分构成
`20:19` 首先是视觉编码器
`20:20` 他们使用的是clip
`20:22` 对于文本解码器
`20:23` 他们采用了一个名为VIKA的语言模型
`20:26` 这是首个在共享gt对话数据上进行微调的LM模型
`20:31` 数据集是ms coo
`20:33` 这是一个经过标注的数据集
`20:35` 研究人员雇佣了mechanical turk上的工人
`20:38` 对大量图像进行标注
`20:40` 包括边界框以及描述图像内容的标题
`20:43` 他们基本上为模型合成了一套数据集
`20:46` 因此他们将标题或检测到的物体作为prompt输入给GPT-4
`20:51` 并要求GPT-4生成问题或对话
`20:53` 例如这是ms coco数据集中的一张图像
`20:57` 这是由人工标注的标题
`20:59` 此外这里也是有人工标注的
`21:01` 然后你让GPT-4生成一段对话
`21:05` 你可能会得到一个问题及其对应的答案
`21:07` 你可以向GBT-4提问
`21:09` 让它生成一段详细描述
`21:10` 它大概会生成类似caption的内容
`21:13` 然后你可以让g p t-4做复杂推理
`21:16` 它也能搞定好的
`21:17` 这是合成数据
`21:19` 共15万8000个样本呃
`21:21` 然后他们拿这些数据训练了一个模型
`21:25` 所以这个模型用的是clip
`21:27` 具体来说是表现最好的那个版本
`21:30` YT减L14
`21:31` 也就是1414的patch
`21:35` 咱们直接看这张图
`21:36` 把图像输入视觉编码器clip就能得到一个向量
`21:41` 也就是1414的patch
`21:43` 咱们直接看这张图
`21:45` 把图像输入视觉编码器clip就能得到一个向量
`21:50` 对于文本直接用标准的嵌入生成向量即可
`21:53` 简单来说
`21:54` 文本和图像都被编码成向量
`21:57` 然后这一串向量序列直接送入标准的transformer
`22:00` 然后输出结果
`22:02` 从某种意义上说
`22:04` 我们把这些图像转换成了文本token
`22:07` 因此我们可以利用预训练的语言模型
`22:10` 为了训练这个模型
`22:12` 它们分两个阶段进行
`22:14` 第一阶段动机视觉编码器和语言模型
`22:18` 只训练这个矩阵W
`22:19` 他们称之为对齐阶段
`22:21` 本质上就是确保图像被映射到大致相同的空间
`22:26` 大家好
`22:27` 欢迎来到本系列课程
`22:29` 如果直接使用随机的W
`22:31` 这些图像根本无法生成
`22:32` 也就是说他们的输出肯定不是能表示自然语言token的嵌入向量
`22:36` 因此训练W的目的就是让他的输出尽可能接近自然语言
`22:41` token的嵌入向量
`22:42` 第二阶段则是冻结视觉编码器
`22:45` 随后训练W和语言模型
`22:47` 也就是说他们是在用自己的数据对语言模型进行微调
`22:51` 大家还记得吧
`22:52` 他们的数据主要是图像
`22:53` 并配有对话描述或复杂的推理示例
`22:56` 因此这完全属于图像加文本到文本的任务
`22:59` 这是他们论文中的一个例子
`23:01` 很有意思
`23:02` 他们还特别指出
`23:03` 即便用户的提示词不够清晰
`23:06` 该模型也能表现出色
`23:07` 即使你没有直接问哪里出了问题
`23:09` 他也能给出合理的解释
`23:11` g p t-4显然能做到这一点
`23:13` 但当时的其他模型却不行
`23:15` 关于拉瓦还有问题吗
`23:17` 好那我们先回到2000 023年
`23:21` 接下来我们直接看2024年的lava
`23:24` One vision
`23:25` Lava
`23:25` 论文发表后
`23:26` 又涌现出许多后续工作
`23:28` 比如lava1.5
`23:29` lava next等
`23:30` 不过我认为我接下来要讲的内容基本涵盖了这些创新
`23:34` 其核心思路并未改变
`23:36` 仍沿用那套技术架构
`23:38` 但他们旨在拓展更多种类的多模态应用
`23:41` 因此该系统现在能够处理多张图片及视频
`23:44` 我们来看下面的例子
`23:45` 请看这张示意图
`23:47` 系统现已支持图像输入
`23:49` 例如单张图片输入
`23:51` 也可以是多张图片或者是视频
`23:53` 而视频本质上就是一系列连续的图像帧
`23:55` 这对应于对视频帧进行采样
`23:58` 在此视觉编码器被替换为c clip
`24:02` 在文本解码器方面
`24:03` 他们采用了queen2
`24:05` 这大概是当时性能最强的语言模型了
`24:08` 接着是投影器
`24:10` 也就是适配器负责将视觉编码器的输出转换为语言模型的输入
`24:17` 这里使用了一个两层的MLP多层感知机
`24:21` 而非简单的线性投影
`24:23` 综上所述
`24:24` 这些细微改动就好比你升级了系统内部的组件
`24:27` 但整体架构仍保持大致相同好了
`24:31` 所以这点小改动
`24:33` 这就好比你是个系统
`24:35` 只是升级了里面的零件
`24:36` 以支持更复杂的数据处理
`24:38` 但整体架构还是那种大致相同的系统嗯
`24:42` OCR的关键在于保留细粒度的信息
`24:44` 不然的话J看着就像I这就麻烦了
`24:48` 大家记得clip会把图缩放到336336
`24:52` 你看这是一份文档
`24:53` 要是直接裁成336×336
`24:57` 你就没法读了
`24:59` 这其实是lava1.5论文里提过的
`25:02` 这个思路其实挺简单的
`25:03` 我是说很直接
`25:04` 你把一张图拆成好几块
`25:06` 简单来说
`25:07` 每一块的大小就是视觉编码器要处理的那个分辨率
`25:12` 比如336X336
`25:14` 所以咱们要把所有这些块都编码成向量
`25:17` 然后把它们拼在一起
`25:18` 说白了这就是为了解决视觉编码器搞不定高分辨率的问题
`25:22` 所以咱们不直接做下采样
`25:24` 而是直接裁剪图像去观察不同的局部区域
`25:28` 如果输入的是超高分辨率的图片或视频
`25:31` 生成的token数量会非常多
`25:33` 因此需要对这些token进行下采样处理
`25:35` 不过无论采用哪种分辨率调整方法
`25:38` 核心思路都是自适应
`25:39` 这一点非常关键
`25:40` 这很好
`25:41` 因为transformer本身就具备这种灵活性
`25:44` 无论序列长度如何
`25:45` transformer都能很好地处理
`25:47` 这意味着图像也能支持任意分辨率
`25:50` 再次体现了这种动态适应能力
`25:52` 这就是他们在处理图像时实际采用的方法
`25:56` 假设你有一张论文截图
`25:58` 系统会先对其进行一下采样
`26:00` 同时保留一条路径
`26:02` 这本质上是对整篇论文进行下采样后的编码
`26:07` 接着将其分割成若干块
`26:09` 每块的分辨率都符合视觉编码器的要求
`26:11` 然后分别对每个块进行编码
`26:13` 随后将所有图像块拼接起来
`26:16` 如果数量过多
`26:17` 则通过插值方法减少图像块的数量
`26:19` 这样一来他们就能处理单张图像
`26:22` 多张图像以及视频了
`26:24` 你可以这样理解
`26:25` 虽然从技术上讲
`26:27` 所有这些都可以还原为图像
`26:29` 但他们还是稍微调整了权重
`26:31` 因为视频数据有其特殊性
`26:34` 他们希望确保所有模态之间大致具有可比性
`26:37` 你可以这样理解
`26:38` 虽然从技术上讲
`26:39` 这些内容最终都能还原为图像
`26:41` 但系统还是对它们进行了权重调整
`26:43` 因为视频数据量巨大
`26:45` 目的是确保不同模态之间的数据大致具备可比性好如果有多张图片
`26:52` 就直接用基础分辨率处理
`26:54` 也就是说只有一张图时
`26:55` 我能看得更细致
`26:56` 涂多了就只能粗略浏览
`26:58` 处理视频时
`26:59` 我们会用更低的分辨率或更少的token来表示每一帧
`27:03` 毕竟视频数据量很大
`27:05` 需要节省计算资源
`27:06` 他们最多只取32帧
`27:08` 但这样做会面临上下文长度限制的问题
`27:11` 稍后我们会看到
`27:13` 解决多模态问题的关键之一就在于如何处理长上下文好
`27:17` 那么关于这组数据
`27:21` 我认为他们阐述的核心理念是既要保证数据的高质量
`27:26` 也要保证数据的数量
`27:27` 换一个角度说
`27:28` 这些数据具有极强的针对性
`27:31` 你会发现他们大多是为特定任务定制的
`27:34` 比如视觉问答或是回答关于表格的问题
`27:38` 因此这显然属于后训练阶段
`27:40` 其目标是让模型更好地对齐人的意图
`27:42` 为了让模型执行这些任务
`27:44` 你会专门构建一批对应的任务数据
`27:46` 这项工作也毫不掩饰其目的
`27:49` 本质上这是在蒸馏GPT-4模型
`27:52` 以获取最佳性能
`27:54` 虽然这或许并非最理想的做法
`27:57` 但现实如此
`27:58` 如果没有标注预算
`27:59` 你只能这么做
`28:01` 此外他们使用的数据涵盖单张图片
`28:05` 多张图片以及视频
`28:06` 如果没有标注预算
`28:07` 你只能这么做
`28:09` 此外他们使用的数据涵盖单张图片
`28:11` 多张图片以及视频
`28:13` 训练思路与之前类似
`28:15` 第一阶段主要进行
`28:17` 对其具体来说你只训练投影器
`28:21` 也就是那个适配器
`28:23` 接着进行第二阶段训练
`28:25` 第三阶段则训练整个模型
`28:27` 以前是两个阶段
`28:28` 现在变成了三个
`28:29` 我不确定这背后有什么特别的原则
`28:32` 除了第二阶段似乎是想进一步提升模型的推理能力
`28:36` 使用高质量数据
`28:37` 但更侧重知识
`28:38` 最后阶段则侧重那些类似下游任务的事例
`28:42` 这篇论文发现了一个有趣的现象
`28:45` 即不同模态之间可以迁移
`28:47` 虽然使用高质量数据
`28:49` 但更侧重知识
`28:51` 在最后阶段则侧重那些类似下游任务的例子
`28:55` 好这篇论文里发现一个挺有意思的事儿
`28:57` 就是不同模态之间可以迁移
`28:59` 你会问他一些问题
`29:01` 但在测试时你会看到一张图里既有表格
`29:04` 也有图表
`29:04` 然后就能跟他聊聊天
`29:06` 训练的时候你只用单张图上的OCR数据
`29:10` 而且只在多张图之间做关系推理
`29:13` 但这能帮你搞定那些很有用的情况
`29:15` 比如用来驱动即为agent
`29:17` 给他看一堆截图
`29:18` 也就是多张图
`29:19` 它就能生成描述这些截图的描述
`29:22` 再举个例子
`29:23` 有个叫视觉提示的想法
`29:26` 就是图里有个圆圈
`29:27` 这个圆圈是图的一部分
`29:29` 它的作用是标出你需要对它做点什么的那块区域
`29:32` 再举个例子
`29:33` 有一种叫视觉提示的方法
`29:35` 就是在图中画一个圆圈
`29:37` 这个圆圈是图像的一部分
`29:39` 它的作用是标出你需要对其执行操作的那块区域
`29:42` 所以我第一次看到这
`29:44` 心想唉哟你这是在针对每一个任务下手
`29:47` 这有点像监督学习
`29:48` 不过如果你有足够多的任务
`29:50` 这些模型似乎能实现某种迁移啊
`29:53` 这算是个让人安心的消息好
`29:55` 所以再说一次
`29:57` 这是一个相当标准的VIM架构
`29:59` 视觉编码器加上投影层
`30:01` 再介入语言模型
`30:03` 数据策展工作投入了大量精力
`30:05` 他们很大程度上依赖于合成的特定任务
`30:07` 数据lab系列的一个优势在于它是少数几个能做到这一点的模型之一
`30:12` 谢谢好
`30:13` 接下来咱们聊聊queen模型
`30:15` 所以queen也是从2000023年开始训练多模态模型的
`30:20` 首个版本是queen vl啊
`30:23` queen也从2000023年开始训练多模态模型
`30:26` 视觉编码器方面
`30:28` 他们用了open clip
`30:29` 记得吧
`30:30` open clip就是clip模型的开源复线板
`30:32` 所以它本质上就是个clip编码器
`30:35` 至于适配器呃
`30:36` 他们使用了open clip作为视觉编码器
`30:38` 他们用了一层交叉注意力
`30:41` 结合2D位置嵌入
`30:43` 将特征映射到一个固定尺寸
`30:46` 所以它本质上就是个clip编码器
`30:48` 至于适配器啊
`30:49` 他们通过一层交叉注意力机制将视觉特征映射到固定大小
`30:53` 他们用了一层交叉注意力
`30:55` 结合2D位置嵌入
`30:56` 将特征映射到一个固定尺寸
`30:58` 它们采用了一层交叉注意力机制
`31:01` 结合二维位置嵌入
`31:03` 将特征映射到固定尺寸
`31:05` 比如256
`31:06` 这部分可能被略过了
`31:08` 但特殊token包含了图像标签
`31:10` 边界框
`31:11` 标签box tag以及参考标签rap tag
`31:13` 谢谢
`31:14` 这就是幻灯片导出为HTML格式时的效果
`31:17` 训练分为三个阶段
`31:19` 这与lava模型的做法类似
`31:21` 第一阶段也就是他们所谓的预训练
`31:23` 其实我们并非从零开始预训练
`31:26` 这里使用的是大规模但质量较低的数据
`31:30` 具体做法是冻结语言模型
`31:32` 然后训练视觉编码器和适配器
`31:36` 这与lava模型略有不同
`31:38` 因为lava模型在此阶段并不训练视觉编码器和适配器
`31:42` 谢谢大家
`31:43` 可以看到他们训练所用的一部分数据集
`31:46` 规模达到了14亿个样本
`31:47` 到了第二阶段
`31:49` 他们换用了质量更高
`31:50` 针对特定任务的数据
`31:52` 这里能看到一些VQA数据集中常见的来源
`31:55` 比如图表问答之类
`31:56` 接着他们在这里训练所有参数
`31:58` 最后是第三阶段使用的是指令微调数据
`32:03` 我们冻结视觉编码器
`32:05` 然后训练适配器和语言模型
`32:07` 这里举几个他们能做到的例子
`32:09` 当然因为这是冠
`32:10` 所以有些例子是中文的
`32:12` 我们还希望打造一个能涵盖语言模型能力的模型
`32:17` 既然语言模型能写代码
`32:19` 我们也希望视觉模型在代码方面同样出色
`32:22` 咱们看个例子
`32:23` 如果你能看见的话
`32:25` 这是一张包含金霸王蜘蛛侠和绿巨人的图像
`32:29` 它还能输出边界框
`32:30` 它其实不生成图像
`32:32` 只是输出边界框
`32:33` 接着咱们可以做OCR
`32:35` 也就是文字识别
`32:37` 然后对比一下
`32:39` 所以咱们就用咱们自己的东西好
`32:42` 那queen2算是又一次升级
`32:45` 这里有些新点子
`32:46` 我接下来会过一遍
`32:47` 比如用更大的视觉编码器
`32:49` 他们还开始搞动态分辨率了
`32:51` 我觉得这是最关键的一点
`32:53` 大家还记得咱们从lava到larva one vision的时候吧
`32:56` 那时候他们意识到哦得能处理不同尺寸的图像
`33:00` 你要是想搞视频
`33:01` 那就很明显了
`33:02` 你得需要某种动态分辨率
`33:03` 大家还记得我们从larva到lava one vision的演进过程吗
`33:08` 当时他们意识到必须能够处理不同尺寸的图像
`33:12` 如果要处理视频
`33:13` 那就很明显了
`33:15` 你需要一种动态分辨率机制
`33:17` 这个模型的基本逻辑是这样的
`33:19` 假设你有一张这样的图片
`33:21` 它可能会被编码为1万1000个token
`33:25` 而这张包含公式的小图可能只被映射为八个token
`33:29` 以此类推
`33:30` 那么他们具体是如何实现这一点的呢
`33:33` 基本思路是
`33:34` 这与lava中的任意分辨率概念是一致的
`33:37` 具体来说
`33:38` 每个224224的图像块都会通过VT进行编码
`33:42` 此外他们最初使用的是open clip的vet
`33:46` 但随后对其进行了微调
`33:49` 接着他们尝试压缩上下文长度
`33:52` 将每两个图像块合并为一个
`33:54` 从而减少每个图像的token数量
`33:56` 嗯所以QW2在这里采取了一些不同的做法
`34:00` 因此他们引入了一种称为多模态旋转位置编码
`34:04` multimodel rope的方法
`34:05` 大家还记得rope吗
`34:07` 它的核心思想是嵌入向量具备这样的特性
`34:10` 向量间的内积仅取决于相对距离
`34:13` 在一维情况下
`34:14` 距离即为两个token之间的间隔数
`34:17` 推广到多维版本时
`34:19` 原理基本一致
`34:20` 只是维度扩展到了三维
`34:22` 这三个维度分别是高度
`34:24` 宽度和时间
`34:26` 因此每个位置或图像块现在由一个三元组来表示
`34:31` 推广到多维版本时
`34:33` 原理基本一致
`34:34` 只是维度扩展到了三维
`34:36` 这三个维度分别是高度
`34:38` 宽度和时间
`34:40` 因此每个位置或图像块现在有一个三元组来表示
`34:43` 不过后来当我们审视QWM30会发现这其实并非最优解
`34:49` 于是他们用此来初始化语言模型
`34:52` 这是QWMV2模型
`34:54` 自然是以q w in为基础进行初始化的
`34:57` 接着他们进行了三阶段训练
`34:59` 这与我们之前的做法非常相似
`35:02` 随后他们展示了一系列不同能力
`35:04` 例如视频理解它既能处理数学和代码
`35:08` 也能进行函数调用等等
`35:10` 好的那我快速介绍一下QWE3嗯
`35:14` 所以再说一遍
`35:14` 这里有一些改动
`35:15` 我不认为这是结构上的重大变动
`35:18` 但它们确实会影响模型的质量
`35:19` 这是去年昆汀3V2报告里的图
`35:23` 所以这里的图表基本没变
`35:24` 我提一下
`35:25` 也许后面我会再回到这张图
`35:27` 我来梳理一下这些变化
`35:28` 首先现在他们用的是quin3模型
`35:31` 他们有一系列dance和贸易模型
`35:33` 困的三模型效果非常出色
`35:35` 这对提升最终模型的质量帮助很大
`35:37` 他们重点投入的是长上下文理解能力
`35:40` 上下文长度最高可达256K
`35:43` 这一点非常重要
`35:44` 尤其是当你试图处理长视频时
`35:46` 咱们来谈谈视觉编码器
`35:49` 他们用的是c click2
`35:51` 这基本上是对c click的改进版本
`35:53` 实际上这是完全相同的架构
`35:56` 其设计旨在向后兼容
`35:57` 随后他们改进了rope
`35:59` 咱们先回顾一下
`36:01` 以前只有三个维度
`36:03` 时间宽度和高度
`36:05` 简单来说
`36:06` 他们把第一块维度空间留给了时间
`36:09` 简单来说
`36:10` 他们把第一块维度空间留给了时间
`36:13` 接着给宽度维度
`36:14` 再给高度维度
`36:16` 如果你记得rope
`36:17` 问题就出在这
`36:18` 每个组件其实代表不同的频率
`36:20` 这就导致一个问题
`36:22` 时间维度可能全是低频
`36:23` 而高度维度全是高频
`36:25` 所以他们把这些维度交错排列了
`36:29` 意识到这点后
`36:30` 现在的每个轴都能同时接触到低频和高频信息
`36:35` 另外他们还做了一件事儿
`36:37` 直接引入显示的视频时间戳
`36:40` 所以他们将这些维度交错排列
`36:43` 意识到这一点后
`36:44` 现在每个轴都能同时接触到低频和高频信息
`36:48` 另外他们还做了一件事
`36:51` 直接引入显示的视频时间戳
`36:53` 哎呀嗯你看呃这个可能有点小
`36:57` 不过之前我们讨论过位置嵌入的问题
`36:59` 时间戳在位置编码里是隐含的
`37:02` 视频里的每一帧天生就带着不同的时间信息
`37:05` 之所以有时间概念
`37:06` 纯粹是因为他们位置不同
`37:08` 嗯但在这他们把时间显示化了
`37:10` 所以这些其实是token
`37:12` 0S就是一个token
`37:14` 现在你有了一个代表时间的token啊
`37:16` 他们发现这挺有用的
`37:19` 大概是因为你可以直接引用它
`37:21` 比如两秒后发生了什么
`37:23` 从而增强时间感知
`37:24` 0SS对应一个token
`37:26` 现在你拥有了一个代表时间的token嗯
`37:29` 他们发现这挺有用的
`37:31` 大概是因为你可以直接引用它
`37:33` 比如两秒后发生了什么
`37:34` 咱们得这么想
`37:35` 带视频的例子通常很长
`37:38` 但带单张图片的例子就很短
`37:40` 通常的做法是把每个token1视同仁
`37:43` 这样一来视频样本就会占据主导地位
`37:46` 所以嘛他们大概是具体细节我不太确定
`37:49` 但我猜他们是用视力长度的平方根做了归一化
`37:53` 主要是为了压低那些特别长样本的影响力
`37:56` 最后还有个挺有意思的点
`37:57` 就是他们在adapter上搞了点新花样
`38:00` 咱们回想一下
`38:00` adapter就是负责把视觉编码器和语言模型连起来的那个部件
`38:04` lava最简单就是个线性投影
`38:07` 后来咱们又加了MLP
`38:08` 还搞了种交叉注意力机制
`38:10` 咱们回想一下
`38:11` adapter就是负责把视觉编码器和语言模型连起来的那个部件
`38:17` lava最简单就是个线性投影
`38:20` 后来咱们又加了MLP
`38:22` 还搞了种交叉注意力机制
`38:24` 咱们基本上就是把它们直接加到语言模型的残差流里
`38:29` 所以这算是把视觉编码器和语言模型做了一种深度融合
`38:36` 即将视觉编码器与语言模型进行融合
`38:40` 这与仅输出向量序列的黑盒视觉编码器不同
`38:43` 训练过程分为两个阶段
`38:46` 预训练包含四个阶段
`38:48` 后训练
`38:48` 包含三个阶段
`38:49` 训练过程分为两个阶段
`38:51` 预训练包含四个阶段后训练
`38:53` 包含三个阶段
`38:54` 随后进入三个阶段
`38:55` 逐步在更长的序列上进行训练
`38:58` 序列长度从8K增加至32K再增至256K
`39:01` 大部分token集中在第二和第三阶段之后
`39:05` 进入后训练阶段
`39:06` 团队会使用长链思维数据进行监督微调sf t和知识蒸馏
`39:12` 并辅以强化学习
`39:14` 严格来说
`39:15` 这是一篇侧重系统实现的论文
`39:18` 我标注了一些核心创新点
`39:20` 但文中仍存在不少细节差异
`39:22` 从最终结果来看
`39:23` 这是一个相当出色的模型
`39:25` 也就是说
`39:26` 若将这些基准测试成绩与Gemini g p t-5
`39:29` OPPO4.1等必然模型进行对比
`39:32` 从最终结果来看
`39:33` 这是一个相当不错的模型
`39:36` 所以你看窥模型的实力其实很强
`39:39` 嗯对有同学提问好
`39:42` 下一位受害
`39:56` 以及谢谢
`39:59` 所以问题是在视频生成方面
`40:01` 你怎么判断输出应该是视频还是文本
`40:03` 首先得明确这些模型其实并不生成视频或图片
`40:07` 视频
`40:07` 或者说所有多模态内容都只存在于输入端
`40:10` 你们生成的始终是文本
`40:12` 所以首先得明确这些模型其实并不生成视频或图片
`40:18` 视频
`40:19` 或者说所有多模态内容都只存在于输入端
`40:23` 在这些阶段里
`40:25` 除了RLL你基本上是对每一个token进行监督
`40:28` 所以不存在比如用语言模型当裁判或者评估描述质量的情况
`40:33` 这主要取决于数据集中的内容
`40:35` 不过有了强化学习
`40:36` 你可以尝试不同的反馈机制
`40:38` 没错
`40:39` 我想确认一下这是否属于多模态模型的训练
`40:43` 在那些比肩更热的唇语系统中
`40:46` 从系统角度来看
`40:47` 所以从系统角度来看
`40:50` 我两位一对某些小组可以在空气中
`40:56` 只有让自己认识
`41:00` 每一个人努力测试
`41:03` 总算把暗灭落下来
`41:06` 而与你与我们之手
`41:09` 我更专注于那是完美的
`41:12` 所以从气功角度来看
`41:14` 我认为嗯对某些家族可以在某地中使用路易基的视频
`41:19` 比一个人用于测试
`41:21` 从算法方面
`41:22` 我想呃英语我们只是说我更专注于那是完美的
`41:26` 我的意思是肯定没那么简单
`41:28` 就像你提到的数据集规模更大了
`41:31` 视频数据嘛
`41:32` 你知道的
`41:32` 光是加载它就可能让人头大
`41:34` 通常我们没怎么特意讨论语言模型的数据加载问题
`41:37` 因为它通常不是主要的瓶颈
`41:40` 成本非常低
`41:40` 所以这点得考虑进去
`41:42` 同时确保数据加载与实际计算一步进行的那些原则也都适用
`41:47` 得考虑进去
`41:48` 第二部分嘛
`41:48` 抱歉刚才说到哪了
`41:50` 第二个问题咱们知道了哦
`41:52` 对视频和图像包含的token更多
`41:54` 尤其是视频
`41:55` 第二个问题咱们清楚了对了
`41:57` 视频和图像包含的token更多
`41:59` 尤其是视频
`42:00` 主要是为了防止视频在模型中占比过大
`42:03` 你看你可以直接调低权重
`42:04` 比如token数量太多
`42:05` 觉得不合适
`42:06` 就把权重降下来
`42:07` 咱们上次或者说是两周前聊过数据混合的问题
`42:11` 所以具体怎么加权
`42:13` 只要合理都可以灵活调整
`42:14` 不过文本token的数量其实也非常庞大
`42:17` 所以总的来说这些模型都是在数万亿个token上训练出来的
`42:22` 我觉得多模态token的数量并没有远远超过文本token
`42:26` 对好
`42:27` 你先问问关于X电子被CPI的识别器验证
`42:32` 我有个问题
`42:37` 我觉得多模态标准的数量并没有变变
`42:44` 超过等等
`42:44` 仇恨对哦
`42:46` 你先问了
`42:47` 关于X减4space cpi的适配器变更
`42:50` 我也有个问题
`42:51` 它怎么识别什么是语言
`42:53` 语言模型必须经过预训练
`42:55` 不然就没法对齐
`42:56` 也就没意义了
`42:57` 所以语言模型是冻结的
`42:59` 我们只训练适配器
`43:01` 把给定的视觉编码器和给定的语言模型连接起来
`43:04` 训练方法很简单
`43:05` 你先设定一个token预算
`43:07` 比如671对
`43:08` 就是token
`43:09` 然后开始训练
`43:10` 这里没有自适应阈值
`43:11` 没错
`43:14` 品牌间的差异
`43:16` 也就是品牌数量
`43:19` 你可以接着要跟品牌数量做个对比
`43:23` 所以说就遇到这张了
`43:25` 明天记得这个观点很有意思
`43:29` 拉哥病床虽然设计其他的G7V
`43:31` 但其定国分化是香港帝国目前完整的世界感情道对对
`43:38` 要求公主奇瑞的故事里面
`43:42` 黑人自己心意的报告对
`43:46` 所以问题来了
`43:47` 视觉编码器中的参数量要小得多
`43:50` 答案通常是肯定的
`43:52` 我在想还有什么不同之处
`43:53` 我嗯不在这儿
`43:57` 好的原因在于
`43:59` 从某种意义上说
`44:00` 视觉编码器执行的是非常局部的操作
`44:03` 操作
`44:03` 针对的是一个个小块
`44:04` patch小块非常小
`44:06` 它只是试图理解这些小块
`44:08` 由于缺乏足够的知识
`44:10` 我们无法对这些小块进行推理
`44:12` 因此该模型的核心能力主要仍源于语言模型
`44:15` 我确认一下
`44:16` 找到了大致位置
`44:17` 在这里我们来看一下
`44:19` 对了
`44:20` 你看这个lava模型
`44:21` 也就是完整版
`44:22` 它拥有721参数
`44:24` 是一个语言模型
`44:25` 还有呃抱歉
`44:28` 你刚才说什么好的
`44:29` 我不太确定这个数字是嗯
`44:33` 可能是个笔误啊
`44:35` 抱歉
`44:35` 这是7200万好吧
`44:36` 那确实少得多
`44:37` 投影器参数量小得多
`44:39` 外体通常不到10亿参数嗯
`44:42` 对我想主要是因为它是局部操作
`44:46` 作用范围有限
`44:47` 所以社会内容我们继续嗯
`44:49` 所以困三是我接下来要讲的最后一个视觉语言模型
`44:53` 他提出了一种名为save our performance的方法嗯
`44:58` 这背后涉及大量数据方面的工作
`45:01` 在后续的queen模型中
`45:03` 关于数据和数据混合的细节就不再那么详细了
`45:07` 所以建议大家去读一下喇叭的论文
`45:10` 或者I发布的那篇关于移动端的论文
`45:13` 我之前没来得及细讲
`45:15` 那篇里面的细节更丰富
`45:17` 从queen到queen2再到queen3
`45:19` 这些迭代中的许多变化都旨在提升模型性能
`45:23` 主要是发生了一些种类上的转变
`45:26` 但整体框架保持不变
`45:28` 主要是规模上的扩展整理更多的数据集
`45:31` 意识到必须处理好长上下文
`45:34` 以及可能优化图像处理
`45:36` 诸如此类好接下来咱们聊点不一样的
`45:40` 差别很大
`45:41` 咱们来看一篇meta在2024年发布的论文
`45:45` 叫chameleon
`45:46` 到目前为止
`45:48` VRM的做法是把图像编码成向量
`45:51` 再输入到语言模型里
`45:52` 毕竟它是语言模型
`45:54` 所以只能生成文本
`45:56` 没法生成图像
`45:57` 要解决这个问题
`45:59` 其实有很多路径
`46:01` 你可以直接拿一个VRM再挂载一个扩散头
`46:04` 这样就能生成图像了
`46:05` 但我想聊聊这个思路
`46:07` convenient的想法是
`46:08` 要是我们把所有内容都映射成离散的token
`46:11` 能某种程度上从审美角度看
`46:13` 这呃好吧
`46:15` 也许这说明我是个搞语言的
`46:17` 更关注文本部分
`46:18` 这很有吸引力
`46:19` 因为现在你可以用同一种方式分析和生成图像
`46:22` 一切都被离散化为token
`46:24` 打个比方
`46:25` 假设你想生成图像
`46:28` 你可以输入一个文本提示
`46:30` 然后系统会生成更多的token
`46:33` 或者你也可以反过来把语言和文本的顺序调换一下
`46:37` 这里有个来自肯定有论文的事例
`46:39` 你可以试着给他发个提示
`46:40` 比如输入我无聊了
`46:42` 给我看点鸟的照片
`46:44` 接着它会输出文本
`46:45` 再输出图像
`46:46` 然后再是文本和图像
`46:47` 所以文本和图像是交替出现的
`46:50` 某种意义上
`46:50` omi model的设计理念正是如此
`46:52` 文本和图像真正处于同一个空间
`46:55` 这是通过让所有数据都表现为文本形式来实现的
`46:59` 这是一种可行的方法
`47:00` 谢谢好
`47:01` 接下来我简要讲讲具体是如何实现的
`47:04` 那么你现在需要做的事情是嗯
`47:08` 是将图像映射为离散的token
`47:11` 这个想法源自2017年o word等人的研究
`47:15` 虽然由来已久
`47:16` 它叫向量量化变分自编码器
`47:19` 核心思路是把图像映射成离散码字
`47:23` 你需要学的是一个映射
`47:25` 把它转成连续向量
`47:27` 然后把它舍入到最近的码字
`47:29` 所以你会得到一个码本
`47:31` 假设里面有8000个码字
`47:33` 这些就像圆形向量对应着图像块
`47:36` 然后将其舍入到最近的码字
`47:39` 因此你会拥有一个码本
`47:41` 假设其中包含8000个码字
`47:43` 这些就像是圆形向量对应于图像块
`47:46` 把它舍入到最近的码字
`47:48` 接下来解码器的工作就是拿到这个码字
`47:51` 试着重建图像
`47:52` 然后你训练这个VQE啊
`47:55` 基本就是这么回事
`47:56` 主要是最小化重建损失
`47:57` 因为这里不可微
`47:59` 所以还得加一些其他项
`48:00` 不过我没时间细讲好了
`48:02` 总之最后的结果是512×512的图像
`48:06` 被转换成了1024个token
`48:08` 每个token都来自一个大小为8000的词汇表
`48:11` 谢谢好
`48:13` 现在你手头基本上既有文本
`48:15` 也有图像了
`48:17` 这些基本上就是相关代码
`48:19` 接下来我们需要训练一个新的token分词器
`48:22` 因为情况发生了变化
`48:23` 现在的数据与以往仅处理纯文本是有所不同
`48:26` 即自然语言数据
`48:28` 接下来进入训练阶段
`48:31` 这一步其实非常直观
`48:32` 就是标准的语言模型训练
`48:34` 这里不需要适配器
`48:35` 也无需调整视觉编码器
`48:37` 仅使用语言模型即可
`48:39` 这部分设计颇具吸引力
`48:41` 尽管仍分为两个阶段
`48:43` 但即便是语言模型训练通常也包含两个或更多阶段
`48:46` 第一阶段是训练的主体规模庞大
`48:49` 且为无监督学习模型中
`48:51` 同时包含文本和图像数据
`48:53` 随后进入第二阶段
`48:55` 我们引入高质量数据
`48:57` 这一设计确实十分优雅
`48:59` 但其中存在几个问题
`49:00` 首先他们发现训练过程不太稳定
`49:03` 原因在于
`49:04` 尽管文本和图像处于同一空间
`49:07` 但两者的表现差异显著
`49:09` 所以仅仅把图像数据也称作离散的token
`49:12` 并不能掩盖图像本身依然存在的本质
`49:15` 尤其是文本token
`49:16` 你想想看
`49:17` 要预测下一个词
`49:18` 它的熵值其实是比较低的
`49:21` 大部分词多少都能猜到
`49:23` 但图像token的商非常高
`49:25` 我根本没法确定这个token具体会呈现什么
`49:28` 蓝色结果就是这种混合训练引发了一系列范数方面的问题
`49:35` 参数会不断增大
`49:36` 进而导致损失值不稳定
`49:38` 他们利用QK范数和Z减loss正则化
`49:40` 在一定程度上缓解了这一问题
`49:43` 其中最简loss正则化用于控制范数的增长
`49:46` 具体的实验结果我就不展示了
`49:49` 我想重点介绍这项工作
`49:50` 因为它有一种简洁之美
`49:52` 它仅用一个模型就同等对待所有模态
`49:55` 但是缺点在于
`49:57` 事实证明该模型的性能并不理想
`49:59` 此外离散化过程必然会导致信息丢失
`50:03` 我们可以OCR为例
`50:04` 如果对极小的印刷文字进行离散化
`50:08` 你将无法再识别它们
`50:10` 最后多模态训练也颇具挑战性
`50:13` 我们在queen模型中以略见端倪
`50:15` 当时需要反复调整权重
`50:17` 而在此处
`50:18` 这一问题更为显著
`50:19` VQ减VAE曾经非常流行
`50:22` 当时许多人将其用于图像生成
`50:25` 主要原因在于
`50:25` 如果你手头有一个transformer
`50:27` 该如何让它生成内容
`50:28` 你需要生成离散数据
`50:30` 即将所有数据转化为离散形式
`50:33` 后来扩散模型兴起
`50:35` 成为了一种可行的生成方案
`50:36` 因此这类方法如今已不如从前流行
`50:39` 我们在此简要回顾并总结当前的前沿模型
`50:43` 预计都将具备多模态能力
`50:45` 尤其是原生多模态或全模态模型
`50:48` 遗憾的是
`50:49` GEMINI和GPP发布时被宣传为原生多模态
`50:52` 且能处理所有模态
`50:54` 事实也确实如此
`50:55` 但具体的架构细节并未公开
`50:58` 我推测可能采用了某种例如被保留信息不丢失
`51:02` 可能使用了连续编码器
`51:04` 生成部分则采用了扩散模型
`51:06` 当然这仅是推测
`51:07` 我认为多模态最大的难点在于如何处理非文本模态
`51:12` 还有一个有趣的现象
`51:13` 理解模态和生成模态之间存在一种对称关系
`51:16` 而且具体情况各不相同
`51:18` 也就是说并不存在一种通用的编码器
`51:20` 以clip为例
`51:21` 你主要关注分类目的是提取高层语义
`51:24` 因此这些向量可以做得很小
`51:26` 同时仍能捕捉到高层语义
`51:28` 相反如果你要做光学字符识别或图像生成
`51:32` 就需要非常精细的细节
`51:34` 这正是扩散模型表现优异的原因
`51:36` 因为它能对高低频信息进行微调
`51:39` 谢谢我觉得在处理多模态问题时
`51:42` 我们需要仔细思考
`51:44` 要把它们平衡好
`51:45` 打个比方
`51:46` 视频的信息密度通常低于文本
`51:49` 所以你不想让视频淹没文本
`51:51` 就像我之前提到的
`51:52` 也许目前最好的做法是使用连续编码器
`51:56` 看起来即使科比已经是5年前的技术
`51:58` 类似的思路现在仍是捕捉图像语义的首选方案
`52:02` 传送门仍在广泛使用
`52:05` 至于扩散模型
`52:06` 我刚才没有详细展开
`52:08` 它在生成方面非常出色
`52:09` 好我就讲到这里
`52:12` 关于多模态的讲座就讲到这里
`52:14` 这部分没有实操作业
`52:16` 但如果你感兴趣
`52:18` 我鼓励你去动手试试
`52:20` 比如训练这些模型
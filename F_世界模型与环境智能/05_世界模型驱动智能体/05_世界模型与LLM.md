# 世界模型与 LLM

LLM 擅长语言知识、计划表述和工具调用，但其文本续写并不保证对当前环境状态、执行副作用或物理约束有校准预测。世界模型可补足这一缺口。

## 协同模式

- LLM 提议目标、计划、代码或工具调用；世界模型模拟后果并筛选。
- 世界模型把空间、网页、软件或机器人状态压缩为可供 LLM 查询的结构化摘要。
- LLM 将失败轨迹解释为规则/假设，指导模型采集新的验证数据。

关键是使语言计划可落到明确状态、动作和可验证后置条件，避免仅以语言自洽替代环境验证。
# 环境接口

环境接口把世界转换为 Agent 可交互的契约。最小接口包括 observation、action、reward、terminated/truncated 和 info；真实系统还应补充时间戳、延迟、权限、风险、成本和状态版本。

```text
reset() → 初始观测
step(action) → 观测、奖励、终止、约束/诊断信息
```

OpenAI Gym 风格适合 RL；模拟器 API 适合物理控制；工具/API 环境还要定义参数模式、权限、幂等性、失败恢复和审计。接口设计决定世界模型能学习什么，也决定评测是否可复现。
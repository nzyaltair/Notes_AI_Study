# 基于世界模型的规划与控制

模型预测控制在有限时域内比较候选动作：

$$\min_{a_{t:t+H-1}}\mathbb E[\sum_{k=0}^{H-1}c(s_{t+k},a_{t+k})+c_T(s_{t+H})]$$

执行时只下发首个动作，再观测和重规划。世界模型可支持技能选择、失败预警、主动感知和风险敏感规划。

关键原则：预测的不确定性应进入代价或约束；预测—观测不一致时触发状态更新、降级或恢复，而非继续盲执行；碰撞、力矩和急停约束仍由独立安全层裁决。
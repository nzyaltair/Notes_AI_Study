---
title: "【极致中配】2026年最新版 Stanford CS336: 从头构建大语言模型"
url: "https://www.bilibili.com/video/BV11LEA6eEuj/?p=4"
bvid: "BV11LEA6eEuj"
cid: "39162742456"
author: "常青藤名校课"
upload_date: "2026-06-05"
created: 2026-08-17
tags: ["bilibili", "clippings"]
---

## 简介

如果这个中文配音版对您理解 CS336 有帮助，欢迎点赞、投币、收藏支持一下，也可以点个关注。
我会持续更新更多 Stanford / AI / 大模型相关课程的中文配音课程，尽量帮大家用更低的理解成本看懂这些高质量课程。你的支持会让我更有动力把后续内容继续做完、做好。

## 字幕

`00:00:00` 好我们开始抱歉
`00:00:01` 今天的课件之前没挂出来
`00:00:03` 现在已经上传了
`00:00:04` 如果你想随时翻看
`00:00:06` 可以通过课程网站获取啊
`00:00:07` 如果课程网站上没有
`00:00:08` GITHUB上肯定有
`00:00:09` 今天我们要聊的是
`00:00:10` 我认为更高级的架构思路上一讲
`00:00:13` 主要讲基础transformer
`00:00:15` 以及如何调整基础transformer的不同部分
`00:00:18` 来构建现代语言模型
`00:00:20` 现在我想谈谈我认为更进阶更复杂的内容
`00:00:24` 也就是在常规transformer之上的新进展
`00:00:28` thing今天我要讲两件事
`00:00:30` 第一件是attention的替代方案
`00:00:32` 也就是通过架构改动来支持更长的context
`00:00:35` 这些改动在大多数情况下
`00:00:38` 能让时间复杂度随序列长度成linear time dependence
`00:00:42` 而不是quadratic
`00:00:44` so第二件我想谈的是mixture of experts的概念对吧
`00:00:48` 第一部分是修改attention模块
`00:00:50` 第二部分是修改MLP部分
`00:00:52` which your experts能显著提升硬件利用率
`00:00:54` 并在给定computer预算下支持更多的参数量
`00:00:57` 这就是今天要讲的两个部分和两个核心想法
`00:01:01` 所以我先从attention讲起啊
`00:01:03` 先把背景铺一下啊
`00:01:05` 现在很清楚大家需要更长的context length
`00:01:07` 你希望把大量内容塞进context
`00:01:10` 让模型拥有更多知识
`00:01:11` 也可能它是一个agent
`00:01:12` 需要处理很多东西
`00:01:13` 如果你看这张图
`00:01:14` 横轴是随时间推移的不同模型
`00:01:17` 纵轴是context window大小采用对数尺度
`00:01:20` 可以明显看到
`00:01:21` 许多顶级LM厂商都在竞相提供
`00:01:23` 越来越大的context规模
`00:01:25` 以支持这类更复杂的工作负载啊
`00:01:28` 那么我们该如何控制这些成本呢
`00:01:31` 目前还没有答案
`00:01:32` 右边这张图还展示了
`00:01:34` 随着序列长度增加
`00:01:36` 网络中feed for部分与attention部分
`00:01:38` 各自消耗的computer成本之比
`00:01:40` 可以看到
`00:01:41` FEEDFORWARD在初期占比较大
`00:01:43` 但呈线性增长
`00:01:44` attention是所有位置之间的auto all
`00:01:47` 连接复杂度是quadratic
`00:01:49` 因此随着序列长度增加
`00:01:50` 会迅速超过FEEDFORWARD的开销
`00:01:52` 过去对于序列长度较短的大模型
`00:01:55` feed forward是主要的开销
`00:01:57` 随着序列长度增加
`00:01:58` attention的开销问题日益凸显
`00:02:01` 谢谢
`00:02:02` 那么我们可以用来控制这些开销的
`00:02:05` 基本工具有哪些
`00:02:07` 我们已经讨论过这些工具
`00:02:08` 或者说我们讨论过其中之一
`00:02:10` 并将讨论另一个
`00:02:12` 基本上我们可以采用诸如local attention这样的方法
`00:02:15` 并将它们以不同的混合方式组合
`00:02:17` 以控制global attention极高的开销
`00:02:20` 如果你每八层采用一次GB attention
`00:02:23` 其余层都只用这种高度局部的local attention
`00:02:27` 成本就能得到很好的控制
`00:02:29` 嗯另一条路是系统工程
`00:02:31` 很多人低估了一个事实
`00:02:33` constant factors其实至关重要
`00:02:35` 这门课的主题之一就是你需要关注细节
`00:02:39` 受过传统理论导向计算机科学训练的人
`00:02:42` 很容易觉得啊对
`00:02:43` 重要的是o notation是linear还是quadratic
`00:02:45` 不过我认为对context和attention
`00:02:49` 成本影响最大的进展之一就是flash attention
`00:02:52` 关于flash attention
`00:02:53` 我们在后面的系统课程和系统作业中
`00:02:56` 会更详细的讨论
`00:02:57` 但flash attention其实就是一种非常巧妙的做法
`00:03:00` 把tension运算重新组织成对系统更友好的形式
`00:03:04` 从而最小化memory transfer
`00:03:06` Overhead join
`00:03:07` 座能为attention的性能带来真正真正惊人的提升
`00:03:11` 所以原生PYTORCH是蓝色这条线
`00:03:14` 你可以看看那些序列长度较短的情况
`00:03:16` 那我们大概能做到每秒30~40tr flops
`00:03:20` 接着你用flash attention
`00:03:22` 这本质上是常数因子层面的系统改进
`00:03:25` 你能获得巨大的提升
`00:03:26` 比如性能翻倍到了某些规模
`00:03:29` 你甚至没法把它塞进内存里
`00:03:30` 但有了flash attention
`00:03:31` 由于不再显示生成这些大型attention矩阵
`00:03:34` 你还能继续跑
`00:03:34` 只是会稍慢一些
`00:03:36` 这并不能解决quadratic成本的问题
`00:03:38` 但常数因子的优化非常强大啊
`00:03:41` 不过话说回来
`00:03:42` 如果要处理five hundred one到1000多万个token
`00:03:44` 这些技巧可能就不够用了
`00:03:46` 嗯我们需要在长上下文处理能力上获得更激进
`00:03:49` 更大幅度的提升
`00:03:51` 所以这就引出了一个问题
`00:03:52` 有没有办法让我们对序列长度实现线性时间
`00:03:56` 复杂度或线性依赖
`00:03:57` Thank you
`00:03:57` 那些方法具体是什么样的
`00:03:59` 谢谢好的
`00:04:01` 请看事实证明
`00:04:03` 多年来
`00:04:04` 许多人尝试过各种各样的方法
`00:04:07` 因此我认为其中有一些走了弯路
`00:04:10` 但近几年大概过去两年左右
`00:04:12` 开始出现了一些新的进展
`00:04:14` 出现了一套在linear time attention上相当有效的方案
`00:04:17` 这些方案如今已在大规模场景下
`00:04:19` 经过实战检验啊
`00:04:20` 这也是为什么今年是我第一次讲
`00:04:22` linear time attention的原因
`00:04:23` 因为现在已经很清楚
`00:04:24` 这些方法在大规模部署和生产环境中
`00:04:27` 确实行之有效
`00:04:29` 要真正解释所有这些行之有效的方法
`00:04:31` 我认为你只需要先理解一个核心思想
`00:04:34` 然后我们会在此基础上展开和深入
`00:04:36` 而这个起点性的核心思想就是乘法的结合律
`00:04:40` 那么我们来思考一下attention是如何工作的
`00:04:43` 我们可以用这种紧凑的技法来表示
`00:04:45` attention操作由QK和V组成对吧
`00:04:48` 其矩阵维度如上方所示
`00:04:50` On tuman
`00:04:51` 接下来我会把Q和K拿出来
`00:04:53` 让他们做all to all交互对吧
`00:04:55` 所有的位置都会通过矩阵乘法相互作用
`00:04:58` 然后我会通过这一行用soft max进行归一化
`00:05:01` 离开后我会把它和值V相乘
`00:05:04` 得到attention的输出
`00:05:05` 对啊
`00:05:06` 这就是经典的self attention
`00:05:08` 或者其他类似的attention操作啊
`00:05:10` 现在我们先暂时忘掉attention里有一个soft max
`00:05:14` 我直接把这一行去掉啊
`00:05:16` 先把它放一边
`00:05:17` 如果没有这一行
`00:05:18` 我们或许真的可以改变这个操作的复杂度
`00:05:21` 所以你看attention
`00:05:22` 它的复杂度是in the puts on dollar DK
`00:05:26` 因为我们做了这个QK转制乘法
`00:05:29` 但如果这一行是单位矩阵
`00:05:31` 我只要稍微调整一下括号的位置就行
`00:05:34` 简单来说原本要做QK转至V
`00:05:37` 现在实际上就是直接做QK转至V
`00:05:40` 经过这种重排序之后嗯
`00:05:42` 二次复杂度的部分就发生了变化
`00:05:44` 我们不再需要N的平方向了
`00:05:46` 因为那太糟糕了
`00:05:47` N的值动辄数百万
`00:05:49` 也就是context length啊
`00:05:51` 取而代之的是计算量将取决于NDV和DK
`00:05:55` 以现在我们本质上是在对键和值的维度做乘法
`00:05:59` 理论上这些维度可以很大
`00:06:01` 模型也可以很大
`00:06:02` 但他们通常只在几千到几万的量级
`00:06:04` 没人会在隐藏维度里设置百万级的坐标
`00:06:08` 所以相比原始tension操作中的N平方向
`00:06:12` 这一项的依赖关系要友好得多
`00:06:14` 所以这是你首先要了解的一点
`00:06:17` 乘法的结合律
`00:06:18` 让我们得以改变对序列长度的依赖关系
`00:06:21` GI代嗯
`00:06:23` 接下来我们可以做的另一件事是
`00:06:25` 如果我们考虑这种重排序
`00:06:27` 也就是类似QK转至V的形式
`00:06:30` 我认为它更棒的一点在于它促成了大量研究
`00:06:34` 我将在接下来几张幻灯片中提到的是
`00:06:37` 在self attention场景下
`00:06:39` 右侧的这个矩阵乘法看起来就像一个RNN
`00:06:42` 我不必像上面那样写成这种密集形式
`00:06:45` 而是可以在从左到右遍历context时
`00:06:48` 以增量方式逐步书写
`00:06:50` 我可以对K和V进行相乘更新这个状态S
`00:06:55` 并将其累加到之前的状态上
`00:06:58` 这类似于对KV进行增量式的相乘与累加
`00:07:01` 然后我可以将其与Q相乘
`00:07:03` 以得到输出
`00:07:04` You know
`00:07:04` 上面这种密集形式的操作和这种RNN形式
`00:07:08` 在某种程度上是等价的
`00:07:10` 那么我们做了什么
`00:07:12` 我们从左上角的linear attention开始嗯
`00:07:14` 然后我们以某种方式得到了一个
`00:07:17` 看起来非常像RNA的结构
`00:07:20` RNA的优点是什么
`00:07:22` origin相当不错
`00:07:24` 因为在推理时
`00:07:25` 我有这个S也就是我的状态
`00:07:27` 它的大小是固定的
`00:07:28` 我只是不断的将其向前传递
`00:07:31` 因此出于推理方面的考虑
`00:07:33` RNN非常理想
`00:07:34` 为他们并不理想
`00:07:36` 从训练的角度来看是这样
`00:07:37` 但linear tension的好处在于
`00:07:39` 你可以用两种方式中的任意一种
`00:07:41` 你可以采用这种密集形式
`00:07:44` 这对训练非常有利
`00:07:45` 它是并行的
`00:07:46` 或者你可以采用这种类似RN的串行形式
`00:07:49` 这对推理非常有利
`00:07:50` 这因此你可以兼得两者之长
`00:07:53` 现在嗯至少在所有这些对象的依赖关系方面
`00:07:56` 这非常棒
`00:07:57` 这很好嗯
`00:07:58` 不幸的是
`00:07:59` 这是线性的
`00:08:00` 这不太理想
`00:08:02` 嗯这基本上就是我们讨论的起点嗯
`00:08:05` 目前有什么问题吗
`00:08:07` 这确实是基础内容
`00:08:08` 有个挺有意思的点
`00:08:09` 是
`00:08:10` 有人把这种linear attention
`00:08:11` 用进了他们的attention机制里啊
`00:08:14` 比如mini max m1
`00:08:16` 这是一个规模相当大
`00:08:17` 性能相当强的中文开源模型
`00:08:20` 它采用了七比一的混合方案
`00:08:22` 也就是七个linear tension层
`00:08:24` 再加上一个完整的softmax attention
`00:08:26` 性能整体很强
`00:08:27` 你可以拿它和open a s o three或者d pc gard one
`00:08:29` 这强模型比较
`00:08:30` 竞争力相当不错
`00:08:31` 而且由于它是linear tension
`00:08:34` 对context length的依赖大部分是线性的
`00:08:36` 它并不是完全线性的
`00:08:37` 因为里面还有这些soft max
`00:08:39` 但这种依赖要温和的多
`00:08:42` 对要举几个例子表明
`00:08:44` 即使是这种非常容易理解的basic linear attention
`00:08:47` 只要配合某种完整的dot querratic connections
`00:08:51` 也可以在大规模下相当有效的使用
`00:08:54` 到目前为止
`00:08:55` 还没有人在大规模上真正验证
`00:08:57` Fully linear time attention mechanisms
`00:08:59` 接下来几页要讲的内容都是混合架构诶
`00:09:03` 我们已经讲过linear attention
`00:09:04` 但你可能会觉得linear attention对你来说太天真
`00:09:07` 太简单了
`00:09:08` 这不是我想要的
`00:09:09` 我希望看到更严肃更复杂的神经网络机制
`00:09:13` 那么你现在能做的是呃
`00:09:16` 哦抱歉
`00:09:17` 走的有点远了
`00:09:18` 我们来看linear attention的RNN形式
`00:09:20` 我把linear attention的方程放在这里
`00:09:23` 这样你就不用来回翻看了
`00:09:24` 我们可以对linear attention做一点扩展
`00:09:27` 从而获得表达力更强的更新
`00:09:29` 这就是mob two的由来
`00:09:30` 你们中有些人可能听说过mama
`00:09:32` 我想现在也听说过mob two
`00:09:34` mob three了
`00:09:35` 这些是由albert gu treat out
`00:09:36` 及其合作者提出的一类状态空间模型
`00:09:39` 就嗯
`00:09:40` 他们最初是从这种状态空间
`00:09:44` 理论和视角推导出来的
`00:09:46` 但实际上如果你审视所执行操作的机制
`00:09:50` 你会发现
`00:09:51` 它实际上可以视为线性attention机制
`00:09:53` 的一种非常简单的扩展
`00:09:55` 你的思路是这样的
`00:09:57` 我们从线性attention开始
`00:09:59` 你可能会说线性attention最大的问题在于
`00:10:02` 他总是把状态一直往后传
`00:10:05` 从早期的LSTM时代
`00:10:07` 我们就知道
`00:10:08` 关键在于知道什么时候该把信息往后传
`00:10:11` 什么时候不该传
`00:10:13` 也就是把信息忘掉
`00:10:14` 直接归零
`00:10:15` 我们要做的是受此启发
`00:10:17` 加入一个门控gamma
`00:10:19` 也就是gamma underscore t
`00:10:20` Dogma
`00:10:21` underscore t本身没有状态
`00:10:23` 它只取决于我当前的输入x t gamma underscot
`00:10:27` 用来调节我会把多少状态信息传递到未来
`00:10:31` 现在如果你仔细想想
`00:10:33` 状态依赖项就是所有这些S嗯
`00:10:37` 这个gamma完全不依赖状态
`00:10:39` 因此这意味着计算非常简单
`00:10:41` 这里仍然存在某种二元性
`00:10:43` 你可以将mob two项计算为大型稠密矩阵乘法
`00:10:47` 或在推理时使用这种循环形式
`00:10:50` 从而获得良好的推理性能提升
`00:10:52` 这就是其核心思路
`00:10:53` 你可以像线性attention那样
`00:10:55` 在并行或序列模式下使用它啊
`00:10:59` 如今人们已在大规模场景中应用了这一方法啊
`00:11:03` 比如neo tron three
`00:11:04` 我在上一讲中简单提过
`00:11:06` 他就把mob two用作轻量级层
`00:11:08` Turf in curse
`00:11:09` Supplied in washing and soft max attention
`00:11:11` 他们以各种方式交替使用
`00:11:13` 以管理推理成本与表达能力之间的权衡相比
`00:11:16` quenthree thinking和g p t i s s neutron three
`00:11:18` 的性能表现相当不错
`00:11:20` Um nebe called the anwer
`00:11:23` a fevers touring that you by the context link下
`00:11:25` 也能保持较高的throughput
`00:11:27` 所以你可以看到它确实奏效了
`00:11:29` 虽然这些只是小型前沿模型
`00:11:30` 不是那种真正的大型前沿模型
`00:11:32` 但他们是开源的小型前沿模型
`00:11:34` 哎你你太棒了
`00:11:36` 谢谢
`00:11:36` 所以现在我们基于这种门控思想进行了构建
`00:11:39` 这很不错
`00:11:40` 但你可能会想
`00:11:41` 我们能不能把这个想法继续推得更远
`00:11:44` 让这种递归结构变得越来越复杂
`00:11:47` 嗯从某种意义上说
`00:11:49` 关于你能在这里做的操作
`00:11:51` 有一个经验法则
`00:11:52` 只要你对RNN中的各项进行门控处理
`00:11:55` 如果只包含依赖输入的项
`00:11:58` 而不依赖状态
`00:11:59` 那么你依然能在并行操作用于训练和串行操作
`00:12:03` 用于推理之间保持相当好的对偶关系
`00:12:06` Used
`00:12:06` 所以get adult connect
`00:12:08` 可能是目前应用最广泛的模型之一
`00:12:10` 我会说它属于现在所用的状态空间模型
`00:12:14` 他已经在相当多的论文中得到了验证
`00:12:17` 而且针对这类模型
`00:12:18` 在3.5版本中进行了非常出色的大规模扩展
`00:12:21` 那么让我来讲解一下ga da net是什么
`00:12:24` 并将其与mua two做个对比
`00:12:26` 所以mum two的情况是
`00:12:28` 如果在上面所示
`00:12:29` 我们采用线性的递归形式
`00:12:31` 即状态更新
`00:12:33` 而我要做的是用gamma对它进行门控对吧
`00:12:36` 那哦我忘了解释这个VT
`00:12:37` 抱歉嗯
`00:12:38` 我加上他是为了保持内容的完整性
`00:12:41` number two还做了一处修改
`00:12:43` 他们在YT更新中加入了
`00:12:45` 本质上算是一个残差层的东西
`00:12:47` 这并不是状态更新的核心
`00:12:49` 也不是他们主张这一做法是好主意的核心论据
`00:12:52` 但我把它加进来是为了完整性
`00:12:54` 确保我给出的是真正完整的mua two更新公式
`00:12:57` 而不是只传达核心思想的简化版本
`00:12:59` 嗯好的
`00:13:00` 不过你现在可以先忽略VT
`00:13:02` 这其实只是一种架构上的改进
`00:13:04` 并不是状态更新的核心部分
`00:13:07` get adult net在某种意义上借鉴了这个思路
`00:13:10` 并加入了第二个门控beta t
`00:13:12` 那么beta t的作用是什么啊
`00:13:14` 我把beta t看作是一个无输入操作门控
`00:13:17` 如果贝塔T为零
`00:13:19` 那基本上就意味着不要采用我当前的任何信息
`00:13:22` 不要把它加入到我的状态中
`00:13:24` even嗯
`00:13:25` 我想大家应该都对LSTM比较熟悉
`00:13:28` 这让人想起LTM的思路
`00:13:29` 它通过门控机制来决定是否遗忘
`00:13:32` 以及是否从当前时间部输入信息
`00:13:34` 所以而它遵循的原则非常相似
`00:13:36` 尽管我认为其最初的推导方式截然不同
`00:13:40` 另另外GADOTNET有意思的一点在于
`00:13:43` 这个更新方向这个特性其实来自DOTNET
`00:13:46` 而不是GADOTNET
`00:13:47` 所以与其只做门控
`00:13:49` 比如你也可以直接把这一项设为一减去贝塔T
`00:13:52` 这他们的做法是
`00:13:54` 当你要进行包含这个beta的更新时
`00:13:57` 你会尝试以某种方式将当前的key方向投影出去
`00:14:01` 所以这里的直观理解是
`00:14:03` 我要为当前的key也就是kt库写入信息
`00:14:08` 而在做这件事时
`00:14:09` 我不仅想写入新信息
`00:14:11` 还想擦除之前已经写入其中的所有key
`00:14:14` 这思考更新的一种视角
`00:14:15` 不仅仅是写入新信息
`00:14:17` 我还要清除已经写入的当前key
`00:14:20` 这就是那个蓝色向单位矩阵减去贝塔
`00:14:23` understate t乘以KTKT的转置的作用
`00:14:25` 它本质上是个投影器
`00:14:27` 用来投影掉kt维度上的内容
`00:14:29` 这并不完全精确
`00:14:30` 因为你并没有做单位归一化之类的操作
`00:14:33` 但大致的直观理解是对的
`00:14:35` 嗯在继续往下讲之前
`00:14:37` 我要再提一件事
`00:14:38` 哎呀抱歉
`00:14:39` 我这里有个蓝色方框
`00:14:40` 我接着讲
`00:14:41` Ga dotnet
`00:14:42` 他在不同场景下其实被重新发明过好几次啊
`00:14:46` 如果你试图解决某些原学习最小二乘问题
`00:14:49` 就会出现这种更新这种投影器
`00:14:52` 所以他在快速权重编程或测试时训练中
`00:14:55` 以其他形式被重新发明过
`00:14:57` 那些领域的研究人员基于完全不同的设计原则
`00:15:00` 最终得出了几乎完全相同的解决方案啊
`00:15:03` 谢谢我想说的是嗯
`00:15:06` quthree point five以及作为其前身的queen nex模型
`00:15:10` 是目前最优秀的开源模型之一
`00:15:13` 嗯他们正式采用了这种架构
`00:15:15` 使用的是三比一的门控DOTNET
`00:15:17` 注意力混合架构啊
`00:15:19` 性能非常优秀且合理
`00:15:20` 推理特性出色呃
`00:15:22` 如右侧所示
`00:15:22` 随着context l不断增加
`00:15:24` q one nex的解码吞吐量远高于q one3
`00:15:27` 中间面板
`00:15:28` 展示了该模型与各种闭源模型及上一代模型的
`00:15:31` 性能对比
`00:15:32` 这种混合架构似乎对性能几乎没有任何损害
`00:15:35` 最后我要说的是
`00:15:37` 目前关于混合架构性能的高质量对照研究
`00:15:40` 并不多
`00:15:41` 字节跳动
`00:15:41` seat和加州大学圣克鲁兹分校的研究人员
`00:15:44` 或许做过一项
`00:15:45` 他们比较了moa two gated
`00:15:47` Dotnet
`00:15:47` 以及其他架构在不同混合层数下的表现啊
`00:15:50` 部分结果我认为有些杂乱
`00:15:52` 但我想指出的是
`00:15:54` 右侧面板上的这条虚线
`00:15:56` 这条线是for tension的性能
`00:15:58` 从左往右看
`00:15:59` 非for attention层的数量在逐渐增加啊
`00:16:02` 我们把这些称为RNN层
`00:16:04` 从左往右
`00:16:05` RN层越来越多
`00:16:06` 性能也随之下降
`00:16:07` 但需要注意的是
`00:16:08` 对于一些表现最好的架构
`00:16:10` 比如黄色
`00:16:11` 橙色
`00:16:11` 蓝色这些类似gate dotnet和其他各种变体
`00:16:15` 在低比例下基本没有性能损失
`00:16:17` 但一旦超过某个临界点
`00:16:19` 就会出现更明显的性能下降
`00:16:21` 本例中是long context
`00:16:23` 性能的下降
`00:16:24` 到最后完全切换到RNN时
`00:16:26` 性能下降会变得非常明显
`00:16:28` 所有这些架构都是如此
`00:16:30` 在其他各种评估中情况也一样哦
`00:16:33` 啊
`00:16:34` 我认为其中一些指标架构本身就已经
`00:16:36` 针对他们做了优化
`00:16:38` 比如单键检索这种任务
`00:16:39` 我认为所有这些long contest架构都明确
`00:16:41` 针对它做了优化
`00:16:43` 但如果查看QA性能
`00:16:44` 我们会看到类似的情况
`00:16:46` 随着混合比例的增加
`00:16:47` 性能呈现出相当稳定且清晰的下降趋势
`00:16:51` 最终到达这一纯线性点
`00:16:53` 好的关于线性时间注意力
`00:16:55` 大家还有问题吗
`00:16:56` 我还有最后一个高效注意力的话题要讲
`00:16:59` 不过请讲回到最初的问题
`00:17:01` 你是否比较过attention的并行形式与线性形式
`00:17:06` 说实话
`00:17:07` 他们难道不应该自动就等价吗
`00:17:09` 韦神抱歉
`00:17:10` 我所说的循环recurrent是指你知道的
`00:17:13` 我泛指RNN即任何循环结构
`00:17:16` Then one
`00:17:16` 因此门控DOTNET属于一种循环结构
`00:17:19` 但这与上面那一个并不等价
`00:17:21` 这是带有行规约的
`00:17:23` 全soft max attention
`00:17:24` 第一步是有损的
`00:17:25` 我们在此丢弃了形
`00:17:26` 随后便变为线性形式
`00:17:28` 这是所有这些方法最初始的一步
`00:17:31` 因此这部分是有损的
`00:17:32` 而在此之后
`00:17:34` 从这种线性形式到这种循环形式的等价性
`00:17:37` 则是精确的
`00:17:39` 感谢我们
`00:17:40` 谢谢你做得很好
`00:17:42` 对啊谢谢谢谢谢谢哦
`00:17:45` 哦哦对对对
`00:17:47` 就是这个词对吧嗯
`00:17:48` 所以嗯在线性的tension形式中
`00:17:50` 我们真正做的是把这些KV和Q累加起来
`00:17:54` 在双形式中大致就是这样运作的
`00:17:56` 但你可能希望输出里也包含
`00:17:58` 当前时间部的一些值
`00:18:00` 那可能会像哦
`00:18:01` 我当前时间部这个token的值
`00:18:03` 对下游的各种任务其实挺有用的
`00:18:05` 所以这有点像残差连接
`00:18:06` 我当前输入的值会直接传到attention的输出
`00:18:09` Z则是另一个调值门控
`00:18:10` 你可以用它控制直通量的大小
`00:18:12` 真谢谢
`00:18:12` 很好好
`00:18:13` 所以这就是线性时间
`00:18:15` attention有很多非常有意思的东西
`00:18:19` 同样有意思的是
`00:18:20` 很多被测试过的方法
`00:18:22` 最终都呈现出这种简洁的线性
`00:18:24` attention风格的循环形式
`00:18:26` 尽管有少数例外
`00:18:27` 但大部分方案最终都收敛成了
`00:18:29` 非常类似LSTM的结构
`00:18:31` 好的嗯
`00:18:32` 你想说什么啊
`00:18:34` 对好的
`00:18:34` 所以还有另一种选择
`00:18:36` 我之前讲的是线性attention
`00:18:38` 它具有非常好的复杂度理论特性
`00:18:40` 我想讨论另一种attention优化方法
`00:18:43` 之前没有提到过
`00:18:44` 但它属于这一类重要的高效attention方法
`00:18:47` 所以deep seek u啊
`00:18:50` 在deep six three point two中
`00:18:51` 我记得引入了DSA
`00:18:52` 不是do sparse attention
`00:18:54` 而是deepsec attention
`00:18:55` 他们的做法是不去计算attention
`00:18:58` 那不算不是对所有token计算attention
`00:19:01` 你要做的是先用一个轻量级的索引器
`00:19:04` 从大量token中筛选出一个子集
`00:19:06` 你面对的是非常长的上下文
`00:19:08` 索引器会从中挑选出一个子集
`00:19:10` 这个子集远小于完整序列
`00:19:13` 接着你会对这个较小的子集做全注意力计算
`00:19:17` 机制其实很简单
`00:19:19` 至少前向传播这一步是这样的
`00:19:21` 做法是你照常生成Q和K
`00:19:24` 然后把他们送入这个索引器啊
`00:19:26` 索引器会先算Q和K的内积过一遍value
`00:19:29` 然后利用前面token的信息生成权重
`00:19:32` 最后给出结果
`00:19:33` 这样你就得到了每个位置的激活值
`00:19:36` NANOSCRATCHUTION写
`00:19:38` 可对其进行top k筛选
`00:19:40` 将其中若干个排名靠前的位置纳入注意力计算
`00:19:44` 然后我照常做注意力计算
`00:19:46` 输出键值对这样做的好处是非常非常稀疏
`00:19:50` 如果这个索引器足够轻量
`00:19:52` 那么执行全注意力的这部分
`00:19:55` 就可以在一个非常小的子集上完成
`00:19:57` Indexer doda a
`00:19:59` 我印象中的GOM还共同表明了一点
`00:20:01` 你其实不必用这个索引器去训练模型
`00:20:04` 因为用这个索引器训练本身就够了
`00:20:06` 那个索引器机制可能挺让人头疼
`00:20:09` 而且非常复杂
`00:20:10` 做法是先训练一个普通的transformer
`00:20:13` 然后再做长context扩展时
`00:20:15` 把这个闪电索引器嵌入进去
`00:20:17` so the pre预训练的扩展阶段中
`00:20:20` 训练模型去适应这个索引器
`00:20:23` 所以这么做的实际computer成本其实相当低
`00:20:27` 我想强调一下deep seek的visa point two
`00:20:30` 他们在那儿提出了DSA
`00:20:31` 这个模型相当出色啊
`00:20:33` v three point two在当时跟其他所有前沿模型表现相当
`00:20:37` 比如cloud four point5sonnet或Gemini three
`00:20:40` 他们还放了一张非常直观的图
`00:20:42` 展示了我们的预填充阶段
`00:20:44` 也就是对输入做attention以及解码阶段
`00:20:46` 也就是实际生成内容的过程
`00:20:49` 与上一代没有使用稀疏attention的
`00:20:51` deep seek模型相比
`00:20:53` 这两者的扩展性都非常有利
`00:20:55` so很合理对吧
`00:20:56` 如果你只关注极小部分
`00:20:58` 就能大幅降低attention的计算开销
`00:21:00` 我觉得g o n five是另一个很好的佐证呃
`00:21:03` 他是去年发布的
`00:21:04` 我觉得gon five算是其中之一
`00:21:07` usual性目前能拿到的最好的开源模型之一
`00:21:11` 他们也用了这种DIC方法
`00:21:13` 如果你感兴趣
`00:21:14` 他们的论文里有不错的消融实验
`00:21:16` 对比了带预热的DSA
`00:21:18` 纯DSA以及完全不用DSA的情况
`00:21:21` Donned
`00:21:21` 他们主要想说明的是
`00:21:23` 只要进行完整的DC训练
`00:21:25` 你就不必这么做嗯
`00:21:26` 性能损失很小
`00:21:27` 即便是在chang context检索
`00:21:30` 这种用RNN架构很难处理的任务上
`00:21:32` 表现也接近完整的attention
`00:21:34` 所以这提供了一种降低attention
`00:21:37` 成本的完全不同思路
`00:21:39` 注意我翻回上一页
`00:21:40` 这里不是线性的时间
`00:21:43` 索引器仍需处理完整的auto or attention
`00:21:46` 因为那些QK内积的计算成本并非线性增长
`00:21:50` 不过你可以大幅降低该索引器的成本
`00:21:52` 比如降低其维度或让W非常小
`00:21:55` 这样就能显著节省开销
`00:21:56` 是好的
`00:21:58` 这是一种完全不同的视角
`00:21:59` 而且非常有效
`00:22:00` 我不打算深入展开
`00:22:02` 但事实证明
`00:22:03` top case选择将成为接下来讲座内容的核心
`00:22:05` 在我进入make sure of experts部分之前
`00:22:07` 关于高校attention的提问就到这里
`00:22:09` 请讲
`00:22:10` 那么索引器的时间复杂度是平方级的
`00:22:13` 也就是quadratic吗
`00:22:14` 对因为它必须如此
`00:22:16` 要知道该选什么
`00:22:17` 他确实得遍历所有内容对吧
`00:22:19` 好的
`00:22:20` 这里没有什么巧妙的状态转换机制
`00:22:22` 就是纯粹的暴力内积计算
`00:22:25` 所以如果你要遍历所有内容
`00:22:27` 而之后的步骤也是二次方的
`00:22:29` 我们是怎么在时间上取得优势的呢
`00:22:32` 来这更像是一种系统层面的技巧
`00:22:35` 你会让索引器变得非常清亮
`00:22:37` 实现这一点的方法有很多
`00:22:40` 你可以用更低的精度来做
`00:22:42` 就像他们提到的那样
`00:22:43` 你也可以通过将Q和K进一步
`00:22:46` 投影到更低的维度
`00:22:47` 仅用于索引
`00:22:48` 所以这里有很多你可以想到的技巧
`00:22:49` 但索引器会变得非常清亮
`00:22:51` 所以constant factors非常小
`00:22:53` 然后对于第二点
`00:22:54` 即使是quadratic
`00:22:55` 它也是针对较短的context
`00:22:57` Like the quadratic
`00:22:58` 因为是top k我们可以控制K
`00:22:59` 所以虽然开销大
`00:23:01` 但规模小
`00:23:01` 所以这是另一件事
`00:23:02` 有时不要过于纠结
`00:23:04` 于是不是quadratic
`00:23:05` Thank you
`00:23:06` 有时候cosant factors真的非常重要
`00:23:08` 还有一个问题
`00:23:10` 好的
`00:23:10` 谢谢谢谢
`00:23:12` 没错没错
`00:23:13` 这也可以看作是一种持续预训练策略
`00:23:16` 毕竟出于computer成本和其他原因
`00:23:18` 我们不会从零开始训练长context模型
`00:23:21` 通常的做法是先训练一个叫短context的模型
`00:23:24` 然后再进行长context扩展阶段
`00:23:27` 所以这里的巧妙之处在于
`00:23:29` 既然我们无论如何都要做第二阶段
`00:23:32` 为何不在同一阶段
`00:23:33` 把长context的成本节约一并加上
`00:23:36` 不令人惊讶的是
`00:23:37` 它确实有效
`00:23:38` 坦白说那个看起来挺吓人的top k非口味模块
`00:23:41` 他们居然也能直接集成进去
`00:23:43` 但正如你在下一部分将看到的
`00:23:45` 也许你不必对top k选择如此为
`00:23:48` 那这个过程呢它也是后训练步骤的一部分吗
`00:23:51` 是的
`00:23:52` 据我所知
`00:23:53` 大多数开源模型的常规流程是
`00:23:55` 先做短context预训练
`00:23:57` 再做长context扩展
`00:23:59` 最后进行后训练
`00:24:01` 谢谢是的
`00:24:03` 这里的K的长度是否与短context相近诶
`00:24:07` DM我们会选取那些性能更接近端context的数值
`00:24:12` 并且会对其设定约束对吧
`00:24:15` 所以无论输入规模如何
`00:24:17` 他们都是受限的
`00:24:18` 对其实问题还挺多
`00:24:19` 后三位的保存
`00:24:22` Oh complete
`00:24:24` All you
`00:24:26` Okay
`00:24:26` Good
`00:24:26` Thank you
`00:24:27` That's good
`00:24:28` 好的很好
`00:24:29` 谢谢
`00:24:30` 挺好的
`00:24:30` 都训练稳定性方面的问题
`00:24:32` 问得好
`00:24:33` 我觉得不会
`00:24:33` 据我所知
`00:24:34` 没有文献记载说
`00:24:35` 线性soft max会对attention造成大量问题嗯
`00:24:39` 反过来说
`00:24:40` 由于各种原因
`00:24:41` soft max往往才是问题所在嗯
`00:24:43` 所以我认为这实际上会改善这种架构的稳定性
`00:24:48` Thank you
`00:24:49` 谢谢对
`00:24:49` 关于人类政治
`00:24:50` 关于人类政治
`00:24:52` 业界接下来想考虑的另一种attention替代方案
`00:24:55` 是需要一种更好的attention机制
`00:24:57` 因为社区成本低
`00:24:58` 空缺少
`00:24:59` 现在就算你刚才简略提到的那种有那些亮点
`00:25:03` 我是说各种特性或方向上的亮点
`00:25:05` 对对那是为了展示用的嗯
`00:25:08` 对于未来attention架构会是什么样子
`00:25:11` 我其实也没啥特别准的预测
`00:25:13` Right
`00:25:14` 我这么说可能有点儿打太极
`00:25:16` 但我认为很多情况下
`00:25:17` 我们会把这些技巧全都用上
`00:25:19` 这就好比架构通过整合各种成功的配方
`00:25:22` 变得越来越复杂
`00:25:23` 我觉得还有一个更宏观更高层面的方向
`00:25:26` 主要是利用后训练
`00:25:27` 让模型自己来管理context
`00:25:29` 像压缩检索这些东西
`00:25:31` 本质上都是在最外层加的一层
`00:25:34` 我认为很多
`00:25:35` 接下来的工作会是把这些不同层级整合起来
`00:25:39` 因为我们已经看到了
`00:25:41` 至少线性实践
`00:25:42` attention这块已经很大程度上收敛成了
`00:25:45` 类似LSTM或者线性attention的架构
`00:25:48` Thank you
`00:25:48` 短期内我觉得这不会有大变化
`00:25:50` 谢谢好的哦
`00:25:52` 对最后一个问题能聊聊低精度attention吗
`00:25:56` 我懂f p for attention可行吗
`00:25:58` 嗯我觉得index or的提出部分就是受这类问题启发
`00:26:02` but ufp for attention肯定可行
`00:26:05` 嗯我觉得缺点在于像soft max这类操作精度损失
`00:26:09` 比如微小的下翼
`00:26:11` 上翼可能会真的开始变得很关键
`00:26:13` 有时挺麻烦的
`00:26:14` 所以我觉得这种思路的动机是
`00:26:16` 先用低精度做选择
`00:26:18` 然后再做全精度计算
`00:26:20` 因为这样value vector就能更精细的相加和加权
`00:26:23` 这思路是合理的
`00:26:25` 我不确定有没有真正好的方法
`00:26:27` 能在f p store甚至更低精度下做完整的attention by
`00:26:31` 谢谢好行
`00:26:32` 可以最后一个问题
`00:26:33` 然后我就继续
`00:26:35` 谢谢
`00:26:36` 请讲跟transformer相比
`00:26:38` 新方法的缺点是什么
`00:26:39` 训练是不是比状态空间模型更复杂
`00:26:43` 对问得好
`00:26:44` 总得有点缺点吧
`00:26:45` 不然大家都用了
`00:26:45` 我觉得缺点就是表达能力的问题对吧
`00:26:48` 你知道的soft max attention里的auto all连接非常强大
`00:26:51` 它也很好训练对吧
`00:26:53` 我觉得跟LSTM比起来
`00:26:54` attention以前有个很大的优势
`00:26:55` 就是硬件效率高对吧
`00:26:57` NIKI并行训练
`00:26:58` 我觉得这些状态空间模型之所以流行
`00:27:00` 虽然他们跟LSTM很像
`00:27:02` 而且在表示能力上也有类似的短板
`00:27:05` 是因为现在有了这种清晰的二重性
`00:27:08` 得益于我提到的线性attention
`00:27:10` 这类方法
`00:27:11` 你可以从RNN形式切换到更密集的矩阵
`00:27:14` 乘法形式
`00:27:15` um2那种矩阵乘法形式带来了计算效率
`00:27:18` 所以这种权衡现在似乎已经解决了
`00:27:21` 但你还是得面对这个权衡
`00:27:22` 如果你状态有限
`00:27:23` 还得把所有信息都传过去
`00:27:27` 跟直接传递所有信息相比
`00:27:29` 你会丢失一些信息在状态空间里
`00:27:31` 抱歉我是说跟响应相比
`00:27:33` context length本身
`00:27:34` 状态空间里是不是还有更多内容
`00:27:36` 在状态空间模型里
`00:27:38` 你基本上得考虑上下文长度
`00:27:40` 相对于状态规模的比例对吧
`00:27:42` 我的意思是
`00:27:43` 如果状态规模和上下文一样大
`00:27:45` 那确实可行
`00:27:46` 但代价是计算开销会非常大
`00:27:49` 所以核心问题是
`00:27:50` 如果你想要一个极小的状态
`00:27:52` 就很难把大context里的所有信息都压缩进去
`00:27:55` 这没有免费的午餐
`00:27:57` 也许未来某天我们能做到无需权衡
`00:27:59` 但目前来看权衡点就出在这里
`00:28:02` 谢谢很好
`00:28:03` 那么现在我来讲讲mixture of experts呃
`00:28:05` 我觉得make sure experts值得展开讲一讲
`00:28:07` 从概念上讲
`00:28:08` 他们并没有真正改变格局
`00:28:10` 一种理解mixture of experts的方式
`00:28:12` 是他们只是更高效的mp对吧
`00:28:15` 也就是说你原本有一个MOP
`00:28:17` 然后不知怎么的
`00:28:18` 有人给了你一个更高效的MOP
`00:28:22` 这大致就是mixture of experts的含义
`00:28:24` 但我认为你需要理解其中的原理
`00:28:26` 有a mixture of experts之所以重要
`00:28:28` 主要有两个原因
`00:28:29` 其一就像张幻灯片所显示的
`00:28:31` 如今大家都在做mixture experts
`00:28:33` 都在部署mixture experts
`00:28:35` 如果你希望成为深入理解语言
`00:28:37` 模型及其工作原理的人
`00:28:38` 你就应该明白它是什么
`00:28:40` 对吧
`00:28:40` 这是第一个原因
`00:28:42` 另一个原因是mixture of experts
`00:28:44` 其实有一些非常有意思的特性嗯
`00:28:47` 一些机制性的组件
`00:28:48` 如果你观察mixture of experts是怎么搭建的
`00:28:51` 用了哪些基本原语
`00:28:52` 你会很快在很多地方看到类似
`00:28:53` mixture of experts的原语
`00:28:55` 然后你会意识到哇
`00:28:56` 我可以用神经网络做更多有趣的事情
`00:28:59` 所以我认为mixture of experts既有趣又酷
`00:29:02` 这是一个更宏观的理由
`00:29:03` 你应该去了解他们好
`00:29:05` 那么什么是mixture of experts
`00:29:07` 在某些方面
`00:29:08` mixture of experts是非常简单的东西
`00:29:10` 他们其实就是你mp的替代品
`00:29:12` 左边是transformer中
`00:29:14` 你通常使用的mp in attention有一些归一化
`00:29:17` 然后紧接着是一个FEEDFORWARD
`00:29:19` 这里就是大量密集信息处理发生的地方
`00:29:22` 现在换个思路
`00:29:24` 假设我把那个大的前馈网络FFN
`00:29:27` 拆成几个小的FFN
`00:29:29` 这样我就有了四个前馈网络
`00:29:31` 假设我不把它们切得更碎
`00:29:33` 而是让它们保持和原来一样大
`00:29:35` 并且通过某种机制
`00:29:37` 我有一个系统能告诉我
`00:29:38` 针对每个输入该选择哪个前馈网络
`00:29:41` 这样一来
`00:29:42` 我的参数量变成了原来的四倍
`00:29:45` 至少对于前馈网络部分是这样
`00:29:48` 虽然我有四个前馈网络的参数量
`00:29:51` 但在任何一次前向或反向传播中
`00:29:54` 我只需承担一个前馈网络的计算成本很好
`00:29:58` 所以从某种意义上说
`00:29:59` 如果你读过mixture of experts的原始论文
`00:30:01` 会发现
`00:30:02` 它最初的动机是一种以参数为中心的视角
`00:30:05` 他的出发点大概是这样
`00:30:07` 假设你只是想要更多参数
`00:30:09` 因为你相信参数越多越好
`00:30:11` 情况大致是这样
`00:30:13` 好很好
`00:30:14` 如果我们想要更多参数
`00:30:16` 又不想为更多参数付出computer的代价
`00:30:18` 那就需要类似右侧所示的这种结构
`00:30:21` 因此这就是你对mixture of experts moe
`00:30:25` 应有的心智模型
`00:30:27` 你希望在不影响foo BP的前提下增加参数A
`00:30:31` 那么他们为何如此流行
`00:30:34` 如今你在hug face等平台上获取的任何模型
`00:30:37` 只要超过一定规模
`00:30:38` 都是贸易超过一定规模后
`00:30:41` 你拿到的模型似乎都是某一原因
`00:30:44` 在于不知出于何故
`00:30:46` 如果保持总compute不变
`00:30:48` 而仅增加稀疏参数的数量
`00:30:51` 那么模型的性能通常都会变得更好
`00:30:53` 因此这或许佐证了一种观点
`00:30:56` 更多的参数总体上确实是有益的
`00:30:58` 即使同一时刻只有其中一部分处于激活状态
`00:31:01` 而在本讲座中
`00:31:03` 我将大量引用google的一些原始论文
`00:31:06` 这是FERRIS等人twenty twenty two年发表的论文
`00:31:10` 我记得里面讨论的是switch transformer
`00:31:12` 他们展示出随着专家数量的增加对吧
`00:31:15` 也就是说激活参数数量保持不变
`00:31:17` 专家数量增加
`00:31:18` 语言建模的测试损失就在持续下降
`00:31:21` 下降再下降
`00:31:21` 就是嗯如果你只在意前向传播的FLOS
`00:31:26` 那确实很棒
`00:31:27` 嗯右边展示的是随着训练加速出现的类似情况
`00:31:31` 当你在X轴上增加训练computer时
`00:31:33` 如果在相同的训练computer下
`00:31:35` 使用更多的experts
`00:31:37` 性能就会提升
`00:31:38` 所以在训练和推理阶段
`00:31:40` MOE都能让你轻松取胜
`00:31:42` 这确实很棒
`00:31:43` 非常好
`00:31:43` IM啊
`00:31:44` 这种情况其实已经被反复观察到
`00:31:47` 左边这张图和之前一样
`00:31:49` 来自fittest的论文
`00:31:51` 去年AIT育团队发表了OLMO论文
`00:31:53` 这是他们关于开源
`00:31:55` mixture of experts的分析与训练研究
`00:31:58` 他们也观察到了同样的现象
`00:32:00` 无论是看训练loss
`00:32:01` 验证loss才是下有基准测试表现
`00:32:04` 你都会发现在训练阶段
`00:32:06` 这些MOE模型相比其稠密对应模型
`00:32:09` 表现要好得多
`00:32:10` 也就是说
`00:32:11` 训练一个MOE模型比稠密模型大约快两倍
`00:32:14` Ali distancing yourself
`00:32:16` C疏性在这里带来的好处
`00:32:18` 我认为事实胜于雄辩
`00:32:20` 看看所有发布的MOE
`00:32:22` 就激活参数而言
`00:32:23` 它们比稠密模型强得多
`00:32:25` 这正是right
`00:32:26` 这很关键
`00:32:27` 毕竟你肯定得考虑推理时的浮点运算量
`00:32:30` 或者更广泛地说
`00:32:31` 训练和部署这些系统的成本对吧
`00:32:34` deep CV two和早期的deep c k m e问世时
`00:32:38` 跟当时大家都在训练的那些稠密模型相比
`00:32:41` 确实带来了巨大的转变
`00:32:43` 你会看到哇
`00:32:44` 我们的激活参数少的多
`00:32:46` 但MMOU表现却跟别人一样好
`00:32:47` 甚至更好
`00:32:48` 所以MOE领域确实发生了一些很棒的进展
`00:32:51` 很好好的希望
`00:32:53` 我已经让你相信M1是个有趣的方向
`00:32:56` 值得你花时间去思考和研究哦
`00:32:58` 对最后我想补充一点
`00:33:00` 说明为什么这很重要
`00:33:01` moo e为你提供了另一个并行化的维度啊
`00:33:04` 我想我们还有三节课会专门讲并行化和系统
`00:33:08` 但冰行化确实是LYM中非常关键的一环
`00:33:10` 但语言模型非常庞大
`00:33:12` 无论是训练还是推理
`00:33:13` 都无法装入单个设备
`00:33:15` 因此你需要多种不同的模型切分方式
`00:33:18` 以高效的服务或训练他们四啊
`00:33:21` make sure of experts踩
`00:33:22` 还有个意外的好处
`00:33:23` 每个FEEDFORWARD里的expert天然就是分块的
`00:33:27` 每个expert本身就是一个自然的块
`00:33:30` 你可以把它们放在不同的设备上
`00:33:32` 然后将激活值路由到不同的设备
`00:33:34` 我们稍后会讲
`00:33:35` Expert
`00:33:35` Parallot
`00:33:36` 指的就是这个
`00:33:36` 就为你提供了一个额外的系统优化空间
`00:33:39` 如果你想优化服务器就会非常有用
`00:33:42` 好在西方我认为已经发布过几个体量大
`00:33:45` 能力强的MOE开源模型
`00:33:48` 在大模型方面
`00:33:49` 有lam four和OpenAI推出的GPTOSS
`00:33:52` 不幸的是
`00:33:53` 西方的开源模型发布
`00:33:55` 在很大程度上已经停滞了啊
`00:33:57` 现在虽然发布的没那么多了
`00:33:58` 但这里有两个很好的例子
`00:33:59` 他们各自都是顶级模型
`00:34:01` 不过我觉得MOE研究和训练的主要进展
`00:34:05` 都发生在中国
`00:34:06` quin deep seek以及mini cp m等模型
`00:34:09` 做了一些moi的早期训练工作
`00:34:11` 并推动了它的普及
`00:34:13` 他们在某一方面继续做着非常棒的工作
`00:34:15` 比如你看看早期的QNMOE模型
`00:34:17` 也就是QNWO点5OE
`00:34:20` 他们展示了其27亿活跃参数的小型模型
`00:34:23` 表现优于当时许多7B模型
`00:34:25` 我觉得deep sea和queen的这些早期时政
`00:34:29` 真的让开源社区的其他人都确信
`00:34:32` 这是正确的方向
`00:34:33` 说到deep sick哦
`00:34:35` 有人提问吗
`00:34:35` 是的嗯
`00:34:37` 我对expert parallel有个问题
`00:34:39` 这些expert之间互相通信
`00:34:41` 难道不会形成通信瓶颈吗
`00:34:43` 哈是的嗯
`00:34:44` 从某种意义上说
`00:34:45` 你得承担传输激活值的通信开销
`00:34:48` 我就不细说了
`00:34:50` 但我会演示一个降低这一成本的技巧
`00:34:53` 不过总的来说
`00:34:53` 这其实是一种权衡
`00:34:55` 你换来了更多的聚合
`00:34:56` 浮点运算次数和更低的内存占用
`00:34:59` 但需要为通信成本买单
`00:35:01` LUCY这到底是不是笔划算的交易
`00:35:03` 很大程度上取决于你的拓扑结构
`00:35:06` 以及其他各种因素
`00:35:07` 谢谢不错哦
`00:35:08` 对这部分很关键
`00:35:10` 在训练阶段他们也是稀疏的
`00:35:12` 这正是mixture of experts难以处理的核心原因
`00:35:16` 我这么说
`00:35:16` 有点剧透了几页之后的核心思路
`00:35:18` 但如果我们在训练时激活所有expert
`00:35:21` 那就简单多了明
`00:35:22` 那我们就能看出哪个expert最适合处理
`00:35:25` 我的输入
`00:35:25` 从而学会如何进行路由分配
`00:35:27` 对mixture of expert
`00:35:29` 难点在于训练时你只能激活一个或K个expert
`00:35:32` 所以你并不知道其他那些专家发生了什么
`00:35:36` 你只知道哪些被激活了
`00:35:37` 尽管如此
`00:35:38` 你必须设法学会路由
`00:35:40` 所以这个问题带有强化学习中
`00:35:41` 多臂老虎机问题的色彩嗯
`00:35:44` 但我们既不用强化学习
`00:35:46` 也不用多臂老虎机来解决
`00:35:48` 我们要靠启发式方法和深度学习的魔力
`00:35:51` 来解决好
`00:35:52` 请讲在专家之间切换的力度是什么啊
`00:35:56` 这个力度也就是我们路由专家的尺度
`00:35:58` 就是token turin g
`00:36:00` 我不知道是不是这个意思
`00:36:01` 好对
`00:36:01` 刚才就是这个问题
`00:36:02` 是的就在token曾记
`00:36:03` 所以每个token都会分到一个专家
`00:36:05` 而路由器为了让你建立正确的心智模型
`00:36:08` 其实非常简单
`00:36:09` 一天
`00:36:10` 它们就像是在你的输入和另一端的什么东西
`00:36:13` 之间做一次矩阵乘法
`00:36:14` 所以你不会做任何复杂的事情
`00:36:16` 意思你并不知道这是不是一个医学问题
`00:36:19` 你会觉得哦这个token看起来像是日语
`00:36:21` 那就把它路由到专家期对吧对
`00:36:24` 那么问题来了
`00:36:25` 在训练或推理阶段
`00:36:27` 并行化这些mixture of experts模型有上限吗
`00:36:30` 一般来说并行化是有上限的
`00:36:33` 因为随着你在越来越多的设备上做分片
`00:36:35` 通信开销会激增对吧
`00:36:37` 你们会嗯
`00:36:38` 只要我们把作业设计得当
`00:36:40` 你们在作业里就得处理其中一些问题
`00:36:42` 我们会要求你们去设计网络拓扑之类的东西
`00:36:45` 让分片变得合理
`00:36:47` 或者反过来给定一个网络拓扑
`00:36:49` 你们得想办法如何对模型进行分片之类的啊
`00:36:52` 对很好好没错
`00:36:54` Deep seek orgin train
`00:36:55` deep seek are one等成果而为人所知
`00:36:58` 但我真正欣赏deep seek的一点是
`00:37:00` 这堂课最初的版本
`00:37:01` 本来就是打算逐篇讲解deep seek论文的
`00:37:04` 他们在架构和语言
`00:37:06` 模型研究方面确实做得非常扎实
`00:37:08` 这项工作持续了很长时间啊
`00:37:11` deep seek算是比较早入局的
`00:37:13` 他们通过研究阐明了mixture of experts的优势
`00:37:16` 并做了大量消融实验
`00:37:17` 比如探讨如果使用密集层会怎样
`00:37:20` 如果用哈希路由会怎样
`00:37:22` 如果用switch router会怎样
`00:37:23` 所有这些不同的设计决策
`00:37:25` 你都可以在他们早期的一些deep seek
`00:37:27` mixture of experts论文中看到three
`00:37:29` 如果你对这些问题感兴趣
`00:37:31` 比如架构设计之类的
`00:37:33` 我建议你去读读他们早期的deep sk论文
`00:37:36` 从这些里面能学到很多东西对吧
`00:37:38` 当然进展也没停过啊
`00:37:40` deep CV three v three point two以及GOM等等
`00:37:43` 都已经构建出了
`00:37:44` 非常出色且强大的mixture of experts模型
`00:37:47` future很明显
`00:37:48` 在可预见的未来
`00:37:50` 这就是这些大模型的发展方向
`00:37:52` 至少在未来几年
`00:37:54` 我想会是mixture of experts的天下
`00:37:56` 好那么问题来了
`00:37:58` 为什么他们之前没有更流行起来呢
`00:38:00` 嗯这体现在很多方面
`00:38:02` 我觉得mixture of experts普及的挺慢的啊
`00:38:04` 你看一些论文
`00:38:05` 比如2027年
`00:38:06` google就在研究并努力推动mixture of experts
`00:38:09` Um then say in the mixture of experts
`00:38:11` As a person
`00:38:12` Dosing chi
`00:38:12` 要到twenty
`00:38:13` twenty four year以后
`00:38:14` 为什么呢
`00:38:15` 另外如果你在做各类语言模型研究
`00:38:18` 那你大概率主要是在用稠密模型
`00:38:20` 既然mixture of experts模型这么好
`00:38:23` 为什么不用呢
`00:38:24` 我们能省下compute
`00:38:25` Mixture of experts
`00:38:27` 带来了很多复杂性训练
`00:38:29` mixture of experts也好
`00:38:30` 使用mixture of experts也好
`00:38:31` 或者要做的其他很多事都不容易诶
`00:38:34` 嗯基础设施非常复杂
`00:38:35` 我是说刚才有几个关于并行化的提问
`00:38:39` 但总体上是对的
`00:38:40` 很难以高利用率的方式来并行化experts
`00:38:44` mixture of experts的参数量很大
`00:38:46` 很难塞进单个设备里
`00:38:48` 而且训练它们时真的可能会出大问题
`00:38:50` 在后面的几页幻灯片里
`00:38:52` 你就会看到mixture of experts为什么真的很难处理
`00:38:56` 不过他们并不是容易处理的对象对吧
`00:38:58` 现在确实有一些经验法则
`00:39:00` 但依然不容易好的
`00:39:01` 我用一张幻灯片简要提一下
`00:39:03` 也有人把mixture of experts
`00:39:04` 应用到transformer的tension块里啊
`00:39:07` 已经有几篇相关论文
`00:39:09` 但这种做法并不常见
`00:39:11` 据我所见
`00:39:12` 与仅替换FEEDFORWARD或MOP层相比
`00:39:15` 这种做法并不那么容易驯服
`00:39:17` 也不那么容易让它正常工作啊
`00:39:20` 所以我只讲左边这部分
`00:39:22` 这基本上就是所有大模型的做法
`00:39:24` 在大多数情况下
`00:39:26` 人们并不会把tension heads
`00:39:27` 分片或拆分成experts啊
`00:39:29` 好的
`00:39:30` 以上就是关于mixture of experts的产业概况和现状
`00:39:34` 接下来我们谈谈如何设计一个mixture of experts
`00:39:38` 如何思考mixture of experts
`00:39:41` 为此我将介绍三种不同的变化维度
`00:39:44` 那么某一中有哪些部分可以调整
`00:39:46` 你可以修改路由函数
`00:39:48` 我们知道MOE的核心机制在于
`00:39:51` 路由器需要把token分发给各个专家
`00:39:54` 所以这部分是可以调整的
`00:39:56` 我们可以调整不同专家的规模
`00:39:58` 在给定预算下
`00:39:59` 你可以选择更多但参数较少的专家或者更少
`00:40:03` 但参数较多的专家cult
`00:40:05` 最后我们要谈谈训练
`00:40:07` 因为训练会非常棘手
`00:40:09` 一旦你真正理解某一的工作原理
`00:40:12` 你就会发现
`00:40:13` 要训练一个把token路由到不同位置的系统
`00:40:17` 并不容易诶
`00:40:19` 谢谢好
`00:40:20` 那么在所有情况下
`00:40:22` 我们都不希望激活全部专家对吧
`00:40:25` 我们只希望激活其中一部分专家
`00:40:28` 因此我们总是要选择top k
`00:40:30` 这里有不同的选择方式
`00:40:32` 你可以让token来选择专家
`00:40:34` 由expert来选择token
`00:40:36` 即每个expert挑选自己偏好的token
`00:40:39` 或者由每个token来选择自己偏好的expert
`00:40:42` 或者采用某种非常复杂的router
`00:40:44` 从全局上决定哪些token应分配给哪个expert
`00:40:48` 以实现分配的全局最优
`00:40:50` 因此基本上所有的MOE模型都采用token
`00:40:53` 选择top k的方式
`00:40:55` 因此由token来选择expert
`00:40:58` 会有K个不同的expert被选中进入候选池
`00:41:02` 你可以看一下这张图
`00:41:03` 它来自OO论文
`00:41:05` 展示了token选择与expert选择之间的差异
`00:41:08` 你会发现与expert选择相比
`00:41:10` token选择通常能获得更低的验证损失
`00:41:13` 和更高的下游基准测试分数
`00:41:16` 确实存在成功的expert选择模型
`00:41:18` 他们也能训练的很好
`00:41:19` 但token选择通常更容易跑通
`00:41:21` 也更稳定
`00:41:22` 它已经基本成为我们如今所见
`00:41:24` 所有模型的标准做法
`00:41:25` 我记得lama four系列里
`00:41:27` 有一个模型可能采用了expert选择
`00:41:29` 但我觉得这并不算是什么强有力的背书啊
`00:41:32` 替我辩解一下
`00:41:33` 我记得那是一个还没发布的lama four模型好
`00:41:37` 我们的路由routing其实有好几种做法
`00:41:39` 比如其中一种做法是用一个极小的线性投影
`00:41:43` linear projection来处理
`00:41:44` 也就是先拿到输入路由器
`00:41:46` 会计算内机
`00:41:47` 每个expert都有一个向量方向
`00:41:48` 在内积空间中最接近的那个就会被选中
`00:41:51` 迄今为止这是最常见的路由器
`00:41:54` 它被用于经典的switch transformer和g shard
`00:41:57` 这两篇是早期的google论文
`00:41:59` 它也被用于GROCKMIXTRO
`00:42:01` QUNDBRXDPC的模型对吧
`00:42:03` 你选择的K值因论文而异
`00:42:05` 但大多数情况下就是在计算内积
`00:42:07` 有一件事我一直觉得有点神秘
`00:42:09` 许多论文表明
`00:42:10` 其实你并不需要任何学习得到的路由机制
`00:42:13` 你只需对输入X做哈希
`00:42:15` 然后分发到不同的feit for网络就行
`00:42:18` 六其实这样也行得通
`00:42:19` 它有时能带来收益
`00:42:20` 虽然不如top k那么多
`00:42:22` 但基于哈希的专家路由也确实有人在用啊
`00:42:26` 这是论文里常见的基线
`00:42:28` 但出于诸多实际原因
`00:42:29` 并不会用于真实部署
`00:42:31` 嗯你可以用强化学习来学习路由策略
`00:42:34` 本质上就是一个基于内基的路由器
`00:42:37` 你可以用强化学习
`00:42:39` 把这个路由器当作一种策略来学习
`00:42:42` 就是说因为你是偏经典学习理论那类的
`00:42:45` 机器学习研究者
`00:42:46` 我觉得这应该是思考这个问题的自然方式
`00:42:49` 没想好吧
`00:42:50` 我在做什么
`00:42:51` 这有点像赌博机问题
`00:42:52` 我要从K歌里选一个
`00:42:53` 或者从N个里选K个
`00:42:55` 而且我没法观察到所有结果
`00:42:56` 这就是个赌博机问题
`00:42:57` 我会用赌博机算法或强化学习算法
`00:43:00` 一些最早期的工作里用过这种方法
`00:43:02` 因为这是思考该问题的自然方式
`00:43:05` 比如BENJO在twenty thirteen年的工作
`00:43:07` 但它并不是常见做法
`00:43:09` 我觉得它不常见的原因在于做RL所需的方法
`00:43:13` 在RL算法和随机性两方面都引入了大量开销
`00:43:17` 就F6上面
`00:43:18` 而人们发现有一套不错的启发式方法
`00:43:21` 就像一些可以套用在这个非常简单
`00:43:24` 经典的top k路由方案上的配方
`00:43:26` 基本上基本上就能让它跑起来对吧
`00:43:29` 所以没必要去做更复杂的东西啊
`00:43:31` 最后我想提一个我觉得非常酷的想法
`00:43:34` 作为一个喜欢有道理的东西的人
`00:43:35` 我很喜欢他诶
`00:43:37` 你可以求解一个线性指派问题
`00:43:38` 也就是说我在全局上计算一个分数
`00:43:41` 告诉你把这个token分给那个专家会有多合适
`00:43:44` 对吧
`00:43:44` 所以你计算出所有配对的分数
`00:43:47` 衡量这些分配有多好
`00:43:48` 然后你可以用线性指派来精确求解
`00:43:50` 从专家到token的最优分配
`00:43:53` 它被用过
`00:43:54` 也在某些情况下被证明效果不错
`00:43:56` 我觉得他完全没在大规模场景下出现过
`00:43:59` 因为相对其他方法来说
`00:44:01` 让它跑起来代价极高
`00:44:02` 好的正如刚才提到的
`00:44:04` top k路由基本上就是一种共识路由机制
`00:44:07` 它是怎么运作的
`00:44:08` 非常非常简单
`00:44:09` 如果你之前看DSA那张幻灯片时
`00:44:11` 留意了
`00:44:12` 会发现他跟DCA非常像对吧
`00:44:14` 如果你读过h nets或其他类似论文
`00:44:17` 你也会注意到这一点
`00:44:18` 重模式在很多地方都会出现
`00:44:20` 所以学会识别它很有好处
`00:44:22` 具体操作是把输入拿进来
`00:44:25` 然后让它经过一个FEEDFORWARD
`00:44:28` 然后我会算出一些分数
`00:44:30` 这些分数就是专家和我的输入U之间的soft max值
`00:44:34` S就是先分数
`00:44:36` 然后我会从中挑出得分最高的K歌
`00:44:39` 这就是控制每个专家的门控
`00:44:42` 所以这里是输入
`00:44:43` 这是残差项
`00:44:44` 这是FFN这里有门控
`00:44:46` 而门控由top k控制
`00:44:48` 这大致就是你所设想的top k路由器的公式形式
`00:44:52` 这里你可能唯一需要弄明白的细节是
`00:44:54` 怎么训练门控
`00:44:55` 门控就是通过计算每个expert的权重
`00:44:58` 与输入之间的内积来训练
`00:45:01` 得到的
`00:45:01` 计算开销极低
`00:45:02` 所以我选择门控的方式并不复杂
`00:45:05` 即便是在这个简单的机制中
`00:45:07` deep sick等团队也做了一些创新和变体啊
`00:45:11` deep c k m o e首创了这一理念
`00:45:13` 也就是如今被广泛采用的共享expert机制嗯
`00:45:16` 这个想法非常直观
`00:45:18` 在经典的top k路由器中
`00:45:20` 通常会有K个不同的抱歉
`00:45:22` 这里指的是N个不同的expert
`00:45:24` 你需要从这些expert中进行选择
`00:45:27` 但有时你会想有一些处理步骤
`00:45:29` 一些非常通用的处理
`00:45:31` 我想对所有token都应用
`00:45:33` 也许你并不希望专家每次都不同
`00:45:36` 你希望有些专家始终启用
`00:45:38` 另一些专家则按需启用always
`00:45:41` 那么deep secamv是怎么做的呢
`00:45:43` 他们沿用了最初的专家设计
`00:45:46` 把专家拆成更小更细粒度的单元
`00:45:48` 并将其中一部分细粒度专家设定为
`00:45:51` 始终启用的共享专家对吧
`00:45:53` 这些共享专家会绕过路由器
`00:45:55` 始终处理我们的输入
`00:45:56` 再把结果汇回到输出端
`00:45:58` 哦抱歉
`00:45:59` 这个想法很不错
`00:46:00` 因为事实证明
`00:46:01` 如果采用这种经典的专家设计
`00:46:03` 你其实只是在反复使用大量权重来做通用建模
`00:46:07` 因此你可以把这部分通用处理
`00:46:09` 卸载到那一个共享专家上
`00:46:11` 让其余专家更加专门化啊
`00:46:14` 正如我刚才所说
`00:46:15` DPC很出色的一点是
`00:46:17` 他们在许多论文中都做了细致的消融实验
`00:46:20` 因此他们做了一个非常漂亮的消融实验
`00:46:23` 一种配置是零共享专家
`00:46:25` 16个大型路由专家
`00:46:27` 这其实就是google就有的G12设计
`00:46:29` 与之相比的
`00:46:30` 另一种配置是专家划分得非常细粒度
`00:46:33` 并在其中加入一个共享专家
`00:46:35` 当把专家做得越来越小
`00:46:37` 并加入一个共享专家时
`00:46:38` 你会发现这两项改动都带来了明显的性能提升
`00:46:42` 在某些任务上
`00:46:43` 共享专家的效果提升尤为明显
`00:46:45` 比如trivia q a和natural questions哦
`00:46:47` 蓝黄之间的差距代表是否使用了共享专家
`00:46:50` 总的来说
`00:46:51` 增加专家数量和使用共享专家似乎都有帮助
`00:46:55` well如果你想进一步验证这些说法和观点
`00:46:58` 欧盟的研究也提供了佐证
`00:47:00` 我认为欧盟是一项在西方语境下精心控制的
`00:47:03` mixture of experts研究
`00:47:05` 他们探讨了诸如共享专家是否有效等问题
`00:47:09` 并得出结论
`00:47:10` 共享专家的作用并不显著
`00:47:11` 走关于专家数量
`00:47:13` 他们证明使用大量细粒度专家是有益的
`00:47:16` 尽管他们在共享专家的问题上略有分歧
`00:47:19` 但他们也进行了非常严谨控制良好的消融实验
`00:47:23` 许多近期的MOE模型
`00:47:25` 本质上都遵循了deep sk的设计
`00:47:27` 在此之前曾出现过几个早期的MOE模型
`00:47:31` 前三个模型我之所以反复称赞google
`00:47:34` 是因为他做了许多有趣的工作
`00:47:36` 前三个都来自google mixtro DB r x和rock
`00:47:40` 是西方早期贸易的一些尝试啊
`00:47:43` Deep seek v one
`00:47:44` 提出了细粒度专家和共享专家的设计
`00:47:47` 随后这种设计开始流行
`00:47:49` 其他人也大致跟随了deep seek的路径
`00:47:52` 我的意思是
`00:47:52` 这类似于lama设计在密集transformer中的现象
`00:47:56` 基本上成为了标准
`00:47:58` deep seek moe和deep seek v three
`00:48:00` 基本上就是大家在MOE这一侧所复制的
`00:48:03` 标准设计
`00:48:04` 如果你看point three
`00:48:05` point five g l m或任何这些现代模型
`00:48:07` 你会发现
`00:48:08` 共享专家和细粒度专家
`00:48:10` 在这些模型中都被广泛采用action
`00:48:12` 所以这是一种经过充分实战检验的设计
`00:48:15` 好有问题吗
`00:48:16` 哦好问题
`00:48:17` 如果有一个共享专家
`00:48:19` 或者有几个共享专家对吧
`00:48:21` 没有情况下并行化要怎么工作
`00:48:23` 是的
`00:48:24` 在共享专家的情况下
`00:48:26` 你不会得到任何并行化带来的节省
`00:48:28` 每次激活都必须经过那些共享专家路由
`00:48:31` 你可以把共享专家复制一份
`00:48:32` 基本上是用更多内存来换取更低的通信成本好
`00:48:37` 那这就是路由器的设计空间啊
`00:48:40` 现在我想谈谈某一的训练
`00:48:42` 这里会涉及到一些非常令人意外的
`00:48:44` 我会说是非常深度学习式的东西
`00:48:47` 我记得最初在学习MOE的时候
`00:48:49` 我以为我们根本没办法把这些东西训练好
`00:48:52` 或者说训练的合理
`00:48:53` 但事实证明
`00:48:54` 有很多技巧组合在一起之后
`00:48:56` 效果就是非常好
`00:48:58` 并且不知为何这种鲁棒性表现的很稳健
`00:49:01` 正如我们在前一个问题中会提到的对吧
`00:49:04` 我们在训练时不希望激活所有的expert对吧
`00:49:07` 因为如果那样做
`00:49:08` 我们就要为所有expert支付全部的FOP开销
`00:49:11` 而我们的expert数量非常多
`00:49:14` 所以为了训练时的效率
`00:49:16` 我们需要稀疏性
`00:49:17` 但一旦引入稀疏性
`00:49:18` 门控决策就不再可危了
`00:49:20` 而且我们也看不到
`00:49:21` 所有本可以选择的替代expert
`00:49:23` 所以这两个都是大问题嗯
`00:49:25` 解决办法有好几种
`00:49:26` 我们可以用强化学习来优化门控策略
`00:49:28` 我之前提过这一点
`00:49:29` 但并不太流行嗯
`00:49:31` 你可以采用随机扰动之类的方法
`00:49:33` 如果你熟悉探索与利用老虎机风格的那一套
`00:49:37` 可以试试其中一些方法
`00:49:39` 第三种方法是使用一大堆奇怪的启发式规则
`00:49:43` 来处理门控决策稀疏这一事实
`00:49:45` 从而平衡各个expert的分配
`00:49:47` 猜猜实践中大家用的是哪一种
`00:49:49` 我之前一直在暗示
`00:49:50` 就是第三种对吧
`00:49:52` 这是一套很有趣的启发式方法
`00:49:55` 而且最终都奏效了
`00:49:56` 我先讲讲另外两个
`00:49:58` 他们用的不多啊
`00:49:59` 我会从这里开始讲
`00:50:00` 不过我觉得知道这一点很有用
`00:50:02` 比如CLARK在托尼换那年的研究
`00:50:04` 就探讨了能不能用强化学习来训练路由器
`00:50:07` 这确实管用
`00:50:08` 你可以用强化学习
`00:50:09` 就是图上这条绿线
`00:50:11` 这是一种基线方法
`00:50:12` 有效果不错
`00:50:13` 但由于梯度方差和复杂度问题
`00:50:16` 它其实不是最优解
`00:50:17` 甚至这篇论文里提出的一些方法
`00:50:20` 也顺手用了reinforce梯度来做路由决策
`00:50:23` 现在有一种想法跟强化学习很接近
`00:50:27` 但开始更偏向启发式思路
`00:50:29` 也就是写下目标
`00:50:31` 然后去尝试
`00:50:32` 这就是随机近似的思路
`00:50:34` paper它来自最早那篇关于mixture of experts的论文
`00:50:37` 来自normal zia等人的工作
`00:50:38` 其核心在于训练过程中
`00:50:40` 你并不做硬性决策
`00:50:41` 你做的是随机决策
`00:50:43` 因此你依然保留这种路由设置
`00:50:47` 所以这个H大致就是参数和输入的内积
`00:50:50` 这是标准的路由内机
`00:50:52` 接下来我不直接用它
`00:50:53` 而是注入一种与输入规模相关的噪声
`00:50:56` 然后这就成为我的H
`00:50:58` 现在我会像之前讲top k10那样
`00:51:01` 在soft max中保留top k
`00:51:03` 随后这就作为输出进入我的top k函数
`00:51:09` 那么这是在做什么
`00:51:10` 其实就是说如果两个expert得分非常接近
`00:51:14` 几乎打平
`00:51:15` 那么你会随机选其中一个
`00:51:17` 不选另一个
`00:51:18` 在反向传播过程中
`00:51:20` 反传的方式会使有用的expert获得更高的权重
`00:51:23` 所以这种扰动能帮你打破僵局
`00:51:25` 多做一些探索
`00:51:27` 希望梯度结构能这样运作
`00:51:28` 如果一个expert有用
`00:51:29` 系统会更侧重它
`00:51:30` 让它的权重总体上越来越高
`00:51:33` 所以这是一种可行的做法
`00:51:35` 能让experts更具鲁棒性
`00:51:37` 而soft max让你能学会
`00:51:38` 如何对这K个expert进行排序
`00:51:41` 而不是只对前K个做硬性选择
`00:51:44` 你是用这些元素来加权的
`00:51:46` 然后还有另一种非常相关的方案
`00:51:48` 由FAITAS等人在twin tur2初年提出
`00:51:51` 该方案采用一种均匀的乘法扰动
`00:51:53` 他们这样做是为了强化experts啊
`00:51:56` 让它们更具鲁棒性
`00:51:58` 这种做法以前挺有用
`00:51:59` 但在后来那些做路由的google论文里
`00:52:01` 已经被去掉了哦
`00:52:02` 到底是不是非这么做不可
`00:52:04` 目前还说不清楚
`00:52:05` 后来的消融实验显示
`00:52:07` 不用这些随机鲁棒性技巧
`00:52:09` 反而有助于训练的稳定性和整体
`00:52:12` 最终训练出的MOE的质量
`00:52:14` 这说明你其实不一定非得用这些随机探索项啊
`00:52:18` 谢谢这就带出了你们实际的做法
`00:52:21` 也就是一系列启发式策略
`00:52:23` 最终能给我们一个可用的MOE
`00:52:26` 这些策略里很多本质上都是为了做平衡
`00:52:30` 那么如果你忽略这些探索与利用方面的顾虑
`00:52:33` 直接跑常规的梯度下降会出现什么问题
`00:52:36` 会发生的情况是
`00:52:37` 你会先路由到前K个experts
`00:52:39` 然后这些experts中最强的那个
`00:52:42` 会获得更多的信号
`00:52:43` 反向传播会告诉你那个expert表现不错
`00:52:46` 于是就把他的权重调高对吧
`00:52:48` 把这个参数往那边调一调
`00:52:50` 这样就会产生一种越用越强的效果
`00:52:53` 被选中的专家会获得非常大的权重
`00:52:55` thank权重大意味着他们被选中的频率更高
`00:52:59` 结果就是他们几乎包揽了所有工作
`00:53:02` 所以这种专家坍缩
`00:53:04` 或者说专家饥饿现象是一个非常现实的问题
`00:53:08` human某种意义上
`00:53:09` 这就是你在用启发式方法训练时
`00:53:12` 必须解决的核心问题
`00:53:13` 也就是MOE的启发式训练
`00:53:15` 那么该怎么做呢
`00:53:17` 做法是在总的建模损失之外
`00:53:20` 再加一项启发式损失
`00:53:22` 这项损失的作用是
`00:53:23` 让流向不同专家的token分布更加均衡
`00:53:26` 比如这边switch transformer采用的一种方法
`00:53:29` 你可以看看它具体是怎么做的
`00:53:31` 情况是这样的
`00:53:32` 你有从one到N的不同专家
`00:53:34` 还有一批token哦
`00:53:36` 你想做的是确保这两个内积尽可能小
`00:53:39` again好的
`00:53:40` 那么这里发生了什么
`00:53:41` F是被分发到任意单个expert的token比例
`00:53:45` 也就是单个expert
`00:53:47` 而PIIE则是它的一种更平滑的版本
`00:53:50` 这不再只是判断token是否被分发给某个expert
`00:53:54` 而是指路由分配给该expert的总概率质量
`00:53:58` 自己把这两项相乘嗯
`00:54:00` 至少对我来说
`00:54:01` 这不太像是能从第一性原理推导出来的东西
`00:54:04` Ject
`00:54:05` 我认为理解这个式子作用的
`00:54:07` 最简单方法是对它求导呃
`00:54:10` 所以如果你对PVOI求导
`00:54:12` 记住poi是分配给某个expert的概率质量
`00:54:16` 其导数就是被分配的token比例
`00:54:19` 也就是被分配的token比例
`00:54:21` 所以在某种意义上
`00:54:22` 你可以把它看作梯度空间中的一种惩罚
`00:54:25` 你获得的token越多
`00:54:26` 梯度就越富
`00:54:27` 所以这是在按比例压低
`00:54:29` 那些热门expert的概率质量
`00:54:31` 这就是这个损失函数的作用
`00:54:33` 你可以这样理解它
`00:54:34` 公式四中的目标函数一开始可能不太直观
`00:54:37` 但一旦你理清梯度的作用
`00:54:39` 就会明白他想做什么
`00:54:40` 也就是去推动的
`00:54:42` 所以我们可以把这些想法拿过来
`00:54:44` 基本上重构出deep sk
`00:54:46` V1和V2的mixture of expert系统
`00:54:49` 那怎么构建deep seek的mixture of experts呢
`00:54:52` 你照常计算损失
`00:54:54` 直接穿过expert做反向传播
`00:54:56` 不用管那些不可微性和探索性方面的顾虑
`00:54:59` 但你会加上和switch transformer一样的
`00:55:01` 针对每个expert的平衡机制
`00:55:03` 就是我刚才给你看的那种对吧
`00:55:05` 所以你会用到这样一个损失函数
`00:55:08` 它等于比例F乘以总概率质量P
`00:55:11` 不过deep seek的团队在系统设计上很老练
`00:55:15` 所以他们不只是平衡expert对吧
`00:55:17` four experts他们还想按设备来做平衡
`00:55:21` 比如如果你的expert被分配到不同设备上
`00:55:24` 假设我这边的机器上有四个expert
`00:55:26` 那台机器上也有四个expert
`00:55:28` 诶嗯接下来还有另外四个专家
`00:55:31` 你希望这两台机器保持平衡
`00:55:33` 让他们都以接近满负荷的状态运行对吧right
`00:55:36` 因此你不仅希望专家分布均匀
`00:55:39` 还要确保设备之间的负载均匀对吧
`00:55:42` 这是一个次要目标
`00:55:44` 其运作方式完全相同
`00:55:46` 如果你看这里的函数
`00:55:47` 它不再针对专家
`00:55:48` 而是针对每个设备的负载占比进行操作
`00:55:51` 你引入次要的设备平衡损失
`00:55:53` 仅仅是出于利用率的考虑
`00:55:55` 在啊depsv three中
`00:55:57` 他们使用了一种美专家偏执项
`00:56:00` 并采用了一种在线学习技巧来平衡专家
`00:56:03` 从而摆脱了部分辅助损失本
`00:56:06` 但归根结底
`00:56:07` 我们仍需添加一些辅助损失
`00:56:09` 以确保不会出现极端的不平衡
`00:56:12` 我把这一点作为补充说明
`00:56:13` Deep seek
`00:56:14` v three等模型已经开始摒弃
`00:56:16` 一些我认为不够优雅的辅助损失
`00:56:18` 但截至目前尚无能够完全摆脱他们的方案啊
`00:56:23` 关于负载平衡损失
`00:56:24` 我要讲的最后一点是
`00:56:25` 你可能会问我需要处理这个问题吗
`00:56:28` 这全是一对复杂的启发式规则
`00:56:31` 我真希望能直接去掉负载均衡损失啊
`00:56:34` 但如果你去掉负载均衡损失
`00:56:36` 后果相当灾难性
`00:56:37` 这些结果都来自欧盟论文
`00:56:39` 他们在其中做了一个很好的笑容实验
`00:56:41` 完全去掉了负载均衡损失
`00:56:43` 如果你保留负载均衡损失
`00:56:45` 你会得到顶部这条粉色曲线是非常正常的曲线
`00:56:48` 去掉负载均衡损失后
`00:56:50` 损失会显著上升
`00:56:51` 训练损失的表现也不好
`00:56:54` 也许更能说明问题的是
`00:56:55` 看看在有负载均衡和没有负载均衡时
`00:56:58` 哪些专家在做什么
`00:57:00` 也就是下方这些面板你会看到非常鲜明的差异
`00:57:04` 没有负载均衡时
`00:57:05` 几乎所有token都流向两个专家对吧
`00:57:08` Tokens
`00:57:09` 也就是这里看到的黄色专家和粉红色专家
`00:57:13` 而如果加上负载均衡损失
`00:57:14` 尽管我们用的是这种启发式方法
`00:57:17` 所有这些专家都被token利用起来了
`00:57:19` 所以我们实现了这种均匀的利用率对吧
`00:57:22` 可以看到负载均衡损失
`00:57:23` 确实起到了我们预期的作用
`00:57:25` 如果没有它
`00:57:26` 我们就浪费了大量参数
`00:57:28` 在大部分训练过程中
`00:57:29` 那些专家模块完全没有发挥任何作用
`00:57:32` 谢谢所以这大致就是mixture of experts的训练方式
`00:57:36` 嗯我想说回这一点
`00:57:38` 我觉得这相当令人意外
`00:57:39` 因为你有一个本质上不可微的操作
`00:57:41` 你做的是这种top k选择
`00:57:43` 还有这一项说实话是一个相当复杂的对象
`00:57:45` 嗯你真正需要做的只是加上这个均衡损失
`00:57:48` 让各个专家模块的负载均匀一些
`00:57:51` 然后其余部分你就当做梯度
`00:57:53` 可以直接在系统中传递模型
`00:57:55` 就能训练得非常好啊
`00:57:57` 我觉得其中很重要的一点在于这种动态机制
`00:58:00` 如果一个专家模块有用
`00:58:01` 你就会强化它
`00:58:02` 这是一种正向强化循环
`00:58:04` 而通过均衡负载这一循环又得到了很好的平衡
`00:58:07` cool这两种动态机制互相抵消了
`00:58:12` 挺好say哦
`00:58:13` 最后我要说的是
`00:58:14` 同样的技巧也被用在了DC里的哦
`00:58:17` 我们之前已经看过top k了
`00:58:18` 如果你了解h nets的话
`00:58:20` 就知道他也用在那种结构里
`00:58:21` 波西斯试图去掉token izer的常识
`00:58:23` 所以我觉得以后你会更常看到这种技巧
`00:58:26` 用top k选择
`00:58:27` 再配合负载均衡或其他辅助损失来处理
`00:58:30` 不可微的问题
`00:58:31` 这会成为未来架构设计的一个常规要素
`00:58:33` 旋转的mixture max空间
`00:58:35` 看起来像是一种系统层面的优化
`00:58:37` 不过你能看看他们吗
`00:58:39` 他们在不同领域里实际上是在做同样的事情吗
`00:58:43` 是的
`00:58:43` 有些论文可视化了哪些token被哪些专家激活
`00:58:47` 你都而且因为路由器非常简单啊
`00:58:49` 这些专家并不是那种聪明的专家
`00:58:52` 他们并不是什么医学专家
`00:58:54` 法律专家之类的
`00:58:55` 你确实会看到某些token会被路由到不同的方向
`00:58:59` 比如标点符号可能会被路由到某个专家
`00:59:02` 其他符号则可能被路由到另一个专家背景
`00:59:05` 英语字符机可能会被陆游到另一个专家
`00:59:08` 但这并不是那种你一看就能说哦
`00:59:10` 这是华尔街日报专家之类的情况对吧
`00:59:12` Nice
`00:59:12` 完全不是那样
`00:59:14` 这里不涉及语义
`00:59:15` 也许可以这么说
`00:59:16` 对对对
`00:59:18` 原则上
`00:59:19` 如果per expert loss被严格执行
`00:59:21` 它也会强制实现per device平衡
`00:59:24` 前提是你的专家是均匀划分的对吧
`00:59:26` 但我认为至少我的理解是
`00:59:29` 你不希望把per expert loss调得过高
`00:59:31` 以致真正达到完全均匀
`00:59:33` 因为那会对训练动态产生不利影响
`00:59:36` 但PERADVICE平衡足够重要
`00:59:37` 所以你愿意增加一点额外的损失
`00:59:39` 来鼓励实现PERADVICE平衡
`00:59:41` 而不是其他方面的平衡
`00:59:42` 谢谢好
`00:59:44` 那么在结束之前
`00:59:45` 我再稍微谈谈系统层面的内容训练
`00:59:48` Mixture of experts
`00:59:49` 会引入一些额外的有趣的系统层面的动态
`00:59:53` 当我谈到PARALLEM时
`00:59:54` 主要指的是标准的
`00:59:56` 我们姑且称之为并行的几种形式啊
`00:59:59` 你可以把数据切分成很多小块儿
`01:00:01` 或者说你可以把数据分散到很多小块上
`01:00:03` 数据集把它分散到多台机器上
`01:00:06` 你可以做模型并行
`01:00:07` 即把模型拆分并分布到不同的机器上
`01:00:10` 嗯你可以用各种方式把这两者结合起来
`01:00:13` 当然可以这么做啊
`01:00:14` 但我想我们意识到的是
`01:00:15` 每种并行技术都有某种极限对吧
`01:00:18` 数据并行
`01:00:19` 你知道你的批次大小已经达到上限对吧
`01:00:22` 一旦达到批次大小上限
`01:00:23` 你就不能再通过数据进行并行了
`01:00:25` 对于模型大小存在一些天然的切分点
`01:00:28` 你可以在这些点切分模型一旦用尽这些切分点
`01:00:31` 你就无法再并行了
`01:00:33` 如果把它和专家并行结合起来
`01:00:35` 你就多出一个很好的并行维度啊
`01:00:37` 当你这样做时
`01:00:38` 许多底层实现
`01:00:39` 允许你利用已内置于GPU中的稀疏矩阵乘法嗯
`01:00:43` 所以我要提到的另一件事是
`01:00:45` 如果你有多个专家
`01:00:47` 比如说在一个GPU上
`01:00:48` 一种思考方式是我将进行多个小型矩阵乘法
`01:00:52` 但这不太好
`01:00:53` 因为理想状态下你更希望进行大规模矩阵乘法
`01:00:57` 这样就能啊复用缓存并做各种优化
`01:01:01` 所以更好的做法是利用稀疏性啊
`01:01:04` 块对角矩阵当然只是最基础的形式
`01:01:06` 但你其实可以用更复杂的结构化系数形式
`01:01:09` 这些形式硬件原生支持
`01:01:10` 能让你高效地完成乘法计算
`01:01:12` 以非常清晰极快的方式处理experts和inputs
`01:01:16` 所以这也是MOE好的另一个原因
`01:01:19` 想想MOE的计算模式
`01:01:21` 它们几乎对应于那些在硬件中很容易很高效
`01:01:24` 就能支持的结构化矩阵乘法
`01:01:26` 这个feature moe的发展
`01:01:28` 出现了一种硬件架构协同设计的趋势
`01:01:31` 关于并行性
`01:01:32` 我要提的一点是
`01:01:33` 这是最近才有的新发展啊
`01:01:35` 这来自neutron three
`01:01:36` 我不记得是谁问的这个问题了
`01:01:38` 当我们进行专家并行时
`01:01:40` 必须在设备之间传输激活值
`01:01:42` 已告知你属于那个专家
`01:01:44` 需要把你传过去
`01:01:45` 这会导致显著的通信开销
`01:01:47` 因此人们开始考虑的一种方案是
`01:01:50` 共享专家无需通信
`01:01:52` 可以保持较大的维度
`01:01:53` 而需要通信激活值的专家
`01:01:55` 我希望其使用较小的更低维的向量对吧
`01:01:59` 因此你可以先将残差流进行降维投影
`01:02:03` 然后再执行发送该激活值的集体通信调用
`01:02:06` 这将显著节省通信开销
`01:02:09` 而无需完全承受隐藏维度尺寸较小的缺点
`01:02:13` 因此你实际上可以通过这类投影技巧
`01:02:16` 很好的控制通信开销
`01:02:18` 与MOE并行化之间的权衡
`01:02:21` 谢谢好的
`01:02:22` 最后一个关于MOE的趣闻
`01:02:24` 姑且称之为细节
`01:02:26` 我觉得很有意思
`01:02:27` 不过近年来已经解决了
`01:02:28` 那就是随机性
`01:02:29` 如果你朴素的实现MOE基础设施
`01:02:32` 你会发现
`01:02:33` 除非专家在输入分布上完美平衡
`01:02:35` 否则某些专家可能比其他专家更受青睐
`01:02:38` 如果某个专家
`01:02:40` 比如这里的专家零特别受欢迎
`01:02:42` 你就会遇到该专家对应的token队列
`01:02:44` 不断堆积的情况
`01:02:45` 最终你会觉得队列太长了
`01:02:47` 不得不开始丢弃一些token
`01:02:49` 也就是在早期的MOE推理代码和基础设施里
`01:02:53` 常见做法是直接静默丢弃该专家
`01:02:55` 然后照常继续计算
`01:02:57` 假装无事发生
`01:02:58` 做犯规零假装
`01:02:59` 这没问题
`01:03:00` 这就导致了一些奇怪的随机情况
`01:03:03` 如果其他用户的查询
`01:03:05` 正好命中了你正在用的专家
`01:03:07` 那么你的结果可能反而会变差
`01:03:09` 因为他们可能会把你从专家队列里挤出去
`01:03:12` 这种非常奇怪的事情确实会发生
`01:03:16` 但现在无丢弃架构已经解决了大部分这类问题
`01:03:20` 所以就不再是问题了
`01:03:21` 像mega bloks和其他主流的开源NEOE框架
`01:03:24` 都不再有这个问题
`01:03:25` 好的好
`01:03:27` 关于MOV设计的最后一部分
`01:03:29` 我要谈谈稳定性问题啊
`01:03:31` 我们在上一讲中已经讨论过
`01:03:33` 架构设计中的稳定性问题
`01:03:35` 如果你还记得我当时说过的指数运算是危险的
`01:03:38` 除法也是危险的
`01:03:39` 这意味着soft max是稳定性问题的危险区域
`01:03:42` 那么我们在make sure of experts中做了什么
`01:03:45` 我们在路由机制中又引入了一个soft max对吧
`01:03:48` 因为我们要做top k选择
`01:03:49` 要做soft max
`01:03:50` 这就是我们把token路由到专家的方式
`01:03:53` Um redicant to added that as
`01:03:55` i'm serial experts中的soft max操作
`01:03:58` 潜在地非常非常危险
`01:03:59` bear is soft等人在google mixture of experts啊
`01:04:02` 设计的早期阶段就已经有所涉及
`01:04:04` 发表了一篇专门讨论mixture of experts
`01:04:08` 稳定性的论文
`01:04:09` 这就是他们注意到的问题之一
`01:04:11` 我会讲一些细节
`01:04:12` 在那些更敏感更棘手的部分
`01:04:14` 你会经常看到这个解决方案
`01:04:16` 你可能最终只使用类似float
`01:04:18` thirty two的精度来处理expert router
`01:04:20` 此外这里可能还会有ZLOS
`01:04:23` 我们之前讨论过z loss
`01:04:24` 目的是控制我们观察到的soft max稳定性问题
`01:04:27` 我认为z loss其实相当流行
`01:04:30` 早期就被用来提升mv router的稳定性
`01:04:33` 我一直在深入讨论的OO
`01:04:35` 已经对添加或移除z loss做过消融实验
`01:04:38` 从这些波动剧烈的训练损失曲线
`01:04:40` 可以清楚地看出
`01:04:41` 在router上使用z loss相当有帮助到M1
`01:04:45` 还存在其他一些问题
`01:04:47` 如果你最近使用过语言模型
`01:04:48` 你可能已经体会到了
`01:04:50` MOE微调起来相当令人头疼
`01:04:53` 他们参数数量众多
`01:04:54` 因此如果你尝试微调experts
`01:04:57` 实际上会出现非常严重的过拟合问题
`01:04:59` OS因此你可以看到对于密集模型
`01:05:02` 训练集和验证集的损失相当接近
`01:05:05` 它们不会出现严重的过拟合
`01:05:07` 不过如果你看看稀疏模型
`01:05:09` 在微调这些模型用于下游任务时
`01:05:12` 训练集和验证集之间的差距会非常大
`01:05:15` 我想的是GEOUE基准测试里的一个任务
`01:05:19` 现在你也可以选择只微调
`01:05:22` 非mixture of experts的FEEDFORWARD层啊
`01:05:27` 你也可以指微调
`01:05:28` Attention
`01:05:28` 这些是比较常见的干预方式
`01:05:30` 尤其是attention微调
`01:05:32` 我最近在很多make sure of experts的研究中
`01:05:35` 经常看到those
`01:05:36` 如果你的模型里有非mixture of experts层
`01:05:39` 那当然也可以指微调那些层
`01:05:40` beer is soft等人曾支持这种做法
`01:05:43` 当然这也有苦涩
`01:05:44` 教训师的版本
`01:05:45` 就是也许你应该直接用140万个样本
`01:05:48` 而不是你手头现有的数据量
`01:05:50` 如果你有足够多的数据
`01:05:52` 你可以在微调阶段几乎完全重新训练
`01:05:55` Mixture of experts
`01:05:57` 这样泛化差距就没那么大了
`01:05:59` 好我要提到的最后一个mixture of experts
`01:06:02` 专属技巧是升级复用UPCYCLING这个概念
`01:06:06` 实际上这个方法在过去一年里没那么流行了
`01:06:09` 我觉得今年我都没见过经过升级复用的模型
`01:06:12` 但这确实是个很酷的想法
`01:06:13` 而且早期有一些非常优秀的模型
`01:06:16` 也使用了这种技巧哦
`01:06:18` 所以我想提一下这一点
`01:06:20` 所谓升级复用up cycling
`01:06:22` 其理念是
`01:06:23` 如果你想构建一个mixture of experts
`01:06:26` 或许可以直接采用一个已训练好的稠密模型
`01:06:29` 并基于该稠密模型实例化一个mixture of experts
`01:06:33` 其做法是直接复制所有参数
`01:06:35` 包括MLP
`01:06:36` 你只需生成大量这些MLP的副本
`01:06:39` 你还需要一个路由器
`01:06:40` 其参数为随机初始化
`01:06:42` 随后你只需训练这个模型
`01:06:45` 由于输入分配到不同mp的过程具有随机性
`01:06:48` 你会得到开始具备专门化能力的专家
`01:06:51` 也不会得让嗯
`01:06:53` 最终你实际上会得到一个mixture of experts
`01:06:55` 在一些最早提出升级复用方法的论文中
`01:06:58` 他们表明
`01:06:58` 升级复用方法在此处
`01:07:00` 能显著提升语言建模的精度和性能
`01:07:03` 相较于继续训练同一模型
`01:07:06` 对稠密模型进行升级复用
`01:07:08` 能带来更好的表现
`01:07:09` 这确实是一个值得展示的效果
`01:07:11` BA人们已在多种场景下
`01:07:14` 通过大规模实验验证了这一方法
`01:07:16` 所以mini cp m是另一个我很喜欢的中文模型
`01:07:19` 因为他们做了很多精心控制的消融实验
`01:07:22` 他们有一个mixture of expert模型
`01:07:24` 把mini cp m two point four b模型
`01:07:26` UPCYCLING成了hundred and thirty four
`01:07:28` E参数的模型
`01:07:29` 结果得到了一堆几乎白捡的性能提升
`01:07:32` 还有queen他们在做quan mixture of experts时
`01:07:35` 也是从one点8B的QUAN模型初始化开始的
`01:07:38` 他们把他UPCYCLING成了quin one point five a
`01:07:42` two point seven b模型
`01:07:43` 最后做出来的是首批高性能模型之一
`01:07:46` 我觉得这是较早的大规模UPCYCLING
`01:07:50` 成功案例之一
`01:07:51` 不过现在我觉得没人真这么干了
`01:07:53` 因为大家不会先训一个稠密模型
`01:07:55` 再转过来喂
`01:07:56` 你不如一开始就直接拿make sure of experts
`01:07:58` 来跑你的大型主力训练
`01:08:00` 所以现在没那么常见了
`01:08:02` 但我想提一下
`01:08:03` 因为我觉得在mixture of experts模型的操作空间里
`01:08:06` 这是个重要的知识点
`01:08:08` 好
`01:08:09` 最后我想带大家过一下deep seek的v one
`01:08:12` VA和V3模型
`01:08:13` 我觉得deep seek模型的演进过程很有参考价值
`01:08:16` 另外如果你对mixture of experts或架构设计感兴趣
`01:08:20` 我建议你读读deep sick的论文
`01:08:22` 我觉得这些论文写的很好
`01:08:24` 里面有很多值得深究的细节
`01:08:26` 那么先看deep seek
`01:08:27` mixture of experts架构的v one
`01:08:29` 它实际上已经是许多现代
`01:08:31` mixture of experts的原型了
`01:08:33` 它采用了共享专家和细粒度专家的结构
`01:08:36` 使用标准的TOK路由
`01:08:38` 并配合辅助损失来做平衡
`01:08:41` 怎么说呢
`01:08:42` 在某种程度上可以说
`01:08:43` 这就是mixture of experts模型的标准范式
`01:08:46` 或者说那种理想化的原型
`01:08:48` Deep seek mixture of experts v two
`01:08:50` 基本上就是把这个模型放大对吧
`01:08:52` 所以你有两个共享专家嗯
`01:08:55` 然后是更多的细粒度专家
`01:08:57` 还有一个设备路由组件
`01:09:00` 以及一个通信平衡组件啊
`01:09:02` 这两者本质上都是通过引入辅助损失
`01:09:05` 来优化系统
`01:09:06` Ams
`01:09:07` 我认为重要的是要认识到
`01:09:09` 成功的语言模型训练不仅关乎深度学习
`01:09:12` 也关乎真正重视系统本身
`01:09:15` 因此deepc v two在我看来正是这一理念的体现
`01:09:18` 而在deepc gamo EV three中
`01:09:21` 他们保留了共享专家和细粒度专家的设计
`01:09:25` 但在平衡机制上采用了不同的方法啊
`01:09:28` 他们采用了无需辅助损失
`01:09:30` OLOS的方式来平衡不同的专家
`01:09:32` 此外他们转而使用SIGMOID加soft max的组合
`01:09:36` 这是一种不同的专家加权方式
`01:09:38` 但本质上非常相似
`01:09:39` B最后既然我们已经谈到这里
`01:09:42` 不妨继续梳理deep seek moe vc的其余部分
`01:09:45` 他们还有许多其他有趣的设计
`01:09:48` 我非常欣赏他们思考系统的方式
`01:09:50` 比他们采用了multi headlight and attention
`01:09:52` 即不直接进行QKV操作
`01:09:55` 而是将其表示为这种低维潜在表示
`01:09:58` 所以你现在有了隐藏输入
`01:10:01` 接下来你不再直接生成QK和V
`01:10:04` 而是先首先产生C
`01:10:06` 然后你将基于C来生成QK和V
`01:10:09` 这能让你获得相当可观的计算节省哎
`01:10:12` 我不需要对所有的K和V都做KB缓存
`01:10:15` 我只需存储这些C对吧
`01:10:17` 而且C的维度应该更低对吧
`01:10:20` 这就是它叫MLA潜在激活的原因
`01:10:22` 因为这些C是潜在的
`01:10:23` 正是你需要保存的KV缓存量
`01:10:27` 你可能唯一需要担心的复杂性是在做KB缓存时
`01:10:31` 这可能会和rope冲突
`01:10:33` 所以你得稍微小心一点
`01:10:35` 注意如何旋转不同的维度等等啊
`01:10:37` 这里的技巧是你有一些非潜在维度
`01:10:40` 它们用于编码位置信息
`01:10:41` 不过关于这一点我就不深入展开了
`01:10:44` 最后我想提一个对mixture of experts来说
`01:10:47` 很酷的想法
`01:10:48` 我之前叫他deep seek m o EV three
`01:10:50` 虽然他并没有怎么流行起来
`01:10:51` 那就是MTP
`01:10:52` 也就是多token预测
`01:10:54` 与其只预测下一个token
`01:10:56` 不如一次性预测多个
`01:10:58` 这种做法在统计上是有道理的
`01:11:01` 为什么
`01:11:01` 这是个好主意
`01:11:02` 也许它能让你对未来预测的更准
`01:11:05` 另外从系统角度看也很棒
`01:11:08` 你现在相当于内置了一个投机解码器
`01:11:10` Percy
`01:11:11` 后面讲推理的时候会细说
`01:11:12` 这是一种可以用来加速模型解码的技巧
`01:11:16` 好时间差不多了
`01:11:17` 总结一下
`01:11:18` mixture of experts是一种非常巧妙的思路
`01:11:21` 它利用稀疏性
`01:11:22` 让你拥有的参数
`01:11:23` 比你实际付出的computer成本所对应的要多对吧
`01:11:26` 而emit
`01:11:27` 所以你不必为所有参数的computer成本买单
`01:11:31` 却能享受到这些参数带来的好处啊
`01:11:33` 你一开始可能觉得这种路由问题很难
`01:11:36` 但事实证明
`01:11:36` 即使在大规模下
`01:11:38` 非常简单的方法也很管用
`01:11:39` 到这里
`01:11:40` mixture of experts显然已是大势所趋
`01:11:43` 所以有必要搞清楚他们是如何运作的
`01:11:45` 究竟是什么